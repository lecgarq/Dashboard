# Technical Debt Audit — LECG Dashboard

> Generated by Antigravity on 2026-04-15 — Nuclear-Deep Exhaustive Audit
> Last verified: 2026-04-16 — Cross-referenced with codebase
> Covers: Security, Folder Structure, Dead Code, Frontend Bugs, Type Safety, Database Queries, Deprecated Packages, Deployment, and Architecture Modernization

---

## Table of Contents

1. [🚨 CRITICAL: Security Exposure](#1-critical-security-exposure)
2. [📦 Dependency Health Matrix](#2-dependency-health-matrix)
3. [🗂️ Folder & Naming Structure](#3-folder--naming-structure)
4. [🗑️ Dead Code & Unused Files](#4-dead-code--unused-files)
5. [🔁 Code Duplication](#5-code-duplication-sim--clash)
6. [🐛 Frontend Bugs & Unreliable Practices](#6-frontend-bugs--unreliable-practices)
7. [🛡️ Type Safety Violations](#7-type-safety-violations)
8. [📊 Database Query Optimization](#8-database-query-optimization)
9. [🪵 Logging Hygiene](#9-logging-hygiene)
10. [🧱 Oversized Components](#10-oversized-components)
11. [🚀 Railway Deployment Reliability](#11-railway-deployment-reliability)
12. [🏗️ Architecture Modernization Roadmap](#12-architecture-modernization-roadmap)
13. [📋 Priority Dashboard](#13-priority-dashboard)

---

## 1. CRITICAL: Security Exposure

### `JSOON.JSON` — Production Secrets in Repository

| Variable | Value Present | Risk |
| --- | --- | --- |
| `DATABASE_URL` | ✅ Full Supabase connection string | Database takeover |
| `GOOGLE_SERVICE_ACCOUNT_KEY` | ✅ Full private key PEM | Google Workspace impersonation |
| `OPENAI_API_KEY` | ✅ `sk-proj-...` | Billing theft |
| `GOOGLE_CLIENT_SECRET` | ✅ `GOCSPX-...` | OAuth hijack |
| `APS_CLIENT_SECRET` | ✅ Full secret | Autodesk account abuse |
| `UPLOADTHING_SECRET` | ✅ `sk_live_...` | File CDN abuse |
| `TRELLO_TOKEN` | ✅ Full token | Board read/write |
| `AUTH_SECRET` | ✅ JWT signing key | Session forgery |

**Status**: Not in `.gitignore`. Not tracked by git currently (confirmed with `git ls-files`), but one accidental `git add .` would push all secrets to remote.

**Fix**:

```bash
echo "JSOON.JSON" >> .gitignore
```

---

## 2. Dependency Health Matrix

### 🔴 Outdated / Legacy Packages (Requires Action)

| Package | Current | Latest | Status | Action Required |
| --- | --- | --- | --- | --- |
| `next-auth` | `5.0.0-beta.30` | `5.0.0-beta.30` (still beta) | ⚠️ **Perpetual beta** — no stable v5 exists | Monitor; v5 is the recommended path, but the "beta" tag carries risk. Consider pinning exact version. |
| `@auth/prisma-adapter` | `^1.6.0` | `2.11.1` | 🔴 **Major behind** | Upgrade to `^2.11.1` — includes security patches and NextAuth v5 compatibility improvements |
| `class-variance-authority` | `^0.7.1` | — (succeeded by `cva`) | 🔴 **Legacy package** | Migrate to `cva` v1.x — `class-variance-authority` is a dead-end |
| `zod` | `^4.3.6` | `^4.x` (latest) | ⚠️ **Breaking from v3** | Audit all schemas for deprecated methods: `.strict()`, `.passthrough()`, `.merge()`, `.email()` on string |
| `@prisma/client` | `^5.22.0` | `^6.x` | ⚠️ **Major behind** | Prisma 6 has improved query engine; evaluate upgrade path |
| `lucide-react` | `^0.575.0` | `1.x` | ⚠️ **Pre-1.0** | Upgrade to stable `^1.x` when available |

### 🟡 Dev-Only Dependencies in Production Context

| Package | Status | Action |
| --- | --- | --- |
| `@ngrok/ngrok` | ✅ In `dependencies` (not `devDependencies`) | **Move to `devDependencies`** — ngrok is a dev tunnel, not a production requirement. Shipping it to Railway bloats the bundle. |

### 🟢 Healthy Packages (No Action Required)

| Package | Version | Status |
| --- | --- | --- |
| `next` | `^15.5.12` | ✅ Current |
| `react` / `react-dom` | `19.2.3` | ✅ Current |
| `@trpc/*` | `^11.0.0` | ✅ Current |
| `framer-motion` | `^12.35.0` | ✅ Current |
| `googleapis` | `^171.4.0` | ✅ Current |
| `@tiptap/*` | `^3.20.x` | ✅ Current |
| `@dnd-kit/*` | `^6.x/10.x` | ✅ Active |
| `date-fns` | `^4.1.0` | ✅ Current |
| `yjs` / `y-websocket` | `^13.6.29` / `^3.0.0` | ✅ Current |
| `tailwindcss` | `^4` | ✅ Current |
| `openai` | `^6.25.0` | ✅ Current |

---

## 3. Folder & Naming Structure

### Current Structure Issues

| Issue | Location | Severity | Description |
| --- | --- | --- | --- |
| **`lib/` is a flat dumping ground** | `lib/` (26+ files) | 🟠 HIGH | Mixes server-only modules (`google-chat.ts`), client-only code (`particle-zones.tsx`), shared types (`categories.ts`), and event buses (`clash-events.ts`). No subdirectory organization. |
| **`server/actions/` is empty** | `server/actions/` | 🟢 LOW | Ghost directory — delete it |
| **`tmp/` is massive** | `tmp/` (40+ files, 11 dirs) | 🟠 HIGH | Contains old CDP browser profiles, HTML dumps, debug logs, cookie files, `db_dump.json`. Not cleaned up. Even though gitignored, adds noise locally. |
| **Inconsistent component casing** | `components/` | 🟡 MED | Domain folders use `kebab-case` (`clash/`, `trello/`) but component files use `PascalCase` (`ChatPanel.tsx`). This is acceptable for Next.js but should be codified. |
| **Page files contain full implementations** | `app/(dashboard)/settings/users/page.tsx` (34KB) | 🟠 HIGH | Full admin panel in a single page file. Should be a thin shell importing components. |
| **`JSOON.JSON` — misspelled** | Root | 🔴 CRIT | Typo of "JSON" — rename to `env.local.json` or delete entirely |

### Recommended `lib/` Reorganization

```
lib/
├── client/                    ← "use client" only
│   ├── oauth-connect.ts
│   ├── particle-zones.tsx
│   └── chunk-load-error.ts
├── server/                    ← "server-only" (already exists)
│   ├── integrations/          ← External service wrappers
│   │   ├── google-chat.ts
│   │   ├── google-calendar.ts
│   │   ├── google-directory.ts
│   │   ├── google-forms.ts
│   │   ├── google-drive.ts
│   │   ├── sheets.ts
│   │   ├── trello.ts
│   │   ├── aps.ts
│   │   └── ai.ts
│   ├── email/
│   │   └── email.ts
│   ├── google-service-auth.ts
│   ├── integration-errors.ts
│   └── logger.ts
├── events/                    ← All 4 singleton event buses
│   ├── clash-events.ts
│   ├── sim-events.ts
│   ├── trello-events.ts
│   └── user-events.ts
├── shared/                    ← Isomorphic / shared code
│   ├── categories.ts
│   ├── holidays.ts
│   ├── wiki-sections.ts
│   ├── wiki-utils.ts
│   ├── google-oauth.ts
│   └── utils.ts
├── trpc.ts                    ← Client-side tRPC setup
└── providers.tsx              ← Context providers
```

---

## 4. Dead Code & Unused Files

### Dead Dependencies

| Package | Evidence | Action |
| --- | --- | --- |
| **`resend`** | NOT in `package.json` (already removed) but `RESEND_API_KEY` and `RESEND_EMAIL` env vars still present in `JSOON.JSON` | Remove env vars |
| **`@ngrok/ngrok`** | Zero imports across entire codebase. Only in `package.json` dependencies. | Move to `devDependencies` or remove |

### Dead Environment Variables

| Variable | Status | Reason |
| --- | --- | --- |
| `RESEND_API_KEY` | 🔴 Dead | Email moved to Gmail OAuth |
| `RESEND_EMAIL` | 🔴 Dead | Email moved to Gmail OAuth |
| `NEXTAUTH_SECRET` | 🟡 Duplicate | Same value as `AUTH_SECRET` — NextAuth v5 only needs `AUTH_SECRET` |
| `NEXTAUTH_URL` | 🟡 Duplicate | Same value as `AUTH_URL` — NextAuth v5 only needs `AUTH_URL` |

### Potentially Unused Files

| File | Lines | Evidence | Action |
| --- | --- | --- | --- |
| `lib/particle-zones.tsx` | 67 | Client context for particle animations — verify if consumed by any component tree | Audit usage; delete if orphaned |
| `lib/holidays.ts` | 28 | **Hardcoded to 2026** — will silently stop working Jan 1 2027 | Make dynamic or extend years |
| `lib/chunk-load-error.ts` | 14 | Used by `error.tsx` ✅ | **Keep** — confirmed active |
| `app/shadcn-tailwind.css` | ~50 | Separate Tailwind config file | Merge into `globals.css` |

---

## 5. Code Duplication (Sim ↔ Clash)

**~2,000 lines** of functionally identical code:

| Layer | Clash File | Sim File | Size Each |
| --- | --- | --- | --- |
| tRPC Router | `server/routers/clash.ts` (10KB) | `server/routers/sim.ts` (10.6KB) | ~300 lines |
| Event Bus | `lib/clash-events.ts` | `lib/sim-events.ts` | 83 lines |
| Page | `app/(dashboard)/clash-detection/page.tsx` (17KB) | `app/(dashboard)/sim-automation/page.tsx` (17KB) | ~500 lines |
| SSE Endpoint | `app/api/clash-updates/` | `app/api/sim-updates/` | ~100 lines |
| Prisma Models | `ClashWiki`, `ClashTask` | `SimWiki`, `SimTask` | Schema-level |

**Impact**: Every bug fix or feature must be applied in 2 places. Every QA pass must be run twice. This is the #1 maintainability risk in the codebase.

**Fix**: Generic `BaseModule` factory pattern (see previous document).

---

## 6. Frontend Bugs & Unreliable Practices

### 🔴 Critical Frontend Issues

| Issue | File | Lines | Description |
| --- | --- | --- | --- |
| **`useEffect` missing deps** | `ChatPanel.tsx` | L326-334 | Keyboard handler depends on `onClose`, `hasPrev`, `hasNext`, `currentIndex`, `allPreviewable` but dep array only contains `[attachment.name, hasPrev, hasNext]` — stale closure for `onClose` and `onNavigate` |
| **`useEffect` missing deps** | `ChatPanel.tsx` | L490-497 | Escape handler depends on `close` function but dep array is `[open]` — `close` captures stale `setView` reference |
| **`initViewer` not in deps** | `BimViewer.tsx` | L65-75 | `useEffect` calls `initViewer()` but it's a regular function, not in the dependency array. The function captures `urn` and `tokenData` from closure — stale data risk |
| **`window.Autodesk` declared as `any`** | `BimViewer.tsx` | L14 | Global `Window.Autodesk = any` — no type safety for APS Viewer SDK |
| **Silent error swallowing** | `DualAuthGuard.tsx` | L26 | `catch {}` with empty block — errors vanish entirely |
| **Silent error swallowing** | `WikiEditor.tsx` | L141, L249 | `catch {}` with empty blocks — Yjs connection failures are invisible to the user |
| **`window.prompt()` usage** | `WikiEditor.tsx` | L675 | Uses `window.prompt('URL')` for link input — ugly, blocks the main thread, and is not mobile-friendly |

### 🟠 Unreliable Patterns

| Issue | Files | Description |
| --- | --- | --- |
| **No error boundaries beyond root** | Only `app/(dashboard)/error.tsx` exists | Subpage crashes (e.g., in `ChatPanel`) bubble up to the dashboard-level boundary, unmounting the entire page. Individual panels/modals should have their own boundaries. |
| **SSE auto-reload loop risk** | `error.tsx` L28-38 | On chunk load error, auto-reloads with a 15s debounce. But if the deployment has changed chunks, it will hard-loop for 15 seconds before showing the error. |
| **`suppressHydrationWarning` on `<body>`** | `app/layout.tsx` L37 | Suppresses hydration mismatches globally on `<body>` — masks real hydration bugs instead of fixing them. The `<html>` suppression (L34) is standard for `next-themes`, but `<body>` is suspicious. |
| **Hardcoded admin email** | `server/auth.ts` L138 | `"luis.cortes@hermosillo.com"` is hardcoded as a secondary admin alias. Should be an env var. |
| **`createPortal` without cleanup guard** | `ChatPanel.tsx` L468 | Media preview modal uses `createPortal(modal, document.body)` — if the component unmounts mid-animation, the portal may leak |
| **No `loading` state for APS Viewer** | `BimViewer.tsx` L80 | Uses wildcard `7.*` version for APS Viewer CDN script — could break on a minor Autodesk CDN update |

### 🟡 Minor Frontend Issues

| Issue | File | Description |
| --- | --- | --- |
| **`window.location.href` used unsafely** | `Sidebar.tsx`, `ChatPanel.tsx`, `EventModal.tsx`, `RegistrationForm.tsx`, `DualAuthGuard.tsx` | Direct `window` access. Most are protected by `"use client"` but there's no SSR guard wrapper. If Next.js ever server-renders these, they'll crash. |
| **Magic numbers in notifications** | `use-chat-notifications.ts` | Oscillator frequencies (880, 440), gain values (0.45, 0.001), timing (0.01, 0.4, 0.5, 250ms) are all magic numbers — should be named constants |
| **Popover positioning assumes viewport** | `MonthView.tsx` L155-158 | Uses `window.innerHeight/innerWidth` for popover clamping — doesn't account for scroll containers |
| **No `Suspense` boundaries** | Multiple pages | tRPC queries don't use `<Suspense>` — causes full page flicker on navigation instead of streaming |

---

## 7. Type Safety Violations

> Audit correction (2026-04-16): the original server-side `as any` findings in `server/trpc.ts` and `server/auth.ts` were resolved in Session 2. Current type-safety debt is now concentrated in external SDK boundaries and large client components such as `lib/aps.ts`, `lib/google-calendar.ts`, `lib/google-chat.ts`, `lib/google-directory.ts`, `components/clash/WikiEditor.tsx`, `components/families/BimViewer.tsx`, and `app/(dashboard)/settings/users/page.tsx`.

### `as any` Casts (Server-Side)

| File | Line | Cast | Risk |
| --- | --- | --- | --- |
| `server/trpc.ts` | 46 | `(ctx.session.user as any).role` | Role access is untyped at the procedure level |
| `server/trpc.ts` | 52 | `(ctx.session.user as any).role` | Same issue — role should be in proper type |
| `server/auth.ts` | 121 | `} as any` | Credentials authorize return — should be properly typed |
| `server/auth.ts` | 129 | `PrismaAdapter(db) as any` | Known NextAuth v5 beta adapter type mismatch |
| `server/auth.ts` | 187 | `account as any` | Used to access `access_token` etc. — should use proper Account type |
| `server/auth.ts` | 264-267 | 4× `as any` casts | Session user augmentation — `next-auth.d.ts` types exist but aren't flowing through |

**Root Cause**: The `next-auth.d.ts` type declarations exist but aren't fully connected to the auth callbacks. The session `user` type doesn't include `role`, `providers`, `hasCredentials`, `moduleAccess` in the callback signatures.

**Fix**: Properly extend `AdapterUser` and `Session["user"]` so the callbacks receive typed objects.

---

## 8. Database Query Optimization

### Missing Indexes

| Model | Column | Query Location | Impact |
| --- | --- | --- | --- |
| `Family` | `phase` | `kpi.ts` (5 queries) | Full table scan on every dashboard load |
| `Family` | `dueDate` | `kpi.ts` (deadline queries) | Sequential scan for future dates |
| `Family` | `updatedAt` | `kpi.ts` (recent families) | Sort without index |
| `ClashTask` | `status` | `kpi.ts` (3 queries) | Full table scan |
| `ClashTask` | `dueDate` | `kpi.ts` (deadline queries) | Sequential scan |
| `SimTask` | `status` | `kpi.ts` (via sim router) | Full table scan |

### Fix: Add to `schema.prisma`

```prisma
model Family {
  @@index([phase])
  @@index([dueDate])
  @@index([updatedAt])
}

model ClashTask {
  @@index([status])
  @@index([dueDate])
  @@index([updatedAt])
}

model SimTask {
  @@index([status])
  @@index([dueDate])
}
```

Audit correction (2026-04-16): `User.username` is already backed by the unique btree index `User_username_key` created by the initial migration because `username` is declared as `@unique` in `prisma/schema.prisma`. Adding a second plain `@@index([username])` would be redundant.

### 17-Query KPI Blast

`kpi.getHomeDashboard` fires **17 parallel Prisma queries** on EVERY homepage load. Even though they run in parallel, this creates a burst of 17 connection checkouts from the Supabase pool.

**Impact**: Under concurrent load, this can exhaust the 15-connection Supabase pool limit.

**Fix**: Consolidate into 2-3 raw SQL CTEs:

```sql
WITH counts AS (
  SELECT
    (SELECT COUNT(*) FROM "Family") AS family_total,
    (SELECT COUNT(*) FROM "Family" WHERE phase = 'DONE') AS family_done,
    (SELECT COUNT(*) FROM "ClashTask") AS clash_total,
    (SELECT COUNT(*) FROM "ClashTask" WHERE status = 'DONE') AS clash_done,
    (SELECT COUNT(*) FROM "User") AS user_count
)
SELECT * FROM counts;
```

### Raw SQL Inventory (auth.ts + users.ts fixed)

`server/auth.ts` and `server/routers/users.ts` no longer use raw SQL for user/account/admin flows. Remaining raw SQL is limited to performance-specific paths: 6 call sites across 2 files.

| File | Area | Current Use | Notes |
| --- | --- | --- | --- |
| `server/routers/kpi.ts` | Dashboard counts | One `$queryRaw` CTE | Intentional performance consolidation: 10 count queries -> 1 CTE |
| `server/routers/lod.ts` | Semantic search + cache maintenance | `$queryRawUnsafe` / `$executeRawUnsafe` | Intentional; Prisma cannot express pgvector distance (`<=>`) or related cache cleanup cleanly |

---

## 9. Logging Hygiene

Server-side structured logging cleanup is complete in the originally audited hotspots. After the 2026-04-16 client logger pass, only **6 raw `console.*` calls** remain in runtime code outside the centralized logger shims.

| Area | Count | Examples |
| --- | --- | --- |
| `components/clash/WikiEditor.tsx` | 6 | WebSocket / paste / drop diagnostics in the still-dirty editor surface |

`lib/server/logger.ts` and `lib/client-logger.ts` intentionally write to `console.*` and are not counted as debt.

**Fix direction**: finish the `WikiEditor.tsx` cleanup once that file is no longer in the active dirty set, or route the remaining diagnostics through `clientLogger`.

```typescript
// Before (auth.ts L77)
console.log(`[AUTH DEBUG] Autodesk Raw Profile:`, JSON.stringify(profile));

// After
const logger = createLogger("auth");
logger.debug("Autodesk profile received", { profile });
```

---

## 10. Oversized Components

| File | Size | Lines Est. | Decomposition Targets |
| --- | --- | --- | --- |
| `components/dashboard/ChatPanel.tsx` | **49 KB** | ~1,485 | `MessageAttachments`, `MediaPreviewModal`, `SpaceListView`, `ConversationView`, `DrawerHeader`, `ChatAvatar` — these are already internal functions but should be **separate files** |
| `components/trello/CardDialog.tsx` | **41 KB** | ~1,200 | `CardChecklist`, `CardAttachments`, `CardComments`, `CardHeader` |
| `app/(dashboard)/settings/users/page.tsx` | **34 KB** | ~1,000 | Extract to `components/admin/UserManagement.tsx`, `PendingRequests.tsx`, `RoleEditor.tsx` |
| `app/(dashboard)/users/UsersDirectoryClient.tsx` | **32 KB** | ~900 | `DirectoryGrid`, `PersonCard`, `FreeBusyIndicator`, `SearchBar` |
| `components/dashboard/calendar/EventModal.tsx` | **31 KB** | ~900 | `EventForm`, `RoomPicker`, `AttendeeList`, `TimeSelector` |
| `components/clash/WikiEditor.tsx` | **28 KB** | ~800 | `EditorToolbar`, `SectionTabs`, `ImageUploadHandler` — has **9 `useEffect` hooks** (extreme) |
| `components/families/FamilyDetailPanel.tsx` | **27 KB** | ~800 | `FamilyHeader`, `ChangelogList`, `DeliverableGrid`, `AiDescriptionPanel` |
| `components/layout/Sidebar.tsx` | **22 KB** | ~600 | `SidebarNav`, `ProviderLinks`, `ModuleAccessGuard` |

**Rule of Thumb**: Any component > 300 lines should be split. Any component with > 3 `useEffect` hooks needs architectural review.

---

## 11. Railway Deployment Reliability

### Current Gaps

| Gap | Risk | Fix |
| --- | --- | --- |
| **No health check endpoint** | Railway can't verify app is serving | Add `GET /api/health` |
| **No `railway.toml`** | Relies on Nixpacks auto-detection | Add explicit build/deploy config |
| **No `postinstall` script** | `prisma generate` may not run | Add `"postinstall": "prisma generate"` |
| **No engine lock** | Node version drift | Add `"engines": { "node": ">=20" }` |
| **`@ngrok/ngrok` in prod deps** | Bloats Railway build by ~15MB | Move to `devDependencies` |
| **No build caching** | Every deploy reinstalls from scratch | Configure Railway deploy cache |
| **Double env vars** | `AUTH_SECRET`/`NEXTAUTH_SECRET` duplication | Remove `NEXTAUTH_SECRET` and `NEXTAUTH_URL` |

### Recommended `railway.toml`

```toml
[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
```

### Recommended Health Check

```typescript
// app/api/health/route.ts
export function GET() {
  return Response.json({
    status: "ok",
    timestamp: new Date().toISOString(),
    node: process.version,
  });
}
```

### Recommended `package.json` Additions

```json
{
  "engines": { "node": ">=20" },
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

---

## 0. Debt Resolution Progress

> Last updated: 2026-04-16 — Session 7 client logging + typecheck sweep verified

### ✅ Completed (Session 1)

| Item | What was done |
| --- | --- |
| **Security: JSOON.JSON** | Added to `.gitignore` with `*.secrets.json` wildcard |
| **ngrok → devDeps** | Moved `@ngrok/ngrok` to `devDependencies` in `package.json` |
| **postinstall** | Added `"postinstall": "prisma generate"` script |
| **railway.toml health check** | Added `healthcheckPath`, `healthcheckTimeout`, `prisma generate` in buildCommand |
| **Health endpoint** | Created `app/api/health/route.ts` |
| **Hardcoded admin alias** | Replaced `"luis.cortes@hermosillo.com"` with `process.env.ADMIN_EMAIL_ALIAS` |
| **Prisma indexes** | Added `@@index` on `Family(phase,dueDate,updatedAt)`, `ClashTask(status,dueDate,updatedAt)`, `SimTask(status,dueDate)` |
| **useEffect stale closures** | Fixed `ChatPanel.tsx` keyboard handler deps + `close` wrapped in `useCallback`. Fixed `BimViewer.tsx` by moving `initViewer` inside effect + `scriptLoaded` state. |
| **Auth debug logs** | Replaced all 7 `console.log/error` `[AUTH DEBUG]` calls with structured `authLogger` |
| **Raw SQL in auth.ts** | Replaced `$executeRaw UPDATE/INSERT` with `db.user.update/create`; replaced `$queryRaw moduleAccess` with `db.userModuleAccess.findMany()` |
| **KPI 17-query blast** | Consolidated 10 count queries into 1 SQL CTE → 8 total connections instead of 17 |
| **@auth/prisma-adapter** | Bumped `^1.6.0` → `^2.11.1` in `package.json` |

### ✅ Actions Completed After Session 1

1. **`npm install`** — done
2. **`npx prisma migrate dev --name add-indexes`** — done, migration `20260415234208_add_indexes` applied
3. **`ADMIN_EMAIL_ALIAS`** — added to Railway env panel

> Correction (2026-04-16): the Session 5 audit note above was a false positive. `User.username` already has the unique btree index `User_username_key` from the initial migration because the field is declared as `@unique`.

---

## ✅ Completed (Sessions 3–4: LOD Checker Integration + Phase 3.5)

| Item | What was done |
| --- | --- |
| **LOD Checker — native integration** | Replaced iframe with native Next.js page. Built `LodSearchBar`, `LodStatsBar`, `LodDetailPanel` components. tRPC router `server/routers/lod.ts` with pgvector search, stats, CRUD, upload. |
| **pgvector + IVFFlat index** | `vector(768)` column + IVFFlat index applied to Supabase via migration `20260416011500_fix_lod_search_foundation`. |
| **Data migration** | 24,619 families migrated via `scripts/migrate-lod-data.cjs` at ~41/sec. 23,965 pgvector embeddings populated. |
| **Phase 5: remove iframe env var** | `NEXT_PUBLIC_LOD_CHECKER_URL` removed from `.env`. Iframe fully dead. |
| **Phase 3.5: APS folder search** | `server/routers/aps-search.ts` — pLimit(3) BFS traversal, 60s timeout via Promise.race, 1h `ApsProjectSearchCache`, `ApsRevitModel` type with full `folderPath`. |
| **LodGraphCanvas** | Canvas-based UMAP scatter plot (23,965 nodes), color by category (stable hash → HSL), zoom/pan/click → LodDetailPanel. Added Search/Graph tab toggle to LOD Checker page. |

---

## ✅ Completed (Session 2)

| Item | What was done |
| --- | --- |
| **console.* → structured logger** | Replaced the originally audited server-side raw console calls in `auth.ts`, `users.ts`, `sheets.ts`, `trello.ts`, `families.ts`, `clash.ts`, `sim.ts`, and `aps.ts`. Client/runtime diagnostics still remain in hooks, `WikiEditor`, `FamilyDetailPanel`, and error boundaries. |
| **as any casts — trpc.ts** | Removed `as any` on `ctx.session.user.role` — types now flow from `next-auth.d.ts` |
| **as any casts — auth.ts** | Removed `as any` on credentials return, account upsert fields, and session callback; replaced adapter cast with `@ts-expect-error` comment |
| **holidays.ts** | Rewrote to compute Mexican public holidays dynamically for any year using Monday-shift rules; no longer hardcoded to 2026 |
| **CVA migration** | Investigated — `class-variance-authority` is still at 0.7.1, npm `cva` package is squatted by an unrelated party; no actionable migration exists yet. Marked N/A. |

---

## ✅ Completed (Session 6)

| Item | What was done |
| --- | --- |
| **`users.ts` raw SQL migration** | Replaced legacy raw SQL in `server/routers/users.ts` with Prisma model operations for directory/profile reads, `PendingRequest`, `PasswordResetToken`, `UserModuleAccess`, and avatar updates. Removed the manual raw-SQL row helper types from the router. |
| **Verification** | `npx tsc --noEmit --pretty false` passed. `rg -n '\$queryRaw|\$executeRaw|\$queryRawUnsafe|\$executeRawUnsafe' server/routers/users.ts` returned no matches. |

---

## ✅ Completed (Session 7)

| Item | What was done |
| --- | --- |
| **Client logger consolidation** | Added `lib/client-logger.ts` and migrated `use-event-source.ts`, `use-chat-pulse.ts`, `FamilyDetailPanel.tsx`, `WikiEditorBoundary.tsx`, and `app/(dashboard)/error.tsx` off direct raw `console.*` calls. |
| **Typecheck blocker cleanup** | Fixed repo-wide `tsc` blockers in `app/api/gmail/attachment/route.ts`, `app/api/wiki-media/[id]/route.ts`, `lib/email.ts`, `lib/server/google-service-auth.ts`, and `lib/sound-engine.ts`. |
| **Verification** | `npx tsc --noEmit --pretty false` passed. `rg -n "console\\.(log|warn|error|info|debug)" app components hooks lib --glob '!**/*.md'` now shows only 6 client calls in `WikiEditor.tsx` plus the intentional centralized logger sinks. |

---

## 12. Architecture Modernization Roadmap

### Tier 1: Immediate (< 1 day)

| Item | Effort | Impact | Status |
| --- | --- | --- | --- |
| Add `JSOON.JSON` to `.gitignore` | 1 min | 🔴 Security | ✅ Done |
| Remove dead env vars (`RESEND_*`, `NEXTAUTH_*` dupes) | 10 min | 🟡 Hygiene | ⬜ TODO — remove from Railway env panel (local .env cleaned) |
| Move `@ngrok/ngrok` to devDependencies | 1 min | 🟢 Build size | ✅ Done (run `npm install`) |
| Delete `server/actions/` empty dir | 1 min | 🟢 Hygiene | ✅ Already gone |
| Add `railway.toml` + health check | 30 min | 🟠 Reliability | ✅ Done |
| Add `postinstall` + `engines` to `package.json` | 5 min | 🟡 Reliability | ✅ Done |

### Tier 2: Next Sprint (1-3 days)

| Item | Effort | Impact | Status |
| --- | --- | --- | --- |
| Upgrade `@auth/prisma-adapter` to `^2.11.1` | 2h | 🟠 Security | ✅ Done (run `npm install`) |
| Migrate `class-variance-authority` → `cva` v1 | 4h | 🟡 Modernize | ⬜ TODO — next session |
| Ensure hot-path Prisma/DB indexes are present | 1h | 🟠 Performance | ✅ Done — `Family`, `ClashTask`, `SimTask` were added in `20260415234208_add_indexes`; `User.username` is already covered by `User_username_key` from the initial migration. |
| Fix `useEffect` dependency bugs in `ChatPanel` + `BimViewer` | 2h | 🔴 Bugs | ✅ Done |
| Replace `catch {}` silent blocks with error logging | 1h | 🟠 Reliability | ✅ Done (auth.ts) |
| Replace hardcoded secondary admin email with env var | 10 min | 🟡 Security | ✅ Done (add `ADMIN_EMAIL_ALIAS` env var on Railway) |
| Consolidate KPI to 2-3 SQL queries | 4h | 🟠 Performance | ✅ Done (10 counts → 1 CTE) |

### Tier 3: Backlog (1-2 weeks)

| Item | Effort | Impact | Status |
| --- | --- | --- | --- |
| Abstract Sim/Clash into `BaseModule` pattern | 2-3 days | 🟠 Maintainability | ⬜ TODO |
| Decompose 8 oversized components | 2-3 days | 🟡 Maintainability | ⬜ TODO |
| Migrate remaining `console.*` to structured logger | 3h | 🟡 Observability | ⚠️ Partial — only 6 client/runtime console calls remain, all isolated to `WikiEditor.tsx`. |
| Fix all `as any` casts in auth system | 4h | 🟡 Type safety | ✅ Done — trpc.ts, auth.ts session/credentials/adapter casts resolved |
| Reorganize `lib/` folder structure | 2h | 🟡 Organization | ⬜ TODO — see recommended structure in §3 |
| Replace `window.prompt()` with proper modal | 1h | 🟡 UX | ⬜ TODO — `WikiEditor.tsx` L675 |
| Make `holidays.ts` dynamic (multi-year) | 1h | 🟢 Correctness | ✅ Done — now computes any year using Monday-shift rules |
| Add `Suspense` boundaries for streaming | 4h | 🟡 UX | ⬜ TODO |
| Add per-panel error boundaries | 2h | 🟡 Reliability | ⬜ TODO |
| Replace raw SQL in `auth.ts` with Prisma methods | 1h | 🟢 Type safety | ✅ Done — `$executeRaw` replaced with `db.user.update/create`; `$queryRaw` replaced with `db.userModuleAccess.findMany()` |
| Replace legacy raw SQL in `users.ts` with Prisma methods | 2h | 🟢 Type safety | ✅ Done — `PendingRequest`, `PasswordResetToken`, `UserModuleAccess`, directory/profile/avatar flows migrated to Prisma on 2026-04-16 |
| Migrate `class-variance-authority` → `cva` v1 | 4h | 🟡 Modernize | ❌ N/A — npm `cva` is squatted; CVA v1 not yet released |
| `User.username` index audit | 30min | 🟠 Performance | ✅ Resolved — `username @unique` already created `User_username_key`; duplicate `@@index` not needed. |

---

## 13. Priority Dashboard

| # | Category | Items | Severity | Est. Effort |
| --- | --- | --- | --- | --- |
| 1 | 🔴 **Security** | JSOON.JSON, hardcoded admin email | CRITICAL | 15 min |
| 2 | 🔴 **Frontend Bugs** | useEffect stale closures, silent catches | CRITICAL | 3 hours |
| 3 | 🟠 **Outdated Deps** | @auth/prisma-adapter, CVA | HIGH | 6 hours |
| 4 | 🟠 **Database** | Remaining intentional raw SQL in `kpi.ts` and `lod.ts` | HIGH | 3 hours |
| 5 | 🟠 **Deployment** | railway.toml, health check, engines | HIGH | 1 hour |
| 6 | 🟠 **Duplication** | Sim/Clash clone (~2,000 lines) | HIGH | 2-3 days |
| 7 | 🟡 **Oversized Files** | 8 components > 20KB | MEDIUM | 2-3 days |
| 8 | 🟡 **Logging** | 6 client/runtime `console.*` calls in `WikiEditor.tsx` | MEDIUM | 1 hour |
| 9 | 🟡 **Type Safety** | External SDK and large-component `any` usage | MEDIUM | 4 hours |
| 10 | 🟡 **Dead Code** | Env vars, empty dirs, ngrok | MEDIUM | 30 min |
| 11 | 🟡 **Folder Structure** | `lib/` reorganization | MEDIUM | 2 hours |
| 12 | 🟢 **Minor UX** | window.prompt, Suspense | LOW | 5 hours |

### Total Estimated Effort: ~8-10 working days

### Historical Immediate Win (< 30 min, completed in Sessions 1-5):

```bash
# 1. Gitignore secrets
echo "JSOON.JSON" >> .gitignore

# 2. Move ngrok to dev deps
npm install --save-dev @ngrok/ngrok && npm uninstall @ngrok/ngrok

# 3. Delete empty directory
rmdir server/actions

# 4. Add health check
# (create app/api/health/route.ts)
```
