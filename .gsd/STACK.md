# Technology Stack - LECG Dashboard

> Auto-generated by Antigravity on 2026-04-15 (Master Phase)

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Next.js    | 15.5.12 | Core Framework (App Router) |
| Node.js    | ^20.x   | Runtime Environment |
| React      | 19.2.3  | UI Library |
| TypeScript | 5.9.3   | Static Typing |

## Dependencies

### Core Production

| Package | Version | Purpose |
|---------|---------|---------|
| `@trpc/server`   | ^11.0.0       | Type-safe API |
| `@prisma/client`  | ^5.22.0       | Database Client |
| `next-auth`      | ^5.0.0-beta.30| Authentication |
| `yjs`            | ^13.6.29      | CRDT Collaboration |
| `@tiptap/react`  | ^3.20.0       | Rich Text Editor |
| `framer-motion`  | ^12.35.0      | Animations |
| `zod`            | ^4.3.6        | Validation |

### External SDKs

| Package | Version | Purpose |
|---------|---------|---------|
| `@aps_sdk/*` | ^1.x       | Autodesk Platform Services |
| `googleapis`  | ^171.4.0   | Google Cloud / Workspace |
| `uploadthing` | ^7.7.4     | File Hosting |
| `openai`      | ^6.25.0    | AI Integrations |

### Styling & UI

| Package | Version | Purpose |
|---------|---------|---------|
| `tailwindcss`  | ^4        | Utility-first CSS |
| `lucide-react` | ^0.575.0  | Icon Library |
| `radix-ui`    | ^1.4.3    | Headless UI Components |
| `shadcn`      | ^3.8.5    | Component System |

## Operational Patterns Inventory

| Pattern | Implementation | Purpose |
|---------|----------------|---------|
| **Context-Singleton**      | `server/trpc.ts` | Process-wide Project/ID residency |
| **Tiered Protection**     | tRPC Middleware  | Role-based router isolation |
| **Event-Driven Signaling** | `EventEmitter`    | Reactive service orchestration |
| **Single-Flight Cache**    | Native Promises  | Concurrent API call deduplication |
| **Global Click Intercept** | React Hook       | Deterministic UI loading state |

## Infrastructure & Services Matrix

The application orchestrates several interconnected services to provide a unified BIM dashboard experience.

| Service | Technology | Internal Port | Purpose |
|---------|------------|---------------|---------|
| **Dashboard**    | Next.js    | 3000          | Main Web Interface & tRPC API |
| **Yjs Server**   | Node.js    | 4444          | CRDT Sync & Prisma Persistence |
| **LOD Backend**  | Flask      | 8080          | LOD Analytics Processing |
| **LOD Frontend** | Vite       | 5173          | LOD Visualization Portal |
| **Database**     | PostgreSQL | 5432          | Unified Relational Storage |

## DevOps & Environment Logic

### Environment Patching

The system uses `scripts/patch-env.js` to automatically detect the host's local IP address and propagate it to the `.env` settings. This ensures that the Yjs server and internal service interlinks function correctly across tunneling services and local Wi-Fi networks.

### Multi-Service Orchestration

The primary entry point for development is `scripts/run_dev_stack.py`. This Python orchestrator:

1. Validates PostgreSQL status.
2. Clears the `.next` cache to avoid stale static generation issues.
3. Automatically frees required ports by killing conflicting PIDs.
4. Concurrently launches the Dashboard, Yjs Server, and LOD Checker with prefixed logging.

## Data & Derived Metrics

The KPI system (`kpiRouter`) derives high-level business intelligence from modular relational data.

| Metric | Derivation |
|--------|------------|
| **Capacity** | (Active Tasks / (Active Users * 8h)) |
| **Velocity** | Ratio of Phase:DONE items over Month period |
| **QA Score** | Integrated Clash Detection status (Review/Done) |

## Configuration

The application uses environment variables defined in `.env`. Key variables include:

- `DATABASE_URL`: Connection string for PostgreSQL.
- `NEXTAUTH_SECRET`: Secret key for session encryption.
- `APS_CLIENT_ID` / `APS_CLIENT_SECRET`: Autodesk credentials.
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`: Google OAuth credentials.

## Outdated Packages Summary

The project is currently using several beta versions (e.g., `next-auth`) which should be monitored for stable releases.

- `@tanstack/react-query`: Update to latest v5 patch is recommended.
- `framer-motion`: Rapidly evolving, check for v12.x stability.
