# Technology Stack - LECG Dashboard

> Auto-generated by Antigravity on 2026-04-15 (Super Deep Phase)

## Local Runtime

| Technology | Version | Purpose                     |
|------------|---------|-----------------------------|
| Next.js    | 15.5.12 | Core Framework (App Router) |
| Node.js    | ^20.x   | Runtime Environment         |
| React      | 19.2.3  | UI Library                  |
| TypeScript | 5.9.3   | Static Typing               |

## Dependencies (Dashboard Core)

### Production

| Package          | Version        | Purpose            |
|------------------|----------------|--------------------|
| `@trpc/server`   | ^11.0.0        | Type-safe API      |
| `@prisma/client` | ^5.22.0        | Database Client    |
| `next-auth`      | ^5.0.0-beta.30 | Authentication     |
| `yjs`            | ^13.6.29       | CRDT Collaboration |
| `@tiptap/react`  | ^3.20.0        | Rich Text Editor   |
| `framer-motion`  | ^12.35.0       | Animations         |
| `zod`            | ^4.3.6         | Validation         |

### External SDKs (Integration)

| Package       | Version   | Purpose                     |
|---------------|-----------|-----------------------------|
| `@aps_sdk/*`  | ^1.x      | Autodesk Platform Services  |
| `googleapis`   | ^171.4.0  | Google Cloud / Workspace    |
| `uploadthing` | ^7.7.4    | File Hosting                |
| `openai`      | ^6.25.0   | AI Integrations             |

## Low-Level Implementation Patterns

| Pattern                  | Implementation           | Purpose                                     |
|--------------------------|--------------------------|---------------------------------------------|
| **Single-Flight**        | `Map<string, Promise<T>>`| Concurrent API call deduplication           |
| **Event Stream (SSE)**   | `ReadableStream`          | Pull-based real-time "Push" emulation      |
| **Sequential Batching**  | `batch.map()` + `await`  | API quota hit avoidance (Chat/Directory)    |
| **Fuzzy Key Scraper**    | `COST_CENTER_KEYS`        | Extraction of un-mapped Google metadata     |
| **Buffered Invalidation**| `useChatPulse` + `Set`   | Client-side burst event handling            |

## Service Matrix & Connectivity

| Service        | Technology | Internal Port | Protocol   | Purpose                     |
|----------------|------------|---------------|------------|-----------------------------|
| **Dashboard**  | Next.js    | 3000          | HTTP / SSE | Web Interface & API Hub     |
| **Yjs Server** | Node.js    | 4444          | WebSocket  | CRDT Collaboration Sync     |
| **Database**   | PostgreSQL | 5432          | SQL        | Relational & Binary Storage |

## Internal Performance Configuration

The application implements specific tuning for streaming and high-frequency polling:

- **Streaming Headers**: Uses `X-Accel-Buffering: no`, `no-cache`, and `no-transform` to prevent proxy buffering of the SSE stream.
- **Heartbeating**: 20-second `: keep-alive` comments for the browser's persistent connection.
- **Polling Interval**: Fixed 5-second tick for the Google Chat SSE poll.
- **Batch Size**: Restricted to 5 (Directory) or 3 (Chat) concurrent items to manage API throughput.

## Database & Authentication Schema

- **IDs**: Using `cuid()` for cross-platform uniqueness and deterministic string ordering.
- **JWT Augmentation**: Custom `role` and `moduleAccess` claims enable server-side validation without database lookups on every request.
- **Bytes Storage**: Binary Yjs state stored as `Bytea` (PostgreSQL) to support sub-millisecond CRDT merges.

## Outdated Packages

The project is currently using several beta versions (e.g., `next-auth`) which should be monitored for stable releases.

- `@tanstack/react-query`: Update to latest v5 patch is recommended.
- `framer-motion`: Rapidly evolving, check for v12.x stability.
