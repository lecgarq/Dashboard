---
phase: 10
plan: 1
wave: 1
---

# Plan 10.1: Native UploadThing S3 Orphan Deletion Hooks

## Objective
Fix the cascading storage bloat. Currently `server/routers/tasks.ts` natively invokes `ctx.db.userTask.delete()`. While the Postgres row vanishes, the linked AWS S3 asset controlled by UploadThing stays fully intact. This plan writes the post-operation middleware routing an unmapped `utapi.deleteFiles` loop solving the technical constraint.

## Context
- .gsd/TECHNICAL_DEBT_MATRIX.md
- src file: `server/routers/tasks.ts`
- src API: `uploadthing/api` 

## Tasks

<task type="auto">
  <name>Extract Active Asset URLs Before Cascade</name>
  <files>c:\LECG\Dashboard\server\routers\tasks.ts</files>
  <action>
    - Inject an `await ctx.db.taskAttachment.findMany({ where: { taskId: input.id }})` directly into `deleteTask` prior to calling the final deletion logic.
    - Map the returned struct extracting merely the `url` payloads into a raw string array.
  </action>
  <verify>Syntax validation mapping local TypeScript dependencies.</verify>
  <done>Raw array strings are accurately staged prior to database deletion block without halting main router flow.</done>
</task>

<task type="auto">
  <name>Execute UTAPI Deletion Handlers</name>
  <files>c:\LECG\Dashboard\server\routers\tasks.ts</files>
  <action>
    - Import `{ UTApi } from "uploadthing/server"`.
    - Extract the native `file_key` securely parsing the Next.js URL structures natively (splitting string patterns after `/f/`).
    - Resolve `await utapi.deleteFiles(fileKeys)` seamlessly bridging the mutation closure logic.
    - Wrap in `try/catch` resolving generic warning logs (using `logger.ts`) so UploadThing 500 API errors don't revert successful Prisma row deletions.
  </action>
  <verify>Run the `knip` command locally ensuring the library imports correctly from pure module spaces.</verify>
  <done>S3 Buckets accurately discard binary blobs eliminating dead storage footprints.</done>
</task>

## Success Criteria
- [ ] Orphan blocks inside AWS are securely matched and removed directly in tandem with PostgreSQL deletion queries.
- [ ] Router does not break the standard return logic.
