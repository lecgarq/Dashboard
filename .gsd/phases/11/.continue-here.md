---
phase: debt-resolution-sweep
task: pending-verification
status: in_progress
last_updated: 2026-04-16T23:59:00.000Z
---

<current_state>
The repo debt sweep is implemented. Shared module abstractions, live families virtualization, Redis APS caching, proxy/upload hardening, auth env cleanup, and LOD CUDA affinity changes are all in place, and `npm run build` succeeds on Next.js `16.2.4`.
</current_state>

<completed_work>

- Replaced duplicate Sim/Clash routers with `server/routers/module-router.ts`.
- Replaced duplicate Sim/Clash pages with `components/modules/ModuleDocumentationPage.tsx`.
- Split Trello page responsibilities into dedicated components under `components/trello/`.
- Reduced WikiEditor by extracting video-node, media, Yjs-provider, and link-dialog helpers.
- Switched the live families route to `components/families/KanbanBoard`.
- Moved APS project cache from Prisma to Redis with search-aware cache keys and explicit invalidation.
- Removed proxy interception from `/api/*` routes and added direct UploadThing auth middleware.
- Canonicalized auth env handling around `AUTH_URL` / `AUTH_SECRET`.
- Added CUDA device pinning support to the LOD engine and pipeline subprocess.
- Rewrote `.gsd/TECHNICAL_DEBT.md` and `.gsd/STATE.md` to match the verified repo state.

</completed_work>

<remaining_work>

- Browser smoke tests for the main dashboard routes were not run in this pass.
- `npx prisma generate` is still blocked locally by a locked Windows Prisma engine DLL.
- Live Railway env pruning for legacy `NEXTAUTH_*` vars was not automated in this pass.

</remaining_work>

<next_action>
1. Close any process locking `node_modules/.prisma/client/query_engine-windows.dll.node`.
2. Re-run `npx prisma generate`.
3. Run browser smoke tests across auth and the main dashboard modules.
</next_action>
