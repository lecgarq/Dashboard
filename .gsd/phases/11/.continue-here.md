---
phase: 11-redis-and-production-debug
task: in_progress
status: in_progress
last_updated: 2026-04-16T23:13:09.125Z
---

<current_state>
Production Railway deployment is crashing. Latest push (commit 9b1d090) is currently building on Railway. Three bugs were found and fixed in sequence — waiting on final deployment to confirm app is fully live.
</current_state>

<completed_work>

- Phase 6 (A11y): COMPLETE — aria-labels added to WikiEditor, GlobalSearch, MailPanel. Committed 49c93f6.
- Phase 11 (Redis): COMPLETE — LodSearchCache migrated from Postgres to Upstash Redis. lib/redis.ts created, lod.ts rewired, Prisma migration run. Committed a379949.
- Redis resilience fix: Changed lib/redis.ts from eager singleton to lazy getRedis() returning null when env vars missing. Committed a704ccf.
- ngrok crash fix 1: start-production.cjs no longer calls process.exit(1) on tunnel failure. Committed d4b016f.
- ngrok crash fix 2: ngrok addr was hardcoded to 3000 but Railway uses PORT=8080. Fixed to use process.env.PORT. Committed 9b1d090.
- Pushed all commits to origin/deploy — Railway auto-deploys from this branch.
</completed_work>

<remaining_work>

- Wait for Railway build of commit 9b1d090 to complete (was just pushed, ~2 min build time).
- Verify health endpoint responds: `curl https://dashboard-production-42cf.up.railway.app/api/health` → should return `{"status":"ok"}`
- Verify ngrok tunnel works: `curl https://divertible-dudishly-kina.ngrok-free.dev/api/health`
- Navigate to live URL in browser and test login + key dashboard pages.
- Write SUMMARY.md files for phases 9, 10, 12 (plans exist, work done, no summaries written yet).
- Update .gsd/STATE.md to reflect current session work.
</remaining_work>

<decisions_made>

- Redis client uses lazy getRedis() returning null rather than crashing on missing env vars — app degrades gracefully (pgvector still works, just no cache).
- ngrok tunnel failure is now a warning not a fatal crash — Railway domain still serves traffic even if tunnel fails.
- Upstash vars must be scoped to the Dashboard service in Railway (not just added globally) — user confirmed adding them.
- NGROK_DOMAIN on Railway = divertible-dudishly-kina.ngrok-free.dev (NOT .ngrok-free.app — that was wrong and caused ERR_NGROK_313).
</decisions_made>

<blockers>

- Railway project token (RAILWAY-DASHBOARD-TOKEN) is read-only — cannot write env vars via API. User must manage env vars manually on railway.com.
- Railway personal token (RAILWAY-TOKEN) has log read access but not env var write access.
- Local ngrok CLI not in PATH — cannot run `ngrok config add-authtoken` from Claude's shell.
</blockers>

<context>
Railway deployment was in a crash loop since before this session. Root causes found in order:
1. UPSTASH_REDIS_REST_URL was only added to one service on Railway, not the Dashboard service → Redis constructor threw at first request load → crash loop.
2. NGROK_DOMAIN was set to wrong TLD (.app instead of .dev) → ngrok ERR_NGROK_313 → process.exit(1) → crash loop.
3. ngrok addr hardcoded to 3000 but Railway injects PORT=8080 → ngrok tunnel connected but forwarded to wrong port.

All three are now fixed and pushed. The Railway deployment b89dcbd4 showed SUCCESS but still 502 — this is likely CDN cache lag from the crash loop. The new commit 9b1d090 should resolve it completely once deployed.

Railway project: 232d26e6-d60a-493b-81c9-ecd42ac4b235
Dashboard service: a552f05c-f2c4-4a4c-b1bd-524c84972810
Production URL: https://dashboard-production-42cf.up.railway.app
Live ngrok URL: https://divertible-dudishly-kina.ngrok-free.dev
</context>

<next_action>
Start with: Check Railway deployment status for commit 9b1d090 — run:
`curl -s https://dashboard-production-42cf.up.railway.app/api/health`
If {"status":"ok"} → navigate browser to live URL and test the dashboard.
If still 502 → fetch deployment logs with personal token ba0ca070-20b1-4cfc-9f4d-3478715e19ae using the deploymentLogs GraphQL query.
</next_action>
