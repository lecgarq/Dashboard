---
phase: 9
plan: 1
wave: 1
---

# Plan 9.1: Install & Boot `@tanstack/react-virtual`

## Objective
The absolute DOM bloat surrounding our `KanbanBoard` structure causes horrific memory leaks when loading `n > 200` items locally. This plan will natively install `@tanstack/react-virtual` into the package configuration, wrapping the `DroppableColumn` array mapping rendering into a headless dynamic viewport pipeline.

## Context
- .gsd/TECHNICAL_DEBT_MATRIX.md
- src file: `components/families/KanbanBoard.tsx`
- package map: `package.json`

## Tasks

<task type="auto">
  <name>Install @tanstack/react-virtual Core</name>
  <files>package.json</files>
  <action>
    - Command: `npm install @tanstack/react-virtual`
    - What to avoid: Do not update other `@tanstack` libraries like `react-query` to prevent breaking existing tRPC hook typings.
  </action>
  <verify>npm list @tanstack/react-virtual</verify>
  <done>Library installed locally without dependency conflicts.</done>
</task>

<task type="auto">
  <name>Refactor KanbanBoard Map Iteration</name>
  <files>c:\LECG\Dashboard\components\families\KanbanBoard.tsx</files>
  <action>
    - Implement `useVirtualizer` hook natively wrapping the `families.map()` iterator inside `DroppableColumn`.
    - Provide a rigid bounding `div` enforcing `overflow-y: auto`.
    - Ensure `@dnd-kit/sortable` elements explicitly pass `virtualRow.index` preventing misalignments during dragging transitions. 
  </action>
  <verify>Check strict Typescript compilation executing `npm run knip` inside the root tree.</verify>
  <done>All 200+ list elements are truncated strictly via absolute positioning and CSS transforms dynamically.</done>
</task>

## Success Criteria
- [ ] Profiler reads < 40 DOM nodes rendered concurrently per column boundary.
- [ ] Drag-and-drop mechanics function flawlessly over the virtualized window bounds tracking scroll speed natively.
