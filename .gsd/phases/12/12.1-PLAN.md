---
phase: 12
plan: 1
wave: 1
---

# Plan 12.1: Granular Next.js `<Suspense>` Boundaries

## Objective
The current codebase maps monolithic `Promise.all()` fetching arrays on root layouts specifically inside `/exam/page.tsx` and `/clash-detection/page.tsx`. This plan breaks the execution boundary out natively, nesting data fetches downward towards strictly mapped Server Components wrapping them individually in `<React.Suspense>`.

## Context
- .gsd/TECHNICAL_DEBT_MATRIX.md
- src API: `app/(dashboard)/clash-detection/page.tsx`
- src API: `app/(dashboard)/exam/page.tsx`

## Tasks

<task type="auto">
  <name>Deconstruct Layout Promises</name>
  <files>c:\LECG\Dashboard\app\(dashboard)\clash-detection\page.tsx</files>
  <action>
    - Remove the primary `await Promise.all([api.trello.getBoard(), api.clash.getTopics()])` loop at the very top of `page.tsx`.
    - Inject the raw unawaited promises strictly down into isolated Component properties `boardPromise={trelloPromise}`.
  </action>
  <verify>Syntax validation tracking Server Action payload drops.</verify>
  <done>Layout compiles structurally preventing await blocks on the primary rendering shell.</done>
</task>

<task type="auto">
  <name>Wrap Sub-Components with Suspense Fallbacks</name>
  <files>c:\LECG\Dashboard\app\(dashboard)\clash-detection\page.tsx</files>
  <action>
    - Ensure `use(boardPromise)` exists functionally inside the actual child mapping layers.
    - Wrap the UI instantiation inside `<React.Suspense fallback={<SkeletonKanban />}>` preventing the parent context from freezing white.
  </action>
  <verify>Run `npm run build` validating RSC (React Server Components) payload chunks split cleanly.</verify>
  <done>User interaction loads seamlessly mapping layout arrays without waiting for Trello REST boundaries to clear.</done>
</task>

## Success Criteria
- [ ] Network timeline clearly depicts Next.js streaming the DOM tree immediately prior to HTTP payloads resolving payload data.
