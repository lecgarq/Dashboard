# Cleanup M0–M7 — execution record (2026-06-10)

Approved plan: `implementation_plan.md` (repo root). Rollback point: tag `cleanup-baseline-2026-06-10`.

## Shipped

| Milestone | Commits | Summary |
|---|---|---|
| M0 | `b525e44` | knip config hardened; baseline tag; quarantine dir |
| M1 | `8ad0b95`, `6377b88`, `7329c1e` | Root junk quarantined; secrets → `%USERPROFILE%\.lecg-secrets\`; design assets → `docs/superpowers/assets/`; Railway configs removed; APS_DOCS (2,419 files) → `C:\LECG\Archive\APS_DOCS-2026-06-10`, replaced by `docs/aps-reference.md` |
| M2 | `8ed5eec` | 25 dead source files deleted; 3 knip false positives kept + annotated in knip.ts |
| M3 | `327a4b6` | 16 deps + 3 type devDeps pruned; dotenv explicit; radix meta-package idiom |
| M4 | `b1f25d8` | 195 unused exports demoted/deleted across 86 files; duplicate exports fixed |
| M5 | `099b624` | 118 unreferenced scripts → `scripts/_attic/2026-06-10/` (gitignored, on disk) |
| M6 | `e54ba79` | ClashToolRelease/SimToolRelease models + dead getReleases/createRelease tRPC surface removed (tables were empty) |
| M7 (partial) | `b5387e4`, `4bda8e5`, `01f323a` | users.ts router → 4 domain modules (35 tRPC keys verified identical); ChatPanel.tsx 1,489→101 + 7 modules; WikiEditor.tsx 1,182→838 + extensions/toolbar modules |

Gates held throughout: tsc 0 errors (ignoring concurrent-WIP `dcIngest.test.ts`), lint 0 errors, vitest full-suite green, knip ~0 actionable, prod build green.

## Remaining debt (deferred, needs owner approval per item)

1. **`lib/acc/dcIngest.ts` split (1,625 LOC)** — BLOCKED: file has concurrent uncommitted WIP and is the nightly DC cron path. Do after that WIP lands + a healthy cron day. Side quest: fix `AccDcIngestRun.rowsByModule` telemetry (always 0).
2. **Clash/Sim module merge** — biggest payoff/blast radius; needs its own spec + session (DB models + 2 routers + cloned UI → one `moduleKey`-parameterized engine).
3. **Taxonomy move** — `users/access-analysis/accTaxonomy*` → `lib/acc/taxonomy/` so the legacy route dir can die; deferred (active WIP zone).
4. **WikiEditor remainder (838 lines)** — upload pipeline (~135 lines), Yjs sync effect (~80), autosave machinery still inline; they close over component state. Extract only with dedicated UAT.
5. **ConversationView.tsx (470 lines)** — message list + composer share ~14 closures; split would need state restructuring (context or reducer), not a mechanical move.
6. **ESLint has no rules** — `eslint.config.mjs` is ignores-only; `next/core-web-vitals` config was never wired. Lint gate currently only catches directive hygiene.
7. **`scripts/_attic/2026-06-10/`** — empty it after ~2 weeks of healthy DC cron logs.

## Verification commands

```powershell
npx tsc --noEmit; npm run lint; npm run test; npm run knip
# build without touching live .next:
NEXT_DIST_DIR=.next-new npm run build
```
