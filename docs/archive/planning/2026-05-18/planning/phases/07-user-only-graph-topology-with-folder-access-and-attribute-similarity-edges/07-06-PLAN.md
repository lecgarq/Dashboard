---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 06
type: execute
wave: 3
depends_on: ["07-05"]
files_modified:
  - app/(dashboard)/users/AccUsersGraph.tsx
autonomous: false
requirements: [GRAPH7-05, GRAPH7-06, GRAPH7-08, GRAPH7-09, GRAPH7-11]
must_haves:
  truths:
    - "AccUsersGraph reads accFoldersRouter.getMatrix and passes the rows to buildAccTopologyGraph."
    - "AccUsersGraph resolves user → roleIds → folderIds (transitive folder-access) once per data refresh, memoized."
    - "Filter panel renders the 4 new control groups: Show folders toggle, permTier checkbox group, similarity-dim toggle group, similarity-min slider (range 1..5), View-mode toggle (multi/user-only)."
    - "All 5 new filter keys round-trip through the URL (folders, ptiers, simDims, simMin, view)."
    - "viewMode='user-only' visibly hides all non-user nodes via setVisibleIndices and the similarity-edges remain rendered."
    - "Per-edge color buffer driven by permTier (role-folder edges) and dimension (similarity edges); folder hub color teal/slate."
    - "Slider scrub on the physics panel does NOT trigger similarity edge recompute (Pitfall 5 — memoize keyed on simMin/simDims/dataHash)."
  artifacts:
    - path: "app/(dashboard)/users/AccUsersGraph.tsx"
      provides: "Full 2D wiring: data fetch, transitive folder-access resolution, filter panel additions, URL persistence, edge-color buffer"
  key_links:
    - from: "AccUsersGraph.tsx"
      to: "server/routers/acc-folders.ts:getMatrix"
      via: "trpc.accFolders.getMatrix.useQuery"
      pattern: "accFolders\\.getMatrix"
    - from: "AccUsersGraph.tsx (filter panel)"
      to: "URL params (folders, ptiers, simDims, simMin, view)"
      via: "useSearchParams + router.replace pattern (match existing URL plumbing)"
      pattern: "(folders|ptiers|simDims|simMin|view)="
---

<objective>
Wire all new topology data into the 2D `AccUsersGraph.tsx`: fetch `accFolders.getMatrix`, resolve transitive user→folder access, pass to extended `buildAccTopologyGraph`, drive per-edge color from permTier/dimension, add the 4 new filter panel controls, persist them via URL, and apply view-mode visibility.

Purpose: This is the user-facing 2D shipping plan. Includes a checkpoint at end for Luis to verify FPS + visual fidelity.
Output: One modified file plus a UAT checkpoint.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/accGraphFilters.ts
@app/(dashboard)/users/accGraphOrganicLayout.ts
@server/routers/acc-folders.ts
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-RESEARCH.md
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data fetch + transitive folder-access resolution (memoized)</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
    1. Add `const folderMatrixQuery = trpc.accFolders.getMatrix.useQuery();` near the existing tRPC queries (search `useQuery` to find the right cluster).
    2. Build a `SimilarityInput` from the existing `enrichedUsers` array. Memoize with `useMemo` keyed on `(enrichedUsers, folderMatrixQuery.data)`:
       - For each user, walk their `perProjectRoles` (or equivalent — check existing user shape; if absent, use `roles: string[]`) → set of roleIds.
       - For each user, resolve their `folderIds` transitively: iterate folderMatrix rows where `row.roleId ∈ user.roleIds`, collect distinct `folderId`. Per RESEARCH Pitfall 2 + Open Question 1.
       - `projectIds` from existing user.projectId list (or `BulkAccUser` equivalent).
       - `company` from `user.company` (or `companyName`).
       - `adminTier`: derive from existing admin tier shape — `hub` if `isAccountAdmin`, `project` if `accessLevels.projectAdmin`, `executive` if `accessLevels.executive`, else null. Reuse `lib/acc/adminTierShape` if appropriate.
    3. Pass `folderMatrixQuery.data?.rows`, `similarityInput`, `new Set(filters.simDims)`, `filters.simMin` to `buildAccTopologyGraph`. Memoize the topology call on `(snapshot, filters.simDims, filters.simMin, folderMatrixQuery.data, filters.showFolders)` — NOT on the physics-slider state (Pitfall 5).
    4. When `filters.showFolders === false`, pass `folderMatrix=undefined` to `buildAccTopologyGraph` so folder hubs are not emitted at all.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "AccUsersGraph" || echo "OK"</automated>
  </verify>
  <done>Folder matrix fetched, similarity input built, topology rebuilds only on filter-shape changes (not physics sliders).</done>
</task>

<task type="auto">
  <name>Task 2: Filter panel UI additions + URL persistence + per-edge color buffer</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
    1. Find the existing filter panel JSX (grep for one of: `<FilterPanel`, `roles.includes`, the existing GraphFilters consumer JSX). Append a new `<section>` with:
       - **Show folders** — switch bound to `filters.showFolders` (default ON).
       - **Permission tiers** — 4 checkboxes (`view, upload, edit, control`) bound to `filters.permTiers`.
       - **Similarity dimensions** — 5 checkboxes (`folder-access, roles, projects, company, admin-tier`) bound to `filters.simDims`.
       - **Similarity strength** — range slider 1..5 step 1 bound to `filters.simMin` with label "Min shared attributes".
       - **View mode** — radio or two-button toggle bound to `filters.viewMode` (`multi` / `user-only`).
       Match existing component style (search the file for `<label`, `<input type="checkbox"`, `<input type="range"` patterns and reuse).
    2. URL persistence — locate the existing URL reader/writer (grep `URLSearchParams` and `router.replace` or `window.history.replaceState`). Add round-trip for:
       - `folders=true|false`
       - `ptiers=v,u,e,c` (single letters joined by comma; absent = all)
       - `simDims=fa,r,p,c,a` (joined by comma; absent = all)
       - `simMin=N` (integer; absent = 2)
       - `view=user-only|multi` (absent = multi)
    3. Per-edge color buffer: when building `linkColors` for cosmos, derive color from each `AccTopologyLink`:
       - `kind === "role-folder"` → color from a `PERM_TIER_COLOR` LUT keyed on `permTier`: view `#9CA3AF`, upload `#A78BFA`, edit `#FBBF24`, control `#F87171`.
       - `kind === "user-similarity"` → color from a `SIM_DIM_COLOR` LUT: folder-access `#5EEAD4`, roles `#60A5FA`, projects `#F472B6`, company `#A3E635`, admin-tier `#FB923C`.
       - `kind === "folder-project"` → default container neutral `#94A3B8`.
       - Other existing kinds: keep current color.
       Pass `{ perEdgeColors: linkColors }` to the extended `buildLinkColorBuffer`.
    4. Visibility: in the existing visible-indices builder, plumb `nodeMatchesFilters` with the extended filter state. Confirm `kind: "folder"` and viewMode='user-only' both correctly hide via existing setVisibleIndices path.
    5. Filter edges by enabled `permTiers` and `simDims` AT ADAPTER LEVEL (after `buildAccTopologyGraph` returns): drop links where `kind==="role-folder"` and `permTier` not in `filters.permTiers`; drop links where `kind==="user-similarity"` and `dimension` not in `filters.simDims`. (Memoize again on `(topology, filters.permTiers, filters.simDims)`.)
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "AccUsersGraph" || echo "OK"</automated>
  </verify>
  <done>Filter panel renders 4 new control groups; URL params round-trip; per-edge color rendered; viewMode=user-only hides non-user nodes; permTier/simDims toggles drop matching edges.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — visual + FPS check at hub scale</name>
  <files>(checkpoint — no file modifications)</files>
  <action>Pause for Luis to verify the 2D wiring per the steps below. Do not proceed to summary until approved or gaps recorded.</action>
  <verify>Luis types "approved" or describes specific gaps.</verify>
  <done>Approval recorded; FPS observation captured for Plan 07-09.</done>
  <what-built>
    2D AccUsersGraph now renders folder hubs (depth-2 collapsed, slate/teal), role↔folder edges colored by permTier (4 colors), user↔user similarity edges colored by dimension (5 colors), with full filter panel + URL persistence + user-only view mode.
  </what-built>
  <how-to-verify>
    1. `npm run dev`; visit `/users` route in the dashboard.
    2. Filter panel: confirm Show folders toggle, 4 permission-tier checkboxes, 5 similarity-dimension checkboxes, similarity slider (1..5), View mode toggle all render and respond.
    3. Folder hubs should be visibly distinct (teal/slate color), clustered near their parent project hubs.
    4. Toggle View mode → user-only: all project/role/module/access/folder hubs should disappear; user nodes + colorful parallel similarity edges remain. Toggle back → restored.
    5. Drag the similarity slider from 1 → 5: edge count should visibly decrease.
    6. Toggle off all permTier checkboxes: all role-folder edges disappear; folder hubs remain visible but with no incoming role edges.
    7. Copy the URL with a non-default state, paste into a fresh tab: filter state restored.
    8. FPS sanity: open dev tools performance/FPS meter. Scroll/zoom the graph with all dimensions ON. Report observed FPS at hub scale (acceptable: ≥30 fps in 2D — Pitfall 1 / GRAPH7-12).
    9. Slider scrub on the existing PHYSICS sliders (separation, cluster) should feel smooth — NOT chunky. (Pitfall 5 — memoization gate.)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compile clean (no new errors).
- Manual UAT checkpoint passes.
- Filter panel and URL persistence both functional.
</verification>

<success_criteria>
- 2D folder hubs + role-folder edges + similarity edges render with correct colors.
- View-mode user-only correctly isolates users + similarity edges.
- Filter panel controls drive both topology shape and URL.
- FPS observation captured in SUMMARY (informs Phase 7 perf gate, Plan 07-09).
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-06-SUMMARY.md` including the FPS observation.
</output>
