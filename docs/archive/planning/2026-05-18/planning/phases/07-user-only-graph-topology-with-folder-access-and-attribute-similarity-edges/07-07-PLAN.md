---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - app/(dashboard)/users/sphere3d/topologyAdapter.ts
  - app/(dashboard)/users/sphere3d/filterAdapter.ts
  - app/(dashboard)/users/sphere3d/EdgesLines.tsx
  - app/(dashboard)/users/sphere3d/NodesPoints.tsx
autonomous: true
requirements: [GRAPH7-01, GRAPH7-02, GRAPH7-03, GRAPH7-04, GRAPH7-06, GRAPH7-10, GRAPH7-11]
must_haves:
  truths:
    - "PRECONDITION: 07-01-SUMMARY records DECISION: 3D-WIRING=PROCEED. Otherwise this plan is a no-op stub SUMMARY noting the defer."
    - "3D topology adapter consumes the same buildAccTopologyGraph output as 2D — folder hubs (conditional on 3D-FOLDERS=GO), similarity edges always."
    - "GPU edge buffer extended with per-edge byte packing: 3 bits aDimension + 3 bits aPermTier + 2 bits reserved."
    - "Edge fragment shader samples a 16×1 RGBA LUT texture for color (no branching per fragment)."
    - "filterAdapter visibility predicate ANDed with (viewMode !== 'user-only' || node.kind === 'user') AND (kind !== 'folder' || showFolders)."
    - "NodesPoints folder color hardcoded teal/slate (Pitfall 4)."
  artifacts:
    - path: "app/(dashboard)/users/sphere3d/topologyAdapter.ts"
      provides: "Extended buildEdgeBuffers with aPermTier + aDimension per-edge attributes"
    - path: "app/(dashboard)/users/sphere3d/filterAdapter.ts"
      provides: "Visibility predicate extended for viewMode + showFolders"
    - path: "app/(dashboard)/users/sphere3d/EdgesLines.tsx"
      provides: "Fragment shader 16-texel color LUT + interleaved aPackedFlags byte"
    - path: "app/(dashboard)/users/sphere3d/NodesPoints.tsx"
      provides: "Folder color override branch"
  key_links:
    - from: "sphere3d/topologyAdapter.ts"
      to: "lib/acc/folderHubCollapse.ts + lib/acc/userSimilarity.ts"
      via: "imported pure modules"
      pattern: "(collapseFoldersToDepth|computeSimilarityEdges)"
---

<objective>
Extend the 3D sphere renderer (Phase 6 stack) with the same topology additions as 2D: similarity edges (always), folder hubs (conditional on Phase 4 GRAPH-04 GO recorded in 07-01's PHASE-DEPS.md). GPU buffer additions: per-edge byte packing for `aDimension` + `aPermTier` → single texture-LUT sample in the edge fragment shader.

Purpose: 3D parity. The data shape is shared with 2D; only the GPU buffer plumbing differs.
Output: Four modified files. Conditional execution per Wave-0 gate.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-RESEARCH.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-03-PLAN.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-04-PLAN.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-05-PLAN.md
@lib/acc/folderHubCollapse.ts
@lib/acc/userSimilarity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 gate check + bail-out if 3D-WIRING=DEFER</name>
  <files>.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-07-SUMMARY.md</files>
  <action>
    Read `PHASE-DEPS.md`. Branch:

    - If `DECISION: 3D-WIRING=DEFER` → write a stub SUMMARY at `07-07-SUMMARY.md` documenting the defer reason (Phase 6 incomplete), mark this plan SKIPPED, and exit. Do NOT modify any source files. Commit with `docs(07-07): defer 3D wiring — Phase 6 incomplete`.
    - If `DECISION: 3D-WIRING=PROCEED` → proceed to Task 2. Note `DECISION: 3D-FOLDERS` (GO|NO-GO|DEFERRED) — pass this through to Task 2 as `enableFolderHubs` boolean.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md &amp;&amp; grep -E "^DECISION: 3D-WIRING=" .planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md</automated>
  </verify>
  <done>Either stub SUMMARY committed (DEFER path), or gate confirmed PROCEED and folder-hub flag determined.</done>
</task>

<task type="auto">
  <name>Task 2: Extend topologyAdapter + filterAdapter with folder hubs (conditional) + similarity edges (always) + viewMode/showFolders predicate</name>
  <files>app/(dashboard)/users/sphere3d/topologyAdapter.ts,app/(dashboard)/users/sphere3d/filterAdapter.ts</files>
  <action>
    **topologyAdapter.ts:**
    1. Import and call `buildAccTopologyGraph` from `app/(dashboard)/users/accGraphOrganicLayout.ts` with the new inputs (folderMatrix + similarityInput + simDims + simMin). Pass `folderMatrix=undefined` when `enableFolderHubs=false`.
    2. Extend `buildEdgeBuffers` to emit a parallel `Uint8Array` `packedFlagsBuf` of length `linkCount` where each byte = `(dimIndex &amp; 0x07) | ((permTierIndex &amp; 0x07) &lt;&lt; 3)`:
       - dimIndex: 0..4 for the 5 similarity dimensions (`folder-access=0, roles=1, projects=2, company=3, admin-tier=4`), 7=none.
       - permTierIndex: 0..3 for view/upload/edit/control, 7=none.
       - Edges with neither (e.g. existing kinds) pack as `0xFF` (both none → distinguishable in shader as "use default color").
    3. Return `packedFlagsBuf` alongside the existing edge buffers.

    **filterAdapter.ts:**
    1. Accept `viewMode: ViewMode` and `showFolders: boolean` in the predicate fn signature.
    2. AND existing visibility predicate with:
       - `if (viewMode === "user-only" &amp;&amp; node.kind !== "user") return false;`
       - `if (node.kind === "folder" &amp;&amp; !showFolders) return false;`
    3. Continue to write `aVisible` 0/1 into the existing `params` DataTexture or buffer (whatever Phase 6 plan 06-04 chose).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "sphere3d/(topology|filter)Adapter" || echo "OK"</automated>
  </verify>
  <done>topologyAdapter emits packedFlagsBuf; filterAdapter honors viewMode + showFolders.</done>
</task>

<task type="auto">
  <name>Task 3: EdgesLines fragment shader 16-texel color LUT + NodesPoints folder color override</name>
  <files>app/(dashboard)/users/sphere3d/EdgesLines.tsx,app/(dashboard)/users/sphere3d/NodesPoints.tsx</files>
  <action>
    **EdgesLines.tsx:**
    1. Add a `Uint8Array` per-vertex attribute `aPackedFlags` (length = vertexCount; each edge contributes 2 vertices, both share the same byte) — wire from `packedFlagsBuf` produced in Task 2.
    2. Create a 16×1 RGBA `DataTexture` uniform `uEdgeColorLUT` filled with:
       - indices 0..4: the 5 similarity-dimension colors (folder-access teal, roles blue, projects pink, company lime, admin-tier orange — same hex as 2D plan 07-06).
       - indices 8..11: the 4 permTier colors (view slate, upload violet, edit amber, control red).
       - index 15 (`0xFF` packed default): neutral edge color (current existing default).
    3. Vertex shader: pass `aPackedFlags` through as `vPacked` varying (declare as `float` since attributes are floats — cast `float packed = aPackedFlags;`).
    4. Fragment shader: derive LUT index. If `packed == 255.0` use index 15; else if `(int(packed) &gt;&gt; 3) &amp; 0x07 != 7` use that as permTier (index 8 + permTierIdx); else use `(int(packed) &amp; 0x07)` as dim index. One `texture2D(uEdgeColorLUT, vec2((index+0.5)/16.0, 0.5))` sample. Multiply by existing `aVisible`-driven alpha.
    5. Confirm WebGL2 attribute limit not exceeded (existing attrs from 06-05 + 1 new byte attribute — should be fine; verify by counting attrs in the existing code).

    **NodesPoints.tsx:**
    1. Find existing per-vertex color attribute setup. Add a branch: when source node has `kind === "folder"`, write the teal color `#5EEAD4` (RGB 0x5E/255, 0xEA/255, 0xD4/255) instead of running through `getCategoryColor`. Pitfall 4 defense.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "sphere3d/(EdgesLines|NodesPoints)" || echo "OK"</automated>
  </verify>
  <done>3D edges colored per permTier/dimension via LUT; folder nodes teal; no WebGL2 attribute-limit errors.</done>
</task>

</tasks>

<verification>
- 07-07-SUMMARY exists in either DEFER or PROCEED form.
- If PROCEED: `npx tsc --noEmit -p .` clean.
- 3D edges have visible color differentiation per permTier and dimension at runtime (verified in Plan 07-09 UAT).
</verification>

<success_criteria>
- 3D wiring proceeds only when Wave-0 gate allows.
- Folder hubs in 3D only when GRAPH-04 GO recorded.
- Per-edge GPU color LUT in place; no shader branching cost.
</success_criteria>

<output>
After completion, finalize `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-07-SUMMARY.md` (replace stub if PROCEED).
</output>
