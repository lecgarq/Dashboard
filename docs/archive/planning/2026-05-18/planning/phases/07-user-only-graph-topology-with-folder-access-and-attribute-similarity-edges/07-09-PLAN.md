---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 09
type: execute
wave: 4
depends_on: ["07-06", "07-07", "07-08"]
files_modified:
  - .planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PERF-REPORT.md
autonomous: false
requirements: [GRAPH7-12, DASH-18]
must_haves:
  truths:
    - "FPS measured at hub-scale in both 2D and 3D (if 3D shipped per gate) with all 5 similarity dimensions ON + folders ON."
    - "Interactivity contract observed: hover detail / click-through / cross-widget spotlight functions on folder hubs AND user nodes."
    - "PERF-REPORT.md captures FPS numbers, edge-count totals, and a GO/NO-GO recommendation for retaining Phase 7 as default."
    - "Luis explicitly approves or requests changes via the checkpoint."
  artifacts:
    - path: ".planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PERF-REPORT.md"
      provides: "FPS + interactivity UAT findings + approval decision"
      contains: "DECISION:"
  key_links:
    - from: "PERF-REPORT.md"
      to: "Phase 7 SUMMARY"
      via: "decision feeds milestone closure"
      pattern: "DECISION:"
---

<objective>
Wave-4 acceptance gate. Measure FPS at hub scale in 2D (always) and 3D (when 3D-WIRING=PROCEED). Verify interactivity contract on new node kinds (folder) and edge families (role-folder, user-similarity). Capture findings in PERF-REPORT.md and obtain Luis's explicit approval.

Purpose: Phase 7 ships only after Luis confirms the new topology renders smoothly and the interactivity contract (feedback memory `feedback_widget_interactivity`) is satisfied.
Output: One report file + a recorded approval decision.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-06-SUMMARY.md
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-07-SUMMARY.md
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold PERF-REPORT.md template + run automated edge-count probe</name>
  <files>.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PERF-REPORT.md</files>
  <action>
    Create `PERF-REPORT.md` with structure:

    ```
    # Phase 7 Performance Report
    Measured: <date>

    ## Configuration
    - Hub-scale user count: <from BulkAccUser count query>
    - Total folder hubs (depth-2 collapsed): <from buildAccTopologyGraph trace>
    - Total similarity edges (all 5 dims, minShared=2): <from trace>
    - 3D shipped: YES | NO (per PHASE-DEPS.md)

    ## 2D Surface (AccUsersGraph)
    - FPS idle: <observed>
    - FPS during pan/zoom: <observed>
    - FPS during physics-slider scrub: <observed>
    - Interactivity:
      - Hover detail on folder hub: WORKS | BROKEN
      - Click-through to side panel: WORKS | BROKEN
      - Cross-widget spotlight: WORKS | BROKEN

    ## 3D Surface (Sphere3DGraph) — if shipped
    - FPS idle: <observed>
    - FPS during orbit/zoom: <observed>
    - Edge-LUT color visibly differentiates permTier and dimension: YES | NO

    ## Findings
    <prose: what's smooth, what's not, any pitfall observed>

    ## DECISION: PHASE-7-ACCEPT=APPROVED|GAPS|REVERT
    ```

    Automated probe: run `npm run dev` in background, hit the `/users` page programmatically (or document manual hit), and have a small dev-only `console.log(topologyGraph.links.length)` in `buildAccTopologyGraph` so the executor can paste the count. (Don't ship the console.log — remove after.)
  </action>
  <verify>
    <automated>test -f .planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PERF-REPORT.md</automated>
  </verify>
  <done>Report scaffolded; edge/folder counts captured from real data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Luis UAT — FPS + interactivity sign-off</name>
  <files>(checkpoint — fills PERF-REPORT.md after observation)</files>
  <action>Pause for Luis to walk through both surfaces, fill in PERF-REPORT.md observations, and sign off.</action>
  <verify>PERF-REPORT.md DECISION line populated with APPROVED|GAPS|REVERT.</verify>
  <done>Approval (or gap list) recorded; Phase 7 closes if APPROVED.</done>
  <what-built>
    Phase 7 topology extensions: folder hubs (2D always, 3D conditional), 5-dimension parallel similarity edges, user-only view toggle, 4-tier role-folder edge coloring, full filter panel with URL persistence on both surfaces.
  </what-built>
  <how-to-verify>
    1. Open `/users` (2D AccUsersGraph). Confirm:
       - Folder hubs visible (teal/slate); filter panel "Show folders" toggle hides them.
       - 4 permTier colors visible on role-folder edges; per-tier checkboxes filter correctly.
       - 5 similarity-dimension colors visible on user-similarity edges; per-dim checkboxes filter correctly.
       - Similarity slider 1..5 visibly changes edge density.
       - View mode → user-only: all non-user nodes vanish, similarity edges remain.
       - Hover a folder hub → side-panel/hover-detail shows folder info (per DASH-18 interactivity contract).
       - Click a folder hub → cross-widget spotlight if applicable.
       - URL reflects state; paste in new tab restores.
       - FPS ≥ 30 in 2D with all dims ON.
    2. If 3D shipped (per PHASE-DEPS.md DECISION: 3D-WIRING=PROCEED): switch to the 3D Sphere3DGraph surface and repeat the visual + interactivity checks. FPS target: ≥ 60.
    3. Fill in PERF-REPORT.md with observations.
    4. Approve or list gaps that need closure.
  </how-to-verify>
  <resume-signal>Type "approved" if accepting, or describe gaps for /gsd:plan-phase --gaps.</resume-signal>
</task>

</tasks>

<verification>
- PERF-REPORT.md contains a DECISION line: APPROVED | GAPS | REVERT.
- If APPROVED → Phase 7 closes.
- If GAPS → run `/gsd:plan-phase 07 --gaps` to plan closure.
- If REVERT → unwind via git revert; document why in retrospective.
</verification>

<success_criteria>
- PERF-REPORT.md committed with full data.
- Luis signs off explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-09-SUMMARY.md` capturing the decision and any follow-up gaps.
</output>
