---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 08
type: execute
wave: 3
depends_on: ["07-01", "07-04"]
files_modified:
  - app/(dashboard)/users/sphere3d/filterUrl.ts
  - app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx
  - app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts
autonomous: true
requirements: [GRAPH7-05, GRAPH7-06, GRAPH7-08, GRAPH7-09]
must_haves:
  truths:
    - "PRECONDITION: 07-01-SUMMARY records DECISION: 3D-WIRING=PROCEED. Otherwise stub SUMMARY noting defer."
    - "filterUrl.ts reads + writes the same 5 new keys as the 2D surface (folders, ptiers, simDims, simMin, view) so URL state is portable between 2D and 3D."
    - "Sphere3DFilterPanel renders the same 4 new control groups (Show folders, permTiers, simDims, simMin slider, View mode)."
    - "Round-trip Vitest tests cover the 5 new keys (default omitted; custom value preserved; invalid value defaulted)."
  artifacts:
    - path: "app/(dashboard)/users/sphere3d/filterUrl.ts"
      provides: "Extended read/writeFiltersFromUrl with 5 new keys"
    - path: "app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx"
      provides: "Filter UI panel — 4 new control groups"
    - path: "app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts"
      provides: "Round-trip coverage for 5 new keys"
  key_links:
    - from: "filterUrl.ts"
      to: "GraphFilters (extended in Plan 07-04)"
      via: "readFiltersFromUrl returns GraphFilters, writeFiltersToUrl accepts GraphFilters"
      pattern: "GraphFilters"
---

<objective>
Extend the Phase 6 `filterUrl.ts` and `Sphere3DFilterPanel.tsx` (both lifted in Phase 6 plan 06-08) with Phase 7's 5 new filter keys. Keep symmetry with the 2D plan 07-06 URL keys so state is portable between surfaces.

Purpose: 3D filter UI parity. Pure additive change on top of Phase 6 plan 06-08's helpers.
Output: Three modified files (existing helper + existing component + existing test file).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-08-PLAN.md
@app/(dashboard)/users/accGraphFilters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 gate check + bail-out if 3D-WIRING=DEFER</name>
  <files>.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-08-SUMMARY.md</files>
  <action>
    Read `PHASE-DEPS.md`. If `DECISION: 3D-WIRING=DEFER` → write stub SUMMARY and exit. Else proceed to Tasks 2-3.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/PHASE-DEPS.md</automated>
  </verify>
  <done>Either stub SUMMARY committed or gate confirmed PROCEED.</done>
</task>

<task type="auto">
  <name>Task 2: Extend filterUrl.ts + round-trip tests</name>
  <files>app/(dashboard)/users/sphere3d/filterUrl.ts,app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts</files>
  <action>
    **filterUrl.ts:**
    1. Extend the existing `readFiltersFromUrl` to also parse:
       - `folders` → boolean, default `true`. Accepts `"true"`/`"false"` strings.
       - `ptiers` → array of `PermTierKey`. Comma-joined letters (`"v,u,e,c"`). Default = all four. Invalid letters dropped.
       - `simDims` → array of `SimilarityDimKey` short codes: `fa=folder-access`, `r=roles`, `p=projects`, `c=company`, `a=admin-tier`. Default = all five. Invalid codes dropped.
       - `simMin` → integer 1..5 clamp, default 2.
       - `view` → `"user-only" | "multi"`, default `"multi"`.
    2. Mirror in `writeFiltersToUrl`: emit each key only when value differs from default (keep URL short).
    3. Use the same casts/clamps for safety.

    **filterUrl.test.ts:**
    Append a `describe("Phase 7 filter keys", ...)` block with cases:
    1. Defaults round-trip: empty URL → DEFAULT_FILTERS-equivalent values; write(DEFAULT_FILTERS) → empty querystring.
    2. Custom values round-trip: every key set to a non-default → URL contains exactly those keys → read returns the same values.
    3. Invalid values: `simMin=99` → clamped to 5; `simMin=0` → clamped to 1; `ptiers=v,x,e` → returns `["view","edit"]`; `view=foo` → defaults to `"multi"`.
    4. Mixed state: only `view=user-only` set → other keys at defaults.
  </action>
  <verify>
    <automated>npx vitest run app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts</automated>
  </verify>
  <done>All round-trip tests pass; 5 new keys parsed and emitted symmetrically.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Sphere3DFilterPanel.tsx with the 4 new control groups</name>
  <files>app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx</files>
  <action>
    Append after the existing filter controls (match the component style — the file was created by Phase 6 plan 06-08):

    1. **Show folders** switch — bound to `filters.showFolders`, default ON. Disabled when `filters.viewMode === "user-only"` (no folders visible anyway).
    2. **Permission tiers** checkbox group — 4 checkboxes (view/upload/edit/control) bound to `filters.permTiers`. Disabled when `filters.viewMode === "user-only"`.
    3. **Similarity dimensions** checkbox group — 5 checkboxes bound to `filters.simDims`.
    4. **Similarity strength** range slider 1..5 step 1 bound to `filters.simMin`, with label "Min shared attributes ≥".
    5. **View mode** two-button toggle bound to `filters.viewMode` (Multi / User-only).

    Component remains controlled (`props.filters` + `props.onFiltersChange`) — owns no state, per 06-08 contract. Use the same `AccGraphFilterState` (or whatever GraphFilters alias Phase 6 chose) so a single filter shape works across surfaces.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "Sphere3DFilterPanel" || echo "OK"</automated>
  </verify>
  <done>Panel renders 4 new control groups; controlled-component contract preserved; URL state portable to/from 2D.</done>
</task>

</tasks>

<verification>
- `npx vitest run app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts` green.
- TypeScript compile clean.
</verification>

<success_criteria>
- 5 new URL keys round-trip with defaults + invalid-input safety.
- 3D filter panel reaches parity with 2D filter panel for Phase 7 controls.
</success_criteria>

<output>
After completion, finalize `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-08-SUMMARY.md` (replace stub if PROCEED).
</output>
