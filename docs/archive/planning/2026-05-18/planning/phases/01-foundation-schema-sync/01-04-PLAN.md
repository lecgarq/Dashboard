---
phase: 01-foundation-schema-sync
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - server/routers/acc-sync.ts
  - server/routers/root.ts
  - components/layout/SyncFreshnessPill.tsx
  - components/layout/Sidebar.tsx
autonomous: false
requirements: [SYNC-03]

must_haves:
  truths:
    - "tRPC accSync.getSyncFreshness returns Quick Sync last run + Deep Sync last completed times from Postgres (not memory)"
    - "Freshness query survives container restart because data lives in SyncMeta and AccDataConnectorJob tables"
    - "Sidebar bottom shows a 'Last synced: 2h ago' pill when expanded, condensed dot when collapsed"
    - "Pill shows distinct states for: never synced, last sync success, last sync failed, last sync skipped"
    - "Deep Sync UI polling cadence matches the SYNC-03 5-30 second window (implemented as 15s refetch interval while a non-terminal job exists)"
  artifacts:
    - path: "server/routers/acc-sync.ts"
      provides: "accSyncRouter with getSyncFreshness procedure (and getActiveDeepSyncJob for polling cadence)"
      contains: "getSyncFreshness"
      exports: ["accSyncRouter"]
      min_lines: 30
    - path: "server/routers/root.ts"
      provides: "accSync router registered on app router"
      contains: "accSync"
    - path: "components/layout/SyncFreshnessPill.tsx"
      provides: "Client component reading trpc.accSync.getSyncFreshness + formatDistanceToNow"
      contains: "formatDistanceToNow"
      min_lines: 40
    - path: "components/layout/Sidebar.tsx"
      provides: "SyncFreshnessPill mounted in the existing border-t bottom section (above the collapse button)"
      contains: "SyncFreshnessPill"
  key_links:
    - from: "components/layout/SyncFreshnessPill.tsx"
      to: "tRPC accSync.getSyncFreshness"
      via: "trpc.accSync.getSyncFreshness.useQuery"
      pattern: "accSync\\.getSyncFreshness"
    - from: "server/routers/acc-sync.ts"
      to: "Prisma SyncMeta + AccDataConnectorJob"
      via: "ctx.db queries (no memory cache — SYNC-03 restart-survives)"
      pattern: "syncMeta.*findUnique"
    - from: "components/layout/Sidebar.tsx"
      to: "components/layout/SyncFreshnessPill.tsx"
      via: "JSX mount in border-t bottom div"
      pattern: "<SyncFreshnessPill"
---

<objective>
Surface sync freshness in the existing dashboard chrome. Add a tRPC `accSync` router that reads from `SyncMeta` and `AccDataConnectorJob` (the persistent tables Plan 03 populates), and mount a small "Last synced: 2h ago" pill in the sidebar bottom section. Status survives Railway container restarts because the source of truth is Postgres, not in-memory state — directly satisfying SYNC-03.

Purpose: Luis (and any future viewer) can glance at the sidebar to confirm the dashboard is showing fresh data. Failures (which trigger an email per Plan 03) are also visible at-a-glance via the pill state.
Output: New tRPC router file, registered in root; new client pill component mounted inside `Sidebar.tsx`'s existing bottom div.
</objective>

<critical_addendum date="2026-05-11">
Before implementation, read `01-IMPLEMENTATION-HANDOFF.md`.

The workspace may already contain an edited `components/layout/SyncFreshnessPill.tsx`. Claude Code must inspect the current file and `git diff -- components/layout/SyncFreshnessPill.tsx` before changing it. Preserve any user or prior-agent work that already satisfies this plan, and patch only the missing behavior.

This plan is still limited to persistent sync freshness visibility:

- Do not add Quick Sync or Deep Sync buttons.
- Do not add a sync status page, dashboard route, or new tab.
- Do not implement actual ACC extraction or Data Connector CSV import here.
- Do not eager-load last file activity into the main users payload.

Acceptance hinges on restart-surviving Postgres state (`SyncMeta` + `AccDataConnectorJob`), visible expanded/collapsed sidebar states, and 15-second polling only while a deep sync is pending/running.
</critical_addendum>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema-sync/01-CONTEXT.md
@.planning/phases/01-foundation-schema-sync/01-RESEARCH.md
@.planning/phases/01-foundation-schema-sync/01-01-SUMMARY.md
@server/routers/root.ts
@server/trpc.ts
@components/layout/Sidebar.tsx

<interfaces>
<!-- Existing tRPC router registration pattern: server/routers/root.ts exports
       export const appRouter = router({ users: usersRouter, workspaceRouter, …, })
     Add: accSync: accSyncRouter -->

<!-- protectedProcedure from "../trpc" provides ctx.db (Prisma client) -->

<!-- Sidebar bottom anatomy (from RESEARCH Pattern 6):
       <div className="border-t border-white/60 px-3 py-3"> <!-- the bottom section -->
         { /* collapse button currently lives here */ }
       </div>
     SyncFreshnessPill goes INSIDE this div, ABOVE the collapse button.
     Pill must respect the sidebar's existing collapsed/expanded state — read whatever prop or context Sidebar already uses for that. -->

<!-- date-fns formatDistanceToNow is in package.json (^4.1.0). Import path: "date-fns" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create accSync tRPC router with getSyncFreshness + getActiveDeepSyncJob</name>
  <files>server/routers/acc-sync.ts, server/routers/root.ts</files>
  <action>
**Part A — Create `server/routers/acc-sync.ts`:**

Use the existing `router` and `protectedProcedure` imports (match the pattern in `server/routers/users.ts` — same trpc helper file path). Export `accSyncRouter`:

```typescript
import { router, protectedProcedure } from "../trpc";

export const accSyncRouter = router({
  // SYNC-03: Reads persistent state, not in-memory — survives container restart.
  getSyncFreshness: protectedProcedure.query(async ({ ctx }) => {
    const [quickMeta, deepMeta, latestDeepSuccess] = await Promise.all([
      ctx.db.syncMeta.findUnique({ where: { id: "quick" } }),
      ctx.db.syncMeta.findUnique({ where: { id: "deep" } }),
      ctx.db.accDataConnectorJob.findFirst({
        where: { status: "success" },
        orderBy: { completedAt: "desc" },
        select: { completedAt: true, requestId: true },
      }),
    ]);
    return {
      quick: {
        lastRunAt: quickMeta?.lastRunAt ?? null,
        lastStatus: quickMeta?.lastStatus ?? null,
        lastError: quickMeta?.lastError ?? null,
      },
      deep: {
        lastRunAt: deepMeta?.lastRunAt ?? null,
        lastStatus: deepMeta?.lastStatus ?? null,
        lastError: deepMeta?.lastError ?? null,
        lastSuccessCompletedAt: latestDeepSuccess?.completedAt ?? null,
        lastSuccessRequestId: latestDeepSuccess?.requestId ?? null,
      },
    };
  }),

  // Drives the polling cadence — pill speeds up refetch when a deep sync is in flight.
  getActiveDeepSyncJob: protectedProcedure.query(async ({ ctx }) => {
    const active = await ctx.db.accDataConnectorJob.findFirst({
      where: { status: { in: ["pending", "running"] } },
      orderBy: { startedAt: "desc" },
      select: { requestId: true, status: true, startedAt: true },
    });
    return active ?? null;
  }),
});
```

**Part B — Register in `server/routers/root.ts`:**
Import `accSyncRouter` and add `accSync: accSyncRouter` to the `appRouter` object. Match the alphabetical/grouping convention already in that file. Do not modify other routers.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "accSync" server/routers/root.ts</automated>
  </verify>
  <done>Router file exists with both procedures; root.ts registers `accSync`; TS compiles clean; tRPC type generation (if applicable) picks up the new procedures.</done>
</task>

<task type="auto">
  <name>Task 2: SyncFreshnessPill component + mount in Sidebar</name>
  <files>components/layout/SyncFreshnessPill.tsx, components/layout/Sidebar.tsx</files>
  <action>
**Part A — Create `components/layout/SyncFreshnessPill.tsx`:**

Client component (`"use client"` at top). Props: `{ collapsed?: boolean }` (read whatever name `Sidebar.tsx` already uses — match the existing convention; if Sidebar passes nothing, fall back to a context lookup or just always render expanded).

Behavior:
  - Calls `trpc.accSync.getSyncFreshness.useQuery(undefined, { refetchInterval: deepSyncActive ? 15_000 : 5 * 60_000, staleTime: 60_000 })`.
  - Calls `trpc.accSync.getActiveDeepSyncJob.useQuery(undefined, { refetchInterval: 15_000 })` — the result determines `deepSyncActive` (boolean) which speeds up the main freshness refetch when a deep job is pending/running. This implements the SYNC-03 "5–30 second polling" intent.
  - Picks the *most recent* timestamp between `quick.lastRunAt` and `deep.lastSuccessCompletedAt` and formats with `formatDistanceToNow(date, { addSuffix: true })` → "2 hours ago".
  - Visual states:
    - **No data yet** (both null): muted "Never synced".
    - **Last sync success**: subtle dot + "Last synced: 2h ago" (use existing dashboard token green if available; otherwise a CSS variable).
    - **Last sync failed**: red dot + "Sync failed 2h ago" (clickable? No — drill-down deferred per CONTEXT). Title attribute shows `lastError`.
    - **Last sync skipped** (deep): yellow dot + "Skipped 2h ago" (Title: "previous still in-flight"). Optional in v1; if cramped, treat as success-color (skipped is not a failure).
    - **Deep sync active**: small spinner overlay + label "Deep sync running…".
  - When `collapsed === true`: render only the colored dot (no text), with the full status string as `title` attribute for hover tooltip.

Use Tailwind classes matching the rest of the sidebar (text-xs, text-zinc-500/etc — sample from existing sidebar elements). No new design tokens; reuse what `Sidebar.tsx` already imports.

**Part B — Mount in `components/layout/Sidebar.tsx`:**

Locate the bottom section `<div className="border-t border-white/60 px-3 py-3">` (lines ~476–487 per RESEARCH Pattern 6). Insert `<SyncFreshnessPill collapsed={isCollapsed} />` (or whatever prop name matches the existing collapsed state) ABOVE the collapse button, separated by a small bottom margin. Add `import { SyncFreshnessPill } from "./SyncFreshnessPill";` at the top.

If the sidebar bottom div has tight padding that makes the pill cramped, add a `mb-2` to the pill wrapper and a small flex column layout. Do NOT restructure the rest of the sidebar.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>SyncFreshnessPill.tsx exists and compiles; Sidebar.tsx imports and mounts it; `npm run build` succeeds; visual rendering checked in Task 3 (human-verify).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — sidebar freshness pill in dev</name>
  <action>Human verifies the pill renders correctly in expanded/collapsed sidebar states and reflects SyncMeta + AccDataConnectorJob state. See what-built and how-to-verify below.</action>
  <files>components/layout/SyncFreshnessPill.tsx, components/layout/Sidebar.tsx</files>
  <verify>Human reply with approval signal per resume-signal block.</verify>
  <done>User responds `approved` or `approved-with-notes`.</done>
  <what-built>
- tRPC `accSync` router with `getSyncFreshness` + `getActiveDeepSyncJob` procedures (server/routers/acc-sync.ts).
- Registered on `appRouter` as `accSync` (server/routers/root.ts).
- `SyncFreshnessPill` client component (components/layout/SyncFreshnessPill.tsx) — reads tRPC freshness, formats "Last synced: 2h ago" with date-fns, supports collapsed/expanded sidebar states, adaptive polling interval (15s when deep sync active, 5min otherwise).
- Mounted in `Sidebar.tsx` bottom border-t section, above the collapse button.
  </what-built>
  <how-to-verify>
1. **Start dev server:** `npm run dev`. Open the dashboard at http://localhost:3000.

2. **Sidebar expanded — pill is visible:** Look at the bottom of the left sidebar, just above the collapse button. You should see a small "Last synced: …" line. If Plan 03's release script was run locally, it should say "Last synced: a few moments ago" (or similar). If never run, it should say "Never synced".

3. **Sidebar collapsed — pill condenses to a dot:** Click the sidebar collapse button. The text should disappear and only a small colored dot should remain. Hover the dot — a tooltip should show the full status string.

4. **Status colors are distinguishable:**
   - Manually edit `SyncMeta` row `id="quick"` via `npx prisma studio`: set `lastStatus = "failed"`, `lastError = "test failure"`. Refresh the dashboard. The pill should turn red and read "Sync failed …". Hover tooltip should show "test failure".
   - Set back to `lastStatus = "success"`, `lastError = null`. Refresh. Pill should return to the success color.

5. **Polling cadence speeds up when a deep sync is "running":**
   - In `prisma studio`, insert a fake `AccDataConnectorJob` row with `status="running"`, `requestId="test-fast-poll"`, `startedAt=now()`.
   - Open the browser DevTools Network tab and filter for `trpc`. You should see `getSyncFreshness` requests firing roughly every 15 seconds (instead of the slower 5-minute idle cadence).
   - Delete the fake row.

6. **Restart survival (SYNC-03 acceptance):**
   - Stop the dev server (Ctrl+C). Restart `npm run dev`. The pill should still show the same last-sync time (sourced from Postgres, not memory).
  </how-to-verify>
  <resume-signal>
Reply with one of:
  - `approved` (pill renders correctly in expanded + collapsed states; failure color works; polling cadence shifts; restart-survives)
  - `approved-with-notes` + describe any visual nits to defer
  - `failed` + screenshot or browser console error
  </resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- `grep -c "accSync\\b" server/routers/root.ts` >= 1
- `grep -c "SyncFreshnessPill" components/layout/Sidebar.tsx` >= 1
- Human-verify checkpoint approved (covers all SYNC-03 acceptance: visible, restart-survives, polling 5-30s while active)
</verification>

<success_criteria>
- SYNC-03 satisfied: status visible in dashboard chrome, polls Postgres at 5-30s cadence while deep sync active, survives container restart
- No new dashboard tab/page created — pill lives in existing sidebar chrome (matches CONTEXT.md "no new tabs/pages/views" rule)
- Failure state visible at-a-glance, supplementing the email alert from Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-sync/01-04-SUMMARY.md` documenting:
- Sidebar collapsed-state prop name actually used (vs research guess)
- Final polling cadence chosen (default 15s active / 5min idle, or adjusted at human-verify)
- Any visual decisions deferred to a polish pass
</output>
