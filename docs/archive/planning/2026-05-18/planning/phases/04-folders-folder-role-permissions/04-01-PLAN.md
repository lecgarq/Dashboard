---
phase: 04-folders-folder-role-permissions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/acc/permissionMapping.ts
  - lib/acc/permissionMapping.test.ts
autonomous: true
requirements: [FLDR-03]
must_haves:
  truths:
    - "All 6 documented action sets map to the correct UI tier label"
    - "Extra actions beyond a tier match closest tier and flag extended=true"
    - "Missing actions round DOWN to highest tier whose actions are all present"
    - "Unknown actions log a warning and are ignored — mapping continues on remaining known actions"
    - "mapActions() never throws; null tier is returned when no tier matches"
  artifacts:
    - path: "lib/acc/permissionMapping.ts"
      provides: "TIER_DEFINITIONS constant + mapActions() pure function + PermTier type"
      exports: ["PermTier", "TIER_DEFINITIONS", "mapActions"]
    - path: "lib/acc/permissionMapping.test.ts"
      provides: "Vitest unit tests covering all 6 tiers + 3 edge cases"
      min_lines: 80
  key_links:
    - from: "lib/acc/permissionMapping.ts"
      to: "lib/acc/folderCrawl.ts (Plan 04)"
      via: "mapActions() called at ingest to compute permType + extended flag"
      pattern: "mapActions\\("
    - from: "lib/acc/permissionMapping.ts"
      to: "FolderPermissionsWidget (Plan 06)"
      via: "PermTier type imported for cell rendering + hover detail"
      pattern: "import.*PermTier.*permissionMapping"
---

<objective>
Create the pure permission-mapping module that powers Phase 4's cell semantics and orphan detection. This is the most testable, lowest-risk deliverable in the phase, and downstream tasks (crawl ingest + matrix widget) depend on its exports.

Purpose: Single source of truth for ACC `actions[]` → UI tier label, with explicit handling for the three edge cases locked in CONTEXT.md (extra/missing/unknown actions). Pure module, zero I/O, fully unit-testable.

Output: `lib/acc/permissionMapping.ts` (typed constant + `mapActions()`), `lib/acc/permissionMapping.test.ts` (Vitest suite covering all 6 tiers + 3 edge cases).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-folders-folder-role-permissions/04-CONTEXT.md
@.planning/phases/04-folders-folder-role-permissions/04-RESEARCH.md
@APS_DOCS/HOW TO/HOW_TO_Extract_Folder_Role_Permissions.md

<interfaces>
<!-- Wave 0 contract — downstream plans (04, 06) import these. -->

```typescript
// lib/acc/permissionMapping.ts (to be created by this plan)

export type PermTier =
  | "View Only"
  | "View+Download"
  | "Upload Only"
  | "View+Download+Upload"
  | "View+Download+Upload+Edit"
  | "Full Controller";

export const TIER_DEFINITIONS: ReadonlyArray<{
  tier: PermTier;
  actions: ReadonlySet<string>;
}>;

export interface MapActionsResult {
  tier: PermTier | null;
  extended: boolean;
  extendedActions: string[];
  unknownActions: string[];
}

export function mapActions(rawActions: string[]): MapActionsResult;
```

Tier order (high → low, used for round-down selection):
1. Full Controller — {VIEW, DOWNLOAD, COLLABORATE, PUBLISH, EDIT, CONTROL}
2. View+Download+Upload+Edit — {VIEW, DOWNLOAD, COLLABORATE, PUBLISH, EDIT}
3. View+Download+Upload — {VIEW, DOWNLOAD, COLLABORATE, PUBLISH}
4. Upload Only — {PUBLISH}
5. View+Download — {VIEW, DOWNLOAD, COLLABORATE}
6. View Only — {VIEW, COLLABORATE}

Known actions: VIEW, DOWNLOAD, COLLABORATE, PUBLISH, EDIT, CONTROL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write failing tests for all 6 tiers + 3 edge cases</name>
  <files>lib/acc/permissionMapping.test.ts</files>
  <action>
Create a Vitest test file covering, at minimum, these 9 groups:

**Tier matches (6 groups, one per tier):**
1. `["VIEW","COLLABORATE"]` → tier="View Only", extended=false
2. `["VIEW","DOWNLOAD","COLLABORATE"]` → tier="View+Download"
3. `["PUBLISH"]` → tier="Upload Only"
4. `["VIEW","DOWNLOAD","COLLABORATE","PUBLISH"]` → tier="View+Download+Upload"
5. `["VIEW","DOWNLOAD","COLLABORATE","PUBLISH","EDIT"]` → tier="View+Download+Upload+Edit"
6. `["VIEW","DOWNLOAD","COLLABORATE","PUBLISH","EDIT","CONTROL"]` → tier="Full Controller"

**Edge cases (3 groups):**
7. Extra actions: `["VIEW","DOWNLOAD","COLLABORATE","BONUS_ACTION"]` is actually an unknown case; instead use a synthetic future-known extra such as adding an action to the FULL set — easier: pass `["VIEW","COLLABORATE","DOWNLOAD","PUBLISH","SUPERPOWER"]` where SUPERPOWER is unknown. For TRUE extended (extra known action beyond tier), use a tier that has a strict subset relationship — but per the tier table, every tier-2 has all of tier-1's actions, so "extra" means actions present beyond what the matched tier requires. Test: pass `["VIEW","COLLABORATE","WEIRDKNOWN"]` — but to keep it pure, simpler: test that when actions exactly equal a tier, extended=false; when actions = tier ∪ {one unknown}, unknownActions=[unknown] and tier still matches (round-down). And when actions = HigherTier minus one EDIT, the result rounds down to next tier and `extendedActions` is empty. **Define "extended" precisely: extended=true means there is at least one KNOWN action in the input that is NOT part of the matched tier's required action set.** Since tiers are subset-chained (each higher tier ⊇ lower), the only way to get extended=true with known actions is if input has actions from a higher tier that don't combine to fully promote. Example: `["VIEW","COLLABORATE","EDIT"]` — best fit is "View Only" (needs VIEW+COLLABORATE), EDIT is extra known → tier="View Only", extended=true, extendedActions=["EDIT"].

8. Missing actions (round-down): `["VIEW","DOWNLOAD","COLLABORATE","PUBLISH","EDIT","CONTROL"]` minus EDIT = `["VIEW","DOWNLOAD","COLLABORATE","PUBLISH","CONTROL"]` → must round DOWN to "View+Download+Upload" (CONTROL is extended), NOT "Full Controller".

9. Unknown actions: `["VIEW","COLLABORATE","FROBNICATE"]` → tier="View Only", unknownActions=["FROBNICATE"], and `console.warn` is called. Use `vi.spyOn(console,"warn")` to assert the warning fired. Also test empty input `[]` → tier=null.

Use `describe`/`it` blocks per group. The file MUST exist before Task 2 attempts implementation. Run `npx vitest run lib/acc/permissionMapping.test.ts` — every test must FAIL with import error (module does not exist yet) — this is the RED state.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/permissionMapping.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|permissionMapping)" | head -5</automated>
  </verify>
  <done>Test file exists, contains ≥9 test groups (6 tiers + 3 edge cases + empty input), `vitest run` exits non-zero with "Cannot find module" or equivalent RED error.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement permissionMapping.ts to pass all tests</name>
  <files>lib/acc/permissionMapping.ts</files>
  <action>
Implement the pure module following the interface contract above:

1. Export `PermTier` literal union (6 strings).
2. Export `TIER_DEFINITIONS` ordered high→low (Full Controller first). Each entry: `{ tier: PermTier, actions: ReadonlySet<string> }`. Use `as const`.
3. Export `KNOWN_ACTIONS = new Set(["VIEW","DOWNLOAD","COLLABORATE","PUBLISH","EDIT","CONTROL"])` (internal const, no need to export).
4. Export `mapActions(rawActions: string[]): MapActionsResult`:
   - Partition input into `known` and `unknown` arrays by `KNOWN_ACTIONS.has()`.
   - If `unknown.length > 0`: `console.warn("[permission-mapping] Unknown ACC actions (ignored): " + unknown.join(", "))`. NOT `throw`.
   - Build `knownSet = new Set(known)`.
   - Iterate `TIER_DEFINITIONS` in order (highest first). The first tier whose `actions` are ALL present in `knownSet` is the match.
   - For matched tier: `extendedActions = known.filter(a => !tier.actions.has(a))`; `extended = extendedActions.length > 0`.
   - If no tier matches (e.g., empty input or only `CONTROL` alone): return `{ tier: null, extended: false, extendedActions: [], unknownActions: unknown }`.

Re-run `npx vitest run lib/acc/permissionMapping.test.ts` — every test must PASS (GREEN). Fix the implementation, not the tests, if anything fails.

DO NOT introduce React, DB, or I/O. This module is pure — no imports beyond TypeScript built-ins.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/permissionMapping.test.ts</automated>
  </verify>
  <done>All test groups pass. File exports `PermTier`, `TIER_DEFINITIONS`, `mapActions`. No external imports. `npx vitest run lib/acc/permissionMapping.test.ts` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/acc/permissionMapping.test.ts` exits 0 with all groups passing.
- `grep -E "^export (type|const|function)" lib/acc/permissionMapping.ts` shows PermTier, TIER_DEFINITIONS, mapActions.
- No `import.*from.*(prisma|react|next)` in permissionMapping.ts (pure module).
</verification>

<success_criteria>
- FLDR-03 fully satisfied (mapping table + edge-case behavior + unit tests).
- Module ready for consumption by Plan 04 (crawl ingest) and Plan 06 (widget).
- Test file becomes the regression net for any future ACC action-set change.
</success_criteria>

<output>
After completion, create `.planning/phases/04-folders-folder-role-permissions/04-01-SUMMARY.md` recording the exact tier definitions used (so Plans 04/06 can reference without re-reading source).
</output>
