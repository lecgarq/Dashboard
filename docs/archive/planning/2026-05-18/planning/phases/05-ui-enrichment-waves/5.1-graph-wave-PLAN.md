---
phase: 05-ui-enrichment-waves
plan: 5.1-graph-wave
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/AccUsersGraph.tsx
  - app/(dashboard)/users/accGraphFilters.ts
  - app/(dashboard)/users/accGraphFilters.test.ts
  - app/(dashboard)/users/adminTierShape.ts
  - app/(dashboard)/users/adminTierShape.test.ts
  - app/(dashboard)/users/folderCluster.ts
  - app/(dashboard)/users/folderCluster.test.ts
  - app/(dashboard)/users/dashboard/selectionContext.tsx
  - app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  - server/routers/acc-graph.ts
  - server/routers/acc-members.ts
  - server/routers/root.ts
  - components/ui/tooltip.tsx
  - lib/acc/acc-types.ts
autonomous: false
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, DASH-18]

must_haves:
  truths:
    - "`accMembers.enrichedUsers` tRPC procedure exists in `server/routers/acc-members.ts`, registered in `server/routers/root.ts`, and surfaces aggregated v2.0 fields (status, projectAdmin, executive, companyName, perProjectRoleNames, isAccountAdmin) per email; consumed by DashboardClient and merged into BulkAccUser arrays without modifying bulkAccSummary"
    - "Hovering a user node shows a rich tooltip card (name, status, company, accessLevels, last sign-in, last file activity) without click"
    - "Per-project role filter group appears in graph filter sidebar and AND-intersects with status/module facets"
    - "Toolbar 'Show admin tiers' toggle (OFF default) renders hub admins as star + gold halo + 1.5x size, project admins as diamond, executives as circle-with-ring"
    - "If Phase 4 GO: folder nodes render in a separate cluster region with user->folder edges thin+translucent color-coded by permission tier; clicking a folder opens side-panel folder detail and spotlights connected users"
    - "If Phase 4 NO-GO: GRAPH-04 is documented as deferred contingency in the phase SUMMARY and folder behaviour is fulfilled exclusively via DASH wave matrix fallback"
    - "Every modified graph affordance honours the DASH-18 interactivity contract: hover detail, click-through to side panel, cross-widget spotlight"
    - "Label polish curve (pow(zoom,0.2) clamped [0.85,1.4], pill backgrounds, 11px floor / 18px ceiling) is unchanged"
    - "60 FPS hard floor preserved with all new overlays + filters active on UAT hardware"
  artifacts:
    - path: "app/(dashboard)/users/adminTierShape.ts"
      provides: "Pure mapper from BulkAccUser admin flags -> {tier, shape, size, haloColor}"
      exports: ["adminTierFor", "AdminTier"]
    - path: "app/(dashboard)/users/adminTierShape.test.ts"
      provides: "Vitest coverage for hub/project/executive tier classification + sizing"
    - path: "app/(dashboard)/users/accGraphFilters.ts"
      provides: "perProjectRoles[] dimension AND-intersected with existing filters"
      contains: "perProjectRoles"
    - path: "app/(dashboard)/users/accGraphFilters.test.ts"
      provides: "Vitest coverage for perProjectRoles intersection (4 combinations minimum: alone, +status, +module, +status+module)"
    - path: "server/routers/acc-graph.ts"
      provides: "perProjectRoleFacets query + (Phase4-GO) folder cluster data query"
      exports: ["accGraphRouter"]
    - path: "server/routers/acc-members.ts"
      provides: "enrichedUsers procedure — bridges AccProjectMember -> per-email aggregated v2.0 fields (status, projectAdmin, executive, companyName, perProjectRoleNames)"
      exports: ["accMembersRouter"]
    - path: "server/routers/root.ts"
      provides: "Registers accGraph + accMembers namespaces (NOT _app.ts — verified file is root.ts)"
    - path: "components/ui/tooltip.tsx"
      provides: "shadcn Radix tooltip primitive added if missing"
    - path: "app/(dashboard)/users/AccUsersGraph.tsx"
      provides: "Hover tooltip card, toolbar admin-tier toggle, role sidebar group, folder cluster (conditional)"
    - path: "app/(dashboard)/users/dashboard/selectionContext.tsx"
      provides: "SelectedFinding union extended with kind:'folder', kind:'adminTier'"
  key_links:
    - from: "AccUsersGraph.tsx hover handler"
      to: "Tooltip card content"
      via: "hoveredNode state -> Tooltip.Content render"
      pattern: "hoveredNode"
    - from: "AccUsersGraph.tsx toolbar toggle"
      to: "cosmos.gl pointSizes / pointColors / pointShapes"
      via: "setPoints({...}) with regenerated typed arrays (not in-place mutation)"
      pattern: "setPoints"
    - from: "accGraphFilters.ts perProjectRoles"
      to: "filtered node set"
      via: "AND-intersection with existing facets"
      pattern: "perProjectRoles"
    - from: "Folder node click (Phase 4 GO only)"
      to: "DashboardSidePanel folder body"
      via: "setSelected({ kind: 'folder', folderUrn })"
      pattern: "kind:\\s*['\"]folder['\"]"
---

<objective>
Surface v2.0 ACC data through the cosmos.gl spatial graph: hover-detail tooltip card, per-project role filter, 3-tier admin overlay, and (conditional on Phase 4 GO) folder-node cluster. Bake the DASH-18 interactivity contract into every affordance from task 1; no retrofits.

Purpose: GRAPH-01..04 + DASH-18 (graph slice) per ROADMAP Phase 5 + 05-CONTEXT locked decisions.
Output: An enriched AccUsersGraph that ships hover tooltip, role filter sidebar group, admin-tier toggle, and a Phase-4-conditional folder cluster — all wired to the existing selectionContext + DashboardSidePanel.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-enrichment-waves/05-CONTEXT.md
@.planning/phases/05-ui-enrichment-waves/05-RESEARCH.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/accGraphFilters.ts
@app/(dashboard)/users/dashboard/selectionContext.tsx
@app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
@server/routers/root.ts
@lib/acc/acc-types.ts
@prisma/schema.prisma

<interfaces>
<!-- Key contracts. Executor: do NOT explore the codebase for these — use them directly. -->

From app/(dashboard)/users/dashboard/selectionContext.tsx (current union — extend it):
```typescript
export type SelectedFinding =
  | { kind: "junk"; finding: JunkRoleFinding }
  | { kind: "duplicate"; finding: DuplicateRoleFinding }
  | { kind: "outlier"; finding: OutlierFinding }
  | { kind: "role"; role: string; severity: Severity | undefined }
  | { kind: "admin"; email: string }
  | { kind: "day"; dateIso: string; emails: string[] }
  | { kind: "userActivity"; email: string }
  | null;
```

Phase 5 additions for this plan:
```typescript
  | { kind: "folder"; folderUrn: string }            // GRAPH-04 + DASH matrix fallback
  | { kind: "adminTier"; tier: "hub" | "project" | "executive" }   // toolbar/legend click
```

From prisma/schema.prisma AccProjectMember (verified):
```
isAccountAdmin Boolean (HQ-level — used to identify HUB admin tier)
projectAdmin   Boolean (accessLevels.projectAdmin)
executive      Boolean (accessLevels.executive)
status         String  (per-project)
```

Cosmos.gl alpha inversion (memory): `getSimulationAlpha()` in beta.9 returns `1 - progress`. Existing gate lives at AccUsersGraph.tsx line ~1414 — mirror that pattern; do NOT re-invert.

**Cosmos.gl PointShape enum (VERIFIED from `node_modules/@cosmos.gl/graph/dist/index.d.ts:163-164` and `index.js:287` — beta.9):**
```
0 = Circle, 1 = Square, 2 = Triangle, 3 = Diamond, 4 = Pentagon, 5 = Hexagon, 6 = Star, 7 = Cross, 8 = None
```
Use **3 (Diamond)** for project admin and **6 (Star)** for hub admin in `adminTierShape.ts`. Executive "circle-with-ring" = shape 0 (Circle) + a halo ring rendered as a second draw pass or pre-baked sprite (no native "ring" enum). No Context7 lookup needed at execution time.

**Data bridge (Pitfall 1 — HIGH cost if missed):** `bulkAccSummary` reads ONLY from `accMemberCache`. It does NOT touch `AccProjectMember`. Phase 5 v2.0 fields (status, projectAdmin, executive, companyName, products, perProjectRoleNames) MUST come through a NEW `accMembers.enrichedUsers` tRPC procedure created in Task 1. DO NOT extend `bulkAccSummary` — it serves the graph snapshot path. Use the additive merge pattern: `DashboardClient.tsx` runs both queries in parallel and merges on email. New `BulkAccUser` fields are all OPTIONAL.

**HOVER_OPACITY_DIM decision (locked by planner — Pitfall 4):** Keep `HOVER_OPACITY_DIM = 0.4` as-is in `dashboardTokens.ts`. CONTEXT's "~20%" is treated as approximate. Rationale: the token is shared with 3 existing dashboard widgets (AdminAccess, OutlierCombos, Recommendations) that passed UAT 2026-05-08 at 0.4. Changing to 0.2 would force a full re-UAT of unrelated widgets. Graph-side spotlight uses the same 0.4 for consistency.

Label polish curve (UAT-approved 2026-05-08, MUST NOT regress):
- size = pow(zoom, 0.2) clamped [0.85, 1.4]
- pill backgrounds
- 11px floor / 18px ceiling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — DATA BRIDGE (accMembers.enrichedUsers) + install tooltip + scaffold test files + extend SelectedFinding union</name>
  <files>
    server/routers/acc-members.ts
    server/routers/root.ts
    lib/acc/acc-types.ts
    components/ui/tooltip.tsx
    app/(dashboard)/users/adminTierShape.ts
    app/(dashboard)/users/adminTierShape.test.ts
    app/(dashboard)/users/accGraphFilters.test.ts
    app/(dashboard)/users/folderCluster.ts
    app/(dashboard)/users/folderCluster.test.ts
    app/(dashboard)/users/dashboard/selectionContext.tsx
  </files>
  <action>
**FIRST — DATA BRIDGE (Pitfall 1 mitigation, REQUIRED for every Wave 5.1 widget downstream):**

Create `server/routers/acc-members.ts` exporting `accMembersRouter` with the `enrichedUsers` protected query:
```typescript
enrichedUsers: protectedProcedure.query(async ({ ctx }) => {
  const members = await ctx.db.accProjectMember.findMany({
    where: { project: { status: "active" } },
    select: {
      email: true, status: true, projectAdmin: true, executive: true,
      companyName: true, products: true, addedOn: true, lastSignIn: true, isAccountAdmin: true,
      project: { select: { id: true, name: true } },
      projectRoles: { select: { role: { select: { name: true } } } },
    },
  });
  // Reduce per email: any-active wins; collect perProjectRoleNames as distinct set.
  // Returns: { email, aggregatedStatus, projectAdmin, executive, isAccountAdmin, companyName,
  //   perProjectRoleNames: string[], perProjectStatuses: { projectId, projectName, status, projectAdmin }[] }[]
  return aggregateByEmail(members);
})
```
Graceful empty state: zero rows → empty array (NOT null). Add `staleTime: 5 * 60 * 1000` on the consumer side.

Register `accMembers` namespace in `server/routers/root.ts` (NOT `_app.ts` — the file is `root.ts`, verified). Also pre-register the `accGraph` namespace import (router itself created in Task 2).

Extend `lib/acc/acc-types.ts` `BulkAccUser` interface with OPTIONAL Phase 5 fields (Pitfall 5 — must be `?` to avoid breaking 15+ consumer files):
```typescript
aggregatedStatus?: "active" | "pending" | "deleted";
projectAdmin?: boolean;
executive?: boolean;
isAccountAdmin?: boolean; // (likely already present — verify)
companyName?: string | null;
perProjectRoleNames?: string[];
```

In `DashboardClient.tsx` (NO changes to `bulkAccSummary`!), run `trpc.accMembers.enrichedUsers.useQuery(undefined, { staleTime: 300_000 })` in parallel with `bulkAccSummary` and merge on email. Missing emails → fields stay undefined. If `DashboardClient.tsx` editing is out of this task's file list, add it — this merge is load-bearing for Tasks 2-4.

**SECOND — Tooltip primitive + helper scaffolding:**

Install shadcn Radix tooltip primitive: `npx shadcn@latest add tooltip` (skip if components/ui/tooltip.tsx already exists). Verify component exports `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`.

Create `app/(dashboard)/users/adminTierShape.ts` exporting:
- `type AdminTier = "hub" | "project" | "executive" | "none"`
- `interface AdminTierVisual { tier: AdminTier; shape: "star" | "diamond" | "ring" | "circle"; sizeMultiplier: number; haloColor: string | null }`
- `function adminTierFor(user: { isAccountAdmin?: boolean; projectAdmin?: boolean; executive?: boolean }): AdminTierVisual`

Classification rules (CONTEXT locked + cosmos enum verified):
- isAccountAdmin === true → tier:"hub", shape:"star" (cosmos enum **6**), sizeMultiplier:1.5, haloColor:"#FFD700" (gold)
- else projectAdmin === true → tier:"project", shape:"diamond" (cosmos enum **3**), sizeMultiplier:1.0, haloColor:null
- else executive === true → tier:"executive", shape:"ring" (cosmos enum **0** Circle + halo ring overlay — no native ring shape), sizeMultiplier:1.0, haloColor:null
- else → tier:"none", shape:"circle" (cosmos enum **0**), sizeMultiplier:1.0, haloColor:null

Also export `function adminTierShapeEnum(tier: AdminTier): number` returning the cosmos.gl PointShape integer for `setPointShapes` Float32Array fill: hub→6, project→3, executive→0, none→0.
Precedence: hub > project > executive > none (a user that is both hub admin AND project admin renders as hub).

Create `app/(dashboard)/users/adminTierShape.test.ts` with Vitest cases covering:
- pure hub admin → star/1.5/gold
- pure project admin → diamond/1.0/null
- pure executive → ring/1.0/null
- non-admin → circle/1.0/null
- hub+project (precedence) → star/1.5/gold
- project+executive (precedence) → diamond/1.0/null

Create `app/(dashboard)/users/folderCluster.ts` (skeleton — pure helpers, will be fleshed out in Task 4 if Phase 4 GO):
- `interface FolderClusterPosition { x: number; y: number; z: number }`
- `function folderInitialPosition(urn: string, index: number, total: number): FolderClusterPosition` — places folders in an offset region (e.g. +800,+800 world space) deterministically by index, spiral layout
- `function permissionTierEdgeColor(actions: string[]): [r: number, g: number, b: number, a: number]` — maps the 6 documented permission combinations to color+alpha (View Only / View+Download / Upload Only / View+Download+Upload / View+Download+Upload+Edit / Full Controller). Thin (a≈0.25) translucent baseline.

Create `app/(dashboard)/users/folderCluster.test.ts` with Vitest cases:
- folderInitialPosition determinism (same index/urn → same coords)
- folderInitialPosition offset is non-overlapping with default user cluster origin (|x|+|y| > 600 baseline)
- permissionTierEdgeColor returns 4 distinct colors for the 4 listed tiers (View / Download / Edit / Full); alpha within [0.2, 0.45]

Extend `app/(dashboard)/users/dashboard/selectionContext.tsx`:
- Add `| { kind: "folder"; folderUrn: string }` to SelectedFinding
- Add `| { kind: "adminTier"; tier: "hub" | "project" | "executive" }` to SelectedFinding
- Update `isSelectionValid()` so `adminTier` is always valid; `folder` validates when folderUrn is non-empty (Phase 4 will own the join-against-real-folders refinement).

Add stub render branches in `app/(dashboard)/users/dashboard/DashboardSidePanel.tsx` for `kind === "folder"` and `kind === "adminTier"`:
- folder body: placeholder card "Folder: {folderUrn}" with note "Full body wired in Task 4 (Phase 4 GO) or via DASH matrix in Wave 5.3" — keeps the runtime contract closed.
- adminTier body: lists users in that tier from BulkAccUser via existing context.
Both bodies must call `setSelected(null)` on close button click (consistent with existing bodies).

DO NOT modify the AccUsersGraph render path in this task. The `accGraph` router and `perProjectRoleFacets` are scaffolded in Task 2 — only `accMembers.enrichedUsers` ships in this task.

Per-task verification: `npm test -- --run adminTierShape folderCluster` — both test files PASS. Manual smoke: dev server starts, `trpc.accMembers.enrichedUsers` returns array (may be empty if Pitfall 10 — accProjectMember not yet synced — that's acceptable).
  </action>
  <verify>
    <automated>npm test -- --run adminTierShape folderCluster</automated>
  </verify>
  <done>tooltip primitive present, adminTierShape + folderCluster modules + tests green, SelectedFinding union extended with folder/adminTier, side-panel stubs render without crash.</done>
</task>

<task type="auto">
  <name>Task 2: GRAPH-02 hover tooltip card + GRAPH-01 per-project role filter (data + render + hover + click + spotlight in one slice)</name>
  <files>
    server/routers/acc-graph.ts
    server/routers/root.ts
    app/(dashboard)/users/accGraphFilters.ts
    app/(dashboard)/users/accGraphFilters.test.ts
    app/(dashboard)/users/AccUsersGraph.tsx
  </files>
  <action>
Implement two requirements as one vertical slice to keep the interactivity contract intact.

1) Server: create `server/routers/acc-graph.ts` exporting `accGraphRouter` with:
- `perProjectRoleFacets`: protected query returning `{ name: string; memberCount: number }[]` sorted by name. Query `AccProjectRole` joined to `AccRole`, distinct on roleId, exclude memberId=null (no unassigned-default rows). `staleTime` consumer side = 5 min.
Register `accGraph` namespace in `server/routers/root.ts` (file is `root.ts`, not `_app.ts` — verified).

2) Filters: extend `app/(dashboard)/users/accGraphFilters.ts`:
- Add `perProjectRoles: string[]` to the filter state shape (empty = no constraint).
- Apply AND-intersection: a node passes only if `perProjectRoles.length === 0` OR the node's set of per-project role names contains at least one selected.
- Source of per-node role names: `BulkAccUser.perProjectRoleNames` (already added as optional in Task 1, populated by `accMembers.enrichedUsers` and merged into the user array client-side in `DashboardClient.tsx`). **Do NOT extend `bulkAccSummary`** — Pattern 6 in RESEARCH locks this. Filter logic must treat `perProjectRoleNames === undefined` as empty (graceful degradation if enrichedUsers query is in flight or empty).

3) Extend `app/(dashboard)/users/accGraphFilters.test.ts` (existing file) with at minimum:
- perProjectRoles alone reduces set correctly
- perProjectRoles AND status (both must match)
- perProjectRoles AND module (both must match)
- perProjectRoles AND status AND module (all three must match)
- empty perProjectRoles is a no-op (does not change result vs without perProjectRoles)

4) UI: in `AccUsersGraph.tsx`
- Add a collapsible "Role" group in the existing filter sidebar, alongside status/module. Use the existing collapsible pattern in this file — do NOT introduce a new pattern. Populate from `trpc.accGraph.perProjectRoleFacets.useQuery(undefined, { staleTime: 300_000 })`. Each row: checkbox + role name + count badge.
- Hover tooltip card (GRAPH-02): import `Tooltip*` from `components/ui/tooltip.tsx`. Add `[hoverEmail, setHoverEmail] = useState<string | null>(null)` and `[hoverPos, setHoverPos] = useState<{x:number;y:number}|null>(null)`. Wire to the existing cosmos.gl pointer-over handler (do NOT add a second hover listener — find the existing one). Render an invisible 1×1 div anchored at `hoverPos` as the Tooltip.Trigger; the Tooltip.Content shows: name, email, status pill, companyName, accessLevels (projectAdmin / executive booleans), last sign-in (use existing `fmtRelative` if available, else write inline), last file activity (lazy via `trpc.accActivity.getLastFileActivity.useQuery({email}, {enabled: !!hoverEmail, staleTime: 300_000})` — reuse the Phase 03-03 procedure; **never eager-load** into bulkAccSummary).
- Color encoding is never the sole signal: status pill carries the literal text ("Active" / "Pending" / "Deleted"), accessLevels carry text badges, never bare color swatches.

5) Cross-widget spotlight: when a per-project role checkbox is toggled, dim non-matching nodes via the existing `pointGreyoutOpacity` mechanism (find current call site in AccUsersGraph.tsx and reuse). Clicking a tooltip's role badge calls `setSelected({ kind: 'role', role: <name>, severity: undefined })` to cross-spotlight other dashboard widgets (existing kind).

Constraint reminders:
- DO NOT mutate cosmos.gl point arrays in place; recreate the typed array and call `setPoints({...})` (Pattern 2 in RESEARCH).
- DO NOT regress the label polish curve.
- DO NOT add a Refresh / Sync All button anywhere.

Per-task verification: `npm test -- --run accGraphFilters` — extended test file PASSES; running `npm run typecheck` (or `tsc --noEmit`) reports no new errors in modified files.
  </action>
  <verify>
    <automated>npm test -- --run accGraphFilters</automated>
  </verify>
  <done>perProjectRoleFacets query live, sidebar role group renders with counts and AND-intersects, hover tooltip card shows enriched detail using lazy file-activity fetch, no in-place point-array mutation, label curve unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: GRAPH-03 3-tier admin overlay (toolbar toggle, shape/halo/size, URL-param persisted)</name>
  <files>
    app/(dashboard)/users/AccUsersGraph.tsx
    app/(dashboard)/users/adminTierShape.ts
    lib/acc/acc-types.ts
  </files>
  <action>
**Cosmos.gl PointShape enum is ALREADY VERIFIED** (see context block above and `node_modules/@cosmos.gl/graph/dist/index.d.ts:163-164`): 0=Circle, 1=Square, 2=Triangle, 3=Diamond, 4=Pentagon, 5=Hexagon, 6=Star, 7=Cross, 8=None. Use `adminTierShapeEnum()` from Task 1. No Context7 lookup needed.

The `setPointShapes(Float32Array)` API is already used at `graphRenderers.ts:734, 1352-1367`. Mirror that call site's pattern. For halo/ring (hub + executive tiers), use a second draw pass via cosmos.gl's existing color buffer modulation OR a pre-baked sprite atlas — both already employed in `graphRenderers.ts`. Avoid introducing a new uniform.

1) `BulkAccUser` already has `isAccountAdmin?`, `projectAdmin?`, `executive?` (added in Task 1 as optional). Source data flows in via `accMembers.enrichedUsers` merged in `DashboardClient.tsx`. NO new Prisma changes here.

2) In `AccUsersGraph.tsx`:
- Add a toolbar toggle button "Show admin tiers" — OFF by default. Persist state via URL search param (mirror the existing `writeGraphDisplayMode` pattern; do NOT introduce localStorage).
- When ON: for each node, compute `adminTierFor(user)` from `adminTierShape.ts` and regenerate three typed arrays (`pointSizes`, `pointColors`, `pointShapes`). Use `adminTierShapeEnum(tier)` to fill the shapes Float32Array (hub→6 Star, project→3 Diamond, executive→0 Circle, none→0 Circle). Hub admins: star shape + gold halo + 1.5× size. Project admins: diamond. Executives: circle-with-halo-ring. Non-admins: unchanged.
- Halo (hub gold + executive ring): no native halo uniform in beta.9. Render via the existing color-buffer modulation pattern from `graphRenderers.ts` OR a slightly larger pre-baked sprite. Prefer color-buffer modulation (already proven in repo).
- When OFF: restore baseline arrays. Always use `setPoints({...})` with newly-allocated typed arrays — no in-place mutation.
- Add a small legend overlay (text + glyph) when toggle is ON: "★ Hub admin · ◆ Project admin · ◯ Executive". Color is paired with shape; shape is the load-bearing signal.

3) DASH-18 contract for this toggle:
- Hover any admin-tier node → tooltip from Task 2 already covers detail (no regression).
- Click any admin-tier glyph → `setSelected({ kind: 'adminTier', tier: <hub|project|executive> })`. DashboardSidePanel adminTier body (from Task 1 stub) now lists matching users with link to existing user side-panel detail.
- Cross-widget spotlight: when adminTier is selected, dim non-matching nodes via `pointGreyoutOpacity` (reuse mechanism from Task 2). AdminConstellation widget in Wave 5.3 will mirror this — no widget edits in this plan.

4) Gating: mirror the existing simulation-alpha gate at line ~1414. Do NOT re-invert (cosmos.gl beta.9 alpha is already inverted; memory locked).

5) Performance: profile the toggle with React DevTools and the in-app perf overlay (if present). If toggle ON drops below 60 FPS on a hub of ~3,000 nodes, reduce halo to a single pre-baked sprite atlas (no per-point uniform) and re-measure. Document the chosen technique in the SUMMARY.

Per-task verification: `npm test -- --run adminTierShape` still green (no regression).
  </action>
  <verify>
    <automated>npm test -- --run adminTierShape</automated>
  </verify>
  <done>Toolbar toggle persists via URL, three admin tiers visually distinct via SHAPE (not color alone), legend present, click sets adminTier selection, cross-widget spotlight works, 60 FPS maintained.</done>
</task>

<task type="auto">
  <name>Task 4: GRAPH-04 dependency-check + folder cluster (CONDITIONAL on Phase 4 GO/NO-GO)</name>
  <files>
    .planning/phases/05-ui-enrichment-waves/5.1-graph-wave-PHASE4-GATE.md
    server/routers/acc-graph.ts
    app/(dashboard)/users/folderCluster.ts
    app/(dashboard)/users/folderCluster.test.ts
    app/(dashboard)/users/AccUsersGraph.tsx
    app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  </files>
  <action>
This task is the LAST item in Wave 5.1 — DO NOT block earlier tasks on it. Two-step gate.

STEP A — Dependency check (always run, ~2 minutes):
- Inspect `.planning/phases/04-folders-and-folder-role-permissions/` (or whatever the Phase 4 directory is named). Look for:
  - A SUMMARY.md or VERIFICATION.md indicating perf pre-flight result (GO or NO-GO).
  - A `accFolder` tRPC router exposing folder detail (path, roles, member counts, orphan flags) — likely at `server/routers/acc-folder.ts`.
- Write `5.1-graph-wave-PHASE4-GATE.md` capturing: phase-4 status (complete / in-flight / not-started), perf result (GO / NO-GO / not-yet-decided), router availability (yes / no — and exact procedure name if yes).

STEP B — Branch on result:

CASE 1 — Phase 4 GO confirmed, accFolder.getDetail or equivalent exists:
- Add `accGraph.folderClusterNodes` query to `server/routers/acc-graph.ts` returning `{ urn: string; path: string; orphanRoleCount: number; userEmails: string[]; permissionTierByEmail: Record<string, "view"|"download"|"edit"|"full"> }[]`. Aggregate from `AccFolder` + `AccFolderPermission` + `AccProjectRole`. Cap edges to "most-restricted permission tier only" per user-folder pair (RESEARCH Pitfall 4 mitigation).
- In `AccUsersGraph.tsx`: gated by a NEW toolbar toggle "Show folders" (OFF by default, URL-param persisted). When ON: append folder nodes using `folderInitialPosition(urn, index, total)` from Task 1; append edges using `permissionTierEdgeColor(...)`. Folder nodes: distinct shape (e.g. square), distinct base color, slightly larger than user-baseline. Orphan-role folders: amber halo (use existing color token if present; CONTEXT: subtle amber).
- Click folder node → `setSelected({ kind: 'folder', folderUrn })`. In `DashboardSidePanel.tsx` flesh out the folder body from Task 1 stub: render path (breadcrumb via slash-split), role table (name | members assigned | orphan flag), and a "spotlight users with access" button that highlights connected users in the graph via `setHighlightedEmails(folderData.userEmails)` (or equivalent — reuse existing spotlight setter).
- Performance: confirm 60 FPS on UAT hardware with folders ON + admin tiers ON + filters active. If below, halve folder edge count (top-permission-tier only) and re-measure.
- Extend `folderCluster.test.ts` if new helpers landed.

CASE 2 — Phase 4 NO-GO (perf rejected folders in graph):
- Do NOT add the "Show folders" toggle. Do NOT add folder cluster to AccUsersGraph.tsx.
- In `5.1-graph-wave-PHASE4-GATE.md` document the contingency: "GRAPH-04 deferred per Phase 4 NO-GO. Folder surface delivered exclusively via DASH wave matrix + folder-detail side-panel body (Wave 5.3)."
- Confirm the `kind: "folder"` selection branch in `DashboardSidePanel.tsx` is still wired (will be used by the DASH matrix in Wave 5.3) — keep the Task 1 stub but mark it "Phase-5.3 owns the body content".
- Append to phase SUMMARY: GRAPH-04 not shipped; fallback contract logged; contingency for future v2.x rendering revisit.

CASE 3 — Phase 4 not complete yet at execution time:
- HUMAN CHECKPOINT (see Task 5). Ask the user to confirm whether to (a) wait for Phase 4 to land, (b) ship Wave 5.1 without GRAPH-04 and revisit later, or (c) assume GO and proceed. Do NOT make this call autonomously.

Constraints applying to all cases:
- DO NOT regress the label polish curve.
- DO NOT introduce manual sync UI.
- DO NOT mutate cosmos.gl point arrays in place.

Per-task verification: `npm test -- --run folderCluster` (CASE 1 & 3-then-CASE 1) — all tests pass; CASE 2 — folderCluster tests still pass (helpers remain dead code, retained for v2.x revisit).
  </action>
  <verify>
    <automated>npm test -- --run folderCluster</automated>
  </verify>
  <done>5.1-graph-wave-PHASE4-GATE.md exists and documents the case; if CASE 1: folder nodes render in offset cluster with permission-tier edges, click opens folder side panel, orphan halo visible, 60 FPS maintained; if CASE 2: contingency logged, no graph regression; if CASE 3: checkpoint reached.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT — DASH-18 interactivity contract gate for Wave 5.1</name>
  <what-built>
GRAPH-01 (per-project role filter sidebar group, AND-intersecting), GRAPH-02 (hover tooltip card with enriched detail), GRAPH-03 (3-tier admin overlay toggle with shape+halo+size), and GRAPH-04 (folder cluster — conditional on Phase 4 GO).
  </what-built>
  <how-to-verify>
Run the dev server. Navigate to /users. Then:

1. Hover any user node — confirm tooltip card shows: name, email, status pill (with literal text), company, projectAdmin/executive badges (text + color), last sign-in, last file activity. Tooltip should appear within ~150ms, not require a click. Verify the label polish curve (zoom in/out — labels stay between 11px and 18px, pill backgrounds intact).

2. Open the filter sidebar — confirm a new "Role" collapsible group lists per-project roles with member counts. Toggle one role; confirm node count drops (AND-intersect). Toggle a status facet ON top — confirm result is intersection (both constraints applied).

3. Click toolbar "Show admin tiers" — confirm hub admins render as star + gold halo + ~1.5× size; project admins as diamond; executives as circle-with-ring. Toggle OFF — graph returns to baseline. Click an admin-tier glyph — confirm the side panel opens with the matching tier's user list. Open another dashboard widget tab and confirm cross-widget dimming respects the selection.

4. If Phase 4 GO: toggle "Show folders" — confirm folder nodes appear in a visibly separate cluster region (not interleaved with users). Edges between users and folders are thin, translucent, color-varying by permission tier. Click a folder — side panel shows folder path, role table, orphan-role flag if any. The "spotlight users with access" button dims non-connected nodes.
   If Phase 4 NO-GO: confirm `5.1-graph-wave-PHASE4-GATE.md` documents the NO-GO outcome and that no "Show folders" toggle is present in the graph.

5. FPS check: in the in-app perf overlay (if available) or via browser performance profiler, confirm ≥60 FPS with admin tiers ON + a role filter active + (if GO) folders ON. If below 60 FPS, this task FAILS regardless of visual correctness.

6. Confirm: no Refresh button, no Sync All button, no new manual-sync UI surfaced anywhere.

7. Confirm: no widget regresses the label polish curve.

Type "approved" when all 7 checks pass, OR describe specific failures (e.g. "FPS dropped to 42 with folders ON" or "executive tier shape is wrong").
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --run adminTierShape accGraphFilters folderCluster` — all three suites green.
2. `npm run typecheck` (or `tsc --noEmit`) — no new errors in files_modified.
3. Manual UAT (Task 5) — all 7 checks pass.
4. Grep `selectionContext.tsx` confirms `kind: "folder"` and `kind: "adminTier"` branches present.
5. Grep `AccUsersGraph.tsx` does NOT show in-place mutation of pointColors/pointSizes/pointShapes arrays (look for assignment to existing array elements without setPoints call).
</verification>

<success_criteria>
- All 5 must_haves truths observable.
- GRAPH-01, GRAPH-02, GRAPH-03 fully shipped; GRAPH-04 either shipped (Phase 4 GO) or formally deferred via gate doc (Phase 4 NO-GO).
- DASH-18 interactivity contract (hover → click → spotlight) satisfied for every graph affordance.
- 60 FPS hard floor preserved.
- Label polish curve unchanged.
- No manual sync UI introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-enrichment-waves/5.1-graph-wave-SUMMARY.md` documenting: which case Task 4 took (1/2/3), Context7-verified cosmos.gl per-point API, FPS measurement under the worst-case overlay combination, and any decisions deferred to v2.x.
</output>
