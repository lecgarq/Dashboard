---
phase: 05-ui-enrichment-waves
plan: 5.2-list-wave
type: execute
wave: 2
depends_on: [5.1-graph-wave]
# NOTE: depends_on 5.1 because 5.1 ships the `accMembers.enrichedUsers` data bridge AND the optional BulkAccUser fields. Per-CONTEXT, LIST wave doesn't need 5.1 GRAPH UI — only the data bridge contract.
files_modified:
  - app/(dashboard)/users/UsersDirectoryClient.tsx
  - app/(dashboard)/users/AccUserSidePanel.tsx
  - app/(dashboard)/users/accStatusReduction.ts
  - app/(dashboard)/users/accStatusReduction.test.ts
  - app/(dashboard)/users/productsTierMap.ts
  - app/(dashboard)/users/productsTierMap.test.ts
  - app/(dashboard)/users/useVisibleRowEmails.ts
  - lib/acc/acc-types.ts
  - server/routers/acc-activity.ts
  - server/routers/acc-members.ts
  - components/ui/tooltip.tsx
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04, DASH-18]

must_haves:
  truths:
    - "List consumes `BulkAccUser.aggregatedStatus` / `projectAdmin` / `executive` / `companyName` populated by `accMembers.enrichedUsers` from Wave 5.1 — bulkAccSummary is unmodified, ACTV-03 lazy contract preserved, products fetched per-user via new `accMembers.getProductsForUser` lazy procedure"
    - "User list shows a `status` column with active/pending/deleted pill (text + color) reduced from per-project statuses by 'any-active wins' rule"
    - "Status multi-select facet exists in the toolbar and AND-intersects with existing filters"
    - "User list shows a small inline 'Admin' pill next to user name when projectAdmin is true on any project; matching filter facet present"
    - "Last-file-activity column lazy-loads on row visibility (IntersectionObserver), shows skeleton shimmer while loading, em-dash with tooltip 'No activity in 90d' when empty"
    - "Side panel shows a collapsible 'Module Access' section listing each ACC module (Docs, Build, Design Collaboration, etc.) with its tier (administrator / member / none) — never raw JSON"
    - "Every list affordance satisfies DASH-18 contract: hover detail (tooltip on empty/admin pills), click-through (row click opens side panel; status/admin pill click filters to that facet), cross-widget spotlight unchanged from v1"
    - "Last-file-activity is NEVER eager-loaded into BulkAccUser or FindingsContext (ACTV-03 contract preserved)"
  artifacts:
    - path: "app/(dashboard)/users/accStatusReduction.ts"
      provides: "Pure reducer from per-project statuses[] -> aggregated 'active'|'pending'|'deleted'"
      exports: ["reduceMemberStatus", "AggregatedStatus"]
    - path: "app/(dashboard)/users/accStatusReduction.test.ts"
      provides: "Vitest cases for any-active wins, any-pending else, all-deleted edge"
    - path: "app/(dashboard)/users/productsTierMap.ts"
      provides: "Parser + display-label map for AccProjectMember.products JSON -> {module,tier}[]"
      exports: ["parseProductsJson", "moduleTierLabel"]
    - path: "app/(dashboard)/users/productsTierMap.test.ts"
      provides: "Vitest cases covering all 8 documented modules + 3 tiers + unknown-key passthrough"
    - path: "app/(dashboard)/users/useVisibleRowEmails.ts"
      provides: "IntersectionObserver pool hook returning currently-visible row emails (200px rootMargin prefetch)"
      exports: ["useVisibleRowEmails"]
    - path: "server/routers/acc-activity.ts"
      provides: "getLastFileActivityBatch({emails: string[]}) procedure for lazy column"
    - path: "lib/acc/acc-types.ts"
      provides: "BulkAccUser extended with aggregatedStatus + perProjectAdmin booleans (NOT lastFileActivity)"
  key_links:
    - from: "UsersDirectoryClient.tsx row visibility"
      to: "trpc.accActivity.getLastFileActivityBatch"
      via: "useVisibleRowEmails -> useQuery({emails}, {enabled})"
      pattern: "getLastFileActivityBatch"
    - from: "Status pill click"
      to: "Status filter facet"
      via: "setStatusFilter([clickedStatus])"
      pattern: "setStatusFilter"
    - from: "AccProjectMember.products JSON"
      to: "Module Access side panel section"
      via: "parseProductsJson + moduleTierLabel"
      pattern: "parseProductsJson"
---

<objective>
Enrich the user list (UsersDirectoryClient + AccUserSidePanel) with the v2.0 per-project member fields surfaced through `BulkAccUser` and lazy tRPC queries: status column + facet, projectAdmin indicator + facet, lazy last-file-activity column, and side-panel Module Access section.

Purpose: LIST-01..04 + DASH-18 (list slice) per ROADMAP Phase 5 + 05-CONTEXT locked decisions. Status reduction = "any-active wins → active; else any-pending → pending; else deleted" (CONTEXT lock).
Output: A user list where every new field follows the interactivity contract and last-file-activity remains lazy per ACTV-03.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-enrichment-waves/05-CONTEXT.md
@.planning/phases/05-ui-enrichment-waves/05-RESEARCH.md
@.planning/phases/05-ui-enrichment-waves/5.1-graph-wave-SUMMARY.md
@app/(dashboard)/users/UsersDirectoryClient.tsx
@app/(dashboard)/users/AccUserSidePanel.tsx
@lib/acc/acc-types.ts
@lib/acc/modules.ts
@server/routers/acc-activity.ts
@server/routers/users.ts
@prisma/schema.prisma

<interfaces>
From prisma/schema.prisma AccProjectMember (verified):
- status: String (per-project)
- projectAdmin: Boolean
- executive: Boolean
- products: Json  (e.g. { docs: "member", build: "administrator", ... })
- addedOn: DateTime?

From Phase 03-02 (verified via STATE.md):
- `trpc.accActivity.getLastFileActivity({ email })` exists — single-user lazy query.
This plan ADDS `getLastFileActivityBatch({ emails: string[] })` for the list column to avoid N parallel single-user requests.

From Wave 5.1 (PREREQUISITE — already shipped before this wave executes):
- `server/routers/acc-members.ts` exists, exports `accMembersRouter` with `enrichedUsers` procedure returning per-email `{ aggregatedStatus, projectAdmin, executive, companyName, perProjectRoleNames, perProjectStatuses, isAccountAdmin }`.
- `BulkAccUser` already has the optional fields (`aggregatedStatus?`, `projectAdmin?`, `executive?`, `companyName?`, `perProjectRoleNames?`).
- `DashboardClient.tsx` already merges `accMembers.enrichedUsers` into the BulkAccUser array consumed by `UsersDirectoryClient`.

ACTV-03 contract (verified, MUST hold):
- `lastFileActivity` is NOT a field on BulkAccUser. Never load it eagerly. Lazy-fetch only via the activity router.

CONTEXT-locked status reduction (already implemented server-side in `accMembers.enrichedUsers` per Wave 5.1):
- any project active -> "active"
- else any project pending -> "pending"
- else "deleted"
This plan does NOT re-implement the reducer in a new helper; it consumes `aggregatedStatus` already populated on `BulkAccUser`. The pure helper `accStatusReduction.ts` is created here ONLY for tests + a fallback used by `UsersDirectoryClient` if `aggregatedStatus` is undefined (graceful degradation if enrichedUsers query is in flight).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — accStatusReduction + productsTierMap + useVisibleRowEmails + tRPC batch procedure + BulkAccUser type</name>
  <files>
    app/(dashboard)/users/accStatusReduction.ts
    app/(dashboard)/users/accStatusReduction.test.ts
    app/(dashboard)/users/productsTierMap.ts
    app/(dashboard)/users/productsTierMap.test.ts
    app/(dashboard)/users/useVisibleRowEmails.ts
    server/routers/acc-activity.ts
    lib/acc/acc-types.ts
    server/routers/users.ts
  </files>
  <action>
1) Create `app/(dashboard)/users/accStatusReduction.ts`:
- `type AggregatedStatus = "active" | "pending" | "deleted"`
- `function reduceMemberStatus(statuses: readonly string[]): AggregatedStatus`
- Rule (CONTEXT lock): if any status is "active" → "active"; else if any is "pending" → "pending"; else "deleted". Treat unknown values as "deleted" for the reduction. Empty input → "deleted".
- Create `accStatusReduction.test.ts` with cases: all-active, mixed active+pending, all-pending, mixed pending+deleted, all-deleted, empty, unknown-only.

2) Create `app/(dashboard)/users/productsTierMap.ts`:
- Use Zod to parse `AccProjectMember.products` JSON safely. The 8 documented modules per REQUIREMENTS MEM-03: docs, designCollaboration, modelCoordination, build, autoSpecs, insight, cost, projectAdministration. Tier values per MEM-03: "administrator" | "member" | "none". Unknown module keys pass through as-is with `unknownModuleLabel(key)`.
- Exports:
  - `interface ProductTier { module: string; label: string; tier: "administrator"|"member"|"none"|string; tierLabel: string }`
  - `function parseProductsJson(raw: unknown): ProductTier[]` — returns sorted array (documented modules in canonical order first, unknowns after). Use `moduleLabel()` from `lib/acc/modules.ts` for display labels of known modules. tierLabel maps administrator→"Administrator", member→"Member", none→"None"; unknown tier strings pass through.
  - On invalid JSON or non-object input → return `[]`.
- Create `productsTierMap.test.ts` covering: each of the 8 modules with each of the 3 tiers (24 cases — assert presence + label), unknown module key passthrough, unknown tier passthrough, invalid input (null/string/number/[]) → [].

3) Create `app/(dashboard)/users/useVisibleRowEmails.ts` implementing the hook from RESEARCH Example 5: accepts a Map<email, HTMLElement> (or a ref-registration callback API), uses one shared IntersectionObserver with `rootMargin: "200px"`, returns the array of currently-visible emails. Debounce updates by ~50ms with `requestAnimationFrame` coalescing to avoid update storms during scroll. Pure hook, no tests needed (covered by Task 3 integration check).

4) Extend `server/routers/acc-activity.ts`:
- Add `getLastFileActivityBatch` protected query: accepts `{ emails: z.array(z.string()).max(200) }` (cap batch size to bound DB load). Returns `Record<string, { timestamp: string; action: string; rawAction: string } | null>`. Implementation: one `AccActivity` query with `where: { userEmail: { in: emails }, rawAction: { in: <file-related rawAction list> } }` ordered by `(userEmail, createdAt DESC)`, then reduce in JS to most-recent-per-email. Reuse the file-related rawAction list from Phase 03-02 (if exported); if not exported, copy the constant and add a TODO to dedupe.
- Do NOT touch the existing single-user `getLastFileActivity` procedure.

5) `BulkAccUser` extension is ALREADY DONE in Wave 5.1 (Task 1). Do NOT re-extend. Do NOT modify `server/routers/users.ts` `bulkAccSummary` — Pattern 6 in RESEARCH locks this. Instead:
- Extend `accMembers.enrichedUsers` in `server/routers/acc-members.ts` (created in Wave 5.1) to also include `perProjectStatuses: { projectId, projectName, status, projectAdmin }[]` on each row if not already present (Wave 5.1 spec already includes it — verify and add if missing).
- For LIST-04 Module Access, add a NEW lazy procedure `accMembers.getProductsForUser({ email })` in `server/routers/acc-members.ts` returning `{ projectId; projectName; products: Json }[]`. Per-user lazy, NOT eager-loaded into `enrichedUsers` (keeps the bulk query cheap; products JSON can be ~1KB per project × N projects).
- For LIST-02, the `aggregatedStatus`/`projectAdmin`/`executive` fields surface as `BulkAccUser.aggregatedStatus`/`BulkAccUser.projectAdmin`/`BulkAccUser.executive` (already optional via Wave 5.1).

Per-task verification: `npm test -- --run accStatusReduction productsTierMap` — both suites green.
  </action>
  <verify>
    <automated>npm test -- --run accStatusReduction productsTierMap</automated>
  </verify>
  <done>Pure helpers + tests green, IntersectionObserver hook created, batch tRPC procedure registered, BulkAccUser extended with aggregatedStatus + perProjectAdmin + perProjectStatuses, products NOT promoted onto BulkAccUser, lastFileActivity NOT promoted onto BulkAccUser.</done>
</task>

<task type="auto">
  <name>Task 2: LIST-01 + LIST-02 — status column + facet + Admin pill + facet, with hover/click/spotlight contract</name>
  <files>
    app/(dashboard)/users/UsersDirectoryClient.tsx
  </files>
  <action>
In `UsersDirectoryClient.tsx`:

1) Status column (LIST-01):
- Add a dedicated column "Status" rendering a pill from `aggregatedStatus`. Pill text + color:
  - active → green token, label "Active"
  - pending → amber token, label "Pending"
  - deleted → muted/gray token, label "Deleted"
- Pill text is load-bearing; color is supplementary (accessibility lock — color never sole signal).
- Hover the pill: shadcn Tooltip showing the per-project breakdown from `perProjectStatuses` (e.g. "Active on 3, Pending on 1, Deleted on 0"). Reuse `components/ui/tooltip.tsx` (already installed Wave 5.1).
- Click the pill: filter the list to that aggregated status (calls existing filter setter). Acts as a single-status filter shortcut. Re-clicking same pill clears that constraint.

2) Status multi-select facet (LIST-01):
- Add a "Status" multi-select group to the toolbar/filter UI, matching the existing filter facet pattern (do NOT introduce a new pattern). Options: Active / Pending / Deleted. AND-intersects with existing filters (module, role, etc.).

3) projectAdmin indicator (LIST-02):
- Render a small "Admin" pill inline next to the user's name (left of name or right — match existing inline-badge convention in this file). Show only when `perProjectAdmin === true`.
- Hover: Tooltip with text "Project admin on N projects" using `perProjectStatuses.length` filtered by projectAdmin. (If projectAdmin breakdown isn't on perProjectStatuses, add `projectAdmin: boolean` to that nested record in Task 1's BulkAccUser extension.)
- Click: filters list to projectAdmin=true (single-toggle facet shortcut).

4) projectAdmin filter facet (LIST-02):
- Add a "Project Admin" facet to the toolbar — a single toggle (on/off/any). AND-intersects with existing filters.

5) DASH-18 contract:
- Row click already opens the side panel (existing behaviour) — do not change.
- New pills satisfy: hover detail (Tooltip), click-through (filter shortcut), cross-widget spotlight (clicking does NOT call setSelected — the spotlight signal here is the visible list reduction, consistent with how existing facets work). Document this interpretation in the SUMMARY.

Constraints:
- DO NOT regress the existing label polish curve (not applicable here, but no new label-style overrides anywhere).
- DO NOT introduce any Refresh/Sync All UI.
- Reuse existing pill component if one exists in `components/ui/` (probably `Badge`). Do not write a new pill component.

Per-task verification: `npm run typecheck` reports no new errors; manual smoke — render list, see pills, hover tooltip, click pill filters, multi-select facet works.
  </action>
  <verify>
    <automated>npm test -- --run accStatusReduction</automated>
  </verify>
  <done>Status column + facet + click filter live. Admin pill + facet + click filter live. Tooltips on both. AND-intersection with existing filters verified manually.</done>
</task>

<task type="auto">
  <name>Task 3: LIST-03 lazy last-file-activity column + LIST-04 Module Access side panel section</name>
  <files>
    app/(dashboard)/users/UsersDirectoryClient.tsx
    app/(dashboard)/users/AccUserSidePanel.tsx
  </files>
  <action>
1) LIST-03 — Lazy last-file-activity column in `UsersDirectoryClient.tsx`:
- Add column "Last File Activity".
- Use the `useVisibleRowEmails` hook from Task 1. Register each row's DOM element via a `ref` callback (or `data-email` attribute on a stable wrapper element).
- Drive a single `trpc.accActivity.getLastFileActivityBatch.useQuery({ emails: visibleEmails }, { enabled: visibleEmails.length > 0, staleTime: 300_000, keepPreviousData: true })`. Batch size capped at 200 server-side; client must chunk if visibleEmails exceeds 200 (rare — viewports typically show <100). For chunking, prefer the simplest approach: take the first 200 and accept a one-frame delay for the rest as they scroll into view.
- Cell rendering states:
  - Not-yet-fetched (email not yet in visibleEmails, or query in flight) → skeleton shimmer block. Use a simple `<div className="h-4 w-24 animate-pulse rounded bg-muted" />` — do NOT introduce a new shimmer library.
  - Fetched, has activity → relative timestamp + small action icon if available; click opens user side panel with the existing Phase 03-03 activity drill-down (kind: 'userActivity').
  - Fetched, no activity (null result) → em-dash `—` wrapped in `<Tooltip>` showing "No activity in 90d" (CONTEXT lock).
- DO NOT add lastFileActivity to BulkAccUser. DO NOT eager-load. (ACTV-03 enforcement.)
- Sort behaviour: if user clicks the column header to sort, fetch ALL emails first (batch-pagination by 200) before sorting. Acceptable UX: show a loading indicator on the column header while populating. CONTEXT permits this approach (deferred from Phase 03-03).

2) LIST-04 — Module Access section in `AccUserSidePanel.tsx`:
- Add a new collapsible section titled "Module Access".
- Source data: `trpc.accMembers.getProductsForUser.useQuery({ email: selectedEmail }, { enabled: !!selectedEmail, staleTime: 300_000 })` — the lazy per-user procedure added in Task 1. Returns `{ projectId; projectName; products: Json }[]`. NEVER eager-load products into `enrichedUsers` or `bulkAccSummary` — that would balloon the bulk payload.
- For EACH project the user is a member of, render a sub-card with project name and module/tier rows using `parseProductsJson()` from Task 1. Render `module.label`: `tierLabel` (e.g. "Docs: Member"). If multiple projects show the same module/tier, deduplicate at the user level by collapsing into a top-level "Across all projects" group AND showing per-project deviations underneath. Discretion item — pick the clearer of: per-project sub-cards (simpler) OR top-level + deviations (denser). Document choice in SUMMARY.
- Use `moduleLabel()` from `lib/acc/modules.ts` (already exists per RESEARCH) for known modules. Unknown modules render as raw key with a warning icon + tooltip "Unknown module from APS".
- Default collapsible state: claude's discretion — recommend OPEN (information-dense, matches user's interactivity preference from feedback memory).
- DASH-18 contract: hover module row → Tooltip with full label (in case truncated); click a tier badge → optional cross-widget spotlight (defer; this side-panel surface is already a drill-down endpoint).

Constraints:
- No Refresh / Sync All UI.
- Never render raw JSON.
- Maintain ACTV-03 lazy contract for LIST-03.

Per-task verification: visual smoke + `npm run typecheck`.
  </action>
  <verify>
    <automated>npm test -- --run productsTierMap</automated>
  </verify>
  <done>Last-file-activity column lazy-loads via IntersectionObserver, shows shimmer→value→em-dash-with-tooltip, never eager-loaded. Side panel Module Access section parses products JSON to "Module: Tier" rows, never raw JSON, with collapsible behaviour.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — DASH-18 interactivity contract gate for Wave 5.2</name>
  <what-built>
LIST-01 (status column + multi-select facet + click-pill-to-filter), LIST-02 (projectAdmin inline pill + facet), LIST-03 (lazy last-file-activity column with IntersectionObserver, skeleton shimmer, em-dash empty state), LIST-04 (Module Access collapsible side-panel section).
  </what-built>
  <how-to-verify>
Run dev server, navigate to /users user list.

1. Status column: visible on every row with a coloured pill carrying literal text ("Active" / "Pending" / "Deleted"). Hover one — tooltip shows per-project breakdown. Click one — list filters to that status; click again — filter clears.

2. Status multi-select facet in toolbar — selecting Active+Pending shows union; combining with an existing facet (e.g. module) shows AND-intersection (smaller set).

3. "Admin" inline pill next to user name appears only for users with projectAdmin on at least one project. Hover — tooltip names project count. Click — list filters to project admins only.

4. Project Admin facet in toolbar — toggles list to admin-only.

5. Last File Activity column:
   - On initial render, off-screen rows show skeleton shimmer (NOT em-dash, NOT a value).
   - Scroll down — newly-visible rows fire batch queries; cells populate.
   - Rows with no activity in 90d show em-dash with tooltip "No activity in 90d".
   - Verify ACTV-03: open browser network tab, scroll to a user, confirm a `getLastFileActivityBatch` request fires. Confirm `bulkAccSummary` response does NOT include lastFileActivity fields.

6. Click a user row — side panel opens. Find the "Module Access" collapsible section. It should list each module (Docs, Build, etc.) with its tier (Administrator / Member / None). NEVER raw JSON. Unknown modules show a warning icon.

7. Performance: scroll the list rapidly — no visible jank, no console errors. Filter changes should feel instant (<100ms perceived).

8. Confirm no Refresh / Sync All / manual-sync UI anywhere on the list page.

Type "approved" or describe specific failures (e.g. "raw JSON visible in Module Access section" / "bulkAccSummary includes lastFileActivity in network tab" / "shimmer never resolves").
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --run accStatusReduction productsTierMap` — both green.
2. `npm run typecheck` — no new errors.
3. Manual UAT (Task 4) — all 8 checks pass.
4. Grep `lib/acc/acc-types.ts` for `lastFileActivity` on `BulkAccUser` — must NOT match (ACTV-03 guard).
5. Grep `server/routers/users.ts` (or wherever bulkAccSummary lives) — `lastFileActivity` must NOT appear in the procedure's return shape.
</verification>

<success_criteria>
- All must_haves truths observable.
- LIST-01..04 fully shipped.
- DASH-18 contract satisfied per list affordance.
- ACTV-03 lazy contract preserved: no eager last-file-activity load.
- No manual sync UI introduced.
- Label polish curve untouched (no list code changes label rendering).
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-enrichment-waves/5.2-list-wave-SUMMARY.md` documenting: status reducer behaviour validated, Module Access rendering strategy chosen (per-project vs top-level+deviations), any batch-cap effects observed at high row counts, ACTV-03 enforcement confirmed via network tab.
</output>
