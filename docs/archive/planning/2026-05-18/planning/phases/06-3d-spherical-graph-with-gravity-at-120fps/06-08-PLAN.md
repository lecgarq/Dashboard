---
phase: 06-3d-spherical-graph-with-gravity-at-120fps
plan: 08
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - app/(dashboard)/users/sphere3d/filterUrl.ts
  - app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx
  - app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts
autonomous: true
requirements: [GRAPH3D-04, GRAPH3D-05]
must_haves:
  truths:
    - "Filter state round-trips through the URL: readFiltersFromUrl(writeFiltersToUrl(state)) === state"
    - "Filter panel renders the same filter controls as the 2D AccUsersGraph (role/company/active toggles + date range), at parity"
    - "Sphere3DFilterPanel is a standalone component the host (Sphere3DGraph in 06-09) can drop next to the canvas — no canvas/r3f imports"
    - "URL persistence helpers are pure functions, unit-tested"
  artifacts:
    - path: "app/(dashboard)/users/sphere3d/filterUrl.ts"
      provides: "readFiltersFromUrl / writeFiltersToUrl pure helpers (lifted from AccUsersGraph)"
      exports: ["readFiltersFromUrl", "writeFiltersToUrl"]
    - path: "app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx"
      provides: "Filter UI panel — controlled component, owns no state, fires onFiltersChange"
    - path: "app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts"
      provides: "Round-trip + edge cases (empty filters, all filters set, invalid URL params)"
  key_links:
    - from: "Sphere3DFilterPanel.tsx"
      to: "AccGraphFilterState (existing type)"
      via: "props.filters: AccGraphFilterState; props.onFiltersChange(next: AccGraphFilterState)"
      pattern: "AccGraphFilterState"
---

<objective>
Lift the filter panel UI + URL-persistence helpers out of AccUsersGraph.tsx into standalone files under `sphere3d/`. The final composition (06-09) imports these directly. AccUsersGraph stays intact until 06-10 deletes it.

Purpose: Decompose the monolith. Plan 06-09 (final composition + E2E) shouldn't have to also lift filter UI — that's too much. This plan does the lift cleanly and tests it independently.
Output: 3 files (1 pure helper + 1 component + 1 test).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-RESEARCH.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/sphere3d/filterAdapter.ts

<interfaces>
Existing AccGraphFilterState type lives in the filter adapter or AccUsersGraph — find via grep and import, don't redefine.

`readFiltersFromUrl(): AccGraphFilterState` reads from `window.location.search` (returns default empty filters if missing/invalid).
`writeFiltersToUrl(filters: AccGraphFilterState): void` updates the URL via `history.replaceState` (no reload).

Both helpers must be SSR-safe (guard `typeof window !== 'undefined'`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: filterUrl.ts — lift pure URL helpers + Vitest</name>
  <files>
app/(dashboard)/users/sphere3d/filterUrl.ts,
app/(dashboard)/users/sphere3d/__tests__/filterUrl.test.ts
  </files>
  <action>
1. Grep AccUsersGraph.tsx for the existing URL serialization logic (search for `URLSearchParams` / `history.replaceState` / `location.search`). Identify the shape of AccGraphFilterState.
2. Extract `readFiltersFromUrl()` and `writeFiltersToUrl(filters)` as pure functions into `sphere3d/filterUrl.ts`. SSR-safe.
3. Write `__tests__/filterUrl.test.ts` covering:
   - Round-trip: write then read returns the same state.
   - Empty filters → URL has no filter params.
   - All filters set → URL contains expected keys.
   - Invalid/corrupt URL param → returns default state, doesn't throw.
   - Date range serialization (ISO strings, not raw Date objects).
4. AccUsersGraph.tsx keeps its inline copies for now — 06-10 deletes the file entirely, so no need to refactor it to import from the new location.
  </action>
  <verify>
    <automated>npx vitest run app/\(dashboard\)/users/sphere3d/__tests__/filterUrl.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Helpers exported, Vitest green (≥4 tests), SSR-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Sphere3DFilterPanel.tsx — controlled component lift</name>
  <files>app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx</files>
  <action>
1. Locate the filter UI JSX inside AccUsersGraph.tsx (per RESEARCH/CONTEXT, roughly lines ~700–1100).
2. Lift the JSX + handlers into `Sphere3DFilterPanel.tsx` as a controlled component:
   - Props: `{ filters: AccGraphFilterState; onFiltersChange: (next: AccGraphFilterState) => void; nodes: AccGraphInstanceNode[] /* for derived option lists like company set, role set */ }`.
   - Owns NO state — fully controlled. Parent (06-09 Sphere3DGraph) owns the filter state + URL persistence.
   - NO imports from three / r3f / drei / cosmos.gl. Pure DOM + existing UI primitives from `components/ui/*` (shadcn). This is what makes the panel droppable next to the canvas.
3. Match the EXISTING filter affordances at parity (CONTEXT-locked: "no new filter types beyond what the 2D graph offers"). Role toggles, company toggles, active/inactive toggle, date range — whatever AccUsersGraph currently exposes.
4. Don't change visual treatment beyond what's needed to fit the new layout. Polish is for a later phase.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sphere3DFilterPanel imports cleanly, has zero three/r3f imports, props are controlled-component shape.</done>
</task>

</tasks>

<verification>
- Typecheck clean.
- Vitest filterUrl green.
- Visual smoke: import Sphere3DFilterPanel into a scratch page with a stub onFiltersChange → all controls render.
</verification>

<success_criteria>
- 3 files (1 helper + 1 component + 1 test)
- filterUrl helpers SSR-safe + round-trip tested
- Filter panel is controlled component with no 3D dependencies
- Filter affordances at PARITY with existing 2D graph (no additions, no removals)
</success_criteria>

<output>
After completion, create `06-08-SUMMARY.md` listing: filter affordances lifted, AccGraphFilterState shape, line range that was lifted out of AccUsersGraph.
</output>
