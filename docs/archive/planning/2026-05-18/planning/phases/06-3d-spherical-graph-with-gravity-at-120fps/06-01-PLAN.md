---
phase: 06-3d-spherical-graph-with-gravity-at-120fps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - app/(dashboard)/users/Sphere3DGraph.tsx
  - app/(dashboard)/users/sphere3d/SphereCanvas.tsx
  - app/(dashboard)/users/sphere3d/deterministicMode.ts
autonomous: true
requirements: [GRAPH3D-03, GRAPH3D-11]
must_haves:
  truths:
    - "Sphere3DGraph mounts at the Users 'graph' tab without runtime errors"
    - "react-three-fiber + drei + seedrandom are installed and resolvable"
    - "Canvas mounts a r3f scene with WebGL2; emits clean empty-state if WebGL2 absent"
    - "Replay mode (?replay=1) sets a window.__sphere3dReady flag and disables auto-rotate"
  artifacts:
    - path: "app/(dashboard)/users/Sphere3DGraph.tsx"
      provides: "Public component with same {users,onSelectUser} contract as AccUsersGraph"
      min_lines: 30
    - path: "app/(dashboard)/users/sphere3d/SphereCanvas.tsx"
      provides: "r3f <Canvas> wrapper, OrbitControls, dev Stats"
    - path: "app/(dashboard)/users/sphere3d/deterministicMode.ts"
      provides: "Replay flag detection + seeded RNG factory (seedrandom)"
  key_links:
    - from: "app/(dashboard)/users/Sphere3DGraph.tsx"
      to: "sphere3d/SphereCanvas.tsx"
      via: "default child of <Canvas> wrapper"
      pattern: "SphereCanvas"
---

<objective>
Install the r3f/drei/seedrandom stack, scaffold the new Sphere3DGraph component with the exact public contract of the existing AccUsersGraph (`{ users: BulkAccUser[]; onSelectUser?: (email)=>void }`), and stand up an empty r3f Canvas wrapper plus the deterministic-replay mode helper. NO physics, NO points yet — only the shell.

Purpose: Establish the integration point and lazy-load surface so downstream waves can fill in physics, rendering, and interaction without disturbing the existing graph mount until Wave 5 cuts over.
Output: Installed deps, scaffold files, replay helper.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-CONTEXT.md
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-RESEARCH.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/UsersDirectoryClient.tsx
@lib/acc/graphSnapshot.ts

<interfaces>
From app/(dashboard)/users/AccUsersGraph.tsx (CURRENT public contract — DO NOT change):
```ts
export interface AccUsersGraphProps {
  users: BulkAccUser[];
  onSelectUser?: (email: string) => void;
}
```
Sphere3DGraph MUST export the same prop interface so a one-line import swap in UsersDirectoryClient.tsx (Wave 5) works.

From lib/acc/graphSnapshot.ts — AccGraphInstanceNode shape (id, label, color, email, projectId, isAdmin, roles[], modules[], lastSignIn, companyRole, individualAccess).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install r3f stack + verify peer ranges</name>
  <files>package.json, next.config.ts</files>
  <action>
Run `npm install @react-three/fiber@^9 @react-three/drei@^10 seedrandom` and `npm install -D @types/seedrandom`. three@^0.184 is already present — do not bump.

After install, run `npm ls three` and assert exactly one resolved version (drei v10 + r3f v9 must agree with the installed three). If two copies of three appear, add an npm override pinning three to the installed version.

Open `next.config.ts`. Add `transpilePackages: ['three']` (merge into existing config object — do not overwrite). If `transpilePackages` already lists three, leave as-is.

Verify: `npm run build` (or at minimum `npx tsc --noEmit`) succeeds with no new "Cannot find module" errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Deps installed, single three resolved, next.config has transpilePackages including 'three', typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Sphere3DGraph + SphereCanvas + deterministicMode</name>
  <files>
app/(dashboard)/users/Sphere3DGraph.tsx,
app/(dashboard)/users/sphere3d/SphereCanvas.tsx,
app/(dashboard)/users/sphere3d/deterministicMode.ts
  </files>
  <action>
Create `app/(dashboard)/users/sphere3d/deterministicMode.ts`:
- Export `isReplayMode(): boolean` — reads URLSearchParams `replay=1` (client-only, guards `typeof window`).
- Export `createSeededRng(seed = 'lecg-phase6')` — wraps `seedrandom`, returns a `() => number` in [0,1).
- Export const `REPLAY_WARMUP_FRAMES = 240` and `REPLAY_FIXED_DELTA = 1 / 60`.
- Export `signalReady()` — sets `(window as any).__sphere3dReady = true`.

Create `app/(dashboard)/users/sphere3d/SphereCanvas.tsx` ("use client"):
- Default-export a React component `SphereCanvas({ children })`.
- Renders r3f `<Canvas>` with `frameloop="always"`, `dpr={[1,2]}` (force `dpr=1` under replay), `gl={{ antialias: false, powerPreference: 'high-performance' }}`, camera `{ position: [0,0,30], fov: 42, near: 0.05, far: 500 }`.
- Inside: drei `<OrbitControls enableDamping dampingFactor={0.075} minDistance={0} maxDistance={120} />` and dev-only drei `<Stats />` when `process.env.NODE_ENV === 'development'`.
- Children render inside the Canvas.
- On mount: check WebGL2 by reading `gl.capabilities.isWebGL2` via a child component that calls `useThree()` and logs a single warning if false (we do not block rendering here; Wave 2 enforces WebGL2 for GPGPU).

Create `app/(dashboard)/users/Sphere3DGraph.tsx` ("use client"):
- Export interface `Sphere3DGraphProps { users: BulkAccUser[]; onSelectUser?: (email: string) => void; }` (import BulkAccUser from `./AccAnalysisPanel` exactly like AccUsersGraph does — verify import path matches).
- Default export function `Sphere3DGraph(props)` returning `<div className="h-full w-full" data-testid="sphere3d-root"><SphereCanvas /></div>`. Empty scene for now — Wave 2 fills it.
- NOT yet wired into UsersDirectoryClient — that swap is Wave 5 (Plan 06-08).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three files exist, typecheck passes, importing Sphere3DGraph from another file resolves and renders an empty Canvas.</done>
</task>

</tasks>

<verification>
- `npm ls three` → single version
- `npx tsc --noEmit` → no errors
- `grep -n "transpilePackages" next.config.ts` → includes 'three'
- Import smoke test (manual, optional): temporarily import Sphere3DGraph in a scratch page → empty Canvas renders, no console errors
</verification>

<success_criteria>
- Sphere3DGraph.tsx, sphere3d/SphereCanvas.tsx, sphere3d/deterministicMode.ts exist
- Public prop type matches AccUsersGraphProps exactly
- npm install succeeds, single three resolved
- typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-01-SUMMARY.md` capturing the resolved versions, any peer-range overrides applied, and the exact public prop interface.
</output>
