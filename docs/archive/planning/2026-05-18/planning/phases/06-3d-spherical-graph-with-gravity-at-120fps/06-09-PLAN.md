---
phase: 06-3d-spherical-graph-with-gravity-at-120fps
plan: 09
type: execute
wave: 5
depends_on: ["06-06", "06-07", "06-08"]
files_modified:
  - app/(dashboard)/users/sphere3d/replayHarness.ts
  - app/(dashboard)/users/Sphere3DGraph.tsx
  - tests/e2e/sphere3d.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [GRAPH3D-03, GRAPH3D-07, GRAPH3D-08, GRAPH3D-09, GRAPH3D-10, GRAPH3D-11]
must_haves:
  truths:
    - "?replay=1 + warm-up frames + window.__sphere3dReady flag enables stable Playwright screenshots"
    - "Playwright suite is green on Chromium, WebKit, AND Firefox — all three browsers run the SAME ≥10-test suite (CONTEXT-locked cross-browser obligation)"
    - "Frame-time p95 reported in test output (drei Stats hook or custom measurement)"
    - "Sphere3DGraph composes all sphere3d/* primitives (PhysicsCompute + NodesPoints + EdgesLines + PickPoints + AutoRotateGroup + CameraTween + BillboardLabels + Sphere3DFilterPanel) into a working component"
  artifacts:
    - path: "app/(dashboard)/users/sphere3d/replayHarness.ts"
      provides: "Warmup runner + window flag setter + DPR override under ?replay=1"
    - path: "app/(dashboard)/users/Sphere3DGraph.tsx"
      provides: "Final composition — owns data fetch (trpc.users.getPrecomputedGraph), filter state, integrates all sphere3d/* primitives"
    - path: "tests/e2e/sphere3d.spec.ts"
      provides: "≥10 Playwright E2E tests covering initial render, click-select, camera tween, dolly-into-ball, orbit drag, auto-rotate idle, filter reflow with outward drift, edge highlight on hover/select, label LOD reveal on dolly, replay determinism"
    - path: "playwright.config.ts"
      provides: "Three browser projects: chromium, webkit, firefox — ALL un-skipped"
  key_links:
    - from: "Sphere3DGraph.tsx"
      to: "trpc.users.getPrecomputedGraph + accGraphFilters + filterAdapter + topologyAdapter + filterUrl"
      via: "data fetch + filter state → adapter outputs"
      pattern: "(getPrecomputedGraph|useFilteredVisibility|buildEdgeBuffers|readFiltersFromUrl)"
    - from: "tests/e2e/sphere3d.spec.ts"
      to: "?replay=1 URL + window.__sphere3dReady + window.__sphere3dPickAt"
      via: "page.goto with replay flag + waitForFunction"
      pattern: "(__sphere3dReady|__sphere3dPickAt)"
---

<objective>
Compose all sphere3d primitives into the final Sphere3DGraph component (data fetch + filter state + filter panel + canvas pipeline). Add the replay harness that makes Playwright screenshots deterministic. Write the heavy cross-browser E2E suite (Chromium + WebKit + Firefox, ≥10 tests each).

Purpose: This is "actually wire up the product." Plan 06-10 only deletes cosmos.gl after this passes.
Output: 1 harness + 1 final component + 1 E2E spec + playwright config.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-RESEARCH.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/sphere3d/PhysicsCompute.tsx
@app/(dashboard)/users/sphere3d/NodesPoints.tsx
@app/(dashboard)/users/sphere3d/EdgesLines.tsx
@app/(dashboard)/users/sphere3d/PickPoints.tsx
@app/(dashboard)/users/sphere3d/AutoRotateGroup.tsx
@app/(dashboard)/users/sphere3d/BillboardLabels.tsx
@app/(dashboard)/users/sphere3d/CameraTween.tsx
@app/(dashboard)/users/sphere3d/SphereCanvas.tsx
@app/(dashboard)/users/sphere3d/Sphere3DFilterPanel.tsx
@app/(dashboard)/users/sphere3d/filterUrl.ts
@app/(dashboard)/users/sphere3d/filterAdapter.ts
@app/(dashboard)/users/sphere3d/topologyAdapter.ts
@app/(dashboard)/users/sphere3d/positionMirror.ts
@app/(dashboard)/users/sphere3d/deterministicMode.ts

<interfaces>
trpc.users.getPrecomputedGraph returns `{ hit, nodes, edges, nodeIds, dataHash, stats, positions }` — Sphere3DGraph fetches this (same as AccUsersGraph did at line 492).

PhysicsApi.onReady (06-04), PickApi.onReady (06-06) — both use the same `(api) => void` pattern. Host stores both refs.

PositionMirror (06-07) is constructed once in Sphere3DGraph and passed to both CameraTween and BillboardLabels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: replayHarness.ts</name>
  <files>app/(dashboard)/users/sphere3d/replayHarness.ts</files>
  <action>
Export `useReplayHarness({ physics }: { physics: PhysicsApi | null })`:
- If `!isReplayMode()` → no-op.
- Inside `useFrame`: if a warmup counter `< REPLAY_WARMUP_FRAMES` (240), increment; once == 240, set `(window as any).__sphere3dReady = true` exactly once.
- Force `gl.setPixelRatio(1)` on mount under replay mode (override the Canvas dpr).
- Disable Stats HUD if mounted.
- Return current warmup progress for optional UI use.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hook compiles; under ?replay=1 with a mounted physics, window.__sphere3dReady becomes true after ~4s.</done>
</task>

<task type="auto">
  <name>Task 2: Final Sphere3DGraph.tsx composition</name>
  <files>app/(dashboard)/users/Sphere3DGraph.tsx</files>
  <action>
Replace the Wave 1 scaffold with the full composition:
- Fetch via `trpc.users.getPrecomputedGraph` (mirror exact options from AccUsersGraph.tsx:492).
- Build derived data: `nodes` array, edge buffers via `buildEdgeBuffers(nodes)`, cluster ids via `assignClusterIds(nodes)`.
- Filter state: `useState<AccGraphFilterState>(() => readFiltersFromUrl())`, on change call `writeFiltersToUrl(filters)`. (Both from `sphere3d/filterUrl.ts`.)
- `useFilteredVisibility(nodes, filters)` → `{ visibility, version }`.
- Selected node index state + selected email derivation.
- Layout: `<Sphere3DFilterPanel>` (left) + `<SphereCanvas>` (right) inside the existing tab container, identical CSS classes to AccUsersGraph wrapper so the tab layout doesn't shift.
- Store `physicsApi` and `pickApi` via `useState`; both set by their respective `onReady` callbacks.
- Construct a single `positionMirror` (via useMemo, keyed on physicsApi) and pass to CameraTween + BillboardLabels.
- Inside `<SphereCanvas>`:
  ```tsx
  <AutoRotateGroup>
    <PhysicsCompute nodes={nodes} onReady={setPhysicsApi} />
    {physicsApi && (<>
      <NodesPoints physics={physicsApi} visibility={visibility} visibilityVersion={version} ... />
      <EdgesLines physics={physicsApi} edgeBuffers={edgeBuffers} ... />
      <BillboardLabels physics={physicsApi} mirror={mirror} nodes={nodes} />
      <PickPoints physics={physicsApi} nodes={nodes} onReady={setPickApi} />
      <CameraTween physics={physicsApi} mirror={mirror} selectedNodeIndex={selectedIdx} controlsRef={controlsRef} />
      <ReplayHarness physics={physicsApi} />
    </>)}
  </AutoRotateGroup>
  ```
- WebGL2 absent → PhysicsCompute's `onReady(null)` triggers an empty-state message instead of Canvas children.
- Click handler on the Canvas wrapper div: extracts cssX/cssY → calls `pickApi.pickAt(x, y)` → if `idx >= 0`, set selectedIdx and fire `onSelectUser(nodes[idx].email)`.

NOT YET wired into UsersDirectoryClient — that's Plan 06-10. This file just exists, importable, working in isolation.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sphere3DGraph renders a complete ball from real (or fixture) data, supports click-to-select, filters fade + drift nodes outward, dolly-zooms into the ball.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright config (3 browsers, un-skipped) + ≥10-test E2E spec</name>
  <files>tests/e2e/sphere3d.spec.ts, playwright.config.ts</files>
  <action>
**playwright.config.ts:** Configure THREE projects, ALL active (no `.skip`, no TODO comments — CONTEXT-locked cross-browser obligation):
- `chromium` (devices['Desktop Chrome'])
- `webkit` (devices['Desktop Safari'])
- `firefox` (devices['Desktop Firefox'])
Same `testDir`, same `webServer: { command: 'npm run dev', port: 3000 }`, `use.baseURL: 'http://localhost:3000'`.

**tests/e2e/sphere3d.spec.ts** — ≥10 tests, each project runs the same suite:

1. **initial-render** — `goto('/users?tab=graph&replay=1')`, `waitForFunction(() => (window as any).__sphere3dReady === true, { timeout: 30000 })`, assert `data-testid="sphere3d-root"` visible, screenshot baseline.
2. **single-click-select** — `page.evaluate(() => (window as any).__sphere3dPickAt(centerX, centerY))` returns a valid node idx; side panel opens for that user.
3. **camera-tween** — after select, screenshot 500ms later differs from initial (camera moved).
4. **dolly-into-ball** — dispatch `wheel` events on canvas with `deltaY: -2000` cumulatively, screenshot — verify camera is now INSIDE the ball (visible node count drops as nodes leave the frustum, OR use a debug hook `window.__sphere3dCameraDistance()` to assert distance < ball radius).
5. **orbit-drag** — synthesize pointerdown→pointermove→pointerup on canvas with horizontal delta, screenshot differs from initial in rotation axis.
6. **auto-rotate-idle** — DISABLE replay flag for THIS test (or use a separate `?test=auto-rotate` that turns on rotation but keeps physics warm). Wait 4s after init, screenshot at t=4s vs t=5s differ.
7. **filter-reflow-with-drift** — programmatically toggle a filter via the filter panel (`page.getByRole('checkbox', { name: /role/i }).first().click()`), wait 2s, screenshot — hidden nodes are visibly outside the ball envelope (drift outward) AND faded.
8. **edge-highlight-on-select** — after select (test 2), assert via screenshot diff that some edges are brighter than baseline (or expose `window.__sphere3dHighlightedNodeIdx()` debug hook to assert).
9. **label-lod-reveal-on-dolly** — count visible label DOM/text nodes at default camera (should be ~0–5 since lodDistance gates them) vs after dolly-in (should be >0); under replay we hide labels, so this test runs WITHOUT replay or with a `?showLabels=1` override.
10. **replay-determinism** — load twice (`?replay=1&seed=42`), wait for ready, screenshot — pixel-diff identical (or near-identical within tolerance ≤ 1%).

Add debug hooks needed for tests as small additions to Sphere3DGraph: `window.__sphere3dCameraDistance`, `window.__sphere3dHighlightedNodeIdx`, etc. Guard each behind `isReplayMode()` so prod doesn't ship them.

Add `"e2e": "playwright test"` to package.json scripts if not present.

Tolerance for screenshot diffs is high — these are smoke tests for behavior, not pixel fidelity gates.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/sphere3d.spec.ts</automated>
  </verify>
  <done>≥10 Playwright tests pass on chromium, webkit, AND firefox (3 × 10 = 30 test runs green); no `.skip()` remains in the spec.</done>
</task>

</tasks>

<verification>
- Vitest unit suites still green.
- Playwright suite green across chromium + webkit + firefox.
- `npx tsc --noEmit` clean.
- Sphere3DGraph mounted in dev, manual rotation + select + filter all work.
</verification>

<success_criteria>
- Replay harness sets ready flag deterministically
- Sphere3DGraph is the complete, importable replacement for AccUsersGraph (same props)
- Playwright suite green on ALL three browsers (CONTEXT-locked)
- ≥10 tests covering all CONTEXT-locked interactions: render, select, tween, dolly-in, orbit, idle-rotate, filter-drift, edge-highlight, label-LOD, replay-determinism
- No `.skip()` or TODO comments deferring cross-browser work
</success_criteria>

<output>
After completion, create `06-09-SUMMARY.md` recording: Playwright test count, per-browser pass rate, frame-time p50/p95 observed during render test on each browser, any browser-specific quirks discovered.
</output>
