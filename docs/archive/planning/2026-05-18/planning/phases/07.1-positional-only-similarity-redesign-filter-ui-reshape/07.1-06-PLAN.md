---
phase: 07.1-positional-only-similarity-redesign-filter-ui-reshape
plan: "06"
type: execute
wave: 4
depends_on: ["07.1-05"]
files_modified:
  - .planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md
autonomous: false
requirements: [GRAPH71-19, GRAPH71-20]
must_haves:
  truths:
    - "No user-similarity edges drawn on the canvas at hub scale"
    - "Pie-glyphs render on users with perms; neutral circles on no-perm users"
    - "Clustering filter section works: simStr sliders affect node position with smooth live reflow (no whole-graph reshuffle)"
    - "User-only view toggle at top of panel toggles between user-only and multi modes"
    - "role-folder edges visible + tier-colored; folder-project edges visible + neutral"
    - "URL state for simStr round-trips (copy URL, paste in fresh tab, filters restored)"
    - "Line 69 gate flag is flipped/removed in the deployed bundle"
    - "3D cheap subset (GRAPH71-20) deferred — recorded in UAT.md with explicit deferral rationale"
  artifacts:
    - path: .planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md
      provides: "Manual UAT checklist + DECISION line"
      contains: "DECISION:"
  key_links:
    - from: "manual UAT"
      to: "/users dashboard page on Railway prod"
      via: "Luis tests at hub scale"
      pattern: "https://.*/users"
---

<objective>
Phase-end manual UAT + administrative close. No code changes here — this is the verification gate that flips Phase 07.1 from "shipped" to "accepted."

Purpose: Phase 07.1 lives or dies by visual correctness at hub scale. Automated tests can't verify "no similarity edges drawn" or "pie-glyphs visible + sized correctly under zoom." Luis owns this verdict.

Output: `UAT.md` with checklist results + DECISION line. Pattern mirrors Phase 7 plan 07-09 PERF-REPORT.md and Phase 4 plan 07 PERF-GATE.md (literal DEFERRED tokens for unmeasurable cells, no fabricated numbers).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-CONTEXT.md
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scaffold UAT.md with checklist + automated signals captured</name>
  <files>.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md</files>
  <action>
Write UAT.md with these sections:

```markdown
# Phase 07.1 — UAT

**Phase:** 07.1-positional-only-similarity-redesign-filter-ui-reshape
**Status:** PENDING-MANUAL-UAT
**Deploy:** [Railway URL — confirm before testing]

## Automated Signals (captured at plan-end)

- Vitest: [run `npx vitest run` and paste pass/fail count]
- tsc: [run `npx tsc --noEmit` and confirm clean]
- Lint: [run `npm run lint` and confirm clean or note pre-existing only]
- Build: [run `npm run build` and confirm exit 0]
- grep gate: `grep -n 'false until.*positional-only' app/(dashboard)/users/AccUsersGraph.tsx` → 0 matches expected
- grep similarity branch: `grep -n 'user-similarity' app/(dashboard)/users/graphRenderers.ts app/(dashboard)/users/accGraphOrganicLayout.ts` → 0 matches expected
- grep Topology literal: `grep -nE '"Topology"|>Topology<' app/(dashboard)/users/AccUsersGraph.tsx` → 0 matches expected

## Manual UAT Checklist (Luis on Railway prod at hub scale)

### Visual contract
- [ ] No user-similarity edges drawn anywhere on /users canvas → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Pie-glyph segments visible on users with permissions → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Users with no permissions render as neutral solid circles → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Pie size feels right under zoom (clamped [6, 14] px) → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] role↔folder edges visible + tier-colored → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] folder↔project edges visible + neutral color → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12

### Filter UI contract
- [ ] "Clustering" section heading present (NOT "Topology") → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] 5 strength sliders (one per dim) + checkboxes → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] "Minimum similarity" slider at bottom of Clustering → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] "User-only view" toggle at TOP of filter panel (above Layout/other sections) → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Tier checkbox swatches double as legend (no separate legend chrome) → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12

### Interaction contract
- [ ] Hover user node → side panel shows tier breakdown + sample folders → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Scrubbing a simStr slider produces smooth local reflow (NOT whole-graph reshuffle) → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Toggling a dim off shrinks that cluster's pull smoothly → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Unchecking a tier hides users whose max tier matches → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12

### URL contract
- [ ] simStr URL key writes when any value !== 1.0 → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Copy URL, paste fresh tab → simStr restored, sliders show non-default values → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Existing Phase 7 URLs (without simStr) still load → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12

### Perf
- [ ] FPS at hub scale with all 5 dims at simStr=1.0 → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12
- [ ] Memory growth on zoom scrub → DEFERRED TO MANUAL UAT — captured at phase end per Luis directive 2026-05-12

## Deferred / Out of Scope (recorded per CONTEXT)
- **GRAPH71-20 (3D cheap subset):** DEFERRED — 3D-WIRING gate from Phase 7 plan 07-01 still records DECISION=3D-WIRING=DEFER; sphere3d/* directory empty. Per RESEARCH §Open Question 4 recommendation, all 3D work waits for the 3D follow-up phase.
- 3D Sphere3DGraph full parity — deferred to follow-up phase.
- "Suggest collaborators" UX, per-dim simMin, domain-tuned default strengths — all deferred per CONTEXT.

## Decision

DECISION: PHASE-07.1-ACCEPT=PENDING-MANUAL-UAT

When Luis runs UAT, flip to one of:
- DECISION: PHASE-07.1-ACCEPT=APPROVED (all checklist items pass)
- DECISION: PHASE-07.1-ACCEPT=GAPS (some items fail → spawn /gsd:plan-phase 07.1 --gaps)
- DECISION: PHASE-07.1-ACCEPT=REVERT (fundamental issue, roll back 07.1 commits)
```

Capture the automated signals at the top by actually running the commands. Use literal DEFERRED tokens for every manual cell — same pattern as Phase 7 plan 07-09 PERF-REPORT.md and Phase 4 plan 07 PERF-GATE.md. No fabricated FPS numbers.
  </action>
  <verify>
    <automated>grep -c "DEFERRED TO MANUAL UAT" .planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md</automated>
  </verify>
  <done>UAT.md exists with checklist + automated signals captured + DECISION line at PENDING-MANUAL-UAT + ≥15 DEFERRED tokens (one per manual cell).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 CHECKPOINT: Luis runs manual UAT on Railway prod</name>
  <files>.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md</files>
  <action>
**This task is a checkpoint — Claude does NOT execute these steps. Luis does.**

What's built (for Luis's reference): The complete Phase 07.1 redesign — similarity → forces, pie-glyph nodes, Clustering filter panel, promoted user-only toggle, simStr URL persistence, line-69 gate flipped.

How to verify (Luis runs these steps):

1. Push deploy branch to origin (Railway auto-deploys per `[[feedback_always_push_railway]]`):
   `git push origin deploy`
2. Wait for Railway deploy to complete.
3. Open the Railway prod URL → /users dashboard.
4. Walk through every checklist item in `.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md` "Manual UAT Checklist" section.
5. For each item: check the box if passing, OR replace the DEFERRED token with a short note describing the gap.
6. Flip the DECISION line at the bottom of UAT.md:
   - All pass → `DECISION: PHASE-07.1-ACCEPT=APPROVED`
   - Some fail → `DECISION: PHASE-07.1-ACCEPT=GAPS` + list gaps, then run `/gsd:plan-phase 07.1 --gaps`
   - Fundamental issue → `DECISION: PHASE-07.1-ACCEPT=REVERT`

Resume signal: Luis types "approved" or describes gaps.
  </action>
  <verify>
    <automated>grep -E "^DECISION: PHASE-07\\.1-ACCEPT=(APPROVED|GAPS|REVERT)" .planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/UAT.md</automated>
  </verify>
  <done>UAT.md DECISION line flipped from PENDING-MANUAL-UAT to APPROVED, GAPS, or REVERT by Luis.</done>
</task>

</tasks>

<verification>
- UAT.md present at phase root
- Automated signals captured (Vitest pass count, tsc clean, build exit code, grep negative results)
- All manual cells carry literal DEFERRED token until Luis fills them
- DECISION line present
- GRAPH71-20 explicitly marked deferred with rationale
</verification>

<success_criteria>
- GRAPH71-19: UAT framework in place; Luis owns the verdict
- GRAPH71-20: 3D cheap subset deferred per gate, recorded in UAT.md
</success_criteria>

<output>
Create `.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-06-SUMMARY.md` recording: automated signals snapshot, deferred token count, Luis's verdict timestamp (when filled), final DECISION value.
</output>
