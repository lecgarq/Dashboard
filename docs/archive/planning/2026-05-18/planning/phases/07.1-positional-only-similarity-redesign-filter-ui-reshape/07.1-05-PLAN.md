---
phase: 07.1-positional-only-similarity-redesign-filter-ui-reshape
plan: "05"
type: execute
wave: 3
depends_on: ["07.1-04"]
files_modified:
  - app/(dashboard)/users/AccUsersGraph.tsx
autonomous: true
requirements: [GRAPH71-04, GRAPH71-06, GRAPH71-07, GRAPH71-08, GRAPH71-09, GRAPH71-10, GRAPH71-11, GRAPH71-12, GRAPH71-17, GRAPH71-18]
must_haves:
  truths:
    - "Filter panel: 'Topology' section renamed 'Clustering'; 5 dim rows each have checkbox + 0-1 slider; simMin renamed 'Minimum similarity' at bottom of Clustering"
    - "'User-only view' toggle is at top of filter panel (above all sections), not buried inside Clustering"
    - "Tier swatches (the 4 permTier checkboxes) double as on-screen legend — no separate legend chrome rendered"
    - "User nodes render pie-glyphs sized by zoom (clamped [6, 14] px); no-perm users render neutral solid circle"
    - "Hovering a user node shows tier breakdown + sample folders in the existing side panel"
    - "simStr URL key (compact letter aliased, default-only-write) round-trips through readFiltersFromUrl/writeFiltersToUrl"
    - "Hot-path simStr scrub calls renderer.updateForceLinkStrengths ONLY (no setLinks)"
    - "Line 69 gate flag flipped from false to true (or comment + commented-out flag removed entirely)"
  artifacts:
    - path: app/(dashboard)/users/AccUsersGraph.tsx
      provides: "Filter panel reshape + pie-glyph wiring + simStr URL + hot-path strength update + gate flip"
      contains: "Clustering"
      contains_not: "Topology"
  key_links:
    - from: "AccUsersGraph filter panel"
      to: "accGraphFilters.GraphFilters.simStr (Plan 07.1-03)"
      via: "5 SliderControl onValueChange handlers"
      pattern: "simStr"
    - from: "AccUsersGraph effect on filters.simStr"
      to: "renderer.updateForceLinkStrengths"
      via: "recompose strengths via buildForceLinkBuffer (or strengths-only path), call updateForceLinkStrengths"
      pattern: "updateForceLinkStrengths"
    - from: "AccUsersGraph node render"
      to: "cosmos graph.setImageData + setPointImageIndices"
      via: "buildPieGlyphImageData() at mount; per-user index via tierPortfolioKey"
      pattern: "setImageData|setPointImageIndices"
    - from: "AccUsersGraph zoom handler"
      to: "graph.setPointImageSizes"
      via: "pow(zoom, 0.2) clamped [6, 14] (mirrors label polish curve)"
      pattern: "setPointImageSizes"
    - from: "AccUsersGraph readFiltersFromUrl / writeFiltersToUrl"
      to: "URL key 'simStr' parsing/serialization"
      via: "parseSimStr/writeSimStr per RESEARCH §Code Examples #3"
      pattern: "simStr"
---

<objective>
The single big UI plan — reshape the 2D AccUsersGraph filter panel to the Clustering mental model, wire pie-glyphs onto cosmos.gl, add simStr URL round-trip, route simStr scrubs through the hot-path `updateForceLinkStrengths`, and flip the gate flag at line 69.

Purpose: This plan makes the redesign user-visible. All upstream pure modules (07.1-01..03) and renderer plumbing (07.1-04) culminate here.

Output: Updated `AccUsersGraph.tsx` (one file, multiple distinct chunks). Visual UAT happens in 07.1-06.

NOTE: This is a large plan (one file, ~6 distinct chunks). Boundary justified by: (a) the entire file is the integration surface — splitting would force passing local state across plans; (b) every chunk is reading/writing the same component-local refs; (c) Phase 7 plan 07-06 already proved a comparable 3-chunk single-file edit is tractable (Task 1+2, commits 0eb3cd9 + 1183e32). Treat this as one plan executed as a single context block.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-RESEARCH.md
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-01-SUMMARY.md
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-02-SUMMARY.md
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-03-SUMMARY.md
@.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-04-SUMMARY.md
@app/(dashboard)/users/AccUsersGraph.tsx
@lib/acc/similarityForceModel.ts
@lib/acc/pieGlyphRaster.ts
@lib/acc/userMaxPermTier.ts

<interfaces>
Key call surfaces (from earlier plans):
```typescript
// lib/acc/similarityForceModel.ts
buildForceLinkBuffer(input: StrengthInput): ForceLinkBuffer;

// lib/acc/pieGlyphRaster.ts
buildPieGlyphImageData(): { images: ImageData[]; keyToIndex: Map<string, number> };
tierPortfolioKey(tiers: PermTier4[]): string;

// lib/acc/userMaxPermTier.ts
computeUserMaxPermTiers(input): Map<string, PermTier4 | null>;
getUserTierPortfolio(input, userId): PermTier4[];

// app/(dashboard)/users/graphRenderers.ts (Plan 07.1-04)
renderer.setForceLinkChannel(pairs: Int32Array, strengths: Float32Array): void;
renderer.updateForceLinkStrengths(strengths: Float32Array): void;
```

Existing surfaces in AccUsersGraph.tsx (READ before editing):
- Line 69: gate flag `// false until the positional-only redesign lands` — flip here
- Lines 79-92: PERM_TIER_COLOR + FOLDER_PROJECT_EDGE_COLOR LUTs (reuse, don't redefine)
- Lines 430-516: readFiltersFromUrl / writeFiltersToUrl (extend with simStr)
- Line 955: buildExtendedTopology (where forceLinks now lives — pass through)
- Line 2516: phase7TopologyActive (review whether this gate still gates anything)
- Lines 2594+: filter panel structure (Layout section sits above Topology)
- Lines 2742-2856: Topology section UI — rename to Clustering + add 5 strength sliders + reposition simMin
- Line 2750-2763: ToggleFilterControl "View mode" — promote OUT of section, mount at top of panel above Layout
- Lines 2796-2800: existing tier swatches (drop separate legend; these ARE the legend)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter panel reshape — Clustering section + promoted user-only toggle + tier swatches as legend</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
1. Rename literal text on filter panel: `"Topology"` → `"Clustering"` (search at line ~2746).
2. Drop the always-on conditional that wrapped Topology section behind `graphMode === "folder-permissions"` (line ~2743 per RESEARCH §State of the Art). Clustering section should be **always visible** because `viewMode === "user-only"` is the new default.
3. Promote "View mode" / "User-only view" ToggleFilterControl OUT of Clustering section. Mount it at the top of the filter panel, ABOVE the Layout section. Render as a single labeled toggle "User-only view" (when off, viewMode = "multi"; when on, "user-only"). Keep URL key `view` semantics unchanged.
4. Replace the 5 simDim checkboxes with 5 rows of `{ Checkbox + SliderControl 0-1 step=0.05 }`. Checkbox toggles dim in `filters.simDims`; SliderControl drives `filters.simStr[dim]`. When checkbox OFF, slider visually disabled (opacity 50%, ignore changes).
5. Rename `simMin` slider label "Minimum similarity"; keep at bottom of Clustering.
6. Tier swatches: ensure the 4 permTier checkboxes render their color swatch dot (lines ~2796-2800). Remove any separate legend chrome rendered elsewhere in the file (grep `legend` or `Legend` for any standalone chunk; nuke or repurpose). The swatch + label IS the legend.
7. The `Show folders` toggle: relocate to Layout/View group if not already there; grey out (disabled) when `viewMode === "user-only"` (Claude's Discretion per RESEARCH Open Question 6).

Verify: `npm run build` succeeds OR `npx tsc --noEmit` clean. Manually open the file and confirm no "Topology" literal remains in the JSX section (`grep -n '"Topology"' app/(dashboard)/users/AccUsersGraph.tsx` should return zero matches).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; ! grep -nE '"Topology"|>Topology<' app/(dashboard)/users/AccUsersGraph.tsx</automated>
  </verify>
  <done>Clustering section present with 5 strength sliders, user-only toggle promoted, tier swatches = legend, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Pie-glyph + maxPermTier wiring + tooltip extension + simStr URL round-trip</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
1. At mount (one-shot useMemo or useEffect with empty dep array), call `buildPieGlyphImageData()` once and pass to `graph.setImageData(images)`. Cache `keyToIndex`.
2. Compute `userMaxPermTiers` and `userTierPortfolios` via `computeUserMaxPermTiers({ userRoleIds, folderMatrix: folderMatrixQuery.data })`, memoized on `[userRoleIds, folderMatrixQuery.data]`. (`userRoleIds` is built from snapshot.userRoleAssignments — find existing similar mapping in AccUsersGraph for shape; Phase 7 plan 07-06 already builds a transitive folder-access SimilarityInput, reuse that user→roleIds derivation.)
3. Attach `maxPermTier` to each user node in the FilterableNode-shaped object passed to `nodeMatchesFilters` (filter mapping path). Plan 07.1-03 already added the field; just supply it.
4. For each user node index, look up `tierPortfolioKey(userTierPortfolios.get(userId))` → `keyToIndex.get(key)` → push into a Float32Array; pass to `graph.setPointImageIndices(indices)`. For users with empty portfolio (no perms), set the index to `-1` (cosmos.gl treats negative as "no image" → falls back to shape).
5. Zoom curve for pie size: hook the existing zoom handler (find via grep for `pointSizeScale` or `setPointSizes`); per zoom change compute `size = clamp(64 * pow(zoom, 0.2), 6, 14)`. Call `graph.setPointImageSizes(new Float32Array(...).fill(size))` — single value broadcast across all points by uniform fill. (Mirrors label polish curve memory `[[project_label_polish_curve]]`.)
6. Tooltip: extend the existing side panel body for user selection (find via grep for `SidePanelState` or hover handler). Add a "Permissions" section: tier breakdown counts (per tier: number of role-folder edges contributing) + "Sample folders" list (top 5 by tier rank). Data: derive from `folderMatrixQuery.data` filtered to the user's roleIds.
7. URL round-trip for `simStr`: implement `parseSimStr` and `writeSimStr` per RESEARCH §Code Examples #3 (lines 478-507). Wire into existing `readFiltersFromUrl` (must spread `...DEFAULT_FILTERS` before assigning, per STATE.md decision 07-04 Rule 3) and `writeFiltersToUrl`. URL key: `simStr`. Default `1,1,1,1,1` is NOT written.
   Also: existing tier-edge filter usage of `filters.permTiers` in `colorForTopologyLink` must be removed (Plan 07.1-04 deleted that branch in graphRenderers; if any AccUsersGraph-local consumer still passes permTiers as edge-color gate, drop it here too — node-level only per Pitfall 7).

Verify: tsc clean. Open the file at line 69 and confirm the gate flag is flipped (next task) but first check the new wiring builds cleanly.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -n "setImageData\\|setPointImageIndices\\|setPointImageSizes\\|updateForceLinkStrengths\\|simStr" app/(dashboard)/users/AccUsersGraph.tsx | wc -l</automated>
  </verify>
  <done>Pie-glyph LUT bound, per-user indices set, zoom curve drives image sizes, tooltip extended, simStr URL round-trips; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Hot-path simStr scrub + force-link channel feed + flip line 69 gate</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
1. Shape-path effect (depends on `[filters.simDims, filters.simMin, snapshot, folderMatrixQuery.data]` — exclude simStr!):
   - Build similarity input from snapshot (reuse Phase 7 plan 07-06 derivation).
   - Call `computeSimilarityEdges(input, filters.simDims, filters.simMin)`.
   - Call `buildForceLinkBuffer({ edges, simStr: filters.simStr, pairStrengthCutoff: 0, topK: TOPK_CAP, nodeIndex, maxStrength: 5 })` where `TOPK_CAP` is the constant chosen in Plan 07.1-01 TOPK-PERF.md (default 20 if measurement deferred).
   - Cache the resulting buffer + the underlying edges/dimMax in a ref for hot-path recomposition.
   - Call `renderer.setForceLinkChannel(forceLinkBuffer.pairs, forceLinkBuffer.strengths)`.

2. Hot-path effect (depends ONLY on `[filters.simStr]`):
   - From the cached edges + dimMax ref, recompose strengths only — DO NOT rebuild pairs.
   - Pure helper inline: walk edges, multiply by `simStr[dim] * (sharedCount / dimMax[dim])`, sum per canonical pair, cap at maxStrength. (Or expose a `recomposeStrengths` helper from `lib/acc/similarityForceModel.ts` — Claude's Discretion; preferred: add `recomposeStrengths(cachedEdges: CachedEdgeArray, simStr): Float32Array` to that module.)
   - Call `renderer.updateForceLinkStrengths(strengths)` — NEVER `setForceLinkChannel`.
   - This is the "live reflow, smooth perturbation" path (Pitfall 1).

3. Flip the gate flag at line 69:
   - Read line 69 first to see exact current form (the flag is commented-out per the line-69 comment: `false until positional-only lands`).
   - Either: (a) remove the comment + commented-out flag entirely (cleanest); or (b) set the flag to `true`. Pick (a) unless the flag is actively referenced elsewhere in the file (grep first). If referenced, set to `true` and update the comment.

4. Remove any orphaned imports / dead code: e.g., `SIM_DIM_COLOR` LUT (was used to color similarity edges in Phase 7; now dead). Grep `SIM_DIM_COLOR` — if only declared and unused, delete.

Verify: tsc clean; `npm run lint` clean (or at least no new errors); the existing test file `app/(dashboard)/users/cosmosUtils.test.ts` still passes.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run app/(dashboard)/users/ &amp;&amp; ! grep -nE 'false until.*positional-only' app/(dashboard)/users/AccUsersGraph.tsx</automated>
  </verify>
  <done>Hot-path simStr scrub uses updateForceLinkStrengths only; shape-path rebuilds force buffer on simDims/simMin/data change; line 69 gate flipped (or removed); tsc + vitest clean.</done>
</task>

</tasks>

<verification>
- `grep '"Topology"\|>Topology<' app/(dashboard)/users/AccUsersGraph.tsx` → 0 matches
- `grep "Clustering" app/(dashboard)/users/AccUsersGraph.tsx` → 1+ match (section heading)
- `grep "simStr" app/(dashboard)/users/AccUsersGraph.tsx` → multiple matches (URL helpers + effects + 5 sliders)
- `grep "setImageData\|setPointImageIndices" app/(dashboard)/users/AccUsersGraph.tsx` → 1+ each
- `grep "updateForceLinkStrengths" app/(dashboard)/users/AccUsersGraph.tsx` → 1+ (hot-path effect)
- `grep "false until.*positional-only" app/(dashboard)/users/AccUsersGraph.tsx` → 0 (gate removed/flipped)
- tsc + vitest clean
</verification>

<success_criteria>
- GRAPH71-04: hot-path strength-only update wired (simStr effect calls only updateForceLinkStrengths)
- GRAPH71-06: Topology renamed Clustering
- GRAPH71-07: user-only view toggle promoted to top of filter panel
- GRAPH71-08: tier swatches = legend (no separate legend chrome)
- GRAPH71-09: pie-glyph LUT bound to graph.setImageData; per-user indices set
- GRAPH71-10: zoom-clamped [6, 14] px pie size via setPointImageSizes
- GRAPH71-11: no-perm users render neutral (index -1 → shape fallback)
- GRAPH71-12: tooltip carries tier breakdown + sample folders
- GRAPH71-17: simStr URL key round-trips
- GRAPH71-18: line 69 gate flipped (or removed)
</success_criteria>

<output>
Create `.planning/phases/07.1-positional-only-similarity-redesign-filter-ui-reshape/07.1-05-SUMMARY.md` recording: chosen TOPK_CAP constant, whether gate was deleted vs flipped, any deviations from RESEARCH (e.g., if zoom curve normalization needed adjustment), Vitest results, screenshot-worthy file lines for the manual UAT in Plan 07.1-06.
</output>
