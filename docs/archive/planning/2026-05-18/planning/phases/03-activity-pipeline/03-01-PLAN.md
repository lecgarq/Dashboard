---
phase: 03-activity-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
autonomous: true
requirements:
  - ACTV-01
  - ACTV-02
  - ACTV-04
must_haves:
  truths:
    - "AccActivity schema supports lossless raw action + autodeskId + userEmail + sourceFile + dedup key"
    - "createMany({ skipDuplicates: true }) becomes a real ON CONFLICT DO NOTHING — duplicate ingest does not grow row count"
    - "unzipper and csv-parse are installed and importable in Node 22 runtime"
    - "UnresolvedAttribution table exists for forensic logging of failed inviter matches"
  artifacts:
    - path: "package.json"
      provides: "unzipper + csv-parse dependencies"
      contains: "unzipper"
    - path: "prisma/schema.prisma"
      provides: "AccActivity ALTER + UnresolvedAttribution model + new indexes"
      contains: "model UnresolvedAttribution"
    - path: "prisma/schema.prisma"
      provides: "AccActivity dedup unique constraint"
      contains: "@@unique([autodeskId"
  key_links:
    - from: "AccActivity"
      to: "AccProjectMember"
      via: "userEmail (lowercased) lookup at query time"
      pattern: "userEmail.*String"
    - from: "AccActivity"
      to: "deep-sync-ingest dedup"
      via: "@@unique composite key on (autodeskId, rawAction, createdAt, projectId)"
      pattern: "@@unique"
---

<objective>
Establish the Phase 3 foundation: install streaming-ingest dependencies (unzipper, csv-parse), extend AccActivity with the columns and dedup constraint the ingest pipeline requires, and add UnresolvedAttribution for forensic logging. This plan is intentionally migration-only — no application code changes — so the Prisma migration can be reviewed in isolation and applied to Railway before Wave 1 begins.

Purpose: ACTV-01/02/04 cannot land until the schema can store autodeskId, lowercased userEmail, raw action strings, sourceFile marker, and a dedup unique key. Without the dedup, every nightly re-ingest of PAST_7_DAYS overlapping range will multiply rows 7×.

Output: package.json + package-lock.json with unzipper + csv-parse pinned; prisma/schema.prisma with AccActivity v2 columns and UnresolvedAttribution model; new migration applied locally (`npx prisma migrate dev`) and verified with `npx prisma validate`.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-activity-pipeline/03-CONTEXT.md
@.planning/phases/03-activity-pipeline/03-RESEARCH.md
@prisma/schema.prisma

<interfaces>
Current `AccActivity` model at prisma/schema.prisma:530-543 (verified by researcher):
```prisma
model AccActivity {
  id         String   @id @default(cuid())
  autodeskId String
  projectId  String?
  action     String
  service    String?
  tool       String?
  details    String?
  createdAt  DateTime
  // indexes: (autodeskId, createdAt DESC), (projectId, createdAt DESC), (action)
}
```

Gaps to close in this plan:
- ADD `userEmail String?` (lowercased at ingest time)
- ADD `sourceFile String` ("project" | "admin") to distinguish CSVs
- ADD `rawAction String` (replaces or aliases `action` — see Task 2 decision branch)
- ADD `@@unique([autodeskId, rawAction, createdAt, projectId])` for dedup — with sentinel `""` for null projectId on admin rows (CONTEXT discretion + RESEARCH Open Q3 resolution)
- ADD `@@index([userEmail, createdAt(sort: Desc)])` — drives ACTV-03 lazy query
- ADD model `UnresolvedAttribution` per RESEARCH Code Example shape
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install unzipper and csv-parse</name>
  <files>package.json, package-lock.json</files>
  <action>Run `npm install unzipper@^0.12.3 csv-parse@^6.2.1` (versions verified by researcher; types ship in-package, no @types/* needed). Verify both appear in package.json `dependencies` (not devDependencies — used by production cron). Verify Node import works:
```bash
node -e "const u=require('unzipper'); const c=require('csv-parse'); console.log(typeof u.Parse, typeof c.parse);"
```
Expect: `function function`. If unzipper transitive deps (bluebird, duplexer2, fs-extra, graceful-fs, node-int64) trigger any vulnerability warnings, document in SUMMARY but do not block — researcher already accepted them.</action>
  <verify>
    <automated>node -e "const u=require('unzipper'); const c=require('csv-parse'); if(typeof u.Parse!=='function' || typeof c.parse!=='function') process.exit(1);"</automated>
  </verify>
  <done>package.json contains "unzipper": "^0.12.3" and "csv-parse": "^6.2.1" in dependencies; require() works in Node 22.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AccActivity + add UnresolvedAttribution model</name>
  <files>prisma/schema.prisma</files>
  <action>BEFORE editing schema: run `npx prisma db execute --stdin <<< 'SELECT count(*) FROM "AccActivity";'` against the local dev DB (or Railway if local is empty) to determine whether `action` rename is safe. If count is 0, rename `action` → `rawAction`. If count > 0, KEEP `action` as-is (treat it as raw by convention; document in SUMMARY) and add no `rawAction` column.

Modify `AccActivity` model (prisma/schema.prisma around line 530):
- ADD `userEmail String?` (will be lowercased at ingest; nullable until backfill).
- ADD `sourceFile String @default("project")` ("project" or "admin").
- IF rename safe: rename `action` → `rawAction`. ELSE keep `action`.
- ADD composite unique: `@@unique([autodeskId, action, createdAt, projectId])` (use `rawAction` if renamed). NOTE: Postgres treats NULL != NULL in unique constraints, so admin rows with `projectId IS NULL` will not dedup. The ingest pipeline (Plan 02) will write a sentinel `""` for admin rows to make the constraint effective. Document this in a schema comment: `// projectId: empty-string sentinel for admin rows so @@unique dedup works`.
- ADD `@@index([userEmail, createdAt(sort: Desc)])` — drives ACTV-03 `getFileActivityForUser` lookup.
- KEEP existing indexes: `(autodeskId, createdAt DESC)`, `(projectId, createdAt DESC)`, `(action)` or `(rawAction)`.

ADD new model `UnresolvedAttribution` (place after AccActivity model):
```prisma
model UnresolvedAttribution {
  id          String   @id @default(cuid())
  activityId  String                                // logical FK to AccActivity.id (not enforced — soft retention)
  rawEmail    String?                               // parsed from details column
  rawDetails  String?                               // full details for forensics
  reason      String                                // "no_email_match" | "no_autodesk_id_match" | "ambiguous_match"
  createdAt   DateTime @default(now())

  @@index([reason])
  @@index([createdAt])
}
```

After edits run `npx prisma format` then `npx prisma validate`. Both must exit 0.

Use Prisma for the migration — NOT manual SQL. Do not hand-roll ALTER statements.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>`npx prisma validate` exits 0; AccActivity has userEmail + sourceFile + @@unique dedup key + (userEmail, createdAt DESC) index; UnresolvedAttribution model exists; rename decision documented in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and apply Prisma migration</name>
  <files>prisma/migrations/{timestamp}_acc_activity_v2/migration.sql</files>
  <action>Run `npx prisma migrate dev --name acc_activity_v2 --create-only` to generate the migration SQL without applying. Inspect the generated SQL:
- ALTER TABLE adds `userEmail` and `sourceFile` columns.
- Adds composite unique index for dedup.
- Adds (userEmail, createdAt DESC) index.
- Creates UnresolvedAttribution table.

VERIFY the migration is **additive-only** (no DROP COLUMN, no destructive rename unless Task 2 confirmed zero rows). If Prisma generated a destructive rename for action→rawAction despite Task 2 showing >0 rows, abort and revert Task 2.

Then apply: `npx prisma migrate dev` (no --create-only flag). This applies the migration to the local dev DB AND regenerates Prisma Client. Verify Prisma Client has new fields:
```bash
node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); console.log(Object.keys(p.accActivity), typeof p.unresolvedAttribution);"
```
Expect `unresolvedAttribution` to be a function/object.

Note: Railway migration application is the operator's responsibility post-merge; this task only proves the migration is well-formed and applies cleanly to dev.</action>
  <verify>
    <automated>npx prisma migrate status</automated>
  </verify>
  <done>Migration file generated under prisma/migrations/; `npx prisma migrate status` reports "Database schema is up to date"; new Prisma Client exposes `prisma.unresolvedAttribution` accessor.</done>
</task>

</tasks>

<verification>
- `npm ls unzipper csv-parse` reports both at expected versions, no UNMET PEER.
- `npx prisma validate` exits 0.
- `npx prisma migrate status` reports schema in sync with migrations folder.
- Local DB query `SELECT column_name FROM information_schema.columns WHERE table_name = 'AccActivity'` returns `userEmail`, `sourceFile`.
- `SELECT * FROM "UnresolvedAttribution" LIMIT 0` succeeds.
</verification>

<success_criteria>
- unzipper@^0.12.3 and csv-parse@^6.2.1 installed and required by Node 22.
- AccActivity has userEmail (nullable, lowercased convention), sourceFile, @@unique dedup composite key, and (userEmail, createdAt DESC) index.
- UnresolvedAttribution model exists with reason + createdAt indexes.
- Migration applied locally; ready for Railway promotion.
- No destructive renames on tables with existing rows.
</success_criteria>

<output>
After completion, create `.planning/phases/03-activity-pipeline/03-01-SUMMARY.md` documenting:
- The action/rawAction rename decision (and the row count that drove it).
- The exact migration filename + checksum.
- Any unzipper transitive-dep vulnerability warnings observed.
- Confirmation that Plan 02 can now import { parse } from "csv-parse" and unzipper from "unzipper".
</output>
