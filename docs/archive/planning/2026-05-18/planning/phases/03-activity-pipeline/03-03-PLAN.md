---
phase: 03-activity-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - app/(dashboard)/users/UsersDirectoryClient.tsx
  - app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
autonomous: true
requirements:
  - ACTV-03
  - ACTV-05
must_haves:
  truths:
    - "User list shows a grouped 'File Activity' header spanning View/Upload/Edit/Delete sub-columns"
    - "Hovering a user-list row debounces a tRPC prefetch for that user's file-activity timestamps; opening the side panel reads from cache (no extra request)"
    - "Cells show relative time (e.g., '2d ago') with absolute date on hover tooltip; 'Never' values sort to bottom"
    - "DashboardSidePanel exposes a paginated, type-grouped activity drill-down for a selected user with Date Range and Project filters"
    - "Drill-down sections (Files / Member events / Project events / Other) show counts, expand by default, paginate at 25 rows with 'Load more' inside each section"
  artifacts:
    - path: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      provides: "Grouped 'File Activity' multi-row header + 4 cells per row + hover prefetch"
      contains: "getFileActivityForUser"
    - path: "app/(dashboard)/users/dashboard/DashboardSidePanel.tsx"
      provides: "User-activity body kind with nested type-section drill-down"
      contains: "listForUser"
  key_links:
    - from: "UsersDirectoryClient row"
      to: "trpc.accActivity.getFileActivityForUser"
      via: "useUtils().prefetch on debounced mouseEnter"
      pattern: "getFileActivityForUser\\.prefetch"
    - from: "DashboardSidePanel"
      to: "trpc.accActivity.listForUser"
      via: "cursor-paginated useInfiniteQuery per type-section"
      pattern: "listForUser"
---

<objective>
Wire the user-list and side-panel UI to the accActivity tRPC procedures from Plan 02. Adds the grouped 'File Activity' multi-row header to UsersDirectoryClient with hover prefetch, and extends DashboardSidePanel with a nested type-section activity drill-down per ACTV-05.

Purpose: ACTV-03 contract says the per-user activity query must be lazy — only fired on user interaction (hover or panel open). This plan implements that contract on the consumption side. ACTV-05 contract demands type-grouped pagination, expand-by-default, 25 rows per section, "Load more" inside each section.

Output: Visible 4-column file-activity strip in user list; on-hover prefetch; side-panel drill-down with three type sections (Files / Member events / Project events) + Other catch-all, each independently paginated and filterable by Date Range + Project.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-activity-pipeline/03-CONTEXT.md
@.planning/phases/03-activity-pipeline/03-RESEARCH.md
@.planning/phases/03-activity-pipeline/03-02-SUMMARY.md
@app/(dashboard)/users/UsersDirectoryClient.tsx
@app/(dashboard)/users/dashboard/DashboardSidePanel.tsx

<interfaces>
Plan 02 contract (consumed here):
```typescript
trpc.accActivity.getFileActivityForUser.useQuery({ email: string })
  → { lastView: Date|null, lastUpload: Date|null, lastEdit: Date|null, lastDelete: Date|null }

trpc.accActivity.listForUser.useInfiniteQuery({
  email: string,
  categories?: ActivityCategory[],
  projectId?: string,
  dateRange?: { from: Date, to: Date },
  cursor?: { createdAt: Date, id: string },
  limit: number  // default 25
}) → { rows: AccActivity[], nextCursor: {createdAt, id} | null }
```

ActivityCategory values: "view" | "upload" | "edit" | "delete" | "memberEvent" | "projectEvent" | "other".

UI section grouping (CONTEXT lock):
- "Files" section → categories: [view, upload, edit, delete]
- "Member events" section → [memberEvent]
- "Project events" section → [projectEvent]
- "Other" section → [other]

Date-fns is already installed; use `formatDistanceToNow(date, { addSuffix: true })` for "2d ago".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UsersDirectoryClient — grouped header + hover prefetch + 4 cells</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>Modify the user list table:

1. **Grouped header** (LIST-03 prep / CONTEXT lock): Add a two-row table header. Top row has a `<th colSpan={4} className="text-center border-b">File Activity</th>` spanning the four sub-columns; other existing columns use `rowSpan={2}`. Bottom row has four sub-headers: `View | Upload | Edit | Delete` (CONTEXT.md grants exact column order discretion — use this order: chronologically descending intensity). Each sub-header is clickable: clicking sorts the table by that timestamp DESCENDING; `Never` values sort to bottom (use `.sort((a,b) => (a===null) - (b===null) || b - a)` pattern).

2. **Hover prefetch** (ACTV-03): Each user row registers `onMouseEnter` and `onMouseLeave`. On enter, start a 250ms debounce (per CONTEXT.md "200-300ms" discretion); when the debounce fires, call `utils.accActivity.getFileActivityForUser.prefetch({ email })`. On leave, cancel the debounce. Use `useDebouncedCallback` if already in codebase (grep first); otherwise a 12-line `useRef + setTimeout` hook is acceptable. Add `staleTime: 5 * 60_000` to the underlying `useQuery` call so back-and-forth hovering doesn't refire.

3. **Four cells per row**: Each row reads from `trpc.accActivity.getFileActivityForUser.useQuery({email}, {enabled: hovered || inSidePanel, staleTime: 5*60_000})`. While `enabled === false`, render skeleton dashes (`—`) — this honors the "NOT eager-loaded" contract since the query doesn't fire until interaction.

4. **Cell format**: When data arrives, render `formatDistanceToNow(date, {addSuffix: true})` (e.g., "2 days ago" — short form via `formatDistanceToNowStrict` if preferred for compactness). Wrap in `<Tooltip>` (use existing dashboard tooltip primitive — grep `@radix-ui/react-tooltip` or `components/ui/tooltip`) showing the absolute ISO timestamp. If date is null → render `<span className="text-muted-foreground">Never</span>`.

5. **Open side panel on cell click**: Clicking any of the four cells opens DashboardSidePanel with `kind: "userActivity"` (or whatever discriminator the panel uses — see Task 2) and the user's email. This is the bridge to the drill-down.

DO NOT eager-load file activity for the full visible list. The query MUST be gated by `enabled: hovered || panelOpenForThisUser`.</action>
  <verify>
    <automated>npx tsc --noEmit app/(dashboard)/users/UsersDirectoryClient.tsx && npm run lint -- app/(dashboard)/users/UsersDirectoryClient.tsx</automated>
  </verify>
  <done>User list renders 'File Activity' grouped header + 4 sub-columns; rows show '—' until first hover; hover triggers a single tRPC request after 250ms; data caches; sub-header sort works with Never-to-bottom; clicking a cell opens DashboardSidePanel.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardSidePanel — userActivity body with nested type sections</name>
  <files>app/(dashboard)/users/dashboard/DashboardSidePanel.tsx</files>
  <action>Extend the panel's discriminated-union `body` to support a new kind: `{ kind: "userActivity", email: string }`. Reuse the existing `kind: "admin"` shell if it already accepts an email and renders a profile header — extend rather than fork if the shape is compatible (verify by reading the current file structure first).

UI layout (top to bottom):
1. **Header**: User name + avatar + email (reuse existing profile-card pattern).
2. **Filter bar** (compact row, sticky if the panel scrolls):
   - `[Date range ▾]` — popover with presets (All time, Last 7d, Last 30d, Last 90d, Custom). Default: **All time** (CONTEXT lock).
   - `[Project ▾]` — popover with project search; default: **All projects** (CONTEXT lock).
3. **Type sections** — render in this fixed order:
   - "Files (N)" — categories: ["view","upload","edit","delete"]
   - "Member events (N)" — categories: ["memberEvent"]
   - "Project events (N)" — categories: ["projectEvent"]
   - "Other (N)" — categories: ["other"]
   Each section header shows count (N) — derive from a `count` query OR from the cumulative `rows.length` of paginated results (cheaper; document choice in SUMMARY).
   Each section is **expanded by default** (CONTEXT lock) but collapsible (chevron toggle); state stored in component-local `useState<Record<sectionKey, boolean>>` defaulting to all-true.
4. **Row rendering** inside each section:
   - Format: `{actionLabel} → {target} · {projectName} · {relativeTime}` (e.g., "Uploaded → Foo.dwg · ACME Tower · 2 days ago"). `target` derived from `details` (parse heuristically — fallback to raw details if no clear target). `projectName` looked up via projectId → AccProject in the tRPC response shape (extend Plan 02's listForUser response if needed to include projectName; if Plan 02 didn't, fetch project map once at panel open via a cached `accSync.listProjects` or equivalent — check existing routers).
   - Rows are NON-INTERACTIVE in v1 (CONTEXT lock: "Phase 5 may add cross-widget spotlight"). No click handler, no cursor:pointer.
5. **Pagination**: Each section uses `trpc.accActivity.listForUser.useInfiniteQuery({email, categories: <section's categories>, projectId, dateRange, limit: 25})`. Render rows + "Load more" button at section bottom when `hasNextPage`. Initial load fetches first page of all 4 sections in parallel (4 simultaneous queries — acceptable; React Query dedupes).
6. **Empty states** (CONTEXT lock):
   - User has zero activity at all → `<EmptyState>No activity yet</EmptyState>` (no sections rendered).
   - Filters return zero rows in this section → inline section message: `No activity in this range` + a small "Clear filters" link that resets dateRange + project filter.

Re-use existing Sheet / Tabs / Popover primitives from `components/ui/*`. Do not introduce a new modal library. Avatar sizes, color tokens, spacing — match existing dashboard design system (CONTEXT discretion).

Mobile responsiveness: the existing DashboardSidePanel renders as a Sheet on mobile; preserve that. Filter bar may wrap to two rows on narrow screens.

Performance: Initial open = 4 paginated queries (one per type section) at 25 rows each = ~100 rows fetched. Acceptable. Subsequent "Load more" clicks add 25 rows to that section only.</action>
  <verify>
    <automated>npx tsc --noEmit app/(dashboard)/users/dashboard/DashboardSidePanel.tsx && npm run lint -- app/(dashboard)/users/dashboard/DashboardSidePanel.tsx</automated>
  </verify>
  <done>DashboardSidePanel handles `kind: "userActivity"`; renders 4 type sections expanded-by-default with counts; Date Range + Project filters update all sections; 25-row pagination with per-section "Load more" works; empty states render correctly; rows are non-interactive.</done>
</task>

</tasks>

<verification>
- TypeScript typechecks across both modified files.
- Manual UAT (operator): navigate to /users, hover a row, observe a single network request to accActivity.getFileActivityForUser after ~250ms; click a file-activity cell, observe side panel opens with 4 sections populated; toggle a section closed/open; click "Load more" inside Files section to fetch next 25 rows; change Date Range to Last 7d, observe all 4 sections refetch.
- Empty user (no activity in DB): side panel renders "No activity yet" without rendering empty sections.
</verification>

<success_criteria>
- ACTV-03 contract honored: file-activity query never fires before hover/panel-open.
- ACTV-05 contract honored: nested type-section grouping, expand-by-default, counts in headers, 25 rows + "Load more" per section, filter row above sections, correct empty states.
- LIST-03 partial: 4-column grouped "File Activity" header with relative-time cells (full LIST-03 status/products/etc. comes in Phase 5).
- All filter changes propagate to all 4 sections (single source of truth in panel state).
</success_criteria>

<output>
After completion, create `.planning/phases/03-activity-pipeline/03-03-SUMMARY.md` documenting:
- The debounce timing chosen (250ms suggested).
- Whether section count (N) is from a count query or from the cumulative rows-loaded.
- The target/projectName derivation approach for the row label format.
- Confirmation that hover-only prefetch is observable in the network tab.
</output>
