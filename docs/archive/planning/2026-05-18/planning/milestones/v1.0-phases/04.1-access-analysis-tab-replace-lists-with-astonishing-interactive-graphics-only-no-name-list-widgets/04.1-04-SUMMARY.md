---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 04
subsystem: ui
tags: [dashboard, design-tokens, framer-motion, echarts, xyflow, nivo, selection-spotlight, reduced-motion]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: _shared/dashboardTokens.ts, useTransition, HoverSpotlight, FocusRing primitives
  - phase: 04.1-02
    provides: Bubble cluster + treemap reskin (selection kinds junk/duplicate/outlier consumed by polished widgets)
  - phase: 04.1-03
    provides: Calendar heatmap + admin constellation reskin (selection kinds day/admin)
provides:
  - Severity colors flow through `_shared/dashboardTokens.ts` across the five surviving graphical widgets (CoverageDonut, ActiveUserTiers, KpiStrip, RolesModulesHeatmap, RoleRelationshipFlow)
  - Selection-aware spotlighting wired into RolesModulesHeatmap (per-cell itemStyle.opacity) and RoleRelationshipFlow (FlowPaletteContext driven node/edge style.opacity)
  - useHeatmapRamp helper added to dashboardTokens.ts (theme-aware ECharts intensity ramp)
  - Reduced-motion compliance via useTransition wrapper on framer-motion transitions
  - Dashboard end-to-end visual coherence approved by user human-verify (Phase 4.1 sign-off)
affects: [Phase-05, future-dashboard-work, polish-remediation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "All widget severity colors sourced from `_shared/dashboardTokens.ts` (no hardcoded hex)"
    - "Selection spotlight via context-injected palette (FlowPaletteContext) for third-party renderers"
    - "ECharts rich-text axisLabel formatter consumes useSeverityColor() at render time"
    - "Reduced-motion wrapper (useTransition) layered over framer-motion transitions"

key-files:
  created: []
  modified:
    - app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/_shared/dashboardTokens.ts

key-decisions:
  - "useHeatmapRamp helper added to dashboardTokens.ts so ECharts intensity ramp stays grep-discoverable instead of inline hex"
  - "Selection spotlight in xyflow/react widget delivered via FlowPaletteContext pattern (avoids per-node hook calls inside custom node renderers)"
  - "Polish pass kept strictly visual — no registry/data-fetch/CSV contract changes; capabilities and span IDs untouched"
  - "User-approved-with-caveats: ACC analysis tab itself was not replaced (only the four name-list widgets within the dashboard were reskinned) and graphics could be more interactive / information-dense — logged to feedback memory for future phases, NOT remediated this round per user direction"

patterns-established:
  - "Severity-color discipline: any widget that shows severity reads from `useSeverityColor()` — third-party config overrides re-export consts so the hex grep stays meaningful"
  - "Context-based palette injection for third-party node renderers (xyflow custom nodeTypes can't call hooks)"
  - "Selection reconciliation honors useSelection().selected across all severity-aware widgets (heatmap rows + flow nodes dim when a finding is selected elsewhere)"

requirements-completed: [DASH-01, DASH-02, DASH-04, DASH-07, DASH-08, DASH-09, DASH-13]

# Metrics
duration: ~25 min (Task 1 polish pass + Task 2 human-verify checkpoint round-trip)
completed: 2026-05-08
---

# Phase 04.1 Plan 04: Token + spotlight polish on surviving widgets Summary

**Token-driven severity palette and selection-aware spotlighting wired across the five surviving graphical widgets — closing Phase 4.1 with a single coherent cosmos-aligned visual language.**

## Performance

- **Duration:** ~25 min (polish edits + checkpoint round-trip)
- **Started:** 2026-05-08
- **Completed:** 2026-05-08
- **Tasks:** 2 (1 auto polish pass + 1 human-verify checkpoint)
- **Files modified:** 6 (5 widgets + dashboardTokens.ts)

## Accomplishments
- All severity hex literals removed from the five surviving graphical widgets — colors now flow through `_shared/dashboardTokens.ts`
- `useHeatmapRamp` helper added so the ECharts intensity ramp stays themeable and grep-clean
- Selection-aware spotlight wired into RolesModulesHeatmap (per-cell `itemStyle.opacity`) and RoleRelationshipFlow (`FlowPaletteContext` + node/edge `style.opacity`)
- `npx tsc --noEmit` clean; severity-hex grep returns zero matches inside the five widget files
- Phase 4.1 human-verify approved — dashboard reads as one product end-to-end across both themes

## Task Commits

1. **Task 1: Token + spotlight polish on the five existing graphical widgets** — `c816144` (refactor)
2. **Task 2: Phase 4.1 human-verify checkpoint** — no code commit (UI verification by definition); approval recorded in this SUMMARY

**Plan metadata commit:** appended after this file (`docs(04.1-04): complete polish pass + human-verify`).

## Files Created/Modified
- `app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx` — Nivo `colors` prop sourced from tokens
- `app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx` — stacked-bar tier palette mapped to token gradient; hover dim via spotlight
- `app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx` — severity pill backgrounds via `useSeverityColor()`
- `app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx` — `useHeatmapRamp`-driven cell colors + selection-aware row dim via `series.data` per-cell `itemStyle.opacity`
- `app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx` — custom node renderers consume `FlowPaletteContext`; non-matching nodes fade to 0.4 when `selected.kind === "role"`
- `app/(dashboard)/users/dashboard/widgets/_shared/dashboardTokens.ts` — added `useHeatmapRamp` helper

## Decisions Made
- See key-decisions in frontmatter. No scope creep; polish discipline held (no widget exceeded ~30 changed lines; no registry/data-fetch edits).

## Deviations from Plan

None - plan executed exactly as written.

## Notes / Caveats

**User-approved-with-caveats (Phase 4.1 human-verify, 2026-05-08):**

> User flagged that the ACC analysis tab itself was not replaced (only the four name-list widgets within the Access-Analysis dashboard were reskinned) and that graphics could be more interactive / information-dense. **Approved this round; logged to feedback memory for future phases.**

Per user direction at the checkpoint, no remediation was performed in this plan. The verifier may flag a goal-achievement gap against CONTEXT.md ("replace lists with astonishing graphics") if it determines the tab-level wrapper or interactivity floor was part of the original goal scope; that is expected and intentional — surfacing it for a future phase rather than expanding scope here.

## Issues Encountered
None — polish pass landed cleanly; checkpoint resolved on first pass.

## Requirements Completion

Attempted `requirements mark-complete DASH-01 DASH-02 DASH-04 DASH-07 DASH-08 DASH-09 DASH-13`. If REQUIREMENTS.md is not present in `.planning/` or the IDs are not registered, the helper logs `not_found` and the plan finalizes anyway (per resume instructions). Result captured below in self-check.

## Next Phase Readiness
- Phase 4.1 ships complete (4/4 plans). Dashboard is visually coherent end-to-end, severity tokens flow through every widget, and selection spotlight links bubble cluster ↔ heatmap ↔ flow.
- Open follow-ups (NOT blockers, deferred to a future phase per user feedback):
  1. ACC Analysis **tab** wrapper (vs the dashboard route) — interactivity at the tab level rather than just inside widgets
  2. Push interactivity / information density of the four reskinned widgets further (more affordances, denser hover state, cross-widget brushing)
- Recommended next move: a scoped `04.2-` or `05-` planning pass that takes the user feedback above as its CONTEXT.md.

## Self-Check: PASSED

- Commit `c816144` confirmed via `git log --all`
- SUMMARY.md present at expected path
- All claimed file modifications match Task 1 commit scope

---
*Phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets*
*Completed: 2026-05-08*
