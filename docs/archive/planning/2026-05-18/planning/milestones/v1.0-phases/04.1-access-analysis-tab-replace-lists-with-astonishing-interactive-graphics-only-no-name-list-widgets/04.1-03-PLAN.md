---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-06
  - DASH-10
  - DASH-11

must_haves:
  truths:
    - "RecentlyAddedWidget renders 90-day SVG calendar heatmap (GitHub-style grid) — zero <table>, zero member names in widget body"
    - "Calendar cell color encodes joins-per-day count; window selector (7d/30d/90d, default 30d) highlights active band by dimming non-band cells"
    - "Calendar cell click dispatches setSelected({ kind: 'day', dateIso, emails }) — opens existing DashboardSidePanel"
    - "Day bucketing uses local-date string in browser timezone (date-fns format yyyy-MM-dd) — Pitfall 5 timezone shift handled"
    - "AdminAccessWidget renders SVG orbit constellation — zero <table>, zero <li>, zero email/full-name DOM"
    - "Constellation orbit positions pre-computed in useMemo via cos/sin; framer-motion drives spring entrance only"
    - "Hover on admin node reveals INITIALS only (e.g. 'LC' for Luis Cortes); click dispatches setSelected({ kind: 'admin', email })"
    - "Both widgets show shape-matched skeleton (CalendarSkeleton / OrbitSkeleton) on loading"
    - "Both widgets keep CSV download button in their header"
    - "Edge cases handled: 0 admins (empty-state), 1 admin (centered single node), 2 admins (minimum visual separation enforced)"
    - "Reduced-motion users: entrance skipped; hover/selection retained"
    - "Keyboard nav: tab into widget, arrow keys traverse, Enter dispatches setSelected; SortableWidget drag does NOT fire"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx"
      provides: "90-day calendar heatmap (SVG, d3-time + framer-motion)"
      contains: "timeDays"
      min_lines: 100
    - path: "app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx"
      provides: "Animated orbit constellation (SVG, hand-rolled orbit math + framer-motion)"
      contains: "Math.cos"
      min_lines: 80
  key_links:
    - from: "RecentlyAddedWidget.tsx"
      to: "selectionContext setSelected"
      via: "rect onClick"
      pattern: "setSelected\\(\\{\\s*kind:\\s*[\"']day[\"']"
    - from: "AdminAccessWidget.tsx"
      to: "selectionContext setSelected"
      via: "node onClick"
      pattern: "setSelected\\(\\{\\s*kind:\\s*[\"']admin[\"']"
    - from: "Both widgets"
      to: "_shared/dashboardTokens.ts"
      via: "import tokens + useTransition"
      pattern: "from [\"']\\./_shared/"
---

<objective>
Rewrite RecentlyAddedWidget (table → 90-day calendar heatmap) and AdminAccessWidget (table → animated orbit constellation) using SVG + d3-time/d3-scale + framer-motion. Click routes to the side panel via the new `kind: "day"` and `kind: "admin"` selection union members landed in 04.1-01. Widget IDs (`recent`, `admins`) and registry spans unchanged.

Purpose: Second half of the four name-list-widget reskin. Paired in one plan because they share the new union members from 04.1-01 and have non-overlapping file ownership with 04.1-02 (parallel execution within Wave 2).

Output: Two widget files fully rewritten; CSV preserved; constellation handles 0/1/2-admin edge cases inline (CONTEXT deferred-ideas: handle inline, no separate phase).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-CONTEXT.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-RESEARCH.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-01-PLAN.md
@app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
@app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
@app/(dashboard)/users/dashboard/widgetRegistry.ts
@app/(dashboard)/users/dashboard/selectionContext.tsx

<interfaces>
<!-- BulkAccUser fields used here (lib/acc/acc-types) -->
```ts
interface BulkAccUser {
  email: string;
  name: string;
  addedOn: string | null;        // ISO from Phase 4 P02
  isAccountAdmin: boolean;       // from Phase 4 P01
  companyRole?: string | null;
  // ...
}
```

<!-- New SelectedFinding kinds (Wave 1) -->
```ts
| { kind: "admin"; email: string }
| { kind: "day"; dateIso: string; emails: string[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite RecentlyAddedWidget as 90-day calendar heatmap</name>
  <files>app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx</files>
  <action>
Replace the entire file. Keep the named export `RecentlyAddedWidget` and `WidgetCommonProps` signature (this widget uses `users`).

1. **Imports:** React `useMemo, useState`; `timeDays, timeWeek` from `d3-time`; `scaleSequential` from `d3-scale`; `interpolateBlues` from `d3-scale-chromatic`; `motion, AnimatePresence` from `framer-motion`; `format, subDays, startOfDay, parseISO` from `date-fns` (already a dep — confirm); `useSelection` from `../selectionContext`; tokens / hooks from `./_shared/*`; `CalendarSkeleton`; CSV helpers + Button + Download icon.

2. **Local state:** `windowDays: 7 | 30 | 90` default `30` (matches Phase 4 P07 toggle behavior). 3-button group (`<Button>`s with size="sm" variant toggled) above the SVG, in the same header row as the CSV button.

3. **Bucketing (Pitfall 5):** Build `Map<dateIso, string[]>` where the key is `format(parseISO(u.addedOn), "yyyy-MM-dd")` in the user's local TZ; value is the list of emails added that day. Skip users with `addedOn === null`.

4. **Grid:** today = `startOfDay(new Date())`, start = `subDays(today, 89)`. `days = timeDays(start, today)` → 90 cells. Layout = 13 columns × 7 rows (or 14 × 7 to safely fit 90 days into a rectangular grid — choose 13×7=91 with the first cell as a half-cell padding, or 14×7=98 with trailing nulls; pick whichever you implement cleanly). CELL=14, GAP=2. SVG `viewBox` matches.

5. **Color:** `scaleSequential(interpolateBlues).domain([0, max])` where `max = Math.max(1, ...counts.values())`. Empty cells use `var(--secondary)` or `NEUTRAL_LIGHT/DARK` from tokens (theme-aware via `useSeverityColor()` doesn't apply — use a separate `useNeutral()` if you need; otherwise inline a `useTheme()` check).

6. **Window highlight:** Cells whose date is within `windowDays` of today get full opacity (HOVER_OPACITY_FOCUS); older cells animate to `HOVER_OPACITY_DIM`. `useTransition(CELL_SPRING)` for the dim animation.

7. **Cells:** `motion.rect` per day. `initial={{ opacity: 0 }}`, `animate={{ opacity: bandOrSpotlight }}`, `transition={{ delay: weekIndex * 0.005, ...useTransition(CELL_SPRING) }}` so rows fade in left-to-right (subtle entrance — disabled by reduced-motion).

8. **Click:** `onClick={() => setSelected({ kind: "day", dateIso: k, emails: counts.get(k) ?? [] })}`. Skip empty cells (no `onClick`, no `tabIndex`).

9. **Hover tooltip:** native `<title>` per cell: `"{format(d, 'EEE MMM d')}: {n} member{s}"` — no member names, only counts (CONTEXT decision).

10. **Hover spotlight:** Use `useHoverSpotlight()` keyed by dateIso. Optional — if it complicates the band-highlight logic, skip and rely on the band dim alone (document the choice in a code comment).

11. **Keyboard nav:** Arrow keys move focus across the grid (left/right by day, up/down by row); Enter calls setSelected. `event.stopPropagation()` on SVG keydown.

12. **Loading state:** show `<CalendarSkeleton />` when users array is empty (Phase 4 sentinel pattern).

13. **CSV button:** download all users with `addedOn` within current window. Columns: `Date, Email, Name, CompanyRole`. Reuse Phase 4's `downloadCsv` helper.

14. **Aria:** SVG `role="img" aria-roledescription="90 day join calendar"`. Each cell `aria-label={`${format(d, 'PPP')}: ${n} members added`}`. Title summary above SVG (visually hidden): "{N} members added in last {windowDays} days".

15. **Empty state:** if zero users with `addedOn`, render centered text "No recently-added members in last 90 days".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "RecentlyAddedWidget" | head -10 ; echo "---" ; grep -nE "table|<ul|<li|<TableRow|<TableCell" "app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx" | head -5 ; echo "---" ; grep -n "timeDays\|setSelected" "app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx" | head -10</automated>
  </verify>
  <done>File compiles; contains `timeDays(...)` and `motion.rect`; zero list/table JSX; setSelected fires with kind:"day"; window selector renders 7/30/90; CSV button present.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AdminAccessWidget as animated orbit constellation</name>
  <files>app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx</files>
  <action>
Replace the entire file. Keep the named export `AdminAccessWidget` and the WidgetCommonProps signature.

1. **Imports:** React `useMemo, useState`; `motion, AnimatePresence` from `framer-motion`; `useSelection` from `../selectionContext`; tokens + hooks from `./_shared/*`; `OrbitSkeleton`; CSV helpers.

2. **Filter:** `const admins = useMemo(() => users.filter(u => u.isAccountAdmin), [users])`. (Account-level admins only — DASH-05, no project admins.)

3. **Layout:** W=320, H=320, center=(160, 160). `RADIUS = 110` for ≥3 admins. Pre-compute orbit positions in `useMemo`:
   ```ts
   admins.map((u, i) => ({
     u,
     cx: cx0 + R * Math.cos((2 * Math.PI * i) / admins.length - Math.PI / 2),
     cy: cy0 + R * Math.sin((2 * Math.PI * i) / admins.length - Math.PI / 2),
   }))
   ```

4. **Edge cases (Pitfall 6):**
   - 0 admins → render an empty state at center: "No account admins" (no skeleton; this is a real result, not loading).
   - 1 admin → place the single node at center (cx0, cy0), with a subtle `motion` pulse (`scale: [1, 1.06, 1]` repeat).
   - 2 admins → enforce a minimum separation by hardcoding placements (e.g. positions at angles -90° and +90° on a smaller R=80) so they don't read as static poles.
   - ≥3 admins → standard ring.

5. **Center anchor:** Render a faint center "sun" — small `<circle cx={cx0} cy={cy0} r={6} fill={NEUTRAL}/>` with a soft ring/grid as a visual focal point (drop-shadow filter via `filter="drop-shadow(0 0 8px ...)"`). Optional — keep the canvas from feeling empty.

6. **Orbit nodes:** `motion.g` per admin, `initial={{ opacity: 0, scale: 0.4 }}`, `animate={{ opacity: getOpacity(email), scale: 1, x: cx, y: cy }}`, `transition={useTransition(ORBIT_SPRING)}`. Inside each `<g>`: `<circle r={14} fill={HIGHLIGHT}/>`. Wrap in `tabIndex={0} role="button"` (Pitfall 8 — focus on group).

7. **Initials reveal (CONTEXT exception):** `useState<hovered: string | null>` plus mouse handlers. When hovered/focused, render a `<text>` child centered on the node showing initials (first letter of given name + first letter of family name; fallback first 2 letters of email local-part if name missing). NO full name, NO email — initials only on hover. Click reveals identity via the side panel.

8. **Click:** `setSelected({ kind: "admin", email: u.email })`.

9. **Hover spotlight:** Wire `useHoverSpotlight()` keyed by email; non-hovered nodes dim to HOVER_OPACITY_DIM.

10. **Selected highlight:** if `selected?.kind === "admin" && selected.email === u.email`, force opacity 1, scale 1.08, render `<FocusRing>` around it.

11. **Keyboard nav:** Arrow keys cycle through admins by orbit angle; Enter triggers setSelected. Stop event propagation.

12. **Loading state:** if Phase 4 sentinel pattern (users empty AND findings sentinel) — render `<OrbitSkeleton />`. Otherwise render the constellation (even with 0 admins — the "No account admins" empty state is correct).

13. **CSV button:** download admins. Columns: `Email, Name, CompanyRole, AddedOn, LastSignIn`.

14. **Aria:** SVG `role="img" aria-roledescription="account admin constellation"`. Each `<g>` gets `aria-label={`Account admin ${i+1} of ${n}`}` (NO names in aria — preserves the "names exit canvas" rule even from screen readers; keep this consistent with CONTEXT lock; full identity arrives via the side panel which has full table). Visually hidden summary: `<title>{n} account admins</title>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "AdminAccessWidget" | head -10 ; echo "---" ; grep -nE "<table|<ul|<li|<TableRow|<TableCell" "app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx" | head -5 ; echo "---" ; grep -n "Math.cos\|setSelected" "app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx" | head -10</automated>
  </verify>
  <done>File compiles; contains `Math.cos(`/`Math.sin(` orbit math + `motion.g`; zero list/table JSX; setSelected fires with kind:"admin"; 0/1/2-admin branches all rendered; CSV button present.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across both files.
- Both widgets keep their existing widget-registry id (`recent`, `admins`) and span.
- `grep -nE "u\.name|u\.email" {both widgets}` returns either nothing (preferred) or only references inside CSV-row builders / aria-label — never inside JSX text content.
</verification>

<success_criteria>
- Calendar heatmap: 90 cells, color-encoded counts, 7/30/90 window dim, click → side panel with that day's members.
- Constellation: orbit nodes, hover→initials, click→side panel; 0/1/2-admin edges look intentional, not broken.
- CSV downloads still work.
- Phase 4 contracts (widget IDs, spans, props) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-03-SUMMARY.md`
</output>
