---
phase: 02.5-acc-data-filter-refinement
plan: 03
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - app/(dashboard)/users/graphRenderers.ts
  - app/(dashboard)/users/AccUserSidePanel.tsx
autonomous: true
requirements:
  - FILT-01
  - DATA-01

must_haves:
  truths:
    - "Cosmos.gl renderer hides excluded nodes by setting their point size to 0 (no pixel, no hit-test) — not by dimming"
    - "Canvas2D renderer continues to fully skip nodes not in frame.userIndices (already correct — no regression)"
    - "Edges incident to a hidden node are not drawn in either renderer"
    - "A 150ms CSS opacity fade-out plays on the canvas wrapper when the visible set shrinks; effect is purely CSS so it costs zero GPU/CPU during steady state"
    - "AccUserSidePanel renders a 'Company Role' row showing the user's companyRole or 'Unspecified' when null"
  artifacts:
    - path: "app/(dashboard)/users/graphRenderers.ts"
      provides: "Cosmos setVisibleIndices method (zero-size for excluded), Canvas2D edge-skip when endpoint hidden"
      contains: "setVisibleIndices"
    - path: "app/(dashboard)/users/AccUserSidePanel.tsx"
      provides: "Company Role row in side panel with Unspecified fallback"
      contains: "Company Role"
  key_links:
    - from: "filters change"
      to: "Cosmos point sizes buffer"
      via: "CosmosGraphRenderer.setVisibleIndices(visibleSet) → setPointSizes(...)"
      pattern: "setPointSizes"
    - from: "AccUserSidePanel.tsx"
      to: "selected user companyRole"
      via: "panel reads node.companyRole and renders 'Unspecified' when null"
      pattern: "companyRole"
---

<objective>
Add the visual half of FILT-01 (renderer-level hide that is fully invisible, not greyed-out) and the side-panel display half of DATA-01. Implements Cosmos.gl zero-size hide, edge-skip when endpoint is hidden, the CSS fade-out transition, and the Company Role row in the side panel.

Purpose: Plan 02 produces a correct `visibleNodeIdxRef`; this plan makes the renderers actually honor it in a way that meets the "fully hidden, not greyed-out" hard constraint. Side-panel work is grouped here because both edits are renderer-and-display concerns and the file set does not collide with plan 02.

Output: A `setVisibleIndices(visibleSet)` method on `CosmosGraphRenderer`, edge-skip logic in both renderers, a CSS-only fade wrapper, and a Company Role row in `AccUserSidePanel.tsx`.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-acc-data-filter-refinement/02.5-CONTEXT.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-RESEARCH.md
@app/(dashboard)/users/graphRenderers.ts
@app/(dashboard)/users/AccUserSidePanel.tsx
@.planning/phases/02.5-acc-data-filter-refinement/02.5-01-SUMMARY.md

<interfaces>
<!-- Cosmos zero-size hide pattern (RESEARCH.md Pattern 4) -->
```typescript
function buildNodeSizeBufferWithVisibility(
  nodeCount: number,
  connections: Int32Array | null,
  visibleSet: ReadonlySet<number>
): Float32Array {
  const sizes = new Float32Array(nodeCount);
  for (let i = 0; i < nodeCount; i++) {
    if (!visibleSet.has(i)) { sizes[i] = 0; continue; }
    const degree = connections?.[i] ?? 0;
    sizes[i] = degree > 3 ? 6 : degree > 0 ? 4 : 3;
  }
  return sizes;
}
// graph.setPointSizes(buildNodeSizeBufferWithVisibility(...));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cosmos zero-size hide + edge-skip in both renderers + CSS fade wrapper</name>
  <files>app/(dashboard)/users/graphRenderers.ts, app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
    Step A — `graphRenderers.ts`, `CosmosGraphRenderer`:
    1. Add a `setVisibleIndices(visibleSet: ReadonlySet<number>)` method. Internally:
       - Recompute the point-size Float32Array using the helper in `<interfaces>` above (excluded indices get size 0).
       - Call `this.graph.setPointSizes(sizes)` — this is the Cosmos v3 API used today for size updates.
       - Cache the last visible set on the instance so subsequent slider/HUD redraws don't reset sizes.

    2. In the existing `draw()` / frame update path that builds the link buffer, **skip edges where either endpoint is not in the visible set**. Look for the place that emits the link arrays for `setLinks` (or whichever Cosmos API is in use today) and gate each link with:
       ```typescript
       if (!visibleSet.has(srcIdx) || !visibleSet.has(tgtIdx)) continue;
       ```
       If the edge buffer is built once at init and not per-frame, add a `setVisibleEdges` companion that rebuilds the link buffer and calls the Cosmos link-set API again. Pick the path that matches how the existing code rebuilds links when topology changes — match existing patterns.

    3. Per RESEARCH.md Pitfall 5: zero-size points still participate in physics. **Accept this** as documented; the CONTEXT.md says "physics simulation reflows" on filter change, which implies hidden-node-physics-residue is acceptable. Do NOT attempt per-node physics disable in Cosmos v3 beta — the API doesn't support it cleanly.

    Step B — `graphRenderers.ts`, `CanvasGraphRenderer.draw()`:
    1. The current code iterates `frame.userIndices` for nodes — already correct (excluded user nodes are simply skipped). Verify this with a grep.
    2. The edge draw loop must also skip edges where either endpoint isn't in `frame.userIndices` ∪ `frame.visibleNonUserIndices` (or whatever the visible-set field is called after plan 02 lands). If today's code draws all edges unconditionally, add the gate. If it already gates on visible endpoints, no change needed — note this in the SUMMARY.

    Step C — `AccUsersGraph.tsx`, fade-out wrapper:
    1. Wrap the canvas container `<div>` (the one that holds the Cosmos canvas + Canvas2D fallback) so it has:
       ```tsx
       <div className={cn(
         "transition-opacity duration-150 ease-out",
         isFilterTransitioning ? "opacity-60" : "opacity-100"
       )}>
       ```
    2. Add `const [isFilterTransitioning, setIsFilterTransitioning] = useState(false);`
    3. In the same effect that calls `rebuildVisibleIndices()` on filter change, set `isFilterTransitioning(true)` before the call and `setTimeout(() => setIsFilterTransitioning(false), 150)` after. Cheap, zero shader cost. Per RESEARCH.md Pattern 5.

    4. After plan 02 lands, call `cosmosRenderer.setVisibleIndices(newVisibleSet)` from inside `rebuildVisibleIndices` (or wherever the renderer is notified of visibility changes). If a clean hook point doesn't exist yet, expose `cosmosRendererRef.current?.setVisibleIndices(...)` from the same place that posts visibility to the worker.

    Avoid: do NOT use `DIMMED_USER_COLOR` or set alpha=0.1 on hidden nodes — the CONTEXT.md hard-bans greying-out. Do NOT introduce a per-frame WebGL animation for the fade — CSS opacity on the wrapper is sufficient.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20</automated>
    Manual trace: grep `setVisibleIndices` appears in `graphRenderers.ts`; grep `isFilterTransitioning` appears in `AccUsersGraph.tsx`; grep `setPointSizes` shows the new size-buffer rebuild path.
  </verify>
  <done>
    `CosmosGraphRenderer.setVisibleIndices` zeros excluded points; both renderers skip edges to hidden endpoints; CSS fade wrapper plays on filter change; no greying-out anywhere; `tsc --noEmit` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Company Role row to AccUserSidePanel</name>
  <files>app/(dashboard)/users/AccUserSidePanel.tsx</files>
  <action>
    1. In the side panel's user-info section (where existing rows like email, addedOn, etc. are rendered), add a plain row matching the existing styling:
       ```tsx
       <Row label="Company Role" value={user.companyRole ?? "Unspecified"} />
       ```
       Use the same `Row` (or equivalent) component pattern already in the file. If the file uses inline JSX, follow that pattern instead — match the existing visual style exactly. Per CONTEXT.md: "plain row alongside other user fields, consistent with existing panel layout. No special badge."

    2. Place the row near the top of the user-info block (under name/email, above project/role lists), or wherever the existing field ordering puts roles. Pick the placement that reads most naturally given the existing layout.

    3. The selected user's `companyRole` is on the `SimNode` after plan 01. If the side panel currently receives a different shape, thread the field through whatever prop chain is in use (most panels in this codebase get a `selectedNode` prop containing the full `SimNode`).

    4. Do NOT yet add the companyRole **filter UI** — that is plan 04. This task is only the side-panel display half of DATA-01.

    Avoid: no badge styling, no colored chip, no icon — plain text matching existing rows. Per CONTEXT.md decision.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20</automated>
    Manual: grep `Company Role` appears in `AccUserSidePanel.tsx`.
  </verify>
  <done>
    `AccUserSidePanel.tsx` renders a `Company Role` row that shows the user's companyRole or `Unspecified` when null. Styling matches existing rows. `tsc --noEmit` is clean.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Grep `setVisibleIndices` finds the new Cosmos method and at least one caller in `AccUsersGraph.tsx`
- Grep `Company Role` finds the new side-panel row
- Grep confirms NO greying-out / dimming logic was added (search for `0\\.1`, `DIMMED`, etc. for new occurrences)
</verification>

<success_criteria>
- Cosmos hides excluded nodes via `setPointSizes(0)` — no pixel, no hit-test
- Canvas2D continues to skip excluded user nodes; edges to hidden endpoints are not drawn in either renderer
- Filter change triggers a 150ms CSS opacity fade on the canvas wrapper (zero shader cost)
- Side panel shows the Company Role row with Unspecified fallback
- No greyed-out / dimmed nodes anywhere — fully invisible per spec
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-acc-data-filter-refinement/02.5-03-SUMMARY.md` documenting:
- Whether the Canvas2D edge loop already skipped hidden endpoints or needed a new gate
- How `setVisibleIndices` is called from `AccUsersGraph.tsx` (which hook point)
- Any Cosmos v3 API quirks encountered with `setPointSizes` (per the "Three Cosmos.gl API Traps" notes from Phase 02-05)
- Side-panel Row placement choice
</output>
