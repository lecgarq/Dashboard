---
phase: 02-cosmos-gl-renderer
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/(dashboard)/users/AccUsersGraph.tsx
  - app/(dashboard)/users/cosmosUtils.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "User can toggle a Lasso mode in the toolbar; cursor becomes crosshair, pan/zoom suspended"
    - "User can draw a freeform polygon over the canvas; an SVG overlay traces the path during drag"
    - "On pointer release, all nodes whose world-space position falls inside the polygon are selected"
    - "A side panel summarizes the selection: count, top 5 roles, top 10 modules, sample user ids"
    - "Selected nodes are visually highlighted in both Canvas 2D and Cosmos renderers via the existing frame.highlightSet pipeline"
    - "A 'Clear selection' button resets polygon state and restores normal interaction"
  artifacts:
    - path: "app/(dashboard)/users/cosmosUtils.ts"
      provides: "pointInPolygon ray-casting helper"
      contains: "pointInPolygon"
    - path: "app/(dashboard)/users/AccUsersGraph.tsx"
      provides: "Lasso toolbar toggle, SVG overlay, polygon selection state, multi-select side panel"
      contains: "polygonSelect"
  key_links:
    - from: "AccUsersGraph.tsx pointer handlers"
      to: "cosmosUtils.ts pointInPolygon"
      via: "screen→world transform + per-node ray-cast test"
      pattern: "pointInPolygon"
    - from: "AccUsersGraph.tsx polygonSelection state"
      to: "graphRenderers.ts frame.highlightSet"
      via: "override highlightSet to polygon's index set while selection active"
      pattern: "highlightSet"
---

<objective>
Add a freeform lasso/polygon multi-select tool to the graph: user draws a shape, all enclosed nodes become a selection, a side panel summarizes the selection's role and module composition.

Purpose: Surfaced post-02-02 verification — user wants to inspect arbitrary regions of the graph beyond filter-driven selection. This is the "select a clump and see what's in it" capability that powers later analysis workflows (Phase 4 access analysis builds on this).

Output: Renderer-agnostic polygon selection that works in both Canvas 2D and Cosmos modes, with a clean toolbar toggle and clearable side panel.

Reference: https://cosmos.gl/?path=/story/examples-clusters--polygon-selection (Cosmos polygon selection demo); https://observablehq.com/@d3/quadtree-brush (canvas-overlay drawing pattern).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-cosmos-gl-renderer/02-03-PLAN.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/graphRenderers.ts
@app/(dashboard)/users/cosmosUtils.ts

<reference_urls>
- Cosmos polygon-selection story: https://cosmos.gl/?path=/story/examples-clusters--polygon-selection
- D3 quadtree-brush: https://observablehq.com/@d3/quadtree-brush
</reference_urls>

<interfaces>
Existing screen→world transform in AccUsersGraph.tsx (~line 920) — used by drag pointermove to map clientX/Y to graph coordinates. Reuse this for polygon vertex translation.

Existing dim/bright batching in CanvasGraphRenderer + Cosmos same-user path (added in 02-03) consume `frame.highlightSet`. Reuse — no new render path needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pointInPolygon helper + unit test</name>
  <files>app/(dashboard)/users/cosmosUtils.ts, app/(dashboard)/users/cosmosUtils.test.ts (new)</files>
  <action>
1. Export `pointInPolygon(px: number, py: number, poly: Float32Array): boolean` using ray-casting. `poly` is flat `[x0,y0,x1,y1,...]`; closure implied (algorithm wraps last→first).
2. Add minimal vitest covering: square containment (point inside true, outside false), point on edge (consistent result), degenerate polygon (< 3 vertices returns false), winding-direction independence.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npx vitest run cosmosUtils</automated>
  </verify>
  <done>
pointInPolygon exported and pure. Unit tests cover containment, edge cases, and winding independence — all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lasso mode toolbar toggle + SVG overlay</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
1. Add toolbar button "Lasso" alongside Canvas2D/GPU toggle. State: `const [lassoActive, setLassoActive] = useState(false);`.
2. When `lassoActive`: cursor `crosshair`; existing pan/zoom handler must short-circuit (don't pan during draw).
3. Refs: `lassoPathRef = useRef<number[]>([])` for screen-space points; state `lassoPath` for re-render.
4. pointerdown when lassoActive: clear path, push starting `[clientX, clientY]`. pointermove: append if distance from last >= 4px. pointerup: close polygon, run selection (Task 3), exit lasso (`setLassoActive(false)`).
5. Render `<svg className="absolute inset-0 pointer-events-none">` with `<polyline points={lassoPath.join(',')}>` styled `stroke-blue-500 stroke-2 fill-blue-500/10`. Conditional on `lassoActive || lassoPath.length > 0`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Manual: click Lasso, draw a shape, confirm SVG trace visible. pointerup ends lasso mode.
  </verify>
  <done>
Lasso toolbar toggle works. SVG overlay traces freeform pointer path. Pan/zoom suspended during draw.
  </done>
</task>

<task type="auto">
  <name>Task 3: Polygon-close → selection → multi-select side panel</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
1. On pointerup with lassoActive: translate each polygon screen-space point to world coords (mirror existing inverse-view transform near line 920).
2. Build Float32Array poly of world-space vertices.
3. For each visible node: read position from `posRef.current`, call `pointInPolygon(nx, ny, poly)`. Collect matching indices.
4. Add helper `summarizePolygon(indices, accUsers): { count, byRole, byModule, sampleIds }` — top 5 roles by count, top 10 modules by count, first 10 ids.
5. State: `[polygonSelection, setPolygonSelection] = useState<{ indices: number[]; summary: ... } | null>(null)`.
6. Render right-side panel (existing detail-panel slot) when `polygonSelection !== null`. Show count + role+module breakdown + sample ids. Include "Clear selection" button → resets state.
7. While `polygonSelection !== null`, override `frame.highlightSet` to polygon's index set so both renderers emphasize via existing dim/bright pipeline (no new render path needed).

**Important:** Do NOT use Cosmos's built-in `selectPointsInRange` — keep selection renderer-agnostic so Canvas 2D works identically.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Manual: enable Lasso, draw around a cluster, release. Side panel shows count + top roles + top modules. Enclosed nodes highlighted in both renderers. "Clear selection" resets.
  </verify>
  <done>
Polygon-close fires selection, side panel renders with summary, frame.highlightSet override emphasizes selected nodes in both renderers. Clear button restores baseline.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — lasso end-to-end</name>
  <files>(no files modified)</files>
  <action>Run acceptance checks against dev build. Pause for sign-off.</action>
  <verify><automated>echo "manual checkpoint"</automated></verify>
  <done>User types "approved" after all 6 manual checks pass.</done>
  <how-to-verify>
1. `npm run dev`, visit /users on a 500+ node hub.
2. Click "Lasso" toolbar button — cursor becomes crosshair; pan/zoom suspended.
3. Draw a freeform shape around a visible cluster — SVG overlay traces the path live.
4. Release — side panel appears with count, top 5 roles, top 10 modules, first 10 ids.
5. Toggle Canvas 2D ↔ GPU — selected nodes highlight identically in both modes.
6. Click "Clear selection" — panel disappears, highlight clears, lasso button is re-armable.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 pass, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes after every task
- [ ] `pointInPolygon` unit tests pass
- [ ] Manual checkpoint passed
- [ ] No regression in 02-03 verified behavior (drag, sliders, same-user highlight, FPS HUD)
</verification>

<success_criteria>
1. Lasso toolbar toggle works; cursor + pan-suspend correct.
2. SVG overlay traces freeform polygon during draw.
3. Polygon-close selects all enclosed nodes; side panel summarizes role+module composition.
4. Selection visually emphasized in both renderers via existing highlightSet pipeline.
5. Clear button resets state cleanly; tool is re-armable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cosmos-gl-renderer/02-04-SUMMARY.md` per template, noting any TECHNICAL_DEBT entries (e.g., quadtree spatial-index for polygon test if 5K+ nodes — currently O(n) per polygon, acceptable for <2K).
</output>
