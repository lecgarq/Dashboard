Disabled 2026-05-18 (FOURTH re-arm; auto-deletion mystery still unresolved).

Today's outcome:
  - Bug A (ingestRunId schema drift) FIXED + migration applied (commit b3f25bd)
  - Bug B/C edits in dcIngest.ts in-tree but uncommitted
  - Bug D (CSV column-name drift) DISCOVERED but NOT yet diagnosed
  - Diagnostic log added to dcActivityCsvIngest.ts (commit pending) — will print
    actual column names on first parsed row of every CSV
  - All today's quota burned (APS enforced 429 on final attempt — local counter
    said 23/25 but APS refused submit at "0 used" run, meaning real consumption
    was higher than DB tracked)

DO NOT DELETE THIS FILE before tomorrow's diagnostic run produces column names.
If you wake up before 10:00 UTC and this file is gone, recreate it manually
or rename scripts/dc-daily-ingest.cjs to scripts/dc-daily-ingest.cjs.off as a
fallback kill mechanism.

Tomorrow's workflow (2026-05-19, fresh 25/25 quota at 00:00 UTC):
  1. Verify .dc-ingest.disabled is still present (if not, see fallback above)
  2. Remove kill switch
  3. Run: node --env-file=.env scripts/dc-daily-ingest.cjs
  4. grep '[dcActivityCsvIngest:diagnostic]' on the log to see column names
  5. Update dcActivityCsvIngest.ts:135/143/144 with real column names
  6. Remove diagnostic log
  7. Re-probe; verify AccActivity row count grows

Spec: docs/superpowers/specs/2026-05-18-dc-ingest-drift-recovery-design.md
Memory: project_phase09_dc_ingest_drift, project_dc_kill_switch_autodelete
