---
phase: 02.5-acc-data-filter-refinement
plan: 04
type: execute
wave: 3
depends_on: ["02.5-02", "02.5-03"]
files_modified:
  - app/(dashboard)/users/AccUsersGraph.tsx
autonomous: false
requirements:
  - FILT-02
  - FILT-03
  - DATA-01

must_haves:
  truths:
    - "Filter panel renders two native <input type=\"date\"> fields labeled From and To; entering a range filters the visible graph; clearing both shows all users again"
    - "Filter panel renders one iOS-style toggle per module; all toggles default ON; toggling a module off filters out users whose access intersects only that module"
    - "Filter panel renders a multi-select dropdown of distinct companyRole values, with a search box at the top; an Unspecified bucket appears for null values"
    - "Filter panel header shows 'Showing X of Y users' whenever any filter is non-default"
    - "A Clear all filters button is visible only when at least one filter is non-default; one click resets to DEFAULT_FILTERS and clears the URL params"
    - "When zero users match an active filter set, an overlay reads 'No users match these filters - adjust filters to see results' with a Clear filters button"
  artifacts:
    - path: "app/(dashboard)/users/AccUsersGraph.tsx"
      provides: "Date range inputs, module toggles, companyRole multi-select with search, count header, clear-all button, empty-state overlay"
      contains: "type=\"date\""
  key_links:
    - from: "Module toggle click"
      to: "filters.disabledModules state"
      via: "toggle handler appends/removes module id from disabledModules array"
      pattern: "disabledModules"
    - from: "Clear all button"
      to: "DEFAULT_FILTERS + URL params clear"
      via: "setFilters(DEFAULT_FILTERS) which writeFiltersToUrl flushes to empty querystring"
      pattern: "DEFAULT_FILTERS"
---

<objective>
Build the user-facing filter UI on top of the logic core from plan 02 and the renderer hide from plan 03. Adds the date-range inputs (FILT-02 UI), iOS-style module toggles (FILT-03 UI), companyRole multi-select with search (DATA-01 filter UI), the count summary header, the empty-state overlay, and the Clear-all button. Closes with a human-verify checkpoint that exercises the full end-to-end behavior.

Purpose: Phase 2.5 ships only when the user can actually drive the new filter behaviors from the UI. This plan delivers that and verifies it interactively.

Output: Filter panel UI overhaul in `AccUsersGraph.tsx` plus a recorded human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-acc-data-filter-refinement/02.5-CONTEXT.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-RESEARCH.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-02-SUMMARY.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-03-SUMMARY.md
@app/(dashboard)/users/AccUsersGraph.tsx

<interfaces>
<!-- iOS-style module toggle (RESEARCH.md Code Examples) -->
```tsx
function ModuleToggle({ label, enabled, onToggle }: { label: string; enabled: boolean; onToggle: () => void }) {
  return (
    <div className="flex items-center justify-between py-0.5">
      <span className="text-[11px] text-gray-600 truncate max-w-[140px]">{label}</span>
      <button role="switch" aria-checked={enabled} onClick={onToggle}
        className={cn(
          "relative inline-flex h-4 w-7 shrink-0 rounded-full border-2 border-transparent transition-colors duration-200",
          enabled ? "bg-emerald-500" : "bg-gray-200"
        )}>
        <span className={cn(
          "pointer-events-none inline-block h-3 w-3 rounded-full bg-white shadow transform transition-transform duration-200",
          enabled ? "translate-x-3" : "translate-x-0"
        )} />
      </button>
    </div>
  );
}
```

<!-- companyRole multi-select with search -->
See RESEARCH.md "companyRole Multi-Select with Search" — plain implementation, no headless UI library.

<!-- Empty-state overlay -->
See RESEARCH.md Pattern 6 — absolute-positioned overlay rendered when visibleCount === 0 && hasActiveFilters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build filter UI controls (date range, module toggles, companyRole multi-select)</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
    Replace the existing filter panel section with the new controls. Existing role/lastAddedBucket/adminAccess controls stay as-is.

    1. **Date range** (FILT-02):
       - Two native `<input type="date">` fields labeled "From" and "To".
       - Bound to `filters.dateFrom` and `filters.dateTo` (YYYY-MM-DD strings — the format `<input type="date">` returns natively).
       - onChange handler updates the corresponding filter field.
       - Place inside a small section labeled "Last Activity" (or "Date Range" — pick what reads naturally next to existing controls).
       - **Important** about labeling: if plan 01's diagnostic log revealed that `lastSignIn` is unavailable from the API and the field is silently `null` for everyone, the active range would exclude all users. If this is the case (check 02.5-01-SUMMARY.md), update the section label to "Added Date" and document the limitation in this plan's SUMMARY. This is the documented fallback per RESEARCH.md Open Question 2.

    2. **Module toggles** (FILT-03):
       - Vertical list, one `ModuleToggle` per module from `moduleOptions` (built in plan 02).
       - `enabled` = NOT `filters.disabledModules.includes(moduleId)`.
       - `onToggle` = if currently enabled, push moduleId into disabledModules; else remove it.
       - Section label: "Modules" with a hint underneath: "Toggle off to filter out users with access only to that module."

    3. **companyRole multi-select with search** (DATA-01):
       - Search-box `<input type="text">` at the top filtering the option list by `String.includes` (case-insensitive). Per CONTEXT.md.
       - Below, a scrollable list of options each rendered as a clickable chip/row showing the role name and count.
       - Clicking a row toggles the value in `filters.companyRoles`.
       - Include the synthetic `Unspecified` option (mapped from null companyRole users).
       - Section label: "Company Role".

    4. **Filter panel header — count summary**:
       - At the top of the filter panel, render `Showing {visibleCount} of {totalCount} users` whenever `activeFilterCount > 0`.
       - When `activeFilterCount === 0`, hide the header (or show a static "All users" string — Claude's discretion).

    5. **Clear all filters button**:
       - Render only when `activeFilterCount > 0`.
       - onClick: `setFilters(DEFAULT_FILTERS)`.
       - Place it near the panel header (top-right of the count summary line). Styling: small ghost button matching existing styles.

    6. **Empty-state overlay**:
       - When `visibleUserCount === 0 && activeFilterCount > 0`, render an overlay over the canvas using the snippet in RESEARCH.md Pattern 6.
       - Z-index above the canvas, below modal layers. The "Clear filters" button inside the overlay calls `setFilters(DEFAULT_FILTERS)` (same handler as the top-of-panel button — extract a single handler).

    Styling note: match the existing filter-panel typography and spacing already used for `roles` / `lastAddedBuckets` / `adminAccess` controls — the existing styling is the source of truth, do not introduce a new design language.

    Per CONTEXT.md "Claude's Discretion": exact placement of clear-all button, overlay copy nuances, and tween curve for the 150ms fade are yours to make reasonable choices on. Document choices in SUMMARY.

    Avoid: no new design system / UI library. No date picker library. No headless UI dropdown library. All native + Tailwind.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Filter panel renders all four control groups (existing + date range + module toggles + companyRole). Count header, Clear-all button, and empty-state overlay render under their documented conditions. `tsc` + `npm run build` pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Phase 2.5 end-to-end</name>
  <files>app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>Pause execution and present the verification steps below to the user. Do not proceed until the user replies "approved" (or describes issues that route to gap-closure planning).</action>
  <verify>User signs off the 9-section walkthrough below with "approved".</verify>
  <done>User has typed "approved" or has filed specific issues for gap-closure.</done>
  <what-built>
    Phase 2.5 complete stack: companyRole + lastSignIn extracted from ACC, surfaced in side panel, exposed as filter dimensions; date range filter; module on/off toggles with all-on default; recursive cascade hide; URL query-param persistence; CSS fade transition; empty-state overlay; Clear-all button.
  </what-built>
  <how-to-verify>
    1. Start the dev stack: `npm run dev` and open the users / graph page.

    2. **DATA-01 (companyRole)**:
       a. Open the side panel for any user — confirm a "Company Role" row exists. It should show the user's role or "Unspecified" when null.
       b. In the filter panel, expand the "Company Role" section — confirm a search box at top + a scrollable list of distinct roles with counts. The "Unspecified" bucket should appear if any user has null companyRole.
       c. Type a partial role name in the search box → only matching options remain.
       d. Click an option → it becomes selected (visually distinct), graph filters, count header updates.

    3. **FILT-01 (fully hidden)**:
       a. Activate any filter that excludes some users.
       b. Confirm excluded users are GONE from the canvas — not greyed-out, not faded. The canvas should look like those users never existed.
       c. Confirm edges to/from hidden users are also gone.
       d. Confirm cascade: if all users connected to a project disappear, the project node itself disappears (zoom out to verify). Same for role and module nodes.
       e. Click in empty space where a hidden user used to be → nothing happens (no hit-test on hidden nodes).

    4. **FILT-02 (date range)**:
       a. Pick a "From" date in the past, leave "To" empty → only users with lastSignIn ≥ From remain.
       b. Pick a "To" date as well → inclusive on both ends.
       c. Clear both inputs → all users return.
       d. Note: if the SUMMARY for plan 01 documented that `lastSignIn` is unavailable and the section is labeled "Added Date" instead, verify against `addedOn` semantics.

    5. **FILT-03 (module toggles)**:
       a. All toggles default ON → all users visible (assuming no other active filters).
       b. Toggle one module OFF → users whose access is exclusively that module disappear; users with that module + others remain.
       c. Toggle all modules OFF → users with module access disappear, users with no module access remain visible (per CONTEXT.md decision: users with no modules always pass module check).
       d. Toggle all back ON → everyone returns.

    6. **URL persistence**:
       a. Apply several filters → confirm the URL updates with `roles=...&moff=...&from=...&to=...&admin=...`.
       b. Hard refresh the page → filter state is restored exactly from URL.
       c. Copy URL into a new tab → same filtered view loads.

    7. **Count header + Clear all + Empty state**:
       a. With any filter active → header reads `Showing X of Y users`. Clear-all button is visible.
       b. Click Clear-all → all filters reset, URL params disappear, header hides.
       c. Apply filters that match zero users → empty-state overlay appears with message and Clear-filters button. Click it → returns to all-visible.

    8. **Drag-while-filter-changes**:
       a. Start dragging a node, then (without releasing) change a filter — confirm the drag continues smoothly. On pointerup, the filter then applies.

    9. **Performance sanity**:
       a. On the production-scale hub (~25k nodes), apply and clear filters several times → no visible lag spike beyond the 150ms fade. Cosmos GPU physics should reflow visible nodes within a couple of seconds.

    Resume by typing `approved` if all 9 sections check out, or describe specific issues to feed back into a gap-closure plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues for gap-closure planning</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` passes
- Manual: walk through all 9 verification sections in the checkpoint
- Manual: confirm no `localStorage.getItem("acc-graph-filters")` calls remain (`grep -r "acc-graph-filters" app/`)
</verification>

<success_criteria>
- All four phase requirements are observable from the UI:
  - DATA-01: companyRole shown in side panel, usable as filter dimension
  - FILT-01: filtered users + cascading nodes are fully invisible
  - FILT-02: from/to date inputs filter inclusively
  - FILT-03: module toggles default all-on, exclude-list semantics
- URL params are the single source of truth (no localStorage filter persistence)
- Empty state, Clear-all, count summary, and 150ms fade all behave per CONTEXT.md
- User signs off with "approved" at the human-verify checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-acc-data-filter-refinement/02.5-04-SUMMARY.md` documenting:
- Final UI layout decisions (where Clear-all sits, what the empty-state overlay looks like)
- Whether the date filter labels "Last Activity" or fell back to "Added Date" (depends on plan 01 diagnostic outcome)
- Performance observations on the 25k-node hub
- Any gaps surfaced at human-verify (these become candidates for `/gsd:plan-phase --gaps`)
</output>
