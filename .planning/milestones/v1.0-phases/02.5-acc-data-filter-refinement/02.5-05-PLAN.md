---
phase: 02.5-acc-data-filter-refinement
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/graphRenderers.ts
  - app/(dashboard)/users/AccUsersGraph.tsx
autonomous: false
gap_closure: true
requirements: [FILT-01]

must_haves:
  truths:
    - "When the user toggles a module OFF (or sets a date range, or selects a companyRole) the excluded user nodes disappear from the canvas immediately — not greyed out, fully invisible."
    - "Edges to/from hidden user nodes are also invisible."
    - "Re-enabling the filter brings the nodes back without a page reload."
    - "When usePhysics=true (Cosmos GPU physics) the hide is still effective."
  artifacts:
    - path: "app/(dashboard)/users/graphRenderers.ts"
      provides: "setVisibleIndices that survives draw() nodeCount-change overwrites; identity cache reset on relevant code paths"
    - path: "app/(dashboard)/users/AccUsersGraph.tsx"
      provides: "Filter effect that reliably calls setVisibleIndices once renderer ref is populated; no silent drop when ref is briefly null"
  key_links:
    - from: "filters state change"
      to: "GPU point-size buffer with zeros at excluded indices"
      via: "useEffect → rebuildVisibleIndices → cosmosRendererRef.current.setVisibleIndices → setPointSizes"
      pattern: "setVisibleIndices.*visibleIndexSetRef"
    - from: "draw() nodeCount-change branch"
      to: "applies current visible set when rebuilding the size buffer"
      via: "buildNodeSizeBuffer must respect lastVisibleSet (or be re-applied immediately after)"
      pattern: "nodeCount !== this.lastNodeCount"
---

<objective>
Fix FILT-01: filtering changes do not visually hide nodes in the canvas. The user reports the canvas "just blinks and stays the same." Code path is wired but the GPU buffer either never receives the zeroed sizes or is overwritten by a subsequent `draw()` call. This plan instruments the path, identifies the root cause among 5 suspects from the verification report, applies the minimal fix, and gates on a human re-verify.

Purpose: Without this fix, the entire phase fails its primary success criterion (truth #2 in ROADMAP) and FILT-02 / drag-while-filter cannot even be exercised.

Output:
- Diagnostic instrumentation that proves which suspect(s) are at fault.
- Minimal, targeted code fix in `graphRenderers.ts` and/or `AccUsersGraph.tsx`.
- All temporary diagnostic logs removed before commit (or gated behind a debug flag and disabled by default).
- Human-verify confirms excluded nodes disappear immediately on filter change.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-VERIFICATION.md
@.planning/phases/02.5-acc-data-filter-refinement/02.5-04-SUMMARY.md
@app/(dashboard)/users/graphRenderers.ts
@app/(dashboard)/users/AccUsersGraph.tsx

<interfaces>
<!-- Key contracts the executor needs — no exploration required. -->

From app/(dashboard)/users/graphRenderers.ts (current state):

```typescript
// CosmosGraphRenderer
private lastVisibleSet: ReadonlySet<number> | null = null;  // line 330
private lastNodeCount: number;                              // tracked by draw()

// Existing draw() branch that may overwrite zeroed sizes (line ~537):
//   if (nodeCount !== this.lastNodeCount) {
//     this.graph.setPointSizes(buildNodeSizeBuffer(nodeCount, this.nodeConnections));
//   }
// NOTE: this branch does NOT consult this.lastVisibleSet, so excluded indices get full sizes again.

setVisibleIndices(visibleSet: ReadonlySet<number>): void {
  if (visibleSet === this.lastVisibleSet) return;   // identity cache (line 818)
  this.lastVisibleSet = visibleSet;
  // builds Float32Array, zero for excluded, degree-based for included
  // calls this.graph.setPointSizes(sizes)
}

destroy(): void {
  // ...
  this.lastVisibleSet = null;  // line 856
}
```

From app/(dashboard)/users/AccUsersGraph.tsx:

```typescript
const cosmosRendererRef = useRef<CosmosGraphRenderer | null>(null);
const visibleIndexSetRef = useRef<Set<number>>(new Set());

// Filter effect (line ~1199-1205):
useEffect(() => {
  rebuildVisibleIndices();
  cosmosRendererRef.current?.setVisibleIndices(visibleIndexSetRef.current);
  // ... CSS fade trigger ...
}, [filters, rebuildVisibleIndices]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument the FILT-01 hide path with gated diagnostics, reproduce, and identify root cause(s)</name>
  <files>app/(dashboard)/users/graphRenderers.ts, app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
Add temporary diagnostic logs gated behind `const DEBUG_FILT01 = true;` (module-scope const at top of each file). All logs MUST use the prefix `[FILT-01-debug]` so they can be grep-removed at the end. Do NOT use `console.log` directly — wrap every log call in `if (DEBUG_FILT01) console.log(...)`.

Suspect coverage — instrument all 5 root causes from 02.5-VERIFICATION.md:

1. **Suspect #1 — cosmosRendererRef null at filter time.** In AccUsersGraph.tsx immediately before line 1202, log:
   `[FILT-01-debug] filter-effect cosmosRenderer=`, `!!cosmosRendererRef.current`, `usePhysics=`, `usePhysicsRef.current`, `visibleSize=`, `visibleIndexSetRef.current.size`, `totalInstances=`, `instancesRef.current?.length`.

2. **Suspect #2 — draw() nodeCount-change overwrites zeroed sizes.** In graphRenderers.ts inside the `if (nodeCount !== this.lastNodeCount)` branch (~line 537), log:
   `[FILT-01-debug] draw nodeCount-change`, `prev=`, `this.lastNodeCount`, `next=`, `nodeCount`, `lastVisibleSetSize=`, `this.lastVisibleSet?.size ?? "null"`.

3. **Suspect #3 — Cosmos GPU physics ignores setPointSizes.** In `setVisibleIndices` after the `setPointSizes(sizes)` call (~line 837), log:
   `[FILT-01-debug] setVisibleIndices applied`, `total=`, `nodeCount`, `visible=`, `visibleSet.size`, `zeroed=`, `nodeCount - visibleSet.size`, `usePhysics=`, `(this as any).usePhysics ?? "n/a"`.

4. **Suspect #4 — lastVisibleSet stale across renderer re-init.** In `setVisibleIndices` at the very top, before the identity check, log:
   `[FILT-01-debug] setVisibleIndices called`, `sameRefAsLast=`, `visibleSet === this.lastVisibleSet`, `lastIsNull=`, `this.lastVisibleSet === null`.

5. **Suspect #5 — frame.userIndices mismatch in Canvas2D path.** In AccUsersGraph.tsx wherever `frame.userIndices` (or the equivalent buffer that drives Canvas2D edge-skip) is populated by the worker tick, log on first tick after a filter change:
   `[FILT-01-debug] frame.userIndices size=`, `frame.userIndices?.length`, `visibleIndexSetRef.size=`, `visibleIndexSetRef.current.size`. (If you cannot locate the exact site cheaply, log inside `rebuildVisibleIndices` after `visibleIndexSetRef.current = visibleSet` showing both `visibleSet.size` and any `instIdxRef.current?.length`.)

Run the dev server (`npm run dev` if not already running, or just trust hot-reload). Reproduce: load `/users`, wait for graph render, toggle one module OFF, observe console + canvas.

Document findings in a scratch file `.planning/phases/02.5-acc-data-filter-refinement/02.5-05-DIAGNOSTIC.md` with:
- Console log dump from one filter toggle
- Which suspect(s) the logs implicate (one or more)
- A one-paragraph hypothesis explaining the root cause

DO NOT remove the diagnostic logs yet — Task 2 needs them to verify the fix.

This task ends with the diagnostic file written. NO production code changes beyond the gated logs.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.5-acc-data-filter-refinement/02.5-05-DIAGNOSTIC.md && grep -q "Suspect" .planning/phases/02.5-acc-data-filter-refinement/02.5-05-DIAGNOSTIC.md</automated>
  </verify>
  <done>Diagnostic file exists with concrete log evidence and a named root cause among the 5 suspects (or explicit ruling-out of all 5 plus a new hypothesis).</done>
</task>

<task type="auto">
  <name>Task 2: Apply minimal fix and remove diagnostic logs</name>
  <files>app/(dashboard)/users/graphRenderers.ts, app/(dashboard)/users/AccUsersGraph.tsx</files>
  <action>
Based on the root cause from Task 1, apply the minimal targeted fix. Default fixes per suspect (apply ONLY the one(s) the diagnostic implicates — do not blanket-apply all):

**Fix for Suspect #1 (renderer ref null at filter time):** In AccUsersGraph.tsx where the renderer is assigned (line ~839 `cosmosRendererRef.current = renderer;`), immediately follow with:
```ts
if (visibleIndexSetRef.current.size > 0) renderer.setVisibleIndices(visibleIndexSetRef.current);
```
This guarantees the visible set is applied as soon as the renderer becomes available, even if filter state was already non-default.

**Fix for Suspect #2 (draw() overwrites zeroed sizes):** In graphRenderers.ts, modify the `if (nodeCount !== this.lastNodeCount)` branch (~line 537-553) so that after computing `buildNodeSizeBuffer(nodeCount, this.nodeConnections)`, if `this.lastVisibleSet` is non-null, zero-out the entries for indices NOT in `this.lastVisibleSet` BEFORE calling `setPointSizes`. Do NOT null `lastVisibleSet` here — preserve it across the rebuild. Add a comment: `// FILT-01: preserve current visibility filter when rebuilding size buffer for new node count`.

**Fix for Suspect #3 (GPU physics ignores setPointSizes):** In graphRenderers.ts `setVisibleIndices`, after `setPointSizes(sizes)`, also call `this.graph.render?.()` (already present) and additionally call `this.graph.setConfig?.({ ... })` only if the diagnostic confirms physics is overriding. Most likely fix: pause/resume the simulation around the size upload via Cosmos's `pause()` / `start()` if available; otherwise use `setConfigPartial({ simulationDecay: 1 })` momentarily — but ONLY if Suspect 3 is the confirmed cause. Otherwise skip.

**Fix for Suspect #4 (stale lastVisibleSet across re-init):** In graphRenderers.ts `setVisibleIndices`, weaken the identity cache: replace the `visibleSet === this.lastVisibleSet` short-circuit with a content-aware check `if (this.lastVisibleSet && this.lastVisibleSet.size === visibleSet.size && [...visibleSet].every(i => this.lastVisibleSet!.has(i))) return;` — or simply remove the identity cache entirely (it's an optimization, not correctness). The simpler and safer choice is REMOVAL: delete the identity-cache short-circuit and always rebuild. Document the removal in a comment.

**Fix for Suspect #5 (frame.userIndices mismatch):** Wire `frame.userIndices` to the same Set that `rebuildVisibleIndices` produces. If the worker independently writes a different array, override it with `Array.from(visibleIndexSetRef.current)` after each tick.

After applying the fix:
1. Re-test: toggle module OFF, observe canvas — excluded users must disappear.
2. Confirm via `[FILT-01-debug]` logs that the previously-implicated path now behaves correctly.
3. Remove ALL `[FILT-01-debug]` logs and the `DEBUG_FILT01` constants from both files. Use grep to confirm zero matches: `grep -r "FILT-01-debug" app/` should return nothing.
4. Run `npx tsc --noEmit` — must be 0 errors.
5. Run `npm run build` — must pass clean.
6. Update `.planning/phases/02.5-acc-data-filter-refinement/02.5-05-DIAGNOSTIC.md` with a "Resolution" section naming the applied fix.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit 2>&1 | tail -5 && grep -r "FILT-01-debug" app/ ; echo "exit=$?"</automated>
  </verify>
  <done>TypeScript clean. Zero `FILT-01-debug` matches in app/. Diagnostic file has Resolution section. Fix is committed (separate commit from Task 1's instrumentation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human re-verify FILT-01 hide effect</name>
  <what-built>FILT-01 renderer-hide fix. Excluded user nodes should now visually disappear from the canvas immediately when filters change. Edges to hidden nodes should also be invisible.</what-built>
  <how-to-verify>
1. Visit `/users` (dev or production build — both should work).
2. Wait for graph to render fully (you should see the user nodes scattered in the canvas).
3. Note the bottom-right instance count — call this Y.
4. In the filter panel, toggle ONE module OFF.
5. Expected: nodes whose access is ONLY that module disappear within ~150ms. The "Showing X of Y users" header updates. Canvas does NOT just blink and stay the same.
6. Toggle the same module back ON. Nodes reappear.
7. Set a date range (From: 2024-01-01, To: 2024-12-31, or any range that should exclude some users). Excluded users disappear.
8. Open a companyRole filter and select one role. Only users with that role remain visible.
9. Click "Clear all". All users return.
10. Optional stress test: toggle modules rapidly. No console errors. No ghost nodes. No visual flicker beyond the intended 150ms fade.

Report PASS or describe what still doesn't work.
  </how-to-verify>
  <resume-signal>Type "approved" if FILT-01 visually hides excluded nodes and edges, or describe what is still broken so we can iterate.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` returns 0 errors
- `npm run build` succeeds
- `grep -r "FILT-01-debug" app/` returns 0 matches
- Human-verify task 3 returns "approved"
- Diagnostic markdown file exists with Resolution section
</verification>

<success_criteria>
- User toggles a module OFF and the affected user nodes vanish from the canvas (not greyed-out — fully invisible).
- Edges connected to hidden user nodes also vanish.
- Date range and companyRole filters produce the same hide behavior.
- Re-enabling filters brings nodes back.
- No console errors during filter toggling.
- Behavior is consistent in both `usePhysics=true` (Cosmos GPU) and (if reachable) Canvas2D fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-acc-data-filter-refinement/02.5-05-SUMMARY.md` covering:
- Which of the 5 suspects was the actual root cause
- The minimal fix applied
- Diagnostic logs removed (grep evidence)
- Human-verify outcome and any remaining caveats
</output>
