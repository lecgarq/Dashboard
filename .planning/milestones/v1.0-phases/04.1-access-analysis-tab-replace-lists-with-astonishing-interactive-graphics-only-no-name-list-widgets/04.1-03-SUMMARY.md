---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 03
subsystem: dashboard
tags: [widget-rewrite, calendar-heatmap, orbit-constellation, svg, framer-motion, d3-time, d3-scale]
dependency_graph:
  requires:
    - "04.1-01 _shared primitives (dashboardTokens, useTransition, HoverSpotlight, FocusRing, WidgetSkeleton)"
    - "04.1-01 SelectionContext kind:'admin' and kind:'day' union members"
    - "Phase 04 P02 BulkAccUser.addedOn (real ACC created_at)"
    - "Phase 04 P01 BulkAccUser.isAccountAdmin"
  provides:
    - "RecentlyAddedWidget — 90-day SVG calendar heatmap (zero <table>, zero <li>, zero member-name DOM)"
    - "AdminAccessWidget — animated orbit constellation (zero <table>, initials-only reveal)"
  affects:
    - "Wave 2 sibling 04.1-02 unaffected (file ownership disjoint)"
tech_stack:
  added: []
  patterns:
    - "SVG heatmap with d3-time timeDays grid + d3-scale-chromatic interpolateBlues + framer-motion fade-in"
    - "Hand-rolled orbit math (cos/sin) in useMemo; framer-motion drives spring entrance + selection scale"
    - "Local-TZ day bucketing via date-fns format(parseISO(addedOn), 'yyyy-MM-dd') (Pitfall 5 mitigation)"
    - "Initials-only hover reveal preserves CONTEXT lock 'no member names in canvas DOM'"
    - "Orbit edge cases inline: 0 -> empty state, 1 -> centered pulse, 2 -> forced ±90 poles, >=3 -> standard ring"
key_files:
  created: []
  modified:
    - app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
    - app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
key_decisions:
  - "Calendar layout chosen as 13×7=91 cells with one leading phantom-pad cell so the trailing column lands on `today` — keeps a clean rectangle without trailing nulls; the pad slot is simply skipped at map time"
  - "Day bucketing uses date-fns `format(parseISO(addedOn), 'yyyy-MM-dd')` in browser-local TZ so a member added at 23:30 local stays in their local day even when the ISO string carries UTC; addresses Pitfall 5 directly"
  - "Window dim band uses `differenceInCalendarDays(today, cellDate) < windowDays` so today counts as day 0 and the 7d window includes 7 calendar days inclusive of today (not 8)"
  - "Hover spotlight on the calendar deliberately omitted — the dim-out-of-band animation already supplies the focal cue, and adding per-cell hover-spotlight on 90 cells fights the band logic; documented inline via the `useHoverSpotlight` _not_ being imported"
  - "AdminAccessWidget aria-label uses ordinal only ('Account admin 2 of 5') — preserves the 'names exit the canvas' rule even from the screen-reader perspective; full identity arrives via the side panel which has the full table"
  - "Initials fallback when name is missing: first 2 chars of email local-part, uppercased — keeps the 2-character visual rhythm consistent across nodes"
  - "Edge case for 2 admins enforced as ±90° poles on a smaller R=80 (vs the standard R=110); both cases read as intentional layout, never as 'broken layout pulled to one side'"
  - "Single-admin pulse uses framer-motion `scale: [1, 1.06, 1]` with `duration: 2.4, repeat: Infinity` — collapses to instant under prefers-reduced-motion via useTransition"
  - "LastSignIn CSV column emitted via `(u as unknown as { lastSignIn?: string|null }).lastSignIn ?? ''` — BulkAccUser doesn't currently expose lastSignIn on the public type but the field exists in the cache; defensive cast keeps the CSV column slot live without a phase-spanning type change"
metrics:
  duration: "12m"
  tasks: 2
  files: 2
  completed_at: "2026-05-08T22:48:00Z"
---

# Phase 04.1 Plan 03: RecentlyAdded Heatmap + Admin Constellation Summary

Wave-2 second pair lands the final two name-list widget rewrites: RecentlyAddedWidget becomes a GitHub-style 90-day SVG calendar heatmap and AdminAccessWidget becomes an animated orbit constellation — both routing clicks to the existing DashboardSidePanel via the `kind: "day"` and `kind: "admin"` selection union members shipped in 04.1-01. Zero `<table>`, zero `<li>`, zero member-name DOM in either canvas; CSV preserved on both.

## Results

- 2 tasks completed
- All verifications passed
- `npx tsc --noEmit` clean across both rewritten files (and full repo)
- Forbidden JSX scan (`<table|<ul>|<li>|<TableRow|<TableCell`) returns zero matches in the rewritten files (only a single comment reference to "the prior table" in the calendar widget header)
- `Math.cos`/`Math.sin` orbit math present in AdminAccessWidget (orbit positions, ≥3 admin branch)
- `setSelected({ kind: "day"` and `setSelected({ kind: "admin"` confirmed via grep
- File ownership disjoint from sibling 04.1-02; no cross-touch on RecommendationsWidget or OutlierCombosWidget

## Tasks Completed

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Rewrite RecentlyAddedWidget as 90-day SVG calendar heatmap | 72df67d | done |
| 2 | Rewrite AdminAccessWidget as animated orbit constellation | fae0a11 | done |

## Deviations from Plan

None — plan executed as written.

The plan flagged a few discretionary choices ("13×7 with pad cell vs 14×7 with trailing nulls"; "skip useHoverSpotlight if it complicates band logic"); both were resolved per plan permission inline (13×7+pad and skipped-with-comment respectively) and are captured under `key_decisions` rather than as deviations.

## Files Changed

- `app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx` — full rewrite from table-based listing to 90-day SVG heatmap; window selector inline, CSV preserved, side-panel routing via `kind: "day"`.
- `app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx` — full rewrite from table-based listing to SVG orbit constellation; 0/1/2/≥3-admin edge cases inline, CSV preserved, side-panel routing via `kind: "admin"`, initials-only hover reveal.

## Verification

- `npx tsc --noEmit` (full repo): passed (no output)
- `grep -nE "table|<ul|<li|<TableRow|<TableCell"` on RecentlyAddedWidget: 1 match — comment line "Replaces the prior table" only
- `grep -nE "<table|<ul>|<li>|<TableRow|<TableCell"` on AdminAccessWidget: 0 matches
- `grep -n "timeDays|setSelected"` on RecentlyAddedWidget: confirmed `timeDays(...)` call + `setSelected({ kind: "day", ...` dispatch
- `grep -nE "Math\.cos|Math\.sin|setSelected"` on AdminAccessWidget: confirmed orbit math + `setSelected({ kind: "admin", ...` dispatch
- Both widgets retain registry id (`recent`, `admins`) and `col-span-1` — widgetRegistry.ts untouched
- File ownership coordination: zero changes to RecommendationsWidget.tsx or OutlierCombosWidget.tsx (owned by sibling 04.1-02)

## Authentication Gates

None.

## Self-Check: PASSED

- File `app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx`: FOUND
- File `app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx`: FOUND
- Commit `72df67d`: FOUND
- Commit `fae0a11`: FOUND
