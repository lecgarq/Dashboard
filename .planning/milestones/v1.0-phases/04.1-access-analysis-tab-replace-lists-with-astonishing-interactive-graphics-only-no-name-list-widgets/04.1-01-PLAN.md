---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/dashboard/widgets/_shared/dashboardTokens.ts
  - app/(dashboard)/users/dashboard/widgets/_shared/useTransition.ts
  - app/(dashboard)/users/dashboard/widgets/_shared/HoverSpotlight.tsx
  - app/(dashboard)/users/dashboard/widgets/_shared/FocusRing.tsx
  - app/(dashboard)/users/dashboard/widgets/_shared/WidgetSkeleton.tsx
  - app/(dashboard)/users/dashboard/selectionContext.tsx
  - app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  - app/(dashboard)/users/dashboard/DashboardClient.tsx
  - package.json
autonomous: true
requirements:
  - DASH-09
  - DASH-10
  - DASH-13

must_haves:
  truths:
    - "dashboardTokens.ts exports SEVERITY_COLOR (light + dark), spring presets, hover dim/focus opacities — single source of truth"
    - "SelectedFinding union accepts kind: 'admin' (single admin user) and kind: 'day' (date string + member emails)"
    - "DashboardSidePanel switch handles all 6 kinds without TS errors (junk, duplicate, outlier, role, admin, day) — exhaustiveness check passes"
    - "SelectionProvider reconciles selected against new findings on data refresh — invalid selections auto-clear, valid selections persist with refreshed payload"
    - "WidgetSkeleton primitive exports per-shape skeletons (BubbleSkeleton, TreemapSkeleton, CalendarSkeleton, OrbitSkeleton)"
    - "HoverSpotlight + FocusRing + useTransition (reduced-motion-aware) helpers exported and consumable by widgets"
    - "d3-hierarchy, d3-scale, d3-time, d3-time-format, d3-scale-chromatic + types installed (npm ls succeeds)"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/widgets/_shared/dashboardTokens.ts"
      provides: "Shared palette + spring tokens + hover/focus opacity constants"
      exports: ["SEVERITY_COLOR_LIGHT", "SEVERITY_COLOR_DARK", "BUBBLE_SPRING", "CELL_SPRING", "ORBIT_SPRING", "HOVER_OPACITY_DIM", "HOVER_OPACITY_FOCUS", "useSeverityColor"]
    - path: "app/(dashboard)/users/dashboard/widgets/_shared/useTransition.ts"
      provides: "Reduced-motion-aware transition wrapper"
      exports: ["useTransition"]
    - path: "app/(dashboard)/users/dashboard/widgets/_shared/HoverSpotlight.tsx"
      provides: "Hover-spotlight (lift + dim siblings) helper or hook"
    - path: "app/(dashboard)/users/dashboard/widgets/_shared/FocusRing.tsx"
      provides: "Cosmos-glow keyboard focus outline component"
    - path: "app/(dashboard)/users/dashboard/widgets/_shared/WidgetSkeleton.tsx"
      provides: "Per-widget shape-matched skeleton primitives"
      exports: ["BubbleSkeleton", "TreemapSkeleton", "CalendarSkeleton", "OrbitSkeleton"]
    - path: "app/(dashboard)/users/dashboard/selectionContext.tsx"
      provides: "Extended SelectedFinding union + reconciliation effect"
      contains: "kind: \"admin\""
  key_links:
    - from: "app/(dashboard)/users/dashboard/DashboardSidePanel.tsx"
      to: "selectionContext SelectedFinding union"
      via: "switch (selected.kind) covering admin + day"
      pattern: "case \"admin\"|case \"day\""
    - from: "app/(dashboard)/users/dashboard/DashboardClient.tsx"
      to: "SelectionProvider"
      via: "passes findings so provider can reconcile selection on refresh"
      pattern: "SelectionProvider.*findings"
---

<objective>
Wave 1 foundation. Land the cross-widget shared primitives (palette tokens, spring tokens, reduced-motion wrapper, hover/focus helpers, shape-matched skeletons) AND the selection-context extensions (admin + day kinds, reconciliation on refresh) AND DashboardSidePanel exhaustiveness updates BEFORE the four widget rewrites in Wave 2.

Purpose: Researcher Pitfall 10 — extending SelectedFinding without simultaneously updating the panel switch produces tsc errors that block parallel widget work. Pitfall 4 — selection-reconciliation must live in one place (the provider), not duplicated per widget. Token unification is the lever the polish wave (04.1-06) pulls without touching widget bodies.

Output: Five new `_shared/` files, an extended selection context, a panel switch that compiles against the new union, and the d3-hierarchy/d3-scale/d3-time/d3-scale-chromatic deps installed.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-CONTEXT.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-RESEARCH.md
@app/(dashboard)/users/dashboard/selectionContext.tsx
@app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
@app/(dashboard)/users/dashboard/DashboardClient.tsx
@app/(dashboard)/users/dashboard/findingsContext.tsx
@app/(dashboard)/users/dashboard/widgetRegistry.ts

<interfaces>
<!-- Existing SelectedFinding union (selectionContext.tsx) -->
```ts
export type SelectedFinding =
  | { kind: "junk"; finding: JunkRoleFinding }
  | { kind: "duplicate"; finding: DuplicateRoleFinding }
  | { kind: "outlier"; finding: OutlierFinding }
  | { kind: "role"; role: string; severity: Severity | undefined }
  | null;
```

<!-- DashboardFindings shape (lib/acc/dashboardAnalytics.ts) -->
```ts
export interface DashboardFindings {
  junkFindings: JunkRoleFinding[];
  duplicateFindings: DuplicateRoleFinding[];
  outliers: OutlierFinding[];
  // ... tiers, roleSeverityIndex, etc.
}
```

<!-- Cosmos palette source (graphRenderers.ts comments — for reference only, do NOT import) -->
- Light bg `#F8F7F4`
- Dimmed grey `#9CA3AF`
- shadcn destructive `#EF4444`, primary `#14213D`, ring `#1D3557`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install d3-hierarchy/d3-scale/d3-time/d3-scale-chromatic deps</name>
  <files>package.json, package-lock.json</files>
  <action>
Run two npm installs from the repo root:

```
npm install d3-hierarchy d3-scale d3-time d3-time-format d3-scale-chromatic
npm install -D @types/d3-hierarchy @types/d3-scale @types/d3-time @types/d3-time-format @types/d3-scale-chromatic
```

`d3-force` and `framer-motion` already exist — do NOT add them again. Do NOT add `@cosmograph/react` (out of scope per REQUIREMENTS.md). Do NOT add ECharts/Nivo (already exist; not used by new widgets).

After install, run `npm ls d3-hierarchy d3-scale d3-time d3-time-format d3-scale-chromatic` to confirm versions are resolved (should print non-empty version lines, no UNMET PEER DEPENDENCY for the d3 modules added).

If `npm install` warns about peer deps unrelated to d3, leave them alone — the Phase 4 baseline is already running with those warnings.
  </action>
  <verify>
    <automated>npm ls d3-hierarchy d3-scale d3-time d3-time-format d3-scale-chromatic 2>&1 | grep -v "(empty)" | head -20</automated>
  </verify>
  <done>All five d3 packages and their @types/* counterparts present in package.json dependencies; npm ls returns version lines for each.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared widget primitives (_shared/ folder)</name>
  <files>
    app/(dashboard)/users/dashboard/widgets/_shared/dashboardTokens.ts,
    app/(dashboard)/users/dashboard/widgets/_shared/useTransition.ts,
    app/(dashboard)/users/dashboard/widgets/_shared/HoverSpotlight.tsx,
    app/(dashboard)/users/dashboard/widgets/_shared/FocusRing.tsx,
    app/(dashboard)/users/dashboard/widgets/_shared/WidgetSkeleton.tsx
  </files>
  <action>
Create five files under `app/(dashboard)/users/dashboard/widgets/_shared/`:

1. **dashboardTokens.ts** — single source of truth for palette + motion. Export:
   - `SEVERITY_COLOR_LIGHT = { HIGH: "#EF4444", MEDIUM: "#F59E0B", LOW: "#3B82F6" } as const`
   - `SEVERITY_COLOR_DARK  = { HIGH: "#FF6B6B", MEDIUM: "#FFB454", LOW: "#5DADE2" } as const`
   - `NEUTRAL_LIGHT = "#9CA3AF"`, `NEUTRAL_DARK = "#6B7280"`
   - `HIGHLIGHT_LIGHT = "#1D3557"`, `HIGHLIGHT_DARK = "#5DADE2"` (Cosmos focus glow leans cool)
   - Spring `Transition` consts (import type from `framer-motion`):
     - `BUBBLE_SPRING = { type: "spring", stiffness: 180, damping: 22, mass: 1 }`
     - `CELL_SPRING   = { type: "spring", stiffness: 220, damping: 26, mass: 1 }`
     - `ORBIT_SPRING  = { type: "spring", stiffness: 90,  damping: 14, mass: 1.4 }`
   - Constants: `HOVER_OPACITY_DIM = 0.4`, `HOVER_OPACITY_FOCUS = 1.0`
   - React hook `useSeverityColor(): typeof SEVERITY_COLOR_LIGHT` — reads `useTheme()` from `next-themes`, returns the appropriate map (resolves `"system"` via `resolvedTheme`).
   - Top-of-file comment: "Cosmos-leaning palette duplicated from graphRenderers.ts (#F8F7F4 bg, #9CA3AF dim grey). Future refactor may extract a single Cosmos token module — Open Question 2 in 04.1-RESEARCH.md."

2. **useTransition.ts** — exports `useTransition(spring: Transition): Transition`. Uses framer-motion's `useReducedMotion()` hook; returns `{ duration: 0 }` when reduce is true, otherwise the input spring. SSR-safe (useReducedMotion returns null on server).

3. **HoverSpotlight.tsx** — exports a `useHoverSpotlight()` hook that returns `{ hoveredId: string | null, getOpacity: (id: string) => number, bind: (id: string) => { onMouseEnter, onMouseLeave, onFocus, onBlur } }`. `getOpacity` returns `HOVER_OPACITY_FOCUS` when no hover OR when id matches; otherwise `HOVER_OPACITY_DIM`. Add a tiny doc comment showing the intended `motion.*` integration pattern.

4. **FocusRing.tsx** — exports a `<FocusRing visible: boolean, x, y, r? | width?+height?, rx? />` component that renders an SVG `<rect>` or `<circle>` with `fill="none"`, `stroke={HIGHLIGHT_*}`, `strokeWidth={2}`, `filter="drop-shadow(0 0 6px {color})"`, and `pointer-events: none`. Animate `opacity` 0↔1 via `motion.*` with a fast spring. Used by widgets to render Cosmos-glow keyboard focus outlines instead of the browser default.

5. **WidgetSkeleton.tsx** — exports four named functions:
   - `BubbleSkeleton({ width = 600, height = 360 })` — 8–12 ghost circles arranged via static positions (no d3 import — keep skeleton zero-cost), pulsing via `motion.circle animate={{ opacity: [0.3, 0.6, 0.3] }} transition={{ duration: 1.6, repeat: Infinity }}`.
   - `TreemapSkeleton({ width = 360, height = 360 })` — 6–8 ghost rects, same pulse.
   - `CalendarSkeleton({ width, height })` — 7×13 grid of small rects, same pulse.
   - `OrbitSkeleton({ width = 320, height = 320 })` — 1 center circle + 5 orbit dots on a faint circular guide.
   All skeletons must respect prefers-reduced-motion (skip the pulse animation when reduce is true) — import `useTransition` from `./useTransition`.

All files use `"use client"` if they call React hooks. All exports must be named (no default exports). TypeScript strict — no `any`. Run `npx tsc --noEmit` (or the repo's typecheck script if different) to confirm compile.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "_shared/(dashboardTokens|useTransition|HoverSpotlight|FocusRing|WidgetSkeleton)" | head -20 ; echo "---" ; ls "app/(dashboard)/users/dashboard/widgets/_shared/"</automated>
  </verify>
  <done>All five files exist; tsc emits zero errors that reference any `_shared/` file; named exports match the contract above.</done>
</task>

<task type="auto">
  <name>Task 3: Extend SelectedFinding union + reconciliation + DashboardSidePanel switch</name>
  <files>
    app/(dashboard)/users/dashboard/selectionContext.tsx,
    app/(dashboard)/users/dashboard/DashboardSidePanel.tsx,
    app/(dashboard)/users/dashboard/DashboardClient.tsx
  </files>
  <action>
**3a — selectionContext.tsx**
Extend the discriminated union:

```ts
export type SelectedFinding =
  | { kind: "junk"; finding: JunkRoleFinding }
  | { kind: "duplicate"; finding: DuplicateRoleFinding }
  | { kind: "outlier"; finding: OutlierFinding }
  | { kind: "role"; role: string; severity: Severity | undefined }
  | { kind: "admin"; email: string }                            // NEW — single admin from constellation
  | { kind: "day"; dateIso: string; emails: string[] }          // NEW — calendar cell click
  | null;
```

Add reconciliation. SelectionProvider gains an optional prop `findings?: DashboardFindings` (typed via existing `DashboardFindings` import — already imported in findingsContext consumers). Inside the provider, add `useEffect` that runs when `findings` reference changes:

```ts
useEffect(() => {
  if (!findings || !selected) return;
  // Validate the current selection still exists in the new findings.
  const stillExists = isSelectionValid(selected, findings);
  if (!stillExists) setSelected(null);
}, [findings, selected]);
```

Add a small private `isSelectionValid(sel, findings)` helper that returns true if:
  - `kind:"junk"` and `findings.junkFindings.some(f => f.role === sel.finding.role)`
  - `kind:"duplicate"` and `findings.duplicateFindings.some(f => f.roleA === sel.finding.roleA && f.roleB === sel.finding.roleB)`
  - `kind:"outlier"` and `findings.outliers.some(f => f.moduleSet.join(",") === sel.finding.moduleSet.join(","))`
  - `kind:"role" | "admin" | "day"` always returns `true` (those don't depend on findings going stale within Phase 4.1's scope; future enhancement).

Note in a code comment: "Pitfall 4 (04.1-RESEARCH.md): selection reconciliation centralized here so widgets stay dumb."

**3b — DashboardSidePanel.tsx**
Update `selectedKey` and the `PanelBody` switch to handle the two new kinds. TypeScript exhaustiveness via `const _exhaust: never = selected` in default branches.

For `kind: "admin"`: render an `AdminBody` that takes `{ email, users }`, looks up the user by email (case-insensitive), and shows name / companyRole / lastSignIn / projects-count / a single-row MembersTable (or just a Section with the email). Header title: "Account admin: {name || email}". CSV button on MembersTable already provides export.

For `kind: "day"`: render a `DayBody` that takes `{ dateIso, emails, users }` and reuses `MembersTable` with `csvName={"recent-${dateIso}-members.csv"}`. Header title: "Members added on {formatted dateIso}". Use the existing `date-fns` (already a dep).

**3c — DashboardClient.tsx**
Pass the existing computed findings into SelectionProvider:

```tsx
<FindingsProvider value={findings}>
  <SelectionProvider findings={findings}>
    {/* existing children */}
  </SelectionProvider>
</FindingsProvider>
```

(Confirm the actual provider nesting in DashboardClient.tsx — Phase 4 P08 decision: SelectionProvider wraps INSIDE FindingsProvider. Keep that order; only add the `findings` prop.)

Run typecheck after edits — there should be zero TS errors anywhere referencing SelectedFinding, including in the four widgets that will be rewritten next wave (those widgets currently only use `kind: junk|duplicate|outlier|role`, all of which are preserved).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(selectionContext|DashboardSidePanel|DashboardClient)\.tsx" | head -20</automated>
  </verify>
  <done>SelectedFinding union has 6 kinds; DashboardSidePanel switch is exhaustive (TS no-error); SelectionProvider clears stale selections when findings change; DashboardClient passes findings into SelectionProvider.</done>
</task>

</tasks>

<verification>
- `npm ls` shows the five new d3 packages installed.
- `npx tsc --noEmit` passes with zero errors anywhere in `app/(dashboard)/users/dashboard/`.
- Repo build smoke-test (optional): `npm run build` reaches the `users/dashboard` route without breaking — only run if quick.
- Code grep confirms no widget file imports a hex color that should have come from `dashboardTokens.ts` (this gets enforced in Wave 2).
</verification>

<success_criteria>
- `_shared/` folder exists with the five primitives, all named exports compile.
- `SelectedFinding` union includes `admin` and `day` kinds; panel renders both bodies.
- SelectionProvider auto-clears stale junk/duplicate/outlier selections when findings refresh.
- Wave 2 widget rewrites can import from `_shared/dashboardTokens` and `selectionContext` without further scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-01-SUMMARY.md`
</output>
