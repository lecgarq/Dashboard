---
phase: 04-access-analysis
plan: 07
type: execute
wave: 3
depends_on:
  - 04-05
files_modified:
  - app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
  - app/(dashboard)/users/dashboard/findingsContext.tsx
  - app/(dashboard)/users/dashboard/widgetRegistry.ts
  - app/(dashboard)/users/dashboard/DashboardClient.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-09
  - DASH-13
must_haves:
  truths:
    - "KPI strip shows total members, total roles, total modules, and counts of HIGH/MEDIUM/LOW findings + duplicate pairs + outlier sets"
    - "Recommendations widget lists junk roles tiered HIGH/MEDIUM/LOW + duplicate-role pairs with severity badges (DASH-09 widget surface)"
    - "Outlier combos widget lists module sets held by <5% of members"
    - "Role-relationship flow diagram (React Flow) shows roles as nodes with edges between duplicate pairs (visual reinforcement of DASH-04)"
    - "Recently-added widget has a 7d/30d/90d toggle (default 30d) and lists members within window by addedOn"
    - "Admin-access widget lists ONLY users where isAccountAdmin === true (NOT projectAdmin)"
    - "Findings are computed ONCE at the page level via computeAllFindings and passed to all consumers via context (Pattern 3 — single source of truth)"
    - "Each chart/table widget exposes a Download CSV button"
    - "Recommendations CSV columns are exactly: Type, Severity, Roles, Members, Modules, SuggestedAction (DASH-13)"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/findingsContext.tsx"
      provides: "FindingsContext + useFindings hook providing DashboardFindings to all widgets"
      exports: ["FindingsProvider", "useFindings"]
    - path: "app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx"
      provides: "Full-width 6-tile KPI strip"
      min_lines: 40
    - path: "app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx"
      provides: "shadcn Table of junk + duplicate findings with severity badges + Download CSV (Type, Severity, Roles, Members, Modules, SuggestedAction)"
      min_lines: 60
    - path: "app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx"
      provides: "Table or list of <5% module-combination outliers + Download CSV"
      min_lines: 30
    - path: "app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx"
      provides: "@xyflow/react graph with role nodes + duplicate-pair edges"
      min_lines: 50
    - path: "app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx"
      provides: "shadcn Table + 7d/30d/90d ToggleGroup + Download CSV"
      min_lines: 40
    - path: "app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx"
      provides: "shadcn Table filtered to isAccountAdmin=true + Download CSV"
      min_lines: 30
  key_links:
    - from: "DashboardClient.tsx"
      to: "lib/acc/dashboardAnalytics.ts computeAllFindings"
      via: "useMemo(() => computeAllFindings(users), [users])"
      pattern: "computeAllFindings"
    - from: "FindingsProvider"
      to: "All findings-consuming widgets"
      via: "React context (single source of truth — Pattern 3)"
      pattern: "useFindings"
    - from: "AdminAccessWidget.tsx"
      to: "BulkAccUser.isAccountAdmin"
      via: "users.filter(u => u.isAccountAdmin === true)"
      pattern: "isAccountAdmin"
    - from: "RecentlyAddedWidget.tsx"
      to: "BulkAccUser.addedOn"
      via: "differenceInDays(now, parseISO(u.addedOn)) <= window"
      pattern: "addedOn"
    - from: "RoleRelationshipFlowWidget.tsx"
      to: "DuplicateRoleFinding[]"
      via: "render one ReactFlow edge per duplicate pair"
      pattern: "@xyflow/react"
---

<objective>
Build the six remaining dashboard widgets that surface findings (KPI strip, Recommendations, Outliers, Role-relationship flow, Recently-added, Admin-access) and introduce the shared FindingsContext that gives every widget a single source of truth for severity badges and recommendations (Pattern 3).

Purpose: This plan finishes the widget set defined by CONTEXT.md and lands the Pattern-3 contract — analytics are computed ONCE in DashboardClient and consumed by both the Recommendations widget AND any future inline severity badge on roles in other charts (DASH-09's "wherever else they appear" requirement).

Output: All 9 widgets rendering real data; findings computed once and shared via context; CSV-per-widget operational; Recommendations CSV in the locked column order.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-access-analysis/04-CONTEXT.md
@.planning/phases/04-access-analysis/04-RESEARCH.md
@.planning/phases/04-access-analysis/04-01-SUMMARY.md
@.planning/phases/04-access-analysis/04-02-SUMMARY.md
@.planning/phases/04-access-analysis/04-03-SUMMARY.md
@.planning/phases/04-access-analysis/04-05-SUMMARY.md

# Existing files to consume
@lib/acc/dashboardAnalytics.ts
@lib/acc/csvExport.ts
@lib/acc/acc-types.ts
@components/ui/badge.tsx
@components/ui/table.tsx
@components/ui/toggle-group.tsx
@app/(dashboard)/users/dashboard/widgetRegistry.ts
@app/(dashboard)/users/dashboard/DashboardClient.tsx

<interfaces>
```typescript
// From 04-03:
import {
  computeAllFindings,
  type DashboardFindings,
  type JunkRoleFinding,
  type DuplicateRoleFinding,
  type OutlierFinding,
  type Severity,
} from "@/lib/acc/dashboardAnalytics";

// From 04-05:
import { downloadCsv } from "@/lib/acc/csvExport";

// New context (this plan):
const FindingsContext = createContext<DashboardFindings | null>(null);
export function FindingsProvider({ findings, children }: { findings: DashboardFindings; children: ReactNode });
export function useFindings(): DashboardFindings;
```

```typescript
// Recommendations CSV row shape (LOCKED by DASH-13):
type RecommendationCsvRow = {
  Type: "Junk" | "Duplicate";
  Severity: "HIGH" | "MEDIUM" | "LOW";
  Roles: string;        // ", "-joined
  Members: number;
  Modules: string;      // ", "-joined
  SuggestedAction: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FindingsContext + KpiStripWidget + RecommendationsWidget (with locked CSV columns)</name>
  <files>app/(dashboard)/users/dashboard/findingsContext.tsx,app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx,app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx</files>
  <action>
    1) `findingsContext.tsx`:
       - `"use client"`. Create `FindingsContext = createContext<DashboardFindings | null>(null)`.
       - `FindingsProvider({ findings, children })` returns `<FindingsContext.Provider value={findings}>{children}</FindingsContext.Provider>`.
       - `useFindings()` reads context, throws if null ("must be inside FindingsProvider").

    2) `KpiStripWidget.tsx` (full-width, `col-span-2` per registry):
       - `"use client"`. Props: `{ users: BulkAccUser[] }`.
       - `useFindings()` for finding counts.
       - Render shadcn `Card` containing a 6-tile flex/grid:
         - Total members: `users.length`
         - Total roles: distinct count across `users[].allRoles`
         - Total modules: distinct count across `users[].allModules`
         - HIGH findings: count of `junkRoles` with severity HIGH + duplicate pairs (per row, count once)
         - MEDIUM findings: count of MEDIUM
         - LOW findings: count of LOW
       - Each tile has a label + large number + a colored dot indicating severity (HIGH=red, MEDIUM=amber, LOW=gray).
       - Download CSV exports the 6 tiles as rows `[{Metric, Value}, ...]`.

    3) `RecommendationsWidget.tsx` (full-width, `col-span-2`):
       - `"use client"`. `useFindings()`. NO `users` prop needed.
       - Render shadcn `Table` with columns: Type, Severity, Roles, Members, Modules, Suggested Action.
       - Rows = junkRoles (Type="Junk") + duplicateRoles (Type="Duplicate"). Severity column shows a `Badge` with color by severity (HIGH=destructive, MEDIUM=warning, LOW=secondary). For duplicates the badge defaults to MEDIUM (per 04-03 SUMMARY decision).
       - Suggested Action strings:
         - Junk HIGH: "Delete role — zero members, zero modules, all-inactive >90d"
         - Junk MEDIUM: "Review for deletion — {2 signals fired}"
         - Junk LOW: "Investigate — {1 signal fired}"
         - Duplicate: "Consider merging '{roleA}' and '{roleB}' (modules identical, name overlap {n}%)"
       - Each row is clickable — set up an `onClick` that calls a `setSelectedFinding(finding)` callback received via prop (the actual side panel comes in 04-08). For now, wire prop `onSelect?: (finding: JunkRoleFinding | DuplicateRoleFinding) => void`; if undefined, no-op.
       - Download CSV — exact columns LOCKED by DASH-13:
         ```
         Type, Severity, Roles, Members, Modules, SuggestedAction
         ```
         For Junk: Roles = the role name; Members = affectedMembers.length; Modules = "" (zero in HIGH case).
         For Duplicate: Roles = `${roleA}, ${roleB}`; Members = unique count of affectedMembers; Modules = ", ".join(union of modules).

    Avoid: computing findings inside this widget (Anti-pattern: drift); changing CSV column casing or order (LOCKED by spec).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>FindingsContext + useFindings exported; KpiStripWidget shows 6 tiles; RecommendationsWidget lists junk + duplicate rows with badges; Download CSV header line is exactly `Type,Severity,Roles,Members,Modules,SuggestedAction`.</done>
</task>

<task type="auto">
  <name>Task 2: OutlierCombosWidget + RoleRelationshipFlowWidget + RecentlyAddedWidget + AdminAccessWidget</name>
  <files>app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx,app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx,app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx,app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx</files>
  <action>
    1) `OutlierCombosWidget.tsx` (`col-span-1`):
       - `"use client"`. `useFindings()`. Render shadcn `Table` with columns: Module Set, Members, % of Total.
       - Rows from `findings.outlierCombos`. Module Set = comma-joined sorted modules.
       - Empty state: "No unusual access patterns detected."
       - Download CSV: `[{ ModuleSet, MemberCount, TotalMembers, Percent }]`.

    2) `RoleRelationshipFlowWidget.tsx` (`col-span-2`):
       - `"use client"`. `useFindings()` + props `{ users: BulkAccUser[] }`.
       - Use `@xyflow/react` `<ReactFlow>` inside a sized container `<div style={{ height: 400 }}>`.
       - Nodes: one node per role that appears in any duplicateRoles finding (limit visible nodes to roles involved in duplicates to avoid clutter; if 0 duplicates, render empty state "No duplicate-role pairs detected").
       - Edges: one edge per `DuplicateRoleFinding` (source=roleA, target=roleB), labeled with `${nameOverlap*100}%`. Edge color reflects nameOverlap intensity.
       - Each node: custom default node with role name + small severity badge if `roleSeverityIndex.get(role)` is set.
       - Layout: simple force/static (use the default `ReactFlow` rendering with manual positions in a circle — `position: { x: cos(i)*200, y: sin(i)*200 }` — or use `dagre` if already in deps; do NOT pull a new layout dep).
       - `<Controls />` and `<Background />` from `@xyflow/react`.
       - Download CSV: `[{ RoleA, RoleB, NameOverlap, Members, Modules }]`.
       - Avoid: importing the deprecated `reactflow` package — use `@xyflow/react` (RESEARCH.md State of the Art).

    3) `RecentlyAddedWidget.tsx` (`col-span-1`):
       - `"use client"`. Props: `{ users: BulkAccUser[] }`.
       - Local `useState` for `windowDays: 7 | 30 | 90` (default 30).
       - shadcn `ToggleGroup type="single"` with three buttons "7d / 30d / 90d", default 30d.
       - Filter: `users.filter(u => u.addedOn && differenceInDays(now, parseISO(u.addedOn)) <= windowDays)`.
       - Render shadcn `Table` with columns: Email, Name, Company Role, Added (relative — e.g., "5 days ago" via date-fns `formatDistanceToNow`).
       - Empty state: "No members added in the last {windowDays} days."
       - Note about caveat: if 04-02 chose Path B (fallback), display a small inline tooltip on the widget header: "Based on first sync detection — actual ACC join dates not available." Read 04-02 SUMMARY to determine whether to show this caveat.
       - Download CSV: filtered rows.

    4) `AdminAccessWidget.tsx` (`col-span-1`):
       - `"use client"`. Props: `{ users: BulkAccUser[] }`.
       - Filter: `users.filter(u => u.isAccountAdmin === true)`. Strict `=== true` (Pitfall: legacy cache rows may have undefined → exclude).
       - Render shadcn `Table` with columns: Email, Name, Company Role.
       - Empty state: "No account-level admins found."
       - Subtitle: "Account-level only — project admins and shadow admins excluded." (Per CONTEXT.md decision.)
       - Download CSV: filtered rows.

    All four widgets follow the same shadcn `Card` chrome pattern as 04-06 widgets (header with title + Download CSV button, body with content). Each handles empty state inline.

    Avoid: fetching data in widgets (data is passed via props OR context — Pattern 3); recomputing findings (use useFindings); using deprecated `reactflow` package.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>All four widgets compile; build passes; empty states render gracefully; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire findings + remaining widgets into DashboardClient + complete WIDGETS registry</name>
  <files>app/(dashboard)/users/dashboard/DashboardClient.tsx,app/(dashboard)/users/dashboard/widgetRegistry.ts</files>
  <action>
    1) `widgetRegistry.ts` — Replace ALL remaining placeholders so WIDGETS now points to real components for: kpi, recommendations, outliers, flow, recent, admins. Combined with 04-06's coverage/tiers/heatmap, all 9 ids now reference production widgets.

    2) `DashboardClient.tsx` — Compute findings ONCE:
       ```typescript
       const findings = useMemo(
         () => users ? computeAllFindings(users, new Date()) : null,
         [users]
       );
       ```
       - Wrap the `<DndContext>` tree in `<FindingsProvider findings={findings}>` only when `findings` is non-null; otherwise show skeleton.
       - Render path: `Loading? skeleton. Error? inline message. Empty users? "Sync ACC first" prompt with link to existing AccAnalysisPanel. Else: provider + grid.`
       - Each widget receives `users` + `workspaceEmails` via spread props (each component consumes only what it needs). Widgets that need findings call `useFindings()` themselves — DO NOT pass findings as a prop (one source of truth via context).
       - Add a "Refresh" button in the header that triggers `bulkAccSummary.refetch()` AND `workspace.getDirectory.refetch()` AND increments a key on FindingsProvider so memo recomputes (since `users` reference changes on refetch this happens automatically; the key is just for safety).

    3) Manual smoke (UAT happens in 04-08):
       - npm run dev → /users/dashboard → all 9 widgets render with real data.
       - Drag-reorder still works.
       - Each widget's Download CSV produces a non-empty file.
       - Recommendations CSV header line === `Type,Severity,Roles,Members,Modules,SuggestedAction`.
       - Admin widget shows ONLY users with isAccountAdmin=true (count should match what 04-01 SUMMARY documented).

    Avoid: passing `findings` as prop to widgets (use context — Pattern 3 isolation); recomputing findings in any widget; mutating widget order in registry beyond filling components.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>tsc + build pass; /users/dashboard renders all 9 widgets with real data; findings computed once at DashboardClient; widgets consume via useFindings; recommendations CSV header matches the locked column order.</done>
</task>

</tasks>

<verification>
- All 6 widget files + findingsContext.tsx exist
- WIDGETS registry maps all 9 ids to real components (no placeholders)
- DashboardClient calls computeAllFindings via useMemo
- Recommendations CSV header line: `Type,Severity,Roles,Members,Modules,SuggestedAction` (LOCKED by DASH-13)
- AdminAccessWidget filters by `isAccountAdmin === true`
- RecentlyAddedWidget toggle defaults to 30d, supports 7/30/90
- Build passes; no SSR crashes
</verification>

<success_criteria>
- DASH-03/04/05 surface in Recommendations + Outliers + Flow widgets
- DASH-06 visible: configurable timeframe toggle
- DASH-07 visible: account-admin only list
- DASH-09 wired: shared FindingsContext drives both Recommendations widget and (future) inline badges
- DASH-13 layout: 9 widgets in 2-col grid, KPI/Recommendations/Heatmap/Flow are full-width
- DASH-11 + DASH-12 + DASH-13 retained from 04-05/04-06 (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/04-access-analysis/04-07-SUMMARY.md` documenting:
- Pattern 3 (FindingsContext) as the single source of truth + how 04-08 (drill-down) integrates
- Whether the addedOn fallback caveat is being shown in RecentlyAddedWidget
- Account admin count vs project admin count (sanity check on 04-01 plumbing)
</output>
