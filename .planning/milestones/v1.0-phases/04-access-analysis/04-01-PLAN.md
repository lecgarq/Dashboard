---
phase: 04-access-analysis
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/acc/acc-types.ts
  - lib/server/acc-admin.ts
  - server/routers/users.ts
autonomous: true
requirements:
  - DASH-07
must_haves:
  truths:
    - "BulkAccUser objects carry an isAccountAdmin boolean field for every user"
    - "Account-level admins (ACC Admin API accountAdmin flag) are correctly identified — project admins are NOT promoted to account admins"
    - "After a fresh ACC sync, isAccountAdmin reflects the live ACC Admin API value for the user"
  artifacts:
    - path: "lib/acc/acc-types.ts"
      provides: "BulkAccUser.isAccountAdmin field declaration"
      contains: "isAccountAdmin"
    - path: "lib/server/acc-admin.ts"
      provides: "isAccountAdmin extraction from ACC Admin API user payload"
      contains: "isAccountAdmin"
    - path: "server/routers/users.ts"
      provides: "isAccountAdmin propagation through bulkAccSummary"
      contains: "isAccountAdmin"
  key_links:
    - from: "lib/server/acc-admin.ts"
      to: "ACC Admin API user record"
      via: "raw user payload field (accountAdmin / isAccountAdmin)"
      pattern: "accountAdmin|isAccountAdmin"
    - from: "server/routers/users.ts (bulkAccSummary)"
      to: "BulkAccUser shape"
      via: "transform from accMemberCache.data into BulkAccUser"
      pattern: "isAccountAdmin"
---

<objective>
Plumb the ACC account-level admin flag end-to-end so the Phase 4 Admin-Access widget (DASH-07) can list ONLY account admins (not project admins).

Purpose: Research confirmed `lib/server/acc-admin.ts:241` only captures `accessLevels.projectAdmin`. ACC's account-level admin flag lives at the user-record level on the ACC Admin API and is not extracted today. Without this Wave 0 fix, DASH-07 cannot be implemented correctly.

Output: `BulkAccUser.isAccountAdmin: boolean` populated by ACC Admin API, available downstream in `bulkAccSummary` query and any widget consuming `BulkAccUser[]`.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-access-analysis/04-CONTEXT.md
@.planning/phases/04-access-analysis/04-RESEARCH.md

# Existing data-pipeline files this plan modifies
@lib/acc/acc-types.ts
@lib/server/acc-admin.ts
@server/routers/users.ts

<interfaces>
<!-- Key shapes to extend (do NOT redefine — extend in place). -->

From lib/acc/acc-types.ts (existing — extend):
```typescript
export interface BulkAccUser {
  email: string;
  fullName: string | null;
  companyRole: string | null;        // added in Phase 2.5
  lastSignIn: string | null;          // added in Phase 2.5
  syncedAt: string;                   // sync timestamp (NOT join date)
  // ... projects, allRoles, allModules
  // ADD: isAccountAdmin: boolean
}
```

From lib/server/acc-admin.ts (existing — line ~241 currently captures projectAdmin only):
```typescript
// Currently: only accessLevels.projectAdmin is read from ACC Admin API user payload.
// ACC Admin API user record exposes account-level admin flag at the top level
// (likely as `accountAdmin: true` — verify against raw payload during this task).
```

From server/routers/users.ts (existing — bulkAccSummary):
```typescript
// Lines ~904-1007 — transforms accMemberCache.data rows into BulkAccUser shape.
// Add isAccountAdmin to that transform.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — log raw ACC Admin API user payload to identify the account-admin field name</name>
  <files>lib/server/acc-admin.ts</files>
  <action>
    Add a temporary console.log inside the user-record processing loop in lib/server/acc-admin.ts (the same loop where projectAdmin is currently captured around line 241) that logs the FIRST user's raw payload keys and the value of any candidate field: `accountAdmin`, `isAccountAdmin`, `account_admin`. Trigger one ACC sync (call the existing rebuildAccGraphCache or equivalent) — capture the log output.

    Determine the exact field name + type (boolean vs string vs presence-of-role).

    Document the finding inline in a comment at the call site:
      // ACC Admin API account-admin flag: <fieldName> (<type>) — verified <date> via raw-payload log.

    Then REMOVE the temporary console.log (do not commit it). Keep ONLY the documentation comment + the next task's extraction.

    DO NOT extend the schema or types yet — this task only resolves Open Question (which field).

    Avoid: leaving console.log in committed code; assuming `accountAdmin` without verification (research called this LOW confidence on exact field name).
  </action>
  <verify>
    <automated>git diff lib/server/acc-admin.ts | grep -E "accountAdmin|isAccountAdmin" </automated>
  </verify>
  <done>Comment in lib/server/acc-admin.ts documents the verified ACC Admin API field name for account-level admin; no temporary console.log remains in committed code.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BulkAccUser type + plumb isAccountAdmin through acc-admin.ts and bulkAccSummary</name>
  <files>lib/acc/acc-types.ts,lib/server/acc-admin.ts,server/routers/users.ts</files>
  <action>
    1) lib/acc/acc-types.ts — Add `isAccountAdmin: boolean;` to the `BulkAccUser` interface (NOT optional — every user must have a deterministic value). Place near `companyRole`/`lastSignIn` to mirror Phase 2.5 ordering.

    2) lib/server/acc-admin.ts — In the same user-loop where projectAdmin is captured (~line 241), read the verified field from Task 1 and assign it onto the per-user record persisted to `accMemberCache.data` (or whatever local aggregate this loop builds). Default to `false` when the field is absent. Use the verified field name from Task 1.

    3) server/routers/users.ts — In the `bulkAccSummary` transform (~lines 904-1007 per research), pull `isAccountAdmin` off the cached row and emit it onto each BulkAccUser. Default `false` if missing on legacy cache rows (cache predating this change should not crash the query).

    Do NOT change the Prisma schema — the cache column is JSON (`accMemberCache.data`), so adding a new field is a non-migration change. Existing rows will have `false` until next sync; that is acceptable per phase context (read-only dashboard, refresh on sync).

    Avoid: adding a Prisma migration (unnecessary — JSON column); breaking strict-null with `isAccountAdmin?: boolean` (must be deterministic boolean).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "isAccountAdmin" lib/acc/acc-types.ts lib/server/acc-admin.ts server/routers/users.ts</automated>
  </verify>
  <done>BulkAccUser.isAccountAdmin is a non-optional boolean; ACC sync writes it; bulkAccSummary returns it; tsc --noEmit passes; grep finds the field in all three target files.</done>
</task>

<task type="auto">
  <name>Task 3: Resync ACC + verify a known account admin lights up</name>
  <files>lib/server/acc-admin.ts</files>
  <action>
    Trigger a fresh ACC sync via the existing app sync entrypoint (the same one users hit from AccAnalysisPanel "Sync All to ACC"). You may invoke this via the existing tRPC procedure programmatically, or via the dev UI.

    After sync completes, run a one-shot script (or open Prisma Studio / db query) that selects from accMemberCache and lists distinct emails where `data->>'isAccountAdmin' = 'true'`. Confirm the count matches expectations (the user's own account should be present if they are an account admin; it should NOT match the count of all projectAdmin users).

    If the count is 0 unexpectedly: re-check Task 1's field-name verification. If non-zero AND not equal to projectAdmin count: success.

    Document the verification result + count in the plan SUMMARY.

    Avoid: only checking count > 0 (must specifically differ from projectAdmin count to prove the account-vs-project distinction).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Account-admin count from a fresh sync is non-zero, distinct from project-admin count, and the SUMMARY documents the observed numbers.</done>
</task>

</tasks>

<verification>
- BulkAccUser.isAccountAdmin field exists, is non-optional boolean, present in tsc output for downstream consumers
- Fresh ACC sync populates the field for at least one user
- Count of isAccountAdmin=true is distinct from count of any-projectAdmin (proves field semantics differ)
</verification>

<success_criteria>
- DASH-07 unblocked: a downstream widget can `users.filter(u => u.isAccountAdmin)` to render the Admin-Access list
- No Prisma migration introduced (JSON column accommodates the new field)
- Legacy cache rows do not break bulkAccSummary (default `false`)
</success_criteria>

<output>
After completion, create `.planning/phases/04-access-analysis/04-01-SUMMARY.md` documenting:
- Verified ACC Admin API field name + type
- Counts observed (account-admin vs project-admin) post-sync
- Any caveats for downstream widget consumers
</output>
