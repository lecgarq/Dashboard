---
phase: 04-access-analysis
plan: 08
type: execute
wave: 4
depends_on:
  - 04-06
  - 04-07
files_modified:
  - app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  - app/(dashboard)/users/dashboard/selectionContext.tsx
  - app/(dashboard)/users/dashboard/DashboardClient.tsx
  - app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
autonomous: false
requirements:
  - DASH-09
  - DASH-10
  - DASH-13
must_haves:
  truths:
    - "Clicking a junk-role row in Recommendations opens a right-side panel showing affected members, modules, projects, suggested action, and raw data"
    - "Clicking a duplicate-role pair opens a panel showing both roles' members union, modules union, and the suggested merge action"
    - "Clicking an outlier row opens a panel showing the module set and affected members"
    - "Inline severity badges appear on role names inside the heatmap (y-axis labels) and the role-relationship flow nodes — driven by the same roleSeverityIndex consumed by the Recommendations widget (DASH-09)"
    - "Closing the side panel returns the user to the dashboard with no navigation away (no URL change)"
    - "Phase 4 UAT human-verify gate confirms all 13 success criteria from ROADMAP Phase 4"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/DashboardSidePanel.tsx"
      provides: "shadcn Sheet-based drill-down panel rendering finding details"
      min_lines: 80
    - path: "app/(dashboard)/users/dashboard/selectionContext.tsx"
      provides: "SelectionContext + useSelection hook for selected finding state"
      exports: ["SelectionProvider", "useSelection"]
  key_links:
    - from: "DashboardClient.tsx"
      to: "SelectionProvider + DashboardSidePanel"
      via: "wrap subtree, render panel as sibling of grid"
      pattern: "SelectionProvider"
    - from: "RecommendationsWidget / OutlierCombosWidget"
      to: "useSelection().setSelected"
      via: "row onClick"
      pattern: "useSelection"
    - from: "RolesModulesHeatmapWidget y-axis labels"
      to: "useFindings().roleSeverityIndex"
      via: "ECharts axisLabel.formatter rich text with severity color"
      pattern: "roleSeverityIndex"
    - from: "RoleRelationshipFlowWidget custom node"
      to: "useFindings().roleSeverityIndex"
      via: "node renderer reads severity per role"
      pattern: "roleSeverityIndex"
---

<objective>
Add the right-side drill-down panel (DASH-10) that opens on click of any finding without navigating away, surface inline severity badges on roles in the heatmap and flow widgets (DASH-09's "wherever else they appear elsewhere"), and run the final phase-level UAT human-verify gate against all 13 ROADMAP Phase 4 success criteria.

Purpose: Closes the loop on DASH-09 (badges everywhere) and DASH-10 (drill-down). The panel is the "Pattern 4" reuse from `AccUserSidePanel.tsx`. The UAT checkpoint is the last gate before /gsd:verify-work — it ensures the user signs off on the dashboard end-to-end.

Output: Working drill-down panel for all clickable findings, inline severity badges on heatmap + flow widgets, and a passed human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-access-analysis/04-CONTEXT.md
@.planning/phases/04-access-analysis/04-RESEARCH.md
@.planning/phases/04-access-analysis/04-06-SUMMARY.md
@.planning/phases/04-access-analysis/04-07-SUMMARY.md

# Existing precedent for side panel (READ for pattern)
@app/(dashboard)/users/AccUserSidePanel.tsx
@components/ui/sheet.tsx
@components/ui/badge.tsx

<interfaces>
```typescript
// New context (this plan):
type SelectedFinding =
  | { kind: "junk"; finding: JunkRoleFinding }
  | { kind: "duplicate"; finding: DuplicateRoleFinding }
  | { kind: "outlier"; finding: OutlierFinding }
  | { kind: "role"; role: string; severity: Severity | undefined }
  | null;

export function SelectionProvider({ children }: { children: ReactNode });
export function useSelection(): {
  selected: SelectedFinding;
  setSelected: (s: SelectedFinding) => void;
  clear: () => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SelectionContext + DashboardSidePanel (shadcn Sheet) with all four finding kinds</name>
  <files>app/(dashboard)/users/dashboard/selectionContext.tsx,app/(dashboard)/users/dashboard/DashboardSidePanel.tsx</files>
  <action>
    1) `selectionContext.tsx`:
       - `"use client"`. Define `SelectedFinding` discriminated union covering junk / duplicate / outlier / role kinds.
       - `SelectionProvider` wraps children and exposes state via context.
       - `useSelection()` returns `{ selected, setSelected, clear }`.

    2) `DashboardSidePanel.tsx`:
       - `"use client"`. Mirror `AccUserSidePanel.tsx` pattern (shadcn `Sheet` from `components/ui/sheet.tsx`, `side="right"`).
       - `useSelection()` to read `selected`. Open when non-null, close (via Sheet `onOpenChange`) calls `clear()`.
       - Renders different body per kind:
         - **Junk** (`JunkRoleFinding`):
           - Title: "Junk role: {role}" + severity badge.
           - Sections: "Signals fired" (3 checkboxes/badges showing which signals), "Affected members" (table of emails — from finding.affectedMembers), "Affected projects" (list), "Suggested action" (text from RecommendationsWidget action map), "Raw data" (collapsible JSON view).
         - **Duplicate** (`DuplicateRoleFinding`):
           - Title: "Duplicate roles: {roleA} ↔ {roleB}".
           - Sections: name overlap %, modules (union), affected members (table), affected projects, suggested action ("Consider merging — keep {longerName}"), raw data.
         - **Outlier** (`OutlierFinding`):
           - Title: "Unusual access pattern".
           - Sections: module set (chip list), member count + %, affected members (table), suggested action ("Review whether this combination is intentional"), raw data.
         - **Role** (`{ role, severity }`):
           - Title: "Role: {role}" + severity badge if defined.
           - Sections: members holding this role (filter `users` for those whose allRoles contains role), modules typically associated, projects.
       - Wrap content in `framer-motion` `<motion.div>` with a slide-in transition (already-installed library; mirror what AccUserSidePanel does if it uses it, else add a simple `initial={{ x: 30, opacity: 0 }} animate={{ x: 0, opacity: 1 }}`).
       - Provide a "Download CSV" inside the panel for the affected-members table.
       - Width: `sm:max-w-lg` to avoid covering the dashboard at 1280px (CONTEXT.md density requirement).

    Props: panel reads everything it needs (users for cross-reference) from props passed by DashboardClient. Pass `{ users: BulkAccUser[] }` only.

    Avoid: navigating away (no `router.push`); rendering the panel inside any widget (must be a sibling of the grid for layout reasons); placing the panel inside the DndContext (drag-and-drop should not interact with panel content).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>SelectionContext + DashboardSidePanel compile; panel renders different bodies per discriminated kind; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire setSelected into Recommendations + Outliers + heatmap labels + flow nodes; render inline severity badges</name>
  <files>app/(dashboard)/users/dashboard/DashboardClient.tsx,app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx,app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx,app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx,app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx</files>
  <action>
    1) `DashboardClient.tsx`:
       - Wrap subtree with `<SelectionProvider>` INSIDE `<FindingsProvider>` (so panel can read findings too).
       - Render `<DashboardSidePanel users={users} />` as a sibling of the grid (NOT inside DndContext). Place after the grid in JSX so it stacks correctly.

    2) `RecommendationsWidget.tsx`:
       - Replace the existing `onSelect` prop pattern with `useSelection()`.
       - Junk row onClick → `setSelected({ kind: "junk", finding: row })`.
       - Duplicate row onClick → `setSelected({ kind: "duplicate", finding: row })`.

    3) `OutlierCombosWidget.tsx`:
       - Outlier row onClick → `setSelected({ kind: "outlier", finding: row })`.

    4) `RolesModulesHeatmapWidget.tsx` — inline severity badges on Y-axis labels:
       - Use ECharts `yAxis.axisLabel.formatter` with rich-text labels (`yAxis.axisLabel.rich = { hi: { color: "#ef4444", ... }, med: { color: "#f59e0b" }, low: { color: "#6b7280" } }`).
       - Formatter returns a string like `{hi|●} BIM Coordinator` when severity is HIGH (use roleSeverityIndex from useFindings).
       - Add `onChartClick` (`onEvents` prop on `<ReactECharts>`): when a y-axis label or a cell is clicked, dispatch `setSelected({ kind: "role", role, severity: roleSeverityIndex.get(role) })`.

    5) `RoleRelationshipFlowWidget.tsx` — inline severity badges on nodes:
       - Define a custom node type via `nodeTypes` prop on `<ReactFlow>`. The custom node component reads severity from useFindings via prop (pass severity through node `data`).
       - Render a small colored dot or shadcn `Badge` next to the role name based on severity.
       - Edge `onClick` → `setSelected({ kind: "duplicate", finding: ... })` (look up finding by edge ids).
       - Node `onClick` → `setSelected({ kind: "role", role, severity })`.

    Avoid: prop-drilling setSelected (use context); duplicating roleSeverityIndex math (read from useFindings); putting onClick on the entire heatmap canvas (use ECharts onEvents).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Clicking any finding row OR a heatmap label/cell OR a flow node/edge opens the panel with the correct content; inline severity badges visible on heatmap y-axis and flow nodes; build passes; tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 4 UAT — verify all 13 ROADMAP success criteria end-to-end</name>
  <files>app/(dashboard)/users/dashboard/page.tsx</files>
  <action>
    Run > dashboard@0.1.0 build
> next build --webpack

▲ Next.js 16.2.5 (webpack)
- Environments: .env
- Experiments (use with caution):
  · optimizePackageImports
  · serverActions

  Creating an optimized production build ...
✓ Compiled successfully in 23.6s
  Running TypeScript ...
  Finished TypeScript in 15.3s ...
  Collecting page data using 23 workers ...
  Generating static pages using 23 workers (0/25) ...
  Generating static pages using 23 workers (6/25) 
  Generating static pages using 23 workers (12/25) 
  Generating static pages using 23 workers (18/25) 
✓ Generating static pages using 23 workers (25/25) in 898ms
  Finalizing page optimization ...
  Collecting build traces ...

Route (app)
┌ ○ /
├ ○ /_not-found
├ ƒ /account/setup
├ ƒ /api/ai/generate-description
├ ƒ /api/auth/[...nextauth]
├ ƒ /api/auth/callback/trello
├ ƒ /api/chat/media
├ ƒ /api/chat/stream
├ ƒ /api/chat/upload
├ ƒ /api/clash-updates
├ ƒ /api/connect/[provider]
├ ƒ /api/connect/trello
├ ƒ /api/debug/auth
├ ƒ /api/events/trello
├ ƒ /api/events/users
├ ƒ /api/gmail/attachment
├ ƒ /api/health
├ ƒ /api/lod-img/[fileId]
├ ƒ /api/sim-updates
├ ƒ /api/trpc/[trpc]
├ ƒ /api/uploadthing
├ ƒ /api/wiki-collab-token
├ ƒ /api/wiki-media
├ ƒ /api/wiki-media/[id]
├ ƒ /clash-detection
├ ƒ /exam
├ ƒ /families
├ ○ /forgot-password
├ ƒ /home
├ ƒ /lod-checker
├ ○ /login
├ ○ /register
├ ○ /reset-password
├ ƒ /settings/users
├ ƒ /sim-automation
├ ƒ /tasks
├ ƒ /trello
├ ƒ /unauthorized
└ ƒ /users


ƒ Proxy (Middleware)

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

Unknown command: "start"


Did you mean one of these?
  npm star # Mark your favorite packages
  npm stars # View packages marked as favorites
  npm start # Start a package
To see a list of supported npm commands, run:
  npm help (production build — Pitfall 1 surfaces here if Nivo SSR-imports). Then pause for human UAT verification per the steps under "how-to-verify" below. The user must confirm all 16 points pass before unblocking.
  </action>
  <what-built>
    Complete ACC Access Analysis Dashboard at /users/dashboard with:
    - 9 widgets in 2-col grid (Coverage donut, Active tiers, KPI strip full-width, Recommendations full-width, Roles×Modules heatmap full-width, Outlier combos, Role-relationship flow full-width, Recently-added, Admin-access)
    - Drag-reorder with localStorage persistence
    - Drill-down side panel for any finding
    - Inline severity badges on heatmap y-axis labels + flow nodes
    - Per-widget Download CSV (Recommendations CSV columns: Type, Severity, Roles, Members, Modules, SuggestedAction)
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/users/dashboard while logged in as a Workspace Google user.
    2. **DASH-13 (single-page 2-col grid):** All 9 widgets visible on one page at 1280px width — Coverage + Active tiers above the fold (default order). No tabs.
    3. **DASH-01 (Coverage donut):** Donut shows 3 segments — In both / In Workspace, missing ACC / In ACC, missing Workspace. Counts > 0 in at least the "In both" segment.
    4. **DASH-02 (Active tiers):** Stacked bar with 5 buckets summing to total members.
    5. **DASH-03 + DASH-09 (Junk roles):** Recommendations widget lists junk roles with HIGH/MEDIUM/LOW severity badges. Click one — side panel opens with affected members, modules, projects, suggested action, raw data.
    6. **DASH-04 (Duplicate roles):** Recommendations lists duplicate-role pairs (if any exist). Click one — side panel shows both roles + module union.
    7. **DASH-05 (Outlier combos):** Outlier widget lists module sets held by <5% of members (or empty state).
    8. **DASH-06 (Recently added):** Defaults to 30d. Toggle to 7d and 90d — list filters accordingly.
    9. **DASH-07 (Admin access):** Shows ONLY users with isAccountAdmin === true. Verify count differs from project-admin count.
    10. **DASH-08 (Heatmap):** Full-width, cells colored by member count, tooltip works, dataZoom works.
    11. **DASH-09 (inline badges):** Heatmap y-axis + flow nodes show severity badges.
    12. **DASH-10 (drill-down):** Side panel opens for junk/duplicate/outlier/role clicks; closing keeps URL stable.
    13. **DASH-11 (CSV per widget):** Each Download CSV downloads a non-empty file matching screen content.
    14. **DASH-13 (Recommendations CSV columns):** Header line MUST be exactly: Type,Severity,Roles,Members,Modules,SuggestedAction.
    15. **DASH-12 (drag-reorder):** Drag widget, reload, order persists.
    16. No console errors during navigation, drag, or finding clicks.

    If any point fails: describe each failure in the resume signal so /gsd:plan-phase 04 --gaps can address them.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Production build passes AND user has approved all 16 UAT points (or described failures for gap closure).</done>
  <resume-signal>Type "approved" if all 16 verification points pass, or describe each failure (e.g., "DASH-07 admin list still shows project admins; DASH-13 CSV header has wrong casing")</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes
- All 9 widgets render with real data in production build
- Side panel opens for all four finding kinds
- Inline severity badges visible on heatmap + flow
- Recommendations CSV header matches DASH-13 exactly
- Human checkpoint: all 16 UAT points approved
</verification>

<success_criteria>
- DASH-09 + DASH-10 + DASH-13 complete
- Phase 4 UAT human-verify checkpoint passed (all 13 ROADMAP success criteria)
- No remaining placeholders in WIDGETS registry
- Dashboard is end-to-end functional in production build
</success_criteria>

<output>
After completion, create `.planning/phases/04-access-analysis/04-08-SUMMARY.md` documenting:
- UAT outcome (pass/fail per success criterion)
- Any gap-closure items surfaced (if not all approved)
- Final architecture summary (FindingsProvider + SelectionProvider + 9 widgets)
- Performance observations on the 25k-node hub (memoization confirmed)
</output>
