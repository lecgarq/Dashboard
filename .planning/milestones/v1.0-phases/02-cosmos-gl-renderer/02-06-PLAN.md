---
phase: 02-cosmos-gl-renderer
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/graphRenderers.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [REND-02, REND-04]

must_haves:
  truths:
    - "No always-on [02-05-DEBUG] console.log lines remain in app/(dashboard)/users/graphRenderers.ts (gated debug logs in AccUsersGraph.tsx are allowed since they sit behind perfHudEnabled)"
    - "REQUIREMENTS.md traceability table is internally consistent with phase rollup — REND-02 status reflects the locked user decision (Pending/deferred to TD-006), REND-04 status reflects user acceptance (Complete with Firefox-WebGL2 note)"
    - "ROADMAP.md does not introduce new claims about REND-02 being met by the redesigned Separation+Cluster sliders — REND-02 is documented as deferred to TD-006"
  artifacts:
    - path: "app/(dashboard)/users/graphRenderers.ts"
      provides: "CosmosGraphRenderer with setSimulationConfig free of unconditional debug logging"
      contains: "setSimulationConfig"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table reconciled with verifier findings + user decisions"
      contains: "REND-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 plan list / requirements line consistent with REQUIREMENTS.md status"
      contains: "Phase 2: Cosmos.gl Renderer"
  key_links:
    - from: "REQUIREMENTS.md REND-02 row"
      to: ".gsd/TECHNICAL_DEBT.md TD-006"
      via: "explicit cross-reference text in the requirement bullet and/or traceability row"
      pattern: "TD-006"
    - from: "REQUIREMENTS.md REND-04 row"
      to: "user acceptance note (Firefox is WebGL2-compatible)"
      via: "inline note on the requirement bullet"
      pattern: "WebGL2"
---

<objective>
Close two non-blocking gaps surfaced by 02-VERIFICATION.md after user review of the human_needed checkpoint:

1. Remove always-on `[02-05-DEBUG]` `console.log` lines in `app/(dashboard)/users/graphRenderers.ts` at lines 771 and 774. They are wrapped in try/catch but NOT gated by any flag, so they fire on every `setSimulationConfig` call (every slider tick) in production. The peer logs in `AccUsersGraph.tsx` are gated behind `perfHudEnabled` and stay (user decision).

2. Reconcile `.planning/REQUIREMENTS.md` traceability table and `.planning/ROADMAP.md` so REND-02 / REND-04 status is internally consistent with the user's locked decisions:
   - REND-02 stays Pending (or equivalent "Deferred to TD-006") — the redesigned Separation+Cluster sliders DO NOT mark REND-02 met. Cross-reference TD-006 explicitly.
   - REND-04 stays Complete — accepted by user (Firefox is WebGL2-compatible, fallback path is unlikely to be exercised in production). Add a one-line acceptance note.

Purpose: Phase 2 is functionally complete; these are housekeeping items so the next phase (2.5) starts from a clean tree with no stray production logs and no documentation drift. Out of scope: TD-006 slider tuning, TD-007 Canvas2D removal, any code change beyond removing the two flagged log lines.

Output: graphRenderers.ts free of the two unconditional debug logs; REQUIREMENTS.md + ROADMAP.md reconciled with the verifier findings and user decisions.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cosmos-gl-renderer/02-VERIFICATION.md
@.planning/phases/02-cosmos-gl-renderer/02-05-SUMMARY.md
@.gsd/TECHNICAL_DEBT.md
@app/(dashboard)/users/graphRenderers.ts

<interfaces>
<!-- Exact code site for Task 1. -->
<!-- File: app/(dashboard)/users/graphRenderers.ts, lines ~768-776 (setSimulationConfig method). -->
<!-- Current code (verified by Read): -->

```ts
      this.graph.start?.(0.3);
      this.graph.render?.(0.3);
      // eslint-disable-next-line no-console
      try { console.log("[02-05-DEBUG] setSimulationConfig: applied", partial); } catch { /* ignore */ }
    } catch (err) {
      // eslint-disable-next-line no-console
      try { console.log("[02-05-DEBUG] setSimulationConfig: THREW", err); } catch { /* ignore */ }
    }
  }
```

The success-path log (line 771) and error-path log (line 774) are both unconditional. Remove both log statements (and their `// eslint-disable-next-line no-console` comments). Preserve the surrounding try/catch structure of `setSimulationConfig`. The error catch must still swallow, just without the log — leave the catch body empty with a `/* swallow — no-op */` comment so the linter is happy.

Do NOT touch the `[02-05-DEBUG]` logs in `app/(dashboard)/users/AccUsersGraph.tsx` — those are gated by `perfHudEnabled` and are intentional debug aids for ongoing GPU-physics tuning (user decision).
</interfaces>

<reconciliation_targets>
<!-- For Task 2. Verbatim quotes from current docs. -->

REQUIREMENTS.md line 20 (current):
```
- [ ] **REND-02**: User can adjust physics simulation parameters (repulsion, link spring, gravity) in real-time via sliders — bound to Cosmos.gl `setConfig()` live
```

REQUIREMENTS.md line 22 (current):
```
- [x] **REND-04**: User receives a working Canvas 2D graph when the browser does not support WebGL2 — existing `CanvasGraphRenderer` remains as fallback; dual-canvas pattern preserved
```

REQUIREMENTS.md traceability table (lines 81-84):
```
| REND-01 | Phase 2 | Complete |
| REND-02 | Phase 2 | Pending |
| REND-03 | Phase 2 | Complete |
| REND-04 | Phase 2 | Complete |
```

ROADMAP.md line 41:
```
**Requirements**: REND-01, REND-02, REND-03, REND-04
```

User-locked decisions (from verification checkpoint):
- REND-02: stays OPEN / not-met by redesigned sliders. Reconcile by referencing TD-006 in the requirement bullet AND/OR by changing "Pending" → "Deferred (TD-006)" in the traceability table — pick whichever reads cleanest and apply consistently across both locations.
- REND-04: accepted; add a short note that Firefox is WebGL2-compatible so the fallback is unlikely to be exercised in production. Status stays "Complete".
- Phase 2 is COMPLETE in ROADMAP.md (5/5 plans). Do NOT regress that to in-progress.
</reconciliation_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove always-on [02-05-DEBUG] logs from graphRenderers.ts setSimulationConfig</name>
  <files>app/(dashboard)/users/graphRenderers.ts</files>
  <action>
Open `app/(dashboard)/users/graphRenderers.ts`. Locate the `setSimulationConfig` method (around line 733). At lines 770-775 (use Grep on `[02-05-DEBUG]` inside this file to confirm exact current line numbers — line numbers may have drifted by one or two with surrounding edits) you will find:

```ts
      // eslint-disable-next-line no-console
      try { console.log("[02-05-DEBUG] setSimulationConfig: applied", partial); } catch { /* ignore */ }
    } catch (err) {
      // eslint-disable-next-line no-console
      try { console.log("[02-05-DEBUG] setSimulationConfig: THREW", err); } catch { /* ignore */ }
    }
```

Remove both `console.log` statements and the two `// eslint-disable-next-line no-console` comments above them. Result should look like:

```ts
      this.graph.start?.(0.3);
      this.graph.render?.(0.3);
    } catch {
      /* swallow — partial config rejected by graph; next applyer will retry */
    }
  }
```

Notes:
- The outer try-block's error parameter (`err`) is no longer referenced after removing the log, so change `catch (err)` to `catch` (or `catch (_err)` if tsconfig requires the binding) to keep TypeScript/ESLint happy.
- Do NOT introduce a `perfHudEnabled` gate here — the user decision is to delete these logs entirely from `graphRenderers.ts`. Gated peers in `AccUsersGraph.tsx` (lines 804, 815, 829, 846, 850, 1105, 1128, 1136 per the verifier) MUST be left untouched.
- After editing, verify no other unconditional `[02-05-DEBUG]` strings remain in `graphRenderers.ts` via Grep. If any new ones surfaced since verification, remove them too (same rule: anything in `graphRenderers.ts` not gated by `perfHudEnabled` must go).
- Do not touch `setConfigPartial` semantics, the `start(α)+render(α)` pairing, or any of the Three Cosmos API Trap mitigations from 02-05 — those are load-bearing.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard &amp;&amp; (grep -n "02-05-DEBUG" "app/(dashboard)/users/graphRenderers.ts" || echo "OK_NO_MATCHES") &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vitest run app/\(dashboard\)/users/cosmosUtils.test.ts</automated>
  </verify>
  <done>
    - `grep -n "02-05-DEBUG" app/(dashboard)/users/graphRenderers.ts` prints `OK_NO_MATCHES` (i.e. zero hits in that file).
    - `grep -n "02-05-DEBUG" "app/(dashboard)/users/AccUsersGraph.tsx"` still shows the gated peers (we did not break them).
    - `npx tsc --noEmit` passes for the project.
    - `npx vitest run app/(dashboard)/users/cosmosUtils.test.ts` passes (the existing 02-05 unit tests still pass — sanity check that nothing structural in the renderer file leaked into helpers).
    - No lint warnings about unused `err` binding in `setSimulationConfig`'s catch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md + ROADMAP.md so REND-02 and REND-04 status is consistent and reflects user decisions</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
The verifier flagged status drift between phase rollup ("Phase 2: Complete") and per-requirement status ("REND-02: Pending"). User decisions lock the resolution: REND-02 stays open and is explicitly tied to TD-006; REND-04 is accepted with a Firefox-WebGL2 note. Apply the following edits.

**Edit 1 — REQUIREMENTS.md REND-02 bullet (around line 20):**

Replace:
```
- [ ] **REND-02**: User can adjust physics simulation parameters (repulsion, link spring, gravity) in real-time via sliders — bound to Cosmos.gl `setConfig()` live
```

With:
```
- [ ] **REND-02**: User can adjust physics simulation parameters in real-time via sliders — Phase 2 Plan 02-05 redesigned the sliders to Separation + Cluster (driving Cosmos's `simulationRepulsion` / `simulationLinkSpring` / `simulationLinkDistance` / `simulationCluster` via `setConfigPartial`). The original three-slider model (repulsion / link spring / gravity) and the precise feel-tuning are deferred to **TD-006**; this requirement remains open until TD-006 is closed.
```

**Edit 2 — REQUIREMENTS.md REND-04 bullet (around line 22):**

Replace:
```
- [x] **REND-04**: User receives a working Canvas 2D graph when the browser does not support WebGL2 — existing `CanvasGraphRenderer` remains as fallback; dual-canvas pattern preserved
```

With:
```
- [x] **REND-04**: User receives a working Canvas 2D graph when the browser does not support WebGL2 — existing `CanvasGraphRenderer` remains as fallback; dual-canvas pattern preserved. **Note (2026-04-29):** User accepted the fallback as code-only without live-browser exercise — Firefox is WebGL2-compatible, so the fallback path is unlikely to be exercised in production. See also TD-007 (vestigial Canvas2D branch) for eventual removal.
```

**Edit 3 — REQUIREMENTS.md traceability table (line 82):**

Replace:
```
| REND-02 | Phase 2 | Pending |
```

With:
```
| REND-02 | Phase 2 | Deferred (TD-006) |
```

(Leave REND-04 row as `Complete`. Leave REND-01 and REND-03 unchanged.)

**Edit 4 — ROADMAP.md Phase 2 Requirements line (line 41):**

Replace:
```
**Requirements**: REND-01, REND-02, REND-03, REND-04
```

With:
```
**Requirements**: REND-01, REND-02 (deferred to TD-006), REND-03, REND-04
```

(Leave the `- [x] **Phase 2: Cosmos.gl Renderer**` rollup checkbox CHECKED — Phase 2 is complete in the sense that all five plans shipped and the user signed off; REND-02's UX-feel followup is tracked as separate debt, not a phase blocker. Leave the per-plan list under "Phase 2: Cosmos.gl Renderer" untouched — those plans really did ship.)

**Edit 5 — ROADMAP.md Phase 2 progress table (line 96, the "| 2. Cosmos.gl Renderer | 5/5 | Complete | 2026-04-29 |" row):**

Leave as-is. Phase rollup is correct — no edit needed.

Notes:
- Use the Read tool first to confirm the exact current line numbers for each edit (small drift is possible). Use the Edit tool for the changes; do NOT rewrite either file from scratch.
- Do NOT touch other requirements (FOUND-*, DATA-*, FILT-*, UI-*, ANAL-*, CLUS-*, SCALE-*).
- Do NOT touch out-of-scope-table or v1.x-deferred sections.
- Do NOT add a new "Open Issues" section anywhere — TD-006 and TD-007 are already in `.gsd/TECHNICAL_DEBT.md`.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard &amp;&amp; grep -n "TD-006" .planning/REQUIREMENTS.md &amp;&amp; grep -n "Deferred (TD-006)" .planning/REQUIREMENTS.md &amp;&amp; grep -n "WebGL2" .planning/REQUIREMENTS.md &amp;&amp; grep -n "REND-02 (deferred to TD-006)" .planning/ROADMAP.md &amp;&amp; grep -n "Phase 2: Cosmos.gl Renderer" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md REND-02 bullet references "TD-006" and no longer claims `setConfig()` (uses `setConfigPartial`).
    - REQUIREMENTS.md REND-04 bullet contains the user-acceptance note (Firefox WebGL2 + TD-007 reference).
    - REQUIREMENTS.md traceability row for REND-02 reads `Deferred (TD-006)` (not `Pending`).
    - ROADMAP.md Phase 2 `**Requirements**:` line annotates REND-02 with `(deferred to TD-006)`.
    - Phase 2 rollup `[x]` checkbox and `5/5 Complete` progress-table row are unchanged.
    - No edits to FOUND-*, DATA-*, FILT-*, UI-*, ANAL-*, CLUS-*, SCALE-*, out-of-scope, or v1.x sections.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. `grep -nR "[02-05-DEBUG]" "app/(dashboard)/users/graphRenderers.ts"` returns no matches.
2. `grep -nR "[02-05-DEBUG]" "app/(dashboard)/users/AccUsersGraph.tsx"` still returns its existing gated lines (sanity check that we did not over-remove).
3. `npx tsc --noEmit` passes.
4. `npx vitest run app/(dashboard)/users/cosmosUtils.test.ts` passes (existing 02-05 unit tests still green).
5. REQUIREMENTS.md REND-02 row reads `Deferred (TD-006)` and the bullet text references TD-006.
6. REQUIREMENTS.md REND-04 bullet contains the Firefox-WebGL2 acceptance note.
7. ROADMAP.md Phase 2 Requirements line annotates REND-02 with `(deferred to TD-006)`.
8. Phase 2 rollup checkbox and progress-table row are still `[x]` / `5/5 Complete` — we did not regress phase status.
9. No content was changed outside the four flagged locations.
</verification>

<success_criteria>
- Production code: zero unconditional `[02-05-DEBUG]` console.log statements in `graphRenderers.ts`. Gated debug logs in `AccUsersGraph.tsx` retained per user decision.
- Documentation: REQUIREMENTS.md and ROADMAP.md present a consistent picture — REND-02 explicitly deferred to TD-006, REND-04 explicitly accepted with the Firefox-WebGL2 note. No status drift between phase rollup and requirement-level rows. TD-006 and TD-007 remain the only system-of-record for the open follow-ups; this plan does not duplicate them.
- Phase 2 sign-off integrity preserved: `[x] Phase 2: Cosmos.gl Renderer` and `5/5 Complete` are unchanged.
- TypeScript and the existing 02-05 vitest suite still pass — no regression to the live renderer or to lasso, FPS HUD, or 02-05 sim tuning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cosmos-gl-renderer/02-06-SUMMARY.md` documenting:
- Two log lines removed from `graphRenderers.ts:setSimulationConfig` (commit hash).
- REQUIREMENTS.md + ROADMAP.md reconciliation diff (commit hash).
- Confirmation that TD-006 (slider feel) and TD-007 (Canvas2D removal) remain open and were not touched.
- Confirmation that Phase 2 rollup status is unchanged.
- Closes the housekeeping items from 02-VERIFICATION.md (anti-patterns table + status-drift warning); does NOT close TD-006 or TD-007.
</output>
