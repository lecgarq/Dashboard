---
phase: 29-embedding-v2-feature-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/access-analysis/instanceFeatureNumerics.ts
  - app/(dashboard)/users/access-analysis/instanceFeatureNumerics.test.ts
  - app/(dashboard)/users/access-analysis/instanceFeatureTokens.ts
  - app/(dashboard)/users/access-analysis/instanceFeatureTokens.test.ts
  - scripts/build-instance-features.ts
autonomous: true
requirements: [EMB-01, EMB-02]

must_haves:
  truths:
    - "`.embedding/instance-features.jsonl` lines carry `{nodeId, tokens[], numerics{}}` — the six dropped numerics (folderBreadth, accessibleDataBytes, activityTotal, membershipAgeDays, permissionStrength, riskScore) exported RAW, python owns normalization"
    - "Missing numerics are explicit `null`, never silently 0 (membershipAgeDays undefined/null → null; permissionStrength non-number → null)"
    - "`permissionCoverage` enters the categorical token set as `cov:<known|partial|unknown>`"
    - "Every included AND deliberately-excluded dimension is listed with a one-line rationale in an in-code comment block (project identity stays excluded per D5; activityMix/actionCounts deferred)"
    - "A TS unit test pins the numeric extractor's null handling and raw-value passthrough"
  artifacts:
    - "app/(dashboard)/users/access-analysis/instanceFeatureNumerics.ts"
    - "app/(dashboard)/users/access-analysis/instanceFeatureNumerics.test.ts"
  key_links:
    - from: "scripts/build-instance-features.ts"
      to: "app/(dashboard)/users/access-analysis/instanceFeatureNumerics.ts"
      via: "import + per-node emission into the jsonl line"
      pattern: "instanceFeatureNumerics"
    - from: "app/(dashboard)/users/access-analysis/instanceFeatureTokens.ts"
      to: "NodeFeatureSnapshot.permissionCoverage"
      via: "new categorical token"
      pattern: "cov:"
---

<objective>
EMB-01/EMB-02 (TS half): the feature builder exports the full hybrid vector raw material.
Today `scripts/build-instance-features.ts` writes `{nodeId, tokens[]}` only — all numeric
magnitude is discarded before python ever sees it. After this plan the jsonl line is
`{nodeId, tokens[], numerics{}}` (additive shape), with the six dropped numerics exported
raw and `permissionCoverage` joining as a categorical token. Python normalization is
Plan 29-02; this plan only guarantees the raw material and its null semantics.
</objective>

<context>
Required reading before executing:

- @.planning/phases/29-embedding-v2-feature-fidelity/29-CONTEXT.md (decisions §2, defaults)
- @app/(dashboard)/users/access-analysis/instanceFeatureTokens.ts
- @app/(dashboard)/users/access-analysis/instanceFeatureTokens.test.ts
- @app/(dashboard)/users/access-analysis/interactionTypes.ts (NodeFeatureSnapshot lines 40–120)
- @scripts/build-instance-features.ts
</context>

<tasks>
1. **Create `app/(dashboard)/users/access-analysis/instanceFeatureNumerics.ts`.**
   Export `InstanceFeatureNumerics` (all fields `number | null`) and
   `instanceFeatureNumerics(f: NodeFeatureSnapshot): InstanceFeatureNumerics`:
   - `folderBreadth`: `f.permissionTypeSummary?.folderBreadth ?? null`
   - `accessibleDataBytes`: `f.accessibleDataBytes ?? null`
   - `activityTotal`: `f.activityTotal ?? null`
   - `membershipAgeDays`: `f.membershipAgeDays ?? null` (field is nullable-optional)
   - `permissionStrength`: `typeof f.permissionStrength === "number" ? f.permissionStrength : null`
   - `riskScore`: `f.riskScore ?? null`

   Top-of-file comment block = the EMB-01 rationale table: every included dim
   (categorical tokens + the six numerics + `cov:` token) and every excluded dim
   (project identity — D5 spec; `signinBucket` — redundant with `recency:`;
   `activityMix`/`actionCounts` — deferred, aggregate `activityTotal` + recency carry the
   magnitude signal; `membershipBucket` — superseded by raw `membershipAgeDays`;
   `moduleFlags` — redundant with `mod:` multi-hot; `riskFlags` booleans — aggregated in
   `riskScore`), one line each.

2. **Add the coverage token.** In `instanceFeatureTokens.ts` add
   `out.add(\`cov:${f.permissionCoverage}\`)`. Extend `instanceFeatureTokens.test.ts`
   with an assertion for `cov:known|partial|unknown`.

3. **Wire the exporter.** In `scripts/build-instance-features.ts` change the line shape to
   `JSON.stringify({ nodeId, tokens: instanceFeatureTokens(f), numerics: instanceFeatureNumerics(f) })`.
   No other script changes.

4. **Write `instanceFeatureNumerics.test.ts`** pinning: (a) full snapshot → raw values
   pass through untouched; (b) empty/minimal snapshot → every field `null`, never 0;
   (c) `permissionStrength: 0` and `accessibleDataBytes: 0` survive as `0` (falsy-zero
   trap — `?? null` keeps 0, test proves it).

5. **Gates.**
   - `npx vitest run "app/(dashboard)/users/access-analysis/instanceFeatureNumerics.test.ts" "app/(dashboard)/users/access-analysis/instanceFeatureTokens.test.ts"`
   - `npx tsc --noEmit`

6. **Commit** by explicit path (the 5 files above only); inspect
   `git diff --cached --name-only` first. Message style: `feat(29-01): ...`.
</tasks>

<verification>
- Both focused vitest files green (exact output quoted in SUMMARY).
- `npx tsc --noEmit` clean.
- No repo-map gate needed: no router/Prisma/import-architecture change (script + sibling
  module in an existing directory).
</verification>

<success_criteria>
Mirrors the truths: jsonl line shape `{nodeId, tokens, numerics}`, explicit-null missing
semantics unit-pinned, `cov:` token live, rationale table present in-code.
</success_criteria>
