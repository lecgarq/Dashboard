---
phase: 29-embedding-v2-feature-fidelity
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified: []
autonomous: true
requirements: [EMB-04, EMB-05, EMB-06]

must_haves:
  truths:
    - "The full pipeline ran against the live DB: build-instance-features.ts exported ~22k rows, compute_instance_embeddings.py passed its own quality gate and upserted with a recorded embeddingRunId"
    - "Duplicate-profile rate before (~87% baseline definition) and after, trustworthiness old-vs-new, and pipeline runtime are captured verbatim from the run output"
    - "The live graph (/users/spatial-graph on :3000) renders the new coordinates with no (0,0)-fallback console warnings"
    - "v2.4 Group-by/slider anchor morphs still function on the new embedding baseline (EMB-06 smoke check) and the PERF-02 frozen-handle invariant (GraphCanvas.test.ts) is green"
  artifacts:
    - ".embedding/instance-features.jsonl (regenerated, not committed)"
    - "run output captured for 29-VERIFICATION.md"
  key_links:
    - from: "AccInstanceEmbedding (new run)"
      to: "AccessAnalysisShell.tsx:680-692 static layer join"
      via: "accDcGraph.instanceEmbedding procedure (untouched)"
      pattern: "instanceEmbedding"
---

<objective>
EMB-06 (+ EMB-04/05 recorded evidence): land the new embedding on the live graph. This
plan changes no source files — it runs the rebuilt pipeline against the live DB, captures
the gate metrics as phase evidence, and smoke-checks the live surface. `staticLayer.ts`
anchors lerp FROM the embedding baseline, so a map that breaks Group-by/slider morphs is
a phase failure even with passing projection metrics (CONTEXT specifics).
</objective>

<context>
- @.planning/phases/29-embedding-v2-feature-fidelity/29-CONTEXT.md (verification §)
- @.planning/STATE.md "v2.5 grounding facts" (consumption + anchor paths)
- app/(dashboard)/users/access-analysis/staticLayer.ts (read-only reference)
</context>

<tasks>
1. **Export features.** `npx tsx scripts/build-instance-features.ts` — expect ~22,279
   rows written to `.embedding/instance-features.jsonl`. Record the exact count.

2. **Run the projection.** `python scripts/compute_instance_embeddings.py` — capture the
   FULL stdout: node/unique counts, duplicate rates old/new definition, trustworthiness
   old/new, gate PASS, runtime, `embeddingRunId`. Gate FAIL → stop the phase, report
   (do not tune-and-retry without recording each attempt).

3. **DB spot check.** Read-only: count rows carrying the new `embeddingRunId`; sample 3
   rows for finite x/y within ±1000 and non-empty neighbors Json.

4. **PERF-02 invariant + focused tests.**
   `npx vitest run "GraphCanvas.test"` (frozen-handle invariant) plus any staticLayer
   test file if present. Must be green; quote the tail.

5. **Live graph smoke (EMB-06).** No rebuild needed — coordinates are data; `:3000`'s
   running build reads them through the untouched procedure. In the browser on
   `http://localhost:3000/users/spatial-graph`:
   - map renders the NEW layout (tight islands, no single mega-blob);
   - console: zero "missing embedding" / (0,0)-fallback warnings;
   - anchor-morph smoke: pick a Group-by dimension, drag its slider — nodes morph from
     the new baseline and settle back on release; cluster color mode still coherent.
   Screenshot for the phase record.

6. **No commit** (no source diff). `.embedding/` output stays untracked as today.
</tasks>

<verification>
- All numbers/quotes captured verbatim for `29-VERIFICATION.md` (written at phase
  completion, not here).
- Regressions vs pre-existing failures separated: the 14 acc-dc-graph e2e drift failures
  and 3 usePredicateEngine unit failures are PRE-EXISTING (STATE Deferred Items) — do not
  re-attribute them to this phase.
</verification>

<success_criteria>
Mirrors the truths: live-DB run with recorded runId + gate metrics, live graph renders
new coords cleanly, anchor morphs + PERF-02 green.
</success_criteria>
