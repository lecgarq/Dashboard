---
phase: 34
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/access-analysis/usePredicateEngine.ts
  - app/(dashboard)/users/access-analysis/__tests__/usePredicateEngine.test.tsx
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "`npm test` is fully green — zero carried failures"
    - "the code-vs-test verdict (aperture filter broken in live UI vs tests mis-encoding intent) is evidence-backed and recorded in the plan summary"
    - "if code is fixed, the fix sits at the narrowest shared boundary (resolver-building/predicate path), not per-caller"
    - "no consolidation of the three duplicate usePredicateEngine test files (CONTEXT deferred item)"
  artifacts:
    - ".planning/phases/34-test-guard-health-sweep/34-02-SUMMARY.md (verdict + rationale)"
  key_links:
    - pattern: "buildApertureValueResolvers"
      in: "app/(dashboard)/users/access-analysis/usePredicateEngine.ts"
---

# Plan 34-02: TEST-04 — resolve the 3 usePredicateEngine aperture failures

## Objective

3 failures in `__tests__/usePredicateEngine.test.tsx` ("Phase 25 aperture filter"
describe): reproduced live 2026-07-20 — `featureValueForDim(f,"riskScore",resolvers)`
returns `""` (expected `"High"`), and both mask-predicate tests get `0.15` for
nodes that should match. Root-cause candidate FOUND during planning:
`groupByDimensions()` (groupByDimensions.ts) was narrowed to a curated
`PRIMARY_GROUP_OPTIONS` list on this redesign branch — `riskScore` is not in the
list, so `buildApertureValueResolvers` never builds its resolver and the legacy
switch falls through to `""`. (`company` IS curated, which is why the
visible-subset test still passes.) NOTE: CONTEXT said the resolver test passed;
live tree says it now fails too — trivial staleness, recorded here, scope
unchanged.

Decision 3 pre-authorizes the smallest root-cause code fix if the live aperture
filter is broken; if the tests mis-encode current intended behavior, correct the
tests instead. No mid-phase owner checkpoint.

## Context

@.planning/phases/34-test-guard-health-sweep/34-CONTEXT.md (TEST-04 evidence, Decision 3)
@app/(dashboard)/users/access-analysis/usePredicateEngine.ts
@app/(dashboard)/users/access-analysis/groupByDimensions.ts
@app/(dashboard)/users/access-analysis/__tests__/usePredicateEngine.test.tsx

## Tasks

1. **Verdict investigation (decides code vs test).** Trace where the live filter
   aperture gets its dimension ids: `Toolbar.tsx` (builds resolvers at line 92)
   and `AccessAnalysisShell.tsx` (line 406) — find the filter-chip source
   (aperture panel / FilterChips) and answer: can `riskScore` (or any
   non-curated banded dim) still appear as an `activeFilters` key at runtime?
   - **YES → live bug (Decision 3 path):** a user can select a riskScore filter
     whose values never match → every node dims. Fix at the shared boundary:
     `buildApertureValueResolvers` must iterate the set of filterable banded
     dims (the same set the aperture UI offers), not the narrower curated
     Group-into list. Smallest fix: build resolvers from the filterable-dim
     source the aperture actually uses (reuse the existing helper that
     enumerates aperture dims; do NOT invent a new registry).
   - **NO → tests stale:** the redesign deliberately narrowed filterable dims to
     the curated list; `riskScore` can no longer be filtered. Correct the tests
     to a curated banded dim (e.g. `activityVolume` band or `company`) so they
     still pin the banded-resolver contract, and note the intent change.
2. Implement the chosen fix. Record verdict + repro evidence + rationale in
   `34-02-SUMMARY.md` (REQUIREMENTS TEST-04 wording — no new doc).
3. **Gates.** `npx vitest run` over the three predicate test files (root
   `usePredicateEngine.test.ts`, `__tests__/usePredicateEngine.test.ts`,
   `__tests__/usePredicateEngine.test.tsx`) — all green. Then full `npm test` —
   fully green, zero carried failures (this is the TEST-04 acceptance). Then
   `npx tsc --noEmit`. If a genuinely pre-existing failure UNRELATED to
   TEST-04 surfaces in `npm test`, report it separately — do not silently absorb
   or ignore it; TEST-04's "fully green" claim must reflect the real run.
4. Commit explicit paths only (the touched source/test files):
   `fix(access-analysis): restore banded aperture resolvers (TEST-04)` or
   `test(access-analysis): re-pin aperture tests to curated dims (TEST-04)` per
   verdict.

## Verification gates

- `npx vitest run` on the three predicate files — 0 fails.
- `npm test` — fully green (exact tail of the run recorded in summary).
- `npx tsc --noEmit` — clean.

## Success criteria

Mirrors must_haves: npm test fully green; verdict + rationale in summary; fix at
shared boundary; duplicate test files left unconsolidated.
