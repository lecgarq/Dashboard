---
phase: 34
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - tests/e2e/acc-dc-graph.spec.ts
  - tests/e2e/acc-3d-lasso.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02]
must_haves:
  truths:
    - "acc-dc-graph.spec.ts passes green on an isolated :3100 production build with EXPECTED_NODE_COUNT pinned exactly at 22_279"
    - "dead selectors ('User name thumb', removed physics-shell sidebar testids) are repointed to the real v2.4+ surface (group-by-select / 'Grouping strength') or deleted with each deletion named in the summary"
    - "3D-flag-gated blocks stay gated (test.skip(!ACC_3D_GRAPH)) — not deleted, not force-enabled"
    - "acc-3d-lasso.spec.ts passes on consecutive recorded flag-on runs without tripping its timeout budget; the lever is chosen from reproduced evidence"
  artifacts:
    - "tests/e2e/acc-dc-graph.spec.ts (re-baselined)"
    - "tests/e2e/acc-3d-lasso.spec.ts (budget lever)"
  key_links:
    - pattern: "22_279"
      in: "tests/e2e/acc-dc-graph.spec.ts"
    - pattern: "group-by-select|Grouping strength"
      in: "tests/e2e/acc-dc-graph.spec.ts"
---

# Plan 34-04: E2E-01/02 — re-baseline the graph e2e suite + close the lasso budget flake

## Objective

Make `acc-dc-graph.spec.ts` able to gate Phases 35–36: bump the node-count pin
16_942 → 22_279 (Decision 1: exact assert, comment stays), replace dead
curated-slider/physics-shell selectors with the real v2.4+ surface, and close the
lasso spec's timeout flake with an evidence-chosen lever (CONCERNS §3.4).

Depends on 34-01: the isolated build is invoked via powershell from bash; the
guard must already allow isolatedDist-wrapped builds (34-01's unit test covers
exactly that form).

## Context

@.planning/phases/34-test-guard-health-sweep/34-CONTEXT.md (E2E-01/02 evidence + defaults)
@tests/e2e/acc-dc-graph.spec.ts (1,167 lines — pin at :17, dead selectors at 236/771/775)
@tests/e2e/acc-3d-lasso.spec.ts (setTimeout(360_000) at :54; inner 120s waitForFunction at :23–25)
@playwright.config.ts (timeout 120_000; webServer = next dev --webpack on :3100, NEXT_DIST_DIR=.next-e2e)
@.claude/skills/lecg-dashboard/references/deploy-sequence.md (isolated-build mechanics)

## Tasks

1. **Fresh baseline run (VERIFY the fail list — the "14 fails" figure is from
   2026-07-14; v2.5 shipped since).** Establish the harness exactly as prior
   phases did (memory: 2026-07-14 dep-update ran the suite on an isolated :3100
   prod build; the checked-in config starts its own `:3100` server with
   `NEXT_DIST_DIR=.next-e2e`). Follow whichever form
   `deploy-sequence.md`/prior VERIFICATION artifacts record as the established
   e2e invocation; build isolated via PowerShell single-quoted from bash. Run
   `npm run test:e2e -- tests/e2e/acc-dc-graph.spec.ts` and record the actual
   fail list before editing anything.
2. **Re-baseline (E2E-01):**
   - `EXPECTED_NODE_COUNT = 22_279` (comment "bump this if the dataset changes"
     stays; no tolerance band, no live-DB derivation).
   - Repoint each dead selector to the current equivalent surface:
     `page.getByLabel("User name thumb")` (lines 236, 771, 775) → the
     `group-by-select` testid / "Grouping strength" label surface
     (`GroupByControls.tsx`); adapt the assertions to what the new surface
     actually promises. Delete a test ONLY when the covered feature no longer
     exists in any form — name every deletion in the summary.
   - Leave `test.skip(!ACC_3D_GRAPH)` blocks gated.
   - Iterate runs until 0 drift failures remain on the default-flag path.
3. **Lasso flake (E2E-02):** reproduce with the flag on
   (`NEXT_PUBLIC_ACC_3D_GRAPH=1`) and identify WHICH budget trips: the spec
   already carries `test.setTimeout(360_000)`, so the suspect is the inner
   `waitForFunction` 120_000 at `gotoGraph` (line 23–25) or the config-level
   webServer/global limits. Choose the lever the evidence supports (scoped
   inner-timeout raise / `test.slow()` / reduced `NEXT_PUBLIC_ACC_GRAPH_TEST`
   fixture — CONCERNS §3.4 options). Smallest change that closes the observed
   trip point; no blanket global-timeout raise for all specs.
4. **Consecutive recorded passes (SC #2):** run the lasso spec flag-on at least
   twice consecutively; record both results. Run the full default-flag
   `acc-dc-graph.spec.ts` suite once more green after all edits.
5. Commit both spec files by explicit path:
   `test(e2e): re-baseline graph suite to 22,279-node v2.4+ surface; close lasso budget (E2E-01/02)`.
6. Clean up: kill the :3100 server; leave `:3000` untouched throughout.

## Verification gates

- `acc-dc-graph.spec.ts` full run green on the isolated :3100 harness (output
  recorded — pass/fail counts).
- `acc-3d-lasso.spec.ts` two consecutive flag-on passes (outputs recorded).
- `npx tsc --noEmit` clean (spec edits are TS).
- No changes outside the two spec files (playwright.config.ts only if the
  chosen E2E-02 lever demands it — record the rationale if so).

## Success criteria

Mirrors must_haves: exact 22_279 pin, real-surface selectors, gated 3D blocks
intact, evidence-chosen lasso lever with consecutive recorded passes.
