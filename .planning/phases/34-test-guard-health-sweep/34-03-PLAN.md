---
phase: 34
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/compute_instance_embeddings.py
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "after a gate-passing run, AccInstanceEmbedding rows whose nodeId is absent from the current snapshot set are deleted (983 stale → 0), verified on a REAL run"
    - "the prune count is reported in the run output alongside the upsert count"
    - "a trustworthiness-gate failure aborts before ANY write, including the prune"
    - "delete-only — no schema change, no soft-delete column, no new dependency"
  artifacts:
    - "scripts/compute_instance_embeddings.py (prune in the post-gate write path)"
  key_links:
    - pattern: "Pruned"
      in: "scripts/compute_instance_embeddings.py"
    - pattern: "DELETE FROM \"AccInstanceEmbedding\""
      in: "scripts/compute_instance_embeddings.py"
---

# Plan 34-03: PIPE-02 — embedding pipeline prunes stale rows

## Objective

`scripts/compute_instance_embeddings.py` upserts but never deletes: 983 stale
old-shape rows sit in `AccInstanceEmbedding` (figure from the milestone-open
audit — re-verify live). Add a delete-only prune of rows whose nodeId is absent
from the current run's `node_ids`, strictly AFTER the EMB-05 gate-pass point
(gate fail at lines 367–384 `sys.exit(1)`s before `_upsert` at 398 — the prune
must sit behind the same gate).

## Context

@.planning/phases/34-test-guard-health-sweep/34-CONTEXT.md (PIPE-02 evidence)
@scripts/compute_instance_embeddings.py (lines 300–405: gate, `_upsert`, run summary)

## Tasks

1. **VERIFY the stale-row figure live** before touching code: count rows whose
   `nodeId` is not in the current snapshot set (read `.embedding/instance-features.jsonl`
   node ids — same file `main()` loads — and run a read-only count query, e.g. a
   short psycopg snippet or `npx prisma db execute`-equivalent read). Record the
   actual before-count (expected ≈983). If `.embedding/instance-features.jsonl`
   is missing, first regenerate it with the established
   `build-instance-features.ts` step (read-only extract; check its invocation in
   scripts/package.json before running).
2. **Implement the prune** inside the existing post-gate write path: extend
   `_upsert` (or add `_prune(node_ids)` called immediately after `_upsert` in
   `main()` — same transaction scope not required; keep it simplest) with
   `DELETE FROM "AccInstanceEmbedding" WHERE NOT ("nodeId" = ANY(%s))`, passing
   `list(node_ids)` (~22k ids — one statement, no chunking needed for text[]
   ANY). Capture `cur.rowcount` as the prune count. Print in the existing run
   summary style: `Upserted N ... / Pruned M stale`. Nothing before the
   gate-pass point writes; gate fail still exits before both upsert and prune.
3. **Real run (SC #5 — dry-run does not satisfy).** Run the script end-to-end
   with the project's established Python invocation (check how prior phases ran
   it — venv/python version — before invoking). Record: gate PASS line, upsert
   count, prune count (expected first-run prune = the verified before-count),
   wall time.
4. **Verify after-state:** stale-row count query now returns 0. Record
   before→after in the summary.
5. Commit `scripts/compute_instance_embeddings.py` only:
   `feat(pipeline): prune stale AccInstanceEmbedding rows post-gate (PIPE-02)`.

## Verification gates

- Read-only stale count before (≈983) and after (0) — both recorded.
- Real run output shows `GATE PASS` (or first-run skip line), upsert count, and
  the prune count in the same summary print.
- No schema diff (`prisma/schema.prisma` untouched), no new imports beyond the
  already-used psycopg.

## Success criteria

Mirrors must_haves: prune after gate only, count reported, real-run 983→0
evidence, delete-only.
