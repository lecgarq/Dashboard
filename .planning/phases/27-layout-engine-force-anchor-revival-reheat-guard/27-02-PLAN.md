---
phase: 27-layout-engine-force-anchor-revival-reheat-guard
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - "app/(dashboard)/users/access-analysis/physicsLayer.ts"
  - "app/(dashboard)/users/access-analysis/staticLayer.ts"
  - "app/(dashboard)/users/access-analysis/staticLayer.test.ts"
  - "app/(dashboard)/users/access-analysis/clusterTransitionLayer.ts"
  - "app/(dashboard)/users/access-analysis/clusterTransitionLayer.test.ts"
  - "app/(dashboard)/users/access-analysis/GraphCanvas.tsx"
  - "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"
  - "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
  - "app/(dashboard)/users/access-analysis/__tests__/embeddingMapSeam.test.tsx"
autonomous: true
requirements: [LAY-01, LAY-02, LAY-03, LAY-04]
must_haves:
  truths:
    - "All-zero layout strengths return the exact current similarity embedding."
    - "The strongest non-zero catalog dimension alone drives the anchor field, with catalog order resolving ties."
    - "The live default renderer consumes catalogTargets and catalogWeights while Cosmos remains frozen."
    - "Anchor layouts retain an organic similarity residual and never use the grid descriptor for multiple active dimensions."
    - "Retargeting starts from the visible buffer, settles toward the new target in the owner-approved 600ms window, and reduced motion settles immediately."
  artifacts:
    - path: "app/(dashboard)/users/access-analysis/staticLayer.ts"
      provides: "Allocation-stable similarity-to-catalog-anchor target projection and lazy target registration seam"
      exports: ["createStaticLayer", "catalogAnchorTarget"]
    - path: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      provides: "Live default render callback over catalog targets/weights and strongest-dimension selection"
      contains: "catalogAnchorTarget"
    - path: "app/(dashboard)/users/access-analysis/clusterTransitionLayer.ts"
      provides: "Continuous retargetable 600ms morph primitive"
      contains: "durationMs"
  key_links:
    - from: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      to: "app/(dashboard)/users/access-analysis/staticLayer.ts"
      via: "createStaticLayer receives initial targets/weights; layoutTarget projects them each frame"
      pattern: "createStaticLayer\(nodeIds, xy, targets, dimWeights\)"
    - from: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      to: "app/(dashboard)/users/access-analysis/activeGrouping.ts"
      via: "activeGroupingDimension selects one strongest target before catalogAnchorTarget runs"
      pattern: "activeGroupingDimension"
    - from: "app/(dashboard)/users/access-analysis/GraphCanvas.tsx"
      to: "app/(dashboard)/users/access-analysis/clusterTransitionLayer.ts"
      via: "Frozen 2D renderer eases the current visible positions toward the live anchor target"
      pattern: "durationMs: 600"
---

<objective>
Turn the already-built structural catalog target/weight arrays into the authoritative default
2D layout target. Keep the verified embedding as the exact zero state, preserve its local
similarity texture inside dimension anchors, and let the existing frozen Cosmos position pump
perform one continuous retargetable morph.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/27-layout-engine-force-anchor-revival-reheat-guard/27-CONTEXT.md
@.planning/phases/27-layout-engine-force-anchor-revival-reheat-guard/27-01-SUMMARY.md
@app/(dashboard)/users/access-analysis/activeGrouping.ts
@app/(dashboard)/users/access-analysis/catalogTargets.ts
@app/(dashboard)/users/access-analysis/catalogWeights.ts
@app/(dashboard)/users/access-analysis/staticLayer.ts
@app/(dashboard)/users/access-analysis/clusterTransitionLayer.ts
@app/(dashboard)/users/access-analysis/GraphCanvas.tsx
@app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
@app/(dashboard)/users/access-analysis/layoutDescriptor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the strongest-wins organic anchor projection</name>
  <files>app/(dashboard)/users/access-analysis/physicsLayer.ts, app/(dashboard)/users/access-analysis/staticLayer.test.ts, app/(dashboard)/users/access-analysis/staticLayer.ts</files>
  <action>
    Write failing tests proving: no active value returns the baseline byte-for-byte; one
    dimension moves toward its target; the strongest of two wins; equal strengths use the
    supplied catalog order; per-node dimWeights gate the blend; and the full-strength result
    retains a small fixed fraction of the baseline as organic within-anchor texture rather
    than collapsing every member onto one point. Implement this allocation-stably in
    staticLayer using activeGroupingDimension and the existing smooth progress curve. Extend
    createStaticLayer to retain supplied targets/weights and expose one optional registration
    method that mutates those same maps for Phase 27's lazy catalog. Do not import gridLayout,
    create a second engine, or touch d3/Cosmos.
  </action>
  <verify><automated>npx vitest run "app/(dashboard)/users/access-analysis/staticLayer.test.ts" "app/(dashboard)/users/access-analysis/activeGrouping.test.ts"</automated></verify>
  <done>The pure target projector proves General, strongest-wins, weights, deterministic ties, and organic no-grid output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tune the existing transition seam to 600ms and reduced motion</name>
  <files>app/(dashboard)/users/access-analysis/clusterTransitionLayer.test.ts, app/(dashboard)/users/access-analysis/clusterTransitionLayer.ts, app/(dashboard)/users/access-analysis/GraphCanvas.test.ts, app/(dashboard)/users/access-analysis/GraphCanvas.tsx</files>
  <action>
    Add failing pure tests for a duration-configured exponential morph: it moves on the first
    frame, is materially settled by 600ms without overshoot, and a mid-flight target reversal
    continues from the current displayed buffer. Parameterize the existing transition layer
    instead of adding another animator, and have GraphCanvas request 600ms for layout changes.
    Add native prefers-reduced-motion detection in GraphCanvas and bypass easing for an
    immediate target; keep the frozen Cosmos simulation contract and existing rAF pump.
  </action>
  <verify><automated>npx vitest run "app/(dashboard)/users/access-analysis/clusterTransitionLayer.test.ts" "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"</automated></verify>
  <done>Motion is continuous and retargetable around 600ms; reduced motion is immediate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consume catalog targets and weights in the live embedding-map path</name>
  <files>app/(dashboard)/users/access-analysis/__tests__/embeddingMapSeam.test.tsx, app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx</files>
  <action>
    First extend the embedding seam test so zero strengths return the fixture's exact embedding
    and a stronger target beats a weaker target without enabling GPU simulation. In the shell,
    build initial targets for the union of available structural sliders and current aperture
    dimensions, pass targets/weights into createStaticLayer, and replace the flag-OFF blob
    descriptor target with catalogAnchorTarget over getLiveValues. Keep the parked flag-ON
    descriptor path intact. Hide flag-OFF cluster chips whose old packed-footprint coordinates
    no longer describe the anchor field. Do not call the grid path or Cosmos start.
  </action>
  <verify><automated>npx vitest run "app/(dashboard)/users/access-analysis/__tests__/embeddingMapSeam.test.tsx" "app/(dashboard)/users/access-analysis/staticLayer.test.ts" "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"</automated></verify>
  <done>The production default layout target reads catalog targets/weights, starts at the exact embedding, and follows one strongest anchor.</done>
</task>

</tasks>

<verification>
- Focused Vitest: static layer, active grouping, transition layer, GraphCanvas, and embedding seam.
- `npx tsc --noEmit`
- `node scripts/repo-map/check.cjs`
- Scope diff contains no `gridLayout` integration, no new dependency, and no `/access-analysis` charts-page change.
</verification>

<success_criteria>
- LAY-01/LAY-02: catalog targets and weights physically move the live graph.
- LAY-03: slider and winner changes morph continuously from the current visible positions.
- LAY-04: multi-active state selects one organic anchor field and never produces a grid.
- Cosmos remains frozen throughout the default render path.
</success_criteria>

<output>
Create `.planning/phases/27-layout-engine-force-anchor-revival-reheat-guard/27-02-SUMMARY.md` when done.
</output>
