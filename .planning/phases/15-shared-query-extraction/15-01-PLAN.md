---
phase: 15-shared-query-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/folderPermQuery.ts
  - lib/server/templateFolderTerrain.ts
  - lib/server/folderPermissionTerrainView.ts
autonomous: false
requirements: [QUERY-01]

must_haves:
  truths:
    - "lib/server/folderPermQuery.ts exists and exports loadFolderPermRows(projectId, { l2Only? })"
    - "templateFolderTerrain.ts obtains its permission rows via loadFolderPermRows(TEMPLATE_MTY_ID), not an inline base-join $queryRaw"
    - "folderPermissionTerrainView.ts obtains its L2 permission rows via loadFolderPermRows(projectId, { l2Only: true })"
    - "No duplicated base-join SQL remains in either caller (the shared join lives only in folderPermQuery.ts)"
    - "TEST-03 and TEST-02 pass byte-identical (test files unchanged); the terrain Vitest suites are green"
    - "npx tsc --noEmit exits with 0 errors"
    - "/template-mty and /access-analysis render identically to pre-phase (owner visual check)"
  artifacts:
    - path: "lib/server/folderPermQuery.ts"
      provides: "Shared base AccFolderPermission join loader (loadFolderPermRows), plain tagged-template, two scope branches"
      exports: ["loadFolderPermRows"]
      min_lines: 25
    - path: "lib/server/templateFolderTerrain.ts"
      provides: "template-mty terrain loader now importing the shared join instead of inlining it"
      contains: "loadFolderPermRows"
    - path: "lib/server/folderPermissionTerrainView.ts"
      provides: "access-analysis terrain loader now importing the shared join (l2Only) instead of inlining it"
      contains: "l2Only"
  key_links:
    - from: "lib/server/templateFolderTerrain.ts"
      to: "lib/server/folderPermQuery.ts"
      via: "loadFolderPermRows(TEMPLATE_MTY_ID) inside loadTemplateFolderTerrain Promise.all"
      pattern: "loadFolderPermRows\\(\\s*TEMPLATE_MTY_ID"
    - from: "lib/server/folderPermissionTerrainView.ts"
      to: "lib/server/folderPermQuery.ts"
      via: "loadFolderPermRows(projectId, { l2Only: true }) inside loadFolderPermissionTerrain Promise.all"
      pattern: "loadFolderPermRows\\(projectId,\\s*\\{\\s*l2Only"
    - from: "lib/server/folderPermQuery.ts"
      to: "@/server/db"
      via: "plain tagged-template db.$queryRaw on both scope branches"
      pattern: "db\\.\\$queryRaw"
---

<objective>
Extract the base per-project `AccFolderPermission` join — the 5-column
`folder_id | role_id | role_name | perm_type | n_actions` select currently
duplicated across `lib/server/templateFolderTerrain.ts` (all folders) and
`lib/server/folderPermissionTerrainView.ts` (L2-only, "Project Files" parent
join) — into one owned module `lib/server/folderPermQuery.ts`. Both terrain
loaders import and call it instead of inlining the SQL. This is QUERY-01 /
REF-02: a pure server-side query relocation, behavior-preserving, no
workshop-visible change.

Purpose: give the shared join a single owner so the Phase 16-19 monolith splits
and the AccFolderPermissionSummary projection build on one query, with provably
zero change to what `/template-mty` and `/access-analysis` show.

Output: `lib/server/folderPermQuery.ts` (new) plus two minimally-edited callers;
TEST-02 (`folderPermissionTerrainView.test.ts`) and TEST-03
(`templateFolderTerrain.sharedQuery.test.ts`) stay byte-identical and green;
`npx tsc --noEmit` clean.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-shared-query-extraction/15-CONTEXT.md

# Source under change (read fully before editing)
@lib/server/templateFolderTerrain.ts
@lib/server/folderPermissionTerrainView.ts

# Golden masters — MUST stay byte-identical, do NOT edit
@lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts
@lib/server/__tests__/folderPermissionTerrainView.test.ts
</context>

<constraints>
Locked by 15-CONTEXT.md — honor exactly, do not re-derive or "improve":

- Target file is `lib/server/folderPermQuery.ts` (fixed by QUERY-01), in the
  verified `lib/server/` root. No `src/...` paths.
- One function only: `loadFolderPermRows(projectId: string, opts?: { l2Only?: boolean })`
  returning the CURRENT raw snake_case rows unchanged
  (`folder_id | role_id | role_name | perm_type | n_actions`). Use the
  `{ l2Only }` options object, not a positional boolean.
- Plain tagged-template `db.$queryRaw` on BOTH branches. NO `Prisma.sql` /
  `Prisma.raw` / `Prisma.join` / string-concatenated SQL. (TEST-03 assertion #4
  checks `mock.calls[0]` contains `TEMPLATE_MTY_ID`; a composed query passes a
  single `Sql` object as `calls[0][0]` and breaks byte-identical.)
- The all-folders vs L2-only scope difference is PRESERVED, not unified.
- Extraction scope = base join ONLY. Do NOT touch the single-use queries:
  `parentPerms`, `loadTerrainProjects` density, `loadFolderPermissionOverview`
  GROUP BY/CTE, or the "Project Files" parent-grants query.
- Do NOT modify either test file. Do NOT touch `/users/spatial-graph`. Do NOT
  "fix" the 2 pre-existing `FolderPermissionTerrain.test.tsx` WIP failures
  (unrelated branch debt, deferred to Phase 16).
- Commit by EXPLICIT PATH only (never `-A` / `.`) — the branch carries unrelated
  WIP and `.planning/` migration deletions; verify `git diff --cached --name-only`
  before committing.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/server/folderPermQuery.ts owning the shared base join</name>
  <files>lib/server/folderPermQuery.ts</files>
  <action>
Create the new server-only module. Start it with `import "server-only";` then
`import { db } from "@/server/db";`. Importing `db` from `@/server/db` is
NON-NEGOTIABLE — TEST-02 and TEST-03 mock that exact module, so the extracted
query must run against the same mocked client or the golden masters break.

Export exactly one async function
`loadFolderPermRows(projectId: string, opts?: { l2Only?: boolean })` that
resolves to the current raw snake_case rows unchanged. Define a row interface
(name at your discretion, e.g. FolderPermRow) with fields folder_id: string,
role_id: string, role_name: string, perm_type: string, n_actions: number, and
return `Promise<FolderPermRow[]>`.

Inside the function, branch synchronously on `opts?.l2Only` and return the
matching plain tagged-template `db.$queryRaw` call. The `db.$queryRaw` call MUST
be reached synchronously with NO `await` before it (return it directly, or
`return await`) so the callers' Promise.all call-ordering and TEST-03's
"called exactly once / calls[0] contains TEMPLATE_MTY_ID" both hold. The ONLY
interpolated value in each branch is `${projectId}`.

- Default branch (all folders — template-mty): copy the SELECT / FROM / JOIN /
  WHERE text VERBATIM from templateFolderTerrain.ts lines 179-186. It selects
  five aliases — folder_id (from fp.folderId), role_id (from fp.roleId),
  role_name (from r.name), perm_type (from fp.permType), and n_actions (from
  COALESCE(cardinality(fp.actions), 0)::int) — over "AccFolderPermission" fp
  joined to "AccRole" r and "AccFolder" f, scoped by f."projectId" = ${projectId}.
- l2Only branch (access-analysis): copy the SELECT / FROM / JOIN / WHERE text
  VERBATIM from folderPermissionTerrainView.ts lines 96-107. It is the identical
  five-alias select plus one extra join:
  JOIN "AccFolder" parent ON f."parentId" = parent.id AND parent.name = 'Project Files'.

HARD CONSTRAINT: both branches are plain tagged templates — do NOT use
`Prisma.sql`, `Prisma.raw`, `Prisma.join`, or string concatenation to build the
SQL. If you prefer to avoid repeating the SELECT text, you may only do so via
plain-JS structuring that still emits a tagged-template literal per branch;
never inject a raw SQL fragment through `${...}` (that would parameterize the
SQL text and break the query). Repeating the identical select text across the
two branches is the safe default and is explicitly allowed.

Add a short JSDoc: this module owns the shared base AccFolderPermission join for
QUERY-01 / REF-02; both scope branches must remain plain tagged templates so
TEST-02/TEST-03 stay byte-identical. Do NOT add any of the single-use queries
(parentPerms, density, overview) here — base join only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Expected: 0 errors. The new module type-checks standalone; callers are
    unchanged in this task, so the terrain suites still pass.
  </verify>
  <done>lib/server/folderPermQuery.ts exists, exports loadFolderPermRows(projectId, { l2Only? }), has two plain tagged-template db.$queryRaw branches (default all-folders; l2Only adds the Project Files parent join), imports db from @/server/db, and uses no Prisma.sql/raw/join. npx tsc --noEmit is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire both terrain loaders to call loadFolderPermRows; remove inline base joins</name>
  <files>lib/server/templateFolderTerrain.ts, lib/server/folderPermissionTerrainView.ts</files>
  <action>
templateFolderTerrain.ts:
- Add `import { loadFolderPermRows } from "@/lib/server/folderPermQuery";`
  (alias-first, matching this file's `@/` import style; a relative
  `./folderPermQuery` is equally acceptable).
- In loadTemplateFolderTerrain's Promise.all, replace the inline base-join
  $queryRaw block (currently the third element, lines 179-186) with
  `loadFolderPermRows(TEMPLATE_MTY_ID)`. Keep it as the SAME third element so the
  destructured `perms` binding is unchanged, and keep the existing `.map()` to
  camelCase for buildFolderTerrain (line 194) VERBATIM.

folderPermissionTerrainView.ts:
- Add the same import
  (`import { loadFolderPermRows } from "@/lib/server/folderPermQuery";`).
- In loadFolderPermissionTerrain's Promise.all, replace the inline base-join
  $queryRaw block (currently the `perms` element, lines 96-107) with
  `loadFolderPermRows(projectId, { l2Only: true })`. It MUST stay at the SAME
  array position (element index 2, right after the folders query) so the ordered
  5-call `mockResolvedValueOnce` chain in TEST-02 (`toHaveBeenCalledTimes(5)`)
  still lines up. Keep every downstream snake_case use of `perms` untouched
  (perms.map(p => p.folder_id ...), roleNameById.set(p.role_id, p.role_name),
  etc.).
- Do NOT touch the other queries in this file (folders, parentPerms,
  dcRoleUsers, liveRoleUsers, loadTerrainProjects, loadFolderPermissionOverview)
  and do NOT remove the existing `import { Prisma }` — it is still used by the
  overview's Prisma.join and must stay.

Both files: update the top-of-file `// SPLIT-PENDING: REF-02` header comments to
state that the shared join now lives in lib/server/folderPermQuery.ts
(extraction complete); keep the TEST-02/TEST-03 references. Do NOT modify either
test file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts lib/server/__tests__/folderPermissionTerrainView.test.ts</automated>
    Also confirm the shared base join no longer lives in either caller and the
    test files are byte-identical:
    `grep -Fn 'r.name AS role_name, fp."permType" AS perm_type' lib/server/templateFolderTerrain.ts lib/server/folderPermissionTerrainView.ts` returns NO matches (the discriminating base-join select line is gone from both callers);
    `grep -Fn loadFolderPermRows lib/server/folderPermQuery.ts lib/server/templateFolderTerrain.ts lib/server/folderPermissionTerrainView.ts` matches in all three;
    `git diff --name-only -- lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts lib/server/__tests__/folderPermissionTerrainView.test.ts` is EMPTY (no test-file edits).
  </verify>
  <done>Both callers import and call loadFolderPermRows (template with TEMPLATE_MTY_ID; view with projectId + { l2Only: true }) at the same Promise.all positions; no duplicated base-join SQL remains in either caller; TEST-02 and TEST-03 pass unchanged; npx tsc --noEmit is clean. The Prisma import in the view file is retained for the untouched overview query.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner visual parity on /template-mty + /access-analysis and explicit-path commit review</name>
  <action>
    After Tasks 1-2 and all automated gates pass, confirm zero visible behavior
    change on the two affected workshop pages and that only the three intended
    source files are staged before commit. This is the final Phase 15 gate.
    Follow the how-to-verify steps; resume only on owner approval. Stage by
    explicit path only (never `git add -A` / `.`).
  </action>
  <what-built>
Shared base AccFolderPermission join extracted to lib/server/folderPermQuery.ts;
both terrain loaders rewired to call loadFolderPermRows (all-folders for
template-mty; l2Only for access-analysis). npx tsc --noEmit is clean and TEST-02
+ TEST-03 pass byte-identical (test files unedited).
  </what-built>
  <how-to-verify>
1. Owner visual parity (SC#5): with the app running, open `/template-mty` and
   confirm the folder-permission terrain renders identically to pre-phase (same
   folders, tiers/colors, bar heights, roster-derived heights). Open
   `/access-analysis`, pick a project, and confirm the per-project folder-
   permission terrain (folders x roles, tiers, user counts) is unchanged. No
   layout, color (zinc theme), or data shift.
2. Scope + explicit-path commit check (BEFORE committing): stage ONLY the three
   source files by explicit path — never `git add -A` / `git add .`. Then run
   `git diff --cached --name-only` and confirm it lists ONLY
   lib/server/folderPermQuery.ts, lib/server/templateFolderTerrain.ts,
   lib/server/folderPermissionTerrainView.ts (plus the .planning plan/roadmap/
   context docs if commit_docs stages them). Nothing under
   app/(dashboard)/users/spatial-graph, no unrelated branch WIP, no .planning
   migration deletions.
3. Regression boundary: confirm the 2 known FolderPermissionTerrain.test.tsx WIP
   failures are unchanged — not newly broken, not "fixed" here (deferred to
   Phase 16).
  </how-to-verify>
  <resume-signal>Type "approved" once the two pages render identically and the staged paths are clean, or describe any visual/scope drift.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server loader -> PostgreSQL | The shared query crosses into the DB; the only value crossing is projectId. No client input is introduced by this refactor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | loadFolderPermRows in lib/server/folderPermQuery.ts | mitigate | Only projectId is interpolated, and it flows through the plain tagged-template db.$queryRaw as a bound parameter (identical to the pre-existing inline queries). `l2Only` selects a STATIC SQL branch — it never contributes SQL text. No Prisma.raw / string concatenation. |
| T-15-02 | Information disclosure | terrain scope (all-folders vs L2-only) | accept | The WHERE projectId scope and the l2Only "Project Files" parent join are copied verbatim from the originals — no scope widening. Row-bound + scope pinned by TEST-02/TEST-03 golden masters. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages are installed in this phase; no new dependencies. No supply-chain surface added. |
</threat_model>

<verification>
Run in order; all must pass before the checkpoint / commit:

1. `npx tsc --noEmit` -> 0 errors (before any rebuild; gate order per SKILL.md).
2. `npx vitest run lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts lib/server/__tests__/folderPermissionTerrainView.test.ts`
   -> all green. TEST-03 assertion #4 (called once + calls[0] contains
   TEMPLATE_MTY_ID) and TEST-02 (5 ordered $queryRaw calls) are the load-bearing
   assertions.
3. Byte-identical proof:
   `git diff --name-only -- lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts lib/server/__tests__/folderPermissionTerrainView.test.ts`
   -> EMPTY (no edits to either test file).
4. Residual-SQL grep (SC#2): the discriminating base-join select line is gone
   from both callers -
   `grep -Fn 'r.name AS role_name, fp."permType" AS perm_type' lib/server/templateFolderTerrain.ts lib/server/folderPermissionTerrainView.ts`
   -> no matches. Positive: `grep -Fn loadFolderPermRows` matches in all three
   source files.
5. Broader terrain suite may be run, but do NOT treat the 2 pre-existing
   FolderPermissionTerrain.test.tsx WIP failures as regressions — they are
   unrelated and out of scope for this phase.
6. Owner visual parity on `/template-mty` and `/access-analysis` (checkpoint).
7. Explicit-path staging: `git diff --cached --name-only` shows only the three
   source files (+ planning docs). `/users/spatial-graph` untouched.

No rebuild of `:3000` is required for this phase unless the owner wants to run
the visual check against a fresh build; if so, follow the tsc -> build -> Task
Scheduler restart sequence and stop `:3000` first.
</verification>

<success_criteria>
Maps 1:1 to the Phase 15 ROADMAP success criteria:

1. `lib/server/folderPermQuery.ts` exists and owns the 5-column
   AccFolderPermission join (row-bound, project-scoped, COALESCE null-path)
   previously duplicated across the two callers. (Task 1)
2. Both callers import the base join from `lib/server/folderPermQuery.ts`; no
   duplicated join SQL remains in either file. (Task 2 + verification #4)
3. `templateFolderTerrain.sharedQuery.test.ts` (TEST-03) passes byte-identical
   with zero test-file changes; the terrain Vitest suites are green.
   (verification #2, #3)
4. `npx tsc --noEmit` exits with 0 errors. (verification #1)
5. `/template-mty` and `/access-analysis` render identically to pre-phase.
   (checkpoint / verification #6)
</success_criteria>

<output>
Create `.planning/phases/15-shared-query-extraction/15-01-SUMMARY.md` when done.
Record: the exact three files changed, the final loadFolderPermRows signature +
return type, confirmation that both test files are byte-identical (git diff
empty), the tsc + Vitest results, and the staged-path list proving explicit-path
commit hygiene.
</output>

## Dashboard self-check

- **Context:** Loaded STATE.md, ROADMAP.md (Phase 15, 5 SCs), REQUIREMENTS.md
  (QUERY-01), 15-CONTEXT.md (locked decisions), PROJECT.md, SKILL.md; read both
  source files and both golden-master tests; verified `npm test`=`vitest run`
  (Vitest 4.1.6) and `@/*`->`./*` alias.
- **Evidence:** Base join confirmed at templateFolderTerrain.ts L179-186
  (all-folders) and folderPermissionTerrainView.ts L96-107 (L2-only, Project
  Files parent join); TEST-03 assertion #4 (`mock.calls[0]` contains
  TEMPLATE_MTY_ID, called once) and TEST-02 ordered 5-call chain confirmed as
  the constraints forbidding Prisma.sql composition and requiring same-position,
  synchronous db.$queryRaw. folderPermQuery.ts does not yet exist.
- **Constraints:** Behavior-preserving, byte-identical TEST-02/TEST-03, plain
  tagged-template only, base-join scope only, explicit-path commit, no new
  WebGL, zinc theme untouched (server-only, no render change),
  `/users/spatial-graph` out of scope, deferred WIP failures not touched.
- **Gates:** `npx tsc --noEmit` -> targeted Vitest (TEST-02/TEST-03) -> test-file
  byte-identical git diff -> residual base-join grep -> owner visual parity ->
  explicit-path `git diff --cached` review.
- **VERIFY:** none — all paths, line numbers, test assertions, and commands are
  grounded in files read this session.
