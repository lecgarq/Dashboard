---
phase: 04-interactions-analytics-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/(dashboard)/users/access-analysis/SliderContext.tsx
  - app/(dashboard)/users/access-analysis/FilterContext.tsx
  - app/(dashboard)/users/access-analysis/SelectionContext.tsx
  - app/(dashboard)/users/access-analysis/Toolbar.tsx
  - app/(dashboard)/users/access-analysis/DimensionFilterPopover.tsx
  - app/(dashboard)/users/access-analysis/SliderSidebar.tsx
  - app/(dashboard)/users/access-analysis/DimensionSlider.tsx
  - app/(dashboard)/users/access-analysis/SelectionPanel.tsx
  - app/(dashboard)/users/access-analysis/UserDetailPanel.tsx
  - app/(dashboard)/users/access-analysis/RightPanelStack.tsx
  - app/(dashboard)/users/access-analysis/selectionQueries.ts
  - app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
  - app/(dashboard)/users/access-analysis/page.tsx
  - app/(dashboard)/users/access-analysis/__tests__/SliderContext.test.tsx
  - app/(dashboard)/users/access-analysis/__tests__/FilterContext.test.tsx
  - app/(dashboard)/users/access-analysis/__tests__/selectionQueries.test.ts
  - app/(dashboard)/users/access-analysis/__tests__/Toolbar.test.tsx
  - app/(dashboard)/users/access-analysis/__tests__/SelectionPanel.test.tsx
autonomous: false
requirements:
  - INTR-06
  - INTR-07
  - ANLY-01
  - ANLY-02

must_haves:
  truths:
    - "Six dimension sliders render in a right sidebar with 0–100 range and live numeric badges (e.g. 'Activity 73')"
    - "Moving any slider 0→100 calls physics.updateSliders with the value normalized 0..1, coalesced via requestAnimationFrame"
    - "Double-clicking a slider thumb resets just that slider; the 'Reset' button resets all six to 0"
    - "Slider values + active filters + search query persist to localStorage and restore on reload (no SSR hydration mismatch)"
    - "Clicking the 2D|3D pill toggles GraphCanvas.mode prop — uses the existing Phase 3 600ms tween"
    - "Six dimension chip popovers in the top toolbar add/remove values from activeFilters; chips highlight when active"
    - "Drawing a lasso opens the right-side SelectionPanel with two pie charts (role + permission tier) populated from DuckDB"
    - "Clicking a pie slice writes a drillDown record that filters within the selection; clicking again clears it"
    - "Selection persists across global filter changes; pie chart recomputes from the still-visible subset"
    - "Right panel stack: clicking a node opens UserDetailPanel; closing returns to lasso pie if a lasso is active, else to SliderSidebar"
  artifacts:
    - path: "app/(dashboard)/users/access-analysis/SliderContext.tsx"
      provides: "Six-dimension slider state + rAF-coalesced physics.updateSliders + localStorage persistence + per-thumb and global reset"
      exports: ["SliderProvider", "useSliders", "DIMENSIONS"]
      min_lines: 80
    - path: "app/(dashboard)/users/access-analysis/FilterContext.tsx"
      provides: "Active filter chip state per dimension + search query + drillDown + localStorage persistence"
      exports: ["FilterProvider", "useFilters"]
      min_lines: 70
    - path: "app/(dashboard)/users/access-analysis/SelectionContext.tsx"
      provides: "Lasso selection indices + isolatedNodeIndex state shared between GraphInteractions and right-panel-stack consumers"
      exports: ["SelectionProvider", "useSelection"]
    - path: "app/(dashboard)/users/access-analysis/Toolbar.tsx"
      provides: "Top bar: search input + 6 dim popovers + lasso toggle + 2D/3D segmented pill + clear-all link"
      exports: ["Toolbar"]
      min_lines: 80
    - path: "app/(dashboard)/users/access-analysis/SliderSidebar.tsx"
      provides: "Right-sidebar home state — 6 Radix sliders stacked, global Reset button at top"
      exports: ["SliderSidebar"]
      min_lines: 40
    - path: "app/(dashboard)/users/access-analysis/SelectionPanel.tsx"
      provides: "Lasso pie panel — header with count + Clear, two DonutPanels (role + tier), slice-click drill-down"
      exports: ["SelectionPanel"]
      min_lines: 60
    - path: "app/(dashboard)/users/access-analysis/RightPanelStack.tsx"
      provides: "Z-stack manager: latest of UserDetail / SelectionPanel / SliderSidebar with framer-motion slide-in"
      exports: ["RightPanelStack"]
    - path: "app/(dashboard)/users/access-analysis/selectionQueries.ts"
      provides: "aggregateSelectionByRole + aggregateSelectionByTier DuckDB queries returning DonutSlice[]"
      exports: ["aggregateSelectionByRole", "aggregateSelectionByTier"]
    - path: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      provides: "Final composition — wraps Toolbar + GraphInteractions(GraphCanvas) + RightPanelStack with all providers"
      exports: ["AccessAnalysisShell"]
  key_links:
    - from: "app/(dashboard)/users/access-analysis/SliderContext.tsx"
      to: "app/(dashboard)/users/access-analysis/physicsLayer.ts"
      via: "rAF-coalesced physics.updateSliders(normalizedValues) on slider change"
      pattern: "physics\\.updateSliders\\("
    - from: "app/(dashboard)/users/access-analysis/Toolbar.tsx"
      to: "app/(dashboard)/users/access-analysis/FilterContext.tsx"
      via: "useFilters() — read activeFilters, setSearchQuery, toggleChip, clearAll"
      pattern: "useFilters\\(\\)"
    - from: "app/(dashboard)/users/access-analysis/SelectionPanel.tsx"
      to: "app/(dashboard)/users/access-analysis/selectionQueries.ts"
      via: "useEffect on [selectionIds] → aggregateSelectionByRole/Tier → setState DonutSlice[]"
      pattern: "aggregateSelectionBy(Role|Tier)"
    - from: "app/(dashboard)/users/access-analysis/selectionQueries.ts"
      to: "@duckdb/duckdb-wasm"
      via: "getDuckDbClient().connection.query(sql) against graph_user_projects + graph_folder_permissions"
      pattern: "connection\\.query"
    - from: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      to: "app/(dashboard)/users/access-analysis/GraphInteractions.tsx"
      via: "Passes activeFilters/searchQuery/lassoActive/lassoSelection/isolatedNodeIndex/drillDown from contexts as props"
      pattern: "GraphInteractions"
    - from: "app/(dashboard)/users/access-analysis/SelectionPanel.tsx"
      to: "app/(dashboard)/users/access-analysis/DonutPanel.tsx"
      via: "Render <DonutPanel slices={roleSlices} /> and <DonutPanel slices={tierSlices} />"
      pattern: "DonutPanel"
---

<objective>
Build the UI chrome and analytics bridge on top of plan 04-01's interaction layer: the top toolbar (search + filter chips + lasso toggle + 2D/3D pill), the right-sidebar slider home state, the lasso → DuckDB → pie chart side panel, and the right-panel z-stack that coordinates all three overlay states. Persist slider + filter state to localStorage. Final composition (AccessAnalysisShell) replaces the legacy page entry.

Purpose: Surface every Phase 4 user gesture via UI controls and produce the lasso-driven role + permission-tier breakdown. This plan turns 04-01's predicate-engine + lasso-overlay primitives into a complete, demo-ready interaction surface.

Output: SliderContext + FilterContext + SelectionContext, Toolbar with 6 dim popovers, SliderSidebar with 6 Radix sliders, SelectionPanel with two DonutPanels + drill-down, UserDetailPanel, RightPanelStack with framer-motion, selectionQueries.ts with two DuckDB aggregations, AccessAnalysisShell composing everything, Vitest coverage, and a human-verify checkpoint at the end.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interactions-analytics-bridge/04-CONTEXT.md
@.planning/phases/04-interactions-analytics-bridge/04-RESEARCH.md
@.planning/phases/04-interactions-analytics-bridge/04-01-SUMMARY.md
@app/(dashboard)/users/access-analysis/physicsLayer.ts
@app/(dashboard)/users/access-analysis/DonutPanel.tsx
@app/(dashboard)/users/access-analysis/duckdbClient.ts
@app/(dashboard)/users/access-analysis/graphSql.ts
@app/(dashboard)/users/access-analysis/interactionTypes.ts
@app/(dashboard)/users/access-analysis/GraphInteractions.tsx

<interfaces>
<!-- From 04-01 (already shipped when this plan runs) -->

From interactionTypes.ts:
```typescript
export interface NodeFeatureSnapshot { /* see 04-01 */ }
export interface PredicateInputs { /* see 04-01 */ }
```

From GraphInteractions.tsx — props this plan must supply:
```typescript
interface GraphInteractionsProps {
  physics: PhysicsLayer;
  features: ReadonlyArray<NodeFeatureSnapshot>;
  mode: "2d" | "3d";
  activeFilters: Record<string, ReadonlySet<string>>;
  searchQuery: string;
  lassoActive: boolean;
  onLassoComplete: (indices: number[]) => void;
  isolatedNodeIndex: number | null;
  onIsolate: (i: number | null) => void;
  lassoSelection: ReadonlySet<number> | null;
  drillDown: Record<string, string> | null;
  children: ReactNode;
}
```

From DonutPanel.tsx:
```typescript
export interface DonutSlice { label: string; value: number; color: string; }
export interface DonutPanelProps { slices: DonutSlice[]; onSliceClick?: (label: string) => void; /* ... */ }
export function DonutPanel(props: DonutPanelProps): JSX.Element;
```

From graphSql.ts:
```typescript
export const GRAPH_ANALYTICS_SOURCE_TABLES = {
  userProjects: "graph_user_projects",
  folderPermissions: "graph_folder_permissions",
  // ...
};
```

Six dimensions (canonical list, used by both contexts and toolbar):
```typescript
export const DIMENSIONS = [
  { id: "role",       label: "Role",       kind: "categorical" },
  { id: "tier",       label: "Tier",       kind: "categorical" },
  { id: "project",    label: "Project",    kind: "categorical" },
  { id: "isExternal", label: "External",   kind: "categorical" },
  { id: "activity",   label: "Activity",   kind: "bucket" },
  { id: "signin",     label: "Sign-in",    kind: "bucket" },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SliderContext + FilterContext + SelectionContext + selectionQueries with tests</name>
  <files>
    app/(dashboard)/users/access-analysis/SliderContext.tsx
    app/(dashboard)/users/access-analysis/FilterContext.tsx
    app/(dashboard)/users/access-analysis/SelectionContext.tsx
    app/(dashboard)/users/access-analysis/selectionQueries.ts
    app/(dashboard)/users/access-analysis/__tests__/SliderContext.test.tsx
    app/(dashboard)/users/access-analysis/__tests__/FilterContext.test.tsx
    app/(dashboard)/users/access-analysis/__tests__/selectionQueries.test.ts
  </files>
  <action>
    **SliderContext.tsx** (RESEARCH Pattern 7 + Pattern 8):
    - Export `DIMENSIONS` constant (6 dims as shown in &lt;interfaces&gt;).
    - `SliderProvider({ physics, children })`: state is `Record&lt;dimId, number&gt;` initialized to `{ role:0, tier:0, project:0, isExternal:0, activity:0, signin:0 }`.
    - `setSliderValue(dimId, value)`: setState + rAF-coalesced `physics.updateSliders(normalizedValues)` where each value is divided by 100. Only one rAF in flight at a time; collapse multiple changes into one call. **Critical**: physics.updateSliders takes the FULL `Record<dimId, number>` of normalized 0..1 values, not partial.
    - `resetAll()`: setState all 0s, then immediate physics.updateSliders.
    - `resetOne(dimId)`: setState that dim to 0, schedule rAF push.
    - localStorage hydration: render with defaults on SSR; in `useEffect`, read `STORAGE_KEY = "lecg.access-analysis.controls.v1"` (shared with FilterContext), JSON.parse the `sliders` field, setState. Wrap in try/catch (Safari/quota). Two-pass mount (RESEARCH Pitfall 3).
    - localStorage write: debounced 200ms write on any state change.
    - `useSliders()` returns `{ values, setSliderValue, resetAll, resetOne }`.

    **FilterContext.tsx**:
    - State: `{ activeFilters: Record&lt;dimId, Set&lt;string&gt;&gt;, searchQuery: string, drillDown: Record&lt;string, string&gt; | null }`.
    - Init all sets empty, search="", drillDown=null.
    - `toggleChip(dimId, value)`: add/remove value from the set for that dim. Returns a NEW Set instance (immutable for React).
    - `clearAll()`: empties all sets, clears search, clears drillDown.
    - `setSearchQuery(q: string)`: setState (debounce happens inside GraphInteractions per 04-01).
    - `setDrillDown(d)`: setState.
    - Same localStorage key as SliderContext (use SAME `STORAGE_KEY`); serialize sets as arrays, deserialize on load. **NOTE**: drillDown is session-only — do NOT persist (it's tied to a lasso which doesn't persist).
    - Hydration: two-pass mount.
    - `useFilters()` returns `{ activeFilters, searchQuery, drillDown, toggleChip, clearAll, setSearchQuery, setDrillDown, isDefault }` where `isDefault` is true when all sets empty AND search === "" (drives "Clear all" link visibility).

    **SelectionContext.tsx** (smaller — session-only state):
    - State: `{ lassoSelection: Set&lt;number&gt; | null, isolatedNodeIndex: number | null }`.
    - `setLasso(indices: number[] | null)`: setState (null = clear).
    - `setIsolated(i: number | null)`: setState.
    - `clearAll()`: both to null/null.
    - No persistence — selections are session.
    - `useSelection()` returns `{ lassoSelection, isolatedNodeIndex, setLasso, setIsolated, clearAll }`.

    **selectionQueries.ts** (RESEARCH Code Example "DuckDB Aggregation"):
    ```typescript
    export async function aggregateSelectionByRole(selectedNodeIds: readonly string[]): Promise<DonutSlice[]>
    export async function aggregateSelectionByTier(selectedNodeIds: readonly string[]): Promise<DonutSlice[]>
    ```
    - Use `getDuckDbClient()` (existing util).
    - Escape single quotes via `id.replaceAll("'", "''")`.
    - Use `CAST(COUNT(*) AS INTEGER)` to avoid BigInt (RESEARCH Pitfall 2). Still defensively `Number()` the result.
    - Role: `WITH sel AS (SELECT role_ids FROM graph_user_projects WHERE concat(user_id,'::',project_id) IN (...)) SELECT COALESCE(role_id,'(no role)') AS label, CAST(COUNT(*) AS INTEGER) AS value FROM sel, UNNEST(role_ids) AS t(role_id) GROUP BY 1 ORDER BY value DESC`.
    - Tier: JOIN `graph_user_projects` ↔ `graph_folder_permissions` on `(user_id, project_id)`, `GROUP BY permission_tier`. If `graph_folder_permissions` is empty (RESEARCH Open Q#1), return `[{ label: "(no tier data)", value: selectedNodeIds.length, color: "#64748b" }]` as a graceful fallback.
    - Color palette as in RESEARCH example; export `PALETTE` so SelectionPanel can keep colors stable.
    - Return empty array on empty input (no query).

    **Tests:**

    `SliderContext.test.tsx` (≥6 assertions):
    - Render provider with mocked physics; setSliderValue(dim, 50) → after rAF flush → physics.updateSliders called once with that dim = 0.5.
    - Multiple setSliderValue in same tick → physics.updateSliders called once (coalesced).
    - resetAll → physics.updateSliders called with all zeros.
    - resetOne(dim) → other dims preserved.
    - localStorage: write a key, mount provider, assert state restored after effect.
    - SSR safety: ensure no `localStorage` access during render (mock `typeof window === "undefined"` path — wrap in describe block with deleted window OR use a render that throws if localStorage touched synchronously).

    `FilterContext.test.tsx` (≥5 assertions):
    - toggleChip adds value; second toggleChip removes it.
    - clearAll empties everything.
    - isDefault is true initially, false after any chip toggle, false after search non-empty.
    - localStorage round-trip: filters serialized as arrays restored as Sets.
    - drillDown is NOT persisted (write a value, reload provider, drillDown is null).

    `selectionQueries.test.ts` (≥4 assertions):
    - vi.mock `./duckdbClient` returning a fake connection with controllable query results.
    - aggregateSelectionByRole with empty input → returns [].
    - aggregateSelectionByRole with 3 IDs → query receives 3 escaped values; result mapped to DonutSlice[] with `Number(value)`.
    - aggregateSelectionByTier with empty `graph_folder_permissions` result → returns the fallback slice `[{label:"(no tier data)", ...}]`.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run app/\(dashboard\)/users/access-analysis/__tests__/SliderContext.test.tsx app/\(dashboard\)/users/access-analysis/__tests__/FilterContext.test.tsx app/\(dashboard\)/users/access-analysis/__tests__/selectionQueries.test.ts</automated>
  </verify>
  <done>All three test files pass with ≥15 assertions; SliderContext rAF-coalesces physics.updateSliders; FilterContext round-trips through localStorage without drillDown; selectionQueries handles empty input, empty tier table, and BigInt→Number cast.</done>
</task>

<task type="auto">
  <name>Task 2: Build Toolbar + DimensionFilterPopover + SliderSidebar + DimensionSlider + tests</name>
  <files>
    app/(dashboard)/users/access-analysis/Toolbar.tsx
    app/(dashboard)/users/access-analysis/DimensionFilterPopover.tsx
    app/(dashboard)/users/access-analysis/SliderSidebar.tsx
    app/(dashboard)/users/access-analysis/DimensionSlider.tsx
    app/(dashboard)/users/access-analysis/__tests__/Toolbar.test.tsx
  </files>
  <action>
    **DimensionSlider.tsx** (RESEARCH Code Example "Radix Slider per Dimension" verbatim):
    - Props `{ dimId, label, value, onChange, onReset }`.
    - Use `@radix-ui/react-slider` (`import * as Slider from "@radix-ui/react-slider"`). The repo's installed `radix-ui` 1.4.3 package re-exports primitives — use that import path if direct subpackage isn't available; verify by trying `radix-ui/slider` if `@radix-ui/react-slider` fails resolution.
    - `Slider.Root` with `value={[value]} min=0 max=100 step=1`.
    - `Slider.Thumb` with `onDoubleClick={onReset}`.
    - Zinc-themed styles per memory note (`feedback_dark_palette_neutral.md`): `bg-zinc-800` track, `bg-blue-500` range fill, `border-blue-500 bg-zinc-900` thumb.
    - Numeric badge `tabular-nums text-muted-foreground` showing current value.

    **SliderSidebar.tsx**:
    - Consume `useSliders()`.
    - Header with title "Dimensions" + global "Reset" button calling `resetAll`.
    - Map `DIMENSIONS` → render six `&lt;DimensionSlider /&gt;` stacked vertically with `gap-4 p-4`.
    - Width fixed (e.g., `w-72`).

    **DimensionFilterPopover.tsx**:
    - Props `{ dim: typeof DIMENSIONS[number]; availableValues: string[]; activeValues: ReadonlySet<string>; onToggle: (value: string) => void }`.
    - Use Radix Popover (`@radix-ui/react-popover` via installed `radix-ui`).
    - Trigger button: `&lt;label&gt; ▾` with count badge if `activeValues.size > 0`.
    - Content: list of chips (button per value); each chip highlighted if `activeValues.has(value)`. Click → `onToggle(value)`.
    - For numeric "bucket" dims (activity, signin), `availableValues` are fixed bucket labels (e.g., `["None","Low","Med","High"]` and `["<7d","<30d","<90d",">90d"]`). For categorical, derive from the feature snapshot at the AccessAnalysisShell level and pass in.

    **Toolbar.tsx**:
    - Props `{ features: NodeFeatureSnapshot[]; mode: "2d"|"3d"; onModeChange: (m) => void; lassoActive: boolean; onLassoToggle: () => void; }`.
    - Consume `useFilters()` for search + activeFilters + toggleChip + clearAll + isDefault.
    - Layout: single horizontal flex row, `gap-2 px-4 py-2 border-b`:
      1. Search input (Ctrl/Cmd+K focus shortcut — `useEffect` listener) — controlled by `searchQuery` from FilterContext; placeholder "Search name or email…".
      2. Six DimensionFilterPopover instances — derive `availableValues` from features once via `useMemo` (collect unique values per dim).
      3. Lasso toggle button (icon + label "Lasso") — disabled when `mode === "3d"`; show tooltip "Lasso available in 2D mode only" when disabled (RESEARCH Pitfall — also Anti-pattern).
      4. 2D|3D segmented pill — two buttons, active one has `bg-blue-500 text-white`.
      5. "Clear all" link at the right edge, only rendered when `!isDefault`.

    **Test (`Toolbar.test.tsx`, ≥5 assertions):**
    - Render with FilterProvider + minimal features; assert all 6 dim popover triggers present.
    - Type in search input → assert FilterContext.searchQuery updated.
    - Click a chip inside a popover → assert activeFilters updated.
    - Mode="3d" → lasso button has `disabled` attribute.
    - "Clear all" only visible after a chip is toggled; clicking it restores isDefault=true.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run app/\(dashboard\)/users/access-analysis/__tests__/Toolbar.test.tsx</automated>
  </verify>
  <done>Toolbar.test.tsx passes ≥5 assertions; DimensionSlider uses Radix slider with double-click reset; SliderSidebar renders 6 sliders + reset button; lasso button disabled in 3D mode; Cmd+K focuses search; "Clear all" link shows only when filters non-default.</done>
</task>

<task type="auto">
  <name>Task 3: Build SelectionPanel + UserDetailPanel + RightPanelStack + AccessAnalysisShell + page.tsx + tests</name>
  <files>
    app/(dashboard)/users/access-analysis/SelectionPanel.tsx
    app/(dashboard)/users/access-analysis/UserDetailPanel.tsx
    app/(dashboard)/users/access-analysis/RightPanelStack.tsx
    app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
    app/(dashboard)/users/access-analysis/page.tsx
    app/(dashboard)/users/access-analysis/__tests__/SelectionPanel.test.tsx
  </files>
  <action>
    **SelectionPanel.tsx**:
    - Props `{ selectedIndices: ReadonlySet<number>; features: ReadonlyArray<NodeFeatureSnapshot>; onClear: () => void }`.
    - Consume `useFilters()` for `drillDown` + `setDrillDown`.
    - Convert `selectedIndices` → nodeIds: `Array.from(selectedIndices).map(i => features[i].nodeId)`.
    - **Visible-subset rule (CONTEXT.md)**: when global filters change, only the still-visible subset feeds the pie. Compute visible IDs by re-running the predicate logic from 04-01's predicate engine — OR (simpler) accept `visibleSelectedIndices: ReadonlySet<number>` as a prop derived in AccessAnalysisShell via `useMemo(() => Set of indices in selection that pass the filter predicate)`. PICK: prop-based — cleaner separation.
    - On `[visibleSelectedNodeIds]` change: call `aggregateSelectionByRole` + `aggregateSelectionByTier` in parallel via `Promise.all`; store results in local state `{ roleSlices, tierSlices, loading }`.
    - Header: "{N} users selected" + "Clear selection" button → `onClear()`.
    - Body: two DonutPanels side-by-side (use grid `grid-cols-2 gap-4`). Pass `onSliceClick={label => setDrillDown(prev => prev?.[dim] === label ? null : { [dim]: label })}` — clicking same label twice clears.
    - Width fixed (e.g., `w-96`).

    **UserDetailPanel.tsx**:
    - Props `{ userIndex: number; features: ReadonlyArray<NodeFeatureSnapshot>; onClose: () => void }`.
    - Display the full feature record for `features[userIndex]`.
    - CONTEXT.md says "shows ALL projects and roles for the clicked user, regardless of current filters" — so this panel must re-query: derive `userId = features[userIndex].nodeId.split("::")[0]` and run a small DuckDB query for all `(user_id == userId)` rows in `graph_user_projects`, list project + role per row.
    - Header: name + email + close button.

    **RightPanelStack.tsx** (RESEARCH Pattern 9):
    - Props `{ mode: "2d"|"3d"; features: NodeFeatureSnapshot[]; visibleSelectedIndices: ReadonlySet<number> | null }`.
    - Consume `useSelection()` for `lassoSelection` + `isolatedNodeIndex` + `clearAll` + `setLasso` + `setIsolated`.
    - Top layer = (isolatedNodeIndex != null) ? UserDetail : (lassoSelection ? SelectionPanel : SliderSidebar).
    - Wrap with `framer-motion` `<AnimatePresence>` + `<motion.div initial={{ x: 320 }} animate={{ x: 0 }} exit={{ x: 320 }} transition={{ duration: 0.2 }}>` so layer transitions slide in from right.
    - Pass `onClear`/`onClose` props that clear the appropriate selection state.

    **AccessAnalysisShell.tsx** (FINAL COMPOSITION):
    - `"use client"` directive.
    - Top-level component used by `page.tsx`.
    - State (local, not context): `mode: "2d"|"3d"` (init "2d"), `lassoActive: boolean` (init false), `features: NodeFeatureSnapshot[] | null` (loaded async), `physics: PhysicsLayer | null`.
    - On mount: load features via `buildFeatureSnapshot({ nodeIds })` where nodeIds comes from physics/positions cache (Phase 2 owns node order); build physics if not yet built.
    - Wrap children in `&lt;SliderProvider physics={physics}&gt;` `&lt;FilterProvider&gt;` `&lt;SelectionProvider&gt;`.
    - Inside providers, render:
      ```tsx
      <div className="flex h-full flex-col">
        <Toolbar features={features} mode={mode} onModeChange={setMode} lassoActive={lassoActive} onLassoToggle={() => setLassoActive(v => !v)} />
        <div className="relative flex flex-1">
          <ShellBody ... />  {/* see below */}
          <RightPanelStack mode={mode} features={features} visibleSelectedIndices={visibleSubset} />
        </div>
      </div>
      ```
    - `ShellBody` is a sub-component (inside same file) that consumes `useFilters` + `useSelection` and renders:
      ```tsx
      <GraphInteractions
        physics={physics}
        features={features}
        mode={mode}
        activeFilters={activeFilters}
        searchQuery={searchQuery}
        lassoActive={lassoActive && mode === "2d"}
        onLassoComplete={indices => { setLasso(indices); setLassoActive(false); }}
        isolatedNodeIndex={isolatedNodeIndex}
        onIsolate={setIsolated}
        lassoSelection={lassoSelection}
        drillDown={drillDown}
      >
        <GraphCanvas physics={physics} mode={mode} />
      </GraphInteractions>
      ```
    - Compute `visibleSubset` in AccessAnalysisShell with `useMemo(() => filterSelectionByPredicate(lassoSelection, features, activeFilters, searchQuery), [...])` — same predicate logic from usePredicateEngine but pure (no setMask). Helper exported from `usePredicateEngine.ts` (add export there if missing — small touch only).
    - Loading state: while features=null, render `&lt;div&gt;Loading graph data…&lt;/div&gt;`.

    **page.tsx**:
    - Replace existing default export with `export default function Page() { return <AccessAnalysisShell />; }`.
    - Remove any legacy access-analysis composition; keep auth wrapping intact (consult existing page.tsx structure; do not break server-side guards).

    **Tests (`SelectionPanel.test.tsx`, ≥4 assertions):**
    - vi.mock `./selectionQueries` to return controlled slices.
    - Render with selectedIndices=Set([0,1,2]); assert "3 users selected" header.
    - Wait for effect → both DonutPanels render with the mocked slices.
    - Click a role slice → assert FilterContext.drillDown = `{ role: "<label>" }`.
    - Click same slice again → drillDown back to null.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run app/\(dashboard\)/users/access-analysis/__tests__/SelectionPanel.test.tsx && cd C:/LECG/Dashboard && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>SelectionPanel.test.tsx passes ≥4 assertions; AccessAnalysisShell composes all providers + Toolbar + GraphInteractions + RightPanelStack; page.tsx exports AccessAnalysisShell; tsc --noEmit passes with zero errors across the access-analysis folder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — complete interactions surface against real Hermosillo data</name>
  <files>app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx</files>
  <action>
    Claude has automated the full implementation in Tasks 1–3. This checkpoint is purely human verification — no further code changes required before running the checks. If verification fails, Claude will diagnose based on the specific step that failed.

    Built and ready for verification: top toolbar with search + 6 dimension chip popovers + lasso toggle + 2D/3D pill + clear-all; right sidebar with 6 dimension sliders + reset; click-isolate with user detail panel; hover tooltip with six fields; lasso → role + tier pie charts with drill-down; localStorage persistence of sliders and filters.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run app/\(dashboard\)/users/access-analysis/__tests__ && npx tsc --noEmit</automated>

    Then perform manual verification in browser:
    1. Start dev server: `cd C:/LECG/Dashboard && pnpm dev` (or `npm run dev`).
    2. Navigate to `http://localhost:3000/users/access-analysis` in a browser.
    3. **INTR-01 (filter)**: Open the "Role" popover; click a role chip. Observe non-matching nodes dim to grey (~0.15 alpha). Confirm no node movement during the toggle. Click again to remove.
    4. **INTR-02 (search)**: Type a 2-3 letter prefix in the search box (e.g., "lu"). Within ~100ms, non-matching nodes dim. Backspace clears. Press Ctrl/Cmd+K — focus jumps to search box.
    5. **INTR-03 (click-isolate)**: Click any node. All others drop to ~0.15. The right-side panel switches to UserDetailPanel showing the user's full record across projects. Press Esc OR click empty background → isolation clears, sliders panel returns. Click another node → re-isolates to that node.
    6. **INTR-04 (hover)**: Hover any node. Tooltip appears anchored above-right with: name, email, project, role, last sign-in, activity count. Move near a viewport edge — tooltip clamps/flips.
    7. **INTR-05 (lasso)**: Make sure mode is 2D. Click the lasso toolbar button; cursor becomes crosshair. Click-drag a closed loop around a cluster of nodes; release. Lasso pie panel slides in from the right showing two donuts (role + tier). Confirm header shows "N users selected". Click a slice in either pie — within the selection, non-matching dim further (drill-down). Click same slice again to clear drill-down. Click "Clear selection" → returns to sliders panel.
    8. **INTR-06 (sliders)**: Move "Activity" slider 0 → 100 slowly. Nodes visibly re-cluster toward the activity axis with continuous monotonic animation (no jump, no mode switch). Double-click the slider thumb → resets to 0. Click "Reset" at the top → all six sliders go to 0.
    9. **INTR-07 (2D/3D toggle)**: Click the "3D" pill. Smooth ~600ms tilt; nodes lift into 3D. Lasso button is now disabled (tooltip explains "Lasso available in 2D mode only"). Click "2D" — flattens back.
    10. **ANLY-01 + ANLY-02 (lasso + pie)**: Re-draw a lasso in 2D. Verify pie charts populate and reflect roles + tiers in the selection. Open a filter chip in the toolbar while a lasso is active — pie chart values recompute based on the still-visible subset of the selection.
    11. **Persistence**: Set some sliders and toggle some chips. Refresh the browser. After load, sliders and filters restore to their previous values (lasso and isolate do NOT persist — by design).
    12. **No regressions**: Mode switches still preserve positions (Phase 3 invariant). No simulation restart during filter/search/lasso/click (verify by checking DevTools console — `physicsLayer` should not log any tick increment after a mask change).
  </verify>
  <done>All 12 verification steps pass against real Hermosillo data; user types "approved" to resume. If any step fails, user describes the failure by step number and Claude diagnoses.</done>
  <what-built>
    Full Phase 4 interaction surface: top toolbar with search + 6 dimension chip popovers + lasso toggle + 2D/3D pill + clear-all; right sidebar with 6 dimension sliders + reset; click-isolate with user detail panel; hover tooltip with six fields; lasso → role + tier pie charts with drill-down; localStorage persistence of sliders and filters.
  </what-built>
  <how-to-verify>
    See &lt;verify&gt; section above for the 12-step verification procedure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues by step number</resume-signal>
</task>

</tasks>

<verification>
- All tests from 04-01 still pass (no Phase 3 or Phase 4-01 regressions).
- All new tests in 04-02 pass (SliderContext ≥6, FilterContext ≥5, selectionQueries ≥4, Toolbar ≥5, SelectionPanel ≥4 — total ≥24 new assertions).
- `npx tsc --noEmit` passes across the access-analysis folder.
- Human verification of all 9 Phase 4 requirements (INTR-01..07 + ANLY-01..02) confirmed in Task 4.
- localStorage round-trip works without SSR hydration warnings.
- Lasso correctly disabled when `mode === "3d"`.
</verification>

<success_criteria>
INTR-06 satisfied: SliderSidebar renders 6 Radix sliders 0–100 with numeric badges; setSliderValue propagates via rAF-coalesced physics.updateSliders; double-click thumb resets one; "Reset" button resets all six.
INTR-07 satisfied: 2D|3D segmented pill in Toolbar flips `mode` prop on GraphCanvas; Phase 3's existing 600ms tween handles the transition.
ANLY-01 satisfied: lasso completion triggers SelectionPanel; selectionQueries.aggregateSelectionByRole + aggregateSelectionByTier produce DonutSlice[]; DonutPanels render.
ANLY-02 satisfied: SelectionPanel re-runs queries on `visibleSelectedIndices` change (driven by FilterContext + SearchQuery via AccessAnalysisShell's `useMemo`); slices are canonical DonutSlice shape; slice click sets drillDown which flows through 04-01's predicate engine.
Phase 4 invariant intact: no code path touches positions or the simulation — only `physics.setMask` (filter/search/lasso/click) or `physics.updateSliders` (sliders).
</success_criteria>

<output>
After completion, create `.planning/phases/04-interactions-analytics-bridge/04-02-SUMMARY.md` documenting:
- Final component tree (AccessAnalysisShell → providers → Toolbar + GraphInteractions + RightPanelStack)
- LocalStorage schema version (v1) and persisted fields
- Any deviations from RESEARCH patterns (e.g. if radix-ui sub-import paths differed)
- Test coverage breakdown and total assertion count
- Confirmation of human-verify checkpoint outcome
- Open items deferred to v2 (multi-lasso, 3D lasso, etc. per CONTEXT.md)
</output>
