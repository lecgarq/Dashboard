---
phase: 03-datatable-primitive
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/ui/DataTable.tsx
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "components/ui/DataTable.tsx renders a virtualized body with a frosted-glass sticky header, single-column 3-state sort, a left-pinned column with a scroll-triggered shadow, row-click via onRowClick, always-visible chevron that toggles a one-at-a-time inline expand peek, and a top-right density toggle"
    - "Density (Comfortable default / Compact) persists in localStorage and changes row height + padding only — font size stays constant"
    - "Sort cycles Ascending → Descending → off and resets to the page-supplied defaultSort each mount (initialState, not controlled)"
    - "Empty state shows distinct no-data vs no-results-adjust-filters messaging driven by hasActiveFilter, with an overridable icon/message and a Clear-filters action that invokes onClearFilters"
    - "The primitive renders correctly in light and dark (zinc) themes via Phase 1 CSS-var tokens with no useTheme() call, introduces no new WebGL context, and npx tsc --noEmit exits 0"
    - "All animation routes through @/components/ui/motion (useSafeVariants) and honors prefers-reduced-motion"
  artifacts:
    - path: "components/ui/DataTable.tsx"
      provides: "The generic virtualized DataTable<T> primitive (FND-05) — sort, sticky glass header, pinned column, row-click, accordion inline expand, density toggle, two-message empty state"
      contains: "export function DataTable"
      min_lines: 200
  key_links:
    - from: "components/ui/DataTable.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable + getSortedRowModel + getExpandedRowModel + column pinning"
      pattern: "useReactTable"
    - from: "components/ui/DataTable.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer with getScrollElement + measureElement on the data-index row wrapper"
      pattern: "useVirtualizer"
    - from: "components/ui/DataTable.tsx"
      to: "@/components/ui/motion"
      via: "AnimatePresence + useSafeVariants for the expand peek and hover lift"
      pattern: "from ['\"]@/components/ui/motion['\"]"
    - from: "components/ui/DataTable.tsx"
      to: "app/globals.css Phase 1 tokens"
      via: "bg-surface-2 / backdrop-blur-md glass header + var(--depth-float) hover lift + border-border/60 hairlines"
      pattern: "bg-surface-2|backdrop-blur-md|--depth-float|border-border"
---

<objective>
Implement `components/ui/DataTable.tsx` — the reusable, virtualized table primitive — to GREEN against the Plan 01 contract suite. This is the deliverable of Phase 3: one generic `DataTable<T>` that every table page (Phase 4 `/users`, Phase 6 `/template-mty`) will consume so the table surfaces never diverge.

Purpose: A single primitive locks the workshop-grade look/behavior contract once. TanStack Table v8 supplies headless sort/expand/pinning logic; `@tanstack/react-virtual` v3 virtualizes the body; Phase 1's `PremiumSurface`/tokens/motion supply the premium 2.5D styling that resolves in both themes with zero new WebGL.
Output: `components/ui/DataTable.tsx` (`"use client"`), all Plan 01 tests GREEN, `npx tsc --noEmit` exits 0.

SCOPE BOUNDARY (hard): This plan delivers the PRIMITIVE only. It exposes callbacks/slots (`onRowClick`, `renderExpanded`, `onClearFilters`, `pinnedColumn`, `defaultSort`). It MUST NOT edit, import into, or wire any real page — no changes under `app/(dashboard)/users/` or `app/(dashboard)/template-mty/`. Those pages wire the primitive in Phase 4 / Phase 6.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-datatable-primitive/03-CONTEXT.md
@.planning/phases/03-datatable-primitive/03-RESEARCH.md

# The contract this plan must satisfy (RED suite from Plan 01).
@components/ui/__tests__/DataTable.test.tsx

# Phase 1 primitives — import surfaces this plan styles with (read, do not modify).
@components/ui/PremiumSurface.tsx
@components/ui/motion.ts

<interfaces>
<!-- Phase 1 contracts the executor uses directly — no codebase exploration needed. -->

From components/ui/PremiumSurface.tsx:
```typescript
export interface PremiumSurfaceProps extends React.ComponentProps<"div"> {
  variant?: "base" | "float" | "glass" | "inset"; // glass = rounded-xl bg-surface-2 border border-surface-border backdrop-blur-md
  glow?: boolean;
  className?: string;
  children: React.ReactNode;
}
export function PremiumSurface(props: PremiumSurfaceProps): React.ReactElement;
```
- Use `variant="base"` (.panel-elevated) for the outer table shell; `variant="glass"` (or the raw `bg-surface-2 backdrop-blur-md` classes) on the sticky thead.

From components/ui/motion.ts (NEVER import motion from framer-motion directly):
```typescript
export { motion, AnimatePresence };
export const fadeIn: { hidden; visible };   // opacity-only, 0.25s
export function useSafeVariants<T>(variants: T): T; // zeroes durations under prefers-reduced-motion
```

Phase 1 token classes (resolve in light + dark via CSS vars in globals.css — NO useTheme() needed):
- Glass header: `bg-surface-2 backdrop-blur-md` (+ `border-surface-border`)
- Hover lift: transition into `shadow-[var(--depth-float)]`
- Hairline row separators: `border-border/60` (matches existing TemplateMembersTable convention)
- Pinned body cell opaque fill so content slides under it: `bg-card`

DataTable public API (locked in Plan 01):
```typescript
import type { ColumnDef, Row, SortingState } from "@tanstack/react-table";
export interface DataTableProps<T> {
  data: T[]; columns: ColumnDef<T>[];
  onRowClick?: (row: Row<T>) => void;
  renderExpanded?: (row: Row<T>) => React.ReactNode;
  pinnedColumn?: string; defaultSort?: SortingState;
  hasActiveFilter?: boolean; onClearFilters?: () => void;
  emptyMessage?: string; filteredEmptyMessage?: string; className?: string;
}
export function DataTable<T>(props: DataTableProps<T>): React.ReactElement;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DataTable core — table instance, virtualized split-scroll body, sticky glass header, pinned column</name>
  <files>components/ui/DataTable.tsx</files>
  <behavior>
    Drives GREEN for FND-05-a (columns render), FND-05-b (3-state single-column sort), FND-05-j (glass header tokens), and the structural prerequisites for the rest:
    - Columns from `ColumnDef<T>[]` render their header labels; body rows render via the virtualizer mock.
    - Header click cycles asc → desc → off; clicking a different header replaces the sort (single-column).
    - The sticky thead container carries `bg-surface-2` and `backdrop-blur-md`.
    - Sort defaults come from `defaultSort` via `initialState.sorting` (so it resets each mount, not persisted).
  </behavior>
  <action>Create `components/ui/DataTable.tsx` with the `"use client"` directive at the top (it uses hooks, localStorage, scroll listeners). Implement the generic `export function DataTable&lt;T&gt;(props: DataTableProps&lt;T&gt;)` against the locked interface.

Build the TanStack Table instance per 03-RESEARCH.md Pattern 3: `useReactTable&lt;T&gt;({ data, columns, state: { sorting, expanded }, onSortingChange: setSorting, onExpandedChange: handleAccordionExpand, getCoreRowModel, getSortedRowModel, getExpandedRowModel, getRowCanExpand: () => true, enableSortingRemoval: true, enableMultiSort: false, initialState: { sorting: defaultSort ?? [], columnPinning: pinnedColumn ? { left: [pinnedColumn] } : {} } })`. `enableSortingRemoval: true` gives the 3-state asc→desc→off cycle (locked decision); `enableMultiSort: false` enforces single-column (locked, no Shift-click — the deferred multi-column idea must NOT appear).

Lay out the split-scroll DOM exactly per 03-RESEARCH.md Pattern 1 / Pitfall 1, because a sticky thead inside the virtualizer's transform-based scroll container disappears on scroll: an outer `flex flex-col h-full overflow-hidden` shell (wrap in `PremiumSurface variant="base"` for the .panel-elevated depth), then a header `overflow-x-auto` div containing a `&lt;table className="w-full table-fixed"&gt;` with a `&lt;thead&gt;` carrying `sticky top-0 z-10` plus the glass tokens `bg-surface-2 backdrop-blur-md border-b border-surface-border`, then a separate body scroll div `ref={scrollRef} className="flex-1 overflow-auto"` that `useVirtualizer` references via `getScrollElement: () => scrollRef.current`. Use `table-fixed` on BOTH the header table and the body, and set explicit `width: header.getSize()` on every `&lt;th&gt;` and the same `column.getSize()` on each body cell so columns align (Pitfall 2) — define a `size` on the mock-friendly columns via the standard ColumnDef `size` field passed by callers.

Header cells: render `flexRender(header.column.columnDef.header, header.getContext())`; for sortable columns attach `header.column.getToggleSortingHandler()` to an onClick, render an up/down arrow (lucide-react `ChevronUp`/`ChevronDown`) keyed off `header.column.getIsSorted()` (`"asc" | "desc" | false`), and add a subtle active-header highlight class when sorted (locked: arrow + active-header highlight). Skip the sort affordance when `header.column.getCanSort()` is false.

Pinned first column (03-RESEARCH.md §Pinned First Column): for the left-pinned column, header cell gets `sticky left-0 z-30 bg-surface-2 backdrop-blur-md`; body cell gets `sticky left-0 z-10 bg-card` (opaque, so scrolled body content slides beneath it). Compute the left offset with `column.getStart("left")`. Detect horizontal scroll with a `scroll` listener on `scrollRef` that sets a `scrolledX` state when `scrollLeft > 0`; when true, add `shadow-[4px_0_8px_-4px_rgba(0,0,0,0.15)]` to the last left-pinned column (`column.getIsLastColumn("left")`) so it reads as a floating layer (locked: soft shadow once scrolled horizontally). Clean up the listener in the effect return (Pitfall 7).

Body: drive `rows = table.getRowModel().rows`; `const rowVirtualizer = useVirtualizer({ count: rows.length, getScrollElement: () => scrollRef.current, estimateSize: () => density === "compact" ? 48 : 72, measureElement: (el) => el?.getBoundingClientRect().height ?? 72, overscan: 5 })`. Render the virtual canvas div `style={{ height: rowVirtualizer.getTotalSize(), position: "relative" }}`, then map `rowVirtualizer.getVirtualItems()` to an OUTER wrapper div per row carrying `data-index={virtualItem.index}` and `ref={rowVirtualizer.measureElement}` (the ref + data-index MUST be on this same outermost element so the expanded peek height is measured — Pitfall 3), positioned `absolute top-0 left-0 w-full` with `transform: translateY(${virtualItem.start}px)`. Inside the wrapper, render the row's visible cells via `flexRender(cell.column.columnDef.cell, cell.getContext())`.

Rows are separated by `border-b border-border/60` hairlines (locked: hairline separators, no zebra striping). Row hover = background highlight + a depth lift: a hover class that transitions into `shadow-[var(--depth-float)]` plus a background highlight token (locked: hover highlight + slight lift).

Do NOT call `useTheme()` — all theming is CSS-var tokens that resolve in both themes (03-RESEARCH.md §Theme constraint). Do NOT use `useWindowVirtualizer` (that is PersonRowList's window-scroll pattern; this primitive has a capped-height container). Do NOT import any WebGL/canvas library — keep the no-new-WebGL guarantee (the Plan 01 import guard asserts this). Do not reference WebGL/canvas package names anywhere in this file, including comments, so the guard cannot self-trip.

Commit this task GREEN: `feat(03-02): DataTable core — sort, virtualized split-scroll body, glass sticky header, pinned column`.</action>
  <verify>
    <automated>npm test -- DataTable 2>&1 | tail -5; npx tsc --noEmit</automated>
  </verify>
  <done>DataTable.tsx exists and exports the generic `DataTable&lt;T&gt;`; FND-05-a/-b/-j tests pass (columns render, sort cycles asc→desc→off single-column, sticky thead carries `bg-surface-2 backdrop-blur-md`); pinned-column sticky classes and scroll-shadow logic present; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement accordion expand, row-click gesture split, density toggle + localStorage</name>
  <files>components/ui/DataTable.tsx</files>
  <behavior>
    Drives GREEN for FND-05-c/-d (chevron toggles a one-at-a-time inline peek), FND-05-e (row-click vs chevron gesture split), FND-05-f/-g (density toggle + localStorage persist):
    - Clicking the always-visible chevron mounts that row's `renderExpanded` peek; clicking again unmounts it.
    - Opening row B auto-collapses row A (accordion).
    - Clicking a non-chevron cell fires `onRowClick(row)` once; clicking the chevron does NOT fire `onRowClick`.
    - Density toggle swaps row-height/padding class only (font-size class constant) and persists to `localStorage["datatable-density"]`; a pre-seeded compact value starts compact.
  </behavior>
  <action>Extend `components/ui/DataTable.tsx`.

Accordion expand state (03-RESEARCH.md Pattern 3 accordion override): hold `const [expanded, setExpanded] = useState&lt;ExpandedState&gt;({})` and pass a `handleAccordionExpand` to `onExpandedChange` that enforces one-at-a-time — when the next expanded set has more than one key, keep only the newly-added key (locked: one-at-a-time accordion). Render an always-visible chevron control at the LEFT of every row (lucide-react `ChevronRight` rotating to `ChevronDown` when expanded) wired to `row.getToggleExpandedHandler()`; the chevron must be visible on every row (not hover-only — locked, projector affordance). The chevron's onClick MUST `stopPropagation()` so it does not also trigger the row click.

Row-click gesture split (locked: two distinct gestures): put the `onClick={() => onRowClick?.(row)}` on the row's main clickable area (the cells), and ensure the chevron button's click is isolated (stopPropagation as above) so a chevron click expands but never calls `onRowClick`, and a click anywhere else calls `onRowClick` but never toggles expand.

Inline peek: when `row.getIsExpanded()`, render `renderExpanded?.(row)` inside an `AnimatePresence` + `motion.div` from `@/components/ui/motion`. Call `const safeExpand = useSafeVariants(fadeIn)` ONCE at component scope (never inside the row map — Pitfall 4 / Rules of Hooks) and reference `safeExpand.hidden`/`safeExpand.visible` per row via `initial`/`animate`/`exit`. The peek is the glance/quick-summary slot only — the primitive owns the expand/collapse mechanism + animation; it must NOT render any page-specific deep detail itself (locked two-layer contract: peek = glance, slide-in panel = deep dive, wired later). The peek content sits inside the same outer `data-index` wrapper so `measureElement` reflows the row on expand.

Density (03-RESEARCH.md Pattern 5): `type Density = "comfortable" | "compact"`; `const DENSITY_KEY = "datatable-density"`. Initialize `const [density, setDensity] = useState&lt;Density&gt;(() => typeof window === "undefined" ? "comfortable" : (localStorage.getItem(DENSITY_KEY) as Density) ?? "comfortable")` (Comfortable is the default — locked). A `toggleDensity` flips comfortable↔compact and writes the new value to `localStorage`. Density changes ROW HEIGHT + PADDING ONLY via a class map (e.g. comfortable `py-3` / compact `py-1.5`, and the `estimateSize` 72 vs 48 from Task 1) — font-size classes stay constant across both so text never degrades at projector distance (locked). Feed `density` into the Task 1 `estimateSize`.

Density toggle control: render it in a table toolbar pinned top-right of the shell (small, always-visible — NOT in an overflow menu; locked: demoable live). A simple two-state toggle/segmented control labeled Comfortable/Compact is sufficient.

Commit this task GREEN: `feat(03-02): accordion expand, row-click gesture split, density toggle + persistence`.</action>
  <verify>
    <automated>npm test -- DataTable 2>&1 | tail -8; npx tsc --noEmit</automated>
  </verify>
  <done>FND-05-c/-d/-e/-f/-g tests pass: chevron toggles a single inline peek (accordion), non-chevron click fires `onRowClick` while chevron click does not, density toggles row-height/padding class only with font-size constant and persists to localStorage; animations route through `@/components/ui/motion` with `useSafeVariants` called once at component scope; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement two-message empty state + Clear-filters slot; finalize phase gates</name>
  <files>components/ui/DataTable.tsx</files>
  <behavior>
    Drives GREEN for FND-05-h/-i and the no-WebGL guard FND-05-l:
    - `data` empty + `hasActiveFilter={false}` → centered icon + "no data yet" message (override via `emptyMessage`).
    - `data` empty + `hasActiveFilter={true}` → "no results — try adjusting your filters" message (override via `filteredEmptyMessage`) + a Clear-filters control that calls `onClearFilters` once.
    - DataTable.tsx contains zero WebGL/canvas imports (guard passes).
  </behavior>
  <action>Extend `components/ui/DataTable.tsx` with the empty-state branch (locked decision): when `rows.length === 0`, render a centered subtle icon (lucide-react default, e.g. an inbox/table glyph — Claude's discretion) + a one-line message in place of the virtualized body. Choose the message by `hasActiveFilter`: false → no-data message (default text overridable by `emptyMessage`); true → filtered message (default text overridable by `filteredEmptyMessage`) PLUS a "Clear filters" button that calls `onClearFilters?.()`. The primitive only EXPOSES the action (the page wires the actual reset in Phase 4) — do not implement any store reset here. The icon/message are overridable per page via the props/slots above (locked).

Finalize: ensure NO import from `three`, `@react-three/fiber`, `@react-three/drei`, `@cosmos.gl`, or any canvas/WebGL library appears anywhere in DataTable.tsx — neither in code nor comments — so the Plan 01 import guard (FND-05-l) passes. If DataTable.tsx has grown past ~300 lines, extract the virtualizer wiring into a sibling `components/ui/useDataTableVirtualizer.ts` (03-RESEARCH.md §File Structure) and add it to this plan's files_modified — otherwise keep it one file.

Run the full repo gates: `npm test` (the whole suite must hold — the Phase 2 baseline plus the new DataTable tests) and `npx tsc --noEmit` exits 0 (whole-tree typecheck including the new test file — repo hard rule). Commit GREEN: `feat(03-02): two-message empty state + clear-filters slot; phase gates green`.</action>
  <verify>
    <automated>npm test -- DataTable 2>&1 | tail -8; npx tsc --noEmit && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>FND-05-h/-i pass (both empty-state messages render with overrides; Clear-filters calls `onClearFilters`); FND-05-l import guard passes (no WebGL/canvas imports); full `npm test` suite holds at or above the Phase 2 baseline; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → DataTable props | Pages pass `data`, `columns`, and callbacks; the primitive renders them but performs no fetching, no network, and no privileged action. All inputs are already-authorized in-memory client data. |
| DataTable → localStorage | The density preference is read/written to `localStorage` (non-sensitive UI state only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | localStorage `datatable-density` value | accept | UI-only preference; a tampered value falls through the `?? "comfortable"` default; cannot affect data or auth. No PII stored. |
| T-03-02 | Information Disclosure | `renderExpanded` peek content | accept | The primitive renders only what the caller passes; the locked two-layer contract keeps the peek to a glance summary. Deep/sensitive detail is the page's responsibility in the slide-in panel (Phase 4/6), out of this primitive's scope. |
| T-03-03 | Denial of Service | very large `data` arrays | mitigate | Virtualization (`@tanstack/react-virtual`) renders only visible rows + overscan, bounding DOM size regardless of dataset length. |
| T-03-SC | Tampering | npm install (`@tanstack/react-table`) | mitigate | Package APPROVED in 03-RESEARCH.md Package Legitimacy Audit (TanStack/table, current 8.21.3, ~4M downloads/wk). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking human checkpoint required. |
</threat_model>

<verification>
- `npm test -- DataTable` — all twelve FND-05 assertions GREEN (a..l).
- `npx tsc --noEmit` exits 0 — whole-tree typecheck including DataTable.tsx and DataTable.test.tsx (repo deploy-blocking rule).
- `npm test` — full suite holds at or above the Phase 2 baseline (no regressions).
- `git diff --name-only` touches ONLY components/ui/DataTable.tsx (and optionally components/ui/useDataTableVirtualizer.ts) — ZERO files under app/(dashboard)/users/ or app/(dashboard)/template-mty/ (scope boundary: no page wiring this phase).
- Grep confirms `useReactTable`, `useVirtualizer`, and `from "@/components/ui/motion"` present; grep confirms NO `import ... from "framer-motion"` direct, NO `useTheme(`, and NO `three`/`@react-three`/`@cosmos.gl` import in DataTable.tsx.
</verification>

<success_criteria>
- `components/ui/DataTable.tsx` renders a virtualized body, frosted-glass sticky header, single-column 3-state sort with arrow + active-header highlight, left-pinned column with scroll-triggered shadow, `onRowClick` row-click, always-visible chevron → one-at-a-time inline expand peek, and a top-right density toggle — typed with `ColumnDef<T>` generics.
- Density persists in localStorage; Compact changes height + padding only (font constant); sort resets to `defaultSort` each mount.
- Empty state distinguishes no-data vs no-results with overridable icon/message and a Clear-filters action slot.
- Renders in light + dark via CSS-var tokens (no `useTheme()`), introduces no new WebGL context; all motion via `@/components/ui/motion`.
- All Plan 01 tests GREEN; `npx tsc --noEmit` exits 0; full suite holds.
- No page wiring — primitive only.
</success_criteria>

<output>
Create `.planning/phases/03-datatable-primitive/03-02-SUMMARY.md` when done.
</output>
