---
phase: 06-template-mty-forma-proposal-polish
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/(dashboard)/forma-proposal/components/FormaParticleAccent.tsx
  - app/(dashboard)/forma-proposal/components/HierarchyViewSkeleton.tsx
  - app/(dashboard)/forma-proposal/loading.tsx
  - app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx
autonomous: true
requirements: [FRM-01, FRM-02]
must_haves:
  truths:
    - "HierarchyView is dynamically imported (ssr:false) with a layout-shaped skeleton fallback; the d3 bundle is prefetched in idle background"
    - "A faint full-bleed real-3D particle accent renders behind the editor with frameloop=demand and pointer-events:none"
    - "The accent introduces NO WebGL context on a data surface — exactly one Canvas exists under forma-proposal and it sits behind the editor"
    - "Outer containers (role rail, editor section, hierarchy view) have restrained PremiumSurface depth; folder-tree rows and tier chips stay flat"
    - "Navigating to /forma-proposal shows a rail+tree-shaped skeleton within ~200ms (route loading.tsx)"
  artifacts:
    - path: "app/(dashboard)/forma-proposal/components/FormaParticleAccent.tsx"
      provides: "R3F drifting-particle background accent (clone of HeaderParticleAccent, lower intensity)"
      contains: "frameloop=\"demand\""
    - path: "app/(dashboard)/forma-proposal/components/HierarchyViewSkeleton.tsx"
      provides: "Layout-shaped skeleton shown while the HierarchyView d3 bundle loads"
      min_lines: 8
    - path: "app/(dashboard)/forma-proposal/loading.tsx"
      provides: "Route-level Suspense skeleton (rail + tree placeholders) for PERF-01 ~200ms"
      min_lines: 10
    - path: "app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx"
      provides: "Editor with dynamic HierarchyView, particle accent layer, PremiumSurface depth, idle prefetch"
      contains: "dynamic("
  key_links:
    - from: "app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx"
      to: "app/(dashboard)/forma-proposal/components/HierarchyView.tsx"
      via: "const HierarchyView = dynamic(() => import('./HierarchyView').then(m=>m.HierarchyView), { ssr:false, loading: HierarchyViewSkeleton })"
      pattern: "dynamic\\("
    - from: "app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx"
      to: "app/(dashboard)/forma-proposal/components/FormaParticleAccent.tsx"
      via: "dynamic ssr:false import, rendered as absolute inset-0 pointer-events-none z-0 background layer"
      pattern: "FormaParticleAccent"
---

<objective>
Wire the /forma-proposal editor up to the premium look: dynamically import the (now-split) `HierarchyView` with `ssr:false` and a layout-shaped skeleton, prefetch the d3 bundle in idle background after first paint, add a faint full-bleed real-3D particle accent behind the editor (`frameloop="demand"`, `pointer-events:none`, off the data region), apply restrained `PremiumSurface` depth to the outer containers only, light-polish the top bar, and add the missing route `loading.tsx`.

Purpose: the wiring half of FRM-01 (deferred d3 via dynamic(ssr:false) + idle prefetch) and all of FRM-02 (PremiumSurface depth + selective real-3D background accent + skeleton). Depends on 06-02 (needs the split `HierarchyView` shell to defer). Runs in Wave 2 parallel with the /template-mty DataTable migration (different files).

Output: A `FormaParticleAccent`, a `HierarchyViewSkeleton`, a route `loading.tsx`, and a rewired `FormaProposalClient`.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-template-mty-forma-proposal-polish/06-CONTEXT.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-RESEARCH.md

# The editor to rewire:
@app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx
# The canonical R3F accent to clone (verified config constants):
@app/(dashboard)/users/HeaderParticleAccent.tsx
# How /users wires the R3F accent via dynamic ssr:false (pattern to mirror):
@app/(dashboard)/users/UsersTableHeader.tsx
# Route loading.tsx pattern:
@app/(dashboard)/users/loading.tsx
# The outer containers to wrap (DO NOT wrap their inner rows):
@app/(dashboard)/forma-proposal/components/RoleRail.tsx

<interfaces>
PremiumSurface — `components/ui/PremiumSurface.tsx`:
```typescript
import { PremiumSurface } from "@/components/ui/PremiumSurface";
// variant: "base" | "float" | "glass" | "inset" (default "base"); RSC-safe.
// CONTEXT.md FRM-02: outer containers only — role rail / editor section / hierarchy view.
// Top bar gets "float" light polish; folder-tree rows + tier chips stay FLAT.
```
HeaderParticleAccent config to clone (from app/(dashboard)/users/HeaderParticleAccent.tsx) —
tune DOWN for a barely-there full-bleed background per CONTEXT.md + RESEARCH:
  PARTICLE_COUNT 180 → ~100-120
  SPREAD_X/Y wider/taller for full-bleed
  DRIFT_SPEED slower
  PARTICLE_COLOR 0x6366f1 (brand indigo — KEEP)
  pointsMaterial opacity 0.35 → ~0.18 (barely-there)
  Canvas: frameloop="demand", gl powerPreference "low-power", style position:absolute inset:0 pointerEvents:"none" zIndex:0
HierarchyView public API is unchanged (8 props) after the 06-02 split. The call site in
FormaProposalClient passes all 8 props today — keep them identical; only the IMPORT becomes dynamic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FormaParticleAccent + HierarchyViewSkeleton + forma-proposal loading.tsx</name>
  <files>app/(dashboard)/forma-proposal/components/FormaParticleAccent.tsx, app/(dashboard)/forma-proposal/components/HierarchyViewSkeleton.tsx, app/(dashboard)/forma-proposal/loading.tsx</files>
  <action>
Create `FormaParticleAccent.tsx` ("use client") as a clone of `app/(dashboard)/users/HeaderParticleAccent.tsx`, default-exported (so it can be `dynamic(() => import("./FormaParticleAccent"), { ssr:false })`). Copy the `ParticleField` inner component and the `Canvas` setup verbatim, then tune for a barely-there full-bleed background per RESEARCH "FormaParticleAccent" recipe: `PARTICLE_COUNT ≈ 110`, wider `SPREAD_X`/taller `SPREAD_Y` for full-bleed coverage, slower `DRIFT_SPEED`, keep `PARTICLE_COLOR = new THREE.Color(0x6366f1)` (brand indigo), set `pointsMaterial` opacity to ~0.18. Canvas MUST keep `frameloop="demand"`, `gl={{ antialias:false, alpha:true, powerPreference:"low-power" }}`, and `style={{ position:"absolute", inset:0, pointerEvents:"none", zIndex:0 }}`. Keep the demand-mode invalidate ticker (the `setInterval(() => invalidate(), N)` gentle-drift driver) from the source so the field drifts. This is the ONLY file under forma-proposal that may import `@react-three/fiber` / `Canvas` (RESEARCH Pitfall 1 / PERF-05).

Create `HierarchyViewSkeleton.tsx` ("use client" not required — pure markup): a layout-shaped skeleton matching the hierarchy view (a faint full-bleed container with a few rounded `bg-muted/20 animate-pulse` node placeholders and connector-line hints), consistent with the page's other skeletons. Export it named so it can be used as the `loading:` fallback and (optionally) reused by the route loader.

Create `app/(dashboard)/forma-proposal/loading.tsx` as a default-exported RSC route skeleton shaped like the editor: a left rail placeholder column + a right tree/canvas placeholder area (rounded `bg-muted/20 animate-pulse` blocks), replacing the plain "Loading your draft…" feel at the route level (PERF-01 ~200ms; CONTEXT.md FRM-02 loading decision). Mirror the container/structure conventions of `app/(dashboard)/users/loading.tsx`.

Run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && grep -rln "from \"@react-three/fiber\"\|Canvas" "app/(dashboard)/forma-proposal" | sort -u</automated>
  </verify>
  <done>`FormaParticleAccent.tsx` (default export, frameloop=demand, pointer-events:none, low opacity, indigo) is the ONLY forma-proposal file importing Canvas/@react-three/fiber; `HierarchyViewSkeleton.tsx` and `loading.tsx` exist as layout-shaped skeletons; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire FormaProposalClient — dynamic HierarchyView + skeleton + idle prefetch + particle layer + PremiumSurface depth + top-bar polish</name>
  <files>app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx</files>
  <action>
In `FormaProposalClient.tsx`:

1. Replace the static `import { HierarchyView } from "./HierarchyView"` with a dynamic import: `const HierarchyView = dynamic(() => import("./HierarchyView").then((m) => m.HierarchyView), { ssr: false, loading: () => <HierarchyViewSkeleton /> })` (import `dynamic` from `next/dynamic` and `HierarchyViewSkeleton` from `./HierarchyViewSkeleton`). The JSX call site keeps passing all 8 props exactly as today (FRM-01; RESEARCH Pitfall 5). This defers the d3 bundle (isolated in `useHierarchyLayout.ts` by 06-02) until hierarchy mode is first shown.

2. Add the idle prefetch (RESEARCH "Idle prefetch pattern"): a `useEffect(() => { ... }, [])` that, after first paint, calls `requestIdleCallback(() => { import("./HierarchyView"); })` with a `setTimeout(..., 2000)` fallback when `requestIdleCallback` is undefined, and cleans up with `cancelIdleCallback`/`clearTimeout`. This makes the first switch to hierarchy mode feel instant.

3. Add the particle accent as a background layer: import `FormaParticleAccent` via `dynamic(() => import("./FormaParticleAccent"), { ssr:false })`. Make the outermost container `relative` and render `<FormaParticleAccent />` as the first child (it is `position:absolute inset-0 pointer-events-none z-0`). Wrap the existing editor content so it sits ABOVE the accent: the content region must be `relative z-[1]` (RESEARCH Pitfall 1 — the accent sits BEHIND, off the data; the editor renders above it). Do NOT place the Canvas inside the scrollable editor/folder-tree region.

4. Apply RESTRAINED PremiumSurface depth to OUTER containers only (CONTEXT.md FRM-02):
   - the role rail outer container, the editor `<section>`, and the hierarchy-view mode container get subtle elevation. Prefer wrapping each outer container in `<PremiumSurface variant="glass">` (rail) / `variant="base"` (editor section + hierarchy container) OR apply the equivalent panel class — choose whichever keeps the existing flex/min-h-0/overflow layout intact. Keep `min-h-0`, `flex-1`, and the existing scroll containers working (do not break the editor's internal scroll).
   - The top bar `<header>` gets LIGHT polish only — `variant="float"` or a subtle shadow token, staying compact (CONTEXT.md: do not rebuild it into a tall premium header). Keep the existing ModeSwitch / JSON / CSV / Reset buttons and the `barButton` style.
   - Folder-tree rows and tier chips stay FLAT and dense — do NOT wrap `FolderTreeAssign` rows, the tier legend footer chips, or individual RoleRail items in any surface. Premium is on the outer shell, speed on the inside.

5. Keep the `if (!d.hydrated)` branch but you MAY leave its inline "Loading your draft…" text as-is (the route `loading.tsx` from Task 1 covers the ~200ms PERF-01 target; the hydration branch is a brief client gate). At Claude's discretion, swap that inline text for `<HierarchyViewSkeleton />`-style placeholders for consistency — optional, not required.

Run `npx tsc --noEmit` (whole tree) and grep-confirm exactly one Canvas under forma-proposal. Do NOT modify any HierarchyView split files (06-02 owns them) beyond the import line.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && test "$(grep -rln "Canvas" "app/(dashboard)/forma-proposal" | wc -l)" = "1" && echo "OK exactly-one-canvas" || echo "FAIL canvas-count"</automated>
  </verify>
  <done>HierarchyView is `dynamic(ssr:false)` with a HierarchyViewSkeleton fallback; an idle-prefetch useEffect imports the bundle after first paint; FormaParticleAccent renders as an absolute inset-0 pointer-events-none z-0 layer with editor content at z-[1]; outer containers (rail/editor/hierarchy) have restrained PremiumSurface depth while folder-tree rows + tier chips stay flat; top bar has light float polish; exactly one Canvas exists under forma-proposal; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The editor operates on a local draft (useFormaDraft, localStorage) already present; no new API, network, or auth boundary. The R3F accent is decorative and non-interactive (pointer-events:none). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | FormaParticleAccent (GPU) | mitigate | frameloop="demand" + ~110 particles + low-power + opacity 0.18 keeps GPU < 400MB (PERF-05). Accent sits behind the editor (pointer-events:none, off the data region) — introduces no WebGL context on a data surface. |
| T-06-05 | Tampering | npm/pip/cargo installs | accept | No installs (@react-three/fiber@9 + three already present from Phase 4). RESEARCH: "Installation: None required". |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rln "Canvas" app/(dashboard)/forma-proposal` returns exactly ONE file (`FormaParticleAccent.tsx`) — PERF-05 / Pitfall 1 guardrail.
- FormaProposalClient imports HierarchyView via `dynamic(ssr:false, loading: HierarchyViewSkeleton)` and has an idle-prefetch `useEffect`.
- Manual (Phase 7): switching to hierarchy mode shows the skeleton then the canvas (first switch feels instant via prefetch); the particle field drifts faintly behind the editor and never blocks clicks; outer containers have depth, folder rows stay flat; route shows a rail+tree skeleton on navigation; GPU < 400MB in Chrome DevTools.
</verification>

<success_criteria>
- HierarchyView deferred via `dynamic(ssr:false)` with a layout-shaped skeleton + idle prefetch (FRM-01 wiring).
- Faint full-bleed real-3D accent behind the editor: `frameloop="demand"`, `pointer-events:none`, indigo, GPU-budget safe (FRM-02).
- NO new WebGL context on a data surface — exactly one Canvas under forma-proposal, sitting behind the editor (PERF-05).
- Restrained PremiumSurface depth on outer containers only; folder rows + tier chips stay flat; top bar lightly polished.
- `app/(dashboard)/forma-proposal/loading.tsx` exists; `npx tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/06-template-mty-forma-proposal-polish/06-04-SUMMARY.md` when done.
</output>
