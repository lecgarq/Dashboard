---
phase: 06-template-mty-forma-proposal-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/forma-proposal/components/useHierarchyLayout.ts
  - app/(dashboard)/forma-proposal/components/HierarchyCanvas.tsx
  - app/(dashboard)/forma-proposal/components/HierarchyView.tsx
  - app/(dashboard)/forma-proposal/components/__tests__/HierarchyView.test.tsx
autonomous: true
requirements: [FRM-01]
must_haves:
  truths:
    - "HierarchyView still exports a function with the exact same 8-prop public signature as before"
    - "The d3-hierarchy stratify+tree layout computation lives in a separate useHierarchyLayout hook"
    - "The render + zoom/pan lives in HierarchyCanvas, the heavy d3 import is isolated to the layout hook"
    - "A test proves the thin shell renders and forwards all 8 props to the canvas"
  artifacts:
    - path: "app/(dashboard)/forma-proposal/components/useHierarchyLayout.ts"
      provides: "d3-hierarchy layout hook: collapsed state, stratify+tree, bbox, toggle, fit"
      contains: "d3-hierarchy"
    - path: "app/(dashboard)/forma-proposal/components/HierarchyCanvas.tsx"
      provides: "Pure SVG/DOM render + zoom/pan/role-picker/controls"
      min_lines: 100
    - path: "app/(dashboard)/forma-proposal/components/HierarchyView.tsx"
      provides: "Thin shell — unchanged 8-prop public API, composes hook + canvas"
      contains: "useHierarchyLayout"
    - path: "app/(dashboard)/forma-proposal/components/__tests__/HierarchyView.test.tsx"
      provides: "Test asserting the shell renders with all 8 props"
      min_lines: 15
  key_links:
    - from: "app/(dashboard)/forma-proposal/components/HierarchyView.tsx"
      to: "app/(dashboard)/forma-proposal/components/useHierarchyLayout.ts"
      via: "shell calls useHierarchyLayout(index, explicit) and spreads result into canvas"
      pattern: "useHierarchyLayout"
    - from: "app/(dashboard)/forma-proposal/components/HierarchyView.tsx"
      to: "app/(dashboard)/forma-proposal/components/HierarchyCanvas.tsx"
      via: "shell renders <HierarchyCanvas {...layout} {...props} /> forwarding all 8 public props"
      pattern: "HierarchyCanvas"
---

<objective>
Split the 316-line `HierarchyView.tsx` into three files — a `useHierarchyLayout` hook (d3-hierarchy layout computation), a `HierarchyCanvas` render component (SVG/DOM + zoom/pan + role picker + controls), and a thin `HierarchyView` shell that composes them — while keeping the public 8-prop API byte-for-byte unchanged. Add a unit test proving the shell forwards all 8 props.

Purpose: FRM-01 (HierarchyView split with public API unchanged; heavy d3 bundle isolated so it can be deferred via `dynamic(ssr:false)` in the next wave). This plan produces ONLY the split; the `dynamic(ssr:false)` wiring + loading skeleton live in plan 06-04 (FormaProposalClient wiring) so the split lands first and is independently testable. Self-contained in the HierarchyView files + a new test — no shared-file conflict with the /template-mty work, so it runs parallel in Wave 1.

Output: Three split files with the public API preserved, plus a HierarchyView shell test.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-template-mty-forma-proposal-polish/06-CONTEXT.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-RESEARCH.md

# The 316-line file to split — split boundary is documented in RESEARCH.md "HierarchyView Split — Exact Boundary (FRM-01)":
@app/(dashboard)/forma-proposal/components/HierarchyView.tsx

# Caller (DO NOT edit in this plan — it stays importing { HierarchyView } statically; the dynamic-import swap is plan 06-04):
@app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx

<interfaces>
Public API that MUST remain unchanged (the 8 props the caller passes):
```typescript
export function HierarchyView(props: {
  index: FolderIndex;
  explicit: ExplicitMap;
  rootLabel: string;
  roles: FormaRole[];
  activeRoleId: string;
  activeRoleLabel: string;
  onPickRole: (roleId: string) => void;
  onSetTier: (folderId: string, tier: FormaTier) => void;
  onApplySubtree: (folderId: string, tier: FormaTier) => void;
  onClear: (folderId: string) => void;
}): JSX.Element
```
Hook signature to create:
```typescript
// useHierarchyLayout.ts
export function useHierarchyLayout(index: FolderIndex, explicit: ExplicitMap): {
  nodes: VNode[];      // VNode { id, x, y, depth }
  links: VLink[];      // VLink { id, sx, sy, tx, ty }
  bbox: { minX: number; maxX: number; minY: number; maxY: number };
  collapsed: Set<string>;
  toggle: (id: string) => void;
  fit: () => void;     // NOTE: fit needs the containerRef which lives in the canvas; see action for the seam
};
```
Types/consts that move with the hook: VNode, VLink, curve(), initialCollapsed(), sortFolders(), ROOT, NODE_W, NODE_H, MIN_SCALE, MAX_SCALE, clamp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the HierarchyView shell test (asserts render + all 8 props forwarded)</name>
  <files>app/(dashboard)/forma-proposal/components/__tests__/HierarchyView.test.tsx</files>
  <behavior>
    - Test: rendering `<HierarchyView {...all 8 props} />` with a minimal FolderIndex (one root folder, empty explicit map) mounts without throwing and renders the role-picker "Viewing" label with `activeRoleLabel`.
    - Test (prop-forward proof): the active role label passed in appears in the DOM (proves activeRoleLabel reaches the canvas), and the root label appears (proves rootLabel reaches the canvas via the root OrgNode).
    - Use a tiny hand-built FolderIndex fixture: build via the same shape `useFormaDraft`/inheritance produces — `{ byId: Map, childrenOf: Map, roots: FormaFolder[] }`. If constructing a FolderIndex by hand is fragile, import the real index builder from `@/lib/forma/inheritance` and feed it one folder. Keep `roles` to a single FormaRole so the picker renders.
    - This test starts RED (file split not done yet) only in the sense that it exercises the new shell; since the public API is unchanged it should also pass against the pre-split file — that is acceptable. The intent is to lock the public contract so the split cannot silently break it.
  </behavior>
  <action>
Create `app/(dashboard)/forma-proposal/components/__tests__/HierarchyView.test.tsx` with `// @vitest-environment jsdom`. Render the existing `HierarchyView` with a minimal but valid `FolderIndex` (build it from `@/lib/forma/inheritance` helpers if a builder exists, e.g. a `buildFolderIndex`/`indexFolders`-style function — check the inheritance module's exports; otherwise construct the `{ byId, childrenOf, roots }` shape by hand for one root folder). Pass all 8 props with no-op handlers and a single `FormaRole`. Assert the component mounts and that `activeRoleLabel` and `rootLabel` text appear in the DOM. Stub `ResizeObserver` and any non-jsdom APIs the component needs (mirror the stubbing pattern used in existing template-mty graph/component tests). Keep it small (one or two `it()` blocks).

Run the test against the CURRENT (un-split) HierarchyView first to confirm the fixture is valid and the assertions pass — this proves the contract before the split, so any post-split breakage is caught.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/forma-proposal/components/__tests__/HierarchyView.test.tsx</automated>
  </verify>
  <done>Test file exists, mounts HierarchyView with all 8 props, asserts activeRoleLabel + rootLabel appear, and passes against the current file; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useHierarchyLayout hook + HierarchyCanvas render, leaving a thin HierarchyView shell</name>
  <files>app/(dashboard)/forma-proposal/components/useHierarchyLayout.ts, app/(dashboard)/forma-proposal/components/HierarchyCanvas.tsx, app/(dashboard)/forma-proposal/components/HierarchyView.tsx</files>
  <action>
Follow the exact split boundary in RESEARCH.md ("HierarchyView Split — Exact Boundary").

Create `useHierarchyLayout.ts` ("use client"): move the `import { stratify, tree } from "d3-hierarchy"` here (this is the heavy bundle to isolate), plus the constants `ROOT`, `NODE_W`, `NODE_H`, `MIN_SCALE`, `MAX_SCALE`, `clamp`, the helpers `sortFolders`, `initialCollapsed`, `curve`, and the `VNode`/`VLink` types. Export a `useHierarchyLayout(index, explicit)` hook that owns the `collapsed` state + `toggle` callback and the `useMemo` that walks visible folders → `stratify` → `tree` → `{ nodes, links, bbox }`. Re-export `VNode`, `VLink`, `ROOT`, `NODE_W`, `NODE_H`, `MIN_SCALE`, `MAX_SCALE`, `clamp`, `curve` so the canvas can import what it needs.

The `fit` callback depends on `containerRef` (a DOM ref) and `bbox`. Keep the `fit` LOGIC in the canvas (it needs the live container size), but pass `bbox` from the hook. So the hook returns `{ nodes, links, bbox, collapsed, toggle }`; the canvas computes `fit` locally from `bbox` + its own `containerRef`. (Do not try to lift containerRef into the hook — the ref must live where the DOM node is.) Update the hook return type and the shell accordingly.

Create `HierarchyCanvas.tsx` ("use client"): move EVERYTHING render-and-interaction — `containerRef`, `view` state + `setView`, the wheel zoom/pan effect, the `pan` ref + pointermove/pointerup effect, `zoomBy`, `fit` (computed from the `bbox` prop + containerRef), `rolesByGroup`/`groupOrder` memos, the `TierMenuItems` component, and the full JSX tree (background grid div, SVG links, node divs with dropdown tier menus, role picker, hint, zoom controls). Its props are: `nodes, links, bbox, collapsed, onToggle` (from the hook) plus the 8 public props `index, explicit, rootLabel, roles, activeRoleId, activeRoleLabel, onPickRole, onSetTier, onApplySubtree, onClear`. Import `VNode, VLink, ROOT, NODE_W, NODE_H, MIN_SCALE, MAX_SCALE, clamp, curve` from `./useHierarchyLayout`. Keep all the lucide-react, dropdown-menu, OrgNode, inheritance, and tiers imports it actually uses.

Rewrite `HierarchyView.tsx` ("use client") as the thin shell: same 8-prop signature, body is `const layout = useHierarchyLayout(props.index, props.explicit); return <HierarchyCanvas nodes={layout.nodes} links={layout.links} bbox={layout.bbox} collapsed={layout.collapsed} onToggle={layout.toggle} {...props} />;`. Pass ALL 8 public props through (RESEARCH Pitfall 5 — partial forwarding breaks the call site). Keep the exported name `HierarchyView` and the same prop type so `FormaProposalClient.tsx` compiles unchanged.

Run `npx tsc --noEmit` (whole tree, including the caller and the new test) and the HierarchyView test. Both must be clean. Confirm `FormaProposalClient.tsx` was NOT modified (`git diff --name-only` must not list it).
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/forma-proposal/components/__tests__/HierarchyView.test.tsx && (git diff --name-only | grep -q "forma-proposal/components/FormaProposalClient.tsx" && echo "FAIL client-touched" || echo "OK client-untouched")</automated>
  </verify>
  <done>Three files exist with the d3-hierarchy import only in `useHierarchyLayout.ts`; HierarchyView is a thin shell forwarding all 8 props to HierarchyCanvas; the shell test passes; `npx tsc --noEmit` exits 0; `FormaProposalClient.tsx` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side refactor of a layout/render component. No data source, API, auth, or input boundary changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | npm/pip/cargo installs | accept | No package installs (d3-hierarchy already present at ^3.1.2). RESEARCH: "Installation: None required". |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (validates the public API is unchanged at the call site).
- `grep -rn "from \"d3-hierarchy\"" app/(dashboard)/forma-proposal/components` returns ONLY `useHierarchyLayout.ts`.
- HierarchyView shell test passes.
- `git diff --name-only` does NOT include `FormaProposalClient.tsx`.
</verification>

<success_criteria>
- HierarchyView is a thin shell with the exact same 8-prop public signature.
- d3-hierarchy (stratify+tree) is isolated in `useHierarchyLayout.ts`.
- Render + zoom/pan lives in `HierarchyCanvas.tsx`.
- The shell test proves all 8 props are forwarded.
- `npx tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/06-template-mty-forma-proposal-polish/06-02-SUMMARY.md` when done.
</output>
