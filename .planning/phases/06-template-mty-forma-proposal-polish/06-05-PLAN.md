---
phase: 06-template-mty-forma-proposal-polish
plan: 05
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - app/(dashboard)/template-mty/components/RoleSimilarityGraph.tsx
  - app/(dashboard)/template-mty/components/RoleOverviewSheet.tsx
  - app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx
  - app/(dashboard)/template-mty/__tests__/RoleSimilarityGraph.test.tsx
autonomous: true
requirements: [TPL-02]
must_haves:
  truths:
    - "The role-similarity graph settles on mount then freezes (no perpetual ticking); dragging a node reheats then re-freezes"
    - "A press with no real movement opens a slide-in role overview; a press-and-drag still moves the node"
    - "Node hover labels and the hover tooltip card never clip past the panel edge (clamped in-bounds)"
    - "The role overview shows role name, folders reached, tier breakdown, and the members who hold the role; member rows click through to the shared UserProfilePanel"
    - "Under prefers-reduced-motion the graph renders already-settled (no animated jiggle)"
  artifacts:
    - path: "app/(dashboard)/template-mty/components/RoleSimilarityGraph.tsx"
      provides: "Settle-and-freeze sim + click-vs-drag + in-bounds label/tooltip clamping + onNodeClick callback"
      contains: "onNodeClick"
    - path: "app/(dashboard)/template-mty/components/RoleOverviewSheet.tsx"
      provides: "DrillSheet role overview with clickable member rows (no new query)"
      contains: "DrillSheet"
    - path: "app/(dashboard)/template-mty/__tests__/RoleSimilarityGraph.test.tsx"
      provides: "Tests: empty state + node-click fires onNodeClick after sub-threshold movement"
      min_lines: 15
  key_links:
    - from: "app/(dashboard)/template-mty/components/RoleSimilarityGraph.tsx"
      to: "app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx"
      via: "RoleSimilarityGraph calls onNodeClick(roleId); the parent hoists selectedRoleId and renders RoleOverviewSheet"
      pattern: "onNodeClick"
    - from: "app/(dashboard)/template-mty/components/RoleOverviewSheet.tsx"
      to: "app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx"
      via: "member-row click calls back into the parent's setProfileEmail (same AuthorProfileDrawer drill target)"
      pattern: "onMemberClick"
---

<objective>
Upgrade the /template-mty role-similarity graph (TPL-02 graph half): make the d3-force simulation settle-then-freeze (no perpetual ticking), distinguish a click from a drag by a movement threshold (click → drill, drag → move), clamp hover labels and the tooltip card so they never clip past the panel edge, and on click open a slide-in `RoleOverviewSheet` (role name, folders reached, tier breakdown, members list) whose member rows click through to the same shared `UserProfilePanel` the table uses — all from data already on the page (no new query, NA-01). Honor reduced-motion by rendering already-settled.

Purpose: TPL-02 graph requirements — non-clipped hover labels and node-click → slide-in role members. Depends on 06-03 because both edit `TemplateAnalysisCharts.tsx` (the parent that hoists drill state); this is Wave 3 to avoid the file conflict.

Output: An upgraded `RoleSimilarityGraph`, a new `RoleOverviewSheet`, the parent wiring in `TemplateAnalysisCharts`, and an updated graph test.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-template-mty-forma-proposal-polish/06-CONTEXT.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-RESEARCH.md

# Current graph (244 lines) — physics, hover, pointer state mapped in RESEARCH "RoleSimilarityGraph — Current State & Required Changes":
@app/(dashboard)/template-mty/components/RoleSimilarityGraph.tsx
# Parent that owns profileEmail + AuthorProfileDrawer (hoist selectedRoleId here, mirror the profileEmail pattern):
@app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx
# Current graph test (extend it):
@app/(dashboard)/template-mty/__tests__/RoleSimilarityGraph.test.tsx
# The slide-in shell to render the overview inside:
@components/ui/DrillSheet.tsx

<interfaces>
DrillSheet — `components/ui/DrillSheet.tsx`:
```typescript
import { DrillSheet } from "@/components/ui/DrillSheet";
interface DrillSheetProps {
  open: boolean; onClose: () => void;
  title?: string; description?: string;
  children: React.ReactNode;
}
// ~480px slide-in from right; the single shared drill shell (INT-01).
```
Graph data types — `app/(dashboard)/template-mty/roleSimilarity.ts`:
```typescript
// SimNode (per node): roleId, roleName, folderCount, maxRank (used today for size+color)
// GraphData: { nodes: SimNode[]; edges: { source; target; weight }[] }
```
Members for the overview come from overview.members (TemplateMember[]) already loaded in
TemplateAnalysisCharts. Filter by role name to get "members who hold this role". NO new query (NA-01).
RoleTreeNode (roleTree prop in the parent) carries per-role tier breakdown (tiers[].label / folders)
— use it for the tier-breakdown section, matched by roleId/roleName.
Reduced motion: import useReducedMotion from framer-motion (or the motion facade) — when true,
stop the sim immediately after creation and render the deterministic circle-seed layout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settle-and-freeze + click-vs-drag + in-bounds clamping on RoleSimilarityGraph (+ test)</name>
  <files>app/(dashboard)/template-mty/components/RoleSimilarityGraph.tsx, app/(dashboard)/template-mty/__tests__/RoleSimilarityGraph.test.tsx</files>
  <behavior>
    - Existing: renders the empty state ("no roles to compare") when nodes is empty — must still pass.
    - New: the component accepts an `onNodeClick?: (roleId: string) => void` prop. When a node is pressed and released with total pointer movement BELOW the click threshold, `onNodeClick` fires with that node's roleId. When movement is at/above the threshold (a drag), `onNodeClick` does NOT fire (the node is repositioned instead).
    - Test approach for the click path: render with a small 2-node graph and a spy `onNodeClick`; fire pointerdown→pointerup on a node `<g>`/`<circle>` with no `pointermove` in between (zero movement) and assert the spy was called with the roleId. Simulate a drag (pointerdown → pointermove beyond threshold → pointerup) and assert the spy was NOT called. d3-force ticking does not need to run for this — the click/drag decision is pure pointer bookkeeping. If the SVG node target is awkward to query, add a stable `data-role-node={roleId}` attribute to each node `<g>` for the test to select.
  </behavior>
  <action>
Apply the four RESEARCH "Required changes for TPL-02" to `RoleSimilarityGraph.tsx`:

1. Settle-and-freeze: add `.on("end", () => sim.stop())` to the simulation so it freezes once it reaches alphaMin (today it ticks forever). On `onNodeDown` (drag start) call `simRef.current?.alphaTarget(0.3).restart()` (already present); on `onUp` after a drag, call `simRef.current?.alphaTarget(0)` and let it coast to the "end" freeze. Do NOT remove the existing `.on("tick", frame)`.

2. Click-vs-drag threshold: extend the `ix` ref to track `totalMovement` (and optionally `downTime`). In `onNodeDown` set `totalMovement = 0` (and `downTime = performance.now()`). In `onMove` (node mode) accumulate `ix.current.totalMovement += Math.hypot(dx, dy)` using the per-event delta. In `onUp`, if mode was "node" and `totalMovement < CLICK_THRESHOLD_PX`, call `onNodeClick?.(node.roleId)` (treat as click) BEFORE releasing fx/fy; otherwise treat as drag (current behavior). Use `CLICK_THRESHOLD_PX = 6` and additionally gate on a short duration (`< 250ms`) per RESEARCH Risk 3 (safer on touch). Keep `e.stopPropagation()` on node pointerdown so a node press never starts a background pan.

3. In-bounds label clamping: clamp the SVG node label position and the hover tooltip card so they never clip past the panel. For the `<text>` label, clamp its `y` to stay within `[12, H - 12]` and its `x` within `[margin, width - margin]` (account for the current `9/v.k` offset). For the hover tooltip card (the absolute-positioned `<div>` at left-3/top-3 today), keep it clamped within the panel bounds (it is already pinned to the top-left corner, which is in-bounds; ensure that if you make it follow the node it uses CSS `clamp()`/min-max so `left`/`top` never exceed `panelWidth - cardWidth` / `panelHeight - cardHeight`). The card must remain `pointer-events:none`.

4. Reduced-motion: read `useReducedMotion()`. When true, after creating the simulation call `sim.stop()` immediately and render the deterministic circle-seed positions as the final layout (no animation) — the seed positions are already computed in the `pnodes` memo, so a stopped sim leaves them in place.

Add the `onNodeClick?: (roleId: string) => void` prop to the component signature. Keep all existing props/behavior (graph, pan, zoom, hover-lit neighbors, reset view) intact. Per Pitfall 6, do NOT add theme/`dark` to the `pnodes`/`pedges` useMemo deps — colors stay read at render time. Add `data-role-node={n.roleId}` to each node `<g>` for testability.

Update `RoleSimilarityGraph.test.tsx`: keep the empty-state test; add the click-fires-onNodeClick (zero movement) and drag-does-not-fire (movement beyond threshold) tests described in <behavior>. Stub `ResizeObserver` if needed. Run `npx tsc --noEmit` and the graph test.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/template-mty/__tests__/RoleSimilarityGraph.test.tsx</automated>
  </verify>
  <done>Sim freezes on "end"; node press with <6px movement (and short duration) fires `onNodeClick(roleId)`, a drag does not; labels + tooltip clamp in-bounds; reduced-motion renders already-settled; graph test (empty + click + drag) passes; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: RoleOverviewSheet + hoist drill state in TemplateAnalysisCharts (members from page data, no new query)</name>
  <files>app/(dashboard)/template-mty/components/RoleOverviewSheet.tsx, app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx</files>
  <action>
Create `RoleOverviewSheet.tsx` ("use client") wrapping `DrillSheet`. Props: `{ open: boolean; onClose: () => void; roleName: string | null; folderCount: number; tiers: Array<{ label: string; count: number }>; members: TemplateMember[]; onMemberClick: (email: string) => void }` (shape to fit the available page data — adjust field names to what TemplateAnalysisCharts can supply from `overview` + `roleTree` + `roleSimilarity`). Render inside `<DrillSheet open title={roleName ?? "Role"}>`: a header with role name + "{folderCount} folders reached", a tier-breakdown section (the tier bars/list — layout is Claude's discretion per CONTEXT.md, reuse the visual language of RoleAccessPie's tier breakdown), and a members list ("members who hold this role"). Each member row is a button that calls `onMemberClick(member.email)` and is non-clickable/de-emphasized when the member has no email (mirror the table rule). Keep it a thin presentational component — all data is passed in.

In `TemplateAnalysisCharts.tsx`, hoist a `selectedRoleId` state (mirror the existing `profileEmail` pattern — RESEARCH Open Q1 recommendation). Pass `onNodeClick={(roleId) => setSelectedRoleId(roleId)}` to `RoleSimilarityGraph`. Derive the overview data for the selected role from already-loaded props (no new query, NA-01):
  - role name + folderCount + tiers from `roleSimilarity.nodes` / `roleTree` matched by roleId,
  - `members` = `overview.members` filtered to those whose `role` matches the selected role name.
Render `<RoleOverviewSheet open={!!selectedRoleId} onClose={() => setSelectedRoleId(null)} ... members={membersForRole} onMemberClick={(email) => { setSelectedRoleId(null); setProfileEmail(email.toLowerCase()); }} />` near the existing `AuthorProfileDrawer`. Member-row click must funnel into the SAME `profileEmail` → `AuthorProfileDrawer` drill (INT-01 — one shared profile target). Closing the role sheet before opening the profile keeps a single sheet visible at a time.

Run `npx tsc --noEmit` and the full template-mty test set. Confirm no new tRPC/useQuery import was added (NA-01) — `grep -n "useQuery\|trpc\." app/(dashboard)/template-mty/components/RoleOverviewSheet.tsx app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx` should show no NEW data fetch for the overview (the existing AuthorProfileDrawer lazy chain is unchanged).
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/template-mty/__tests__/RoleSimilarityGraph.test.tsx</automated>
  </verify>
  <done>`RoleOverviewSheet` renders role name + folders + tier breakdown + members inside DrillSheet; member rows click through via the parent's setProfileEmail into the shared AuthorProfileDrawer; `selectedRoleId` is hoisted in TemplateAnalysisCharts and fed from already-loaded page data (no new query); `npx tsc --noEmit` exits 0 and the template-mty tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Role overview + members are derived entirely from RSC props already loaded on the page (NA-01: no new query). Member-row drill reuses the existing AuthorProfileDrawer lazy chain. No new API, input, or auth boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service | RoleSimilarityGraph (CPU) | mitigate | Settle-and-freeze (`sim.on("end", stop)`) ends perpetual ticking; reheat only on drag. Removes continuous CPU drain (RESEARCH anti-pattern). |
| T-06-07 | Tampering | npm/pip/cargo installs | accept | No installs (d3-force already present). RESEARCH: "Installation: None required". |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test -- --run app/(dashboard)/template-mty/__tests__/RoleSimilarityGraph.test.tsx` passes (empty + click + drag).
- No new tRPC/useQuery for the role overview (NA-01) — overview data is derived from page props.
- Manual (Phase 7): graph settles then freezes; drag moves a node and re-freezes; click (no movement) opens the role overview; member rows open the shared profile; hover labels + tooltip never clip the panel edge; reduced-motion renders already-settled.
</verification>

<success_criteria>
- Graph settles-then-freezes; drag reheats and re-freezes; no perpetual ticking.
- Click-vs-drag threshold: click → slide-in role overview; drag → move node.
- Hover labels + tooltip card clamped in-bounds (non-clipped — the TPL-02 criterion).
- Role overview shows name / folders / tier breakdown / members; member rows → shared UserProfilePanel (INT-01); no new query (NA-01).
- Reduced-motion renders already-settled; `npx tsc --noEmit` exits 0; graph test green.
</success_criteria>

<output>
Create `.planning/phases/06-template-mty-forma-proposal-polish/06-05-SUMMARY.md` when done.
</output>
