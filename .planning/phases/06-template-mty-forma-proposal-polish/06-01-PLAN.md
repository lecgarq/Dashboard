---
phase: 06-template-mty-forma-proposal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/template-mty/loading.tsx
  - app/(dashboard)/template-mty/components/RoleAccessPie.tsx
  - app/(dashboard)/template-mty/components/PermissionAccessChart.tsx
  - app/(dashboard)/template-mty/components/ModuleAccessChart.tsx
autonomous: true
requirements: [TPL-02, TPL-03]
must_haves:
  truths:
    - "Navigating to /template-mty shows a layout-shaped skeleton within ~200ms (route loading.tsx)"
    - "Role / permission / module charts render via the shared themed EChart wrapper and auto-theme in light and dark"
    - "The role pie shows the full donut treatment: per-slice gradient fills, rounded segment ends, hover lift + glow"
    - "Each chart panel and its empty state render inside a PremiumSurface (no plain panel-elevated div remains)"
  artifacts:
    - path: "app/(dashboard)/template-mty/loading.tsx"
      provides: "Route-level Suspense skeleton for /template-mty (PERF-01 ~200ms)"
      min_lines: 10
    - path: "app/(dashboard)/template-mty/components/RoleAccessPie.tsx"
      provides: "Role donut with gradient fills + glow via shared EChart, wrapped in PremiumSurface"
      contains: "@/components/ui/EChart"
    - path: "app/(dashboard)/template-mty/components/PermissionAccessChart.tsx"
      provides: "Permission bar chart via shared EChart, wrapped in PremiumSurface"
      contains: "@/components/ui/EChart"
    - path: "app/(dashboard)/template-mty/components/ModuleAccessChart.tsx"
      provides: "Module bar chart via shared EChart, wrapped in PremiumSurface"
      contains: "@/components/ui/EChart"
  key_links:
    - from: "app/(dashboard)/template-mty/components/RoleAccessPie.tsx"
      to: "components/ui/EChart.tsx"
      via: "import { EChart } from @/components/ui/EChart (shared themed wrapper, not the access-analysis local one)"
      pattern: "@/components/ui/EChart"
    - from: "app/(dashboard)/template-mty/components/PermissionAccessChart.tsx"
      to: "components/ui/PremiumSurface.tsx"
      via: "root wrapper PremiumSurface variant=base replaces panel-elevated"
      pattern: "PremiumSurface"
---

<objective>
Bring the three /template-mty analytics charts and the route loading state up to the shared premium look: migrate all three charts off the old local EChart wrapper onto the shared themed `@/components/ui/EChart` wrapper, apply the full /access-analysis donut treatment to the role pie, wrap every chart panel (and its empty state) in `PremiumSurface`, and add the missing route `loading.tsx` skeleton.

Purpose: TPL-03 (PremiumSurface + skeleton on all panels, PERF-01 ~200ms) and the chart-theming half of TPL-02 (pies use shared depth/glow theming). This is the fastest, lowest-risk win and touches only the three chart components plus a new `loading.tsx` — no shared-file conflict with the table or graph plans, so it runs parallel to the /forma-proposal split in Wave 1.

Output: A `loading.tsx`, three chart components on the shared EChart wrapper with PremiumSurface roots, and the role pie upgraded to gradient/rounded/glow donut segments.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-CONTEXT.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-RESEARCH.md

# Current chart components (all three import the OLD local EChart wrapper):
@app/(dashboard)/template-mty/components/RoleAccessPie.tsx
@app/(dashboard)/template-mty/components/PermissionAccessChart.tsx
@app/(dashboard)/template-mty/components/ModuleAccessChart.tsx

# The donut-treatment recipe to copy (verified source):
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx

# Route loading.tsx pattern to mirror:
@app/(dashboard)/users/loading.tsx

<interfaces>
Shared themed EChart wrapper — `components/ui/EChart.tsx`:
```typescript
import { EChart } from "@/components/ui/EChart";
interface EChartProps {
  option: EChartsOption;
  height?: number;                 // default 280
  onEvents?: Record<string, (params) => void>;
  notMerge?: boolean;              // default true; pass false for animated updates
  className?: string;
}
// Internally calls mergeEChartsTheme(option, dark) and useTheme(); remounts on theme via key={resolvedTheme}.
```
PremiumSurface — `components/ui/PremiumSurface.tsx`:
```typescript
import { PremiumSurface } from "@/components/ui/PremiumSurface";
// variant: "base" | "float" | "glass" | "inset" (default "base"); RSC-safe; no hooks.
```
CONFIRMED at planning time: all three chart files currently import from
`@/app/(dashboard)/access-analysis/components/EChart` (the OLD wrapper, no mergeEChartsTheme) on line 4. RoleAccessPie also reads `useTheme()` directly and uses local dark-branch color consts; PermissionAccessChart and ModuleAccessChart do the same. The shared wrapper already handles theme internally, so the dark-branch color consts that feed axis/text/tooltip can stay (they are passed into the option), but the import MUST change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /template-mty loading.tsx + swap all three charts onto the shared EChart wrapper</name>
  <files>app/(dashboard)/template-mty/loading.tsx, app/(dashboard)/template-mty/components/RoleAccessPie.tsx, app/(dashboard)/template-mty/components/PermissionAccessChart.tsx, app/(dashboard)/template-mty/components/ModuleAccessChart.tsx</files>
  <action>
Create `app/(dashboard)/template-mty/loading.tsx` as a default-exported RSC that renders a layout-shaped skeleton matching the page (a stat-strip placeholder bar plus 3-4 stacked panel placeholders using `rounded-2xl bg-muted/20 animate-pulse` blocks). Mirror the structure of `app/(dashboard)/users/loading.tsx` (a centered max-width container with vertical spacing) but shape it to the template-mty sections (header strip + tall panels), not the users table. This satisfies PERF-01 (~200ms skeleton).

In all THREE chart files, change the EChart import on line 4 from `@/app/(dashboard)/access-analysis/components/EChart` to `@/components/ui/EChart`. This is the canonical themed wrapper that applies `mergeEChartsTheme` (the old local wrapper does not). Do not change any other import. Keep each component's existing `useTheme()`/`resolvedTheme` reads and dark-branch color consts — those feed axis/text/tooltip colors into the option and are still needed; only the wrapper source changes. Per RESEARCH Pitfall 2 and Decision (TPL-02), this import swap must happen in the same commit as the option/treatment changes so the charts actually auto-theme.

Run `grep -rn "access-analysis/components/EChart" "app/(dashboard)/template-mty"` after editing and confirm it returns ZERO matches.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && grep -rcn "access-analysis/components/EChart" "app/(dashboard)/template-mty" | grep -v ':0$' && echo "FAIL old-wrapper-import-remains" || echo "OK no-old-wrapper-import"</automated>
  </verify>
  <done>`loading.tsx` exists and default-exports a skeleton; `grep -rn "access-analysis/components/EChart" app/(dashboard)/template-mty` returns 0 matches; all three charts import `@/components/ui/EChart`.</done>
</task>

<task type="auto">
  <name>Task 2: Full donut treatment on RoleAccessPie + PremiumSurface roots + premium empty states on all three charts</name>
  <files>app/(dashboard)/template-mty/components/RoleAccessPie.tsx, app/(dashboard)/template-mty/components/PermissionAccessChart.tsx, app/(dashboard)/template-mty/components/ModuleAccessChart.tsx</files>
  <action>
Apply the full /access-analysis donut treatment to RoleAccessPie's pie series (copy the recipe verbatim from `RolesPieChart.tsx`): per-slice linear-gradient `itemStyle.color` (top stop = a ~0.22 lightened base, bottom stop = base color), `borderColor` = card background (`dark ? "#18181b" : "#ffffff"`), `borderWidth: 3`, `borderRadius: 7` (rounded segment ends), `shadowBlur: 14` + theme-aware `shadowColor`; `emphasis: { focus: "self", scaleSize: 12, itemStyle: { shadowBlur: 28, shadowColor: ... }, label: { show: true } }` for hover lift + glow; `universalTransition: true`, `animationType: "scale"`, `animationEasing: "elasticOut"`. Provide a small local `lighten(hex, amount)` helper if one is not already imported. Per CONTEXT.md (Decision: charts are display-only), do NOT add any slide-in / Sheet drill from the pie — the existing inline legend + `selectedId` tier-breakdown panel stays exactly as is, and the existing `onEvents.click` that toggles the inline legend stays. The hover lift+glow must NOT imply click-through (no `cursor: pointer` added to the canvas).

Wrap each chart's root in `PremiumSurface`: in RoleAccessPie, PermissionAccessChart, and ModuleAccessChart, replace the outer `<div className="panel-elevated p-5">` with `<PremiumSurface variant="base" className="p-5">` (import `PremiumSurface` from `@/components/ui/PremiumSurface`). Do NOT add a PremiumSurface around the EChart canvas itself beyond this single root swap — one surface per panel.

Upgrade every empty-state branch in the three files to premium polish (CONTEXT.md TPL-03: "Empty states get light premium polish — icon + message inside a PremiumSurface"): replace the plain `rounded-2xl border ... bg-card` empty divs with `<PremiumSurface variant="inset" className="...">` containing a small lucide icon + the existing message text. Keep the existing copy. Keep the legend, the tier-breakdown panel, and the helper `<p>` notes flat (not wrapped in their own surfaces).

Run `npx tsc --noEmit` and the affected unit tests; both must be clean. Do NOT modify any test fixture for these three chart files unless tsc forces it (these charts keep their public props unchanged).
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/template-mty/__tests__/RoleAccessPie.test.tsx app/\(dashboard\)/template-mty/__tests__/ModuleAccessChart.test.tsx</automated>
  </verify>
  <done>RoleAccessPie pie series has per-slice gradient + borderRadius:7 + emphasis glow; all three chart roots are `PremiumSurface variant="base"`; all empty states use `PremiumSurface variant="inset"` with icon + message; `npx tsc --noEmit` exits 0; RoleAccessPie and ModuleAccessChart tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-layer only. All chart data arrives as RSC props already fetched upstream; no user input, no new API route, no new data source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: "Installation: None required"). No new dependencies introduced. |
</threat_model>

<verification>
- `grep -rn "access-analysis/components/EChart" app/(dashboard)/template-mty` returns 0.
- `npx tsc --noEmit` exits 0.
- `npm test -- --run app/(dashboard)/template-mty/__tests__/RoleAccessPie.test.tsx app/(dashboard)/template-mty/__tests__/ModuleAccessChart.test.tsx` passes.
- Manual (Phase 7 projector): gradient fills + rounded segments visible on the role donut in BOTH light and dark; hover lifts + glows a segment; no cursor-pointer on the canvas; route shows a skeleton on navigation.
</verification>

<success_criteria>
- All three /template-mty charts use the shared themed `EChart` wrapper (auto light/dark).
- Role pie has gradient fills, rounded segment ends, and selected/hover glow (full donut treatment).
- Every chart panel + every empty state is a `PremiumSurface`.
- `app/(dashboard)/template-mty/loading.tsx` exists.
- `npx tsc --noEmit` exits 0; no chart unit-test regressions.
</success_criteria>

<output>
Create `.planning/phases/06-template-mty-forma-proposal-polish/06-01-SUMMARY.md` when done.
</output>
