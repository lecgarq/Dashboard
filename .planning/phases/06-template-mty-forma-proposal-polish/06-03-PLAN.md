---
phase: 06-template-mty-forma-proposal-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx
  - app/(dashboard)/template-mty/components/templateMemberColumns.tsx
  - app/(dashboard)/template-mty/components/TemplateMembersTable.tsx
  - app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx
  - app/(dashboard)/template-mty/__tests__/TemplateMembersTable.test.tsx
autonomous: true
requirements: [TPL-01, TPL-03]
must_haves:
  truths:
    - "The members table renders via the premium DataTable with 5 columns (Member/Role/Company/Access/Origin) and column sort"
    - "The search box and the four filter chips (All/Internal/External/Admin) still work, pre-filtering the data passed to DataTable"
    - "Clicking a member row with an email opens the shared UserProfilePanel (via the existing AuthorProfileDrawer)"
    - "Members without an email are non-clickable and visually de-emphasized"
    - "The members-table test suite is green against the new DataTable structure (never left red)"
  artifacts:
    - path: "app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx"
      provides: "Toolbar (search + 4 chips + count) wrapper that pre-filters and feeds DataTable"
      contains: "DataTable"
    - path: "app/(dashboard)/template-mty/components/templateMemberColumns.tsx"
      provides: "ColumnDef<TemplateMember>[] — 5 columns with sizes + sort"
      contains: "ColumnDef"
    - path: "app/(dashboard)/template-mty/__tests__/TemplateMembersTable.test.tsx"
      provides: "Updated tests for the DataTable-based shell (search/filter/sort/click/non-clickable)"
      min_lines: 40
  key_links:
    - from: "app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx"
      to: "components/ui/DataTable.tsx"
      via: "renders <DataTable data={preFilteredRows} columns={...} onRowClick={...} pinnedColumn='name'>"
      pattern: "DataTable"
    - from: "app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx"
      to: "app/(dashboard)/template-mty/templateMembersTable.ts"
      via: "calls filterMembers(members, query, filter) to pre-filter before DataTable"
      pattern: "filterMembers"
    - from: "app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx"
      to: "app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx"
      via: "renders the shell with onSelectMember (prop name unchanged), which still drives the AuthorProfileDrawer"
      pattern: "TemplateMembersTableShell"
---

<objective>
Migrate the /template-mty members grid from its custom ARIA-div table to the premium `DataTable` primitive, preserving every current capability: the search box, the four filter chips (All/Internal/External/Admin), all 5 columns (Member/Role/Company/Access/Origin), the count badge, row-click → shared `UserProfilePanel`, and the email-less-rows-stay-non-clickable behavior. The DataTable does NOT own the toolbar — a `TemplateMembersTableShell` wrapper owns search + chips and pre-filters the array before passing it to DataTable. Update the existing test in the same plan so the suite is never left red.

Purpose: TPL-01 (members table → premium DataTable, sort + row-click → slide-in profile) and the table half of TPL-03 (the DataTable self-wraps in PremiumSurface). Depends on 06-01 only because both touch the template-mty page area in adjacent waves; the actual code dependency is the Phase 3 DataTable primitive (already shipped). Runs in Wave 2 parallel with the /forma-proposal client wiring (different files).

Output: A `TemplateMembersTableShell` + `ColumnDef` file, a slimmed/aliased `TemplateMembersTable`, the wired-in `TemplateAnalysisCharts`, and an updated test.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-template-mty-forma-proposal-polish/06-CONTEXT.md
@.planning/phases/06-template-mty-forma-proposal-polish/06-RESEARCH.md

# Current members table (custom ARIA-div table to replace):
@app/(dashboard)/template-mty/components/TemplateMembersTable.tsx
# Pure filter/sort helpers to reuse (do NOT rebuild filtering):
@app/(dashboard)/template-mty/templateMembersTable.ts
# Caller that passes onSelectMember and owns the AuthorProfileDrawer (edit the table call site only):
@app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx
# Existing test that WILL break on migration — update it in this plan:
@app/(dashboard)/template-mty/__tests__/TemplateMembersTable.test.tsx
# DataTable column-definition pattern to mirror (real codebase example):
@app/(dashboard)/users/DirectoryTableColumns.tsx

<interfaces>
DataTable — `components/ui/DataTable.tsx` (Phase 3, already shipped):
```typescript
import { DataTable } from "@/components/ui/DataTable";
import type { ColumnDef, Row } from "@tanstack/react-table";
interface DataTableProps<T> {
  data: T[];                          // PRE-FILTERED by parent; DataTable owns sort only
  columns: ColumnDef<T>[];
  onRowClick?: (row: Row<T>) => void; // receives full Row<T>; read row.original
  pinnedColumn?: string;              // use "name"
  defaultSort?: SortingState;         // [{ id: "name", desc: false }]
  hasActiveFilter?: boolean;          // drives the filtered-empty message
  onClearFilters?: () => void;
  emptyMessage?: string;
  filteredEmptyMessage?: string;
  className?: string;
}
// DataTable wraps ITSELF in PremiumSurface variant="base" — do NOT double-wrap (RESEARCH Pitfall 3).
```
TemplateMember — `lib/server/templateView.ts`:
```typescript
interface TemplateMember {
  name: string; email: string; company: string; role: string;
  accessLevel: string; isInternal: boolean; isAdmin: boolean;
}
```
Pure helpers — `templateMembersTable.ts` (REUSE, do not reimplement):
```typescript
export type MemberFilter = "all" | "internal" | "external" | "admin";
export function filterMembers(members, query, filter): TemplateMember[];
// (sortMembers also exists but DataTable owns sort now — filterMembers is the one to reuse)
```
Caller seam — TemplateAnalysisCharts.tsx currently renders:
  <TemplateMembersTable members={overview.members} onSelectMember={(email)=>setProfileEmail(email.toLowerCase())} />
The onSelectMember prop name and the profileEmail/AuthorProfileDrawer wiring stay UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build templateMemberColumns (5 ColumnDef) + TemplateMembersTableShell (toolbar + DataTable)</name>
  <files>app/(dashboard)/template-mty/components/templateMemberColumns.tsx, app/(dashboard)/template-mty/components/TemplateMembersTableShell.tsx</files>
  <action>
Create `templateMemberColumns.tsx` ("use client"): export a `ColumnDef<TemplateMember>[]` (or a `makeMemberColumns()` factory if cell rendering needs no external state) with exactly 5 columns, mirroring the pattern in `app/(dashboard)/users/DirectoryTableColumns.tsx` (use `createColumnHelper<TemplateMember>()` or plain ColumnDef objects):
  - `id: "name"`, header "Member", `size: 240`, `enableSorting: true`, cell renders name (font-medium, truncate) over email (text-xs muted, truncate) — preserve the two-line Member cell. accessor/sort key = `name`.
  - `id: "role"`, header "Role", `size: 160`, sortable, cell = role or muted "No role".
  - `id: "company"`, header "Company", `size: 160`, sortable, cell = company or muted "—".
  - `id: "accessLevel"`, header "Access", `size: 110`, sortable, cell = the access badge (admin → primary-tinted pill, else muted pill) exactly as the current table renders it.
  - `id: "origin"`, header "Origin", `size: 120`, sortable, cell = Internal/External pill (emerald vs amber) as today. Sort value = `m.isInternal ? "Internal" : "External"` (use `sortingFn` or an `accessorFn` returning that string).
Column widths are at Claude's discretion per CONTEXT.md; the above are sensible defaults.

Create `TemplateMembersTableShell.tsx` ("use client") following RESEARCH "Pattern 1: DataTable Migration with External Toolbar". It accepts `{ members: TemplateMember[]; onSelectMember?: (email: string) => void }` (same surface the old component exposed). Hold `query` and `filter` state. Compute `const rows = useMemo(() => filterMembers(members, query, filter), [members, query, filter])` — DataTable handles sort, the shell handles filter. Render a toolbar `<div>` (NOT wrapped in its own PremiumSurface) containing: the search input (reuse the existing search markup + the `aria-label="Search members"`), the four filter chip buttons (All/Internal/External/Admin — keep `aria-pressed` and the button text so `getByRole("button", { name: "External" })` still resolves), and the count badge "{rows.length} of {members.length}". Below the toolbar render `<DataTable data={rows} columns={columns} pinnedColumn="name" defaultSort={[{ id: "name", desc: false }]} hasActiveFilter={filter !== "all" || query.length > 0} onClearFilters={() => { setQuery(""); setFilter("all"); }} onRowClick={(row) => { if (row.original.email) onSelectMember?.(row.original.email); }} />`. Do NOT wrap DataTable in PremiumSurface (it self-wraps — RESEARCH Pitfall 3).

Non-clickable rows (no email): the `onRowClick` guard already no-ops without an email. For de-emphasis (muted + cursor-default), set a per-row class via DataTable's row-meta/className seam if available; if DataTable exposes no per-row class hook, apply the muted treatment inside the Member cell (e.g. wrap the cell content in a span that is `opacity-60` when `!row.original.email`) and rely on the guard for non-clickability. Keep the empty members case handled by DataTable's `emptyMessage` ("No members found for this template.") and the filtered-empty case by `filteredEmptyMessage` ("No members match your search.").

Run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit</automated>
  </verify>
  <done>`templateMemberColumns.tsx` exports 5 typed columns with sizes + sort; `TemplateMembersTableShell.tsx` renders the toolbar (search + 4 chips + count) above `<DataTable>` with pre-filtered data, pinned name column, row-click guarded on email, no double PremiumSurface wrap; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the shell into TemplateAnalysisCharts, retire the old table, and update the test in the same commit</name>
  <files>app/(dashboard)/template-mty/components/TemplateMembersTable.tsx, app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx, app/(dashboard)/template-mty/__tests__/TemplateMembersTable.test.tsx</files>
  <action>
In `TemplateAnalysisCharts.tsx`: replace the `import { TemplateMembersTable } from "./TemplateMembersTable"` and its single JSX usage with `TemplateMembersTableShell` (from `./TemplateMembersTableShell`). Keep the `onSelectMember={(email) => setProfileEmail(email.toLowerCase())}` handler and the surrounding `<Reveal><section>...` exactly as-is — only the component swaps. Do NOT touch the `AuthorProfileDrawer` dynamic import or the `profileEmail` state; that drill target is unchanged (INT-01 — one shared profile).

Retire the old `TemplateMembersTable.tsx`: simplest correct option is to convert it to a thin re-export alias — `export { TemplateMembersTableShell as TemplateMembersTable } from "./TemplateMembersTableShell"` — so any stray importer (and the test, see below) keeps compiling. (Do not leave the old ARIA-div implementation in the tree; it would be dead code and would still pass the old selectors, masking regressions.)

Update `TemplateMembersTable.test.tsx` IN THIS COMMIT (RESEARCH Risk 1 + Pitfall 4 — the migration breaks the ARIA selectors and `next build` typechecks test files). Rewrite the tests to exercise the new shell (import `TemplateMembersTableShell`, or keep importing the aliased `TemplateMembersTable`). Preserve the BEHAVIORS under test, updating only the selectors to match the DataTable DOM:
  - renders one row per member with name/email/role/company/access — assert via `screen.getByText(...)` on the visible cell text (these still work; they are text queries, not role queries).
  - search box filters — `fireEvent.change(getByLabelText("Search members"), ...)` then assert the filtered-out name is gone. (Unchanged — the search lives in the shell toolbar.)
  - External chip filters — `fireEvent.click(getByRole("button", { name: "External" }))`. (Unchanged — chips live in the shell toolbar.)
  - sort by Member name desc — replace the `getByRole("table")` + `getAllByRole("row")` approach (DataTable's virtualizer rows are NOT standard `<tr>` accessible by role). Instead click the "Member" column header (query it however the DataTable renders it — likely `getByRole("button"/"columnheader", { name: /Member/i })` or `getByText("Member")`) and assert ordering by checking that the expected name now appears before the other in document order (e.g. compare `compareDocumentPosition`, or assert the first rendered member row's text). Inspect the DataTable DOM at runtime to pick the correct selector — do not assume `<tr>`.
  - row-click calls onSelectMember with email — `fireEvent.click(screen.getByText("Alberto"))`, assert `onSelectMember` called with `"alberto@hermosillo.com"`.
  - ADD a test: a member WITHOUT an email is non-clickable — render a member with `email: ""`, click its name, assert `onSelectMember` was NOT called (covers the RESEARCH "Wave 0 gap: non-clickable member row" and the CONTEXT.md de-emphasis decision).
Stub `ResizeObserver`, `HTMLElement.prototype.scrollIntoView`, and `window.scrollTo` if the DataTable/virtualizer needs them in jsdom (per Phase 02-01 decision in STATE.md). Run `npx tsc --noEmit` and the full template-mty test set; both must be green.
  </action>
  <verify>
    <automated>cd /c/LECG/Dashboard && npx tsc --noEmit && npm test -- --run app/\(dashboard\)/template-mty/__tests__/TemplateMembersTable.test.tsx</automated>
  </verify>
  <done>TemplateAnalysisCharts renders TemplateMembersTableShell with the unchanged onSelectMember handler; the old ARIA-div implementation is gone (file is a re-export alias); the test exercises the new DataTable DOM and includes a non-clickable-no-email case; `npx tsc --noEmit` exits 0 and the members-table test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The members array is an RSC prop already fetched upstream; row-click reuses the existing in-memory email lookup + AuthorProfileDrawer. No new API, input, or auth boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | npm/pip/cargo installs | accept | No installs (@tanstack/react-table v8 + react-virtual v3 already present from Phase 3). RESEARCH: "Installation: None required". |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (including the rewritten test file — RESEARCH Pitfall 4).
- `npm test -- --run app/(dashboard)/template-mty/__tests__/TemplateMembersTable.test.tsx` passes, including the new non-clickable-no-email case.
- The old ARIA-div table implementation no longer exists (TemplateMembersTable.tsx is a re-export alias).
- Manual (Phase 7): 5 columns render, pinned Name column, sortable headers, search + 4 chips + count work, member click opens the shared profile panel, email-less rows are muted + non-clickable.
</verification>

<success_criteria>
- Members table renders via the premium `DataTable` (5 columns, sort, pinned Name).
- Search + 4 filter chips + count badge preserved (toolbar in the shell, data pre-filtered).
- Row-click → shared `UserProfilePanel` via the unchanged AuthorProfileDrawer; email-less rows non-clickable + de-emphasized.
- Test suite green against the new structure (never left red); `npx tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/06-template-mty-forma-proposal-polish/06-03-SUMMARY.md` when done.
</output>
