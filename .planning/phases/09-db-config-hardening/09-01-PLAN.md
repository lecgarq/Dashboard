---
phase: 09-db-config-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/count-acc-data.cjs
  - .env.example
  - lib/server/acc-hot-cache.ts
  - lib/server/acc-hot-cache.test.ts
autonomous: true
requirements: [DB-02, DB-03, DB-04, TEST-01]
must_haves:
  truths:
    - "scripts/count-acc-data.cjs connects without any SSL/TLS-bypass flag and still prints the census counts"
    - ".env.example documents PG_POOL_MAX=32 and NODE_OPTIONS=--max-old-space-size=8192 with scaling-rationale comments (names + literals only, no secret values)"
    - "The includePermissionContexts:true raw-scan branch carries a WARNING about the ~5M-row heap risk and a VERIFY: note that no active (non-test) caller enables it"
    - "npm test runs a DB-free Vitest test asserting the AccFolderPermission GROUP BY aggregate returns at most n_roles x n_projects rows, never raw permission rows"
  artifacts:
    - path: "scripts/count-acc-data.cjs"
      provides: "Census diagnostic without ssl:{rejectUnauthorized:false}"
      contains: "DATABASE_URL"
    - path: ".env.example"
      provides: "Documented PG_POOL_MAX + NODE_OPTIONS scaling config"
      contains: "PG_POOL_MAX=32"
    - path: "lib/server/acc-hot-cache.ts"
      provides: "Guardrail comment on the raw folder-permission scan branch"
      contains: "WARNING"
    - path: "lib/server/acc-hot-cache.test.ts"
      provides: "OOM-regression pinning test for the GROUP BY aggregate row-bound"
      contains: "n_roles"
  key_links:
    - from: "scripts/count-acc-data.cjs"
      to: "DATABASE_URL pool (no TLS bypass)"
      via: "new Client({ connectionString }) with the ssl option removed"
      pattern: "new Client\\(\\s*\\{\\s*connectionString"
    - from: "lib/server/acc-hot-cache.test.ts"
      to: "lib/server/acc-hot-cache.ts getCachedAccDcBulkUsers ($queryRaw GROUP BY)"
      via: "mocked db.$queryRaw returning group rows; assert assembled bound <= n_roles x n_projects"
      pattern: "\\$queryRaw"
---

<objective>
Close the four DB/config concerns that need NO live database: remove the SSL fossil from the census
script (DB-02), document the heap/pool env vars in `.env.example` (DB-03), add a guardrail comment to
the raw folder-permission scan branch (DB-04), and ship the DB-free OOM-regression test that pins the
`AccFolderPermission` GROUP BY aggregate row-bound (TEST-01).

Purpose: These are the type-checkable + Vitest-testable changes with no live-DB side effect and no
rebuild gate, so they run first and unblock the migration plan (09-02). Per `09-CONTEXT.md` Inferred
Dashboard Defaults and the TEST-01 user decision (DB-free unit test, mock `db.$queryRaw`).

Output: A no-TLS census script, documented env-var entries, a warning-commented raw-scan branch, and a
passing in-suite regression guard for the verified ~13k-row aggregate.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-db-config-hardening/09-CONTEXT.md

# DB-02 target: the raw pg.Client with the TLS-bypass fossil (line 14)
@scripts/count-acc-data.cjs

# DB-04 target + TEST-01 target live in the same file:
#   - includePermissionContexts:true raw scan branch (~line 281-295)
#   - the OOM-FIX $queryRaw GROUP BY (folderSummaryByProjectRole, ~line 296-321)
#   - live caller is includePermissionSummary:true (~line 896-900)
@lib/server/acc-hot-cache.ts

# TEST-01 sits alongside the existing colocated suite (extend it):
@lib/server/acc-hot-cache.test.ts

<interfaces>
<!-- Extracted contracts the executor needs — no codebase exploration required. -->

From lib/server/acc-hot-cache.ts — getCachedAccDcBulkUsers(db, input):
  input: { includePermissionContexts?: boolean; includePermissionSummary?: boolean;
           includeActivityMix?: boolean; leanProjects?: boolean }
  - includePermissionContexts === true  -> db.accFolderPermission.findMany(...)  (the ~5M-row RAW SCAN = DB-04 target)
  - includePermissionSummary === true (and NOT contexts) -> db.$queryRaw GROUP BY f."projectId", fp."roleId"
    (folderSummaryByProjectRole; collapses ~5M grant rows to ~13k group rows = TEST-01 target).
    Aggregate row shape: { projectId, roleId, folderCount, totalBytes, permTypes }.
    Result Map key is `${projectId}::${roleId}` -> { folderCount, totalBytes, permTypes }.

From lib/server/acc-hot-cache.test.ts — existing helpers to reuse (do NOT rewrite):
  - versionedModel(findManyRows): returns { count, aggregate (_max stamps), findMany }
  - makeDcDb(): the standard DC mock db (accDcUser, accFolderPermission, accProject, accRole, ...)
  - makePrewarmDb(): superset that adds db.$queryRaw = vi.fn(async () => []) and db.accActivity / db.user
  - invalidateAccHotCache() is called in beforeEach; vi.restoreAllMocks() too.
  Note: getCachedAccDcBulkUsers caches by a version signature derived from .aggregate()._max stamps —
  call invalidateAccHotCache() (already in beforeEach) so each test computes fresh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SSL fossil from count-acc-data.cjs (DB-02)</name>
  <files>scripts/count-acc-data.cjs</files>
  <action>
Per DB-02 (CONCERNS §1.3): the script at line 14 constructs `new Client({ connectionString, ssl: { rejectUnauthorized: false } })`
— a Supabase-era TLS-bypass fossil. The dashboard runs on local trust-auth Postgres (no SSL); the flag
silently succeeds locally but would accept a spoofed cert on any TLS server. Remove the `ssl` option so the
client is `new Client({ connectionString })` — it inherits sslmode from `DATABASE_URL` with no bypass.
Do NOT change the DATABASE_URL parsing (lines 5-11), the table list, or the count logic. Add a one-line
comment above the Client construction noting the local trust-auth rationale (per DB-02, replaces the removed
TLS bypass). Keep the script runnable as `node scripts/count-acc-data.cjs`; it must still print the census table.
Scope: this single file only — do not touch any other script.
  </action>
  <verify>
    <automated>grep -nE "ssl|rejectUnauthorized" scripts/count-acc-data.cjs; test -z "$(grep -nE 'ssl[[:space:]]*:|rejectUnauthorized' scripts/count-acc-data.cjs)" && echo SSL_FOSSIL_REMOVED</automated>
  </verify>
  <done>scripts/count-acc-data.cjs contains no `ssl:` option and no `rejectUnauthorized`; it still constructs a pg Client from `connectionString` (DATABASE_URL) and the census logic is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document PG_POOL_MAX + NODE_OPTIONS in .env.example (DB-03) and add the raw-scan guardrail comment (DB-04)</name>
  <files>.env.example, lib/server/acc-hot-cache.ts</files>
  <action>
DB-03 (.env.example — CONCERNS §1.4/§1.5): `.env.example` is git-IGNORED and unreadable at plan time, so this
task must inspect it idempotently before writing. Run a NAME-ONLY grep first (never print values):
`grep -nE '^(PG_POOL_MAX|NODE_OPTIONS)=' .env.example`. Then ensure BOTH entries exist exactly as the
success criterion requires (these are config scaling literals, NOT secrets — the literals are explicitly
required and may be written):

  PG_POOL_MAX=32
  NODE_OPTIONS=--max-old-space-size=8192

If a key is absent, APPEND it; if present, UPDATE its line to the required literal. Above each, add a comment:
  - PG_POOL_MAX: per DB-03 — sized for access-analysis at scale; the formula is pool x web-workers must stay
    <= the local Postgres `max_connections` ceiling. Default would be 5 (prod) / 10 (dev).
  - NODE_OPTIONS: per DB-03 — `--max-old-space-size=8192` (8 GB heap) required to avoid OOM during the
    AccFolderPermission / bulkUsers loads at hub scale.
Secret hygiene: document NAMES + these two scaling literals + rationale comments ONLY. Do NOT add, copy, or
echo any DATABASE_URL, token, or other secret value from `.env`.

DB-04 (acc-hot-cache.ts — CONCERNS §1.1): on the `if (includePermissionContexts)` branch (~line 281, the one
that runs `db.accFolderPermission.findMany(...)`), add a guardrail comment block IMMEDIATELY ABOVE the branch.
It must (a) WARN that this path materialises the full ~5M-row AccFolderPermission scan into Node heap and
re-opens the OOM window the summary `$queryRaw` GROUP BY (the else-branch below) was added to close, and
(b) carry a `VERIFY:` line stating no active non-test caller enables `includePermissionContexts:true`
(grep-confirmed: only test files reference it). Use the token WARNING in the comment so the gate can detect it.
This is a comment-only change — do NOT alter the branch behavior, the findMany query, the else-branch
$queryRaw, or any types. `npx tsc --noEmit` must still pass.
  </action>
  <verify>
    <automated>grep -nE "^PG_POOL_MAX=32$" .env.example && grep -nE "^NODE_OPTIONS=--max-old-space-size=8192$" .env.example && grep -nE "PG_POOL_MAX|max_connections" .env.example && grep -nE "max-old-space|access-analysis|OOM|heap" .env.example && grep -nE "WARNING" lib/server/acc-hot-cache.ts && grep -nE "VERIFY:" lib/server/acc-hot-cache.ts && npx tsc --noEmit && echo DB03_DB04_OK</automated>
  </verify>
  <done>`.env.example` has `PG_POOL_MAX=32` and `NODE_OPTIONS=--max-old-space-size=8192`, each with a rationale comment and no secret values added; the includePermissionContexts branch in acc-hot-cache.ts has a WARNING + VERIFY: comment with no behavior change; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB-free OOM-regression test pinning the GROUP BY aggregate row-bound (TEST-01)</name>
  <files>lib/server/acc-hot-cache.test.ts</files>
  <behavior>
    - With a mocked db whose $queryRaw returns aggregate group rows (shape { projectId, roleId, folderCount, totalBytes, permTypes }),
      getCachedAccDcBulkUsers(db, { includePermissionSummary: true }) assembles a permission SUMMARY whose
      distinct (projectId, roleId) pairs come ONLY from the aggregate group rows.
    - Assert the count of distinct (projectId x roleId) summary entries consumed is <= n_roles x n_projects
      (the group-row upper bound), and is strictly less than the raw AccFolderPermission row population a
      findMany would have returned (use a fixture where raw rows >> group rows to make the bound meaningful).
    - The summary branch must NOT call db.accFolderPermission.findMany (the raw-scan path); assert findMany on
      that model was not invoked for the summary variant.
    - Test runs with NO live DB: db.$queryRaw is a vi.fn; the only persistence touched is the mock.
  </behavior>
  <action>
Per the TEST-01 user decision in 09-CONTEXT.md: a DB-free unit test that mocks `db.$queryRaw`. Add it to the
existing `lib/server/acc-hot-cache.test.ts` (extend, do not create a parallel file) using the existing
helpers (`makePrewarmDb` already wires `db.$queryRaw = vi.fn(async () => [])`; reuse `versionedModel`,
`makeDcDb`). The OOM fix under guard is the `else`-branch `$queryRaw` GROUP BY in getCachedAccDcBulkUsers
(`folderSummaryByProjectRole`, collapsing ~5M grant rows to group rows keyed `${projectId}::${roleId}`).

Construct a fixture proving the row-bound contract:
  - Define a small role set and project set in the mocked accRole / accProject models (e.g. 2 roles x 2
    projects = 4 group-row upper bound).
  - Mock db.$queryRaw to resolve the aggregate group rows for the GROUP BY query (return <= n_roles x
    n_projects rows with the documented shape). Use mockResolvedValueOnce ordering or a name/string-shape
    discriminator if the same db.$queryRaw also serves the activity/lastFileActivity queries — only the
    folder-summary aggregate matters here; other $queryRaw calls can resolve [].
  - Call getCachedAccDcBulkUsers(db, { includePermissionSummary: true }) (invalidateAccHotCache already runs
    in beforeEach so the version signature recomputes).
  - Assert: the number of distinct (projectId, roleId) summary entries is <= roles.length * projects.length;
    and that db.accFolderPermission.findMany was NOT called for this summary variant (the raw-scan path is the
    contexts branch, which this variant must avoid).
Name the test so the gate can find the bound, e.g. it("bounds the permission-summary aggregate to <= n_roles x n_projects group rows, never raw permission rows", ...). Do NOT modify acc-hot-cache.ts in this task.
Run only this file to keep the loop fast, then run the whole suite to confirm no regression.

NOTE on the count assertion: derive the n_roles x n_projects bound from the mocked accRole / accProject row
counts (not a hardcoded magic number) so the test documents the contract, e.g. `roles.length * projects.length`.
  </action>
  <verify>
    <automated>npm test -- lib/server/acc-hot-cache.test.ts</automated>
  </verify>
  <done>`npm test -- lib/server/acc-hot-cache.test.ts` passes with a new test that mocks db.$queryRaw, asserts the assembled permission-summary entries are bounded by `n_roles * n_projects` group rows (and strictly fewer than the raw-row population), and confirms the raw-scan findMany is not used for the summary variant — with no live-DB dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → local Postgres | `count-acc-data.cjs` opens a pg connection using `DATABASE_URL`; a TLS-bypass flag here weakens cert validation on any non-local server. |
| caller → acc-hot-cache | Any caller enabling `includePermissionContexts:true` triggers the unbounded ~5M-row scan (heap/DoS-by-memory risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | `scripts/count-acc-data.cjs` `ssl:{rejectUnauthorized:false}` | mitigate | DB-02 removes the bypass; the client validates per `DATABASE_URL` sslmode (local trust-auth uses none). |
| T-09-02 | Denial of Service | `includePermissionContexts:true` raw scan in acc-hot-cache.ts | mitigate (doc) | DB-04 warning + VERIFY: note flags the ~5M-row heap risk so no contributor re-opens the OOM window unknowingly; TEST-01 pins the bounded summary path. |
| T-09-03 | Information Disclosure | `.env.example` documentation | accept | DB-03 writes only env-var NAMES + non-secret scaling literals (PG_POOL_MAX=32, NODE_OPTIONS heap size) + rationale comments; no secret values are added (secret-hygiene contract). |
</threat_model>

<verification>
- `grep -nE 'ssl[[:space:]]*:|rejectUnauthorized' scripts/count-acc-data.cjs` returns NOTHING (SSL fossil gone — DB-02).
- `.env.example` contains exactly `PG_POOL_MAX=32` and `NODE_OPTIONS=--max-old-space-size=8192`, each preceded by a rationale comment; no secret values were added (DB-03, secret hygiene preserved).
- `lib/server/acc-hot-cache.ts` has a WARNING + `VERIFY:` comment on the includePermissionContexts:true branch and no behavior change (DB-04).
- `npx tsc --noEmit` passes (whole-tree typecheck, incl. tests).
- `npm test` passes, including the new DB-free TEST-01 aggregate row-bound test (no live-DB dependency).
- Diff touches ONLY: scripts/count-acc-data.cjs, .env.example, lib/server/acc-hot-cache.ts, lib/server/acc-hot-cache.test.ts. No UI surface, no `/users/spatial-graph`, no theme/WebGL changes.
</verification>

<success_criteria>
- DB-02: census script has no TLS-bypass flag and still returns counts.
- DB-03: `.env.example` documents PG_POOL_MAX=32 and NODE_OPTIONS=--max-old-space-size=8192 with scaling comments, no secret values.
- DB-04: raw-scan branch carries the ~5M-row heap WARNING + VERIFY: no-active-caller note.
- TEST-01: `npm test` includes the DB-free Vitest guard asserting aggregate rows <= n_roles x n_projects (never raw permission rows).
- `npx tsc --noEmit` and `npm test` both pass.
</success_criteria>

<output>
Create `.planning/phases/09-db-config-hardening/09-01-SUMMARY.md` when done.
</output>
