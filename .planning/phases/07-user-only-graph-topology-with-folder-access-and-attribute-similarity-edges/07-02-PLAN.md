---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/acc/folderHubCollapse.ts
  - lib/acc/folderHubCollapse.test.ts
autonomous: true
requirements: [GRAPH7-01]
must_haves:
  truths:
    - "Pure function collapses folders to depth N=2 by default."
    - "UNION semantics: descendant permissions are merged onto the depth-N ancestor (no silent drop — Pitfall 3)."
    - "Per-(roleId, permType) deduped after union."
    - "Escape hatch: depth=Infinity preserves every folder unchanged."
    - "All-Vitest tests pass; module has zero non-pure dependencies (no Prisma, no React)."
  artifacts:
    - path: "lib/acc/folderHubCollapse.ts"
      provides: "collapseFoldersToDepth(rows, maxDepth=2) → CollapsedFolder[]"
      exports: ["collapseFoldersToDepth", "CollapsedFolder"]
      min_lines: 40
    - path: "lib/acc/folderHubCollapse.test.ts"
      provides: "Vitest coverage — depth-collapse, union semantics, dedupe, escape hatch"
      contains: "describe.*collapseFoldersToDepth"
  key_links:
    - from: "lib/acc/folderHubCollapse.ts"
      to: "server/routers/acc-folders.ts:FolderMatrixRow"
      via: "import type { FolderMatrixRow }"
      pattern: "FolderMatrixRow"
---

<objective>
Build the pure depth-N folder-collapse module that Plan 07-05 (2D wiring) and Plan 07-07 (3D wiring) consume to convert `accFoldersRouter.getMatrix` rows into a smaller set of folder hubs.

Purpose: Hub-scale (~1,143 active projects) emits 10k+ leaf folders. Topology rendering at that count is cost-prohibitive. Depth-2 collapse with UNION-of-descendants permission semantics keeps topology readable AND preserves the orphan/permission picture.
Output: 1 source file + 1 Vitest file. Zero renderer wiring (deferred to Plan 07-05 / 07-07).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-RESEARCH.md
@server/routers/acc-folders.ts
@lib/acc/permissionMapping.ts

<interfaces>
From server/routers/acc-folders.ts:19:
```typescript
export interface FolderMatrixRow {
  folderId: string;
  folderPath: string;        // e.g. "/Project Files/Plans/02 Architecture"
  projectId: string;
  projectName: string;
  projectCrawlStatus: string;
  roleId: string;
  roleName: string;
  permType: PermTier | string;
  actions: string[];
  orphanReasons: OrphanReason[];
}
```

From lib/acc/permissionMapping.ts: `PermTier` union of 6 strings ("View Only", "View+Download", "Upload Only", "View+Download+Upload", "View+Download+Upload+Edit", "Full Controller").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write Vitest spec for collapseFoldersToDepth</name>
  <files>lib/acc/folderHubCollapse.test.ts</files>
  <action>
    Create the test file with these cases (RED phase — file under test does not exist yet, so import will fail until Task 2):

    1. **Empty input** → returns `[]`.
    2. **All folders shallower than maxDepth** → returns one CollapsedFolder per (projectId, folderId) with original id and unchanged fullPath.
    3. **Folders deeper than maxDepth=2** — given rows with paths `/Project Files/Plans/02 Architecture/A.dwg` and `/Project Files/Plans/02 Architecture/B.dwg` (depth 4 each, same parent at depth 2) → both collapse to ONE CollapsedFolder with `fullPath="/Project Files/Plans"` and `id` starting with `collapsed:`.
    4. **UNION of permissions** — two leaf rows under the same depth-2 ancestor with DIFFERENT (roleId, permType) values → collapsed entry's `permissions` contains BOTH (length=2).
    5. **Dedup by (roleId, permType)** — three leaf rows with the same `(roleId="r1", permType="View Only")` → collapsed entry has exactly one such permission.
    6. **Escape hatch** — passing `maxDepth=Infinity` preserves every original folder (length === input.length, no `collapsed:` prefixed ids).
    7. **Cross-project isolation** — two rows with the same path but different projectIds produce two distinct CollapsedFolder entries.

    Use the import path `import { collapseFoldersToDepth, type CollapsedFolder } from "./folderHubCollapse";` and a synthetic `FolderMatrixRow`-shaped fixture (no need to import the type from server router — use a local interface in the test file or `as any`).

    Run vitest, confirm RED (file-not-found error or test failures). Commit with message `test(07-02): add failing tests for collapseFoldersToDepth`.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/folderHubCollapse.test.ts 2&gt;&amp;1 | grep -E "(FAIL|Cannot find module)"</automated>
  </verify>
  <done>Test file exists, vitest run produces failures (RED), commit recorded.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Implement collapseFoldersToDepth</name>
  <files>lib/acc/folderHubCollapse.ts</files>
  <action>
    Implement per RESEARCH §Code Examples #1. Key points:

    - Input: `rows: ReadonlyArray&lt;FolderMatrixRow-like&gt;`, `maxDepth = 2`.
    - Output: `CollapsedFolder[]` with shape `{ id, projectId, name, fullPath, permissions: Array&lt;{roleId, permType}&gt; }`.
    - Group by `${projectId}::${collapsedPath}` where collapsedPath = "/" + first `maxDepth` non-empty path segments (or original path if depth ≤ maxDepth).
    - id = original folderId when depth ≤ maxDepth; else `collapsed:${key}`.
    - name = the last segment of the collapsed path (or fullPath if no segments).
    - Dedup permissions per collapsed folder by `${roleId}::${permType}`.
    - `maxDepth=Infinity` short-circuits to "depth ≤ maxDepth" branch for every row.

    Define a local input type to keep the module pure (no server-router import — accept structural shape with the 5 fields used: `folderId, folderPath, projectId, roleId, permType`). Export the input type as `FolderHubInputRow` so callers can adapt.

    Run vitest, confirm GREEN. Commit with message `feat(07-02): implement collapseFoldersToDepth (GRAPH7-01)`.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/folderHubCollapse.test.ts</automated>
  </verify>
  <done>All tests pass; module exports `collapseFoldersToDepth`, `CollapsedFolder`, `FolderHubInputRow`; zero Prisma/React imports.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/acc/folderHubCollapse.test.ts` → all tests pass.
- `grep -E "from .(prisma|react|@/server)" lib/acc/folderHubCollapse.ts` → empty (purity check).
</verification>

<success_criteria>
- 7 test cases pass.
- No external runtime deps.
- Two atomic commits (RED, GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-02-SUMMARY.md`.
</output>
