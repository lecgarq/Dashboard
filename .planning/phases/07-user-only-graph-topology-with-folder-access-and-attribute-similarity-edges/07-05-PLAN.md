---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 05
type: execute
wave: 2
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - app/(dashboard)/users/accGraphOrganicLayout.ts
  - app/(dashboard)/users/graphRenderers.ts
  - app/(dashboard)/users/cosmosUtils.ts
  - app/(dashboard)/users/accGraphTopology.test.ts
autonomous: true
requirements: [GRAPH7-01, GRAPH7-02, GRAPH7-03, GRAPH7-04, GRAPH7-11]
must_haves:
  truths:
    - "AccTopologyHubKind extends with 'folder'."
    - "AccTopologyLink extends with optional permTier + dimension + weight."
    - "buildAccTopologyGraph emits folder hubs (via collapseFoldersToDepth), folder↔project edges, role↔folder edges, and user↔user similarity edges (via computeSimilarityEdges)."
    - "GraphRenderNode.kind union includes 'folder'; folder color hardcoded slate/teal — bypasses getCategoryColor (Pitfall 4)."
    - "buildLinkColorBuffer accepts an optional perEdgeColors array (length === linkCount)."
    - "Topology integration tests pass — folder/similarity/permTier edges produced as expected."
  artifacts:
    - path: "app/(dashboard)/users/accGraphOrganicLayout.ts"
      provides: "Extended AccTopologyHubKind + AccTopologyLink + buildAccTopologyGraph"
      contains: "folder"
    - path: "app/(dashboard)/users/graphRenderers.ts"
      provides: "GraphRenderNode.kind += folder; per-kind color resolver branches on folder"
      contains: "kind === \"folder\""
    - path: "app/(dashboard)/users/cosmosUtils.ts"
      provides: "buildLinkColorBuffer extended with perEdgeColors override"
    - path: "app/(dashboard)/users/accGraphTopology.test.ts"
      provides: "Coverage for folder hubs + similarity edges in buildAccTopologyGraph"
  key_links:
    - from: "buildAccTopologyGraph"
      to: "lib/acc/folderHubCollapse.ts:collapseFoldersToDepth"
      via: "called once per (matrix rows, depth) input"
      pattern: "collapseFoldersToDepth"
    - from: "buildAccTopologyGraph"
      to: "lib/acc/userSimilarity.ts:computeSimilarityEdges"
      via: "called once per (users, enabledDims, simMin) input — memoize at adapter caller (Plan 07-06)"
      pattern: "computeSimilarityEdges"
---

<objective>
Wire the new pure modules (folderHubCollapse, userSimilarity) into the 2D topology adapter and renderer node/link types. Add the new "folder" node kind and the new edge attributes (permTier, dimension, weight) to the topology graph emission. Extend `cosmosUtils.buildLinkColorBuffer` to accept per-edge colors so the Plan 07-06 filter panel can drive edge color from permTier or dimension at render time.

Purpose: This is the data-shaping plan. All topology mutations land here; renderer and filter-panel plans consume the new shape.
Output: Three modified source files + extended topology test file. No UI changes.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(dashboard)/users/accGraphOrganicLayout.ts
@app/(dashboard)/users/graphRenderers.ts
@app/(dashboard)/users/cosmosUtils.ts
@app/(dashboard)/users/accGraphTopology.test.ts
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-RESEARCH.md
@lib/acc/folderHubCollapse.ts
@lib/acc/userSimilarity.ts
@lib/acc/permissionMapping.ts
@server/routers/acc-folders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend topology types + buildAccTopologyGraph to emit folder hubs and similarity edges</name>
  <files>app/(dashboard)/users/accGraphOrganicLayout.ts</files>
  <action>
    1. Extend `AccTopologyHubKind`:
       ```typescript
       export type AccTopologyHubKind = "project" | "role" | "module" | "access" | "user" | "folder";
       ```
    2. Extend `AccTopologyLink`:
       ```typescript
       export type AccTopologyLinkKind =
         | AccTopologyHubKind
         | "role-folder"
         | "folder-project"
         | "user-similarity";

       export interface AccTopologyLink {
         source: string;
         target: string;
         kind: AccTopologyLinkKind;
         permTier?: PermTierKey;          // only for role-folder
         dimension?: SimilarityDimKey;    // only for user-similarity
         weight?: number;                  // sharedCount for similarity edges
       }
       ```
       Import `PermTierKey, SimilarityDimKey` from `./accGraphFilters` (type-only).
    3. Extend the signature of `buildAccTopologyGraph` (find the existing function — grep for `export function buildAccTopologyGraph`) to accept an optional `folderMatrix?: FolderHubInputRow[]` and `similarityInput?: SimilarityInput | null` plus `similarityDims: ReadonlySet&lt;SimilarityDim&gt;` and `simMin: number`. When `folderMatrix` is provided:
       - Call `collapseFoldersToDepth(folderMatrix, 2)`.
       - For each `CollapsedFolder`: `addHub("folder", folder.id, folder.name)`.
       - Add `addLink(folderHubId, addHub("project", folder.projectId), "folder-project")`.
       - For each `permissions` entry: collapse 6-tier PermType to 4 UI tiers via a local LUT `{ "View Only": "view", "View+Download": "view", "Upload Only": "upload", "View+Download+Upload": "upload", "View+Download+Upload+Edit": "edit", "Full Controller": "control" }` and `addLink(addHub("role", roleId), folderHubId, "role-folder", { permTier })`.
    4. When `similarityInput` is provided AND non-empty: call `computeSimilarityEdges(similarityInput, similarityDims, simMin)`. For each `SimilarityEdge`: `addLink(userA, userB, "user-similarity", { dimension, weight: sharedCount })`. Keep these edges in the topology REGARDLESS of `viewMode` — viewMode is render-time visibility, not data filtering (Pattern 3).
    5. Export the 4-tier `PermTierKey` LUT as `collapsePermTierKey(permType: string): PermTierKey` so renderers reuse it.

    Backward compatibility: existing callers that pass neither folderMatrix nor similarityInput must continue to work unchanged. Both new params default to `undefined`/`null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "(accGraphOrganicLayout|TS[0-9]+)" || echo "OK"</automated>
  </verify>
  <done>Types extended; buildAccTopologyGraph emits folder + similarity edges when inputs provided; existing callers unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Extend GraphRenderNode + cosmosUtils for per-edge color + folder color override</name>
  <files>app/(dashboard)/users/graphRenderers.ts,app/(dashboard)/users/cosmosUtils.ts</files>
  <action>
    **graphRenderers.ts:**
    1. Grep for `GraphRenderNode` definition (likely around line 13-22 per RESEARCH). Extend its `kind` union with `"folder"`.
    2. Find where node color is resolved (search for `getCategoryColor`). BEFORE that call, branch on `node.kind === "folder"` and assign the hardcoded color `"#5EEAD4"` (teal-300). Add a comment citing Pitfall 4.
    3. Search for `setVisibleIndices` (RESEARCH cites line 1316). Confirm it already supports any node kind — no change needed there; the new "folder" kind flows through naturally because size=0 + shape=8 is kind-agnostic.

    **cosmosUtils.ts:**
    1. Grep for `buildLinkColorBuffer`. Extend its signature:
       ```typescript
       export function buildLinkColorBuffer(
         count: number,
         options?: { defaultColor?: string; perEdgeColors?: ReadonlyArray&lt;string&gt; },
       ): Float32Array
       ```
       If `perEdgeColors` provided, fill per-edge; else fill uniform `defaultColor`.
    2. Add a small `hexToRgba01(hex: string): [number, number, number, number]` helper if one doesn't already exist (check `cosmosUtils.ts` first).
    3. Update existing call sites within `cosmosUtils.ts` and any direct callers in `app/(dashboard)/users/` to use the new options object (or be no-arg compatible). Grep for `buildLinkColorBuffer(` to find call sites.

    Run typecheck.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2&gt;&amp;1 | grep -E "(graphRenderers|cosmosUtils|TS[0-9]+)" || echo "OK"</automated>
  </verify>
  <done>graphRenderers branches on kind=folder for color; buildLinkColorBuffer accepts perEdgeColors; project typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Topology integration tests — folder hubs + similarity edges</name>
  <files>app/(dashboard)/users/accGraphTopology.test.ts</files>
  <action>
    Append a new `describe("Phase 7 topology extensions", ...)` block to the existing test file:

    1. **Folder hubs emitted**: synthetic folderMatrix with 3 rows under one project produces 1 collapsed folder hub + 1 folder-project edge + N role-folder edges with correct permTier.
    2. **No folder hubs without input**: calling buildAccTopologyGraph with `folderMatrix=undefined` returns a graph with NO `kind: "folder"` hidden nodes (regression guard).
    3. **Similarity edges**: synthetic similarityInput with 3 users sharing 2 roles and minShared=2 produces 3 user-similarity edges (one per pair), each with `dimension: "roles"` and `weight: 2`.
    4. **Per-permTier color collapse**: rows with permType "View+Download" and "Full Controller" emit edges with permTier="view" and permTier="control" respectively.
    5. **No similarity edges without input**: calling with similarityInput=null produces zero user-similarity edges (regression guard).

    Run vitest. Commit `feat(07-05): wire folder hubs + similarity edges into 2D topology (GRAPH7-01..04, GRAPH7-11)`.
  </action>
  <verify>
    <automated>npx vitest run app/(dashboard)/users/accGraphTopology.test.ts</automated>
  </verify>
  <done>All new tests pass; existing topology tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run app/(dashboard)/users/accGraphTopology.test.ts` green.
- `npx tsc --noEmit -p .` no new errors.
</verification>

<success_criteria>
- 5 new topology test cases pass.
- 2D topology emits folder hubs + similarity edges when given the new inputs.
- Folder color bypass implemented (Pitfall 4 defended).
- Per-edge color buffer plumbing in place for Plan 07-06 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-05-SUMMARY.md`.
</output>
