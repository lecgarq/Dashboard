---
phase: 07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/accGraphFilters.ts
  - app/(dashboard)/users/accGraphFilters.test.ts
autonomous: true
requirements: [GRAPH7-06, GRAPH7-09, FILT-EXT]
must_haves:
  truths:
    - "GraphFilters interface gains 5 new keys: showFolders, permTiers, simDims, simMin, viewMode."
    - "DEFAULT_FILTERS has the documented defaults (folders ON, all permTiers, all simDims, simMin=2, viewMode='multi')."
    - "nodeMatchesFilters extended: viewMode='user-only' returns false for any non-user kind; folder kind hidden when showFolders=false."
    - "Existing filter test cases still pass — no regression."
  artifacts:
    - path: "app/(dashboard)/users/accGraphFilters.ts"
      provides: "Extended GraphFilters + nodeMatchesFilters predicate"
      exports: ["GraphFilters", "DEFAULT_FILTERS", "nodeMatchesFilters", "PermTierKey", "SimilarityDimKey", "ViewMode"]
    - path: "app/(dashboard)/users/accGraphFilters.test.ts"
      provides: "Coverage for the 5 new filter dimensions"
      contains: "describe.*viewMode|showFolders|permTiers|simDims|simMin"
  key_links:
    - from: "app/(dashboard)/users/accGraphFilters.ts"
      to: "lib/acc/userSimilarity.ts:SimilarityDim"
      via: "import type { SimilarityDim } as SimilarityDimKey"
      pattern: "SimilarityDim"
---

<objective>
Extend the existing `GraphFilters` shape and `nodeMatchesFilters` predicate to carry the 5 new filter dimensions Phase 7 introduces. Pure-module change, fully unit-tested. Consumed by Plan 07-05 (2D adapter wiring) and Plan 07-06 (2D filter panel UI).

Purpose: Filter shape must be defined BEFORE wiring. This isolates the type churn and makes downstream wiring purely additive.
Output: Extended `accGraphFilters.ts` + new tests covering the 5 added dimensions.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(dashboard)/users/accGraphFilters.ts
@app/(dashboard)/users/accGraphFilters.test.ts
@.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Add tests for the 5 new filter dimensions</name>
  <files>app/(dashboard)/users/accGraphFilters.test.ts</files>
  <action>
    APPEND test cases (do not delete existing tests). Add a new `describe("Phase 7 filter dimensions", ...)` block with:

    1. `DEFAULT_FILTERS.showFolders === true`
    2. `DEFAULT_FILTERS.permTiers` deep-equals `["view","upload","edit","control"]` (all four)
    3. `DEFAULT_FILTERS.simDims` deep-equals `["folder-access","roles","projects","company","admin-tier"]`
    4. `DEFAULT_FILTERS.simMin === 2`
    5. `DEFAULT_FILTERS.viewMode === "multi"`
    6. `nodeMatchesFilters({...minimalUserNode, kind: "folder"}, {...DEFAULT_FILTERS, showFolders: false})` → false
    7. `nodeMatchesFilters({...minimalUserNode, kind: "project"}, {...DEFAULT_FILTERS, viewMode: "user-only"})` → false (any non-user kind hidden)
    8. `nodeMatchesFilters({...minimalUserNode, kind: "user"}, {...DEFAULT_FILTERS, viewMode: "user-only"})` → true
    9. `nodeMatchesFilters` with `kind` field absent (legacy nodes) treats node as `kind: "user"` (backward compat).

    Note: `FilterableNode` currently does not have a `kind` field. Add `kind?: "user" | "project" | "role" | "module" | "access" | "folder"` to FilterableNode in this RED test (the test will fail on type-error until Task 2 widens the type).

    Run vitest, confirm RED. Commit `test(07-04): add failing tests for Phase 7 filter dimensions`.
  </action>
  <verify>
    <automated>npx vitest run app/(dashboard)/users/accGraphFilters.test.ts 2&gt;&amp;1 | grep -E "FAIL"</automated>
  </verify>
  <done>New tests added, fail (RED), commit recorded.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Extend GraphFilters / DEFAULT_FILTERS / FilterableNode / nodeMatchesFilters</name>
  <files>app/(dashboard)/users/accGraphFilters.ts</files>
  <action>
    Edits:

    1. Add type aliases:
       ```typescript
       export type PermTierKey = "view" | "upload" | "edit" | "control";
       export type SimilarityDimKey = "folder-access" | "roles" | "projects" | "company" | "admin-tier";
       export type ViewMode = "multi" | "user-only";
       export type GraphNodeKind = "user" | "project" | "role" | "module" | "access" | "folder";
       ```
    2. Extend `GraphFilters` with `showFolders: boolean; permTiers: PermTierKey[]; simDims: SimilarityDimKey[]; simMin: number; viewMode: ViewMode;`.
    3. Update `DEFAULT_FILTERS` with `showFolders: true, permTiers: ["view","upload","edit","control"], simDims: ["folder-access","roles","projects","company","admin-tier"], simMin: 2, viewMode: "multi"`.
    4. Extend `FilterableNode` with optional `kind?: GraphNodeKind`.
    5. In `nodeMatchesFilters`, BEFORE the existing role/admin/etc checks:
       ```typescript
       const kind = node.kind ?? "user";
       if (filters.viewMode === "user-only" &amp;&amp; kind !== "user") return false;
       if (kind === "folder" &amp;&amp; !filters.showFolders) return false;
       // permTiers / simDims / simMin are EDGE-level filters, NOT node-level — applied in adapter, not here.
       ```
    6. Re-export `SimilarityDim` from `lib/acc/userSimilarity` is OPTIONAL — keep `SimilarityDimKey` local to avoid a cross-module type-only cycle. Document with a comment that the two unions must stay in sync (TODO if they ever drift).

    Run vitest, confirm GREEN. Commit `feat(07-04): extend GraphFilters with Phase 7 dimensions (GRAPH7-06, GRAPH7-09)`.
  </action>
  <verify>
    <automated>npx vitest run app/(dashboard)/users/accGraphFilters.test.ts</automated>
  </verify>
  <done>All tests (existing + new) pass; downstream consumers can import the new types.</done>
</task>

</tasks>

<verification>
- `npx vitest run app/(dashboard)/users/accGraphFilters.test.ts` → all green.
- TypeScript compile: `npx tsc --noEmit -p .` (or project default) — no new errors introduced (existing callers of `nodeMatchesFilters` should still typecheck because new fields have defaults via DEFAULT_FILTERS spread).
</verification>

<success_criteria>
- 9 new test cases pass.
- No regressions in pre-existing accGraphFilters tests.
- Two atomic commits (RED, GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-only-graph-topology-with-folder-access-and-attribute-similarity-edges/07-04-SUMMARY.md`.
</output>
