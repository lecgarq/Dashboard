---
phase: 19-raw-scan-retirement-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - scripts/dc-daily-ingest.cjs
  - .planning/codebase/INTEGRATIONS.md
autonomous: false
requirements:
  - PROJ-03

must_haves:
  truths:
    - "scripts/dc-daily-ingest.cjs refreshes AccFolderPermissionSummary after a successful ingest, using the same non-fatal try/catch pattern as the existing person-graph / instance-embedding rebuilds"
    - "The refresh runs BEFORE the build-instance-features.ts step, so the same run's embedding (which reads the projection via includePermissionSummary post-PROJ-02) sees fresh data"
    - "The refresh reuses scripts/backfill-folder-perm-summary.cjs verbatim (server-side INSERT...SELECT...GROUP BY, no Node-side row scan, no duplicated SQL)"
    - ".planning/codebase/INTEGRATIONS.md documents the staleness bound (<=1 daily ingest cycle), the out-of-band folder-crawl caveat, and the manual `node scripts/backfill-folder-perm-summary.cjs` fallback"
    - "Post-refresh reconciliation passes (scripts/verify-folder-perm-summary.cjs → PASS) and /access-analysis + /template-mty render identically after a fresh :3000 rebuild (owner visual check)"
  artifacts:
    - path: "scripts/dc-daily-ingest.cjs"
      provides: "Non-fatal AccFolderPermissionSummary refresh block, ordered first in the success branch, invoking the backfill script"
      contains: "backfill-folder-perm-summary"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "AccFolderPermissionSummary projection refresh + staleness-bound documentation (SC#4)"
      contains: "AccFolderPermissionSummary"
  key_links:
    - from: "scripts/dc-daily-ingest.cjs (success branch)"
      to: "scripts/backfill-folder-perm-summary.cjs"
      via: "execSync('node scripts/backfill-folder-perm-summary.cjs') inside a non-fatal try/catch, before the embedding build"
      pattern: "backfill-folder-perm-summary"
---

<objective>
PROJ-03 (REF-03 completion): keep `AccFolderPermissionSummary` current by re-running the
idempotent server-side aggregate after each successful daily ingest, and document the staleness
bound. This closes the loop opened by PROJ-02 — now that the summary path reads the projection,
the projection must not silently lag the raw tables.

Purpose: matches the Dashboard's "sync is automatic, no manual sync UI" principle and bounds
staleness to <=1 ingest cycle. The projection never drifts unbounded behind the source.

Refresh approach (from 19-CONTEXT, decisive): re-run `scripts/backfill-folder-perm-summary.cjs`
(TRUNCATE + INSERT...SELECT...GROUP BY, entirely server-side, widened 300s txn timeout, bound
checks) from inside `dc-daily-ingest.cjs` using the same non-fatal try/catch the cron already uses
for the person-graph and instance-embedding rebuilds. Reusing the existing script IS the
"single owner of the aggregate SQL" — stronger than extracting a shared helper: the cron and the
standalone backfill run the identical code path, so there is zero SQL duplication and zero drift
risk. MUST stay server-side (never `findMany` the raw table into Node — that re-opens the OOM
window PROJ-02 just guarded); the reused script already guarantees this.

Ordering (load-bearing): the refresh MUST be the FIRST step in the `if (result.status ===
'success')` block, BEFORE the `build-instance-features.ts` embedding build. After PROJ-02,
`scripts/build-instance-features.ts:43` reads the projection (`includePermissionSummary: true`);
if the embedding built before the refresh, it would use the previous day's projection. Refreshing
first keeps every same-run downstream consumer current.

depends_on 19-01: this plan is wave 2 because the refresh-before-embedding ordering only becomes
load-bearing once 19-01 switches `getCachedAccDcBulkUsers` to read the projection, and the
phase-close reconciliation + owner visual parity gate is cleanest run against the already-switched
code (a single :3000 rebuild covers both plans). Files do not overlap 19-01.

Workshop-impact note: renders identically. This plan changes a background cron script and a
planning doc — no `app/` route, component, chart, theme, or WebGL surface; `/users/spatial-graph`
untouched. The owner visual check confirms zero visible change end-to-end.

Data-truthfulness note: the INTEGRATIONS.md entry states the honest staleness bound and the
out-of-band folder-crawl caveat rather than implying instant freshness — under-covered timing is
labelled, not hidden.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-raw-scan-retirement-refresh/19-CONTEXT.md

# Cron: the post-ingest non-fatal rebuild block is the hook point (person-graph + embedding pattern in the success branch)
@scripts/dc-daily-ingest.cjs

# The idempotent server-side backfill the cron will invoke (TRUNCATE + INSERT...SELECT...GROUP BY, 300s txn timeout, bound checks)
@scripts/backfill-folder-perm-summary.cjs

# The reconciliation harness to re-run post-refresh (proves projection == live aggregate)
@scripts/verify-folder-perm-summary.cjs

# Where the staleness note lands + existing DC / folder-crawl / Task-Scheduler sections to match tone
@.planning/codebase/INTEGRATIONS.md

<interfaces>
<!-- The exact non-fatal pattern already used in dc-daily-ingest.cjs success branch — mirror it. -->
if (result.status === 'success') {
  try {
    log('Rebuilding person-similarity graph snapshot...');
    const { execSync } = require('node:child_process');
    execSync('npx tsx scripts/rebuild-person-graph.ts', { stdio: 'inherit' });
  } catch (e) { log('person-graph rebuild failed (non-fatal): ' + e.message); }
  try {
    log('Building per-instance embedding (features → UMAP)...');
    const { execSync } = require('node:child_process');
    execSync('npx tsx scripts/build-instance-features.ts', { stdio: 'inherit' }); // reads the projection (PROJ-02)
    execSync('python scripts/compute_instance_embeddings.py', { stdio: 'inherit' });
  } catch (e) { log('instance-embedding build failed (non-fatal): ' + e.message); }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the projection refresh into the ingest cron + document the staleness bound</name>
  <files>scripts/dc-daily-ingest.cjs, .planning/codebase/INTEGRATIONS.md</files>
  <action>
In `scripts/dc-daily-ingest.cjs`, inside the `if (result.status === 'success') { ... }` block,
insert a new non-fatal refresh as the FIRST step — BEFORE the person-graph rebuild and (critically)
before the `build-instance-features.ts` embedding build:

```js
try {
  log('Refreshing AccFolderPermissionSummary projection (server-side aggregate)...');
  const { execSync } = require('node:child_process');
  execSync('node scripts/backfill-folder-perm-summary.cjs', { stdio: 'inherit' });
} catch (e) {
  log('AccFolderPermissionSummary refresh failed (non-fatal): ' + e.message);
}
```

Notes to encode in a short comment above it:
- Reuses the idempotent server-side backfill (TRUNCATE + INSERT...SELECT...GROUP BY) — the single
  owner of the aggregate SQL; no duplication, no Node-side row scan (PROJ-03).
- MUST precede build-instance-features.ts, which reads the projection via includePermissionSummary
  (PROJ-02), so the same run's embedding uses fresh data.
- Non-fatal (matches the person-graph / embedding blocks): a refresh failure never aborts the
  ingest exit status.
- TRUNCATE takes a brief ACCESS EXCLUSIVE lock (~seconds); concurrent projection readers wait then
  see the new rows — they never observe an empty table.

Do NOT change the ingest logic, exit-code handling, kill-switch, or the person-graph/embedding
blocks — only insert the new block at the top of the success branch.

In `.planning/codebase/INTEGRATIONS.md`, add a subsection under the ACC Data Connector (DC) area
(near the "Folder crawl" block, ~line 103) titled `**AccFolderPermissionSummary projection
(REF-03):**` documenting:
- WHAT: a materialised projection of the `includePermissionSummary` GROUP BY aggregate — per
  `(projectId, roleId)`: `folderCount`, `totalBytes`, `permTypes` (model `prisma/schema.prisma`,
  built by `scripts/backfill-folder-perm-summary.cjs`, reconciled by
  `scripts/verify-folder-perm-summary.cjs`).
- CONSUMER: `lib/server/acc-hot-cache.ts` `includePermissionSummary` path reads it (PROJ-02),
  feeding the per-project permission dims on `/access-analysis`; the runtime hot-cache adds
  <=10min (sliding-TTL ceiling <=1h) on top.
- REFRESH + STALENESS BOUND (SC#4): re-run after every successful `scripts/dc-daily-ingest.cjs`
  (non-fatal). Staleness bound: **<=1 daily ingest cycle**.
- CAVEAT (honest): the projection's SOURCE (`AccFolderPermission`) is updated by the SEPARATE
  folder-crawl task (`scripts/folder-crawl-cron.cjs`), not by the DC ingest. Folder-permission
  changes made by a crawl between ingest runs lag until the next successful ingest refresh.
- MANUAL FALLBACK: `node scripts/backfill-folder-perm-summary.cjs` (idempotent) forces an
  immediate rebuild.
  </action>
  <verify>
    <automated>node --check scripts/dc-daily-ingest.cjs && node scripts/backfill-folder-perm-summary.cjs && node scripts/verify-folder-perm-summary.cjs</automated>
  </verify>
  <done>The success branch runs the backfill script first (non-fatal); `node --check` passes; the backfill exits 0 within bound; reconciliation → VERDICT: PASS; INTEGRATIONS.md documents the staleness bound + caveat + manual fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner visual parity on /access-analysis + /template-mty after rebuild</name>
  <action>
This is the phase-close SC#3 gate (owner visual check). Automation is already done — do NOT
re-implement anything here; rebuild on :3000 and confirm both workshop pages render identically.
Follow the how-to-verify steps below and wait for the owner's resume signal.
  </action>
  <what-built>
PROJ-02 switched the `includePermissionSummary` path to read `AccFolderPermissionSummary` and
hard-guarded the raw-scan branch; PROJ-03 wired the projection refresh into `dc-daily-ingest.cjs`
and documented the staleness bound. All automated gates are green: `npx tsc --noEmit` clean,
`npm test` (TEST-01 on the projection + TEST-02 terrain golden masters byte-identical),
`node scripts/verify-folder-perm-summary.cjs` → PASS.
  </what-built>
  <how-to-verify>
Confirm the phase is behavior-preserving on both workshop pages via a fresh production build
(the change is server-side, so a rebuild is required — SKILL references/deploy-sequence.md):

1. `npx tsc --noEmit` → 0 errors.
2. Stop the running app on :3000 (Task Scheduler "LECG Dashboard" stop, per the deploy sequence).
3. `npm run build` → completes with no errors.
4. Restart the app on :3000.
5. Visit http://localhost:3000/access-analysis — confirm the permission dims (permission
   strength / folder breadth / accessible-data), charts, filters, and the folder-permission
   terrain render IDENTICALLY to before the phase (expected: zero visible change).
6. Visit http://localhost:3000/template-mty — confirm the folder-access-by-tier terrain and
   module bars render IDENTICALLY (expected: zero visible change).
7. (Optional) tail the next `dc-daily-ingest` run's log for
   `Refreshing AccFolderPermissionSummary projection...` to confirm the cron hook fires.
  </how-to-verify>
  <resume-signal>Type "approved" if both pages render identically, or describe any visible diff.</resume-signal>
</task>

</tasks>

<verification>
1. `node --check scripts/dc-daily-ingest.cjs` — cron script parses (no syntax error).
2. `node scripts/backfill-folder-perm-summary.cjs` — the exact command the cron invokes; exits 0,
   inserted rows within the n_projects × n_roles bound.
3. `node scripts/verify-folder-perm-summary.cjs` — post-refresh parity: VERDICT PASS (0 mismatches).
4. `npx tsc --noEmit` — exits 0 (SC#5; no `.ts` changed, gate still run).
5. `npm test` — remains green (TEST-01 + TEST-02 unaffected by cron/doc changes).
6. Scope fence: `git diff --cached --name-only` shows ONLY `scripts/dc-daily-ingest.cjs` and
   `.planning/codebase/INTEGRATIONS.md` (plus the SUMMARY). No `app/`, terrain, or spatial-graph
   files.
7. Owner visual parity checkpoint (Task 2) — SC#3, after the :3000 rebuild.
</verification>

<success_criteria>
- dc-daily-ingest.cjs refreshes AccFolderPermissionSummary (non-fatal) as the first success-branch
  step, before build-instance-features.ts, by invoking scripts/backfill-folder-perm-summary.cjs.
- No Node-side raw scan, no duplicated aggregate SQL (script reused verbatim).
- INTEGRATIONS.md documents the <=1-ingest-cycle staleness bound, the out-of-band folder-crawl
  caveat, and the manual fallback (SC#4).
- `node scripts/verify-folder-perm-summary.cjs` → PASS post-refresh.
- `npx tsc --noEmit` exits 0; `npm test` green.
- Owner confirms /access-analysis + /template-mty render identically after rebuild (SC#3).
</success_criteria>

<output>
After completion, create `.planning/phases/19-raw-scan-retirement-refresh/19-02-SUMMARY.md`.
</output>
