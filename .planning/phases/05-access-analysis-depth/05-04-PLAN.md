---
phase: 05-access-analysis-depth
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - app/(dashboard)/access-analysis/page.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/TerrainReveal.tsx
  - app/(dashboard)/access-analysis/components/DonutSkeletons.tsx
  - app/(dashboard)/access-analysis/page.test.tsx
  - app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx
autonomous: false
requirements: [ACC-01, ACC-03, PERF-02, PERF-05, VIS-01, VIS-05, NA-01, INT-05]

must_haves:
  truths:
    - "The first tier (KPIs + donuts) renders fast with per-panel shaped shimmer skeletons; the timeline and terrain stream in separate Suspense tiers instead of one blocking Promise.all"
    - "The folder-permission terrain starts collapsed and only builds its account-wide overview when the user clicks to expand (no 2-3s first-interaction lag)"
    - "Every panel is wrapped in PremiumSurface with 2.5D depth + staggered reveal; donuts sit in a denser 2-up grid on wide screens, 1-up when narrow; timeline + terrain stay full-width"
    - "The Activity coverage badge appears on each activity-derived panel header"
    - "KPI numbers track the active filter but the count animation fires only on first load, never on filter change"
  artifacts:
    - path: "app/(dashboard)/access-analysis/page.tsx"
      provides: "Suspense-tiered RSC data loading (no single blocking Promise.all)"
      contains: "Suspense"
    - path: "app/(dashboard)/access-analysis/components/TerrainReveal.tsx"
      provides: "collapsed click-to-expand terrain that loads overview on first open"
      contains: "loadOverview"
    - path: "app/(dashboard)/access-analysis/components/DonutSkeletons.tsx"
      provides: "shaped shimmer placeholders (donut rings, KPI tiles, bars)"
      contains: "animate-shimmer"
  key_links:
    - from: "page.tsx tiers"
      to: "React Suspense streaming"
      via: "<Suspense fallback={<...Skeleton/>}> around async tier components"
      pattern: "Suspense"
    - from: "TerrainReveal expand"
      to: "loadOverview server action"
      via: "onExpand fires loadOverview() (NOT loadFolderPermissionTerrain on default project)"
      pattern: "loadOverview"
    - from: "panels"
      to: "PremiumSurface"
      via: "<PremiumSurface variant=\"base\"> wrapping each section"
      pattern: "PremiumSurface"
---

<objective>
The presentation surgery: split `page.tsx`'s single blocking `Promise.all` into Suspense tiers so KPIs + donuts paint fast (PERF-02), wrap every panel in `PremiumSurface` for 2.5D depth + staggered reveal (ACC-01/VIS-01), lay donuts out in a denser 2-up grid (ACC-02 layout), make the terrain collapsed-and-lazy (ACC-03) loading its account-wide overview only on expand, place the Activity coverage badge on activity panels (NA-01), and guard the KPI count so it animates only on first load (VIS-05). Ends with a human visual checkpoint.

Purpose: This is where the page goes from "flat single-column blocking stack" to "fast, deep, live dashboard". It owns the two files (page.tsx + AccessAnalysisCharts.tsx) that the prior waves left for last.
Output: tiered page.tsx, PremiumSurface-wrapped 2-up grid, TerrainReveal, DonutSkeletons, badge placement.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-access-analysis-depth/05-CONTEXT.md
@.planning/phases/05-access-analysis-depth/05-RESEARCH.md
@app/(dashboard)/access-analysis/page.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@components/ui/PremiumSurface.tsx
@components/ui/stat-tile.tsx
@.planning/phases/05-access-analysis-depth/05-02-SUMMARY.md
@.planning/phases/05-access-analysis-depth/05-03-SUMMARY.md

<interfaces>
```typescript
// PremiumSurface (components/ui/PremiumSurface.tsx):
//   <PremiumSurface variant="base" glow? className?>...</PremiumSurface>  // base = panel-elevated (same class panels use now)
// Skeleton shimmer: <Skeleton className="animate-shimmer" /> from @/components/ui/skeleton + --shimmer-stop CSS var.
// Reveal (whileInView, once) from @/components/ui/animated-list — already wraps each section; keep it.
// Terrain component already supports mode="overview" calling loadOverview() lazily (FolderPermissionTerrain.tsx ~line 252).
// page.tsx currently: one Promise.all of 7 loaders + a SEQUENTIAL await loadFolderPermissionTerrain(defaultProject.id) (the expensive part).
// Server actions available: loadOverviewTerrain (loadOverview prop), loadTerrainForProject (loadTerrain prop).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier page.tsx with Suspense streaming + drop the blocking terrain await</name>
  <files>app/(dashboard)/access-analysis/page.tsx, app/(dashboard)/access-analysis/components/DonutSkeletons.tsx, app/(dashboard)/access-analysis/page.test.tsx</files>
  <action>
Goal: KPIs + donuts (Tier 1) stream first; timeline (Tier 2) and terrain (Tier 3) stream behind Suspense. Remove the expensive sequential `loadFolderPermissionTerrain(defaultProject.id)` blocking await entirely (Pitfall 3 — terrain now builds its overview on expand, so `initialTerrain` is no longer pre-loaded).

1. Create `DonutSkeletons.tsx` ("use client" or plain — presentational) exporting shaped shimmer skeletons using `<Skeleton className="animate-shimmer">` (from `@/components/ui/skeleton`) + the existing `--shimmer-stop` token:
   - `DonutPanelSkeleton` — a panel-elevated card with a circular ring shimmer (outer ring + bg-card center hole) ~400px tall.
   - `TimelineSkeleton` — a panel-elevated card with horizontal bar shimmers.
   - `KpiStripSkeleton` — a row of ~6 KPI-tile shimmers matching StatStrip geometry.
   - `DonutGridSkeleton` — 2-up grid of `DonutPanelSkeleton`.
   Geometry/stops are Claude's discretion within the Phase-1 token set; keep within the 200ms shimmer feel.

2. Restructure `page.tsx`:
   - Decompose into RSC tier components (Claude's discretion — keep page.tsx readable; the simplest readable split is preferred). Recommended:
     * Tier 1: an async function/inline await of `Promise.all([loadInstanceView(), loadModuleActivity(), loadActivityByActor(), loadCoordinationByProject(), loadProjectCoverage()])` → renders KPIs + all donuts (the existing `<AccessAnalysisCharts>` donut surface). This is the fast path.
     * Tier 2: a small async RSC (e.g. `<TimelineTier>`) that awaits `loadActivityTimeline()` then renders the timeline client component, wrapped in `<Suspense fallback={<TimelineSkeleton/>}>`.
     * Tier 3: a small async RSC (e.g. `<TerrainTier>`) that awaits the CHEAP `loadTerrainProjects()` then renders `<TerrainReveal>` (Task 2), wrapped in `<Suspense fallback={<DonutPanelSkeleton/>}>`. Do NOT call `loadFolderPermissionTerrain` here.
   - DELETE the `defaultProject` selection block + the `initialTerrain = await loadFolderPermissionTerrain(...)` await (lines ~27-36). The terrain overview is loaded on expand by the existing `loadOverview` action.
   - IMPORTANT RSC→client boundary (Pitfall 6): each tier async RSC awaits data then renders the CLIENT component with data as props; do not call client `useQuery`. Keep `AuthorProfileDrawer`'s existing `dynamic(ssr:false)` lazy pattern untouched.
   - To keep the cross-filter (which spans donuts + timeline) working across tiers, the simplest correct approach is: keep `AccessAnalysisCharts` as the single client owner of `selected`+`sliceFilters` and pass timeline/terrain data into it as before, BUT wrap the page's RSC awaits so Tier-1 data resolves and renders the KPIs+donuts shell while Tier-2/3 awaits stream their props in. If splitting the client component across Suspense boundaries is too invasive, an acceptable simpler split is: Suspense-wrap the WHOLE `<AccessAnalysisCharts>` with a Tier-1 fallback (`KpiStripSkeleton`+`DonutGridSkeleton`) and stream timeline+terrain props via nested awaits — provided the heavy terrain await is removed so first paint is fast. Document the chosen decomposition in the SUMMARY. The non-negotiable outcome: NO single Promise.all blocks on the expensive terrain load, and a skeleton shows within the budget.

3. Update `page.test.tsx`: the page no longer pre-loads terrain. Update the `loadFolderPermissionTerrain` mock expectation — assert it is NOT called during initial render (or remove its initial-call assumption); keep the existing "roles donut buckets / 2 roles" assertion working (the donut surface still renders). Adjust mocks for any new tier component boundaries. Keep assertions on `.getAttribute`/`.textContent`; NO jest-dom matchers.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/page.test.tsx"</automated>
  </verify>
  <done>page.tsx streams Tier1 KPIs+donuts behind Suspense with skeleton fallbacks; the blocking loadFolderPermissionTerrain await is gone; timeline + terrain stream in their own tiers; page.test passes.</done>
</task>

<task type="auto">
  <name>Task 2: TerrainReveal collapse-to-expand wrapper (ACC-03)</name>
  <files>app/(dashboard)/access-analysis/components/TerrainReveal.tsx, app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx</files>
  <action>
Create `TerrainReveal.tsx` ("use client") — a collapsed panel that defers building the terrain until expanded:
```typescript
export function TerrainReveal({
  projects, loadTerrain, loadOverview,
}: { projects: TerrainProjectOption[]; loadTerrain: (id: string) => Promise<FolderTerrainData|null>; loadOverview: () => Promise<FolderTerrainData|null>; }) { ... }
```
- Default state collapsed: render a `PremiumSurface variant="base"` header row "Folder permission terrain" with a "Show" button (`data-testid="terrain-expand"`), a one-line subtitle, and NO `<FolderPermissionTerrain>` mounted (so nothing heavy builds).
- On expand (`const [open, setOpen] = useState(false)`), mount `<FolderPermissionTerrain projects={projects} initial={null} loadTerrain={loadTerrain} loadOverview={loadOverview} />`. The terrain's own `mode="overview"` effect fires `loadOverview()` on first open → account-wide overview content immediately (locked decision), with its existing project picker to focus one project. Pass `initial={null}` (no pre-loaded project terrain).
- Collapse toggles back to the header; keep it re-expandable.

In `AccessAnalysisCharts.tsx`: REPLACE the always-rendered `<FolderPermissionTerrain ...>` section (lines ~193-206) with `<TerrainReveal projects={terrainProjects} loadTerrain={loadTerrain} loadOverview={loadOverview} />` (drop the `initialTerrain` prop usage; the prop can remain in the signature for back-compat but is no longer passed to terrain). Keep it full-width (not in the 2-up grid). Terrain stays OUT of cross-filter scope.

Update `FolderPermissionTerrain.test.tsx`: there are 2 PRE-EXISTING failures (concurrent WIP) — do NOT fix or further regress them; only ensure your changes don't add NEW failures. If the test renders the terrain directly (not via TerrainReveal), it should be unaffected. If it relied on `initialTerrain`, note it. Add/extend a TerrainReveal interaction check IF a clean home exists (a new `TerrainReveal.test.tsx` is acceptable if simpler): collapsed by default (terrain canvas/test-id absent), clicking `terrain-expand` mounts the terrain and triggers `loadOverview` (mock it, assert it was called, assert `loadFolderPermissionTerrain`-style default-project load was NOT). NO jest-dom matchers.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"</automated>
  </verify>
  <done>Terrain is collapsed by default; expanding mounts it and loads the account-wide overview via loadOverview (not a default-project preload); the 2 pre-existing terrain failures are unchanged (not increased).</done>
</task>

<task type="auto">
  <name>Task 3: PremiumSurface wraps + 2-up donut grid + coverage badge placement + KPI no-reanimate guard</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx</files>
  <action>
Visual depth + layout + honesty, all in AccessAnalysisCharts.tsx:

1. ACC-01 / VIS-01 — wrap EVERY panel section in `<PremiumSurface variant="base">` (import from `@/components/ui/PremiumSurface`). Keep the existing `<Reveal>` for staggered entrance (it already fires whileInView/once — correct for VIS-05). Pattern per the research example:
   `<Reveal><PremiumSurface variant="base" className="flex flex-col gap-3 p-5 overflow-hidden"><section className="flex flex-col gap-3">...<chart/></section></PremiumSurface></Reveal>`
   The donut components already render an inner `panel-elevated p-5` div — to avoid double-nesting, either drop the chart's inner `panel-elevated` wrapper OR wrap the section content without re-applying panel-elevated. Cleanest: have PremiumSurface be the panel and remove the redundant inner `panel-elevated p-5` from the donut wrappers (keep their `<style>`+content). Claude's discretion — ensure exactly ONE elevated surface per panel (no nested double-card).
   Use `glow` on the PremiumSurface of a panel whose dimension is the ACTIVE slice filter (e.g. the role donut gets `glow` when `sliceFilters.role` is set) — a card-level cue of what's driving the filter.

2. ACC-02 layout — denser 2-up grid for the DONUTS only (Role distribution, Users by company, Activity by role, Activity by company, Activity by module). Wrap them in a responsive grid container: `grid grid-cols-1 lg:grid-cols-2 gap-6` (breakpoint is Claude's discretion — pick where labels stay legible; `lg` is a sane default). The Activity timeline and the TerrainReveal stay FULL-WIDTH (outside the grid). Model Coordination: Claude's discretion (full-width is fine — it is a wide panel). Keep the KPI StatStrip + ProjectPicker + PillBar full-width above the grid.

3. NA-01 — render `<ActivityCoverageBadge covered={covered} total={total} />` (from 05-03) in the header of each ACTIVITY-derived panel: Activity over time, Activity by role, Activity by company, Activity by module. Derive counts with the 05-03 helper: `import { activityCoverageCounts } from "../coverageCounts";` then `const { covered, total } = activityCoverageCounts(coverage);` (coverage is already a prop). Place the badge in/next to the SectionHeader for those four panels only (role/company MEMBERSHIP donuts are not activity-derived → no badge). Do NOT badge the non-activity panels.

4. VIS-05 — KPI count must update on filter but NOT re-animate. The StatStrip's `useEntrance()` fires its framer-motion entrance on MOUNT only (keyed by index, not value), so changing `kpis` values re-renders the number without restarting the entrance — this is already correct. ADD a guard to be safe: ensure the StatStrip is NOT remounted on filter change (no changing `key`), and that nothing wraps it in a value-keyed `<AnimatePresence>`. If a count-up animation component is in play, gate it behind a `useRef(false)` "hasAnimated" flag so it runs once. Document in SUMMARY that KPI entrance is mount-once and values update in place.

Keep all existing `data-testid`s, drill behavior, and the cross-filter wiring from 05-02 intact.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx"</automated>
  </verify>
  <done>Every panel wrapped in a single PremiumSurface; donuts in a 2-up grid (1-up narrow); timeline+terrain full-width; activity panels show the coverage badge; KPI entrance is mount-once; AccessAnalysisCharts tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — depth, tiered load, cross-filter, terrain reveal</name>
  <action>Rebuild on an idle window (never under live :3000) and visually verify the full /access-analysis surface against the steps below; pause for owner sign-off.</action>
  <what-built>Tiered fast-loading /access-analysis with 2.5D PremiumSurface depth, denser 2-up donut grid, gradient-fill donuts on the themed wrapper, client-side cross-filter (click a slice → others refocus + pill + slice glow), "View N people →" slide-in, collapsed lazy terrain, and activity coverage badges — in both light and dark.</what-built>
  <how-to-verify>
    The dashboard runs on :3000 via Task Scheduler. Do NOT run `npm run build` while :3000 is live (it 500s the running app). To preview these changes, rebuild per the project's deploy flow on an idle window, then:
    1. Open /access-analysis. Confirm KPIs + donuts paint fast (~300ms feel) with shaped shimmer skeletons, not one page spinner; timeline + terrain stream in after.
    2. Confirm every panel has clear 2.5D depth (soft layered shadow + catch-light), donuts are in a 2-up grid on a wide window and stack to 1-up when narrowed; timeline + terrain are full-width.
    3. Click a role slice → the OTHER donuts + timeline refocus, the clicked slice pulls out + glows while siblings dim, and a pill appears. The people panel does NOT open. Click the same slice → pill clears.
    4. Add a company slice too → both pills present (AND). Click a pill X → that filter clears; "Clear all" wipes pills but leaves the Project Picker selection intact.
    5. Click "View N people →" on a panel → the shared slide-in people list opens for the current filtered view; clicking a person opens their profile drawer.
    6. The folder-permission terrain starts collapsed ("Show"); clicking it expands to the account-wide overview within ~1s (no 2-3s freeze) with a project picker.
    7. Activity panels show "Activity: 428 / 1,152 projects" (or the live numbers).
    8. Toggle light/dark — all chart labels stay sharp and legible in both.
    9. KPI numbers change when filtering but do NOT re-run the count animation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (lag, depth too subtle/dramatic, layout jump, contrast, motion re-firing).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (REQUIRED before any rebuild — next build typechecks the whole tree incl. tests).
- `npm test`: AccessAnalysisCharts + page tests pass; full passing count holds at/above the 2012 baseline; the 2 pre-existing FolderPermissionTerrain failures are NOT increased.
- Grep: no `loadFolderPermissionTerrain(` call remains in page.tsx (terrain is lazy via TerrainReveal/loadOverview).
- Grep: `Suspense` present in page.tsx; `PremiumSurface` present in AccessAnalysisCharts.tsx; `animate-shimmer` present in DonutSkeletons.tsx.
- No `import { trpc }` / new `useQuery` introduced (Pitfall 2 / PERF-03). No WebGL / echarts-gl (PERF-05).
- Human visual checkpoint approved.
</verification>

<success_criteria>
- Tier 1 (KPIs+donuts) paints fast with per-panel shimmer skeletons; timeline + terrain stream progressively; no single blocking Promise.all on terrain (PERF-02, ACC-03).
- All panels in PremiumSurface 2.5D depth + staggered reveal; donuts 2-up, timeline+terrain full-width (ACC-01, VIS-01).
- Terrain collapsed → expands to account-wide overview on click, no first-interaction lag (ACC-03).
- Activity coverage badge on activity panels (NA-01); pill bar present (INT-05).
- KPI updates without re-animating (VIS-05); no new WebGL (PERF-05).
</success_criteria>

<output>
After completion, create `.planning/phases/05-access-analysis-depth/05-04-SUMMARY.md` documenting the Suspense tier decomposition chosen and how the terrain lazy-load was wired.
</output>
