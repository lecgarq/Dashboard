---
phase: 05-access-analysis-depth
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - app/(dashboard)/access-analysis/components/RolesPieChart.tsx
  - app/(dashboard)/access-analysis/components/CompaniesPieChart.tsx
  - app/(dashboard)/access-analysis/components/ActivityByRolePieChart.tsx
  - app/(dashboard)/access-analysis/components/CompaniesActivityPieChart.tsx
  - app/(dashboard)/access-analysis/components/ModulesPieChart.tsx
  - app/(dashboard)/access-analysis/components/ActivityTimelineChart.tsx
  - app/(dashboard)/access-analysis/__tests__/chartContrast.test.ts
autonomous: false
requirements: [THM-01, VIS-05]

must_haves:
  truths:
    - "Every chart's data-label / center-label / tooltip-title text passes WCAG AA contrast (>=4.5:1) against its surface in BOTH light and dark themes"
    - "Light and dark are tuned to the same contrast bar — no theme was left under-contrast on data values"
    - "Drill/reveal motion is smooth + directional (<=200ms) and fires only on mount/drill, never on filter change"
  artifacts:
    - path: "app/(dashboard)/access-analysis/__tests__/chartContrast.test.ts"
      provides: "automated WCAG AA contrast-ratio assertions on the chart label color tokens for both themes"
      contains: "contrastRatio"
  key_links:
    - from: "chart label color constants"
      to: "WCAG AA threshold"
      via: "contrastRatio(fg, bg) >= 4.5 asserted for light + dark"
      pattern: "4.5"
---

<objective>
Verify and, where needed, fix THM-01 on the densest data surface (the locked decision: THM-01 is verified HERE). Audit every chart's text colors — donut center labels, sub-labels, tooltip titles, timeline axis labels — for WCAG AA (>=4.5:1) contrast against their actual surface in BOTH light and dark (zinc) themes, tuned to the same bar. Add an automated contrast-ratio test so the gate is repeatable, then a projector-brightness human checkpoint.

Purpose: This is the phase's theme/legibility gate. The migration to the canonical wrapper (05-01) handles axis/tooltip chrome; this plan closes the remaining data-label gap and proves it.
Output: any color fixes needed + an automated `chartContrast.test.ts` + a human projector-brightness sign-off.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-access-analysis-depth/05-CONTEXT.md
@.planning/phases/05-access-analysis-depth/05-RESEARCH.md
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx
@lib/colors/echartsTheme.ts
@.planning/phases/05-access-analysis-depth/05-01-SUMMARY.md

<interfaces>
```
Known label colors after 05-01 migration (RESEARCH §8):
  Donut center big-number  cTitle: dark #fafafa / light #111827
  Donut sub-label          cSub:   dark #a1a1aa / light #6b7280
  Surfaces: light card #FFFFFF; dark card ≈ #18181b (rgba(24,24,27,0.96))
Reported ratios (RESEARCH §8):
  light cTitle on #fff = 16:1 (pass) | dark cTitle on #18181b ≈ 20:1 (pass)
  light cSub  on #fff = 4.6:1 (barely passes) | dark cSub on #18181b = 5.6:1 (pass)
Tooltip/axis chrome now comes from mergeEChartsTheme (ECHARTS_DARK/LIGHT palettes in echartsTheme.ts).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated WCAG AA contrast test over all chart label tokens (both themes)</name>
  <files>app/(dashboard)/access-analysis/__tests__/chartContrast.test.ts</files>
  <action>
Create `chartContrast.test.ts` (Node env — pure math, no DOM). Implement a small pure `contrastRatio(fgHex: string, bgHex: string): number` using the WCAG relative-luminance formula (sRGB → linearize → L = 0.2126R+0.7152G+0.0722B → ratio = (Llighter+0.05)/(Ldarker+0.05)). Handle 6-digit hex; for the dark card, use the opaque approximation `#18181b` (the 0.96-alpha tooltip bg over a near-black page is effectively #18181b — document this).

Assert >= 4.5 (WCAG AA for normal text) for EACH chart's data-label colors against its surface, for BOTH themes. Enumerate the actual color constants the charts use (pull them from the chart files post-05-01 — center label, sub label, tooltip title text, and the timeline axis-label color from the palette). Cases (each must be >= 4.5):
  - light center-label on #FFFFFF
  - dark center-label on #18181b
  - light sub-label on #FFFFFF
  - dark sub-label on #18181b
  - light tooltip-title text on light tooltip bg
  - dark tooltip-title text on dark tooltip bg (from ECHARTS_DARK)
  - timeline axis label (both themes) on the card surface
If any pair is < 4.5, that is a RED that Task 2 fixes. Keep the color constants imported or duplicated-with-a-comment so the test tracks the real values. NO jest-dom matchers (this is a pure-math test anyway).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/chartContrast.test.ts"</automated>
  </verify>
  <done>chartContrast.test.ts runs and asserts AA on every chart label pair in both themes; failures (if any) precisely name the under-contrast pair for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any sub-AA label colors so both themes clear the same bar</name>
  <files>app/(dashboard)/access-analysis/components/RolesPieChart.tsx, app/(dashboard)/access-analysis/components/CompaniesPieChart.tsx, app/(dashboard)/access-analysis/components/ActivityByRolePieChart.tsx, app/(dashboard)/access-analysis/components/CompaniesActivityPieChart.tsx, app/(dashboard)/access-analysis/components/ModulesPieChart.tsx, app/(dashboard)/access-analysis/components/ActivityTimelineChart.tsx</files>
  <action>
For any pair Task 1 flagged below 4.5:1, darken (light theme) or lighten (dark theme) the offending label color the MINIMUM needed to clear 4.5:1 while staying tonally consistent (keep it a muted sub-label, just legible). The likely candidate is the light sub-label `#6b7280` on white (4.6:1 — marginal at projector brightness): nudge it to e.g. `#5b616b`/`#52525b` (zinc-600) to give headroom. Apply the SAME treatment philosophy to both themes so neither is left at the floor (CONTEXT: "tune BOTH light and dark to the same contrast bar").
- Only touch the data-label color constants (`cTitle`/`cSub` and any timeline axis-label override). Do NOT change slice `itemStyle.color` (data-series hues), gradient fills, or the emphasis glow from 05-01/05-02.
- Re-run the contrast test until every pair is >= 4.5 in both themes.
- Confirm VIS-05 motion is intact: drill/reveal transitions are smooth + directional (the `animationDurationUpdate`/`universalTransition` set in 05-01/05-02), <=200ms feel, and nothing here introduces a filter-change animation. Do not add motion.
If Task 1 found NOTHING below 4.5, make no color change — just record in the SUMMARY that all pairs already passed and the test now guards it.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/chartContrast.test.ts" && npx tsc --noEmit</automated>
  </verify>
  <done>Every chart label pair >= 4.5:1 in both themes; contrast test green; tsc 0; series hues/gradients/glow unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Projector-brightness legibility sign-off (THM-01)</name>
  <action>Rebuild on an idle window (never under live :3000) and verify chart legibility at projector-reduced brightness in both themes per the steps below; pause for owner sign-off.</action>
  <what-built>WCAG-AA-verified chart legibility across /access-analysis in both light and dark, with an automated contrast gate. THM-01 verified on the densest data surface.</what-built>
  <how-to-verify>
    On the running :3000 dashboard (rebuild on an idle window per the deploy flow — do NOT build under the live :3000):
    1. Open /access-analysis in DARK theme on a secondary display at projector-reduced brightness. Confirm every donut center number, sub-label, legend value, tooltip, and the timeline axis labels are clearly readable — none washed out.
    2. Switch to LIGHT theme at the same brightness. Confirm the same legibility — sub-labels are not too faint.
    3. Spot-check with DevTools (or aXe) the contrast on a donut sub-label and a tooltip title in each theme — both should report >= 4.5:1.
    4. Confirm drill/reveal motion is smooth + directional and does NOT re-fire when you only change a slice filter.
  </how-to-verify>
  <resume-signal>Type "approved" or name any label/theme that reads poorly at projector brightness.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test`: chartContrast.test passes (all pairs >= 4.5:1 both themes); full suite holds at/above the 2012 baseline; the 2 pre-existing FolderPermissionTerrain failures are not increased.
- No new WebGL / no new query introduced (PERF-05 / Pitfall 2).
- Human projector-brightness checkpoint approved.
</verification>

<success_criteria>
- Every chart data label passes WCAG AA in both themes, automated + repeatable (THM-01).
- Light and dark tuned to the same bar — neither left at the floor.
- Drill/reveal motion smooth/directional, mount/drill-only (VIS-05 re-confirmed).
</success_criteria>

<output>
After completion, create `.planning/phases/05-access-analysis-depth/05-05-SUMMARY.md` listing the contrast ratios found per pair and any color nudge applied.
</output>
