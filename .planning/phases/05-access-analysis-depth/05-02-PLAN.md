---
phase: 05-access-analysis-depth
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/PillBar.tsx
  - app/(dashboard)/access-analysis/components/RolesPieChart.tsx
  - app/(dashboard)/access-analysis/components/CompaniesPieChart.tsx
  - app/(dashboard)/access-analysis/components/ActivityByRolePieChart.tsx
  - app/(dashboard)/access-analysis/components/CompaniesActivityPieChart.tsx
  - app/(dashboard)/access-analysis/__tests__/PillBar.test.tsx
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: true
requirements: [INT-04, INT-05, INT-02, VIS-05]

must_haves:
  truths:
    - "Clicking a role/company donut slice sets a slice filter that re-buckets the OTHER donuts + timeline client-side with zero new queries"
    - "Filters stack as AND, one value per dimension; clicking an already-active slice toggles it off"
    - "An active-filter pill bar shows the current slice filters with a per-pill X and a Clear all that leaves the Project Picker untouched"
    - "A 'View N people →' affordance in each filterable panel header opens the shared slide-in people panel for that panel's current filtered view"
    - "Clicking a slice does NOT auto-open the people panel and does NOT re-animate the KPI count"
  artifacts:
    - path: "app/(dashboard)/access-analysis/components/PillBar.tsx"
      provides: "active slice-filter pill bar with per-pill remove + Clear all"
      contains: "Clear all"
    - path: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      provides: "sliceFilters state + applySliceFilters in donut memos + onSliceClick wiring + PillBar render"
      contains: "applySliceFilters"
  key_links:
    - from: "RolesPieChart/CompaniesPieChart/ActivityBy* slice click"
      to: "AccessAnalysisCharts setSliceFilters"
      via: "onSliceClick(dim, val) prop"
      pattern: "onSliceClick"
    - from: "AccessAnalysisCharts donut memos"
      to: "applySliceFilters"
      via: "applySliceFilters(filterRowsBySelection(rows, selected), sliceFilters)"
      pattern: "applySliceFilters\\("
    - from: "PillBar"
      to: "sliceFilters state"
      via: "filters prop + onRemove/onClear callbacks"
      pattern: "onClear"
---

<objective>
Implement the headline INT-04 cross-filter: clicking a chart slice refocuses the OTHER donuts + timeline (zero new queries), drops an active-filter pill (INT-05), and the clicked slice glows while its siblings dim (the visible "lean-in" payoff). Add a "View N people →" affordance (INT-02) that opens the shared slide-in people panel for the current filtered view — the slice click itself does NOT open the panel (locked CONTEXT decision). Honor VIS-05: KPI numbers track the filter but do NOT re-animate.

Purpose: This is the single most important new behaviour of the phase.
Output: `sliceFilters` state + `applySliceFilters` in the donut memos, a new `PillBar` component, `onSliceClick` lifted from the four filterable donuts, and a "View N people" affordance.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-access-analysis-depth/05-CONTEXT.md
@.planning/phases/05-access-analysis-depth/05-RESEARCH.md
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx
@app/(dashboard)/access-analysis/projectFilter.ts

# 05-01 added applySliceFilters + SliceFilters and the RED PillBar stub this plan implements.
@.planning/phases/05-access-analysis-depth/05-01-SUMMARY.md

<interfaces>
```typescript
// From 05-01 (projectFilter.ts):
export type SliceFilters = Record<string, string>;
export function applySliceFilters<T extends { roles?: string[]; company?: string | null }>(
  rows: ReadonlyArray<T>, filters: SliceFilters): T[];
export function filterRowsBySelection<T extends { projectId: string }>(
  rows: ReadonlyArray<T>, selected: ReadonlySet<string>): T[];

// AccessAnalysisCharts already holds:
//   const [selected, setSelected] = useState<Set<string>>(...)   // project picker — UNCHANGED
//   const [profileEmail, setProfileEmail] = useState<string|null>(null)  // person drawer — reuse for "View N people" person clicks
// Donut memos currently: summarizeRoles(filterRowsBySelection(roleRows, selected)), etc.

// RolesPieChart current click handler (RolesPieChart.tsx:204):
//   onEvents={{ click: (p) => p.name && toggleDrill(p.name) }}
// toggleDrill currently only sets local setDrill — lift it via a new onSliceClick prop.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the PillBar component (turns 05-01 RED stub GREEN)</name>
  <files>app/(dashboard)/access-analysis/components/PillBar.tsx, app/(dashboard)/access-analysis/__tests__/PillBar.test.tsx</files>
  <action>
Create `PillBar.tsx` ("use client") satisfying the 05-01 stub. Props:
```typescript
export interface PillBarProps {
  filters: SliceFilters;                       // import type from "../projectFilter"
  onRemove: (dim: string) => void;
  onClear: () => void;
  labels?: Partial<Record<string, string>>;    // optional dim → human label, e.g. { role: "Role", company: "Company" }
}
```
Behavior:
- When `Object.keys(filters).length === 0`, render `null` (no layout — the empty-bar case).
- Otherwise render a horizontal wrap row (`flex flex-wrap items-center gap-2`) with one pill per active filter. Each pill: a small rounded chip (use existing token classes: `border border-border bg-primary/10 text-primary` style, consistent with the repo's chip pattern in RolesPieChart `chip()`), showing `${label}: ${value}` and a trailing X button (`aria-label={`Remove ${label} filter`}`) calling `onRemove(dim)`.
- A trailing "Clear all" button (`type="button"`) calling `onClear`.
- Render pill text as React text nodes (NO dangerouslySetInnerHTML — filter values are server-origin DB strings, render safely; Security note).
- Wrap pill enter/exit in framer-motion `AnimatePresence` + a short fade/scale (≤200ms) per the repo motion facade (`fadeUp`/`useSafeVariants` from `@/components/ui/motion` if convenient; reduced-motion safe). Pill appear/leave animation styling is Claude's discretion.
- Put a stable `data-testid="slice-pill-bar"` on the container and `data-testid` per pill is optional.

Update `PillBar.test.tsx` (05-01 stub): remove the `@ts-expect-error` on the import now that the component exists; confirm the assertions hold (pills render, per-pill X calls onRemove with the dim, Clear all calls onClear, empty filters renders nothing). NO jest-dom matchers.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/PillBar.test.tsx"</automated>
  </verify>
  <done>PillBar renders pills + per-pill X + Clear all; empty filters renders null; PillBar.test passes (GREEN); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sliceFilters state + applySliceFilters into AccessAnalysisCharts; lift onSliceClick from the four donuts</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/components/RolesPieChart.tsx, app/(dashboard)/access-analysis/components/CompaniesPieChart.tsx, app/(dashboard)/access-analysis/components/ActivityByRolePieChart.tsx, app/(dashboard)/access-analysis/components/CompaniesActivityPieChart.tsx</files>
  <action>
In `AccessAnalysisCharts.tsx`:
1. Import `applySliceFilters`, type `SliceFilters` (from `../projectFilter`) and `PillBar`.
2. Add state next to `selected` (line ~101): `const [sliceFilters, setSliceFilters] = useState<SliceFilters>({});` and a toggle:
   ```typescript
   const toggleSliceFilter = (dim: string, val: string) =>
     setSliceFilters((prev) => {
       const next = { ...prev };
       if (next[dim] === val) delete next[dim]; else next[dim] = val;
       return next;
     });
   ```
3. In EACH donut + timeline memo, compose the second filter AFTER `filterRowsBySelection`. The scope is donuts + timeline ONLY (locked decision — terrain + Model Coordination are project-scoped and must NOT receive sliceFilters):
   - `roleSummary`: `summarizeRoles(applySliceFilters(filterRowsBySelection(roleRows, selected), sliceFilters))`
   - `companySummary`: same wrap on roleRows
   - `activityByRoleSummary` / `activityByCompanySummary`: wrap BOTH the actor-rows and membership-rows args with `applySliceFilters(filterRowsBySelection(..., selected), sliceFilters)` (note: ActivityActorRow has no roles/company, so applySliceFilters is a no-op on the actor arg unless those fields exist — it is safe; the real narrowing happens on membershipRows which carry roles+company). To keep activity attribution correct, apply the slice filter to `membershipRows` (which carries roles+company) and let the actor join narrow naturally.
   - `moduleSummary`: wrap moduleRows (ModuleActivityRow has no roles/company → applySliceFilters is a safe no-op, but include the wrap for consistency so a future company-on-module dimension flows).
   - `timelineSummary`: timeline filters internally by the `selected` Set; to keep it in scope, pass a project-id subset narrowed by the active slice. SIMPLER + correct: leave timeline driven by `selected` for now BUT note in SUMMARY that timeline slice-narrowing is by project membership; if the slice filters by role/company, narrow `timelineRows` by the set of projectIds present in `applySliceFilters(filterRowsBySelection(roleRows, selected), sliceFilters)` and intersect with `selected`. Implement the projectId-intersection approach so the timeline refocuses with the donuts (locked: "all donuts and the timeline cross-filter together").
   - Add `sliceFilters` to each memo's dependency array.
4. Add a `data-testid="slice-pill-bar"` PillBar render directly under the ProjectPicker:
   `<PillBar filters={sliceFilters} onRemove={(d) => toggleSliceFilter(d, sliceFilters[d])} onClear={() => setSliceFilters({})} labels={{ role: "Role", company: "Company" }} />`
   (onRemove toggles the active value off; Clear all wipes only sliceFilters — Project Picker `selected` is untouched.)
5. Pass `onSliceClick` to the four filterable donuts:
   - RolesPieChart + ActivityByRolePieChart → `onSliceClick={(val) => toggleSliceFilter("role", val)}`
   - CompaniesPieChart + CompaniesActivityPieChart → `onSliceClick={(val) => toggleSliceFilter("company", val)}`
   - Also pass the currently-active value so the donut can pull-out/glow its slice: `activeSlice={sliceFilters.role}` (role donuts) / `activeSlice={sliceFilters.company}` (company donuts).
   - ModulesPieChart, ActivityTimelineChart, CoordinationByProject, FolderPermissionTerrain get NO onSliceClick (out of cross-filter scope).

In EACH of the four donut components (Roles, Companies, ActivityByRole, CompaniesActivity):
6. Add optional props `onSliceClick?: (value: string) => void;` and `activeSlice?: string;`.
7. In the chart's `onEvents.click` handler, ALSO call `onSliceClick?.(name)` (keep the existing local `toggleDrill`/legend drill behavior — they coexist; the slice click cross-filters, the legend/people drill still works). For the "Others (...)" / warning buckets, do NOT cross-filter (only call onSliceClick for real role/company names).
8. Visual anchor (INT-04 locked: clicked slice pulls out + glows, siblings dim): when `activeSlice` is set, in the series `data.map`, give the matching slice a larger `emphasis`-like lift — set its `itemStyle` with a stronger `shadowBlur` and add `selected`/offset via the pie `selectedMode: "single"` + mark that datum `selected: true`, and reduce non-active slices' opacity (e.g. `itemStyle.opacity: 0.5`) so the active one stands out. Keep it subtle. Exact glow radius/offset is Claude's discretion within the Phase-1 token feel.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" "app/(dashboard)/access-analysis/__tests__/RolesPieChart.test.tsx" "app/(dashboard)/access-analysis/__tests__/CompaniesPieChart.test.tsx"</automated>
  </verify>
  <done>sliceFilters state drives applySliceFilters in donut+timeline memos; four donuts accept onSliceClick + activeSlice; PillBar renders under the picker; existing chart/picker tests still pass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add the "View N people →" affordance + cross-filter interaction tests (extend AccessAnalysisCharts.test)</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
INT-02 affordance (locked: people list opens via an explicit "View N people →" in each filterable panel header, NOT on slice click):
1. In `AccessAnalysisCharts.tsx`, extend `SectionHeader` (or add a sibling element in the filterable sections: Role distribution, Users by company, Activity by role, Activity by company) with an optional right-aligned button "View {n} people →" where `n` is the distinct people count for that panel's CURRENT filtered summary (use the summary's `usersByRole`/`usersByCompany` distinct-person count for the active filter, or the panel total when no slice active). Clicking it opens the shared slide-in people panel for that panel's filtered view.
2. Reuse the existing slide-in path: the page already lazy-loads `AuthorProfileDrawer` for single-person profiles via `profileEmail`. For the LIST view, use the shared `DrillSheet` (`@/components/ui/DrillSheet`) + `PeopleDrillList` (already imported by the donuts) — add a small state `const [peopleSheet, setPeopleSheet] = useState<{ title: string; people: DrillPerson[] } | null>(null)` and render a `DrillSheet` when set, with a `PeopleDrillList` of that panel's current filtered people; a person click inside still calls `setProfileEmail(email.toLowerCase())` (person-level drawer). Do NOT add any tRPC/useQuery — the people come from the already-computed summaries in memory (Pitfall 2).
3. Add `data-testid="view-people-role"` (and -company etc.) on each affordance button and `data-testid="people-sheet"` on the opened sheet for testing.

Extend `AccessAnalysisCharts.test.tsx` (turn the 05-01-noted INT-02/INT-04 gaps GREEN). The existing echarts mock exposes `data-names`; ADD a way to fire a slice click: extend the mock to also render slice buttons OR (simpler) drive cross-filter through the legend buttons which already call `toggleDrill`+`onSliceClick`. Add tests:
- INT-04: render with roleRows where two projects have different roles; click the role legend/slice for "Member"; assert a `slice-pill-bar` pill containing "Member" appears AND the companies/other donut legend reflects only Member-holding memberships (re-bucket happened). Then click "Member" again → pill disappears (toggle off).
- INT-04 AND-stack: set role then company and assert both pills present.
- INT-02: click `view-people-role` and assert `people-sheet` opens and lists the expected person; assert clicking a slice alone does NOT open `people-sheet`.
Use fireEvent + `.textContent` + `getByTestId`/`queryByTestId`; NO jest-dom matchers.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx"</automated>
  </verify>
  <done>"View N people →" opens the shared sheet with the filtered people (no tRPC); slice click cross-filters + drops a pill but does NOT open the sheet; new INT-02/INT-04 tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test`: PillBar + AccessAnalysisCharts + the four migrated donut tests pass; total passing holds at/above baseline (the 05-01 RED stubs are now GREEN for PillBar; ActivityCoverageBadge remains RED until 05-03).
- No `import { trpc }` / `useQuery` added in AccessAnalysisCharts or PillBar (Pitfall 2 / PERF-03). No WebGL added (PERF-05).
- Project Picker `selected` state path is unchanged; "Clear all" does not clear `selected` (assert in test or by inspection).
- Terrain + CoordinationByProject receive NO sliceFilters (scope boundary held).
</verification>

<success_criteria>
- Clicking a role/company slice cross-filters the other donuts + timeline with zero queries (INT-04).
- Active-filter pill bar with per-pill X + Clear all (INT-05); Project Picker untouched by Clear all.
- "View N people →" opens the shared slide-in people list (INT-02); slice click does NOT.
- Clicked slice glows/pulls out, siblings dim (visual anchor).
- KPI count does not re-animate on filter change (VIS-05 — verified by inspection/manual; StatStrip entrance fires on mount only).
</success_criteria>

<output>
After completion, create `.planning/phases/05-access-analysis-depth/05-02-SUMMARY.md` noting the timeline slice-narrowing approach used and how the people-sheet sources its list (in-memory summaries).
</output>
