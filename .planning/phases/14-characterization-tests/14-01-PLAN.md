---
phase: 14-characterization-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/__tests__/folderPermissionTerrainView.test.ts
  - lib/server/folderPermissionTerrainView.ts
  - app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx
  - app/(dashboard)/access-analysis/folderTerrain.ts
  - app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "npm test runs a new characterization test that golden-masters loadFolderPermissionTerrain's assembled FolderTerrainData on a fixed in-memory fixture, and it passes"
    - "npm test runs a new characterization test that golden-masters loadFolderPermissionOverview's assembled FolderTerrainData on a fixed in-memory fixture, and it passes"
    - "npm test runs a shape/key-invariant pin on loadTerrainProjects proving userRoleCount = max(dc, live), and it passes"
    - "FolderPermissionTerrain.tsx, folderTerrain.ts, and HybridAnalyticsSurface.tsx each carry a // SPLIT-PENDING: comment at the top referencing REF-01"
    - "folderPermissionTerrainView.ts carries a // SPLIT-PENDING: comment referencing REF-02 (the shared base join extraction)"
    - "npx tsc --noEmit passes with the new test file included; no loader/transform behavior changed"
  artifacts:
    - path: "lib/server/__tests__/folderPermissionTerrainView.test.ts"
      provides: "Golden-master boundary tests for the three exported terrain loaders"
      contains: "vi.mock(\"@/server/db\""
      min_lines: 90
    - path: "lib/server/folderPermissionTerrainView.ts"
      provides: "REF-02 shared-query SPLIT-PENDING signpost comment (comment-only)"
      contains: "SPLIT-PENDING"
    - path: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      provides: "REF-01 monolith SPLIT-PENDING comment (comment-only)"
      contains: "SPLIT-PENDING"
    - path: "app/(dashboard)/access-analysis/folderTerrain.ts"
      provides: "REF-01 monolith SPLIT-PENDING comment (comment-only)"
      contains: "SPLIT-PENDING"
    - path: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx"
      provides: "REF-01 monolith SPLIT-PENDING comment (comment-only, no new tests)"
      contains: "SPLIT-PENDING"
  key_links:
    - from: "lib/server/__tests__/folderPermissionTerrainView.test.ts"
      to: "lib/server/folderPermissionTerrainView.ts"
      via: "vi.mock(\"@/server/db\") replaces the module-level db; the test drives loadFolderPermissionTerrain / loadFolderPermissionOverview / loadTerrainProjects through the mocked db.$queryRaw + db.accProject.findUnique"
      pattern: "vi\\.mock\\(\"@/server/db\""
---

<objective>
Pin the CURRENT behavior of the access-analysis terrain data-loader boundary
(`lib/server/folderPermissionTerrainView.ts`) with golden-master characterization
tests, and stamp the REF-01 monolith files plus the REF-02 shared-query module
with `// SPLIT-PENDING:` signposts — so the deferred REF-01 (monolith split) and
REF-02 (`folderPermQuery` extraction) refactors are safe to perform later.

This satisfies ROADMAP Phase 14 success criteria #1 (boundary-output pins),
#2 (SPLIT-PENDING comments on all three monoliths → REF-01), and contributes to
#4 (`npm test` green). It implements requirement TEST-02.

Purpose: lock the assembled `FolderTerrainData` output shape AND values so a
future split that perturbs assembly fails loudly. No user-visible change — this
is a safety-net phase for the `/access-analysis` terrain surface.

Output: one new Vitest file (`folderPermissionTerrainView.test.ts`) and
comment-only edits to four existing files.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-characterization-tests/14-CONTEXT.md
@.planning/codebase/TESTING.md

# Subject under test (read fully — the $queryRaw call order is load-bearing)
@lib/server/folderPermissionTerrainView.ts

# Proven mock seam: lib/server view function with a module-level @/server/db,
# mocked via vi.hoisted() + vi.mock("@/server/db"). Mirror this exactly.
@lib/server/coordinationByProjectView.test.ts

# Precedent for the row-bound assertion style and fake-timer usage (TEST-01).
@lib/server/acc-hot-cache.test.ts

# Types consumed by the golden master: FolderTerrainData, TerrainCell,
# TerrainProjectOption, rankForTier. Do NOT re-pin this geometry — folderTerrain
# already has its own __tests__/folderTerrain.test.ts.
@app/(dashboard)/access-analysis/folderTerrain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SPLIT-PENDING signpost comments (comment-only, no behavior change)</name>
  <files>app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx, app/(dashboard)/access-analysis/folderTerrain.ts, app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx, lib/server/folderPermissionTerrainView.ts</files>
  <action>
    Add a top-of-file `// SPLIT-PENDING:` line-comment block to each of the four
    files. This satisfies success criterion #2 (REF-01 on the three monoliths)
    and the CONTEXT signpost decision (REF-02 where the shared base join lives).
    Comment-only — change NO code, imports, or logic.

    For the two client components (`FolderPermissionTerrain.tsx` and
    `HybridAnalyticsSurface.tsx`) the first source line is the `"use client";`
    directive. Insert the comment block ABOVE that directive — SWC permits leading
    comments before `"use client"`, but no statements may precede it, so use plain
    `//` line comments only and keep `"use client";` as the first STATEMENT.

    Wording per file (keep concise, three lines max each):
    - `FolderPermissionTerrain.tsx`: note this is the REF-01 terrain monolith
      slated to split into data-hook / transform / thin-view modules; its server
      boundary is characterized by `lib/server/__tests__/folderPermissionTerrainView.test.ts`,
      so the split stays safe.
    - `folderTerrain.ts`: note REF-01 (transform module to be extracted in the
      split); its pure geometry is already covered by
      `app/(dashboard)/access-analysis/__tests__/folderTerrain.test.ts`. Place the
      comment at line 1, above the existing JSDoc header block.
    - `HybridAnalyticsSurface.tsx`: note REF-01; this client DuckDB/Mosaic monolith
      is COMMENT-ONLY this phase (real logic is browser-only; no new tests added).
    - `folderPermissionTerrainView.ts`: note REF-02 — the base `AccFolderPermission`
      role/perm/folder join in this module is the shared join also used by
      `lib/server/templateFolderTerrain.ts`, to be extracted into
      `lib/server/folderPermQuery.ts` later; pinned by this file's boundary test
      (TEST-02) and the template shared-query contract (TEST-03). Insert above the
      existing `import "server-only";` line.

    Do NOT reword or remove any existing comment/JSDoc. Do NOT touch any other file.
    `/users/spatial-graph` is out of scope and must not appear in the diff.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && grep -lc "SPLIT-PENDING" "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx" "app/(dashboard)/access-analysis/folderTerrain.ts" "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx" "lib/server/folderPermissionTerrainView.ts" | grep -c ":" ; npx tsc --noEmit</automated>
  </verify>
  <done>All four files contain a top-of-file SPLIT-PENDING comment (three reference REF-01, the loader references REF-02); the two "use client" files still have "use client"; as their first statement; `npx tsc --noEmit` is clean; no non-comment lines changed (verify with `git diff`).</done>
</task>

<task type="auto">
  <name>Task 2: Golden-master the three terrain loaders in folderPermissionTerrainView.test.ts</name>
  <files>lib/server/__tests__/folderPermissionTerrainView.test.ts</files>
  <action>
    Create the new Vitest file pinning the three exported boundaries of
    `lib/server/folderPermissionTerrainView.ts`. Per the CONTEXT HYBRID decision:
    golden-master `.toEqual` on each FolderTerrainData boundary; lighter
    key-invariant pin on the project list. No real DB — synthetic in-memory
    fixtures only (data-truthfulness: tests make no live-data claim).

    MOCK SEAM (mirror `coordinationByProjectView.test.ts`): the loaders read a
    module-level `import { db } from "@/server/db"`, so it is NOT injectable —
    mock it. At the very top of the file, before importing the subject:
    declare `const mocks = vi.hoisted(() => ({ queryRaw: vi.fn(), findUnique: vi.fn() }))`,
    then `vi.mock("server-only", () => ({}))` and
    `vi.mock("@/server/db", () => ({ db: { $queryRaw: mocks.queryRaw, accProject: { findUnique: mocks.findUnique } } }))`.
    Then `import { loadFolderPermissionTerrain, loadFolderPermissionOverview, loadTerrainProjects } from "../folderPermissionTerrainView";`.
    Use `beforeEach(() => vi.clearAllMocks())`. Call every loader with `force = true`
    so the module-level TTL cache never serves a stale hit between tests.
    `$queryRaw` is a tagged template, so each call invokes `mocks.queryRaw` once;
    chain `mockResolvedValueOnce(...)` in EXACT dispatch order (the loaders use
    `Promise.all`, which evaluates the array left-to-right).

    A) loadFolderPermissionTerrain("p1", true) — VERIFIED call order:
       1. db.accProject.findUnique  -> mocks.findUnique.mockResolvedValue({ id: "p1", name: "Project One" })
       2. $queryRaw #1 folders      -> [{ id: "f1", name: "01_Client" }, { id: "f2", name: "Design Documents" }]
       3. $queryRaw #2 perms        -> rows keyed { folder_id, role_id, role_name, perm_type, n_actions }:
            { folder_id:"f1", role_id:"r1", role_name:"Architect", perm_type:"Full Controller", n_actions:7 }
            { folder_id:"f2", role_id:"r1", role_name:"Architect", perm_type:"View Only", n_actions:0 }   (inherited: empty actions)
       4. $queryRaw #3 parentPerms  -> [{ role_id:"r1", perm_type:"Full Controller", n_actions:7 }]  (Project Files own grant)
       5. $queryRaw #4 dcRoleUsers  -> [{ role_id:"r1", name:"Ann", email:"ann@x.com" }]
       6. $queryRaw #5 liveRoleUsers-> [{ role_id:"r1", name:"Ann", email:"ann@x.com" }, { role_id:"r1", name:"Bob", email:"bob@x.com" }]
       Run the loader, destructure `const { generatedAt, ...rest } = result!`, assert
       `typeof generatedAt === "string"`, and pin `rest` with a single `.toEqual(...)`.
       CAPTURE-AND-VERIFY: run it once, capture the actual `rest`, and confirm these
       hand-traced invariants hold BEFORE pinning (do not pin a value that violates them):
         - folders ordered numbered-before-unnumbered: [{id:"f1",name:"01_Client"},{id:"f2",name:"Design Documents"}]
         - roles: [{id:"r1",name:"Architect"}]
         - inherited cell (f2/r1, n_actions:0) resolves to tier "Full Controller" (adopts the parent grant, NOT the stored "View Only" floor); rank 6
         - explicit cell (f1/r1) tier "Full Controller", rank 6
         - usersByRole.r1 deduped by email and name-sorted: [{name:"Ann",email:"ann@x.com"},{name:"Bob",email:"bob@x.com"}] (Ann's live dup collapsed)
         - maxUserCount: 2
         - office is a string (officeCodeFor runs for real; "p1"/"Project One" is non-MTY)
       Also add `expect(mocks.queryRaw).toHaveBeenCalledTimes(5)` to lock the call count.
       Add a null-path case: folders `$queryRaw` resolves `[]` -> loader returns null.

    B) loadFolderPermissionOverview(true) — VERIFIED call order:
       1. $queryRaw #1 folderRank -> [{ folder_name:"Design Documents", projects:10 }, { folder_name:"01_Client", projects:8 }]
       2. $queryRaw #2 roleRank   -> [{ role_id:"r1", role_name:"Architect", projects:12 }, { role_id:"r2", role_name:"Owner", projects:5 }]
       3. $queryRaw #3 agg        -> (only runs when folderRank AND roleRank are non-empty — Prisma.join throws on []; keep both non-empty):
            { folder_name:"Design Documents", role_id:"r1", tier:"Full Controller", projects:7 }
            { folder_name:"Design Documents", role_id:"r1", tier:"View Only", projects:3 }
            { folder_name:"01_Client", role_id:"r2", tier:"View Only", projects:5 }
       Destructure out `generatedAt`, pin the rest with `.toEqual`. Hand-traced invariants:
         - projectId "__overview__", projectName "All projects", office "", usersByRole {}, heightMetric "projects"
         - folders sorted numbered-first: [{id:"01_Client",...},{id:"Design Documents",...}]
         - roles preserve roleRank order: [{id:"r1",name:"Architect"},{id:"r2",name:"Owner"}]
         - cells in agg-accumulation order [DD/r1, 01_Client/r2]:
             DD/r1  -> modal tier "Full Controller" (7 > 3), rank 6, userCount 10, tierBreakdown { 6:7, 1:3 }
             01_Client/r2 -> tier "View Only", rank 1, userCount 5, tierBreakdown { 1:5 }
         - maxUserCount 10
       Add `expect(mocks.queryRaw).toHaveBeenCalledTimes(3)`.

    C) loadTerrainProjects(true) — lighter key-invariant pin (third boundary; not a
       full golden master). Call order: $queryRaw #1 rows, #2 dcUsers, #3 liveUsers.
       rows -> [{ id:"p1", name:"Project One", folder_count:5, perm_count:20 }]
       dcUsers -> [{ project_id:"p1", users:3 }]; liveUsers -> [{ project_id:"p1", users:7 }]
       Assert length 1; folderCount 5; permCount 20; `userRoleCount` === 7 (proves the
       `Math.max(dc, live)` staffing signal, picking the live count over the DC count);
       `typeof office === "string"`.

    Add a top-of-file comment noting these are characterization pins protecting the
    deferred REF-01 split (the test file co-locates with the lib/server subject per
    TESTING.md Pattern 2). Do NOT add jest-dom matchers; do NOT touch any source file
    in this task.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run lib/server/__tests__/folderPermissionTerrainView.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The new test file passes: loadFolderPermissionTerrain and loadFolderPermissionOverview each have a single full-object `.toEqual` golden master (generatedAt excluded) matching the hand-traced invariants; the null-path and call-count assertions pass; loadTerrainProjects' userRoleCount=max(dc,live) invariant passes; `npx tsc --noEmit` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → loader (in-process) | Synthetic in-memory rows fed through a mocked `db`; no network, no real DB, no untrusted external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | folderPermissionTerrainView.test.ts | accept | Tests use synthetic fixtures only; no real DB connection (vitest.setup.ts stubs DATABASE_URL), no secrets or PII in fixtures. |
| T-14-02 | Tampering | loader source files | mitigate | Edits are comment-only (SPLIT-PENDING); negative-scope check + `npx tsc --noEmit` confirm no behavior change. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs — no dependency surface added; package-legitimacy gate N/A. |
</threat_model>

<verification>
Phase-level gates (run before any rebuild):
1. `npx tsc --noEmit` — whole-tree typecheck including the new test file (MUST pass before `next build`).
2. `npm test` — full Vitest suite green with the new characterization test included (success criterion #4).
3. Negative scope check: `git diff --name-only` lists ONLY
   `lib/server/__tests__/folderPermissionTerrainView.test.ts` (new) plus comment-only
   edits to `folderPermissionTerrainView.ts`, `FolderPermissionTerrain.tsx`,
   `folderTerrain.ts`, `HybridAnalyticsSurface.tsx`. NO `/users/spatial-graph` files;
   `git diff` shows only added comment lines in the four source files (no logic change).
</verification>

<success_criteria>
- Golden-master `.toEqual` pins exist for `loadFolderPermissionTerrain` and
  `loadFolderPermissionOverview` on fixed in-memory fixtures (ROADMAP #1, TEST-02).
- All three monolith files carry a `// SPLIT-PENDING:` comment referencing REF-01;
  the shared-query loader references REF-02 (ROADMAP #2).
- `npx tsc --noEmit` clean and `npm test` green (ROADMAP #4).
- Diff is test-file + comment-only; no loader/transform behavior changed.
</success_criteria>

<output>
Create `.planning/phases/14-characterization-tests/14-01-SUMMARY.md` when done.
</output>
