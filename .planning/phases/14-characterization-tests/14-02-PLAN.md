---
phase: 14-characterization-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts
  - lib/server/templateFolderTerrain.ts
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "npm test runs a new characterization test that pins the shared AccFolderPermission terrain-query contract consumed by loadTemplateFolderTerrain, and it passes"
    - "The test asserts the stable 5-column contract (folder_id, role_id, role_name, perm_type, n_actions) flows faithfully into the assembled FolderTerrainData"
    - "The test asserts the row-bound: assembled cells <= folders-with-perms x roles (one cell per folder/role), never a raw-permission fan-out"
    - "The test references REF-02 as the deferred lib/server/folderPermQuery.ts extraction target"
    - "templateFolderTerrain.ts carries a // SPLIT-PENDING: comment referencing REF-02 (the shared base join)"
    - "npx tsc --noEmit passes with the new test file; loadTemplateFolderTerrain behavior unchanged"
  artifacts:
    - path: "lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts"
      provides: "Shared AccFolderPermission terrain-query contract characterization (TEST-03)"
      contains: "REF-02"
      min_lines: 60
    - path: "lib/server/templateFolderTerrain.ts"
      provides: "REF-02 shared-query SPLIT-PENDING signpost comment (comment-only)"
      contains: "SPLIT-PENDING"
  key_links:
    - from: "lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts"
      to: "lib/server/templateFolderTerrain.ts"
      via: "vi.mock(\"@/server/db\") replaces the module-level db; the test drives loadTemplateFolderTerrain through mocked db.accProject.findUnique + db.accFolder.findMany + db.$queryRaw (the shared base join)"
      pattern: "loadTemplateFolderTerrain"
---

<objective>
Pin the CURRENT contract of the shared `AccFolderPermission` terrain query — the
role/perm/folder join that `lib/server/templateFolderTerrain.ts` (`/template-mty`)
and `lib/server/folderPermissionTerrainView.ts` (`/access-analysis`) both run — by
characterizing its live template-side consumer `loadTemplateFolderTerrain`. The
pin is a refactor-tolerant CONTRACT (column set + value types + row-bound), not a
golden master, so the deferred REF-02 extraction of `lib/server/folderPermQuery.ts`
can proceed without fighting the test.

This satisfies ROADMAP Phase 14 success criterion #3 (shared-query pin: stable
column count + row-bound + references REF-02) and contributes to #4 (`npm test`
green). It implements requirement TEST-03.

Purpose: lock the shared join's output contract before it is extracted. No
user-visible change — synthetic in-memory fixtures, no live DB, no data claim.

Output: one new Vitest file and a comment-only edit to `templateFolderTerrain.ts`.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-characterization-tests/14-CONTEXT.md
@.planning/codebase/TESTING.md

# Subject: loadTemplateFolderTerrain (untested) + the shared 5-column $queryRaw join.
# buildFolderTerrain (pure) is already covered by __tests__/templateFolderTerrain.test.ts
# — do NOT re-pin it here. The shared join's twin lives in folderPermissionTerrainView.ts.
@lib/server/templateFolderTerrain.ts

# Proven mock seam: lib/server view function, module-level @/server/db, mocked via
# vi.hoisted() + vi.mock("@/server/db"). Mirror this exactly.
@lib/server/coordinationByProjectView.test.ts

# Existing template-terrain test (fixture-tree style + inheritance expectations).
@lib/server/__tests__/templateFolderTerrain.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the REF-02 SPLIT-PENDING signpost on templateFolderTerrain.ts (comment-only)</name>
  <files>lib/server/templateFolderTerrain.ts</files>
  <action>
    Add a top-of-file `// SPLIT-PENDING:` line-comment block above the existing
    `import "server-only";` line. Note REF-02: the base `AccFolderPermission`
    role/perm/folder join in `loadTemplateFolderTerrain` is the shared join also
    run by `lib/server/folderPermissionTerrainView.ts`, to be extracted into
    `lib/server/folderPermQuery.ts` later; its column contract is pinned by
    `lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts` (TEST-03).
    Comment-only — change NO code, imports, or logic. Do NOT touch any other file;
    `/users/spatial-graph` is out of scope.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && grep -c "SPLIT-PENDING" lib/server/templateFolderTerrain.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`templateFolderTerrain.ts` has a top-of-file SPLIT-PENDING comment referencing REF-02; `npx tsc --noEmit` clean; `git diff` shows only added comment lines (no logic change).</done>
</task>

<task type="auto">
  <name>Task 2: Characterize the shared AccFolderPermission query contract in templateFolderTerrain.sharedQuery.test.ts</name>
  <files>lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts</files>
  <action>
    Create the new Vitest file pinning the shared-query CONTRACT consumed by
    `loadTemplateFolderTerrain`. Per the CONTEXT decision this is a MOCK-DB CONTRACT
    (column set + types + row-bound), NOT a golden master — refactor-tolerant so the
    REF-02 extraction does not break it. No real DB.

    MOCK SEAM (mirror `coordinationByProjectView.test.ts`): `loadTemplateFolderTerrain`
    reads a module-level `import { db } from "@/server/db"` (not injectable). At the
    top, before importing the subject: `const mocks = vi.hoisted(() => ({ queryRaw: vi.fn(), findUnique: vi.fn(), findFolders: vi.fn() }))`,
    then `vi.mock("server-only", () => ({}))` and
    `vi.mock("@/server/db", () => ({ db: { $queryRaw: mocks.queryRaw, accProject: { findUnique: mocks.findUnique }, accFolder: { findMany: mocks.findFolders } } }))`.
    Then `import { loadTemplateFolderTerrain } from "../templateFolderTerrain";` and
    `import { TEMPLATE_MTY_ID } from "@/lib/acc/template-mty";`. Use
    `beforeEach(() => vi.clearAllMocks())`. `TEMPLATE_MTY_ROSTER` is real (not mocked)
    — keep assertions roster-independent (structural), do not pin exact roster counts.

    VERIFIED call order (Promise.all, left-to-right):
      1. db.accProject.findUnique -> mocks.findUnique.mockResolvedValue({ name: "Template MTY" })
      2. db.accFolder.findMany    -> mocks.findFolders.mockResolvedValue(folderTree)
      3. $queryRaw #1 perms       -> the shared join rows
    Build a minimal folder tree that buildFolderTerrain accepts (needs a "Project
    Files" root so it does not return null): e.g.
      { id:"pf", parentId:null, name:"Project Files", fullPath:"/PF" }
      { id:"f1", parentId:"pf", name:"01_Client", fullPath:"/PF/01_Client" }
      { id:"f2", parentId:"pf", name:"Design Documents", fullPath:"/PF/Design Documents" }
    Shared-join perms rows use EXACTLY the 5-column contract keys
    { folder_id, role_id, role_name, perm_type, n_actions } — e.g. an explicit grant
    on f1 (perm_type:"Full Controller", n_actions:7) and an inherited grant on f2
    (perm_type:"View Only", n_actions:0).

    Assert the contract (each assertion documents one facet of the shared query):
    1. STABLE COLUMN COUNT: assert the fixture perm rows expose exactly the 5
       contract columns — `expect(Object.keys(permRow).sort()).toEqual(["folder_id","n_actions","perm_type","role_id","role_name"])`.
       Add a comment that these are the columns the REF-02 extraction must preserve.
    2. FAITHFUL COLUMN MAPPING: run `loadTemplateFolderTerrain()`, assert the result
       is non-null, and that each of the 5 columns flows into the assembled output:
         - a cell's `roleName` equals the row `role_name`
         - the explicit cell's `tier` equals its `perm_type` ("Full Controller")
         - the inherited cell (n_actions:0) resolves to its parent's effective tier,
           NOT the stored "View Only" floor (proves perm_type + n_actions are both read)
         - `folderId` values come from the row `folder_id`/folder ids
       (Run once, capture, and confirm these hold before pinning the exact expected values.)
    3. ROW-BOUND (refactor-tolerant, proven by aggregation logic per TEST-01 style):
       assert `result.cells.length <= foldersWithPerms * roles` — one cell per
       (folder, role), never a raw-permission fan-out. With 2 perm-bearing folders x
       1 role the bound is 2; assert `cells.length` is at most that and there are no
       duplicate (folderId, roleId) cells (`new Set(cells.map(c => c.folderId+"|"+c.roleId)).size === cells.length`).
    4. PROJECT SCOPE: assert the join is bound to a single project — the perms
       `$queryRaw` interpolates `TEMPLATE_MTY_ID`. The tagged-template mock receives
       `(stringsArray, ...values)`, so assert
       `expect(mocks.queryRaw.mock.calls[0]).toContain(TEMPLATE_MTY_ID)` and
       `expect(mocks.queryRaw).toHaveBeenCalledTimes(1)`.
    5. NULL PATH: when `accProject.findUnique` resolves null, the loader returns null
       before building — assert that branch.

    Add a top-of-file comment: this file pins the shared `AccFolderPermission`
    terrain-query contract used by both `/template-mty` (here) and `/access-analysis`
    (pinned via `lib/server/__tests__/folderPermissionTerrainView.test.ts`), protecting
    the deferred REF-02 `lib/server/folderPermQuery.ts` extraction. Do NOT add jest-dom
    matchers; do NOT touch any source file in this task.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The new test passes: the 5-column contract assertion, faithful-mapping (incl. inherited-tier resolution), row-bound (cells <= folders x roles, no dup folder/role cells), project-scope (TEMPLATE_MTY_ID interpolated, called once), and null-path cases all pass; the file references REF-02; `npx tsc --noEmit` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → loader (in-process) | Synthetic in-memory rows fed through a mocked `db`; no network, no real DB, no untrusted external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information disclosure | templateFolderTerrain.sharedQuery.test.ts | accept | Synthetic fixtures only; no real DB (vitest.setup.ts stubs DATABASE_URL); no secrets/PII. |
| T-14-04 | Tampering | templateFolderTerrain.ts | mitigate | Edit is comment-only (SPLIT-PENDING REF-02); negative-scope check + `npx tsc --noEmit` confirm no behavior change. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs — no dependency surface added; package-legitimacy gate N/A. |
</threat_model>

<verification>
Phase-level gates (run before any rebuild):
1. `npx tsc --noEmit` — whole-tree typecheck including the new test file (MUST pass before `next build`).
2. `npm test` — full Vitest suite green with the new contract test included (success criterion #4).
3. Negative scope check: `git diff --name-only` lists ONLY
   `lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts` (new) plus a
   comment-only edit to `lib/server/templateFolderTerrain.ts`. NO `/users/spatial-graph`
   files; `git diff` shows only added comment lines in the source file (no logic change).
</verification>

<success_criteria>
- The shared `AccFolderPermission` terrain-query contract is pinned via
  `loadTemplateFolderTerrain`: stable 5-column set, faithful mapping, row-bound, and
  project scope (ROADMAP #3, TEST-03).
- The test file references REF-02 as the deferred extraction target (ROADMAP #3).
- `templateFolderTerrain.ts` carries a `// SPLIT-PENDING:` REF-02 comment.
- `npx tsc --noEmit` clean and `npm test` green (ROADMAP #4); diff is test-file +
  comment-only.
</success_criteria>

<output>
Create `.planning/phases/14-characterization-tests/14-02-SUMMARY.md` when done.
</output>
