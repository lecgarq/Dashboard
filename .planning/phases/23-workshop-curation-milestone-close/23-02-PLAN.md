---
phase: 23-workshop-curation-milestone-close
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - .planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md
  - app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx
  - app/(dashboard)/access-analysis/components/RolesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/UsersTabPanel.tsx
  - app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
  - app/(dashboard)/access-analysis/components/CompareTabPanel.tsx
autonomous: true
requirements: [ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04, ISSUE-05, PERM-01, ENG-01, PIPE-01]

# The 6 TabPanel files are listed ONLY because a within-tab reorder is the one edit this
# plan is permitted to make. A ZERO-DIFF result across all six is a LEGITIMATE PASS —
# see <objective>. The checklist file is the guaranteed deliverable.

must_haves:
  truths:
    - "The live `/access-analysis` panel inventory is recounted from source at 23 panels: Overview 5, Roles 6, Users 2, Companies 3, Projects 6, Compare 1"
    - "Each of the 6 tabs is explicitly assessed for whether it leads with its strongest panel; the verdict (keep / reorder) is written down per tab"
    - "A graph-by-graph owner checklist exists covering all 4 workshop pages at graded depth, workable one panel at a time without devtools"
    - "Every coverage-caveat line in the checklist is quoted or paraphrased from a real honesty label in source, with a file:line citation"
  artifacts:
    - path: ".planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md"
      provides: "The owner-facing graph-by-graph sign-off checklist — the phase's key deliverable"
      contains: "Folder Activity by Role"
  key_links:
    - from: "23-REVIEW-CHECKLIST.md"
      to: "app/(dashboard)/access-analysis/components/*TabPanel.tsx"
      via: "every panel row's name/question column is copied from the live SectionHeader title/subtitle props at the cited mount site"
      pattern: "TabPanel.tsx:"
---

<objective>
Answer ROADMAP criterion #1 and produce the artifact the owner will actually work from.

**Criterion #1 is ANSWERED BY THE TAB IA — it is not a build item.** ROADMAP criterion #1
(written 2026-07-02) asks whether panels should be "grouped or made expandable/collapsible"
rather than shipped flat, because a flat wall of charts would dilute the workshop story.
Phase 20.1 (inserted, 2026-07-04) already built the 6-tab IA that solves exactly that.
CONTEXT.md locks this: "the planner must not manufacture a grouping/collapse scheme to
satisfy stale wording." This plan therefore CONFIRMS the tab IA as the answer and records
it — it does not design a new one.

**What is genuinely left:** does each tab lead with its strongest panel? Reorder *within*
a tab only where it does not.

**A ZERO-DIFF CURATION RESULT IS A LEGITIMATE, SUCCESSFUL OUTCOME.** Do not invent a
reorder to justify the phase. `23-RESEARCH.md` §A already assessed every top-of-tab panel
against its tab's purpose and "finds no evidence compelling a within-tab reorder." Confirm
that independently; if you agree, the correct output is "no reorder warranted" plus the
checklist. Nothing more.

HARD PROHIBITIONS (locked in CONTEXT.md, non-negotiable):
- Do NOT build a grouping / collapse / accordion scheme.
- Do NOT change tab order. Overview → Roles → Users → Companies → Projects → Compare is a
  deliberate widening-to-narrowing funnel and is CORRECT.
- Do NOT rebalance panel counts across tabs. Projects 6 / Users 2 / Compare 1 is NOT a
  problem; padding thin tabs is manufactured work.
- Do NOT cut, demote, or delete any panel. All 23 earn their place.
- Do NOT add a panel, loader, chart, or data source. Zero new data in this phase.

Purpose: confirm the surface is coherent, and hand the owner a checklist he can walk in one
sitting.
Output: `23-REVIEW-CHECKLIST.md`, plus (only if warranted) a small within-tab reorder diff.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-workshop-curation-milestone-close/23-CONTEXT.md
@.planning/phases/23-workshop-curation-milestone-close/23-RESEARCH.md
@app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx
@app/(dashboard)/access-analysis/components/RolesTabPanel.tsx
@app/(dashboard)/access-analysis/components/UsersTabPanel.tsx
@app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx
@app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
@app/(dashboard)/access-analysis/components/CompareTabPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recount the 23 panels from source and rule on within-tab lead order</name>
  <files>app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx, app/(dashboard)/access-analysis/components/RolesTabPanel.tsx, app/(dashboard)/access-analysis/components/UsersTabPanel.tsx, app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx, app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx, app/(dashboard)/access-analysis/components/CompareTabPanel.tsx</files>
  <action>
    Read all six `*TabPanel.tsx` files and independently recount the panels.

    **The count is 23, not 22.** CONTEXT.md's own table says 22 and is WRONG — it
    under-counts the Roles tab. `23-RESEARCH.md` §A proves it: "Folder Activity by Role"
    (`FolderActivityReveal`, mounted `RolesTabPanel.tsx:183-193`) is a SEPARATE
    `PremiumSurface` mount from the folder action heatmap (`FolderActionHeatmap`,
    `RolesTabPanel.tsx:197-200`). Two `PremiumSurface` mounts, two `<Reveal>` wraps → two
    panels. Roles = 6, not 5.

    Confirm the corrected inventory against source (do not copy it on faith):

    | Tab | N | Panels (verify each mount site) |
    |---|---|---|
    | Overview | 5 | Activity over time · Activity by module · Activity share by project · Provisioned modules · Ingest freshness |
    | Roles | 6 | Role distribution · Activity by role · Permission volume by level · Activity recency by role · **Folder Activity by Role** · Folder action heatmap |
    | Users | 2 | Users by permission level · Activity recency detail |
    | Companies | 3 | Users by company · Activity by company · Folder activity by company |
    | Projects | 6 | Issue data coverage · Issues over time · Issues by status · Issues by type · Workflow tools (one shell, 4 donuts) · Model Coordination |
    | Compare | 1 | Folder permission terrain |
    | **Total** | **23** | |

    Then rule, tab by tab, on the ONE open curation question: does this tab lead with its
    strongest panel? Write a one-line verdict per tab into the SUMMARY, with the reason.
    Research's read (confirm or overturn it, do not just echo it):
    - Overview leads "Activity over time" — the account-wide activity shape. Correct.
    - Roles leads "Role distribution" — the membership baseline everything else cuts. Correct.
    - Users leads "Users by permission level" — the sharpest read on who can change things.
    - Companies leads "Users by company" — the company distribution baseline. Correct.
    - Projects leads "Issue data coverage" — matches the locked coverage-precedes-metric
      convention, documented in-code at `ProjectsTabPanel.tsx:26-31`. Correct.
    - Compare has one panel — nothing to order.

    If you find a tab that does NOT lead with its strongest panel, reorder the JSX blocks
    within that file. Rules for any such edit:
    - Move whole `<Reveal>`/`<PremiumSurface>` blocks only. No prop changes, no new imports,
      no loader/gate changes, no data changes.
    - The Roles tab's two folder panels (5, 6) are documented "collapsed by default" lazy
      mounts (`RolesTabPanel.tsx:181-182,195-196`) — that is existing lazy-mount behavior,
      NOT a curation target. Leave it.
    - Preserve every gate condition exactly (`loadFolderRanking && loadFolderDetail`, etc.).

    If no reorder is warranted — which is the expected outcome — change nothing and say so
    explicitly. That is a PASS, not a failure to act.
  </action>
  <verify>
    <automated>grep -c "PremiumSurface" "app/(dashboard)/access-analysis/components/RolesTabPanel.tsx"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    All 6 tab files read; the 23-panel inventory (Roles = 6) is confirmed against source with
    mount-site line numbers; a keep-or-reorder verdict is recorded for each of the 6 tabs;
    any reorder is a pure JSX block move with `npx tsc --noEmit` exit 0. A zero-diff result
    is an acceptable and expected outcome.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the graph-by-graph owner review checklist</name>
  <files>.planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md</files>
  <action>
    Write the checklist the owner will walk on the rebuilt `:3000` in plan 23-03. This is
    the phase's key deliverable. The owner asked for this at the 21-04 checkpoint
    ("graph-by-graph"), so structure it so he can work ONE panel at a time — not as a wall
    of questions.

    **Depth is GRADED (owner decision this session). Do not give all four pages a 23-panel
    treatment:**
    - `/access-analysis` — DEEP. Graph-by-graph, all 23 panels across all 6 tabs, one row
      each. This is the core of the review.
    - `/users` — DEEP-ish, verification only (NO curation edits). Its 2026-07-13 off-roadmap
      changes have never been reviewed: directory summary tiles, external collaborators,
      affiliation filter, company column. One row per changed surface, not per widget.
    - `/template-mty` — SHORT functional pass. It received a real, never-owner-seen feature:
      the richer role-similarity graph (commit `b76356f2`, cluster blobs / curved edges /
      similarity %) plus a label fix (`2e15233f`). ~3 rows: does the graph render, do labels
      stay glued to nodes, is it explainable live.
    - `/forma-proposal` — SHORT visual pass. It received only the LECG brand-palette theme
      commit (`4638020b`, 2 files). ~2 rows: does the palette read correctly, no theme drift.

    NOTE the correction this checklist must carry: CONTEXT.md assumed `/template-mty` and
    `/forma-proposal` were untouched and marked it `VERIFY:`. `23-RESEARCH.md` §E disproves
    it with `git diff v2.2..HEAD`. State it precisely: **no v2.3-*requirement* panel landed
    on either page, but both received off-roadmap commits during the v2.3 window.** Do NOT
    write "untouched" — that claim is disproven and a spot-check would catch it.

    Columns, one row per panel:

    | Tab | Panel | The question it answers | Data source / authority | Known caveat | Verdict |

    - **Panel** — the exact on-screen title. Copy it from the live `SectionHeader title=`
      prop, not from memory.
    - **The question it answers** — copy/compress the live `SectionHeader subtitle=` prop.
      These are already written and descriptive (e.g. `ProjectsTabPanel.tsx:99-100`,
      "When are issues actually being raised?"). Do not invent new copy.
    - **Data source / authority** — the loader that feeds it, plus whether it carries an
      `ActivityCoverageBadge`. Activity-derived panels have one; membership panels
      correctly do NOT (existing convention, documented in-code at `RolesTabPanel.tsx:90`:
      "membership (not activity-derived → no coverage badge)"). Do not flag the absence as
      a defect.
    - **Known caveat** — this column MUST be grounded in the REAL honesty labels that exist
      in source. Cite `file:line`. Do not write plausible-sounding text. The five verified
      labels (`23-RESEARCH.md` §G, all grepped in source):
        · Workflow-tools DC-only caption — `ProjectsTabPanel.tsx:165` ("RFI and Submittal
          events come only from the batch Data Connector feed (the live feed does not report
          them), so recent weeks may lag.")
        · Issue-coverage captions (fetched/total/unavailable) — computed live by
          `deriveIssueCoverageCaption`, `app/(dashboard)/access-analysis/issueFunnelCounts.ts:132`,
          consumed via the `coverageProjects` prop. Never a hardcoded figure.
        · `IssueTypeChart` honest buckets — `UNKNOWN_LABEL = "Unknown type"`
          (`issueTypeCounts.ts:30`) and `NONE_LABEL = "No type set"` (`issueTypeCounts.ts:32`),
          two distinct ranked buckets; plus the never-backfilled guard at
          `IssueTypeChart.tsx:97-110`.
        · Module-attribution ⓘ caveat — `OverviewTabPanel.tsx:92-138` (Radix Tooltip,
          `data-testid="module-caveat"` at line 119), live service/verb split.
        · Activity-recency "Never active" band — `UsersTabPanel.tsx:56` plus the two caption
          elements at `UsersTabPanel.tsx:191-197`.
      Panels with no coverage caveat get "—". Do not manufacture one.
    - **Verdict** — left blank for the owner: `ok` / `fix-now` / `v2.4-seed`.

    **Flag `Issues by type` (`IssueTypeChart`, Projects tab) explicitly at the top of the
    Projects section.** It is the ONLY v2.3 panel with ZERO recorded owner UAT — Phase 22
    closed on live-`:3000` evidence and its `:3100` preflight checkpoint was never run
    (`22-03-SUMMARY.md`, STATE.md Blockers/Concerns). It gets real attention, not a skim.

    Head the file with the triage rule verbatim, so it is in front of the owner at the
    moment each finding is raised:
    - **FIX IN PHASE 23:** copy/label fixes, wrong wording, ordering nits, obvious visual
      defects — anything touching NO loader and adding NO data.
    - **DEFER TO v2.4:** anything needing a new loader, chart, data source, or
      schema/taxonomy change. Record as a milestone seed; do NOT build it.
    - If the owner disputes a NUMBER (not a label), that is a data-truth finding → v2.4,
      unless the cause is a display bug (then it is fix-now).
    - Goal: keep the milestone CLOSEABLE. **There is no Phase 23.1.**

    Also state the presenter test the owner is really grading against (CONTEXT.md
    "Specific Ideas"): can the presenter explain, live and without opening devtools, what
    each panel shows, what data backs it, and where coverage is incomplete?
  </action>
  <verify>
    <automated>test -f ".planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md" && grep -c "Folder Activity by Role" ".planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md"</automated>
    <automated>grep -c "v2.4-seed\|v2.4 seed" ".planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md"</automated>
  </verify>
  <done>
    `23-REVIEW-CHECKLIST.md` exists with 23 `/access-analysis` panel rows across 6 tabs
    (Roles has 6, including "Folder Activity by Role"), plus graded sections for `/users`,
    `/template-mty`, and `/forma-proposal`. The triage rule heads the file. Every caveat
    entry cites a real `file:line`. `Issues by type` is flagged as the zero-UAT panel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit — explicit paths only</name>
  <files>.planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md (+ any reordered TabPanel file)</files>
  <action>
    The branch carries ~100 files of unrelated dirty WIP (deleted `.agent/`, `.agents/`,
    `.gemini/` trees, `.planning/` migration deletions, uncommitted app changes).
    **NEVER `git add -A` or `git add .`.**

    Stage by explicit path, then PROVE the staged set before committing:

    ```
    git add .planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md
    # ...plus each reordered TabPanel file, if and only if Task 1 warranted a reorder
    git diff --cached --name-only     # MUST list ONLY the paths above. If it lists
                                      # anything else, unstage and retry.
    git commit -m "docs(23): panel inventory recount (23 panels) + owner review checklist"
    ```

    If Task 1 produced a reorder, use a separate commit with a `refactor(access-analysis):`
    scope so the ordering diff is independently revertable.
  </action>
  <verify>
    <automated>git diff --cached --name-only</automated>
  </verify>
  <done>Commit(s) landed; `git diff --cached --name-only` proof recorded in the SUMMARY; no unrelated WIP file swept in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes JSX block ordering at most, and writes one planning-doc file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | git index (branch carries ~100 files of unrelated dirty WIP) | mitigate | Explicit-path staging only; `git diff --cached --name-only` proof before every commit; never `git add -A`. A prior wave-1 executor race already swept foreign files into a commit (STATE.md, 20.1-02). |
| T-23-04 | Information Disclosure | `23-REVIEW-CHECKLIST.md` coverage-caveat copy | accept | Content is quoted from existing user-visible UI captions; no credentials, no connection strings, no raw data. |

No package installs, no new external input path, no auth/authz/crypto change.
</threat_model>

<verification>
- `npx tsc --noEmit` → exit 0 (required even for a pure JSX reorder; mandatory if zero-diff too, as a no-op regression guard)
- `npm test -- "app/(dashboard)/access-analysis"` → green (the tab-panel test suite pins mount structure; a reorder must not break it)
- `23-REVIEW-CHECKLIST.md` exists and contains 23 `/access-analysis` panel rows
- `git diff --cached --name-only` → only the planned paths
- No new import added to any TabPanel file: `git diff -- "app/(dashboard)/access-analysis/components/" | grep -cE '^\+\s*import'` → `0`
</verification>

<success_criteria>
The panel inventory is authoritatively 23. Each tab has a recorded lead-panel verdict. The
owner has a checklist he can walk one panel at a time across all four workshop pages. The
code diff is zero or a minimal within-tab reorder — and zero is a pass.
</success_criteria>

<output>
Create `.planning/phases/23-workshop-curation-milestone-close/23-02-SUMMARY.md` when done.
State the curation verdict plainly: either "no reorder warranted — zero-diff curation PASS"
or the exact blocks moved and why.
</output>
