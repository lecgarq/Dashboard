---
phase: 23-workshop-curation-milestone-close
plan: 04
type: execute
wave: 4
depends_on: [23-03]
files_modified:
  - .tools/repo-map/baselines/dependency-cruiser-baseline.json
  - .tools/repo-map/
  - .planning/ROADMAP.md
  - .planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md
autonomous: true
requirements: [ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04, ISSUE-05, PERM-01, ENG-01, PIPE-01]

must_haves:
  truths:
    - "`npx tsc --noEmit` exits 0 and `npm test` reports 0 failures"
    - "TEST-01/02/03 characterization tests are byte-identical against the `v2.2` tag"
    - "No new WebGL/R3F import landed on `/access-analysis` across the whole v2.3 milestone (`v2.2..HEAD`)"
    - "`/users/spatial-graph` received zero changes across the whole v2.3 milestone"
    - "`node scripts/repo-map/check.cjs` exits 0 (stale baseline refreshed first)"
    - "ROADMAP checkboxes for Phase 22 AND Phase 23 are `[x]`"
  artifacts:
    - path: ".planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md"
      provides: "The accepted phase verification record — REQUIRED by scripts/gsd-self-gate.cjs (plan 23-05) which hard-fails without it"
      contains: "status:"
    - path: ".tools/repo-map/baselines/dependency-cruiser-baseline.json"
      provides: "Refreshed dependency-cruiser ratchet baseline — currently stale, makes check.cjs exit 1 on 3 false ERRORs"
  key_links:
    - from: "23-VERIFICATION.md frontmatter `status`"
      to: "scripts/gsd-self-gate.cjs `validatePhaseArtifact()`"
      via: "the script requires status ∈ {passed, owner_approved, owner-approved, approved, complete} AND a `[x]` Phase-23 ROADMAP checkbox, or plan 23-05's final gate cannot pass"
      pattern: "status: owner_approved"
---

<objective>
Prove ROADMAP criteria #3 and #4 with real commands, refresh the one gate that is currently
broken, and write the verification record that plan 23-05's final self-gate structurally
requires.

Two gate hazards, both live-verified in `23-RESEARCH.md`, both handled here:

1. **`node scripts/repo-map/check.cjs` currently EXITS 1** — and it is NOT a Phase 23
   regression. The `dependency-cruiser` ratchet baseline still lists `warningEdges` for three
   `scripts/diag-activity-*.cjs` files that were legitimately deleted 2026-07-10 in an
   off-roadmap cleanup commit (`b95bf5c7`). Until the baseline is refreshed, every run emits
   3 false ERRORs and criterion #4 cannot be evidenced. Refresh it FIRST.

2. **`scripts/gsd-self-gate.cjs` hard-fails without a `[x]` Phase-23 checkbox and an accepted
   `23-VERIFICATION.md`.** Both are written here so plan 23-05 can actually run it.

Also fix the Phase 22 ROADMAP checkbox — Phase 22 is fully shipped (3/3 plans, SUMMARYs
written, STATE reconciled 2026-07-14) but its box is still `[ ]`, which makes every
checkbox-counting tool under-report.

Purpose: turn "the gates are probably green" into recorded, re-runnable evidence.
Output: refreshed repo-map baseline, corrected ROADMAP checkboxes, `23-VERIFICATION.md`.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-workshop-curation-milestone-close/23-RESEARCH.md
@.planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md
@.planning/phases/23-workshop-curation-milestone-close/23-03-SUMMARY.md
@.claude/skills/lecg-dashboard/references/dashboard-verification-sequence.md
@scripts/repo-map/check.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the stale repo-map ratchet baseline so check.cjs can serve as evidence</name>
  <files>.tools/repo-map/baselines/dependency-cruiser-baseline.json, .tools/repo-map/</files>
  <action>
    `node scripts/repo-map/check.cjs` exits 1 today with three ERRORs of the form
    "dependency-cruiser baseline references missing file: scripts/diag-activity-*.cjs".

    Root cause (verified): `.tools/repo-map/baselines/dependency-cruiser-baseline.json` holds
    6 `warningEdges` under rule `no-scripts-to-app` and `warningCounts: [{ id:
    "no-scripts-to-app", count: 6 }]`. Three of those edges name `diag-activity-coordination.cjs`,
    `diag-activity-module-audit.cjs`, and `diag-activity-types.cjs` — all deleted 2026-07-10 in
    commit `b95bf5c7`. `check.cjs`'s `validateBaselineFiles()` fails on any baseline path that
    no longer exists on disk. The generator (`scripts/repo-map/generate.cjs`) regenerates the
    REPORTS but never the BASELINES — baselines are hand-maintained ratchets.

    Steps:
    1. `npm run repo-map` — regenerate `.tools/repo-map/*` reports against the current tree
       (this must run AFTER plans 23-02/23-03, so any curation/fix-now edit is included).
    2. Read the fresh `.tools/repo-map/dependency-cruiser.json` and take its LIVE warning set.
    3. Rewrite `.tools/repo-map/baselines/dependency-cruiser-baseline.json` so `warningEdges`
       and `warningCounts` match the live warnings exactly.
       **RATCHET DOWN ONLY.** Never raise a rule's count above its live value — that would
       silently widen the budget and let a future regression through. If a live warning count
       is HIGHER than the current baseline for any rule, STOP: that is a real new boundary
       violation introduced during v2.3, not baseline staleness. Report it as a finding
       instead of baselining it away.
    4. `node scripts/repo-map/check.cjs` → MUST exit 0.

    Do not touch the ast-grep baseline unless `check.cjs` reports a stale path for it too
    (research found none).
  </action>
  <verify>
    <automated>node scripts/repo-map/check.cjs; echo "exit=$?"</automated>
  </verify>
  <done>`node scripts/repo-map/check.cjs` exits 0. The baseline's `warningCounts` are ratcheted DOWN to (never up from) the live values. No stale-file ERRORs remain.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate sweep + scope-fence proofs for the whole v2.3 milestone</name>
  <files>(read-only — evidence capture)</files>
  <action>
    Run every command below and capture its VERBATIM output for `23-VERIFICATION.md`.
    Re-run; do not assume. `23-RESEARCH.md` explicitly marks its own test-baseline evidence as
    time-sensitive.

    **Criterion #3 — gates green:**
    ```
    npx tsc --noEmit                 # expect exit 0, no output
    npm test                         # expect 0 failed. Research baseline (2026-07-14):
                                     # 2535 passed / 1 skipped / 0 failed, ~46s
    ```
    KNOWN FLAKE, do not let it mask a real regression: `app/(dashboard)/users/access-analysis/physicsLayer.test.ts`
    is a documented order-dependent isolation flake (logged in
    `.planning/phases/22-issue-type-resolution/deferred-items.md`). It did NOT reproduce in
    the research run. **If exactly 1 test fails and it is that file, re-run it in isolation
    (`npx vitest run "app/(dashboard)/users/access-analysis/physicsLayer.test.ts"`) — if it
    passes alone (45/45), that matches the known non-blocking pattern; record it and proceed.
    If ANY OTHER file fails, that is a NEW finding — triage it before close, do not wave it
    through.**

    **Criterion #3 — TEST-01/02/03 byte-identical vs the `v2.2` tag** (each MUST print nothing):
    ```
    git diff v2.2 -- lib/server/acc-hot-cache.test.ts                                # TEST-01 (OOM guard)
    git diff v2.2 -- lib/server/__tests__/folderPermissionTerrainView.test.ts         # TEST-02
    git diff v2.2 -- lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts   # TEST-03
    ```

    **Criterion #4 — scope fence across the WHOLE v2.3 milestone (Phases 20 → 23), not just
    this phase.** Base = the `v2.2` tag (`5974d6f2`), the last commit before v2.3 opened:
    ```
    # No new WebGL/R3F import on /access-analysis (import lines only — a bare `grep -i three`
    # false-positives on the English word "three" in comments and test names):
    git diff v2.2..HEAD -- "app/(dashboard)/access-analysis/" \
      | grep -cE '^\+\s*(import|require).*(three|@react-three|cosmos\.gl|webgl)'
    # expect: 0

    # /users/spatial-graph untouched:
    git diff --stat v2.2..HEAD -- "app/(dashboard)/users/spatial-graph"
    # expect: empty output
    ```

    **Also record, precisely, the `/template-mty` + `/forma-proposal` fact that corrects
    CONTEXT.md** (`23-RESEARCH.md` §E, from `git diff v2.2..HEAD`): no v2.3-*requirement*
    panel landed on either page, but neither is untouched — `/template-mty` took 8 files
    (incl. the `b76356f2` role-similarity graph feature) and `/forma-proposal` took 2 files
    (the `4638020b` brand-palette theme pass). Both were confirmed WebGL-free
    (`RoleSimilarityGraph.tsx` imports react / next-themes / framer-motion / ECharts helpers /
    `TIER_COLORS` — no `three`, no `@react-three`, no cosmos.gl). Write it as fact, not as
    "untouched".
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
    <automated>git diff --stat v2.2..HEAD -- "app/(dashboard)/users/spatial-graph" | wc -l</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exit 0; `npm test` 0 failures (or exactly the 1 known `physicsLayer`
    flake, proven passing in isolation); all three TEST-01/02/03 diffs empty; the WebGL-import
    grep returns 0; the spatial-graph diff is empty. All outputs captured verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 23-VERIFICATION.md, fix the Phase 22 + 23 ROADMAP checkboxes, commit</name>
  <files>.planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md, .planning/ROADMAP.md</files>
  <action>
    **Write `23-VERIFICATION.md`.** Its frontmatter `status` MUST be one of
    `{passed, owner_approved, owner-approved, approved, complete}` — `scripts/gsd-self-gate.cjs`'s
    `validatePhaseArtifact()` hard-fails on anything else, and plan 23-05 runs that script.
    Use `status: owner_approved` (the owner sign-off in 23-03 is the basis).

    Body — one section per ROADMAP success criterion, each with its verbatim command output:
    - **#1 (reinterpreted per CONTEXT.md + research):** the tab IA from Phase 20.1 is the
      answer to the wall-of-charts risk; the live inventory is **23 panels** (Overview 5,
      Roles 6, Users 2, Companies 3, Projects 6, Compare 1) — CONTEXT.md's 22 was wrong, it
      omitted "Folder Activity by Role". Record the per-tab lead-panel verdict from
      `23-02-SUMMARY.md`. **State plainly whether the curation diff was zero — a zero-diff
      curation result is a PASS.** No grouping/collapse scheme was built, no tab order
      changed, no panel counts rebalanced, no panel cut — all four are explicitly REJECTED
      goals in CONTEXT.md, not omissions.
    - **#2:** owner sign-off, captured post-rebuild on `:3000`, across the full four-page
      surface at graded depth. Cite `23-03-SUMMARY.md` + the filled `23-REVIEW-CHECKLIST.md`.
      Note the fix-now / v2.4-seed split from `23-FINDINGS.md`. Call out that
      `Issues by type` (`IssueTypeChart`) now has its first-ever recorded owner verdict.
    - **#3:** the Task-2 gate outputs verbatim.
    - **#4:** the Task-2 scope-fence proofs + `check.cjs` exit 0 (noting the baseline refresh
      in Task 1 and WHY it was needed — pre-existing staleness from `b95bf5c7`, not a v2.3
      regression).

    Add a **Known tooling gap** section (do not hide it, and do not "fix" it by corrupting
    STATE): `scripts/gsd-self-gate.cjs`'s `validateStateAgainstRoadmap()` counts phase
    checkboxes cumulatively across ALL milestones in ROADMAP.md (17 total, v2.1→v2.3) and
    requires STATE.md's `progress.total_phases`/`completed_phases` to match. STATE.md's
    `progress` block is intentionally **milestone-scoped** (6/5 for v2.3). This check will
    therefore report a mismatch regardless of Phase 22/23 — a pre-existing tool/convention
    mismatch, NOT a regression. Plan 23-05 reads the script's `checks[]` array, not its
    top-level `ok`.

    **Fix `.planning/ROADMAP.md` with scoped `Edit` calls — NEVER a whole-file `Write`**
    (the branch's ROADMAP holds three milestones; a full rewrite would destroy them):
    1. Line ~300: `- [ ] **Phase 22: ...` → `- [x] **Phase 22: ...` (shipped 3/3, closed
       2026-07-14 on live evidence — its checkbox was never flipped).
    2. Line ~301: `- [ ] **Phase 23: ...` → `- [x] **Phase 23: ...`
       (Required now, not at 23-05: `gsd-self-gate.cjs --phase 23` refuses to validate a phase
       whose checkbox is `[ ]`. Execution is effectively complete at this point — 23-05 writes
       only milestone-close artifacts.)
    3. Phase 23's `**Plans**: TBD` → the 5-plan list with `[x]` per completed plan.
    4. The Progress table row `| 23. Workshop Curation & Milestone Close | 0/TBD | Not started | - |`
       → `| 5/5 | Complete | 2026-07-14 |` (use the real completion date).
    5. The Phase 22 Progress-table row already says Complete — leave it.

    **Commit by explicit path.** The branch carries ~100 files of unrelated dirty WIP:
    ```
    git add .tools/repo-map/baselines/dependency-cruiser-baseline.json
    git add .tools/repo-map/            # regenerated reports from Task 1
    git add .planning/ROADMAP.md
    git add .planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md
    git diff --cached --name-only       # MUST list only the above. Anything else → unstage.
    git commit -m "chore(23): refresh repo-map baseline, close Phase 22/23 checkboxes, record verification"
    ```
    NEVER `git add -A` or `git add .`.
  </action>
  <verify>
    <automated>grep -nE '^- \[[xX]\] \*\*Phase 2[23]:' .planning/ROADMAP.md | wc -l</automated>
    <automated>grep -nE '^status:' .planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md</automated>
    <automated>git diff --cached --name-only</automated>
  </verify>
  <done>
    `23-VERIFICATION.md` exists with an accepted `status` and one evidence section per ROADMAP
    criterion plus the known-tooling-gap note. Both Phase 22 and Phase 23 ROADMAP checkboxes
    are `[x]` (the grep returns 2). Phase 23's Plans list and Progress-table row are filled.
    ROADMAP was edited with scoped `Edit` calls, never a whole-file `Write`. Commit staged by
    explicit path with proof.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ratchet baseline → future regressions | `.tools/repo-map/baselines/*` is the gate that catches new boundary violations; loosening it silently disarms that gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | `.tools/repo-map/baselines/dependency-cruiser-baseline.json` | mitigate | Ratchet DOWN only — never raise a rule's warning count above its live value. A live count HIGHER than baseline means a real new violation: STOP and report, do not baseline it away. |
| T-23-09 | Tampering | `.planning/ROADMAP.md` (holds all 3 milestones' history) | mitigate | Scoped `Edit` calls only; a whole-file `Write` would destroy v2.1/v2.2 phase records. |
| T-23-10 | Tampering | git index (~100 unrelated dirty WIP files on branch) | mitigate | Explicit-path staging + `git diff --cached --name-only` proof; never `git add -A`. |

No package installs. No source-code change in this plan.
</threat_model>

<verification>
- `node scripts/repo-map/check.cjs` → exit 0
- `npx tsc --noEmit` → exit 0
- `npm test` → 0 failed (or exactly the known `physicsLayer.test.ts` flake, proven passing in isolation)
- `git diff v2.2 -- lib/server/acc-hot-cache.test.ts` → empty (TEST-01)
- `git diff v2.2 -- lib/server/__tests__/folderPermissionTerrainView.test.ts` → empty (TEST-02)
- `git diff v2.2 -- lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts` → empty (TEST-03)
- `git diff v2.2..HEAD -- "app/(dashboard)/access-analysis/" | grep -cE '^\+\s*(import|require).*(three|@react-three|cosmos\.gl|webgl)'` → `0`
- `git diff --stat v2.2..HEAD -- "app/(dashboard)/users/spatial-graph"` → empty
- `grep -nE '^- \[[xX]\] \*\*Phase 2[23]:' .planning/ROADMAP.md` → 2 lines
- `23-VERIFICATION.md` frontmatter `status: owner_approved`
</verification>

<success_criteria>
ROADMAP criteria #3 and #4 are proven with captured command output. `check.cjs` is green for
the first time (stale baseline refreshed). The verification record exists and satisfies
`gsd-self-gate.cjs`'s structural requirements, unblocking the final gate in plan 23-05.
</success_criteria>

<output>
Create `.planning/phases/23-workshop-curation-milestone-close/23-04-SUMMARY.md` when done,
with every gate command's verbatim output.
</output>
