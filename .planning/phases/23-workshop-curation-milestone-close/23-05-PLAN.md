---
phase: 23-workshop-curation-milestone-close
plan: 05
type: execute
wave: 5
depends_on: [23-04]
files_modified:
  - .planning/MILESTONES.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/config.json
  - .planning/milestones/v2.3-ROADMAP.md
  - .planning/milestones/v2.3-REQUIREMENTS.md
autonomous: true
requirements: [ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04, ISSUE-05, PERM-01, ENG-01, PIPE-01]

must_haves:
  truths:
    - "`.planning/MILESTONES.md` retains its v2.0 and v1.0 entries AND gains a v2.3 entry — no history was discarded"
    - "`.planning/STATE.md` frontmatter is intact after every tool write (current_phase/milestone/status not mangled)"
    - "`scripts/gsd-self-gate.cjs --phase 23 --route /users --rebuild` ran; its rebuild/health/route checks passed"
    - "PROJECT.md's v2.3 Active entry is promoted to Validated"
    - "Every v2.4 seed from 23-FINDINGS.md is carried into ROADMAP — no finding is lost"
    - "v2.3 is closed and the milestone is archived"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "Milestone history — v2.0/v1.0 preserved, v2.3 appended"
      contains: "v2.0"
    - path: ".planning/milestones/v2.3-ROADMAP.md"
      provides: "Archived v2.3 roadmap (written by `gsd-tools milestone complete`)"
  key_links:
    - from: "23-FINDINGS.md `## Deferred to v2.4` entries"
      to: ".planning/ROADMAP.md v2.4 seed section + PROJECT.md Active"
      via: "each deferred owner finding is lifted verbatim as a next-milestone seed; a seed recorded at sign-off but not carried forward is a lost finding"
      pattern: "v2.4"
---

<objective>
Close v2.3. Write the milestone artifacts, and do it without tripping the two landmines this
repo has already proven are real.

**LANDMINE 1 — `.planning/MILESTONES.md` is git-tracked but DELETED from the working tree**
(part of the branch's `.planning/` migration WIP). `gsd-tools milestone complete` does
`if (fs.existsSync(milestonesPath)) { insert } else { writeFileSync(new file) }`. Run against
the current tree it will **silently fabricate a fresh MILESTONES.md containing only the v2.3
entry, discarding the v2.0 and v1.0 history that lives in git HEAD.** Restore the file
BEFORE running anything.

**LANDMINE 2 — `gsd-tools state record-session` corrupts STATE.md frontmatter.** This was not
inferred from memory; the researcher **live-reproduced it this session**: one invocation
mangled `current_phase: 23` → `current_phase: 20` and overwrote `current_phase_name` with the
body fragment `"COMPLETE, 5/5 plans"`. It was reverted with `git checkout -- .planning/STATE.md`.
**NEVER run `gsd-tools state record-session` in this phase.** And after ANY gsd-tools command
that writes STATE (including `milestone complete`), immediately `git diff -- .planning/STATE.md`
and repair the frontmatter by hand.

Purpose: v2.3 becomes a closed, archived milestone with its history intact.
Output: MILESTONES.md entry, archived roadmap/requirements, STATE snapshot, PROJECT.md
promotion, ROADMAP v2.4 seeds, config reset.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/config.json
@.planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md
@.planning/phases/23-workshop-curation-milestone-close/23-VERIFICATION.md
@.planning/phases/23-workshop-curation-milestone-close/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore MILESTONES.md, correct STATE frontmatter, then run the final self-gate</name>
  <files>.planning/MILESTONES.md, .planning/STATE.md</files>
  <action>
    **Step 1 — restore MILESTONES.md from git (DO THIS FIRST, before any other command):**
    ```
    git checkout -- .planning/MILESTONES.md
    grep -cE '^## v(2\.0|1\.0)' .planning/MILESTONES.md    # MUST return 2
    ```
    The file is tracked (`git ls-files` confirms) but absent from the working tree
    (`ls` fails). `git show HEAD:.planning/MILESTONES.md` has 38 lines with `## v2.0` and
    `## v1.0` sections. If the grep does not return 2, STOP — do not proceed to
    `milestone complete`.

    **Do NOT backfill v2.1/v2.2 entries.** They shipped and were never logged to
    MILESTONES.md — a real, pre-existing documentation gap, but out of scope here. Record it
    as a v2.4 seed in Task 3 instead. (Owner decision: keep the close tight.)

    **Step 2 — correct STATE.md frontmatter by hand (targeted `Edit`, not `Write`):**
    - `status: active` → `status: complete`.
      This is genuinely correct at milestone close AND it is one of the lifecycle values
      `scripts/gsd-self-gate.cjs` accepts (`planning, in_progress, executing, verifying,
      complete, completed, blocked, not_started, "not started"`). `active` is NOT in that
      set, so the gate fails on it today.
    - Leave `progress` MILESTONE-SCOPED (`total_phases: 6`, `completed_phases: 6` once Phase
      23 lands). **Do NOT inflate it to the roadmap-wide 17.** `gsd-self-gate.cjs` counts
      checkboxes cumulatively across all three milestones and will flag a mismatch — that is
      the known tool/convention gap already documented in `23-VERIFICATION.md`, not something
      to "fix" by corrupting the milestone-scoped convention every other artifact reads.
    - Update `last_activity`, `last_activity_desc`, `stopped_at`, and `current_phase_name` to
      reflect the close.

    **Step 3 — run the FINAL gate:**
    ```
    node scripts/gsd-self-gate.cjs --phase 23 --route /users --rebuild
    ```
    `--route /users` is REQUIRED and must be passed explicitly: the script's
    `inferRoutesForPhase()` regex-scans only the ROADMAP Phase-23 section text, which mentions
    `/access-analysis` and `/template-mty` but **never `/users`** — so `/users` would silently
    go unprobed even though it is in the owner's sign-off scope.

    This works NOW (and could not before) because plan 23-04 wrote `23-VERIFICATION.md` with
    an accepted `status` and flipped the Phase-23 ROADMAP checkbox to `[x]` — the two things
    `validatePhaseArtifact()` hard-requires.

    The script stops the `LECG Dashboard Local` scheduled task, kills anything on :3000, runs
    `npx tsc --noEmit`, runs `npm run build`, restarts the task, then polls the routes for
    <500 — i.e. it performs the manual deploy sequence correctly, including the
    never-build-while-:3000-is-live guard.

    **READ ITS `checks[]` ARRAY, NOT ITS TOP-LEVEL `ok`.** The report is written to
    `.planning/phases/23-workshop-curation-milestone-close/23-SELF-GATE.json`. Expect
    `ok: false` overall because of the known STATE-progress-count mismatch above. What MUST
    pass: the config check, the GSD-health check, the phase-artifact check, the rebuild steps
    (tsc + build), and every route probe (`/api/health`, `/access-analysis`, `/template-mty`,
    `/users`). If a REBUILD or ROUTE check fails, that is a real failure — stop and fix.
    Record the per-check breakdown in the SUMMARY.
  </action>
  <verify>
    <automated>grep -cE '^## v(2\.0|1\.0)' .planning/MILESTONES.md</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/health</automated>
  </verify>
  <done>
    MILESTONES.md is restored with both v2.0 and v1.0 sections present. STATE.md `status` is
    `complete` and its `progress` block is still milestone-scoped. The self-gate ran with
    `--route /users`; its rebuild + health + route checks all passed; the JSON report exists
    and its per-check breakdown is recorded. `:3000` is serving and `/api/health` returns 200.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run milestone complete, then diff-and-repair STATE.md</name>
  <files>.planning/MILESTONES.md, .planning/STATE.md, .planning/milestones/v2.3-ROADMAP.md, .planning/milestones/v2.3-REQUIREMENTS.md</files>
  <action>
    Capture the pre-write STATE so you can prove what the tool changed:
    ```
    cp .planning/STATE.md .planning/STATE.md.pre-close.bak   # scratch copy, DO NOT COMMIT
    node .claude/gsd-core/bin/gsd-tools.cjs milestone complete v2.3 --name "New Graphs"
    ```
    (`complete` is `milestone`'s only subcommand.) It will: archive `ROADMAP.md` →
    `.planning/milestones/v2.3-ROADMAP.md` and `REQUIREMENTS.md` →
    `.planning/milestones/v2.3-REQUIREMENTS.md`; INSERT a `## v2.3 New Graphs (Shipped: <date>)`
    entry into MILESTONES.md (reverse-chronological, above v2.0 — this is why Task 1's restore
    had to come first); and rewrite parts of STATE.md.

    Its pre-flight guard checks that a directory exists under `.planning/phases/` for every
    `#### Phase N` heading in the v2.3 ROADMAP section. All 6 v2.3 phase dirs (20, 20.1, 21,
    21.1, 22, 23) exist, so it should pass WITHOUT `--force`. If it demands `--force`, STOP and
    investigate — do not force past a guard you have not understood.

    Do NOT pass `--archive-phases` (it MOVES all 6 phase dirs into
    `.planning/milestones/v2.3-phases/`). Archival is deferred on this branch — the `.planning/`
    tree is mid-migration with heavy dirty WIP, and moving directories now would tangle with it.
    Record archival as a v2.4 seed in Task 3.

    **Then immediately diff and repair STATE.md:**
    ```
    git diff -- .planning/STATE.md
    ```
    Verify the frontmatter was not mangled. The `milestone complete` path is known to write
    STATE via body-field replacement (`stateReplaceFieldWithFallback`) that may no-op or
    misfire against this repo's structure. Confirm, field by field:
    - `milestone: v2.3` (correct for the closed milestone record)
    - `current_phase: 23`, `current_phase_name: Workshop Curation & Milestone Close`
    - `status: complete`
    - `progress` still milestone-scoped
    If ANY field was corrupted, repair it by hand with a targeted `Edit` against
    `.planning/STATE.md.pre-close.bak` as the reference. This is the exact class of corruption
    the researcher live-reproduced this session.

    Watch stderr for `[gsd-tools] WARNING:` — the script logs (rather than fails) when a
    field-replace does not match. A warning here means a manual STATE edit is required.

    **Never run `gsd-tools state record-session`** — proven to corrupt frontmatter.

    Delete the `.bak` scratch file before staging anything. It must never be committed.
  </action>
  <verify>
    <automated>git diff -- .planning/STATE.md</automated>
    <automated>grep -cE '^## v(2\.3|2\.0|1\.0)' .planning/MILESTONES.md</automated>
  </verify>
  <done>
    MILESTONES.md has three sections (v2.3, v2.0, v1.0 — grep returns 3).
    `.planning/milestones/v2.3-ROADMAP.md` and `v2.3-REQUIREMENTS.md` exist. STATE.md's
    frontmatter was diffed post-write and every field verified/repaired. `state record-session`
    was never invoked. The `.bak` scratch file is deleted.
  </done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md promotion, ROADMAP v2.4 seeds, config reset, commit</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/config.json</files>
  <action>
    All edits below are **scoped `Edit` calls. NEVER a whole-file `Write`** on PROJECT.md or
    ROADMAP.md — ROADMAP carries three milestones of history and PROJECT.md carries the
    project constraints; a full rewrite destroys them.

    **1. `.planning/PROJECT.md` — Active → Validated.**
    Move the Active entry (line ~116): "**New graphs for `/access-analysis` + `/template-mty`**
    — CURRENT MILESTONE v2.3" into the `### Validated` list as:
    `- ✓ **New graphs for /access-analysis** (ISSUE-01–05, PERM-01, ENG-01, PIPE-01) — v2.3
    (8/8 requirements; 23-panel surface owner-approved on :3000 <date>)`
    Match the existing Validated-entry format (see the v2.1/v2.2 lines).

    Keep the other Active entries as-is (SVC-01, spatial-graph milestone, DC-01/DC-02,
    per-folder terrain projection) — they remain deferred candidates.

    **Do not touch PROJECT.md's Constraints section.** Prisma DB as analytics source, zinc
    theme, no new WebGL on data surfaces, `/users/spatial-graph` out of scope, and
    `npx tsc --noEmit` before rebuilds all stand.

    **2. `.planning/ROADMAP.md` — v2.4 seeds.**
    Flip the milestone bullet at the top: `🚧 **v2.3 New Graphs** - Phases 20-23 (in progress...)`
    → `✅ **v2.3 New Graphs** - Phases 20-23 (shipped <date>)`.

    Then write a `## 📦 v2.4 Seed Pool` section carrying forward, each as one line:
    - **Every `## Deferred to v2.4` entry from `23-FINDINGS.md`** — lift the seed statements
      verbatim. A finding the owner raised at sign-off that does not land here is a LOST
      finding; that is the failure mode this whole phase exists to prevent.
    - The four already-deferred v2.3 seeds from REQUIREMENTS.md "Future Requirements": folder
      storage treemap, permission tier × folder-depth heatmap, activity verb/object-type
      breakdown, provisioned-vs-active module coverage.
    - The carried-forward v2.2 candidates already listed at the bottom of ROADMAP: SVC-01,
      spatial-graph milestone, DC-01/DC-02, per-folder terrain projection.
    - **Playwright/dev-server infra fix** (carried from 20.1-07): `playwright.config.ts`'s
      `webServer.command` hardcodes `next dev --webpack`, which 500s on this machine;
      `--turbopack` deterministically corrupts CSS. E2e specs needing a dev server stay
      blocked. Belongs in an infra phase.
    - **Phase 17 SPLIT-04 owner visual sign-off** (carried from v2.2) — accepted on a
      test-basis/DOM-golden basis only; no production route mounts the surface.
    - **MILESTONES.md v2.1/v2.2 backfill** — both shipped but were never logged to
      MILESTONES.md (pre-existing documentation gap, found during this close).
    - **`.planning/` phase-directory archival** — deliberately deferred at the v2.3 close
      (`--archive-phases` not used) because the `.planning/` tree is mid-migration on this
      branch.
    - **`gsd-self-gate.cjs` STATE-count convention mismatch** — the script counts ROADMAP
      checkboxes cumulatively across all milestones while STATE.md's `progress` is
      milestone-scoped, so its state check cannot pass. Either scope the script's count to the
      active milestone or change the STATE convention — a tooling decision, not a product one.

    **3. `.planning/config.json` — reset for the next milestone.**
    Set `workflow.research` back to its milestone-start default and clear any v2.3-specific
    drift. Do NOT change `workflow.build_command` — it MUST stay `"npx tsc --noEmit"`;
    `gsd-self-gate.cjs`'s `validateConfig()` explicitly FAILS if `build_command` matches
    `/npm\s+run\s+build/` (full builds belong to `--rebuild`, which stops `:3000` first).
    Leave `nyquist_validation`, `ai_integration_phase`, and `human_verify_mode: end-of-phase`
    as they are — the gate requires all three.

    **4. Commit — explicit paths only.** The branch carries ~100 files of unrelated dirty WIP
    (deleted `.agent/`, `.agents/`, `.gemini/` trees, `.planning/` migration deletions):
    ```
    git add .planning/MILESTONES.md
    git add .planning/STATE.md
    git add .planning/PROJECT.md
    git add .planning/ROADMAP.md
    git add .planning/config.json
    git add .planning/milestones/v2.3-ROADMAP.md .planning/milestones/v2.3-REQUIREMENTS.md
    git add .planning/phases/23-workshop-curation-milestone-close/
    git diff --cached --name-only     # MUST list ONLY the above. Anything else → unstage.
    git commit -m "chore(v2.3): close New Graphs milestone — 8/8 requirements, 23-panel surface owner-approved"
    ```
    NEVER `git add -A` or `git add .`. Confirm `.planning/STATE.md.pre-close.bak` is NOT in the
    staged set.
  </action>
  <verify>
    <automated>git diff --cached --name-only</automated>
    <automated>node -e "const c=require('./.planning/config.json'); if(/npm\s+run\s+build/.test(c.workflow.build_command)) { console.error('FAIL: build_command must stay npx tsc --noEmit'); process.exit(1);} console.log('config ok:', c.workflow.build_command)"</automated>
  </verify>
  <done>
    PROJECT.md's v2.3 entry is in Validated and its Constraints are untouched. ROADMAP's v2.3
    milestone bullet reads shipped and a v2.4 Seed Pool section carries EVERY `23-FINDINGS.md`
    v2.4 item plus the standing deferred candidates. config.json is reset with `build_command`
    still `npx tsc --noEmit`. Commit staged by explicit path with `git diff --cached --name-only`
    proof; no `.bak` file and no unrelated WIP swept in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD tooling → `.planning/*` history files | `gsd-tools` writes MILESTONES.md/STATE.md automatically and has a proven record of destructive/corrupting writes on this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Destruction of data | `.planning/MILESTONES.md` (tracked, deleted from working tree) | mitigate | `git checkout -- .planning/MILESTONES.md` BEFORE `milestone complete`, verified by `grep -cE '^## v(2\.0\|1\.0)'` returning 2. Without it the tool writes a fresh file and discards v2.0/v1.0 history. |
| T-23-12 | Tampering | `.planning/STATE.md` frontmatter | mitigate | `state record-session` is FORBIDDEN (live-reproduced corruption this session). Every tool STATE write is followed by `git diff -- .planning/STATE.md` and manual field-by-field repair against a pre-write scratch copy. |
| T-23-13 | Destruction of data | `.planning/ROADMAP.md`, `.planning/PROJECT.md` | mitigate | Scoped `Edit` calls only; a whole-file `Write` destroys three milestones of history / the project constraints. |
| T-23-14 | Denial of Service | `:3000` during `gsd-self-gate.cjs --rebuild` | mitigate | The script stops the scheduled task and frees the port before `npm run build`; its route probes prove recovery. |
| T-23-15 | Tampering | git index (~100 unrelated dirty WIP files) | mitigate | Explicit-path staging + `git diff --cached --name-only` proof; never `git add -A`; scratch `.bak` explicitly excluded. |

No package installs, no source-code change, no auth/input/crypto surface in this plan.
</threat_model>

<verification>
- `grep -cE '^## v(2\.3|2\.0|1\.0)' .planning/MILESTONES.md` → `3` (v2.3 added, history preserved)
- `.planning/milestones/v2.3-ROADMAP.md` and `.planning/milestones/v2.3-REQUIREMENTS.md` exist
- `git diff -- .planning/STATE.md` reviewed post-tool-write; frontmatter fields verified/repaired
- `gsd-tools state record-session` never invoked (grep the session transcript / SUMMARY to confirm)
- `.planning/phases/23-workshop-curation-milestone-close/23-SELF-GATE.json` exists; its rebuild + health + route checks pass; `/users` was probed
- `/api/health` → 200 after the self-gate rebuild
- `config.json` `workflow.build_command` is still `npx tsc --noEmit`
- Every `## Deferred to v2.4` entry in `23-FINDINGS.md` appears in ROADMAP's v2.4 Seed Pool
- `git diff --cached --name-only` → only planned `.planning/` paths; no `.bak`, no unrelated WIP
</verification>

<success_criteria>
v2.3 New Graphs is closed: 8/8 requirements delivered, a 23-panel surface owner-approved on a
current `:3000` build, gates green, scope fence proven, history preserved, and every deferred
finding carried forward as a v2.4 seed. No Phase 23.1 exists.
</success_criteria>

<output>
Create `.planning/phases/23-workshop-curation-milestone-close/23-05-SUMMARY.md` when done,
including the post-write STATE.md diff and the self-gate `checks[]` breakdown.
</output>
