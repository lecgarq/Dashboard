---
phase: 23-workshop-curation-milestone-close
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - .planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md
  - .planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md
autonomous: false
requirements: [ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04, ISSUE-05, PERM-01, ENG-01, PIPE-01]

# autonomous: false — Task 1 is a BLOCKING human gate. This is the owner sign-off that
# closes all 8 v2.3 requirements and is the last gate before milestone close.
# `files_modified` lists only the guaranteed writes. Any fix-now source file is discovered
# at the checkpoint (Task 3) and cannot be pre-declared — it is bounded by the triage rule
# (no loader, no data) and gate-proven before commit.

must_haves:
  truths:
    - "The owner has walked all 23 `/access-analysis` panels graph-by-graph on the rebuilt `:3000` and given a per-panel verdict"
    - "`/users`, `/template-mty`, and `/forma-proposal` each received their graded pass and a recorded verdict"
    - "`Issues by type` (IssueTypeChart) — the only v2.3 panel with zero prior owner UAT — has an explicit recorded verdict"
    - "EVERY finding raised is dispositioned as fix-now OR v2.4-seed; none is left open"
    - "No Phase 23.1 is created — the milestone stays closeable"
  artifacts:
    - path: ".planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md"
      provides: "The triaged finding register — the durable record of what the owner said and where each item went"
      contains: "v2.4"
  key_links:
    - from: "23-FINDINGS.md v2.4-seed entries"
      to: ".planning/ROADMAP.md v2.4 seed section"
      via: "plan 23-05 carries each deferred finding forward as a next-milestone seed — a seed recorded here but not carried forward is a lost finding"
      pattern: "v2.4"
---

<objective>
The owner sign-off. This is the human gate the whole phase exists to reach: a graph-by-graph
walk of the full four-page workshop surface on the freshly-rebuilt `:3000`, with every
finding triaged the moment it is raised.

**Scope is the FULL 4-page workshop surface at GRADED depth** (owner's explicit decision):
- `/access-analysis` — DEEP: all 23 panels, all 6 tabs, one at a time. The core of the review.
- `/users` — deep-ish, VERIFICATION ONLY (no curation edits). Never reviewed since its
  2026-07-13 off-roadmap changes.
- `/template-mty` — short FUNCTIONAL pass (the never-seen role-similarity graph feature).
- `/forma-proposal` — short VISUAL pass (the brand-palette theme change).

**The anti-scope-creep rule is the point of this plan.** Every v2.3 UAT round surfaced
findings and every one spawned an inserted phase (Phase 20 → 20.1, Phase 21 → 21.1). That
pattern stretched v2.3 from 4 phases to 6. CONTEXT.md's explicit goal: **keep the milestone
CLOSEABLE. Do not insert a Phase 23.1.** The triage rule below is how — it is applied AT THE
MOMENT each finding is raised, not batched for later scoping.

Purpose: the last gate before milestone close.
Output: `23-FINDINGS.md` (triaged register), a filled-in `23-REVIEW-CHECKLIST.md`, and — only
if fix-now items exist — a small copy/label/visual diff.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-workshop-curation-milestone-close/23-CONTEXT.md
@.planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md
@.planning/phases/23-workshop-curation-milestone-close/23-02-SUMMARY.md
@.claude/skills/lecg-dashboard/references/deploy-sequence.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner graph-by-graph sign-off on the full workshop surface</name>
  <files>.planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md (read + fill Verdict column)</files>
  <action>
    BLOCKING HUMAN GATE. Do not self-approve, do not infer the owner's verdict, do not
    auto-advance past this task — `workflow.auto_advance` does not apply to a sign-off gate.
    Drive the review with Luis exactly as scripted in <how-to-verify> below, one panel at a
    time, triaging each finding out loud the moment it is raised. Then stop and wait for the
    resume signal.
  </action>
  <what-built>
    Nothing new was built. Phases 20–22 delivered all 8 v2.3 requirements; plan 23-01 put the
    current tree on `:3000`; plan 23-02 recounted the surface at 23 panels and produced
    `23-REVIEW-CHECKLIST.md`. This is the review of that surface.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md` and
    walk it with Luis, one panel at a time. Do not present a wall of questions.

    Confirm first that `:3000` is serving the fresh build (plan 23-01 proved
    `.next/BUILD_ID` mtime > HEAD commit time). If `:3000` is down, re-run the deploy
    sequence before starting — a review of a stale surface is worthless.

    1. **`/access-analysis` — DEEP (http://localhost:3000/access-analysis)**
       Tab by tab, in the shipped order (Overview → Roles → Users → Companies → Projects →
       Compare). For each of the 23 panels, ask the presenter test:
       - Does the panel answer the question its subtitle claims?
       - Can you explain live, without devtools, what data backs it and where coverage is
         incomplete?
       - Does the honest-coverage label read correctly on the live build?
       Record `ok` / `fix-now` / `v2.4-seed` per panel in the Verdict column.

       **Give `Issues by type` (Projects tab, `IssueTypeChart`) real attention.** It is the
       only v2.3 panel with ZERO recorded owner UAT — Phase 22 closed on live-`:3000`
       evidence and its `:3100` preflight checkpoint was never run (`22-03-SUMMARY.md`).
       Specifically check: type names resolve to human-readable names (never raw GUIDs);
       the "Unknown type" and "No type set" buckets read as two distinct, honest buckets;
       the top-10 + expand-in-place "Other" works; the per-type project drill works.

    2. **`/users` — VERIFICATION ONLY (http://localhost:3000/users)**
       No curation edits here. Its 2026-07-13 off-roadmap changes have never been reviewed:
       directory summary tiles, external collaborators, affiliation filter, company column.
       Do they read correctly and are the numbers credible?

    3. **`/template-mty` — SHORT FUNCTIONAL pass (http://localhost:3000/template-mty)**
       It picked up a real, never-owner-seen feature during the v2.3 window: the richer
       role-similarity graph (cluster blobs, curved edges, similarity %, commit `b76356f2`)
       plus a label fix (`2e15233f`). Does the graph render, do labels stay glued to their
       nodes, is it explainable in a live demo?
       (Precise framing: no v2.3-*requirement* panel landed here — but the page is NOT
       untouched. `git diff v2.2..HEAD` shows 8 changed files.)

    4. **`/forma-proposal` — SHORT VISUAL pass (http://localhost:3000/forma-proposal)**
       It picked up only the LECG brand-palette theme commit (`4638020b`, 2 files). Does the
       palette read correctly? Any theme drift, contrast, or zinc-consistency problem?

    **TRIAGE EVERY FINDING THE MOMENT IT IS RAISED. Say which bucket it lands in, out loud,
    before moving to the next panel:**
    - **FIX IN PHASE 23** — copy/label fix, wrong wording, ordering nit, obvious visual
      defect. Test: does it touch NO loader and add NO data? Then it is fix-now.
    - **DEFER TO v2.4** — needs a new loader, chart, data source, or schema/taxonomy change.
      Record as a milestone seed. **Do NOT build it in this phase.**
    - **Disputed NUMBER (not a label)** → data-truth finding → v2.4, UNLESS the cause is a
      display bug (then it is fix-now).

    There is no Phase 23.1. If a finding feels too big for fix-now, it is a v2.4 seed by
    definition — that is the correct, intended outcome, not a failure.
  </how-to-verify>
  <resume-signal>
    Type "approved" (or "approved but ..." with the findings). List each finding with its
    triage bucket. If any panel is a hard blocker for the milestone, say so explicitly.
  </resume-signal>
  <verify>
    <human-check>The owner has given a recorded verdict for all 23 `/access-analysis` panels (including `Issues by type`, which had none before) plus `/users`, `/template-mty`, and `/forma-proposal`. Every finding raised carries a spoken triage bucket: fix-now or v2.4-seed.</human-check>
  </verify>
  <done>The owner's resume signal is received and every panel/page has a verdict. No finding is left un-dispositioned.</done>
</task>

<task type="auto">
  <name>Task 2: Write the triaged finding register</name>
  <files>.planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md, .planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md</files>
  <action>
    Fill in the Verdict column of `23-REVIEW-CHECKLIST.md` with the owner's per-panel calls.

    Write `23-FINDINGS.md` with two sections and nothing else:

    **## Fix in Phase 23** — one row per item: panel · exact file(s) · what changes · why it
    qualifies (touches no loader, adds no data). If the owner raised nothing, write
    "None — surface approved as shipped." That is a legitimate, expected outcome.

    **## Deferred to v2.4 (milestone seeds)** — one row per item: panel/surface · what the
    owner asked for · why it is deferred (needs a new loader / chart / data source /
    schema-taxonomy change) · a one-line seed statement plan 23-05 can lift verbatim into
    ROADMAP's v2.4 seed section.

    Record the owner's verbatim words where he gave a verdict. Do not paraphrase approval
    into something stronger than he said. If he said "approved but...", the "but" items are
    findings, not decoration.

    Every finding raised in Task 1 MUST appear in exactly one of the two sections. An
    un-dispositioned finding is a phase failure — it is exactly what spawns a Phase 23.1.
  </action>
  <verify>
    <automated>test -f ".planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md" && grep -cE '^## (Fix in Phase 23|Deferred to v2\.4)' ".planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md"</automated>
  </verify>
  <done>`23-FINDINGS.md` exists with both sections present (either may legitimately be empty, stated explicitly). Every Task-1 finding is in exactly one section. `23-REVIEW-CHECKLIST.md`'s Verdict column is filled for all 23 panels + the three other pages.</done>
</task>

<task type="auto">
  <name>Task 3: Apply fix-now findings (CONDITIONAL — skip entirely if none)</name>
  <files>(discovered at Task 1 — bounded by the triage rule: copy/label/ordering/visual only, NO loader, NO data)</files>
  <action>
    **If `23-FINDINGS.md`'s "Fix in Phase 23" section is empty, SKIP THIS TASK ENTIRELY.**
    Do not invent work to fill it. Record "no fix-now items" in the SUMMARY and move on.

    Otherwise, apply ONLY the items in that section. Hard bounds, enforced:
    - No new loader, no new Prisma query, no new server action, no schema edit.
    - No new chart, panel, or data source.
    - No new npm dependency.
    - No new WebGL / R3F import on any `/access-analysis` file.
    - `/users/spatial-graph` is NOT touched.
    - Preserve the zinc theme, resolved ECharts theme colors, the 6-tab IA, and every
      existing loader/gate condition.

    If, while implementing, an item turns out to need a loader or new data — STOP. It was
    mis-triaged. Move it to the v2.4 section of `23-FINDINGS.md` and say so in the SUMMARY.
    Do not build it.

    Gates after the edits:
    - `npx tsc --noEmit` → exit 0
    - `npm test -- "app/(dashboard)/access-analysis"` → green (extend to `/users` etc. if the
      fix landed there)

    Then REDEPLOY so the approved surface equals the deployed surface — the owner cannot have
    signed off on code that is not live. Use the manual sequence
    (`.claude/skills/lecg-dashboard/references/deploy-sequence.md`):
    Stop-ScheduledTask → kill :3000 → `npx tsc --noEmit` → `npm run build` → Start-ScheduledTask
    → `/api/health` 200. NEVER build while `:3000` is live.

    Then ask the owner for a SHORT confirm on ONLY the fixed panels — not a full 23-panel
    re-walk. If he confirms, the sign-off stands.

    Commit by explicit path. The branch carries ~100 files of unrelated dirty WIP:
    ```
    git add <each fixed file, by explicit path>
    git add .planning/phases/23-workshop-curation-milestone-close/23-FINDINGS.md
    git add .planning/phases/23-workshop-curation-milestone-close/23-REVIEW-CHECKLIST.md
    git diff --cached --name-only    # MUST list only those paths
    git commit -m "fix(23): owner sign-off copy/label corrections"
    ```
    NEVER `git add -A` or `git add .`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>git diff --cached --name-only</automated>
  </verify>
  <done>
    Either: no fix-now items existed and this task was correctly skipped. Or: every fix-now
    item is applied, touches no loader and adds no data, `npx tsc --noEmit` exits 0, the
    tests are green, `:3000` was rebuilt from the fixed tree, the owner confirmed the fixed
    panels, and the commit was staged by explicit path with `git diff --cached --name-only`
    proof.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner → live `:3000` browser session | The owner drives the real production surface with real ACC data during the review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Repudiation | Owner sign-off record | mitigate | Capture the owner's verbatim verdict per panel in `23-REVIEW-CHECKLIST.md` + `23-FINDINGS.md`, committed to git. Phase 22's thin verification trail (no recorded UAT for `IssueTypeChart`) is exactly the gap this closes. |
| T-23-06 | Tampering | git index during fix-now commits | mitigate | Explicit-path staging with `git diff --cached --name-only` proof; never `git add -A`. |
| T-23-07 | Denial of Service | `:3000` during the Task-3 redeploy | mitigate | Stop the scheduled task before `npm run build`; `/api/health` 200 probe proves recovery. |

Fix-now edits are bounded to copy/label/visual by the triage rule — no new input path, no
auth/authz change, no new dependency, no data-layer change.
</threat_model>

<verification>
- Owner verdict recorded for all 23 `/access-analysis` panels + `/users` + `/template-mty` + `/forma-proposal`
- `Issues by type` has an explicit verdict (it had none before this phase)
- `23-FINDINGS.md` exists; every raised finding is in exactly one bucket
- If fix-now items landed: `npx tsc --noEmit` exit 0; targeted tests green; `:3000` rebuilt; `/api/health` 200; owner re-confirmed the fixed panels
- No new WebGL import on `/access-analysis`: `git diff -- "app/(dashboard)/access-analysis/" | grep -cE "^\+\s*(import|require).*(three|@react-three|cosmos\.gl|webgl)"` → `0`
- `/users/spatial-graph` untouched: `git diff --stat -- "app/(dashboard)/users/spatial-graph"` → empty
- `git diff --cached --name-only` → only planned paths on every commit
</verification>

<success_criteria>
The full four-page workshop surface is owner-approved on a current `:3000` build. Every
finding is dispositioned fix-now or v2.4-seed. The milestone is still closeable — no Phase
23.1 was created.
</success_criteria>

<output>
Create `.planning/phases/23-workshop-curation-milestone-close/23-03-SUMMARY.md` when done.
Record the owner's verbatim verdict, the fix-now/v2.4 split, and — if fix-now items landed —
the redeploy evidence.
</output>
