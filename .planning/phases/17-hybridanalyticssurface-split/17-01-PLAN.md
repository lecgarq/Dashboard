---
phase: 17-hybridanalyticssurface-split
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"
autonomous: true
requirements: [SPLIT-03]

must_haves:
  truths:
    - "A NEW characterization test file pins the MAIN DuckDB-Wasm ready path of HybridAnalyticsSurface: when getDuckDbClient() resolves and runGraphAnalyticsQueries returns a status:\"ready\" AnalyticsQueryState, the surface shows the \"DuckDB-Wasm\" badge and renders the Mosaic panels (DistributionPanel / HeatmapPanel / HistogramPanel) — NOT the local fallback bars (SPLIT-03)."
    - "The existing HybridAnalyticsSurface.fallback.test.tsx is byte-identical (zero edits) and still green — this plan adds coverage, it does not touch the fallback pin."
    - "NO source file is moved, split, or edited in this plan — HybridAnalyticsSurface.tsx and every sibling module are untouched. The green baseline is established BEFORE any code moves (this is the SPLIT-04 pre-condition)."
    - "The targeted suite (new main-path test + fallback test) is green under `npx vitest run`, and the new test is committed by explicit path."
  artifacts:
    - path: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"
      provides: "Component-level characterization test that forces the DuckDB-Wasm READY branch and asserts the ready-state DOM (DuckDB-Wasm badge + mocked Mosaic panels), pinning the main query path SPLIT-04 will move into a hook"
      contains: "runGraphAnalyticsQueries"
      max_lines: 200
  key_links:
    - from: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"
      to: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx"
      via: "import { HybridAnalyticsSurface } from \"./HybridAnalyticsSurface\" — same public entry as the fallback test"
      pattern: "import \\{ HybridAnalyticsSurface \\} from \"\\./HybridAnalyticsSurface\""
    - from: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"
      to: "app/(dashboard)/users/access-analysis/duckdbClient.ts"
      via: "vi.mock(\"./duckdbClient\") — getDuckDbClient RESOLVES to a fake { connection } so the main effect runs the ready path"
      pattern: "vi\\.mock\\(\"\\./duckdbClient\""
    - from: "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"
      to: "app/(dashboard)/users/access-analysis/analyticsQueries.ts"
      via: "partial mock (vi.importActual) that overrides runGraphAnalyticsQueries to resolve a status:\"ready\" AnalyticsQueryState while keeping the real EMPTY_ANALYTICS_QUERY_STATE / buildFallbackAnalyticsState / errorToAnalyticsDiagnostic exports"
      pattern: "vi\\.mock\\(\"\\./analyticsQueries\""
---

<objective>
Widen the characterization net around `app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx`
so the **main DuckDB-Wasm query path** is pinned by an automated test BEFORE any code is moved.
This is the SPLIT-03 gate of REF-01: it establishes the green baseline that makes the SPLIT-04 split
(Plan 17-02) safe.

Today the only pin is `HybridAnalyticsSurface.fallback.test.tsx`, which exercises the **rejected**
path (`getDuckDbClient` rejects → "DuckDB-Wasm unavailable" fallback) and the **initializing** path
(`getDuckDbClient` never resolves → immediate fallback rankings). Neither covers the READY branch —
the effect at HybridAnalyticsSurface.tsx lines 593-625 where `getDuckDbClient()` resolves and
`runGraphAnalyticsQueries({ connection, users, folderRows })` returns a `status:"ready"` state, flipping
the surface from local fallback bars to the Mosaic DuckDB-Wasm panels. That effect is exactly the logic
SPLIT-04 relocates into a data-hook, so it MUST be pinned first.

This plan adds ONE new test file and touches nothing else. No source is split here.

Purpose: prove the main-path behavior is characterized and green so SPLIT-04 can move the effect into a
hook with a deterministic before/after safety net.
Output: `HybridAnalyticsSurface.mainQuery.test.tsx`, green and committed; the fallback pin untouched.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The component under test — the READY branch is the pin target (badge + Mosaic panels):
@app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.tsx

# The existing pin to MIRROR (harness, mocks, fixtures) and to keep byte-identical:
@app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.fallback.test.tsx

# The query module whose runGraphAnalyticsQueries / AnalyticsQueryState this test overrides:
@app/(dashboard)/users/access-analysis/analyticsQueries.ts

<interfaces>
<!-- Verified from direct read of HybridAnalyticsSurface.tsx + the fallback test + analyticsQueries.ts. -->

Ready-branch discriminators (what the READY path renders, vs. fallback):
  - Header badge text: isReady → "DuckDB-Wasm"; fallback → the raw status string ("fallback"); the
    fallback test asserts "DuckDB-Wasm unavailable" (diagnostic copy) — the new test must NOT see that.
  - Ready branch renders the Mosaic panels the fallback test already mocks by test id:
      DistributionPanel  → data-testid="mosaic-distribution"  ("Projects per user")
      HeatmapPanel       → data-testid="mosaic-heatmap"       ("Roles x project status")
      HistogramPanel     → data-testid="mosaic-histogram"     ("Top projects" / "Top roles" / "Top companies", 3×)
    Fallback branch renders FallbackBarPanel instead (no mosaic-* test ids).

Mock surface (reuse the fallback test's mocks verbatim, then change only the two below):
  - Keep: vi.mock("@/lib/core/trpc", ...), vi.mock("../useMergedAccUsers", ...),
    vi.mock("./AccessEventsChart"), vi.mock("./DistributionPanel"), vi.mock("./HeatmapPanel"),
    vi.mock("./HistogramPanel"). These give a deterministic 1-user / 1-folder-row fixture and stub the
    Mosaic panels with test ids.
  - Change 1 — ./duckdbClient: canInitializeDuckDbInBrowser → true; getDuckDbClient → RESOLVES a fake
    client shaped { connection: {} } (the main effect only destructures `connection` and passes it to
    runGraphAnalyticsQueries, which is mocked — so an empty object is sufficient).
  - Change 2 — ./analyticsQueries: partial mock via vi.importActual so EMPTY_ANALYTICS_QUERY_STATE,
    buildFallbackAnalyticsState, and errorToAnalyticsDiagnostic keep their REAL implementations, and only
    runGraphAnalyticsQueries is overridden to resolve a synthetic status:"ready" AnalyticsQueryState.

AnalyticsQueryState (from analyticsQueries.ts) — shape for the synthetic ready value:
  { status: "ready", projectMembership: ChartDatum[], roleDistribution: ChartDatum[],
    folderPermissionTiers: ChartDatum[], activityRecency: ChartDatum[], similarityDimensions: ChartDatum[],
    diagnostic: null }
  ChartDatum = { label: string; value: number; field: string; values: string[]; detail?: string }.
  A couple of entries in projectMembership / roleDistribution are enough; the ready branch's visible
  rankings come from the mocked HistogramPanel test ids, not from these arrays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the main-path characterization test (force the DuckDB-Wasm READY branch)</name>
  <files>app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx</files>
  <action>
    Create `HybridAnalyticsSurface.mainQuery.test.tsx` co-located next to the fallback test. Start from
    the fallback test's harness: the `// @vitest-environment jsdom` pragma, the same imports from
    @testing-library/react and vitest, `import { HybridAnalyticsSurface } from "./HybridAnalyticsSurface"`,
    and REUSE verbatim the fallback test's mocks for @/lib/core/trpc, ../useMergedAccUsers,
    ./AccessEventsChart, ./DistributionPanel, ./HeatmapPanel, and ./HistogramPanel (identical fixtures,
    identical test ids). Do NOT modify HybridAnalyticsSurface.fallback.test.tsx.

    Then diverge in exactly two mocks to drive the READY path:
    (a) Mock ./duckdbClient so canInitializeDuckDbInBrowser returns true and getDuckDbClient RESOLVES a
        fake client of shape { connection: {} } (use a hoisted vi.fn, mirroring the fallback test's
        vi.hoisted pattern, but resolve instead of reject).
    (b) Mock ./analyticsQueries with the async importOriginal / vi.importActual pattern: spread the real
        module and override ONLY runGraphAnalyticsQueries with a vi.fn that resolves a synthetic
        status:"ready" AnalyticsQueryState (see the <interfaces> shape). Keeping the real
        EMPTY_ANALYTICS_QUERY_STATE, buildFallbackAnalyticsState, and errorToAnalyticsDiagnostic exports is
        REQUIRED because HybridAnalyticsSurface imports them for the idle/fallback states.

    Write one primary test: render(<HybridAnalyticsSurface />); await waitFor(() => the header shows the
    "DuckDB-Wasm" badge text). Then assert the READY branch rendered: at least one mosaic-distribution and
    one mosaic-heatmap test id are present, and there are three mosaic-histogram test ids (Top projects /
    Top roles / Top companies); and assert the fallback diagnostic copy "DuckDB-Wasm unavailable" is NOT
    present (queryByText returns null). Optionally add a second assertion that the mocked
    runGraphAnalyticsQueries was called with an object carrying `connection`, a `users` array, and a
    `folderRows` array (the mapped folder-matrix rows) — this pins the effect's call contract that
    SPLIT-04 must preserve.

    Do NOT weaken the assertions to make them pass; if the ready branch does not render as expected,
    diagnose against the real component source rather than loosening the test. This test must be a true
    characterization of current behavior.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx"</automated>
  </verify>
  <done>The new test file exists and passes; it asserts the "DuckDB-Wasm" ready badge + the mocked Mosaic panel test ids and the absence of the fallback diagnostic. No other file is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Green-baseline gate — confirm both pins green + fallback byte-identical, commit by explicit path</name>
  <files>app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx</files>
  <action>
    Run the targeted suite for BOTH the new main-path test and the existing fallback test and confirm all
    cases pass. Confirm the fallback pin is byte-identical — `git diff --name-only -- "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.fallback.test.tsx"`
    must print nothing. Confirm NO source file changed — `git status --short` must show only the new
    `HybridAnalyticsSurface.mainQuery.test.tsx` as added (plus any pre-existing unrelated branch WIP that
    this plan did not touch); HybridAnalyticsSurface.tsx and its siblings must be unmodified by this plan.

    Stage ONLY the new test file by explicit path (never `git add -A` / `git add .`), then run
    `git diff --cached --name-only` and confirm the index holds exactly that one path — no unrelated WIP,
    no `.planning` deletions, no `/users/spatial-graph` files. Commit:
    `test(17): pin HybridAnalyticsSurface DuckDB-Wasm main query path (SPLIT-03)`.
    This is the SPLIT-03 green-baseline gate that Plan 17-02 depends on.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.mainQuery.test.tsx" "app/(dashboard)/users/access-analysis/HybridAnalyticsSurface.fallback.test.tsx"</automated>
  </verify>
  <done>Both HybridAnalyticsSurface tests pass; the fallback test is byte-identical (git diff --name-only prints nothing); only the new test file is committed (verified via git diff --cached --name-only); no source file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds a Vitest characterization test only. No new inputs, endpoints, auth paths, schema, or runtime code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | HybridAnalyticsSurface ready-branch behavior contract | mitigate | The new test asserts the DuckDB-Wasm ready DOM (badge + Mosaic panels) and the call contract of runGraphAnalyticsQueries, freezing the main-path behavior before SPLIT-04 moves it |
| T-17-01-SC | Tampering | npm installs | accept | No package installs — a single test file added; nothing touches package.json/lockfile |
</threat_model>

<verification>
- `npx vitest run` on HybridAnalyticsSurface.mainQuery.test.tsx — green (ready-branch DOM asserted).
- `npx vitest run` on HybridAnalyticsSurface.fallback.test.tsx — still green.
- `git diff --name-only` on HybridAnalyticsSurface.fallback.test.tsx prints nothing (byte-identical).
- `git status --short` shows only the new test as added; no source file (HybridAnalyticsSurface.tsx or
  siblings) modified by this plan.
- `git diff --cached --name-only` at commit shows exactly the one new test path.
- tsc note: not required as a hard gate for a test-only change, but the test must type-check under Vitest;
  if `npx tsc --noEmit` is run it must remain 0 (no new type errors introduced by the test).
</verification>

<success_criteria>
- HybridAnalyticsSurface.mainQuery.test.tsx exists, pins the DuckDB-Wasm READY branch, and is green.
- HybridAnalyticsSurface.fallback.test.tsx is byte-identical and green.
- No source file changed; only the new test committed by explicit path.
- SPLIT-03 gate met: the main query path is characterized and green BEFORE any split (Plan 17-02).
</success_criteria>

<output>
Create `.planning/phases/17-hybridanalyticssurface-split/17-01-SUMMARY.md` when done.
</output>

## Workshop impact & data truthfulness

- **Workshop impact:** none — this plan adds a test only. The `/users` access-analysis surface renders
  and behaves exactly as before. No user-visible change is the intended and correct outcome.
- **Data truthfulness:** none affected — no data sources, coverage labels, Prisma models, tRPC procedures,
  or env vars are touched. The test mocks the existing DuckDB-Wasm/tRPC seams; it introduces no fixtures
  that would misrepresent live analytics.

## Dashboard Self-Check

- **Context:** Loaded STATE.md (SPLIT-03 "widen the thin HybridAnalyticsSurface pin first" directive +
  explicit-path commit hazard), ROADMAP.md + REQUIREMENTS.md (SPLIT-03 success criteria), and the real
  source: HybridAnalyticsSurface.tsx (1,328 lines), HybridAnalyticsSurface.fallback.test.tsx, and
  analyticsQueries.ts. No CONTEXT.md / RESEARCH.md exist for this phase (intentional; research disabled).
- **Evidence:** The READY-branch discriminators (the "DuckDB-Wasm" badge; DistributionPanel/HeatmapPanel/
  HistogramPanel rendered under `isReady`) and the main-effect call contract
  (`runGraphAnalyticsQueries({ connection, users, folderRows })`) are read directly from the component
  (lines 593-625, 717-719, 875-1076). The mock surface and `mosaic-*` test ids are copied from the
  existing fallback test. AnalyticsQueryState / ChartDatum shapes read from analyticsQueries.ts.
  Vitest confirmed as the runner (`package.json` test script: `vitest run`, v4).
- **Constraints applied:** Behavior-preserving (no source touched); byte-identical fallback test (no
  edits); co-located test path so `vi.mock("./duckdbClient" | "./analyticsQueries" | "../useMergedAccUsers")`
  resolves to the same modules the component imports; no new WebGL (test mocks the Mosaic panels); zinc
  theme untouched; `/users/spatial-graph` not touched; explicit-path commit.
- **Gates:** `npx vitest run` on both HybridAnalyticsSurface tests; byte-identical git-diff on the fallback
  test; `git status --short` / `git diff --cached --name-only` scope proof. tsc not a hard gate for a
  test-only change (noted).
- **VERIFY:** The exact synthetic `ready`-state fixture and whether a second RED-then-GREEN assertion on
  the `runGraphAnalyticsQueries` call args is included are finalized at execution against the live
  component render — they cannot be byte-pinned at plan time. All paths, symbols, seams, and test ids are
  grounded in files read this session.
</content>
</invoke>
