---
phase: 21.1-overview-tab-uat-follow-ups
plan: 04
type: execute
wave: 2
depends_on: ["21.1-01", "21.1-02", "21.1-03"]
files_modified:
  - app/(dashboard)/access-analysis/mainCharts.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: false
requirements: [UAT-21.1-01, UAT-21.1-02, UAT-21.1-03]

must_haves:
  truths:
    - "The Overview tab shows a side-by-side 2-up row — Activity share by project (left) + Provisioned modules (right) — between 'Activity by module' and 'Ingest freshness'"
    - "Both new panels respond to the global project picker (picker-only filtering, no sliceFilters coupling)"
    - "The Activity-by-module ⓘ caveat states the live service-attributed vs verb-inferred split — the stale '~40.7% disagreement' copy is gone"
    - "The owner has reviewed all 3 items live on a :3100 production-build preflight with the before/after attribution note, :3000 untouched"
  artifacts:
    - path: "app/(dashboard)/access-analysis/mainCharts.tsx"
      provides: "loadProvisionedModules in the eager Promise.all (fan-out 9 → 10)"
      contains: "loadProvisionedModules"
    - path: "app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx"
      provides: "2-up row mounting ProjectActivityDonut + ProvisionedModulesChart; live-split caveat copy"
      contains: "ProvisionedModulesChart"
  key_links:
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "app/(dashboard)/access-analysis/provisionedModulesCounts.ts"
      via: "useMemo(summarizeProvisionedModules(filterRowsBySelection(provisionedModuleRows, selected)))"
      pattern: "summarizeProvisionedModules\\(filterRowsBySelection"
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "app/(dashboard)/access-analysis/projectActivityCounts.ts"
      via: "useMemo(summarizeProjectActivity(filterRowsBySelection(moduleRows, selected)))"
      pattern: "summarizeProjectActivity\\(filterRowsBySelection"
    - from: "app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx"
      to: "ModuleSummary.attribution"
      via: "live percentage rendering in the TRUTH-03 caveat tooltip"
      pattern: "attribution"
---

<objective>
Wire all three Phase 21.1 items into the live Overview tab and close the phase at an owner
checkpoint: mount the two new panels as the locked 2-up row, update the Activity-by-module ⓘ
caveat to the live attribution split, run the full gate sweep, and present everything (plus the
21.1-ATTRIBUTION-DELTA.md evidence) on a `:3100` production-build preflight.

Purpose: turns the three unmounted/unwired stacks from plans 01-03 into the owner-visible
Overview upgrade: trustworthy module attribution, provisioned-vs-used module contrast, and
project share of activity — all in one Overview pass.

Output: user-visible Overview tab changes on `/access-analysis`, green full gate sweep, owner
checkpoint verdict.

**Workshop impact:** `/access-analysis` Overview tab order becomes: Activity over time →
Activity by module (fixed attribution + honest live-split caveat) → [Activity share by project
| Provisioned modules] 2-up row → Ingest freshness. No other tab or workshop page changes;
`/users/spatial-graph` untouched.

**Data truth:** provisioned panel reads live-membership grants (AccProjectMember, full live
coverage); project donut inherits the Overview's activity coverage conventions and states the
Account-level exclusion; caveat split is computed live from the filtered summary, never
hardcoded.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-CONTEXT.md
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-01-SUMMARY.md
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-02-SUMMARY.md
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-03-SUMMARY.md
@app/(dashboard)/access-analysis/mainCharts.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx

<interfaces>
From plans 01-03 (verify exact names against the wave-1 SUMMARYs before wiring):
```typescript
// lib/server/provisionedModulesView.ts (21.1-02)
export interface ProvisionedModuleRow { projectId: string; projectName: string; moduleId: string; count: number; }
export function loadProvisionedModules(force?: boolean): Promise<ProvisionedModuleRow[]>;
// app/(dashboard)/access-analysis/provisionedModulesCounts.ts (21.1-02)
export function summarizeProvisionedModules(rows: ReadonlyArray<ProvisionedModuleRow>): ProvisionedModuleSummary;
// app/(dashboard)/access-analysis/projectActivityCounts.ts (21.1-03)
export function summarizeProjectActivity(rows: ReadonlyArray<ModuleActivityRow>, topN?: number): ProjectActivitySummary;
// app/(dashboard)/access-analysis/moduleCounts.ts (21.1-01)
export interface ModuleSummary { /* ...existing... */ attribution: { serviceCount: number; verbCount: number }; }
```

Existing wiring pattern to replicate (AccessAnalysisCharts.tsx, verified current):
```typescript
const moduleSummary = useMemo(
  () => summarizeModules(filterRowsBySelection(moduleRows, selected)),
  [moduleRows, selected],
);
```

Overview loaders are RSC-EAGER (research-confirmed): Overview is the default-visible tab, so
`loadProvisionedModules()` joins `mainCharts.tsx`'s `Promise.all` (9 → 10 entries) — this is
the documented "cause" exception to the fan-out-review guardrail; item 3 adds NO loader, so
the fan-out grows by exactly one. Do NOT add a lazy per-tab branch for it.

Preflight mechanism (verified: next.config.ts:30 honors NEXT_DIST_DIR): same as 21-04 —
`NEXT_DIST_DIR=.next-uat-21-1 npx next build --webpack`, then
`NEXT_DIST_DIR=.next-uat-21-1 npx next start -p 3100`. `:3000` (Task Scheduler service serving
`.next`) is NEVER touched; NEVER build into `.next` while `:3000` serves it; NEVER run
webpack/turbopack dev servers (both broken on this machine — deferred-items.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire loader, memos, 2-up row, and live-split caveat</name>
  <files>app/(dashboard)/access-analysis/mainCharts.tsx, app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx</files>
  <action>
1. `mainCharts.tsx`: import `loadProvisionedModules` from `@/lib/server/provisionedModulesView`;
   add it as the 10th `Promise.all` entry (destructure `provisionedModuleRows`); pass
   `provisionedModuleRows` to `<AccessAnalysisCharts …>`; update the fan-out doc comment
   (9 → 10, with the "Overview is never lazy-gated" rationale + note item 3 adds no loader).

2. `AccessAnalysisCharts.tsx`:
   - New optional prop `provisionedModuleRows?: ProvisionedModuleRow[]` (typed import from
     `@/lib/server/provisionedModulesView`), doc-commented like the sibling props.
   - Add `...(provisionedModuleRows ?? [])` to the `projectOptions([...])` union so
     live-membership-only projects (AccProjectMember covers all 1,153 live projects; roleRows
     is DC-scoped) become selectable and are default-selected — otherwise
     `filterRowsBySelection` would silently drop their grants (comment this rationale; it
     extends the existing "union of sources so neither donut hides a project" doc block).
   - Two picker-only memos, directly below the `moduleSummary` memo (locked: `selected`, never
     `sliceFilteredProjectIds`, no sliceFilters coupling — same rationale comment as
     moduleSummary's):
     `provisionedModuleSummary = useMemo(() => summarizeProvisionedModules(filterRowsBySelection(provisionedModuleRows ?? [], selected)), [provisionedModuleRows, selected])`
     `projectActivitySummary = useMemo(() => summarizeProjectActivity(filterRowsBySelection(moduleRows, selected)), [moduleRows, selected])`
   - Pass both summaries + a `hasProvisionedRows = provisionedModuleRows !== undefined` style
     presence gate down to `<OverviewTabPanel …>`.

3. `OverviewTabPanel.tsx`:
   - New optional props `projectActivitySummary?: ProjectActivitySummary` and
     `provisionedModuleSummary?: ProvisionedModuleSummary`.
   - Mount the locked 2-up row BETWEEN the "Activity by module" grid and the Ingest-freshness
     panel: one `grid grid-cols-1 gap-6 lg:grid-cols-2` wrapper with two `<Reveal><PremiumSurface
     variant="base" className="flex flex-col gap-3 p-5 overflow-hidden">` panels (identical
     shell to the existing panels — no card-inside-card):
     LEFT: `SectionHeader title="Activity share by project"` (subtitle stating top-10 + Other
     and the Account-level exclusion in one line; `ActivityCoverageBadge` with the same
     covCovered/covTotal props the module donut uses) + `<ProjectActivityDonut summary={…}/>`.
     RIGHT: `SectionHeader title="Provisioned modules"` (subtitle: member×project module access
     grants for the selected projects — "what's rolled out" vs the activity donut's "what's
     used") + `<ProvisionedModulesChart summary={…}/>`.
     Gate each panel on its summary prop's presence (mirror the `timelineRows ?` convention);
     both absent → row absent.
   - Rewrite the TRUTH-03 ⓘ caveat copy (the `~40.7%` TooltipContent): compute from
     `moduleSummary.attribution` — e.g. with `const { serviceCount, verbCount } = moduleSummary.attribution`
     and `pct = (n) => total > 0 ? ((n / (serviceCount + verbCount)) * 100).toFixed(1) : "0"`:
     "Module attribution is service-first: {pct(serviceCount)}% of activity in view carries
     Autodesk's service tag; the remaining {pct(verbCount)}% is classified from each
     activity's rawAction verb taxonomy." Keep the `data-testid="module-caveat"` testid and
     the hover/focus-only tooltip mechanics unchanged; guard division by zero (empty selection
     → keep a neutral sentence, never NaN%). Live figures only — no hardcoded percentages
     anywhere (CONTEXT.md locked).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Fan-out is 10; both memos are picker-only; 2-up row renders between module donut and ingest freshness; caveat renders live percentages; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Shell test extension + full gate sweep</name>
  <files>app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
1. Extend `__tests__/AccessAnalysisCharts.test.tsx` (inspect its existing conventions first —
   Radix Tabs activate on `fireEvent.mouseDown`, NOT click, and NOT userEvent, per 20.1-05):
   ADD cases (never modify existing ones): (a) with `provisionedModuleRows` + `moduleRows`
   props provided, the Overview tab renders both new section titles ("Activity share by
   project", "Provisioned modules"); (b) with `provisionedModuleRows` omitted, the
   "Provisioned modules" title is absent (presence gating pinned); (c) a project present ONLY
   in `provisionedModuleRows` appears in the picker options (the options-union pin).

2. Full gate sweep, in order:
   - `npx tsc --noEmit` → 0 errors.
   - `npm test` → full suite green; baseline 2421 passed / 1 skipped — new totals must be
     ≥ baseline passed with 0 failed, 0 regressions (report exact numbers in the SUMMARY).
   - Scope check: `git status --short` + `git diff --name-only <phase-start>..HEAD` — changed
     files across the whole phase are exactly the union of the four plans' `files_modified`
     (plus SUMMARYs/planning docs); NOTHING under `/users/spatial-graph`, no new npm deps
     (package.json untouched), no new WebGL imports.
   - Commit by explicit path only; `git diff --cached --name-only` proof before the commit.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Shell test additions green; full suite ≥ 2421 passed / 0 failed; scope diff exact; commits explicit-path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner checkpoint on :3100 production-build preflight</name>
  <action>
Automate everything first, THEN pause for the owner. Preflight build (`:3000` untouched
throughout — verify the Task Scheduler service PID before and after; NEVER a dev server,
NEVER the live `.next` dist — both dev servers are broken on this machine and building into
`.next` while `:3000` serves it is forbidden):
- `$env:NEXT_DIST_DIR=".next-uat-21-1"; npx next build --webpack`
- `$env:NEXT_DIST_DIR=".next-uat-21-1"; npx next start -p 3100`
- Confirm `http://localhost:3100/access-analysis` returns 200 before presenting.
Then present the verification steps below to the owner (with `21.1-ATTRIBUTION-DELTA.md` open)
and pause.
  </action>
  <verify>`http://localhost:3100/access-analysis` returns 200 on the isolated `.next-uat-21-1` production build; `:3000` PID unchanged.</verify>
  <done>Owner verdict recorded verbatim; `:3100` server stopped and port released; `:3000` confirmed untouched.</done>
  <what-built>All 3 Phase 21.1 Overview items live: (1) Provisioned-modules horizontal bars with project drill, (2) service-first Activity-by-module attribution with live-split ⓘ caveat + quantified before/after note, (3) Activity share by project donut (top-10 + Other, module-breakdown drill, Account-level excluded).</what-built>
  <how-to-verify>
1. Present to the owner on `:3100`, Overview tab:
   - Panel order: Activity over time → Activity by module → [Activity share by project |
     Provisioned modules] → Ingest freshness; zinc theme, no layout overflow at laptop +
     projector widths.
   - Item 2: hover the ⓘ on "Activity by module" — live service/verb split percentages (no
     "~40.7%" copy). Walk the owner through `21.1-ATTRIBUTION-DELTA.md`: which modules moved,
     by how much, and why the honest magnitude is small (the ~966 Model-Coordination figure
     was already fixed 2026-06-05).
   - Item 1: Provisioned modules bars react to the project picker; click a bar → top-projects
     drill list; zero-grant modules listed in the muted footer; narrow the picker to a
     selection with no grants → honest empty state.
   - Item 3: donut shows top-10 + "Other (N projects)"; caption states the live Account-level
     exclusion figure; click a project slice → its module breakdown below the legend (no
     scroll jump); click again → closes; Other slice not drillable; zero-activity selection →
     honest empty state.
   - Cross-check: picker narrowing updates BOTH new panels + the module donut together;
     sliceFilters (role/company chips) do NOT affect the two new panels (picker-only, locked).
2. After the verdict: stop the `:3100` server (confirm port released); confirm `:3000` service
   still running untouched.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (each becomes a gap-closure item)</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 errors; `npm test` full suite ≥ 2421 passed / 1 skipped / 0 failed.
- Overview 2-up row present at the locked position; caveat shows live split; fan-out exactly 10.
- Owner checkpoint verdict recorded verbatim in the SUMMARY with the preflight evidence (dist name, ports, PIDs).
- `/users/spatial-graph` untouched; no new deps; no new WebGL; explicit-path commits with `git diff --cached --name-only` proof.
</verification>

<success_criteria>
- All 3 owner UAT items are user-visible on the Overview tab and owner-reviewed live.
- Attribution fix backed by `21.1-ATTRIBUTION-DELTA.md` live evidence presented at the checkpoint.
- Full gate sweep green; scope diff exactly matches the phase's planned files.
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-04-SUMMARY.md`
</output>
