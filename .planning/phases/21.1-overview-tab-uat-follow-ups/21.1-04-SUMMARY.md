---
phase: 21.1-overview-tab-uat-follow-ups
plan: 04
subsystem: ui
tags: [echarts, vitest, access-analysis, overview-tab, attribution, owner-checkpoint]

# Dependency graph
requires:
  - phase: 21.1-overview-tab-uat-follow-ups (plan 01)
    provides: classifyActivity(rawAction, service) + ModuleSummary.attribution live split
  - phase: 21.1-overview-tab-uat-follow-ups (plan 02)
    provides: loadProvisionedModules + summarizeProvisionedModules + ProvisionedModulesChart (unmounted)
  - phase: 21.1-overview-tab-uat-follow-ups (plan 03)
    provides: summarizeProjectActivity + ProjectActivityDonut (unmounted)
provides:
  - All 3 Phase 21.1 UAT items live on the /access-analysis Overview tab (2-up row + live-split caveat)
  - Owner-approved module-attribution taxonomy (5 gap-closure corrections in lib/acc/activityClassification.ts)
affects: [phase 22 planning (Overview tab is now the post-21.1 baseline)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "MODULE_OVERRIDES map in activityClassification.ts: classifier-layer module reassignment for catalog actions, checked before the Preconstruction->AdminActions routing, without touching the generated excel catalog"
    - "Owner-delegated taxonomy review: checkpoint verdict delegates a mapping audit to the orchestrator; corrections land as classifier-layer overrides with live-DB row-volume evidence in the phase's ATTRIBUTION-DELTA doc"

key-files:
  created: []
  modified:
    - app/(dashboard)/access-analysis/mainCharts.tsx
    - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
    - app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx
    - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
    - app/(dashboard)/access-analysis/page.test.tsx
    - app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts
    - lib/acc/activityClassification.ts
    - lib/acc/activityClassification.test.ts
    - .planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md

key-decisions:
  - "Sheets cluster (sheet verbs + version-set family + sheets service rescue) routes to Build — OWNER-DIRECTED at the final look ('approved BUT move sheets and friends to the Build module'); Sheets is an ACC Build tool, the cluster follows the tool"
  - "publish-entity stays Design Collaboration (docs-tagged Revit model publish, not a Sheets action) — Design Collaboration honestly shrinks to a ~53-row sliver and is NOT padded"
  - "notify-final-members -> Data Management (Docs review-workflow sibling family), add-folder-naming-standard -> Datum (naming-standard family) — classifier-layer MODULE_OVERRIDES/EXTRA_ACTIONS, generated catalog untouched"
  - "Both new Overview panels are picker-only (selected, never sliceFilteredProjectIds) — locked in the plan, pinned by shell tests"

patterns-established:
  - "MODULE_OVERRIDES: owner-corrected module destinations live at the classifier layer keyed by canonical action id, never by editing accTaxonomyActions.generated.ts"

requirements-completed: [UAT-21.1-01, UAT-21.1-02, UAT-21.1-03]

# Metrics
duration: "~40min (tasks 1-2) + checkpoint waits + ~50min gap-closure across 2 continuation sessions"
completed: 2026-07-06
status: complete
---

# Phase 21.1 Plan 04: Overview Wiring + Owner Checkpoint Summary

**All 3 Phase 21.1 UAT items wired live into the /access-analysis Overview tab (2-up row: Activity share by project + Provisioned modules; live service/verb-split ⓘ caveat), owner-approved on a `:3100` production preflight after a 5-correction owner-delegated module-attribution taxonomy review that finished with the owner-directed Sheets-cluster → Build move.**

## Performance

- **Duration:** ~40min (Tasks 1-2) + 2 checkpoint waits + ~50min gap-closure (2 continuation sessions)
- **Completed:** 2026-07-06
- **Tasks:** 3/3 (2 auto + 1 checkpoint with 2 owner rounds + 5 gap-closure corrections)
- **Files modified:** 9 across 4 commits

## Commits

| Commit | Scope |
|---|---|
| `944e4d18` | Task 1: wiring — `mainCharts.tsx` fan-out 9→10 (`loadProvisionedModules` eager), `AccessAnalysisCharts.tsx` picker-only memos + options union, `OverviewTabPanel.tsx` 2-up row + live-split caveat |
| `d699a299` | Task 2: shell test additions (2-up row mount, presence gating, options-union pin) + full gate sweep |
| `1dca75d3` | Gap-closure: corrections 1-4 (owner-delegated mapping review) + test pins + regenerated delta evidence |
| `55c4b78d` | Gap-closure: correction 5 (owner-directed Sheets cluster → Build) + test pins + regenerated delta evidence |

## Checkpoint History (Task 3, verbatim verdicts)

1. **First owner review** (live `:3100`, `.next-uat-21-1` webpack production build): owner
   questioned `publish-entity`/`create-version-set` module attribution and **delegated** the
   mapping review — *"I'll hand that to you — review the types of activities and the modules
   assigned, use your best effort."* Recorded as: **"Approved with delegated mapping review —
   4 taxonomy corrections applied per owner delegation."**
2. **Final look** (rebuilt `:3100` with corrections 1-4): **"approved BUT move sheets and
   friends to the Build module"** — correction 5, owner-directed, applied; `:3100` rebuilt
   again with all 5 corrections for the owner's post-close glance.

## What Was Built

### Task 1: Wiring (commit `944e4d18`)

- `mainCharts.tsx`: `loadProvisionedModules` joined the eager `Promise.all` (fan-out 9→10,
  documented cause-exception: Overview is the default-visible tab, never lazy-gated; item 3
  adds no loader).
- `AccessAnalysisCharts.tsx`: optional `provisionedModuleRows` prop; `projectOptions` union
  extended with provisioned rows (AccProjectMember covers all 1,153 live projects; roleRows is
  DC-scoped — without the union, live-membership-only projects would be silently dropped by
  `filterRowsBySelection`); two picker-only memos (`provisionedModuleSummary`,
  `projectActivitySummary`) keyed on `selected`, never `sliceFilteredProjectIds`.
- `OverviewTabPanel.tsx`: locked 2-up row (`lg:grid-cols-2`) between "Activity by module" and
  Ingest freshness — left `ProjectActivityDonut`, right `ProvisionedModulesChart`, each gated
  on its summary prop; TRUTH-03 ⓘ caveat rewritten to compute live percentages from
  `moduleSummary.attribution` (division-by-zero guarded; the stale "~40.7%" copy is gone).

### Task 2: Shell tests + gate sweep (commit `d699a299`)

Added shell test cases (Radix `fireEvent.mouseDown` convention): 2-up row titles render with
both props; "Provisioned modules" absent when its prop is omitted; a provisioned-only project
appears in the picker options. Full sweep at that point: tsc clean, 2470 passed / 1 skipped /
0 failed.

### Task 3 gap-closure: 5 module-attribution corrections (commits `1dca75d3`, `55c4b78d`)

All in `lib/acc/activityClassification.ts` via its extension points (`SERVICE_TO_MODULE`,
new `MODULE_OVERRIDES` map, `EXTRA_ACTIONS`) — the generated excel catalog was never edited.
Live row volumes verified against the same per-`(project, action, service)` aggregate
`scripts/diag-attribution-delta.cjs` uses (4,722,412 total activities):

| # | Correction | Live rows |
|---|---|--:|
| 1 | `sheets` service rescue-target follows the sheets cluster (`view-sheet-public-link` 66, `create-public-link-for-sheets` 59) | 125 |
| 2 | Version-set family unified with the Sheets cluster (`create-version-set` 16, `add-version-to-set` 13, `rename/update-version-set` 0) | 29 |
| 3 | `notify-final-members`: Admin Actions → Data Management (review-workflow sibling of `notify-reviewers`/`submit-review`/`claim-review-task`) | 48 |
| 4 | `add-folder-naming-standard`: Unmapped/docs-rescue → Datum (naming-standard family) | 19 |
| 5 | **OWNER-DIRECTED** Sheets cluster → Build: sheet verbs (`view-sheet` 19,362, `publish-sheet` 2,358, `export-sheet` 1,815, `delete-sheet` 535, `shared-with-recipients-for-sheets` 133, `print-sheet` 29) + corrections 1-2's destinations superseded to Build | 24,386 total (0.52%) |

Deliberately NOT moved: `publish-entity` (51 rows — docs-tagged Revit model publish, stays
Design Collaboration, owner-confirmed), `send-entity-to-project` (catalog Design
Collaboration), `restore-version`/`create-set` (Docs file-versioning/Sets → Data Management).
`sheets` stays in `UMBRELLA_SERVICES` (never overrides a mapped verb; only rescues Unmapped,
now into Build).

Test updates: `activityClassification.test.ts` gained an "owner-delegated mapping review"
describe block pinning every correction (including the `publish-entity`-stays pin so the
Sheets move can never drag it); `moduleCounts.test.ts`'s 2 stale fixtures updated
(`notify-final-members`, `add-version-to-set`). Repo-wide grep confirmed no other fixtures pin
the affected actions (`accTaxonomyActions.smoke.test.ts` pins the raw generated catalog, which
is intentionally unchanged; `activityCategories.test.ts` is the unrelated icon-category system).

## Verification (final state, all 5 corrections)

- `npx tsc --noEmit` → 0 errors.
- `npm test` full suite → **2477 passed / 1 skipped / 0 failed** (baseline 2470 from Task 2;
  +7 new pins, zero regressions; run again post-correction-5, same totals).
- `21.1-ATTRIBUTION-DELTA.md` regenerated from the live DB after each correction round; final
  per-module AFTER totals: Build 320,793 / Data Management 4,378,510 / Admin Actions 23,024 /
  Design Collaboration 53 / Datum 32 / Unmapped 0. Service-attributed 4,721,564 (99.98%) /
  verb-inferred 848 (0.0180%).
- Preflight: `NEXT_DIST_DIR=.next-uat-21-1 npx next build --webpack` (exit 0) →
  `next start -p 3100`, `curl http://localhost:3100/access-analysis` → 307 auth redirect.
  `:3000` Task Scheduler PID `56060` verified unchanged before/after every :3100
  stop/rebuild/start cycle. No dev server ever used; the live `.next` dist never touched.
- Scope: explicit-path staging only, `git diff --cached --name-only` verified before all 4
  commits; `/users/spatial-graph` untouched; no new npm deps; no new WebGL.

## Deviations from Plan

### Owner-driven scope additions (checkpoint gap-closure, not plan deviations)

The plan's Task 3 anticipated gap-closure items from the checkpoint. Two rounds occurred:
corrections 1-4 (owner-delegated audit) and correction 5 (owner-directed Sheets → Build).
Correction 5 supersedes the Design Collaboration destination corrections 1-2 originally
chose — the final taxonomy is the owner's.

No other deviations — Tasks 1-2 executed as written.

## Workshop Impact

`/access-analysis` Overview tab is now: Activity over time → Activity by module (owner-approved
attribution + live service/verb-split ⓘ caveat) → [Activity share by project | Provisioned
modules] 2-up row → Ingest freshness. The Activity-by-module donut's Build slice now includes
all Sheets-tool activity (~24.4k rows, 0.52%); Design Collaboration honestly shows only
Revit-publish events (~53 rows). Both new panels respond to the global project picker only.
No other tab or workshop page changed; `/users/spatial-graph` untouched.

## Data Truthfulness

- The ⓘ caveat computes its service/verb split live from `moduleSummary.attribution` for the
  rows in view — never hardcoded, division-by-zero guarded.
- **Design Collaboration sliver consequence:** after the owner-directed Sheets → Build move,
  Design Collaboration legitimately holds only `publish-entity` + `send-entity-to-project`
  (~53 rows account-wide). This is honest and expected — the module's real activity in this
  account is Revit-publish events; the donut must not pad it, and the zero/low-volume module
  treatment (muted footer listing) already handles it.
- **Model Coordination suppression (owner asked about attribution correctness):** Model
  Coordination remains deliberately excluded from the donut (locked 2026-06-05 decision,
  commit `6164cfae` — `issue-*` verbs → Build, catalog modelCoordination actions → Data
  Management). The owner's checkpoint question was answered with the live
  `21.1-ATTRIBUTION-DELTA.md` evidence: the stale "~966 Model Coordination" figure predates
  that fix and is superseded; the real service-vs-verb movement is 188 rows (0.0040%).
- Provisioned-modules panel reads live `AccProjectMember.products` (full 1,153-project live
  coverage, 0 null/empty rows — no coverage caption needed). Activity-share donut states its
  Account-level exclusion figure live.

## Known Stubs

None. All three UAT items are fully wired and live; no placeholder data paths remain.

## Dashboard Self-Check

- **Context:** `.planning/STATE.md`, `21.1-04-PLAN.md`, `21.1-CONTEXT.md`, plans 01-03
  SUMMARYs, `activityClassification.ts` + both test files, `accTaxonomyActions.generated.ts`,
  `accTaxonomyStatic.ts`, `moduleCounts.ts`, `diag-attribution-delta.cjs` all read before edits.
- **Evidence:** module ids verified in `accTaxonomyStatic.ts` (`designCollaboration`, `datum`,
  `build`); action catalog entries verified in `accTaxonomyActions.generated.ts`; all row
  volumes from live-DB queries (scratch scripts, deleted after use); attribution split from
  `scripts/diag-attribution-delta.cjs` output.
- **Constraints:** zinc theme untouched; ECharts resolved theme colors; no new WebGL; no new
  deps; `/users/spatial-graph` untouched; components never touch Prisma (classifier is pure
  `lib/acc`); explicit-path commits with staged-diff proof.
- **Gates:** `npx tsc --noEmit` (0 errors, re-run after every correction round), full
  `npm test` (2477/1/0, re-run after correction 5), production webpack build to isolated
  `.next-uat-21-1` (exit 0), live `:3100` 307 probe, `:3000` PID stability check.
- **VERIFY:** none remaining.

## Self-Check: PASSED

- FOUND: `app/(dashboard)/access-analysis/mainCharts.tsx` (loadProvisionedModules wired)
- FOUND: `app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx` (2-up row + live caveat)
- FOUND: `lib/acc/activityClassification.ts` (MODULE_OVERRIDES + all 5 corrections)
- FOUND: `.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md` (5-correction review section)
- FOUND: commit `944e4d18` (Task 1)
- FOUND: commit `d699a299` (Task 2)
- FOUND: commit `1dca75d3` (corrections 1-4)
- FOUND: commit `55c4b78d` (correction 5)
