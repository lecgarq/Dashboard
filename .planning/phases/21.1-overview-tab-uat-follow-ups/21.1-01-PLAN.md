---
phase: 21.1-overview-tab-uat-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/acc/activityClassification.ts
  - lib/acc/activityClassification.test.ts
  - lib/acc/moduleCountsTypes.ts
  - lib/server/moduleActivityView.ts
  - app/(dashboard)/access-analysis/moduleCounts.ts
  - app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts
  - scripts/diag-attribution-delta.cjs
  - .planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md
autonomous: true
requirements: [UAT-21.1-02]

must_haves:
  truths:
    - "Activity rows carrying an Autodesk service tag are attributed service-first; rows without one keep the existing verb-taxonomy classification"
    - "A previously-Unmapped rawAction with a recognized service value now lands in a real module instead of Unmapped"
    - "summarizeModules exposes live service-attributed vs verb-inferred activity volumes so the UI caveat can state the split honestly"
    - "A quantified before/after module-movement note exists for the owner checkpoint, computed from live DB data (not the stale docs/activity-module-audit.md figures)"
  artifacts:
    - path: "lib/acc/activityClassification.ts"
      provides: "classifyActivity(rawAction, service?) with SERVICE_TO_MODULE narrow override + attributedBy field"
      contains: "SERVICE_TO_MODULE"
    - path: "lib/server/moduleActivityView.ts"
      provides: "SQL union preserving serviceGroup/service as a grouping dimension"
      contains: "serviceGroup"
    - path: "app/(dashboard)/access-analysis/moduleCounts.ts"
      provides: "summarizeModules passing row.service through + attribution counters on ModuleSummary"
      contains: "attribution"
    - path: ".planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md"
      provides: "Live before/after attribution-movement evidence for the owner"
  key_links:
    - from: "app/(dashboard)/access-analysis/moduleCounts.ts"
      to: "lib/acc/activityClassification.ts"
      via: "classifyActivity(row.rawAction, row.service) through the moduleOverrides barrel"
      pattern: "classifyActivity\\(row\\.rawAction, row\\.service\\)"
    - from: "lib/server/moduleActivityView.ts"
      to: "AccActivityAccds.serviceGroup / AccActivity.service"
      via: "raw SQL union SELECT + GROUP BY"
      pattern: "serviceGroup"
---

<objective>
Fix the owner-flagged "Activity by module" data bug (Phase 21.1 item 2, UAT-21.1-02): make
module attribution service-first where Autodesk's service tag is populated, keeping the verb
taxonomy as the fallback, and produce the live quantified before/after evidence the owner
checkpoint requires.

Purpose: the owner flagged the Overview donut's numbers as wrong. Research (21.1-RESEARCH.md,
live DB measurements 2026-07-06) shows the real fixable disagreement is small (~1-2% of
service-tagged rows, concentrated in Design Collaboration / Data Management / Admin Actions
boundary calls — NOT Model Coordination, already fixed by commit 6164cfae). The fix must be
honest about that: real movement, small magnitude, live figures.

Output: service-aware classifier + service-preserving loader SQL + attribution counters on
`ModuleSummary` (consumed by plan 21.1-04's caveat copy), plus `21.1-ATTRIBUTION-DELTA.md`
with live before/after module counts.

**Workshop impact:** `/access-analysis` Overview tab "Activity by module" donut numbers change
(that is the point). No visual/layout change in this plan — caveat copy update lands in 21.1-04.

**Data truth:** `AccActivityAccds.serviceGroup` is 100% populated (4 values: docs/issues/sheets/
admin); `AccActivity.service` is ~42.1% populated (7 values: docs/sheets/bridge/issues/
submittals/rfis/admin). Neither can resolve all 10 donut modules — 6 modules have zero service
representation — so a wholesale service replacement is wrong (research Pitfall 2). The locked
CONTEXT decision "service wins where populated, verb fallback" is implemented as a NARROW
override per 21.1-RESEARCH.md (research corrections override CONTEXT per its Claude's-Discretion
clause): decisive services override, umbrella services defer to intentional verb refinements.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-CONTEXT.md
@.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-RESEARCH.md
@lib/acc/activityClassification.ts
@lib/server/moduleActivityView.ts
@app/(dashboard)/access-analysis/moduleCounts.ts
@lib/acc/moduleCountsTypes.ts

<interfaces>
Current production classification chain (the ONLY production call path, grep-verified in research):
```
lib/acc/activityClassification.ts  → classifyActivity(rawAction: string): { moduleId, label, category }
  └─ re-exported verbatim by app/(dashboard)/access-analysis/moduleOverrides.ts (4-line barrel — DO NOT add logic there, Phase 10 BND-02)
       └─ app/(dashboard)/access-analysis/moduleCounts.ts summarizeModules() (sole production caller)
```

From lib/acc/moduleCountsTypes.ts (current):
```typescript
export interface ModuleActivityRow {
  projectId: string;
  projectName: string;
  rawAction: string;
  count: number;
}
```

From app/(dashboard)/access-analysis/moduleCounts.ts (current ModuleSummary):
```typescript
export interface ModuleSummary {
  slices: ModuleSlice[];
  total: number;
  activeModules: number;
  zeroModules: Array<{ id: string; name: string }>;
  typesByModule: Map<string, ActivityType[]>;
}
```

Module id constants already in lib/acc/activityClassification.ts:
`ADMIN_ACTIONS_ID = "adminActions"`, `DATA_MANAGEMENT_ID = "dataManagement"`, `BUILD_ID = "build"`,
`UNMAPPED_MODULE = "unmapped"` (exported).

Prisma schema (verified): `AccActivity.service String?` (line 569), `AccActivityAccds.serviceGroup String?` (line 591).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service-first precedence in classifyActivity + characterization tests</name>
  <files>lib/acc/activityClassification.ts, lib/acc/activityClassification.test.ts</files>
  <action>
Extend `classifyActivity` to `classifyActivity(rawAction: string, service?: string | null)`,
returning `{ moduleId, label, category, attributedBy: "service" | "verb" }`. The added
`attributedBy` field is ADDITIVE — existing callers/tests that destructure only the original
3 fields keep compiling and passing unchanged, and the `service`-omitted path must behave
byte-identically to today (all existing tests pass unmodified).

Add a `SERVICE_TO_MODULE` table (values verified live in 21.1-RESEARCH.md — do not invent
service strings):

```typescript
const SERVICE_TO_MODULE: Readonly<Record<string, string>> = {
  issues: BUILD_ID, submittals: BUILD_ID, rfis: BUILD_ID,   // decisive → Build
  admin: ADMIN_ACTIONS_ID,                                   // decisive → Admin Actions
  docs: DATA_MANAGEMENT_ID, sheets: DATA_MANAGEMENT_ID, bridge: DATA_MANAGEMENT_ID, // umbrella
};
const UMBRELLA_SERVICES: ReadonlySet<string> = new Set(["docs", "sheets", "bridge"]);
```

Precedence rules (implement exactly; comment each in code):
1. Compute the verb-based result exactly as the current function does (zero changes to the
   existing verb logic — do not touch COORDINATION_ISSUE_LABELS, EXTRA_ACTIONS,
   ADMIN_ACTION_IDS, the modelCoordination→dataManagement redirect, etc.).
2. Normalize `service` (trim + lowercase). If absent/empty/unrecognized in SERVICE_TO_MODULE →
   return verb result with `attributedBy: "verb"`.
3. If the verb result is `UNMAPPED_MODULE` and service resolves → return the service module,
   keeping `label: rawAction` and `category: CATEGORY_LABELS.unknown`, `attributedBy: "service"`
   (rescues the ~186 accds Unmapped rows research measured).
4. If service is DECISIVE (issues/submittals/rfis/admin — not in UMBRELLA_SERVICES) and the
   verb module differs → return the service module, keeping the verb result's label/category,
   `attributedBy: "service"`.
5. Otherwise (umbrella docs/sheets/bridge with a mapped verb result, or service and verb
   agree) → keep the verb module/label/category, `attributedBy: "service"` (service was
   populated and recognized — it informed/validated attribution; the more granular verb
   refinement of an umbrella service is intentional, per research: ~8,107 docs→Admin-Actions
   rows are a deliberate permission-verb override, not a bug — do NOT flatten
   designCollaboration/datum/adminActions verb results back to dataManagement).

WHY narrow, not literal "service always wins": serviceGroup/service have only 4-7 coarse
values vs 10 modules; a literal override would destroy the intentional Design Collaboration /
Datum / Admin Actions splits (21.1-RESEARCH.md Pitfall 2, which explicitly overrides a literal
reading of the CONTEXT.md decision).

In `lib/acc/activityClassification.test.ts`, ADD (never modify existing cases) a
`describe("service-first attribution")` characterization block pinning at minimum:
- omitted/undefined/null/empty/unrecognized service → identical result to today's verb path,
  `attributedBy: "verb"`;
- decisive override: a rawAction that verb-classifies to a non-build module + `service:
  "submittals"` → `moduleId: "build"`, `attributedBy: "service"`; same for `"admin"` →
  `adminActions`;
- umbrella deference: a permission verb that verb-classifies to `adminActions` + `service:
  "docs"` → stays `adminActions` (NOT dataManagement), `attributedBy: "service"`;
- unmapped rescue: a nonsense rawAction + `service: "docs"` → `dataManagement` with
  `label` = the raw string, `attributedBy: "service"`; nonsense rawAction + no service →
  stays `unmapped`;
- case/whitespace normalization: `service: " Docs "` behaves as `"docs"`.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/activityClassification.test.ts</automated>
  </verify>
  <done>All pre-existing cases pass unmodified; new service-first block green; `npx tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve service through the loader union + attribution counters in summarizeModules</name>
  <files>lib/server/moduleActivityView.ts, lib/acc/moduleCountsTypes.ts, app/(dashboard)/access-analysis/moduleCounts.ts, app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts</files>
  <action>
1. `lib/acc/moduleCountsTypes.ts`: add optional `service?: string | null` to `ModuleActivityRow`
   (additive — existing literal constructors elsewhere keep compiling).

2. `lib/server/moduleActivityView.ts`: add the service dimension to the union SQL. Both
   branches select a service column aliased identically and it joins the GROUP BY key
   (attribution must be per-(project, action, service) tuple):

```sql
SELECT pid AS "projectId", action AS "rawAction", service, SUM(c)::int AS count
FROM (
  SELECT "projectId" AS pid, "activityVerb" AS action, "serviceGroup" AS service, COUNT(*)::int AS c
    FROM "AccActivityAccds" GROUP BY 1, 2, 3
  UNION ALL
  SELECT COALESCE(NULLIF(d."projectId", ''), '') AS pid, d."rawAction" AS action, d."service" AS service, COUNT(*)::int AS c
    FROM "AccActivity" d
    LEFT JOIN astart a ON a."projectId" = d."projectId"
    WHERE d."projectId" IS NULL OR d."projectId" = '' OR a.s IS NULL OR d."createdAt" < a.s
    GROUP BY 1, 2, 3
) u
GROUP BY pid, action, service
```

   Keep the `astart` CTE and the DC keep-predicate byte-identical. Map `service: p.service ?? null`
   into the returned rows. Then run a one-off row-count smoke check against the live DB
   (read-only, e.g. a throwaway node script or extend an existing diag pattern) and update the
   stale "~2-3k rows (~200 KB)" doc comment with the measured post-change count (research
   bounds it at ~10-20k worst case — still far below OOM-guard scale; if it lands above ~50k,
   STOP and record a deviation).

3. `app/(dashboard)/access-analysis/moduleCounts.ts`: `summarizeModules` passes the service
   through — `classifyActivity(row.rawAction, row.service)` — and accumulates activity-volume
   counters from the returned `attributedBy`:

```typescript
export interface ModuleSummary {
  // ...existing fields unchanged...
  /** Live attribution split for the TRUTH-03 caveat: activity volume attributed with a
   *  recognized Autodesk service tag vs inferred from the verb taxonomy alone. */
  attribution: { serviceCount: number; verbCount: number };
}
```

   Note: `typesByModule` merge stays keyed by rawAction; two rows with the same rawAction but
   different service values that land in DIFFERENT modules must accumulate into their own
   module's type lists (the existing per-module `byRaw` map already handles this — verify with
   a test rather than restructure).

4. `app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts`: update the `mk()` helper
   to accept an optional `service` argument (default `null` — existing call sites unchanged).
   ADD cases: (a) service-tagged row moves module vs verb-only twin (e.g. rawAction that
   verb-maps to dataManagement + `service: "submittals"` → counted under build); (b)
   `attribution` counters: mixed rows produce serviceCount = sum of counts with recognized
   service, verbCount = the rest, serviceCount + verbCount === total; (c) all-null-service rows
   → attribution.serviceCount === 0 and every slice identical to the pre-change expectations
   (regression pin).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts" lib/acc/activityClassification.test.ts</automated>
  </verify>
  <done>Both suites green (existing cases unmodified); tsc clean; loader comment reflects the measured live row count; live smoke-count recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Live before/after attribution-movement evidence for the owner</name>
  <files>scripts/diag-attribution-delta.cjs, .planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md</files>
  <action>
Create `scripts/diag-attribution-delta.cjs` — a READ-ONLY diagnostic mirroring the existing
`scripts/diag-activity-service-xtab.cjs` conventions (same DB/env access pattern; inspect that
file first and copy its bootstrapping). It must:

1. Pull the same compact per-(project, action, service) aggregate the modified
   `moduleActivityView.ts` union produces (duplicate the SQL in the script — scripts must not
   import from `lib/server` per the scripts→app/lib boundary convention; note this in a comment).
2. For every aggregate row, classify twice using the SAME new classifier via the
   `lib/acc/activityClassification` import path the four existing diag scripts already use
   (BND-02 precedent — scripts legitimately import from lib/acc):
   - BEFORE: `classifyActivity(rawAction)` (verb-only)
   - AFTER: `classifyActivity(rawAction, service)` (service-first)
3. Emit: per-module totals before vs after (absolute + delta + % of grand total), a
   from-module→to-module movement matrix for rows that moved, and the overall
   service-attributed vs verb-inferred volume split.

Run it against the live DB and write the output (with a short interpretation paragraph) to
`.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md`. Frame honestly
per research: expected movement is SMALL (order ~1-2% of service-tagged rows, concentrated in
Design Collaboration / Data Management / Admin Actions boundaries — NOT Model Coordination,
already fixed 2026-06-05 by commit 6164cfae). Explicitly state that `docs/activity-module-audit.md`'s
"~966 Model Coordination" figure is stale and superseded. This file is the checkpoint evidence
21.1-04 presents to the owner.
  </action>
  <verify>
    <automated>node scripts/diag-attribution-delta.cjs</automated>
  </verify>
  <done>Script runs clean read-only against the live DB; 21.1-ATTRIBUTION-DELTA.md exists with live per-module before/after totals, a movement matrix, and the attribution split; no writes to any table.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npx vitest run lib/acc/activityClassification.test.ts "app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts"` → green, zero pre-existing case modifications.
- `npx vitest run "app/(dashboard)/access-analysis/__tests__/ModulesPieChart.test.tsx"` → still green (donut consumes ModuleSummary; additive field must not break it).
- Live union row count measured and recorded; bounded well below OOM-guard scale.
- Commits by explicit path only; `git diff --cached --name-only` before every commit; no file outside `files_modified`.
</verification>

<success_criteria>
- Service-first precedence live in `classifyActivity` with characterization tests pinning: service wins where decisive, verb fallback where absent, umbrella deference where refinement is intentional.
- `ModuleSummary.attribution` carries live service/verb volume counters (consumed by 21.1-04).
- `21.1-ATTRIBUTION-DELTA.md` quantifies the real movement from live data.
- No change to `moduleOverrides.ts` (barrel stays 4-line re-export).
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-01-SUMMARY.md`
</output>
