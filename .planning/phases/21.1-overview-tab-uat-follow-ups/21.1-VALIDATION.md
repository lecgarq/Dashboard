---
phase: 21.1
slug: overview-tab-uat-follow-ups
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-06
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | VERIFY: planner to confirm vitest config path from repo |
| **Quick run command** | `npx vitest run <target test file>` |
| **Full suite command** | `npm test` (baseline 2421 passed / 1 skipped) |
| **Estimated runtime** | ~full-suite minutes; targeted files seconds |

---

## Sampling Rate

- **After every task commit:** Run targeted `npx vitest run` on touched test files
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green + `npx tsc --noEmit` clean
- **Max feedback latency:** targeted runs < 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _to be filled by planner from PLAN.md tasks_ | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- Existing vitest infrastructure covers all phase requirements; planner to name new test files per task (aggregate-bound loader test, pure-transform tests, service-first attribution characterization test per 21.1-CONTEXT.md verification expectations).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Owner live checkpoint of the 3 Overview panels | 21.1 UAT items | Visual/UX judgment on live data | `:3100` production-build preflight (isolated dist, `:3000` untouched), same mechanism as 21-04 |
| Before/after attribution movement note | Item 2 | Quantified live-data evidence for owner | Diagnostic query comparing module counts pre/post service-first fix |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s for targeted runs
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
