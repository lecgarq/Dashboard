---
phase: 21.1-overview-tab-uat-follow-ups
verified: 2026-07-06T23:15:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 21.1: Overview Tab UAT Follow-ups Verification Report

**Phase Goal:** Close the 3 verbatim owner UAT items from the Phase 21 checkpoint
(21-04-SUMMARY.md "UAT Feedback / Follow-ups"): (1) UAT-21.1-01 new "Provisioned modules" chart
on the Overview tab; (2) UAT-21.1-02 fix the "Activity by module" attribution data-bug
(service-first attribution, honest live-split caveat); (3) UAT-21.1-03 new "Activity share by
project" donut on the Overview tab.

**Verified:** 2026-07-06T23:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Activity rows carrying an Autodesk service tag are attributed service-first; rows without one keep the existing verb-taxonomy classification | ✓ VERIFIED | `lib/acc/activityClassification.ts:200-260` `SERVICE_TO_MODULE` + precedence rules; `activityClassification.test.ts` "service-first attribution" describe block; targeted Vitest run green (see Behavioral Spot-Checks) |
| 2 | `ModuleSummary.attribution` exposes live service-attributed vs verb-inferred activity volumes | ✓ VERIFIED | `app/(dashboard)/access-analysis/moduleCounts.ts:50-101` — `attribution: { serviceCount, verbCount }` computed from `classifyActivity(row.rawAction, row.service)` |
| 3 | A quantified before/after module-movement note exists for the owner, computed from live DB data | ✓ VERIFIED | `.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md` (105 lines) — live per-module before/after totals, movement matrix, 5-correction owner-review section, explicit statement that the stale "~966 Model Coordination" figure is superseded |
| 4 | A server loader aggregates member-module grants from `AccProjectMember.products` (full live-project coverage, NOT the DC-only subset) bounded by n_projects × n_modules | ✓ VERIFIED | `lib/server/provisionedModulesView.ts` — `db.accProjectMember.findMany(...)` + `reduceModules` (not `AccDcProjectUserProduct`); aggregate-bound test in `provisionedModulesView.test.ts` |
| 5 | A horizontal-bar "Provisioned modules" chart shows grant counts per module with click-to-drill top projects, zero-grant modules labeled honestly | ✓ VERIFIED | `ProvisionedModulesChart.tsx` (161 lines) — bars via `EChart`, local drill state, "No module grants for this selection" empty state, zero-grant muted footer |
| 6 | "Activity share by project" renders as a top-10 + Other donut derived from already-loaded rows (zero new loader), Account-level bucket excluded with a live caption figure, click-to-drill module breakdown | ✓ VERIFIED | `projectActivityCounts.ts` (`summarizeProjectActivity`, no `@/server/db` import) + `ProjectActivityDonut.tsx` (283 lines) — `summarizeModules(rowsByProject.get(drill))` drill, live `accountLevelCount` caption, "No project activity for this selection" empty state |
| 7 | Both new panels are live on the Overview tab (2-up row between "Activity by module" and "Ingest freshness"), picker-only filtered, and the owner reviewed all 3 items live on a `:3100` preflight | ✓ VERIFIED | `mainCharts.tsx` fan-out 9→10 (`loadProvisionedModules` in `Promise.all`); `AccessAnalysisCharts.tsx` picker-only memos (`selected`, never `sliceFilteredProjectIds`); `OverviewTabPanel.tsx` `grid-cols-2` row mounting both components + live caveat computed from `moduleSummary.attribution`; owner checkpoint verdicts recorded verbatim in `21.1-04-SUMMARY.md` (two rounds, final: "approved BUT move sheets and friends to the Build module") |

**Score:** 7/7 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/acc/activityClassification.ts` | `SERVICE_TO_MODULE` + `attributedBy` field | ✓ VERIFIED | Present, wired, 269 lines, `MODULE_OVERRIDES` extension for the 5 owner corrections |
| `lib/server/moduleActivityView.ts` | SQL union preserving `serviceGroup`/`service` | ✓ VERIFIED | Union groups by `pid, action, service`; `service` field on `ModuleActivityRow` |
| `app/(dashboard)/access-analysis/moduleCounts.ts` | `summarizeModules` passes service through + `attribution` counters | ✓ VERIFIED | Confirmed line 71 (`classifyActivity(row.rawAction, row.service)`), line 101 (`attribution` returned) |
| `.planning/phases/21.1-overview-tab-uat-follow-ups/21.1-ATTRIBUTION-DELTA.md` | Live before/after evidence | ✓ VERIFIED | 105 lines, regenerated after each correction round |
| `lib/server/provisionedModulesView.ts` | `loadProvisionedModules()` over `AccProjectMember` | ✓ VERIFIED | 85 lines, `accProjectMember.findMany`, `reduceModules`, `resolveProjectName` |
| `app/(dashboard)/access-analysis/provisionedModulesCounts.ts` | `summarizeProvisionedModules` | ✓ VERIFIED | Exported at line 50 |
| `app/(dashboard)/access-analysis/components/ProvisionedModulesChart.tsx` | Horizontal-bar chart, unmounted-then-mounted | ✓ VERIFIED | 161 lines; imported in `OverviewTabPanel.tsx` |
| `app/(dashboard)/access-analysis/projectActivityCounts.ts` | `summarizeProjectActivity` | ✓ VERIFIED | Exported at line 56 |
| `app/(dashboard)/access-analysis/components/ProjectActivityDonut.tsx` | Donut with local drill | ✓ VERIFIED | 283 lines; imports `summarizeModules`; mounted in `OverviewTabPanel.tsx` |
| `app/(dashboard)/access-analysis/mainCharts.tsx` | `loadProvisionedModules` in eager fan-out (9→10) | ✓ VERIFIED | Line 80: `loadProvisionedModules()` inside the `Promise.all` |
| `app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx` | 2-up row + live-split caveat | ✓ VERIFIED | `grid grid-cols-1 gap-6 lg:grid-cols-2` row (line 154) mounting both components; caveat computed from `moduleSummary.attribution` (lines 55-59) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `moduleCounts.ts` | `activityClassification.ts` | `classifyActivity(row.rawAction, row.service)` | ✓ WIRED | Confirmed at `moduleCounts.ts:71` |
| `moduleActivityView.ts` | `AccActivityAccds.serviceGroup`/`AccActivity.service` | Raw SQL union | ✓ WIRED | Confirmed union SELECT aliases both to `service`, `GROUP BY pid, action, service` |
| `provisionedModulesView.ts` | `modules.ts` | `reduceModules(pairs)` | ✓ WIRED | Confirmed import + call at lines 3, 67 |
| `provisionedModulesView.ts` | `folderActivityView.ts` | `buildProjectNameMap`/`resolveProjectName` | ✓ WIRED | Confirmed import + call at lines 4, 78 |
| `AccessAnalysisCharts.tsx` | `provisionedModulesCounts.ts` | `summarizeProvisionedModules(filterRowsBySelection(...))` | ✓ WIRED | Confirmed at line 242-244 |
| `AccessAnalysisCharts.tsx` | `projectActivityCounts.ts` | `summarizeProjectActivity(filterRowsBySelection(...))` | ✓ WIRED | Confirmed at line 246-248 |
| `ProjectActivityDonut.tsx` | `moduleCounts.ts` | `summarizeModules(rowsByProject.get(drill))` | ✓ WIRED | Confirmed at line 80 |
| `OverviewTabPanel.tsx` | `ModuleSummary.attribution` | Live percentage in TRUTH-03 caveat | ✓ WIRED | Confirmed at lines 57-59, 122-131 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `ProvisionedModulesChart` | `summary` (from `provisionedModuleSummary`) | `loadProvisionedModules()` → `db.accProjectMember.findMany` | Yes — 14,566 live `AccProjectMember` rows, 0/14,566 null/malformed `products` (measured live, per 21.1-02-SUMMARY.md) | ✓ FLOWING |
| `ProjectActivityDonut` | `summary` (from `projectActivitySummary`) | Re-aggregation of `loadModuleActivity()`'s existing rows (no new query) | Yes — same union feeding the existing "Activity by module" donut | ✓ FLOWING |
| Overview caveat percentages | `moduleSummary.attribution` | `classifyActivity` per-row accumulation inside `summarizeModules` | Yes — computed live per filtered selection, division-by-zero guarded | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `npx tsc --noEmit` clean | `npx tsc --noEmit` | 0 errors | ✓ PASS |
| All 9 phase-touched test files pass | `npx vitest run lib/acc/activityClassification.test.ts "app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/ModulesPieChart.test.tsx" "app/(dashboard)/access-analysis/__tests__/provisionedModulesCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/ProvisionedModulesChart.test.tsx" lib/server/provisionedModulesView.test.ts "app/(dashboard)/access-analysis/__tests__/projectActivityCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/ProjectActivityDonut.test.tsx" "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx"` | 9 files / 125 tests passed | ✓ PASS |
| Full suite matches claimed 2477/1/0 | `npm test` (run once, per verifier constraint) | 325 passed \| 1 skipped (326 files); 2477 passed \| 1 skipped (2478 tests), 0 failed | ✓ PASS |
| All 16 claimed commits exist | `git cat-file -e <hash>` for each of e0f0c976, ca98a235, 8d3d1d10, 9b1a8731, 7f8e9f83, 0b6ff7c9, a083219b, 820c1fb1, 0a0b38a9, 02f36003, b9ef9ff9, 944e4d18, d699a299, 1dca75d3, 55c4b78d, 3c4feaba | all 16 present, matching commit subjects to their claimed plans | ✓ PASS |
| Phase scope diff exact | `git log --oneline e0f0c976^..3c4feaba --name-only` | Exactly the phase's SUMMARY/ROADMAP/STATE files + the 25 planned source/test files; no `/users/spatial-graph`, no `package.json`/lockfile changes | ✓ PASS |

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` files exist for this phase and none were declared in the PLAN/SUMMARY files (this is a UI/data-wiring phase, not a migration/tooling phase). SKIPPED — no applicable probes.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| UAT-21.1-01 | 21.1-02, 21.1-04 | Provisioned-modules chart on Overview tab | ✓ SATISFIED | `ProvisionedModulesChart` mounted, live grants from `AccProjectMember.products` |
| UAT-21.1-02 | 21.1-01, 21.1-04 | "Activity by module" attribution data-bug fix, service-first, honest caveat | ✓ SATISFIED | Service-first classifier + live-split caveat wired; quantified evidence in `21.1-ATTRIBUTION-DELTA.md` |
| UAT-21.1-03 | 21.1-03, 21.1-04 | "Activity share by project" donut on Overview tab | ✓ SATISFIED | `ProjectActivityDonut` mounted, zero new loader, Account-level exclusion with live caption |

No formal `.planning/REQUIREMENTS.md` IDs are assigned to this inserted phase (confirmed: `grep -n "UAT-21.1" .planning/REQUIREMENTS.md` returns nothing — matches the phase's own documented decision that tracking lives only on the ROADMAP.md phase line). `.planning/ROADMAP.md`'s Phase 21.1 entry marks all three items `✅ — ALL 3 COMPLETE, owner-approved live on :3100 2026-07-06` — confirmed accurate against the SUMMARY/commit evidence above. No orphaned requirements.

### Anti-Patterns Found

None. Scanned all 11 phase-touched production source files for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|not yet implemented|coming soon` — zero matches (the only "unmounted" mentions in SUMMARY files are historical plan-sequencing notes, not present-tense stubs in the final mounted code). No `TBD`/`FIXME`/`XXX` debt markers in any changed file.

### Owner Checkpoint (already resolved — not re-litigated)

Per the task instructions, the three owner checkpoint rounds documented in `21.1-04-SUMMARY.md`
(initial review → delegated mapping audit → 4 corrections → owner-directed 5th correction moving
the Sheets cluster + version-set family to Build) are accepted as already-resolved human
verification. Verbatim final verdict: *"approved BUT move sheets and friends to the Build
module"* — applied as commit `55c4b78d`. Design Collaboration's honest ~53-row sliver and Model
Coordination's deliberate donut exclusion are pre-existing/intentional, not phase 21.1 defects —
confirmed by reading `21.1-ATTRIBUTION-DELTA.md`'s "Owner-delegated mapping review" section and
`activityClassification.ts`'s `MODULE_OVERRIDES`/pinned tests.

### Unrelated Uncommitted WIP (noted, not a phase 21.1 gap)

`git status --short` shows uncommitted modifications to `ProjectActivityDonut.tsx`,
`ProvisionedModulesChart.tsx`, `AccessAnalysisCharts.tsx`, and 7 other sibling chart components
under `app/(dashboard)/access-analysis/components/` — all are minor hex-color-only palette
tweaks (e.g. `PALETTE`/`FALLBACK_COLOR` constant changes), part of an apparent in-flight,
repo-wide color-harmonization pass that touches far more files than this phase's scope and was
never mentioned in any 21.1 plan/SUMMARY. This is current working-tree WIP layered on top of the
already-committed and verified phase 21.1 code (`HEAD` at commit `3c4feaba` and later contains
the phase's own commits cleanly) — it does not affect the correctness of what phase 21.1 shipped
and is out of this phase's scope to resolve.

### Human Verification Required

None. The owner already completed live UI verification across two checkpoint rounds on a
`:3100` production-build preflight (documented verbatim in `21.1-04-SUMMARY.md`), which the task
instructions direct this verifier to treat as already-satisfied human verification for this
phase's UI truths.

### Gaps Summary

No gaps. All 7 derived observable truths (roadmap Success-Criteria-equivalent, since Phase 21.1
carries no formal `success_criteria` array — it is an inserted urgent phase tracked only on the
ROADMAP.md phase line) are verified against live source code, passing tests (targeted 125/125,
full suite 2477/1/0 reproduced independently in this verification run), and existing git commits
(all 16 claimed hashes confirmed present). Scope is clean: no `/users/spatial-graph` touch, no
new npm dependency, no new WebGL, zinc theme + resolved ECharts colors preserved, empty states
present on both new panels, and data sources match their documented Prisma authority
(`AccProjectMember.products` for provisioned modules; existing `loadModuleActivity()` rows,
re-aggregated with zero new fetch, for the project-activity donut).

---

## Dashboard Self-Check

- **Context:** `.planning/STATE.md`, `.planning/ROADMAP.md` (Phase 21.1 entry), all 4 PLAN.md +
  4 SUMMARY.md files, `21.1-CONTEXT.md`, `21.1-ATTRIBUTION-DELTA.md` read in full. No stale/missing
  artifacts.
- **Scope matched:** `/access-analysis` Overview tab only; confirmed via full phase commit-range
  file diff (no other workshop page touched).
- **Exact artifacts:** every path, function export, and Prisma model claim verified directly
  against source (`Read`/`Grep`), not assumed from SUMMARY prose.
- **Repo roots:** all paths under `lib/`, `app/(dashboard)/access-analysis/`, `scripts/` — no
  generic `src/...` paths introduced.
- **Data truth:** `AccProjectMember.products` (full live coverage, 0.0% null incidence,
  live-measured) and the existing accds+DC-backfill `loadModuleActivity()` union are both
  confirmed as the real data sources; Account-level exclusion and zero-grant modules are labeled,
  not hidden; the live service/verb attribution split replaces the stale "~40.7%" copy.
- **UI constraints:** zinc theme via `useTheme()`/resolved ECharts colors confirmed in both new
  components; no card-in-card; empty states present; no new WebGL (grep-confirmed); responsive
  `lg:grid-cols-2` row.
- **Boundary constraints:** components never touch Prisma directly (`ProvisionedModulesChart`/
  `ProjectActivityDonut` take `summary` props only); shared logic lives in `lib/server/` and
  `app/(dashboard)/access-analysis/*Counts.ts`, matching existing sibling conventions.
- **Gates run:** `npx tsc --noEmit` (0 errors, reproduced), targeted Vitest (125/125,
  reproduced), full `npm test` (2477/1/0, reproduced once per verifier constraint), all 16
  claimed commits confirmed present in `git log`.
- **VERIFY:** none remaining — the one unrelated observation (uncommitted repo-wide color-palette
  WIP touching 10 chart files) is noted above as out-of-scope, not a phase 21.1 claim requiring
  verification.

---

_Verified: 2026-07-06T23:15:00Z_
_Verifier: Claude (gsd-verifier)_
