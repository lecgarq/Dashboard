---
phase: 40
plan: "40-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx (morphPointSet handle method + transitionEasing config)
  - app/(dashboard)/users/access-analysis/activity/activityMotion.ts (new — evolves activityPhysicsStub; + test)
  - app/(dashboard)/users/access-analysis/activity/activityPhysicsStub.ts (superseded/absorbed — see Task 2)
autonomous: true
requirements: [PERF-07]
must_haves:
  truths:
    - "GraphCanvas2D exposes morphPointSet(positions2, durationMs): one setPointPositions(p, true) + render(undefined, durationMs) — cosmos v3's built-in GPU source→target position transition (interpolatePosition shader) animates it; NO custom shader patch, NO per-frame CPU position writes during the transition (VERIFY resolved: render(simulationAlpha?, transitionDuration?) is the public per-call override, index.d.ts:375)"
    - "transitionEasing is set at construct time so morphs ease (quad/cubic-in-out), and morphPointSet resets the prevUploaded no-op-skip buffer"
    - "activityMotion owns the motion contract: rest buffer, ambient choreography on a deterministic ~100k subset of the rendered set (owner decision 1) writing ONLY subset entries per frame, pushed via pushPointSet; ambient suspends during an active morph and under prefers-reduced-motion (fully static)"
    - "The activity path NEVER starts the cosmos force simulation and NEVER performs per-rAF full-buffer CPU position writes — pinned by tests against a fake handle (the PERF-02 frozen-handle invariant EVOLVED: new pins replace the instance-era contract for this path; instance physicsLayer tests remain untouched/green as module tests)"
    - "Contract change is recorded: activityMotion.ts header documents what MAY mutate cosmos state (morph upload seam, ambient subset pushes, LOD setPointSet) and what MAY NOT (force sim, per-frame full-buffer writes)"
  artifacts:
    - app/(dashboard)/users/access-analysis/activity/activityMotion.ts
    - app/(dashboard)/users/access-analysis/activity/activityMotion.test.ts
  key_links:
    - pattern: "morphPointSet"
      in: app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx
    - pattern: "render(undefined,"
      in: app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx
    - pattern: "prefers-reduced-motion|reducedMotion"
      in: app/(dashboard)/users/access-analysis/activity/activityMotion.ts
---

# 40-02 — GPU morph seam + decimated ambient + motion contract pins

## Objective

PERF-07's mechanics: a cosmos-native GPU-interpolated morph seam in
GraphCanvas2D, a decimated CPU ambient layer, and rewritten motion-contract
pins. The dead ≥250k per-frame CPU path cannot return.

## Context (@ paths)

- @.planning/phases/40-dimensions-sliders-at-scale/40-CONTEXT.md
- @app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx — handle API,
  setPointSet/pushPointSet seam, prevUploaded no-op skip
- @app/(dashboard)/users/access-analysis/activity/activityPhysicsStub.ts —
  "Phase 40 owns motion" header; PhysicsLayer escape hatch
- @.planning/phases/37-scale-feasibility-spike/37-BASELINE.md — CPU ambient 73
  fps @106k proven; full-set choreography dead ≥250k; cosmos force sim rejected
- node_modules/@cosmos.gl/graph/dist/index.d.ts — VERIFIED at plan time:
  `render(simulationAlpha?: number, transitionDuration?: number)` (line 375)
  overrides transition duration per call; `transitionEasing` in config;
  Points module has sourcePositionFbo/targetPositionFbo + interpolatePosition
  GPU command; currentPositionTexture stays hit-test-correct mid-transition
  ("matches what's on screen" invariant in Points/index.d.ts)

## Tasks

1. **GraphCanvas2D morph seam** — add optional handle method
   `morphPointSet(positions2: Float32Array, durationMs: number): void`:
   `g.setPointPositions(positions2, true); g.render(undefined, durationMs);
   prevUploaded = null;`. Same-count only (the rendered set), camera untouched.
   Add `transitionEasing: "quad-in-out"` to the construct config (inert while
   every other call path keeps duration 0 — config default transitionDuration
   stays 0 so all existing renders still snap; assert this in the plan test via
   the fake-graph call log, and confirm no behavior change on the 22k-era
   surfaces is possible since they never pass a duration). Fallback clause
   (owner decision 2): if headed smoke shows the transition visibly wrong at
   ~490k (Phase-41 measures fps, this is correctness only), record the
   decimated-morph+snap deviation in the SUMMARY — do not silently swap.
2. **`activityMotion.ts`** — evolve the stub into the activity motion layer
   (keep `createActivityPhysicsStub`+`toStride3` exports working — either
   re-export from activityMotion or absorb the file; update the one importer
   ActivityUniverseShell in place; no dangling stub file left behind):
   - `createActivityMotion({handle, rest2, reducedMotion})` — owns a working
     Float32Array copy of rest; API: `setBase(positions2)` (called on
     LOD/morph-target changes so ambient drifts around the CURRENT layout),
     `startAmbient()/stopAmbient()`, `morphTo(target2, durationMs)` (delegates
     to handle.morphPointSet, suspends ambient for durationMs then resumes),
     `dispose()`.
   - Ambient: deterministic subset = every k-th rendered index sized ~100k
     (owner decision 1); per-frame trig drift (small amplitude, per-node phase
     from index hash — the 37-spike choreography pattern) writing ONLY subset
     entries into the working buffer, then `handle.pushPointSet(working)`.
     rAF loop throttled to the established ~30Hz convention if the spike used
     one (check 37 spike source; else plain rAF). `reducedMotion` true → all
     ambient no-ops, morphTo passes duration 0 (snap).
   - Header comment: the new motion contract (may/may-not list) — this is the
     recorded PERF-02 evolution.
3. **Contract pins — `activityMotion.test.ts`** (the rewritten PERF-02 pins):
   fake handle logging every call. Pin: (a) morphTo → exactly one
   morphPointSet, zero pushPointSet during the morph window; (b) N ambient
   frames → ≤ subsetSize distinct entries changed per frame vs base, full
   buffer length pushed but only subset values differ; (c) no call path ever
   invokes applySliders/setClustering/resumeSimulation (force sim stays dead);
   (d) reducedMotion → zero pushes and snap morphs; (e) stub-compat:
   getPositions/frozen still satisfy GraphCanvas2D init. Old instance-era
   physicsLayer tests are NOT deleted (physicsLayer.ts stays type-imported by
   the kept sidebar); note their scope in the SUMMARY (instance-era module
   pins, superseded for the activity path by these).

## Verification gates

- Focused vitest: activityMotion.test.ts (+ any touched existing canvas tests:
  CosmosCanvasClient.test.ts stays green).
- `npx tsc --noEmit` clean.
- `node scripts/repo-map/check.cjs` (shared component surface changed).

## Success criteria

Truths above hold; ActivityUniverseShell still compiles/mounts unchanged
except the stub import; fps claims stay OUT of tests (Phase 41 owns the gate).
