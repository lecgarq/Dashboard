---
phase: 40
plan: "40-03"
type: execute
wave: 2
depends_on: ["40-01", "40-02"]
files_modified:
  - app/(dashboard)/users/access-analysis/activity/ActivityUniverseShell.tsx (sidebar mount, morph wiring, color-by, labels, LOD suspend, captions)
  - app/(dashboard)/users/access-analysis/activity/ActivityDimensionsPanel.tsx (new — the repopulated sidebar shell composing GroupByControls-style select + DimensionSlider)
  - app/(dashboard)/users/access-analysis/GroupByControls.tsx (data-boundary props: injected options/coverage — only if reuse holds without forking; else panel composes DimensionSlider directly and this stays untouched)
  - app/(dashboard)/users/access-analysis/activity/activityTestBridge.ts (groupBy/colorBy/strength/morphCount fields)
  - app/(dashboard)/users/access-analysis/DimensionSearchBox.tsx (DELETE — owner decision 3, retire)
  - app/(dashboard)/users/access-analysis/PresetBar.tsx (+ __tests__/PresetBar.test.tsx) (DELETE — owner decision 3, retire)
autonomous: true
requirements: [DIM-07, PERF-07]
must_haves:
  truths:
    - "The /users/spatial-graph surface shows a dimensions sidebar with: group-by select (7 dims — author absent, owner decision 4), color-by select (8 dims incl. author), and a grouping-strength DimensionSlider (kept Radix shell, unchanged visuals)"
    - "Strength > 0 morphs the rendered set toward per-category organic centroids via activityMotion.morphTo (GPU transition); drag commits are rAF/throttle-coalesced target recomputes (mix(rest, clump, s)) — never per-rAF-frame full-buffer writes; strength 0 returns exactly to rest"
    - "Group-by change at strength > 0 recomputes targets and morphs (~600ms); every category keeps its own centroid at 957 (archipelago), no grid"
    - "Color-by change swaps the rendered-set color buffer + legend (top-N + grey honest counts); module remains the default and keeps its 7-color language"
    - "Top-N category labels ride the morph via MapClusterLabels (restCenters→centers lerp on live strength); zoom LOD reveals smaller categories per its established behavior"
    - "Region-LOD detail seam is SUSPENDED while strength > 0 (rendered set stays the deterministic stride-10 sample); it resumes when strength returns to 0 — no mid-morph teleport (CONTEXT trap)"
    - "Per-dimension honest coverage caption appears when a dim is active (covered/total from activityDimensions coverage); the standing LOD + author-resolution captions persist through morph"
    - "prefers-reduced-motion → snap repositioning (no GPU tween > 200ms fade, no ambient); dimension dict failure degrades to an honest disabled state, never a blank sidebar"
    - "DimensionSearchBox + PresetBar (+ test) are deleted; any other instance-only wiring is deleted ONLY when (a) zero importers remain after rewiring AND (b) the file carries no uncommitted WIP (git status per file) — WIP-carrying candidates (catalogTargets.ts, dimensionCatalog.types.ts, …) are flagged in the SUMMARY, not deleted"
  artifacts:
    - app/(dashboard)/users/access-analysis/activity/ActivityDimensionsPanel.tsx
  key_links:
    - pattern: "ActivityDimensionsPanel"
      in: app/(dashboard)/users/access-analysis/activity/ActivityUniverseShell.tsx
    - pattern: "morphTo"
      in: app/(dashboard)/users/access-analysis/activity/ActivityUniverseShell.tsx
    - pattern: "MapClusterLabels"
      in: app/(dashboard)/users/access-analysis/activity/ActivityUniverseShell.tsx
    - pattern: "DimensionSlider"
      in: app/(dashboard)/users/access-analysis/activity/ActivityDimensionsPanel.tsx
---

# 40-03 — Sidebar shell repopulated, shell wiring, labels, retirement

## Objective

DIM-07 lands user-visible: the kept sidebar shell repopulated with the 8
activity dims drives group-by/color-by/strength over the activity universe,
with organic morphs, riding labels, honest captions — and the instance-only
search/preset weight retires.

## Context (@ paths)

- @.planning/phases/40-dimensions-sliders-at-scale/40-CONTEXT.md
- @app/(dashboard)/users/access-analysis/activity/ActivityUniverseShell.tsx
- @app/(dashboard)/users/access-analysis/activity/activityDimensions.ts (40-01)
- @app/(dashboard)/users/access-analysis/activity/activityGroupLayout.ts (40-01)
- @app/(dashboard)/users/access-analysis/activity/activityColorBy.ts (40-01)
- @app/(dashboard)/users/access-analysis/activity/activityMotion.ts (40-02)
- @app/(dashboard)/users/access-analysis/GroupByControls.tsx + DimensionSlider.tsx
  + SliderContext.tsx — kept shells; GroupByControls/SliderContext internals are
  instance-shaped (NodeFeatureSnapshot, catalog builders, physics.updateSliders)
- @app/(dashboard)/users/access-analysis/MapClusterLabels.tsx — props verified
  reusable as-is via {mode:"2d", handle} adapter (CONTEXT VERIFY resolved)
- @DESIGN.md — dense zinc chrome, ≤200ms interaction motion

## Tasks

1. **`ActivityDimensionsPanel.tsx`** — the repopulated sidebar (right rail,
   same aside chrome as GroupByControls): group-by select ("None ·
   Embedding" + 7 groupBy dims), color-by select (8 dims, default module),
   `DimensionSlider` (label "Grouping strength", shown when group-by ≠ none),
   active-state summary + per-dim coverage line (coverageText format), honest
   corpus line ("4,904,886 events · rendering ~490k"). Reuse boundary: reuse
   DimensionSlider as-is; reuse GroupByControls only if injected-options props
   are a trivial additive change — otherwise the panel composes the same
   visual pattern directly and GroupByControls stays untouched (record which
   in SUMMARY). SliderContext reuse: its physics/catalog contract is
   instance-shaped — if wiring it needs any fork, use a ~20-line local
   rAF-coalesced strength state in the panel instead (the established
   COMMIT_THROTTLE pattern), recorded as a deviation with reason.
2. **Shell wiring** (`ActivityUniverseShell.tsx`):
   - State: groupBy (default none), colorBy (default module), strength 0–100.
   - Memoized per-dim: coverage, category counts (one pass over rendered-set
     ids), centroids/stats, rest targets. Strength change → target' =
     mix(rest, clump, s) into a reused buffer → `motion.morphTo(target',
     dragMs≈120 / commitMs≈250)`; group-by switch at s>0 → recompute + morph
     600ms. Strength 0 → morph back to rest exactly.
   - Color-by: gather dim colors for the CURRENT rendered set →
     handle.setColors; legend component swaps to buildDimLegend output
     (module default unchanged on first paint).
   - Labels: mount MapClusterLabels ({mode:"2d", handle} ref adapter,
     stats from groupStats, progress = live strength getter, legend-aligned
     colors). Chips fade in with strength (existing opacity prop).
   - LOD suspend: gate the applyLod effect on strength === 0; on returning to
     0, re-run LOD once so region mode can resume.
   - Selection/hover honesty: on morph start, clear hover; lasso stays
     rendered-subset honest (cosmos hit-tests the on-screen positions).
   - reduced-motion: matchMedia once → motion layer + snap durations.
   - Test bridge: add groupBy, colorBy, strength, morphCount, ambientActive.
3. **Retirement sweep** — delete DimensionSearchBox.tsx, PresetBar.tsx +
   PresetBar.test.tsx (clean files, zero importers after CatalogSliderSidebar
   stops using search — verify by rg import sweep first). CatalogSliderSidebar
   itself: if it retains zero mounts and its only value was the instance
   catalog, delete it too under the same two conditions (clean + no
   importers); its WIP-modified dependencies (catalogTargets.ts,
   dimensionCatalog.types.ts, catalogTargets.test.ts, …) are NOT deleted —
   flag as milestone-close debt in SUMMARY + CONCERNS roll.
4. **Focused component test** — ActivityDimensionsPanel render: 8 dims total,
   author absent from group-by options, present in color-by; coverage line
   text; strength slider fires the coalesced callback. Plus a shell-level pure
   check that group-by targets change when the select changes (transform-level,
   not canvas).
5. **Design gate** — visual surface changed: impeccable-detect-tier self-check
   against DESIGN.md (dense, zinc, muted captions, ≤200ms interaction motion,
   reduced-motion static, honest empty/disabled states).

## Verification gates

- Focused vitest (panel test + touched activity tests).
- `npx tsc --noEmit` clean.
- `node scripts/repo-map/check.cjs` (component surface + deletions).
- Full `npm test` at phase completion (TEST-01/02/03 byte-identical).

## Success criteria

Truths above hold; /users/spatial-graph compiles with the sidebar mounted;
deletions leave zero dangling imports; WIP files untouched.
