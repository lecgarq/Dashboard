---
phase: 20-foundation-wins-engagement-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/permissionFootprintView.ts
  - lib/server/permissionFootprintView.test.ts
  - app/(dashboard)/access-analysis/permissionFootprintCounts.ts
  - app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts
  - app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx
  - app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx
autonomous: true
requirements: [PERM-01]

must_haves:
  truths:
    - "A permission-reach-by-role dataset exists with role names, folder counts, and totalBytes as plain JS numbers (never BigInt)"
    - "formatBytes renders human-readable strings like '42.3 GB' across B/KB/MB/GB/TB boundaries"
    - "The chart component renders top ~10 roles as horizontal bars sorted by bytes desc with an 'Other' rollup, and clicking a role shows its per-project breakdown"
  artifacts:
    - path: "lib/server/permissionFootprintView.ts"
      provides: "loadPermissionFootprint loader + pure assemblePermissionFootprint (BigInt→Number conversion happens HERE)"
      exports: ["loadPermissionFootprint", "assemblePermissionFootprint", "PermissionFootprintRow"]
    - path: "app/(dashboard)/access-analysis/permissionFootprintCounts.ts"
      provides: "formatBytes + summarizePermissionFootprint (top-N + Other + per-role project drill)"
      exports: ["formatBytes", "summarizePermissionFootprint"]
    - path: "app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx"
      provides: "Client horizontal-bar chart with role drill"
      min_lines: 60
  key_links:
    - from: "lib/server/permissionFootprintView.ts"
      to: "db.accFolderPermissionSummary"
      via: "findMany select projectId/roleId/folderCount/totalBytes"
      pattern: "accFolderPermissionSummary\\.findMany"
    - from: "app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx"
      to: "@/components/ui/EChart"
      via: "canonical theme-aware chart wrapper (NOT the legacy route-local EChart)"
      pattern: "components/ui/EChart"
    - from: "lib/server/permissionFootprintView.ts"
      to: "Number(r.totalBytes)"
      via: "server-side BigInt conversion"
      pattern: "Number\\(.*totalBytes\\)"
---

<objective>
PERM-01 vertical slice (loader + pure transform + client chart + tests), NOT yet wired
into the page — plan 20-05 does the registration. Delivers "which roles reach the most
data": folder count + human-readable bytes per role from the materialized
`AccFolderPermissionSummary` (22,082 rows, cron-refreshed since v2.2 Ph18/19). Zero touch
of the raw ~6M-row `AccFolderPermission` table.

Purpose: the workshop presenter gets a credible "permission reach by role" story on
`/access-analysis`, and this plan establishes the milestone's BigInt-conversion convention.
Output: loader, pure transform with `formatBytes`, chart component, 3 test files.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-foundation-wins-engagement-panels/20-CONTEXT.md
@.planning/phases/20-foundation-wins-engagement-panels/20-RESEARCH.md
@lib/server/dcCoverageView.ts
@lib/server/projectCoverageView.test.ts
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx
@app/(dashboard)/template-mty/components/PermissionAccessChart.tsx
</context>

<interfaces>
<!-- Verified exports the executor uses directly — no exploration needed. -->

From prisma/schema.prisma (lines 549-561, verified):
```prisma
model AccFolderPermissionSummary {
  id          String   @id @default(cuid())
  projectId   String
  roleId      String
  folderCount Int
  totalBytes  BigInt
  permTypes   String[]
  refreshedAt DateTime @default(now())
  @@unique([projectId, roleId])
}
```

From lib/server/folderActivityView.ts (lines 31-57, verified):
```typescript
export function buildProjectNameMap(projects, dcProjects): Map<string, string>;
export function resolveProjectName(nameById: Map<string, string>, projectId: string): string;
```

From app/(dashboard)/access-analysis/roleColors.ts (verified):
```typescript
export const ROLE_PALETTE: string[];
export function buildRoleColorMap(roleNames: ReadonlyArray<string>): Map<string, string>;
```

From prisma/schema.prisma: `AccRole { id String @id; name String; ... }` — role names
resolve via `db.accRole.findMany({ select: { id: true, name: true } })` (155 rows; live
query confirmed 0 of the 107 distinct summary roleIds are unmatched).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PERM-01 server loader with server-side BigInt conversion + sibling test</name>
  <files>lib/server/permissionFootprintView.ts, lib/server/permissionFootprintView.test.ts</files>
  <action>
    Create `lib/server/permissionFootprintView.ts` following the `dcCoverageView.ts` /
    `projectCoverageView.ts` loader shape (import "server-only", `db` from `@/server/db`,
    module-level 5-minute TTL cache, `force` param). Export:

    - `interface PermissionFootprintRow { projectId: string; projectName: string; roleId: string; roleName: string; folderCount: number; totalBytes: number }`
      — `totalBytes` is a **number**, converted from BigInt here (Pitfall 2 in 20-RESEARCH.md;
      max observed value ~2.96e12 is well inside 2^53, no precision loss).
    - `assemblePermissionFootprint(summaryRows, projects, dcProjects, roles)` — PURE exported
      assembly (testable without DB): builds `nameById` via `buildProjectNameMap`/`resolveProjectName`
      imported from `./folderActivityView`, `roleNameById` Map from roles, maps every summary
      row to a `PermissionFootprintRow` with `Number(r.totalBytes)` and `roleNameById.get(r.roleId) ?? "Unknown role"`.
    - `loadPermissionFootprint(force = false)` — Promise.all of
      `db.accFolderPermissionSummary.findMany({ select: { projectId, roleId, folderCount, totalBytes } })`,
      `db.accProject.findMany({ select: { id, name } })`, `db.accDcProject.findMany({ select: { id, name } })`,
      `db.accRole.findMany({ select: { id, name } })`, then assemble + cache.

    Write the SIBLING test `lib/server/permissionFootprintView.test.ts` (convention verified:
    `projectCoverageView.test.ts` — pure assembly only, never hits the DB; `vi.mock("server-only", () => ({}))`
    if the module is imported directly). Cover: (a) BigInt input → number output and
    `JSON.stringify(rows)` does not throw; (b) unknown roleId → "Unknown role"; (c) project
    name resolution falls back per resolveProjectName for an id absent from both project lists.
  </action>
  <verify>
    <automated>npx vitest run lib/server/permissionFootprintView.test.ts</automated>
  </verify>
  <done>Loader + pure assembly exist; test green; totalBytes is number-typed and JSON-serializable.</done>
</task>

<task type="auto">
  <name>Task 2: formatBytes + summarizePermissionFootprint pure transform + test</name>
  <files>app/(dashboard)/access-analysis/permissionFootprintCounts.ts, app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts</files>
  <action>
    Create `app/(dashboard)/access-analysis/permissionFootprintCounts.ts` (pure, no
    "use client", no DB — same layer as `roleCounts.ts`):

    - `formatBytes(bytes: number): string` — the one genuinely new helper (verified: no
      formatBytes/prettyBytes helper exists anywhere in repo). B/KB/MB/GB/TB units, 1
      decimal above bytes, `"0 B"` for non-finite/<=0. Use the 20-RESEARCH.md sketch.
    - `summarizePermissionFootprint(rows: PermissionFootprintRow[], topN = 10)` — aggregates
      the (already project-filtered) rows per role: `{ bars: { roleId, roleName, folderCount, totalBytes, projectCount }[], projectsByRole: Map<string, { projectId, projectName, folderCount, totalBytes }[]> }`.
      Bars sorted by totalBytes desc; roles beyond topN collapse into a final `"Other (N roles)"`
      bar (aggregated bytes/folders; no drill entry required for Other — Claude's discretion
      per CONTEXT.md). `projectsByRole` keyed by roleName with per-project rows sorted by
      totalBytes desc — this is the click-to-drill payload, sourced entirely from the summary
      rows (no raw-table touch).

    Test in `app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts`
    (convention verified: transforms test in this shared `__tests__/` dir): formatBytes
    boundary cases (0, 512 B, 1 KB, 42.3 GB-style, TB), top-N + Other rollup math, drill
    rows sorted desc, and that output row count is bounded by roles (aggregate-shaped, not
    raw-row-shaped).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts"</automated>
  </verify>
  <done>formatBytes + summarize green; top-10+Other and per-role project drill verified by unit test.</done>
</task>

<task type="auto">
  <name>Task 3: PermissionFootprintChart client component + test</name>
  <files>app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx, app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx</files>
  <action>
    Create `"use client"` `PermissionFootprintChart.tsx` accepting
    `{ rows: PermissionFootprintRow[] }` (parent filters by project selection in plan 20-05).
    Follow `template-mty/components/PermissionAccessChart.tsx`'s horizontal-bar shape
    (`xAxis: { type: "value" }`, `yAxis: { type: "category" }`) rendered via the CANONICAL
    `@/components/ui/EChart` (NOT the legacy route-local `components/EChart.tsx` — its header
    marks it superseded). Behavior:

    - `summarizePermissionFootprint(rows)` in a useMemo; largest role at top.
    - Bar length = totalBytes; axis + bar labels + tooltip use `formatBytes`; tooltip also
      shows folder count and project count.
    - Per-role colors from `buildRoleColorMap` (roleColors.ts) so roles match their color
      in the Role-distribution donut; "Other" gets a muted zinc tone.
    - Click a bar → local drill state (mirror `RolesPieChart.tsx`'s local drill/setDrill
      pattern — NOT the shared sliceFilters bus, per 20-RESEARCH.md Open Q3 resolution):
      renders that role's project list (project name, folder count, formatBytes bytes),
      sorted bytes desc, with a close affordance. Clicking the active bar again closes it.
    - Empty state: "No permission summary rows for this selection." when rows is empty.
    - Motion within the existing <=200ms budget; no new WebGL; theme-resolved colors only.

    Test in `__tests__/PermissionFootprintChart.test.tsx` following the existing
    `RolesPieChart.test.tsx` / `ModulesPieChart.test.tsx` style (mock EChart or assert on
    rendered structure): renders with fabricated rows, drill opens on simulated click
    callback, empty state renders for [].
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx"</automated>
  </verify>
  <done>Component renders top-N horizontal bars with formatBytes labels, role drill works via local state, empty state honest; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/server/permissionFootprintView.test.ts "app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx"` — all green.
- `npx tsc --noEmit` — 0 errors.
- Grep proof: `Number(` conversion of totalBytes lives in `lib/server/permissionFootprintView.ts`, and `accFolderPermission\b` (raw table) is NOT referenced by any new file.
</verification>

<success_criteria>
- PERM-01 slice complete and tested standalone; roadmap SC#1's server-side BigInt
  conversion is implemented at the loader (live-page-load proof happens in plan 20-05).
- No edit to mainCharts.tsx / AccessAnalysisCharts.tsx (owned by plan 20-05).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-wins-engagement-panels/20-01-SUMMARY.md`
</output>
