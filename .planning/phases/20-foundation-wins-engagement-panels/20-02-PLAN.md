---
phase: 20-foundation-wins-engagement-panels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/signInRecencyView.ts
  - lib/server/signInRecencyView.test.ts
  - app/(dashboard)/access-analysis/signInRecencyCounts.ts
  - app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts
  - app/(dashboard)/access-analysis/components/DormantSignInChart.tsx
  - app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx
autonomous: true
requirements: [ENG-01]

must_haves:
  truths:
    - "Dormant-user rows carry a real per-project sign-in recency signal sourced from AccDcUser.lastSignIn joined via AccDcProjectUser (NOT the dead AccProjectMember.lastSignIn field)"
    - "A null lastSignIn always lands in an explicit 'Never signed in' bucket — never dropped, never merged"
    - "The chart shows the 5 ordered bands (<30d / 30-90d / 90-365d / >365d / Never) with the Never bar visually distinguished, and clicking a band drills to user name + company + exact date sorted most-dormant first"
    - "The panel carries an honest DC-coverage scope label (data covers DC-covered projects only)"
  artifacts:
    - path: "lib/server/signInRecencyView.ts"
      provides: "loadSignInRecency loader + pure assembleSignInRecency Map-join"
      exports: ["loadSignInRecency", "assembleSignInRecency", "SignInRecencyRow"]
    - path: "app/(dashboard)/access-analysis/signInRecencyCounts.ts"
      provides: "bucketSignInRecency (null-defensive) + summarizeSignInRecency (bands + drill)"
      exports: ["bucketSignInRecency", "summarizeSignInRecency", "RECENCY_BANDS"]
    - path: "app/(dashboard)/access-analysis/components/DormantSignInChart.tsx"
      provides: "Client vertical-bar chart with band drill + DC-coverage caption"
      min_lines: 60
  key_links:
    - from: "lib/server/signInRecencyView.ts"
      to: "db.accDcProjectUser + db.accDcUser"
      via: "Map-based join by userId (no Prisma relation exists — verified)"
      pattern: "accDcProjectUser\\.findMany"
    - from: "app/(dashboard)/access-analysis/signInRecencyCounts.ts"
      to: "null lastSignIn"
      via: "explicit 'never' band in bucketSignInRecency"
      pattern: "never"
    - from: "app/(dashboard)/access-analysis/components/DormantSignInChart.tsx"
      to: "@/components/ui/EChart"
      via: "canonical theme-aware chart wrapper"
      pattern: "components/ui/EChart"
---

<objective>
ENG-01 vertical slice (loader + pure transform + client chart + tests), NOT yet wired into
the page — plan 20-05 does the registration. Delivers "who has gone quiet, and who never
showed up": users bucketed by sign-in recency bands with an explicit "Never signed in"
bucket, per-project scoped, drillable.

**DOCUMENTED DEVIATION (orchestrator decision, resolves 20-RESEARCH.md Open Question 1 as
Option B):** the roadmap SC#4 and REQUIREMENTS.md name `AccProjectMember.lastSignIn` as the
source. That field was live-verified 100% NULL across all 14,566 rows (dead field —
`AccDcProjectUser.lastSignIn` equally dead at 22,835/22,835 NULL). This plan instead uses
**`AccDcUser.lastSignIn` joined per-project via `AccDcProjectUser`** (1,254/3,870 non-null —
a real distribution; 2,616 nulls feed a genuinely populated Never bucket). This preserves
the criterion's intent (dormant-user visibility with an honest never-signed-in bucket) while
swapping a dead field for the account's established sign-in source (`lib/acc/dcUserAssembly.ts:306`
precedent). Cost: coverage narrows to DC-covered projects (~550 of 1,153) — the panel MUST
carry an explicit scope label per the Dashboard "under-covered sources labeled, not hidden"
constraint. Record this deviation in the SUMMARY.

Purpose: real engagement story for the workshop instead of a degenerate 100%-Never chart.
Output: loader, bucketing/summarize transform, chart component, 3 test files.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-foundation-wins-engagement-panels/20-CONTEXT.md
@.planning/phases/20-foundation-wins-engagement-panels/20-RESEARCH.md
@lib/acc/activeUserTiers.ts
@lib/acc/dcUserAssembly.ts
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx
@app/(dashboard)/access-analysis/relativeTime.ts
</context>

<interfaces>
<!-- Verified schema + helpers the executor uses directly. -->

From prisma/schema.prisma (verified this planning pass):
```prisma
model AccDcUser {          // 3,870 rows; lastSignIn non-null on 1,254 (32.4%)
  id          String    @id
  email       String?
  name        String?
  status      String?
  companyId   String?
  lastSignIn  DateTime?
  ...
}
model AccDcProjectUser {   // 22,835 rows; its OWN lastSignIn is 100% NULL — do not use it
  projectId   String
  userId      String       // joins AccDcUser.id — NO Prisma relation, join in JS by Map
  ...
  @@id([projectId, userId])
}
model AccDcCompany { id String @id; name String; ... }
```

From app/(dashboard)/access-analysis/relativeTime.ts (verified):
```typescript
export function formatAbsolute(iso: string | null | undefined): string;
```

From lib/acc/activeUserTiers.ts — `bucketActiveUserTier(lastSignIn, now)` is the
null-defensive PATTERN to mirror (treats null/undefined/unparseable as "Never"), but its
bands are 7d/30d/90d/>90d — CONTEXT.md locks DIFFERENT bands, so write a NEW function,
do not import this one.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: signInRecency server loader (AccDcUser join via AccDcProjectUser) + sibling test</name>
  <files>lib/server/signInRecencyView.ts, lib/server/signInRecencyView.test.ts</files>
  <action>
    Create `lib/server/signInRecencyView.ts` (import "server-only", 5-min TTL cache, `force`
    param — same shape as dcCoverageView.ts). Export:

    - `interface SignInRecencyRow { projectId: string; name: string; company: string; lastSignIn: string | null }`
      — `lastSignIn` serialized to ISO string in the loader (RSC-boundary-safe); slim fields
      only (payload is ~22.8k rows — same order as the existing 14.5k roleRows the page
      already ships; do NOT add unneeded fields).
    - `assembleSignInRecency(projectUsers, users, companies)` — PURE exported join
      (testable without DB): Map `users` by id, `companies` by id; for each
      `AccDcProjectUser` row emit `{ projectId, name: user?.name ?? user?.email ?? "Unknown user", company: companyName ?? "Unknown company", lastSignIn: user?.lastSignIn?.toISOString() ?? null }`.
      Rows whose userId has no AccDcUser match are kept with nulls (honest, not dropped) —
      mirror the Map-join pattern of `lib/acc/dcUserAssembly.ts` (no FK exists; verified).
    - `loadSignInRecency(force = false)` — Promise.all of
      `db.accDcProjectUser.findMany({ select: { projectId: true, userId: true } })`,
      `db.accDcUser.findMany({ select: { id: true, name: true, email: true, companyId: true, lastSignIn: true } })`,
      `db.accDcCompany.findMany({ select: { id: true, name: true } })`, then assemble + cache.

    **Do NOT read `AccProjectMember.lastSignIn` or `AccDcProjectUser.lastSignIn`** — both
    live-verified 100% NULL (see objective deviation note).

    Sibling test `lib/server/signInRecencyView.test.ts` (pure assembly only, no DB): join
    resolves name/company, missing user → "Unknown user"/null lastSignIn kept, missing
    company → "Unknown company", Date → ISO string, JSON.stringify never throws.
  </action>
  <verify>
    <automated>npx vitest run lib/server/signInRecencyView.test.ts</automated>
  </verify>
  <done>Loader + pure join exist and are green; source is AccDcUser.lastSignIn via AccDcProjectUser; rows are slim + serializable.</done>
</task>

<task type="auto">
  <name>Task 2: recency bucketing + summarize transform + test</name>
  <files>app/(dashboard)/access-analysis/signInRecencyCounts.ts, app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts</files>
  <action>
    Create `app/(dashboard)/access-analysis/signInRecencyCounts.ts` (pure):

    - `RECENCY_BANDS` ordered exactly per CONTEXT.md locked decision:
      `"<30d"`, `"30–90d"`, `"90–365d"`, `">365d"`, `"Never signed in"`.
    - `bucketSignInRecency(lastSignIn: string | null, nowMs: number)` — returns one of the 5
      bands; null / undefined / unparseable dates → "Never signed in" (mirror
      `bucketActiveUserTier`'s defensive parsing, NEW band boundaries: <30 days, 30–90,
      90–365, >365). `now` is an explicit param for deterministic tests.
    - `summarizeSignInRecency(rows: SignInRecencyRow[], nowMs: number)` — returns
      `{ bands: { band, count }[] (all 5 always present, zeros kept, locked order), usersByBand: Map<band, { name, company, lastSignIn }[]> }`
      with drill rows sorted most-dormant first: oldest lastSignIn at top; "Never" rows in
      stable input order (inherently uniform per CONTEXT.md).

    Test in `__tests__/signInRecencyCounts.test.ts`: exact band boundary cases (29d vs 31d,
    89d vs 91d, 364d vs 366d), null → Never (never dropped — total of band counts ===
    rows.length), all 5 bands present when some are zero, drill sort order oldest-first.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts"</automated>
  </verify>
  <done>Bucketing matches CONTEXT.md's locked bands, null bucket explicit and lossless, deterministic under injected now.</done>
</task>

<task type="auto">
  <name>Task 3: DormantSignInChart client component + test</name>
  <files>app/(dashboard)/access-analysis/components/DormantSignInChart.tsx, app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx</files>
  <action>
    Create `"use client"` `DormantSignInChart.tsx` accepting
    `{ rows: SignInRecencyRow[]; dcCoverage?: { covered: number; total: number } }`
    (parent filters rows by project selection and passes the live dcCoverage prop in plan
    20-05 — never hardcode coverage figures). Behavior:

    - Vertical bar chart via `@/components/ui/EChart`, one bar per band in locked order;
      `summarizeSignInRecency(rows, now)` in a useMemo (capture `now` once per mount so the
      chart is render-stable).
    - "Never signed in" bar visually distinguished: muted zinc tone (e.g. a desaturated
      zinc like the existing muted palette entries; exact treatment Claude's discretion —
      must read as "different in kind" from the 4 dated bands, respecting chart-contrast
      conventions in `__tests__/chartContrast.test.ts`).
    - Clicking a band toggles a local drill (RolesPieChart local drill/setDrill pattern —
      NOT the shared sliceFilters bus): list of `name — company — formatAbsolute(lastSignIn)`
      ("Never" for null), sorted most-dormant first (already sorted by the transform).
    - Honest scope caption rendered inside the panel (muted text): when `dcCoverage` is
      provided, "Sign-in dates come from Data Connector metadata ({covered} of {total}
      projects). The live ACC API sync captures no sign-in timestamps." — live numbers only.
    - Empty state: "No sign-in data for this selection (Data Connector coverage only)."
    - Theme-resolved colors, no new WebGL, motion <=200ms.

    Test in `__tests__/DormantSignInChart.test.tsx` (existing *PieChart.test.tsx style):
    renders 5 bands from fabricated rows including nulls, drill callback shows "Never" rows,
    empty state for [], coverage caption renders the injected numbers.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx"</automated>
  </verify>
  <done>Chart renders 5 ordered bands with distinguished Never bar, band drill lists name/company/date most-dormant-first, DC-scope caption from live props; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/server/signInRecencyView.test.ts "app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx"` — all green.
- `npx tsc --noEmit` — 0 errors.
- Grep proof: no new file references `accProjectMember` (the dead field is not read);
  `AccDcUser`-sourced `lastSignIn` is the only recency source.
- The Option B deviation is recorded in 20-02-SUMMARY.md under "Deviations".
</verification>

<success_criteria>
- ENG-01 slice complete and tested standalone with a REAL recency distribution and an
  explicit, populated Never bucket (roadmap SC#4 intent preserved; named-field deviation
  documented). Live never-signed-in verification against project
  `de245cf4-30a9-422c-ad74-6f8c886605ef` (CDMX MELI PLATAH, 276 never-signed-in members)
  happens in plan 20-05's checkpoint.
- No edit to mainCharts.tsx / AccessAnalysisCharts.tsx (owned by plan 20-05).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-wins-engagement-panels/20-02-SUMMARY.md`
</output>
