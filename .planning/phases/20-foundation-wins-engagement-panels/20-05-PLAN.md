---
phase: 20-foundation-wins-engagement-panels
plan: 05
type: execute
wave: 2
depends_on: ["20-01", "20-02", "20-03", "20-04"]
files_modified:
  - app/(dashboard)/access-analysis/mainCharts.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: false
requirements: [PERM-01, ENG-01, ISSUE-01, PIPE-01]

must_haves:
  truths:
    - "All four panels render on /access-analysis in their locked positions: dormant users near Role distribution, permission footprint after the role charts, issue coverage directly above Model Coordination, ingest freshness at the very bottom"
    - "A live page load of /access-analysis shows no BigInt serialization error (roadmap SC#1 — tsc cannot catch this)"
    - "Permission footprint, dormant users, and issue coverage obey the FilterBanner project selection; ingest freshness stays account-global with its caption"
    - "mainCharts.tsx Promise.all lands at 11 entries (ISSUE-01 folded into loadCoordinationByProject) and /access-analysis load time is spot-checked before/after"
    - "The Never-signed-in bucket is verified against a real project with never-signed-in members"
  artifacts:
    - path: "app/(dashboard)/access-analysis/mainCharts.tsx"
      provides: "11-entry Promise.all fan-out + props for the 4 new panels"
      contains: "loadPermissionFootprint"
    - path: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      provides: "4 new panels mounted at locked insertion points with selection filtering"
      contains: "IssueFetchCoverageDonut"
  key_links:
    - from: "app/(dashboard)/access-analysis/mainCharts.tsx"
      to: "lib/server/permissionFootprintView.ts + signInRecencyView.ts + ingestFreshnessView.ts"
      via: "Promise.all entries 9-11"
      pattern: "loadSignInRecency|loadIngestFreshness"
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "filterRowsBySelection"
      via: "useMemo project-selection filtering for the 3 project-keyed panels"
      pattern: "filterRowsBySelection\\((permissionFootprintRows|signInRecencyRows|.*issueCoverage)"
---

<objective>
Wire the four Phase 20 panels (built standalone in plans 20-01..20-04) into the live
`/access-analysis` page: extend `mainCharts.tsx`'s loader fan-out, mount the panels at
CONTEXT.md's locked insertion points in `AccessAnalysisCharts.tsx`, wire project-selection
filtering, then run the full gate sequence and the live-page-load verification (BigInt
serialization + never-signed-in bucket + load-time spot-check) that `tsc` cannot provide.

Purpose: this is where the four requirements become user-visible workshop stories.
Output: wired page + green gates + owner-verified live render.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-foundation-wins-engagement-panels/20-CONTEXT.md
@.planning/phases/20-foundation-wins-engagement-panels/20-RESEARCH.md
@app/(dashboard)/access-analysis/mainCharts.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/access-analysis/projectFilter.ts
</context>

<interfaces>
<!-- Contracts created by the wave-1 plans (verify exact names against their SUMMARYs). -->

From lib/server/permissionFootprintView.ts (plan 20-01):
`loadPermissionFootprint(): Promise<PermissionFootprintRow[]>` — rows carry projectId (filterable).

From lib/server/signInRecencyView.ts (plan 20-02):
`loadSignInRecency(): Promise<SignInRecencyRow[]>` — rows carry projectId (filterable).

From lib/server/ingestFreshnessView.ts (plan 20-04):
`loadIngestFreshness(): Promise<IngestFreshness | null>` — account-global, NOT filtered.

From lib/server/coordinationByProjectView.ts (plan 20-03):
`CoordinationByProjectData.issueCoverage: { runStatus; runStartedAt; runFinishedAt; projects: {projectId; projectName; status; issueCount}[] } | null`
— already flows through the existing `coordinationData` prop; projects rows are filterable.

Components (plans 20-01..04): `PermissionFootprintChart { rows }`,
`DormantSignInChart { rows, dcCoverage? }`, `IssueFetchCoverageDonut` (pre-filtered rows +
run metadata per its plan), `IngestFreshnessPanel { freshness }`.

From app/(dashboard)/access-analysis/projectFilter.ts (verified):
`filterRowsBySelection<T extends { projectId: string }>(rows, selected): T[]`

Current mainCharts.tsx Promise.all (verified, 8 entries):
`loadInstanceView, loadModuleActivity, loadActivityByActor, loadCoordinationByProject,
loadProjectCoverage, loadTerrainProjects, loadActivityTimeline, loadDcCoverage`.

Verified AccessAnalysisCharts.tsx insertion landmarks: 2-col donut grid opens ~line 347
("Role distribution" is the first grid item); Model Coordination `<Reveal>` block ~lines
501-517; drawers/sheets start at `{profileEmail && ...}` ~line 519.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: mount the four panels in AccessAnalysisCharts.tsx with selection filtering</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
    Add four OPTIONAL props to `AccessAnalysisCharts` (page keeps rendering when any is
    absent — same convention as `coordinationData?`/`dcCoverage?`):
    `permissionFootprintRows?: PermissionFootprintRow[]`,
    `signInRecencyRows?: SignInRecencyRow[]`, `ingestFreshness?: IngestFreshness | null`
    (issue coverage arrives via the existing `coordinationData` prop — no new prop).

    Selection filtering (CONTEXT.md locked cross-filter scope) — three `useMemo`s using
    `filterRowsBySelection(rows, selected)` (the same picker-only pattern `moduleSummary`
    uses; do NOT extend `applySliceFilters`/`SliceFilters` — resolved Open Q3):
    permission footprint rows, sign-in recency rows, and
    `coordinationData?.issueCoverage?.projects`. Ingest freshness is NOT filtered.

    Insertion points (CONTEXT.md locked placement, landmarks verified this planning pass):
    1. **DormantSignInChart** — inside the 2-col donut grid, immediately AFTER the "Role
       distribution" `<Reveal>` item (people/engagement neighborhood). Wrap in
       `<Reveal><PremiumSurface variant="base" className="flex flex-col gap-3 p-5 overflow-hidden">`
       with a `SectionHeader` (title "Dormant users", subtitle explaining sign-in recency
       + "Click a band to see who." convention). Use `lg:col-span-2` only if the 5-bar
       chart reads cramped in one column — executor's visual judgment; pass the filtered
       rows + the `dcCoverage` prop for the honest scope caption.
    2. **PermissionFootprintChart** — new FULL-WIDTH `<Reveal><PremiumSurface>` block
       between the closing of the donut grid `</div>` and the Model Coordination block
       (access-domain slot). SectionHeader title "Permission footprint by role", subtitle
       naming the materialized summary source + "Click a role to see its projects."
    3. **IssueFetchCoverageDonut** — directly ABOVE the Model Coordination `<Reveal>`
       (trust precedes metric): its own `<Reveal><PremiumSurface>` with SectionHeader
       title "Issue data coverage", subtitle framing "how much of the issue data can we
       see into?" — rendered only when `coordinationData?.issueCoverage` exists.
    4. **IngestFreshnessPanel** — at the very bottom, after the Model Coordination block
       and BEFORE the drawers/sheets (`{profileEmail && ...}`). No PremiumSurface headline
       framing — it is a muted strip (the component owns its styling).

    Keep the zinc theme, no card-inside-card nesting, existing Reveal/PremiumSurface
    conventions, and do not touch the sliceFilters bus, StatStrip KPI wiring, or any
    existing panel.

    Extend `__tests__/AccessAnalysisCharts.test.tsx` minimally: with the new props supplied
    (fabricated rows), the four new section titles/testids render; without them, the page
    renders exactly as before (no crash, no new sections).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Four panels mount at locked positions behind optional props; 3 project-keyed panels obey the picker; existing page behavior unchanged when props absent.</done>
</task>

<task type="auto">
  <name>Task 2: wire mainCharts.tsx fan-out (8 → 11) and run the full gate sequence</name>
  <files>app/(dashboard)/access-analysis/mainCharts.tsx</files>
  <action>
    In `mainCharts.tsx`: import `loadPermissionFootprint`, `loadSignInRecency`,
    `loadIngestFreshness` from their `@/lib/server/*View` modules and append three entries
    to the existing `Promise.all` (8 → 11 — ISSUE-01 adds NOTHING here because it rides
    `loadCoordinationByProject()`, per the plan 20-03 consolidation; 11 stays under the
    ~12-entry warning threshold from PITFALLS.md Pitfall 4). Pass
    `permissionFootprintRows`, `signInRecencyRows`, `ingestFreshness` to
    `<AccessAnalysisCharts>`. Keep the fan-out flat and parallel — no waterfall.

    Add a one-line comment on the Promise.all documenting the count + the consolidation
    decision (fan-out review artifact for roadmap SC#5).

    Gates (run all, in order):
    1. `npx tsc --noEmit` — 0 errors.
    2. `npm test` — FULL suite green; confirm the pinned characterization suites
       (TEST-01 acc-hot-cache OOM guard, TEST-02 folderPermissionTerrainView golden
       masters, TEST-03 templateFolderTerrain.sharedQuery) pass and their test FILES are
       untouched: `git diff --name-only -- lib/server/__tests__/ lib/server/acc-hot-cache.test.ts` shows none of them.
    3. `node scripts/repo-map/check.cjs` — no NEW boundary warnings vs baseline (new
       imports follow the existing app→lib/server RSC pattern; client components import
       only route-local + components/ui modules).
    4. Load-time structural spot-check: the 3 new loaders are inside the single
       `Promise.all` (parallel, not sequential awaits) — record this plus the before/after
       page-load observation from Task 3 in the SUMMARY (roadmap SC#5's spot-check is an
       eyeball comparison, no formal perf harness exists — consistent with milestone
       research practice).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>11-entry parallel fan-out wired; tsc 0; full suite green; TEST-01/02/03 files untouched; repo-map check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live page-load verification (BigInt, Never bucket, honest buckets, load feel)</name>
  <action>
    Automate first, then pause for the owner: start the dev server
    (`npx next dev --turbopack -p 3100` in background — webpack dev mode is broken in this
    repo), confirm it boots cleanly, then present the how-to-verify steps below and watch
    the dev-server terminal for serialization errors while the owner loads the page.
    Do NOT touch the production `:3000` Task Scheduler service.
  </action>
  <verify>Owner responds "approved" after walking the 6 how-to-verify steps; executor captures any server-log errors observed during the session.</verify>
  <done>Owner approval recorded; no BigInt serialization error in the server log; Never bucket, 4-bucket donut, freshness strip, and load feel all confirmed on the live page.</done>
  <what-built>
    All four Phase 20 panels wired into /access-analysis: permission footprint by role
    (horizontal bars + role→project drill), dormant users by sign-in recency (5 bands incl.
    an explicit Never bucket, DC-coverage caption), issue-fetch coverage donut (4 honest
    buckets above Model Coordination), and a muted ingest-freshness strip at the bottom.
  </what-built>
  <how-to-verify>
    Start a dev server (the production `:3000` Task Scheduler service stays untouched —
    full rebuild/parity belongs to Phase 23 / gsd-self-gate):
    `npx next dev --turbopack -p 3100` (Turbopack — `next dev` webpack mode is broken in
    this repo), then open http://localhost:3100/access-analysis and check:

    1. **BigInt (roadmap SC#1):** the page renders with NO server-terminal error containing
       "serialize a BigInt" / "Do not know how to serialize". The permission-footprint bars
       show human-readable bytes (e.g. "2.7 TB" for the top role) — the executor should
       watch the dev-server terminal while you load.
    2. **Permission footprint:** top ~10 roles + "Other"; clicking a role opens its
       per-project list with folder counts + bytes; colors match the Role-distribution donut.
    3. **Never bucket (roadmap SC#4):** in the project picker, select ONLY
       "CDMX MELI PLATAH" (id de245cf4-30a9-422c-ad74-6f8c886605ef — live-verified to have
       276 never-signed-in members). The "Never signed in" bar is populated and visually
       muted; its drill shows name — company — "Never". The DC-coverage caption reads live
       numbers (~550 of 1,153), not hardcoded text.
    4. **Issue coverage donut:** sits directly above Model Coordination; shows all 4 buckets
       (with all projects selected, expect error ≈ 596 / zero_issues ≈ 437 / ok ≈ 119);
       clicking "Forbidden" or "Error" drills to the project list — the "which projects
       can't we see into?" demo moment; clicking "ok" also drills (consistent behavior).
    5. **Ingest freshness:** muted strip at the very bottom: last-run timestamps, a status
       badge that renders sensibly for the current live status (expected "quarantined" →
       caution tone, not blank), duration, projects processed, "N activity rows this run"
       (~1,086 for the current latest run), the "Account-wide" caption, and an amber stale
       badge if the run is older than ~36h.
    6. **Load feel + theme (roadmap SC#5):** page load feels comparable to pre-phase (no
       multi-second regression); zinc theme intact; panels read as their thematic
       neighborhoods, not a bolted-on section; project-picker changes refocus the three
       project-keyed panels but NOT the ingest strip.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (screenshots welcome)</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — 0 errors (before any rebuild — standing gate).
- `npm test` — full suite green; TEST-01/02/03 characterization files byte-untouched
  (`git diff --name-only` proof).
- `node scripts/repo-map/check.cjs` — no new boundary warnings.
- Live page load performed and approved by owner (BigInt + Never bucket + 4-bucket donut +
  freshness strip + load feel) — recorded in the SUMMARY with the checkpoint outcome.
- `git diff --stat` scope check: only the three files in files_modified (plus wave-1 files
  already committed); `/users/spatial-graph` and `folderPermQuery.ts`/`acc-hot-cache.ts`
  untouched by this plan.
</verification>

<success_criteria>
- Roadmap Phase 20 SC#1-5 all observable on the live page: permission reach with formatted
  bytes and no serialization error; static ingest panel with live AccActivity throughput
  (never rowsByModule); 4-honest-bucket coverage donut framing Model Coordination; dormant
  users with a real, labeled Never bucket verified on a real project (via the documented
  ENG-01 Option B source deviation); 11-entry consolidated fan-out + load-time spot-check.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-wins-engagement-panels/20-05-SUMMARY.md`
(include: fan-out before/after, checkpoint outcome, load-time observation, and the ENG-01
deviation cross-reference).
</output>
