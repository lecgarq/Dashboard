---
phase: 20-foundation-wins-engagement-panels
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/ingestFreshnessView.ts
  - lib/server/ingestFreshnessView.test.ts
  - app/(dashboard)/access-analysis/ingestFreshnessCounts.ts
  - app/(dashboard)/access-analysis/__tests__/ingestFreshnessCounts.test.ts
  - app/(dashboard)/access-analysis/components/IngestFreshnessPanel.tsx
  - app/(dashboard)/access-analysis/__tests__/IngestFreshnessPanel.test.tsx
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "Throughput ('N activity rows this run') is counted live from AccActivity by ingestRunId — rowsByModule is NEVER read (confirmed always-zero)"
    - "The panel renders latest-run started/ended, status badge, duration, projects processed as a single muted stat-tile strip — static per-page-load, no polling"
    - "A run older than the ~36h staleness threshold shows an amber stale badge; unknown status strings (e.g. 'quarantined') render a neutral badge, never blank/crash"
    - "The panel is account-global with an explicit 'account-wide' caption"
  artifacts:
    - path: "lib/server/ingestFreshnessView.ts"
      provides: "loadIngestFreshness — latest AccDcIngestRun + live AccActivity count"
      exports: ["loadIngestFreshness", "IngestFreshness"]
    - path: "app/(dashboard)/access-analysis/ingestFreshnessCounts.ts"
      provides: "staleness calc + status tone mapping + duration formatting (pure, injected now)"
      exports: ["ingestStaleness", "statusTone", "formatRunDuration"]
    - path: "app/(dashboard)/access-analysis/components/IngestFreshnessPanel.tsx"
      provides: "Muted ops-metadata strip"
      min_lines: 40
  key_links:
    - from: "lib/server/ingestFreshnessView.ts"
      to: "db.accActivity.count({ where: { ingestRunId } })"
      via: "live throughput count (indexed — schema.prisma:582)"
      pattern: "accActivity\\.count"
    - from: "app/(dashboard)/access-analysis/components/IngestFreshnessPanel.tsx"
      to: "app/(dashboard)/access-analysis/relativeTime.ts"
      via: "formatRelativeTime/formatAbsolute for started/ended timestamps"
      pattern: "relativeTime"
---

<objective>
PIPE-01 vertical slice: compact, visually secondary ingest-freshness strip — latest
`AccDcIngestRun` (started/ended, status, duration, projects processed) plus live
`AccActivity` throughput for that run. NOT yet mounted — plan 20-05 places it at the very
bottom of `/access-analysis` as muted ops metadata.

Live-verified data shape (2026-07-02): latest run status is `"quarantined"` (NOT a
success/failure 2-state enum — observed values across scripts + live DB: running, success,
partial, failed, quarantined; treat as an open string) with exactly 1,086 matching
`AccActivity` rows via `ingestRunId`.

Purpose: the presenter can answer "is the pipeline fresh?" without leaving the page, and
the always-zero `rowsByModule` telemetry gap stays un-charted per the standing guardrail.
Output: loader, pure staleness/status transform, panel component, 3 test files.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-foundation-wins-engagement-panels/20-CONTEXT.md
@.planning/phases/20-foundation-wins-engagement-panels/20-RESEARCH.md
@app/(dashboard)/access-analysis/relativeTime.ts
@components/ui/stat-tile.tsx
@app/(dashboard)/access-analysis/components/CoordinationByProject.tsx
</context>

<interfaces>
<!-- Verified schema + helpers the executor uses directly. -->

From prisma/schema.prisma (lines 812-829, verified):
```prisma
model AccDcIngestRun {
  id                String    @id @default(cuid())
  startedAt         DateTime  @default(now())
  endedAt           DateTime?
  status            String            // OPEN string: running/success/partial/failed/quarantined observed
  projectsProcessed Int       @default(0)
  rowsByModule      Json              // BANNED — confirmed always-zero, never read it
  ...
  @@index([startedAt(sort: Desc)])
}
```
`AccActivity.ingestRunId` is indexed (schema.prisma:582) — `count()` is cheap.

From app/(dashboard)/access-analysis/relativeTime.ts (verified):
```typescript
export function formatRelativeTime(iso: string | null | undefined, now?: number): string;
export function formatAbsolute(iso: string | null | undefined): string;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ingestFreshness server loader + sibling test</name>
  <files>lib/server/ingestFreshnessView.ts, lib/server/ingestFreshnessView.test.ts</files>
  <action>
    Create `lib/server/ingestFreshnessView.ts` per the verified 20-RESEARCH.md sketch
    (import "server-only"; NO TTL cache needed — CONTEXT.md wants a static per-page-load
    read, and freshness data must not be 5 minutes stale on a fresh load; a cache here
    would also be harmless, executor's call, but do not poll):

    - `interface IngestFreshness { id: string; startedAt: string; endedAt: string | null; status: string; projectsProcessed: number; activityRowCount: number }`
    - `loadIngestFreshness(): Promise<IngestFreshness | null>` —
      `db.accDcIngestRun.findFirst({ orderBy: { startedAt: "desc" }, select: { id, startedAt, endedAt, status, projectsProcessed } })`;
      return null when no run exists; then
      `db.accActivity.count({ where: { ingestRunId: run.id } })` for throughput.
      **NEVER select or read `rowsByModule`** (standing guardrail — always-zero).
      Dates → ISO strings.

    Sibling test `lib/server/ingestFreshnessView.test.ts` (mock `@/server/db` +
    `server-only` per the coordinationByProjectView.test.ts pattern): (a) returns null on
    empty DB (no findFirst result) and does not call count; (b) activityRowCount comes from
    the mocked `accActivity.count`, and the findFirst select does NOT include rowsByModule
    (assert on the calledWith select object); (c) Dates serialized to ISO strings.
  </action>
  <verify>
    <automated>npx vitest run lib/server/ingestFreshnessView.test.ts</automated>
  </verify>
  <done>Loader returns latest-run facts + live AccActivity throughput; rowsByModule provably unread; null-safe on empty DB.</done>
</task>

<task type="auto">
  <name>Task 2: staleness/status/duration pure transform + test</name>
  <files>app/(dashboard)/access-analysis/ingestFreshnessCounts.ts, app/(dashboard)/access-analysis/__tests__/ingestFreshnessCounts.test.ts</files>
  <action>
    Create `app/(dashboard)/access-analysis/ingestFreshnessCounts.ts` (pure, injected now):

    - `STALE_THRESHOLD_HOURS = 36` (Claude's-discretion value per CONTEXT.md, aligned with
      the ≤1-ingest-cycle staleness doc from Ph19 — daily cron + slack).
    - `ingestStaleness(startedAt: string, nowMs: number, thresholdHours = STALE_THRESHOLD_HOURS)`
      → `{ stale: boolean; ageHours: number }` (age from startedAt; run recency is what
      "fresh pipeline" means).
    - `statusTone(status: string)` → `{ label: string; tone: "positive" | "caution" | "negative" | "neutral" }`
      mapping the observed open-string values: success → positive; running → neutral
      ("in progress" label); partial/quarantined → caution; failed → negative; ANY
      unrecognized value → neutral with the raw string as label (Pitfall 4 — never blank,
      never crash). Add a code comment: `// VERIFY: status is an open string; no central enum exists — observed: running/success/partial/failed/quarantined`.
    - `formatRunDuration(startedAt: string, endedAt: string | null)` → "1h 23m" style, or
      "in progress" when endedAt is null.

    Test in `__tests__/ingestFreshnessCounts.test.ts`: staleness boundary at exactly 36h
    (35.9h fresh / 36.1h stale), each known status tone, unknown status ("quarantined-v2")
    → neutral with raw label, duration formatting incl. null endedAt.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/ingestFreshnessCounts.test.ts"</automated>
  </verify>
  <done>Staleness threshold crosses correctly at the boundary; status handled as an open string; deterministic under injected now.</done>
</task>

<task type="auto">
  <name>Task 3: IngestFreshnessPanel component + test</name>
  <files>app/(dashboard)/access-analysis/components/IngestFreshnessPanel.tsx, app/(dashboard)/access-analysis/__tests__/IngestFreshnessPanel.test.tsx</files>
  <action>
    Create `"use client"` `IngestFreshnessPanel.tsx` accepting
    `{ freshness: IngestFreshness | null }`. Behavior (CONTEXT.md locked look):

    - Single slim row of small stat tiles — muted ops-metadata styling (text-xs/sm,
      `text-muted-foreground`, subtle border), deliberately NOT an analytics-headline
      PremiumSurface card with SectionHeader. No chart, no run history, no polling.
    - Tiles: "Last ingest" (formatRelativeTime(startedAt) with formatAbsolute in the title
      attr), "Ended" (or "in progress"), status badge (statusTone → small pill; amber-tinted
      for caution, red for negative, emerald for positive, zinc for neutral — theme-consistent
      Tailwind tones matching existing badge styling in this route), "Duration"
      (formatRunDuration), "Projects processed" (tabular-nums), "Activity rows this run"
      (activityRowCount, tabular-nums).
    - Stale flag: when `ingestStaleness(...).stale`, show an additional amber "stale" badge
      (e.g. "last run {ageHours}h ago").
    - Explicit caption: "Account-wide — not affected by the project filter."
    - `freshness === null` → honest single line: "No Data Connector ingest runs recorded."
    - Capture `Date.now()` once per mount for staleness (render-stable, no timers).

    Test in `__tests__/IngestFreshnessPanel.test.tsx`: renders tiles from a fabricated
    fresh run (no stale badge), stale run shows amber badge, unknown status renders neutral
    badge with raw label, null freshness renders the empty line, account-wide caption present.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/IngestFreshnessPanel.test.tsx"</automated>
  </verify>
  <done>Muted slim strip renders run facts + live throughput + stale/status badges + account-wide caption; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/server/ingestFreshnessView.test.ts "app/(dashboard)/access-analysis/__tests__/ingestFreshnessCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/IngestFreshnessPanel.test.tsx"` — all green.
- `npx tsc --noEmit` — 0 errors.
- Grep proof: `rowsByModule` appears in NO new file (except the VERIFY comment string if
  the executor keeps it — acceptable; it must never be READ from the DB row).
</verification>

<success_criteria>
- PIPE-01 slice complete and tested standalone: throughput from live AccActivity counts,
  open-string status handling, ~36h stale flag, account-wide caption, no polling.
- No edit to mainCharts.tsx / AccessAnalysisCharts.tsx (owned by plan 20-05).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-wins-engagement-panels/20-04-SUMMARY.md`
</output>
