---
phase: 20-foundation-wins-engagement-panels
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/coordinationByProjectView.ts
  - lib/server/coordinationByProjectView.test.ts
  - app/(dashboard)/access-analysis/issueFetchCoverageCounts.ts
  - app/(dashboard)/access-analysis/__tests__/issueFetchCoverageCounts.test.ts
  - app/(dashboard)/access-analysis/components/IssueFetchCoverageDonut.tsx
  - app/(dashboard)/access-analysis/__tests__/IssueFetchCoverageDonut.test.tsx
autonomous: true
requirements: [ISSUE-01]

must_haves:
  truths:
    - "CoordinationByProjectData carries an additive issueCoverage field with per-project fetch results for the latest AccIssueFetchRun — existing fields and consumers unchanged"
    - "The donut always shows all 4 honest buckets (ok / zero_issues / forbidden / error) even when a bucket is 0 — nothing hidden or merged"
    - "Clicking ANY bucket (including ok and zero_issues) drills to that bucket's project list"
    - "A latest run with status 'running' is labeled as in-progress, not presented as final coverage"
  artifacts:
    - path: "lib/server/coordinationByProjectView.ts"
      provides: "Extended loader — issueCoverage sourced from AccIssueProjectFetchResult for the latest run"
      contains: "issueCoverage"
    - path: "app/(dashboard)/access-analysis/issueFetchCoverageCounts.ts"
      provides: "summarizeIssueCoverage — 4 fixed buckets from selection-filtered project rows"
      exports: ["COVERAGE_BUCKETS", "summarizeIssueCoverage"]
    - path: "app/(dashboard)/access-analysis/components/IssueFetchCoverageDonut.tsx"
      provides: "Client donut with per-bucket project drill"
      min_lines: 60
  key_links:
    - from: "lib/server/coordinationByProjectView.ts"
      to: "db.accIssueProjectFetchResult"
      via: "findMany scoped to the latest run id"
      pattern: "accIssueProjectFetchResult\\.findMany"
    - from: "app/(dashboard)/access-analysis/components/IssueFetchCoverageDonut.tsx"
      to: "@/components/ui/EChart"
      via: "canonical theme-aware chart wrapper"
      pattern: "components/ui/EChart"
---

<objective>
ISSUE-01 vertical slice: issue-fetch coverage donut with all 4 honest status buckets
(`ok` / `zero_issues` / `forbidden` / `error`) from the latest `AccIssueFetchRun` /
`AccIssueProjectFetchResult`, drillable to per-bucket project lists. NOT yet mounted —
plan 20-05 places it directly above Model Coordination ("coverage precedes metric").

**Consolidation decision (per orchestrator + 20-RESEARCH.md Open Question 2):** extend
`lib/server/coordinationByProjectView.ts` IN PLACE rather than adding a new standalone
loader — it already fetches the same latest `AccIssueFetchRun` row for its footnote, and
this keeps `mainCharts.tsx`'s `Promise.all` fan-out unchanged for ISSUE-01 (11 total after
plan 20-05, not 12). Phase 21/22 may factor out a shared issue-analytics module later.

Live-verified data shape (2026-07-02): latest run `error=596, zero_issues=437, ok=119` of
`projectsTotal=1152` — error is the LARGEST bucket; "which projects can't we see into?" is
an expected live demo moment per CONTEXT.md.

Purpose: trust framing for all issue metrics (Phase 21 builds directly beneath this donut).
Output: extended loader + tests, pure transform + test, donut component + test.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-foundation-wins-engagement-panels/20-CONTEXT.md
@.planning/phases/20-foundation-wins-engagement-panels/20-RESEARCH.md
@lib/server/coordinationByProjectView.ts
@lib/server/coordinationByProjectView.test.ts
@app/(dashboard)/access-analysis/components/ModulesPieChart.tsx
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx
</context>

<interfaces>
<!-- Verified current state the executor extends. -->

From prisma/schema.prisma (lines 871-902, verified):
```prisma
model AccIssueFetchRun {
  id String @id @default(cuid())
  startedAt DateTime @default(now())
  finishedAt DateTime?
  projectsTotal Int?; projectsOk Int?; projectsForbidden Int?
  issuesUpserted Int?; coordinationCount Int?
  status String @default("running") // running | done | failed
}
model AccIssueProjectFetchResult {
  id String @id
  runId String; projectId String
  projectName String?              // ALREADY carries the name — no join needed
  status String                    // ok | zero_issues | forbidden | error
  issueCount Int @default(0)
  ...
  @@unique([runId, projectId])
  @@index([runId, status])
}
```

From lib/server/coordinationByProjectView.ts (read in full, 56 lines):
```typescript
export interface CoordinationByProjectData {
  rows: CoordinationRow[];
  accessibleProjects: number;
  forbiddenProjects: number;
  latestRunAt: string | null;
  coordinationCount: number;
}
export async function loadCoordinationByProject(force = false): Promise<CoordinationByProjectData>;
// Internals: Promise.all([accIssue.groupBy, accProject.findMany, accIssueFetchRun.findFirst]);
// the findFirst currently selects projectsOk/projectsForbidden/startedAt/coordinationCount — NO id.
// 5-min TTL module cache.
```

Existing test `lib/server/coordinationByProjectView.test.ts` (read in full, 51 lines) mocks
`db.accIssue.groupBy`, `db.accProject.findMany`, `db.accIssueFetchRun.findFirst` via
vi.hoisted — the new `db.accIssueProjectFetchResult.findMany` mock must be added there.
This test is NOT one of the pinned characterization suites (TEST-01/02/03) — additive edits
are allowed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: extend coordinationByProjectView with additive issueCoverage + tests</name>
  <files>lib/server/coordinationByProjectView.ts, lib/server/coordinationByProjectView.test.ts</files>
  <action>
    Additive extension of `loadCoordinationByProject` — existing exported fields, query
    shapes for `accIssue.groupBy`, and the `CoordinationRow` mapping stay byte-equivalent
    in behavior (the `CoordinationByProject.tsx` consumer must not need any change):

    1. Add `id: true, status: true, finishedAt: true` to the existing
       `accIssueFetchRun.findFirst` select.
    2. After the existing Promise.all, when `run` exists, fetch
       `db.accIssueProjectFetchResult.findMany({ where: { runId: run.id }, select: { projectId: true, projectName: true, status: true, issueCount: true } })`
       (max ~1,152 rows for one run — cheap, index-covered by @@index([runId])).
    3. Extend `CoordinationByProjectData` with:
       `issueCoverage: { runStatus: string; runStartedAt: string | null; runFinishedAt: string | null; projects: { projectId: string; projectName: string; status: string; issueCount: number }[] } | null`
       — `null` when no run exists; `projectName` falls back to `"Unknown project"` when
       the stored name is null (never a raw GUID — standing guardrail). Ship the RAW
       per-project rows; bucket counting happens client-side so the donut can obey the
       FilterBanner project selection (CONTEXT.md locked cross-filter scope).
    4. Pitfall 5 (20-RESEARCH.md): do NOT special-case here beyond passing `runStatus`
       through — the client transform renders the in-progress state.

    Update `coordinationByProjectView.test.ts`: add the `accIssueProjectFetchResult.findMany`
    mock to the hoisted mocks + db mock; extend the `findRun` fixture with
    `id/status/finishedAt`; keep both existing assertions UNCHANGED; add new tests:
    (a) issueCoverage.projects populated from the mocked rows with null projectName →
    "Unknown project"; (b) `findMany` called with the latest run's id; (c) no run →
    `issueCoverage` null and findMany not called.
  </action>
  <verify>
    <automated>npx vitest run lib/server/coordinationByProjectView.test.ts "app/(dashboard)/access-analysis/__tests__/CoordinationByProject.test.tsx"</automated>
  </verify>
  <done>issueCoverage is additive (existing tests pass unchanged), per-project rows scoped to the latest run, GUIDs never surface as names.</done>
</task>

<task type="auto">
  <name>Task 2: summarizeIssueCoverage pure transform + test</name>
  <files>app/(dashboard)/access-analysis/issueFetchCoverageCounts.ts, app/(dashboard)/access-analysis/__tests__/issueFetchCoverageCounts.test.ts</files>
  <action>
    Create `app/(dashboard)/access-analysis/issueFetchCoverageCounts.ts` (pure):

    - `COVERAGE_BUCKETS = ["ok", "zero_issues", "forbidden", "error"] as const` with a
      display-label map ("Issues fetched", "Zero issues", "Forbidden", "Error" — exact copy
      Claude's discretion, honest not euphemistic).
    - `summarizeIssueCoverage(projects: { projectId; projectName; status; issueCount }[])`
      — input is the selection-filtered rows (parent applies `filterRowsBySelection`).
      Returns `{ slices: { status, label, count }[] (all 4 ALWAYS present in fixed order,
      zeros kept), projectsByStatus: Map<status, rows[]> }` with drill rows sorted by
      issueCount desc then name. Rows with an unexpected status value go into an extra
      honest bucket (label the raw status) rather than being silently dropped — the status
      column is a string, not an enum.

    Test in `__tests__/issueFetchCoverageCounts.test.ts`: all 4 buckets present when one is
    absent from input, zero counts kept, drill includes ok + zero_issues rows, unexpected
    status surfaces as its own labeled bucket, counts bounded by input rows (aggregate-shaped).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/issueFetchCoverageCounts.test.ts"</automated>
  </verify>
  <done>4 fixed honest buckets guaranteed by the pure transform regardless of input distribution.</done>
</task>

<task type="auto">
  <name>Task 3: IssueFetchCoverageDonut client component + test</name>
  <files>app/(dashboard)/access-analysis/components/IssueFetchCoverageDonut.tsx, app/(dashboard)/access-analysis/__tests__/IssueFetchCoverageDonut.test.tsx</files>
  <action>
    Create `"use client"` `IssueFetchCoverageDonut.tsx` accepting
    `{ coverage: CoordinationByProjectData["issueCoverage"]; selectedProjectIds: ReadonlySet<string> }`
    OR pre-filtered rows (executor's choice — keep filtering consistent with how plan 20-05
    wires the other panels: prefer accepting pre-filtered `projects` rows plus
    `runStatus/runStartedAt/runFinishedAt`). Behavior:

    - Donut via `@/components/ui/EChart`, following the existing donut composition style of
      `ModulesPieChart.tsx`/`RolesPieChart.tsx` (center label = covered-projects total or
      "N projects checked"). All 4 buckets in the legend, zero-count slices included.
    - Bucket colors: theme-resolved, semantically honest — ok = the palette's
      emerald/positive tone, zero_issues = neutral zinc, forbidden = amber, error = red/rose,
      consistent with tones already used in this route's palettes (`roleColors.ts` amber/rose
      precedent); must pass the chartContrast conventions.
    - Clicking ANY slice (including ok/zero_issues) toggles a local drill (RolesPieChart
      local-state pattern, not the shared sliceFilters bus): that bucket's project list
      (name + issue count for ok bucket; name + honest status note otherwise), scrollable
      for the 596-row error bucket (bounded height, overflow-y auto).
    - If `runStatus === "running"` or `runFinishedAt` is null: render an explicit
      "Issue extraction in progress — counts are partial" caption instead of presenting the
      distribution as final (Pitfall 5).
    - Freshness note: "Latest issue fetch: {formatRelativeTime(runStartedAt)}" reusing
      `../relativeTime` (same helper CoordinationByProject.tsx uses).
    - Empty states: coverage null → "No issue fetch run recorded yet."; zero rows after
      project filtering → "No fetch results for this selection."

    Test in `__tests__/IssueFetchCoverageDonut.test.tsx`: renders 4 buckets from fabricated
    rows, drill opens for ok bucket (consistent click behavior), running-state caption
    renders, empty states render.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/IssueFetchCoverageDonut.test.tsx"</automated>
  </verify>
  <done>Donut shows all 4 honest buckets with per-bucket project drill, in-progress runs labeled, freshness shown; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/server/coordinationByProjectView.test.ts "app/(dashboard)/access-analysis/__tests__/issueFetchCoverageCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/IssueFetchCoverageDonut.test.tsx" "app/(dashboard)/access-analysis/__tests__/CoordinationByProject.test.tsx"` — all green (existing coordination consumer untouched).
- `npx tsc --noEmit` — 0 errors.
- Grep proof: no new file hardcodes coverage figures (no literal 596/437/119/1152).
</verification>

<success_criteria>
- ISSUE-01 slice complete and tested standalone; roadmap SC#3's 4-bucket honesty is
  enforced by the pure transform; trust-precedes-metric placement happens in plan 20-05.
- `mainCharts.tsx` untouched (its fan-out is unchanged by ISSUE-01 — the consolidation
  point of this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-wins-engagement-panels/20-03-SUMMARY.md`
</output>
