---
phase: 21-issue-funnel-status-time
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - app/(dashboard)/access-analysis/mainCharts.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: false
requirements: [ISSUE-02, ISSUE-03]

must_haves:
  truths:
    - "First activation of the Projects tab fires loadIssueFunnel exactly once per page load (ref-flag pattern); revisits never refetch"
    - "mainCharts.tsx's eager Promise.all stays at 9 entries — the issue-funnel loader rides a function prop only"
    - "Both charts mount on the Projects tab directly below the IssueFetchCoverageDonut and above Model Coordination, filtered by the project picker only (no sliceFilters)"
    - "npm test full suite green (2392-passed baseline + new tests, 0 regressions); npx tsc --noEmit exits 0"
  artifacts:
    - path: "app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx"
      provides: "Mounted IssueTimelineChart + IssueStatusChart panels with SectionHeaders and loading skeletons"
      contains: "IssueStatusChart"
    - path: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      provides: "4th lazy fetch-once branch (tab === 'projects') + picker-filtered memos + threaded props"
      contains: "issueFunnelFetchedRef"
    - path: "app/(dashboard)/access-analysis/mainCharts.tsx"
      provides: "loadIssueFunnelAction passed as function prop (NOT a Promise.all entry)"
      contains: "loadIssueFunnel={loadIssueFunnelAction}"
  key_links:
    - from: "app/(dashboard)/access-analysis/mainCharts.tsx"
      to: "app/(dashboard)/access-analysis/issueFunnelActions.ts"
      via: "function-prop import"
      pattern: "loadIssueFunnelAction"
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "loadIssueFunnel prop"
      via: "lazy useEffect branch keyed to tab === \"projects\""
      pattern: "tab === \"projects\""
    - from: "app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx"
      to: "components/IssueTimelineChart.tsx + components/IssueStatusChart.tsx"
      via: "JSX mount between IssueFetchCoverageDonut and CoordinationByProject"
      pattern: "IssueTimelineChart"
---

<objective>
Wire the phase end-to-end: pass `loadIssueFunnelAction` down as a function prop from
`mainCharts.tsx` (eager fan-out stays 9), add the 4th lazy fetch-once branch to
`AccessAnalysisCharts.tsx`'s existing per-tab `useEffect` (keyed `tab === "projects"`),
compute the picker-filtered memos, and mount both charts in `ProjectsTabPanel.tsx` directly
below the coverage donut. Then run the full gate set and hand to the owner for the live
Projects-tab check.

Purpose: this is where ISSUE-02/03 become user-visible on `/access-analysis`. Workshop
impact: the presenter shows the full issue picture — volume over time and current status
mix — immediately beneath the coverage donut that frames how trustworthy the picture is.
Output: 3 modified source files + updated shell test; full suite green.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-issue-funnel-status-time/21-CONTEXT.md
@app/(dashboard)/access-analysis/mainCharts.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
@app/(dashboard)/access-analysis/components/RolesTabPanel.tsx

<interfaces>
<!-- Verified from repo files / created by earlier waves. -->

From plan 21-01:
```typescript
// app/(dashboard)/access-analysis/issueFunnelActions.ts
export async function loadIssueFunnelAction(): Promise<IssueFunnelData | null>;
// lib/server/issueFunnelView.ts
export interface IssueFunnelData { monthRows: IssueFunnelMonthRow[]; statusRows: IssueFunnelStatusRow[] }
// both row types carry { projectId; projectName; ...; count } — filterRowsBySelection-compatible
```

From plan 21-03 (exact mount contracts):
```typescript
<IssueTimelineChart summary={TimelineSummary} coverageProjects={IssueCoverageInputRow[]} />
<IssueStatusChart rows={IssueFunnelStatusRow[]} coverageProjects={IssueCoverageInputRow[]} />
```

Existing pieces (verified this session):
- `AccessAnalysisCharts.tsx` lines 251-283: the three lazy state/ref/effect branches to
  extend (activityRecency/permissionLevel/folderScopedActivity) — the 4th branch copies
  this shape exactly.
- `AccessAnalysisCharts.tsx` lines 300-303: `filteredIssueCoverageProjects` already computed
  and threaded to `ProjectsTabPanel` — reuse as-is for both charts' `coverageProjects`.
- `AccessAnalysisCharts.tsx` line 20: `summarizeActivityTimeline` already imported in the
  shell (used for the Overview timeline) — reuse for the issue timeline summary.
- `ProjectsTabPanel.tsx` lines 80-82: the placeholder comment marking the locked mount
  point ("Ph21-22 issue funnel / issue-type charts land here").
- `RolesTabPanel.tsx` lines 135-160: the presence-gated panel + `DonutPanelSkeleton`
  loading convention (`import { DonutPanelSkeleton } from "./DonutSkeletons"`).
- `mainCharts.tsx` lines 105-107: where sibling lazy actions are passed as function props.
- Picker-only filtering (locked): use `selected` directly — NOT `sliceFilteredProjectIds`
  (that would wire the sliceFilters bus in through the back door).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the lazy loader through mainCharts + AccessAnalysisCharts</name>
  <files>app/(dashboard)/access-analysis/mainCharts.tsx, app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx</files>
  <action>
`mainCharts.tsx`: import `{ loadIssueFunnelAction } from "./issueFunnelActions"` and pass
`loadIssueFunnel={loadIssueFunnelAction}` to `<AccessAnalysisCharts>` alongside the existing
lazy actions (lines 105-107). Do NOT add anything to the `Promise.all` — the fan-out comment
block (lines 33-46) should gain one sentence noting the Phase 21 issue-funnel loader also
rides the lazy per-tab path.

`AccessAnalysisCharts.tsx`:
1. Prop: `loadIssueFunnel?: () => Promise<IssueFunnelData | null>` (type import
   `import type { IssueFunnelData, IssueFunnelStatusRow } from "@/lib/server/issueFunnelView"`),
   doc-commented like the sibling lazy props ("Phase 21 ISSUE-02/03: lazy per-tab fetch
   (Projects tab), fired at most once. Presence gates both issue-funnel panels.").
2. State trio + ref, mirroring lines 251-259:
   `issueFunnelData` (`IssueFunnelData | null`), `issueFunnelLoading` (boolean),
   `issueFunnelFetchedRef` (useRef(false)).
3. 4th branch inside the EXISTING lazy-fetch `useEffect` (lines 261-283), keyed
   `tab === "projects"`, ref-flag gated (set the ref BEFORE the await so a no-session null
   never refetches), `.then(setIssueFunnelData).finally(() => setIssueFunnelLoading(false))`.
   Add `loadIssueFunnel` to the effect's dependency array.
4. Picker-only memos (place beside the existing filtered memos, lines 288-303):
   - `const issueTimelineSummary = useMemo(() => summarizeActivityTimeline(issueFunnelData?.monthRows ?? [], selected), [issueFunnelData, selected]);`
     — `selected`, NOT `sliceFilteredProjectIds` (locked picker-only decision; add a
     one-line comment saying so, since the Overview timeline two memos up deliberately
     differs).
   - `const filteredIssueStatusRows = useMemo(() => filterRowsBySelection(issueFunnelData?.statusRows ?? [], selected), [issueFunnelData, selected]);`
5. Thread to `<ProjectsTabPanel>`: `loadIssueFunnel`, `issueFunnelLoading`,
   `issueTimelineSummary`, `filteredIssueStatusRows` (panel props defined in Task 2 as
   optional, so this compiles once both tasks land — implement Task 1 and Task 2 in the
   same working session, committing after both typecheck).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Fan-out count in mainCharts.tsx Promise.all unchanged at 9; 4th lazy branch present with ref-flag; both memos picker-only; props threaded.</done>
</task>

<task type="auto">
  <name>Task 2: Mount both charts in ProjectsTabPanel + pin the lazy-fetch behavior in tests</name>
  <files>app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx, app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
`ProjectsTabPanel.tsx`: add optional props (backward-compatible — existing tests keep
passing without them):
```typescript
loadIssueFunnel?: () => Promise<IssueFunnelData | null>; // presence gates both panels
issueFunnelLoading?: boolean;
issueTimelineSummary?: TimelineSummary;
filteredIssueStatusRows?: IssueFunnelStatusRow[];
```
Replace the lines-80-82 placeholder comment with the two mounted panels, inserted BETWEEN
the IssueFetchCoverageDonut block and the Model Coordination block (locked: "directly below
the coverage donut", coverage precedes metric; the donut frames trust for both). Layout
discretion RESOLVED: two full-width stacked `Reveal > PremiumSurface variant="base"` panels
matching this tab's existing `flex flex-col gap-6` rhythm — Phase 22's issues-by-type chart
appends as a third sibling with zero redesign. Each panel:
- gates on `loadIssueFunnel` presence, renders `<DonutPanelSkeleton />` while
  `issueFunnelLoading` (the RolesTabPanel lines 135-160 convention; import from
  `./DonutSkeletons`);
- `SectionHeader` with question-stating copy (20.1-07 convention, one quiet line —
  suggested, minor wording at Claude's discretion):
  - Timeline: title "Issues over time", subtitle "When are issues actually being raised?
    Every ACC issue by created month — sustained climbs and spikes mark heavy review
    pushes."
  - Status: title "Issues by status", subtitle "Where does the issue pile sit right now?
    All fetched issues by their current ACC status, shown exactly as ACC reports them."
- bodies: `<IssueTimelineChart summary={issueTimelineSummary ?? EMPTY} coverageProjects={filteredIssueCoverageProjects} />`
  and `<IssueStatusChart rows={filteredIssueStatusRows ?? []} coverageProjects={filteredIssueCoverageProjects} />`
  — for the timeline, when `issueTimelineSummary` is undefined pass an empty summary
  (`{ points: [], total: 0, peak: null, busiestYear: null, span: null }`) so the component's
  own empty state renders (never a blank pane).

`__tests__/AccessAnalysisCharts.test.tsx`: first `grep -n "mouseDown\|loadPermissionLevel\|fetch-once"`
in this file to locate the existing lazy-fetch test cases (20.1-06 added them), then add a
mirrored case: render the shell with a `loadIssueFunnel` mock resolving
`{ monthRows: [], statusRows: [] }`, `fireEvent.mouseDown` the Projects tab trigger (Radix
Tabs activates on mouseDown — 20.1-05 deviation note; user-event is NOT installed), assert
the mock fired exactly once, switch away and back, assert still exactly once (ref-flag
pin). If existing ProjectsTabPanel-related cases break on the new markup, update them —
but the new props are optional, so breakage should be nil; investigate before "fixing".
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Both panels mounted at the locked position with skeleton/gating; lazy fetch-once pinned by a shell test; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate sweep</name>
  <files>none (verification only)</files>
  <action>
Run the phase gate set in order:
1. `npx tsc --noEmit` — must exit 0.
2. `npm test` — full Vitest suite (baseline 2392 passed / 1 skipped, per STATE.md). Zero
   regressions tolerated; the suite should GROW by this phase's new test files (loader,
   transforms, two components, shell case). If `UsersDirectoryClient.integration.test.tsx`
   flakes, re-run it in isolation before judging (known pre-existing isolation issue,
   deferred-items.md).
3. Scope proof for the summary: `git log --oneline` for this phase's commits +
   `git diff <phase-start>..HEAD --name-only` — confirm only this phase's planned files
   changed, `/users/spatial-graph` untouched, no new npm dependency in package.json, no
   WebGL import added under app/(dashboard)/access-analysis.
Record exact pass counts in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>tsc exit 0; full suite green with new tests counted; scope proof recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — owner live check on the Projects tab (production-build preflight)</name>
  <action>
Automate everything first: run the webpack production-build preflight (`npx tsc --noEmit`
already green from Task 3; `next build` to an isolated dist such as `.next-uat-21`, then
`next start` on `:3100` per `references/deploy-sequence.md` — NEVER build into the live
`.next` while `:3000` is running), confirm `/access-analysis` returns 200, then present the
verification steps below to the owner and pause.
  </action>
  <what-built>
ISSUE-02 + ISSUE-03 live on the /access-analysis Projects tab: an issues-over-time monthly
area line (zoom brush, peak pin, no YoY) and an issues-by-status donut (all 8 ACC statuses,
click-to-drill per-project lists), both directly below the Issue data coverage donut, both
carrying live coverage subtitles and honest empty states, lazily fetched on first tab visit.
  </what-built>
  <how-to-verify>
`next dev` is broken both ways on this machine (STATE.md: webpack 500s, turbopack corrupts
CSS deterministically) — use the 20.1 production-build preflight per
`.claude/skills/lecg-dashboard/references/deploy-sequence.md`: webpack `next build` to an
isolated dist (e.g. `.next-uat-21`) + `next start` on `:3100`. `:3000` stays untouched.

Then on `/access-analysis`:
1. Open the Projects tab — both new panels appear below "Issue data coverage" and above
   "Model Coordination", skeleton first, then charts (lazy fetch).
2. Timeline: hover months (no YoY line in the tooltip), drag the zoom brush, find the peak
   pin; subtitle under the chart shows live coverage numbers.
3. Status donut: all 8 statuses listed even at 0; click a slice or legend row → per-project
   drill list opens in place (page does NOT cross-filter); click again → closes. No raw
   GUIDs anywhere.
4. Narrow the project picker to a project with zero issues (e.g. a "Zero issues" project
   from the coverage donut's drill) → both charts show "No issues for this view" with the
   coverage-context line. Pick a forbidden/error project → the empty state says data is
   unavailable, not zero.
5. Zinc dark theme intact; switch tabs away/back — no refetch flash, no scroll jump.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>Owner responds "approved" (or all raised issues are resolved and re-checked).</verify>
  <done>Owner has seen both charts live on the `:3100` preflight build and approved; `:3000` untouched throughout.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exit 0 BEFORE any build (standing gate).
- `npm test` full suite green — baseline 2392 passed / 1 skipped + this phase's new tests.
- mainCharts.tsx `Promise.all` still has exactly 9 entries.
- `grep -rn "onSliceClick\|activeSlice" app/(dashboard)/access-analysis/components/IssueStatusChart.tsx` → empty.
- Explicit-path commits with `git diff --cached --name-only` proof (heavy WIP branch).
- Owner checkpoint passed on the `:3100` production-build preflight; `:3000` untouched.
</verification>

<success_criteria>
- All 4 roadmap success criteria for Phase 21 observably true on the Projects tab:
  monthly timeline with live coverage caption (1), 8-status chart with drill (2), explicit
  coverage-aware empty states (3), aggregate-bound loader test + green suite + clean tsc (4).
- Eager fan-out unchanged; lazy fetch-once pinned by test; picker-only filtering; zero
  hardcoded coverage figures; zero raw GUIDs; no new WebGL/deps; /users/spatial-graph untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/21-issue-funnel-status-time/21-04-SUMMARY.md`
</output>
