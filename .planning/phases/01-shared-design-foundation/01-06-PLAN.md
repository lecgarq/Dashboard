---
phase: 01-shared-design-foundation
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - components/ui/DrillSheet.tsx
  - components/ui/__tests__/DrillSheet.test.tsx
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "A single shared slide-in Sheet panel mounts and is importable as the one drill-target for all four pages"
    - "It slides from the right at ~480px width with externally-controlled open/close"
    - "Arbitrary children render inside it (empty shell — no content sources wired)"
  artifacts:
    - path: "components/ui/DrillSheet.tsx"
      provides: "Shared right-slide ~480px drill panel shell (INT-01)"
      exports: ["DrillSheet"]
      min_lines: 30
    - path: "components/ui/__tests__/DrillSheet.test.tsx"
      provides: "open/closed render + width + children + onClose assertions"
  key_links:
    - from: "components/ui/DrillSheet.tsx"
      to: "components/ui/sheet.tsx (radix Dialog)"
      via: "wraps Sheet + SheetContent with side=right and a 480px width override"
      pattern: "SheetContent"
    - from: "components/ui/DrillSheet.tsx"
      to: "components/ui/PremiumSurface.tsx"
      via: "applies premium glass styling to the sheet content surface"
      pattern: "PremiumSurface|glass"
---

<objective>
Create the shared slide-in `DrillSheet` shell at `components/ui/DrillSheet.tsx` (INT-01): one externally-controlled right-slide panel at ~480px (medium-wide) that becomes THE single drill-target every page phase mounts. Built as an EMPTY shell here — it renders arbitrary `children` and has NO content sources wired (CONTEXT: "single shell is THE drill-target for all four pages. Built empty here; sources wired per-page later").

Purpose: Author the one drill panel once so /users, /access-analysis, /template-mty, /forma-proposal all open the same panel (person profile, people-behind-a-segment, role members) instead of each owning a local Sheet. This is the final foundation piece; downstream phases pass `<UserProfilePanel>` or list content as children.
Output: `components/ui/DrillSheet.tsx` + colocated test. SCOPE GUARD (roadmap): build ONLY the empty shell — wire NO drill sources, NO content. Depends on the depth tokens (plan 01) and PremiumSurface (plan 03) for the premium glass content styling.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-design-foundation/01-CONTEXT.md
@.planning/phases/01-shared-design-foundation/01-RESEARCH.md

# Contract: RESEARCH.md §5. The existing shadcn Sheet (radix Dialog) to wrap — note its
# default side="right" + sm:max-w-sm width that must be overridden to 480px, and that it
# imports `Dialog as SheetPrimitive from "radix-ui"` (the unified package, NOT @radix-ui/*):
@components/ui/sheet.tsx
@lib/core/utils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DrillSheet empty shell</name>
  <files>components/ui/DrillSheet.tsx, components/ui/__tests__/DrillSheet.test.tsx</files>
  <behavior>
    - When `open` is true, the sheet content is in the document; when false, it is not (controlled by the external `open` prop).
    - Triggering close (overlay click / Escape / the built-in close button) calls `onClose`.
    - The content panel slides from the RIGHT and is ~480px wide — the width override resets the shadcn `sm:max-w-sm` default (RESEARCH Pitfall 7: pass `className="w-[480px] sm:w-[480px]"` via `cn()`).
    - When `title`/`description` are provided, they render in a SheetHeader; both are optional.
    - Arbitrary `children` render inside the content area (empty shell — accepts anything).
    - The content surface carries premium glass styling (PremiumSurface `glass` variant or matching surface tokens — CONTEXT balanced premium / frosted glass default).
  </behavior>
  <action>
Create `components/ui/DrillSheet.tsx` with `"use client"` (Sheet manages open/close state — RESEARCH §5). Props: `{ open: boolean; onClose: () => void; title?: string; description?: string; children: React.ReactNode }`.

Compose the EXISTING `Sheet` + `SheetContent` from `@/components/ui/sheet` (do NOT re-implement the Sheet or import radix directly — sheet.tsx already wraps `radix-ui`). Wire `Sheet`'s `open={open}` and `onOpenChange={(o) => { if (!o) onClose(); }}` so overlay/Escape/close-button all route to `onClose`. Fix `side="right"`. Override the width to ~480px via `cn("w-[480px] sm:w-[480px]", ...)` on `SheetContent` (RESEARCH Pitfall 7 — `sm:w-[480px]` is required to beat the `sm:max-w-sm` default). Render an optional `SheetHeader` with `SheetTitle`/`SheetDescription` only when `title`/`description` are supplied. Render `children` in the body.

Apply premium glass styling to the content surface using PremiumSurface (`variant="glass"`) wrapping the body, or equivalently the glass surface tokens, so the panel reads as the premium frosted surface (CONTEXT). Keep the shadcn default open/close slide animation (CONTEXT: "shadcn Sheet default animation is acceptable"); optionally trim the 500ms open to ~350ms via a `data-[state=open]:duration-350` className to stay near the smooth-flowing budget (RESEARCH §5 — optional).

This is an EMPTY shell: do NOT import or render any page-specific content (no `UserProfilePanel`, no lists) — `children` is the only content seam. Do NOT wire any trigger or drill source (per-page phases own those).

Write the test first at `components/ui/__tests__/DrillSheet.test.tsx` (`// @vitest-environment jsdom`, `@testing-library/react`). Assert: with `open={false}` the children/content are not rendered; with `open={true}` the children render and the content node carries the `w-[480px]` width class and the right-side slide class; clicking the close affordance (or firing the close) calls the `onClose` spy; `title` renders into the header when provided. Avoid jest-dom matchers — assert via `queryByText`/`container.querySelector` + `className.includes` (MEMORY). Radix Dialog may need a portal/`ResizeObserver`/`IntersectionObserver` stub already present in `vitest.setup.ts` — reuse the existing global stubs (TESTING.md).
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run components/ui/__tests__/DrillSheet.test.tsx && npx tsc --noEmit && echo DRILLSHEET_OK</automated>
  </verify>
  <done>`components/ui/DrillSheet.tsx` exports a right-slide ~480px externally-controlled `DrillSheet` that renders arbitrary children, calls `onClose` on dismiss, applies premium glass styling, wires NO content sources; test passes and `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Empty panel shell; renders caller children, no input parsing, no network. Radix Dialog provides focus-trap / Escape / aria (RESEARCH "Don't Hand-Roll"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | DrillSheet children | accept | The shell renders trusted first-party content passed by page phases; no data fetching or input parsing happens in the shell itself. |
| T-01-06b | Tampering | focus/keyboard handling | mitigate | Reuse shadcn/radix Dialog (focus-trap, Escape-to-close, aria) rather than hand-rolling — accessibility/keyboard correctness is delegated to the audited primitive. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs — uses the already-present radix `Sheet`; new files only. |
</threat_model>

<verification>
- `npx vitest run components/ui/__tests__/DrillSheet.test.tsx` passes.
- `npx tsc --noEmit` exits 0 — this is the phase-closing gate (success criterion 5: "a single shared slide-in Sheet panel mounts and is importable … `npx tsc --noEmit` exits 0").
- Manual (deferred to Phase 7): the panel slides from the right at ~480px, leaves the page visible behind it, and dismisses on overlay/Escape in both themes.
</verification>

<success_criteria>
- A single shared slide-in `Sheet` panel mounts and is importable as the one drill-target for all four pages; `npx tsc --noEmit` exits 0 (INT-01, success criterion 5).
- Right-slide, ~480px, externally controlled, premium glass styling, empty shell — no drill sources wired (CONTEXT + roadmap scope guardrail).
</success_criteria>

<output>
Create `.planning/phases/01-shared-design-foundation/01-06-SUMMARY.md` when done.
</output>
