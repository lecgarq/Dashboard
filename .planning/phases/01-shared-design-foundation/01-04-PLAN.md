---
phase: 01-shared-design-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/colors/echartsTheme.ts
  - components/ui/EChart.tsx
  - components/ui/__tests__/EChart.test.tsx
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "A single shared EChart wrapper auto-applies the correct light/dark palette via resolvedTheme"
    - "The chart remounts cleanly on theme switch via key={resolvedTheme}"
    - "Consumers no longer need their own useTheme call to theme axis/text colors"
  artifacts:
    - path: "lib/colors/echartsTheme.ts"
      provides: "Canonical ECHARTS_LIGHT / ECHARTS_DARK palettes + mergeEChartsTheme pure fn"
      exports: ["ECHARTS_LIGHT", "ECHARTS_DARK", "mergeEChartsTheme"]
    - path: "components/ui/EChart.tsx"
      provides: "Theme-aware ECharts wrapper (canonical location)"
      exports: ["EChart"]
      min_lines: 25
    - path: "components/ui/__tests__/EChart.test.tsx"
      provides: "Theme-injection + key-remount assertions"
  key_links:
    - from: "components/ui/EChart.tsx"
      to: "lib/colors/echartsTheme.ts"
      via: "calls mergeEChartsTheme(option, dark) to inject axis/text colors"
      pattern: "mergeEChartsTheme"
    - from: "components/ui/EChart.tsx"
      to: "next-themes useTheme"
      via: "reads resolvedTheme once and passes key={resolvedTheme} for clean remount"
      pattern: "resolvedTheme"
---

<objective>
Create the canonical theme-aware `EChart` wrapper at `components/ui/EChart.tsx` plus a pure palette module `lib/colors/echartsTheme.ts` (FND-03). The wrapper reads `resolvedTheme` from next-themes ONCE, injects the correct axis/text/tooltip colors via a pure `mergeEChartsTheme(option, dark)` function, and forces a clean canvas remount on theme switch via `key={resolvedTheme}` — eliminating the 11 per-component `useTheme` call sites' drift (RESEARCH.md §3).

Purpose: One wrapper themes every chart consistently in both light and dark (CONTEXT: both themes equal). Charts keep their own multi-color data palettes — this wrapper only handles axis/text/tooltip chrome, not data-series recoloring (CONTEXT: indigo→violet is the accent/glow color, "does not recolor data series").
Output: `lib/colors/echartsTheme.ts`, `components/ui/EChart.tsx`, colocated test. SCOPE GUARD: this plan ONLY creates the canonical wrapper. It does NOT migrate the 11 existing `useTheme` chart call sites and does NOT delete the old `app/(dashboard)/access-analysis/components/EChart.tsx` (that migration is Phase 5 — RESEARCH.md §3 NOTE + Pitfall 1 + Pitfall 6).
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-design-foundation/01-CONTEXT.md
@.planning/phases/01-shared-design-foundation/01-RESEARCH.md

# Contract + canonical palette values: RESEARCH.md §3 + ARCHITECTURE.md §2c/§4a + STACK.md §1.
# The current theme-blind wrapper (props shape to preserve: option, height, onEvents, notMerge):
@app/(dashboard)/access-analysis/components/EChart.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure ECharts palette + mergeEChartsTheme</name>
  <files>lib/colors/echartsTheme.ts, components/ui/__tests__/EChart.test.tsx</files>
  <behavior>
    - `ECHARTS_DARK` and `ECHARTS_LIGHT` are exported palette constants with `text`, `axis`, `tooltipBg`, `tooltipBorder` (and a `chart` series array) using the canonical values from RESEARCH.md §3: dark text `#A1A1AA`/`#E4E4E7`/`#FAFAFA`, dark axis `#3F3F46`, dark tooltip bg `rgba(24,24,27,0.96)` border `#3F3F46`; light text `#374151`/`#111827`, light axis `#E5E7EB`, light tooltip bg `rgba(255,255,255,0.98)` border `#E5E7EB`.
    - `mergeEChartsTheme(option, dark)` is PURE (no React, no DOM, no getComputedStyle — RESEARCH "Don't Hand-Roll": getComputedStyle on canvas returns empty): given an EChartsOption and a `dark` boolean, returns a new option with axis/text/tooltip chrome colors injected from the matching palette, WITHOUT overwriting any caller-provided series `itemStyle`/data colors.
    - Calling it twice with the same args is referentially stable in output shape (idempotent).
  </behavior>
  <action>
Create `lib/colors/echartsTheme.ts` (pure TS, importable from RSC or client — RESEARCH Architectural Responsibility Map). Export `ECHARTS_DARK`, `ECHARTS_LIGHT`, and `mergeEChartsTheme(option, dark)`.

`mergeEChartsTheme` injects: `textStyle.color`, `xAxis`/`yAxis` `axisLabel.color` + `splitLine.lineStyle.color`, `legend.textStyle.color`, and `tooltip.backgroundColor`/`borderColor` from the selected palette — merging shallowly so any caller-set value wins where present, and NEVER touching `series[].itemStyle` or `series[].data` colors (data palettes stay caller-owned). Keep it a pure function (no imports from React/next-themes/DOM).

Write the test first at `components/ui/__tests__/EChart.test.tsx` (this file will also cover the wrapper in Task 2 — start the `describe("mergeEChartsTheme")` block here). Use the colocated `__tests__` + `// @vitest-environment jsdom` convention. Assert: dark palette injects the dark text/axis colors; light palette injects the light ones; a caller-provided `series[0].itemStyle.color` survives untouched through the merge.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run components/ui/__tests__/EChart.test.tsx -t "mergeEChartsTheme" && echo MERGE_OK</automated>
  </verify>
  <done>`lib/colors/echartsTheme.ts` exports `ECHARTS_LIGHT`, `ECHARTS_DARK`, `mergeEChartsTheme`; the pure merge injects axis/text/tooltip chrome from the correct palette and leaves caller series colors intact; the `mergeEChartsTheme` tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme-aware EChart wrapper at canonical location</name>
  <files>components/ui/EChart.tsx, components/ui/__tests__/EChart.test.tsx</files>
  <behavior>
    - Renders `ReactECharts` with the option produced by `mergeEChartsTheme(option, dark)`.
    - Reads `resolvedTheme` once via next-themes; computes `dark = resolvedTheme !== "light"` (defaults dark before resolve — RESEARCH Pitfall 4, avoids a dark→light flash for dark users).
    - Passes `key={resolvedTheme}` to force a clean remount on theme switch (RESEARCH §3 step 3 + STACK §1; prevents stale canvas state).
    - Preserves the existing wrapper's prop surface and defaults: `option`, `height = 280`, `onEvents`, `notMerge = true`; plus accepts an optional `className` for layout.
    - `notMerge` (within-theme update behavior) and `key` (cross-theme remount) coexist — both are passed (RESEARCH §3 "notMerge vs key").
  </behavior>
  <action>
Create `components/ui/EChart.tsx` (the NEW canonical location — RESEARCH Pitfall 1: create ONLY here, do NOT recreate inside `access-analysis/components/`). `"use client"` is required (uses the `useTheme` client hook).

Import `useTheme` from `next-themes`, `ReactECharts` from `echarts-for-react`, and `mergeEChartsTheme` from `@/lib/colors/echartsTheme`. Keep the prop interface a superset of the old wrapper: `{ option: EChartsOption; height?: number; onEvents?: ...; notMerge?: boolean; className?: string }`. Compute `dark = resolvedTheme !== "light"`, call `mergeEChartsTheme(option, dark)`, render `<ReactECharts key={resolvedTheme} option={themed} style={{ height, width: "100%" }} opts={{ renderer: "canvas" }} notMerge={notMerge} lazyUpdate onEvents={onEvents} className={className} />`.

CRITICAL — leave the OLD file `app/(dashboard)/access-analysis/components/EChart.tsx` and its test `app/(dashboard)/access-analysis/__tests__/EChart.test.tsx` completely untouched (RESEARCH Pitfall 1 + Pitfall 6: the old test imports the old path; moving/deleting it breaks the whole-tree `tsc`). The 11 chart call-site migrations are Phase 5, OUT OF SCOPE here.

Extend `components/ui/__tests__/EChart.test.tsx` with a `describe("EChart")` block. Mock `echarts-for-react` (TESTING.md pattern: `vi.mock("echarts-for-react", () => ({ default: (props) => <div data-testid="echart" /> }))`) and mock/stub `next-themes` `useTheme` to return a controllable `resolvedTheme`. Assert: the wrapper passes a `key` equal to `resolvedTheme` (re-render with a different resolvedTheme yields a different key → remount); the rendered option carries the dark chrome colors when `resolvedTheme==="dark"` and light when `"light"`. Avoid jest-dom matchers (MEMORY).
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run components/ui/__tests__/EChart.test.tsx && npx tsc --noEmit && echo ECHART_OK</automated>
  </verify>
  <done>`components/ui/EChart.tsx` exports a theme-aware `EChart` that applies the palette via `mergeEChartsTheme` and remounts via `key={resolvedTheme}`; the old access-analysis EChart + its test are untouched; the new test passes and `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Chart option objects come from trusted page/view code; the wrapper renders to a canvas, no input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | EChart `option` / `onEvents` | accept | Options and event handlers are supplied by trusted first-party page code, not user input. ECharts renders to canvas (no DOM injection). |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses already-present echarts-for-react / next-themes; new files only. |
</threat_model>

<verification>
- `npx vitest run components/ui/__tests__/EChart.test.tsx` passes (both the merge and wrapper describe blocks).
- `npx tsc --noEmit` exits 0 — confirms the old EChart + old test still typecheck (Pitfall 6).
- The old `app/(dashboard)/access-analysis/components/EChart.tsx` is byte-identical to before (no migration in this phase).
</verification>

<success_criteria>
- A single shared `EChart` wrapper auto-applies the correct palette via `resolvedTheme` and remounts cleanly on theme switch (FND-03, success criterion 3).
- Charts keep their own data palettes; only chrome is themed (CONTEXT: accent does not recolor data series).
- No call-site migration, no old-file deletion (scope guardrail).
</success_criteria>

<output>
Create `.planning/phases/01-shared-design-foundation/01-04-SUMMARY.md` when done.
</output>
