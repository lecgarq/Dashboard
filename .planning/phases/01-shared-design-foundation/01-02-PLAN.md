---
phase: 01-shared-design-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [VIS-06]
must_haves:
  truths:
    - "framer-motion resolves to 12.39.0 or newer (React 19 reorder layout-animation fix)"
    - "The existing test suite still passes after the bump (no API regression)"
  artifacts:
    - path: "package.json"
      provides: "framer-motion dependency pinned to ^12.39.0"
      contains: "framer-motion"
  key_links:
    - from: "package.json framer-motion ^12.39.0"
      to: "node_modules/framer-motion"
      via: "npm install resolves the installed version to >=12.39.0"
      pattern: "12\\.(39|[4-9][0-9])"
---

<objective>
Bump `framer-motion` from `^12.38.0` to `^12.39.0` (VIS-06 / RESEARCH.md §10). The 12.39.0 release fixed "Preserve in-flight motion value animations across React 19 reorder unmount/remount" — without it, the Phase 2/4 `/users` table layout animations flicker on React 19's concurrent reorder. This is a preventive upstream bump so the motion facade (plan 05) and all downstream motion work build on the fixed version.

Purpose: Establish the correct framer-motion baseline before any motion facade work. The API is stable across 12.x — no code migration needed.
Output: Updated `package.json` + `package-lock.json`; no source changes.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-design-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump framer-motion to ^12.39.0</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm install framer-motion@^12.39.0` from the repo root (C:/LECG/Dashboard). This updates the `dependencies` entry in `package.json` from `^12.38.0` to `^12.39.0` and rewrites the resolved version in `package-lock.json`.

This is a same-major (12.x) version bump of an already-installed, already-relied-on dependency — NOT a new package. No legitimacy checkpoint is required (the package is already trusted and present in the lockfile). The 12.x API (`motion`, `AnimatePresence`, `useReducedMotion`, `layout`, `layoutId`, `whileInView`) is stable across 12.38 to 12.39; per RESEARCH.md §10 and STACK.md §6, no migration is needed.

After install, confirm the resolved version is 12.39.0 or higher (the verify command below asserts this programmatically).

Staging note (project constraint): stage ONLY `package.json` and `package-lock.json` by explicit path. This branch carries large uncommitted WIP — never `git add -A` or `git add .` (STATE.md blocker; surgical-staging rule). Run `git diff --cached --name-only` before committing to confirm only these two files are staged.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && node -e "const v=require('framer-motion/package.json').version; const [maj,min]=v.split('.').map(Number); if(maj>12||(maj===12&&min>=39)){console.log('FM_OK '+v)}else{console.error('FM_TOO_OLD '+v);process.exit(1)}"</automated>
  </verify>
  <done>`package.json` lists framer-motion at `^12.39.0`; the installed `framer-motion/package.json` version is >= 12.39.0; only `package.json` + `package-lock.json` are staged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no API regression</name>
  <files>package-lock.json</files>
  <action>
Run the unit test suite (`npm test`) to confirm the bump introduces no regression in the components that already import framer-motion directly (e.g. `components/ui/animated-list.tsx`, `components/ui/PageTransition.tsx`, `components/ui/stat-tile.tsx`). No code changes are expected — this task is a guard that the new resolved version is API-compatible with existing usage. If any test fails BECAUSE of the version change (not a pre-existing concurrent-WIP failure), stop and report.

Also run `npx tsc --noEmit` (exit 0 required — the build typechecks the whole tree including test files; a dependency type-shape change would surface here).
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx tsc --noEmit && echo TSC_OK</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; `npm test` shows no new failures attributable to the framer-motion bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | A version bump pulls a new resolved tarball from the npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | framer-motion 12.39.0 tarball | accept | Same-major minor bump of an already-trusted, already-installed dependency; `package-lock.json` pins the integrity hash. No new package introduced. |
| T-01-SC | Tampering | npm install supply chain | mitigate | Only framer-motion (already a known/trusted dep) is touched; lockfile integrity hash recorded; no new transitive package families expected — verify `git diff package-lock.json` shows only framer-motion-related entries. |
</threat_model>

<verification>
- Installed framer-motion >= 12.39.0 (asserted by the Task 1 verify command).
- `npx tsc --noEmit` exits 0.
- `git diff --cached --name-only` lists ONLY `package.json` and `package-lock.json`.
</verification>

<success_criteria>
- framer-motion is at `^12.39.0` in `package.json` and the resolved install is >= 12.39.0 (VIS-06 prerequisite for the motion facade).
- No source files changed; no test regression introduced by the bump.
</success_criteria>

<output>
Create `.planning/phases/01-shared-design-foundation/01-02-SUMMARY.md` when done.
</output>
