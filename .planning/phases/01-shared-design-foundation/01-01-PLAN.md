---
phase: 01-shared-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Depth/glow/glass design tokens resolve in :root (light) and .dark (zinc) themes"
    - "A subtle indigo/violet ambient glow pools in the dark page background"
    - "Consuming TSX can style depth/glass/glow without any hardcoded hex"
  artifacts:
    - path: "app/globals.css"
      provides: "Depth/glow/glass tokens in both themes + indigo/violet ambient glow"
      contains: "--glow-primary"
  key_links:
    - from: "app/globals.css :root block"
      to: "app/globals.css .dark block"
      via: "every new theme-varying token has a matching dark override"
      pattern: "--glow-primary"
---

<objective>
Add the missing depth/glow/glass design tokens to `app/globals.css` so every page phase inherits one shared design language (FND-01). Tune each theme-varying token against both `:root` (light) and `.dark` (zinc) in lockstep (CONTEXT decision: "both themes equal"), and swap the dark `--page-bg` to a subtle indigo/violet ambient glow (CONTEXT decision: "subtle global glow", faint enough to never muddy light mode).

Purpose: This is the upstream token layer that PremiumSurface, DrillSheet, and EChart depend on. No hardcoded hex in any consuming TSX (FND-01 success criterion 1).
Output: New CSS custom properties in `app/globals.css`; no new files.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-design-foundation/01-CONTEXT.md
@.planning/phases/01-shared-design-foundation/01-RESEARCH.md

# Token recipe + exact insertion points are in RESEARCH.md §1 and §6.
# The existing :root block, .dark block, @theme inline block, and .panel-elevated
# catch-light are all in app/globals.css — read the relevant ranges before editing.
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add depth/glow/glass tokens to both themes</name>
  <files>app/globals.css</files>
  <action>
Add the seven missing theme-varying tokens to BOTH the `:root` block and the `.dark` block, per FND-01 and the RESEARCH.md §1 recipe (the indigo/violet values, NOT the older blue/purple values in ARCHITECTURE.md §2a — the CONTEXT.md locked accent is indigo #6366f1 → violet #8b5cf6).

In the `:root` block, insert after the `--particle-line-alpha` line (the light-theme value at line ~159), a "2.5D depth tokens" group: `--glow-primary` (indigo glow, light: `0 0 24px -4px rgba(99,102,241,0.30)`), `--glow-accent` (violet glow, light: `0 0 32px -8px rgba(139,92,246,0.22)`), `--depth-card` (layered: `0 1px 3px rgba(0,0,0,0.06), 0 4px 16px -4px rgba(0,0,0,0.08)`), `--depth-float` (`0 8px 40px -8px rgba(0,0,0,0.14), inset 0 1px 0 rgba(255,255,255,0.08)`), `--glass-fill` (`rgba(255,255,255,0.62)`), `--gradient-border` (`linear-gradient(135deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.04) 100%)`).

In the `.dark` block, insert after the dark `--particle-line-alpha` line (line ~247) the matching dark overrides per RESEARCH.md §1: `--glow-primary` stronger (`0 0 24px -4px rgba(99,102,241,0.55)`), `--glow-accent` (`0 0 32px -8px rgba(139,92,246,0.45)`), `--depth-card` (`0 1px 0 rgba(255,255,255,0.04) inset, 0 4px 16px rgba(0,0,0,0.35)`), `--depth-float` (`0 8px 40px -8px rgba(0,0,0,0.7), inset 0 1px 0 rgba(255,255,255,0.07)`), `--glass-fill` (`rgba(24,24,27,0.72)`), `--gradient-border` (`linear-gradient(135deg, rgba(255,255,255,0.10) 0%, rgba(255,255,255,0.02) 100%)`).

Use a single `--glass-fill` token name (theme-resolved), NOT separate `--glass-fill-light`/`--glass-fill-dark` — the value differs per theme block, which is the established pattern for `--surface-1..3`. Keep all values literal (no `var()` references), since they may later be promoted to `@theme inline`.

Do NOT add these to the `@theme inline` block in this task — they are CSS-class-consumed values, not Tailwind utility tokens (RESEARCH.md Pitfall 5: `@theme inline` entries must be static and these need per-theme overrides). Do NOT touch `.panel-elevated` — it stays as the backing class for PremiumSurface `base`.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && grep -c -- '--glow-primary' app/globals.css | grep -qx 2 && grep -c -- '--depth-float' app/globals.css | grep -qx 2 && grep -c -- '--glass-fill' app/globals.css | grep -qx 2 && grep -q -- '--gradient-border' app/globals.css && echo TOKENS_OK</automated>
  </verify>
  <done>`--glow-primary`, `--glow-accent`, `--depth-card`, `--depth-float`, `--glass-fill`, `--gradient-border` each appear exactly twice (once in :root, once in .dark) with indigo/violet light+dark values; `npx tsc --noEmit` still exits 0 (CSS-only change).</done>
</task>

<task type="auto">
  <name>Task 2: Swap dark page background to subtle indigo/violet ambient glow</name>
  <files>app/globals.css</files>
  <action>
Replace the three neutral-zinc radial-gradient stops in the dark `--page-bg` (lines ~238-242) with the indigo/violet ambient glow per CONTEXT.md ("subtle global glow … very faint indigo/violet glow pooling in the page background") and RESEARCH.md §6: top-left `rgba(99,102,241,0.06)` (indigo), `circle at 85% 15%` `rgba(139,92,246,0.04)` (violet), `circle at 80% 100%` `rgba(99,102,241,0.03)` (indigo). Keep the existing `linear-gradient(180deg, #09090B 0%, #050507 100%)` base layer unchanged. Keep every glow stop under 8% opacity so the frosted glass has a real backdrop to blur against without the background reading as "lit up."

Do NOT change the light `:root` `--page-bg` (lines ~150-154) — RESEARCH.md §6 confirms the existing light blue/amber/teal radials are close enough to the indigo family and a change there risks muddying light mode (CONTEXT.md: ambient glow "must be theme-aware … faint enough that it never muddies light mode").
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && grep -A6 '\-\-page-bg:' app/globals.css | grep -q 'rgba(99, 102, 241' && grep -A6 '\-\-page-bg:' app/globals.css | grep -q 'rgba(139, 92, 246' && echo AMBIENT_OK</automated>
  </verify>
  <done>The dark `--page-bg` uses indigo (`rgba(99,102,241,...)`) and violet (`rgba(139,92,246,...)`) radial stops, all alpha < 0.08; the light `--page-bg` is unchanged; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only token additions; no input crosses a trust boundary, no network, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | app/globals.css token values | accept | Pure presentation tokens; no security-relevant behavior. Wrong values are a visual defect caught at the Phase 7 projector UAT, not a vulnerability. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing — `app/globals.css` edit only. No package-manager surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (Pitfall 6: the build typechecks the whole tree; a CSS-only change must not regress it).
- Manual: with DevTools, toggle `.dark` on `<html>` and confirm `getComputedStyle(document.documentElement).getPropertyValue('--glow-primary')` returns the dark indigo value, and removing `.dark` returns the lighter value (both-themes-equal verification).
</verification>

<success_criteria>
- All six depth/glow/glass tokens resolve in both `:root` and `.dark` (FND-01, success criterion 1).
- Dark page background shows a faint indigo/violet ambient glow; light mode is untouched and not muddied (CONTEXT ambient-glow decision).
- No hardcoded hex is required in consuming TSX — every depth/glass/glow value is a CSS var.
</success_criteria>

<output>
Create `.planning/phases/01-shared-design-foundation/01-01-SUMMARY.md` when done.
</output>
