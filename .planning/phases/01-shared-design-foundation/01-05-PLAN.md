---
phase: 01-shared-design-foundation
plan: 05
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - components/ui/motion.ts
  - components/ui/__tests__/motion.test.tsx
autonomous: true
requirements: [FND-04, VIS-06]
must_haves:
  truths:
    - "A central motion facade exposes reveal/stagger presets (fadeUp, fadeIn, stagger, slideFromRight)"
    - "Entrance motion stays under the hard <400ms total budget (smooth & flowing, ~0.3-0.4s)"
    - "useSafeVariants zeroes durations under prefers-reduced-motion while keeping content visible"
  artifacts:
    - path: "components/ui/motion.ts"
      provides: "Framer re-export facade + variant presets + useSafeVariants hook (the import contract)"
      exports: ["motion", "AnimatePresence", "fadeUp", "fadeIn", "stagger", "slideFromRight", "useSafeVariants"]
      min_lines: 30
    - path: "components/ui/__tests__/motion.test.tsx"
      provides: "Preset shape + reduced-motion zeroing assertions"
  key_links:
    - from: "components/ui/motion.ts"
      to: "framer-motion useReducedMotion"
      via: "useSafeVariants reads useReducedMotion and rewrites transition.duration to 0"
      pattern: "useReducedMotion"
---

<objective>
Create the central motion facade at `components/ui/motion.ts` (FND-04 + the VIS-06 motion/3D facade CONTRACT). It re-exports `motion` + `AnimatePresence` from framer-motion, exports named variant presets matching the CONTEXT "smooth & flowing" personality (eased fade-up + gentle stagger, ~0.3-0.4s, soft ease-out, under the hard <400ms total entrance budget), and exports `useSafeVariants` — the single enforcement point that zeroes durations under `prefers-reduced-motion` while leaving content visible (CONTEXT: "content still appears, just instantly").

Purpose: Establish the import contract "never import framer-motion directly in page components — always import from `@/components/ui/motion`" so reduced-motion enforcement is a single-point change. This is the VIS-06 facade contract; actual R3F 3D accents are Phase 4 (/users header) and Phase 6 (/forma-proposal) — OUT OF SCOPE here, and R3F is NOT installed in this phase (RESEARCH §12).
Output: `components/ui/motion.ts` + colocated test. Depends on the framer-motion 12.39.0 bump (plan 02). SCOPE GUARD: do NOT modify `components/ui/animated-list.tsx` — it already has reduced-motion-safe `useEntrance`/`AnimatedExpand`/`Reveal` and is imported by existing components; `motion.ts` is a superset for NEW code only (RESEARCH §4 + Open Question 3).
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-design-foundation/01-CONTEXT.md
@.planning/phases/01-shared-design-foundation/01-RESEARCH.md

# Contract + preset values: RESEARCH.md §4 + ARCHITECTURE.md §2e/§5c + STACK.md §3.
# The existing reduced-motion-safe utilities (DO NOT MODIFY — reuse the EASE constant):
@components/ui/animated-list.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motion facade — presets + useSafeVariants</name>
  <files>components/ui/motion.ts, components/ui/__tests__/motion.test.tsx</files>
  <behavior>
    - Re-exports `motion` and `AnimatePresence` from framer-motion (importable from anywhere).
    - `fadeUp`: hidden `{ opacity: 0, y: 8 }` → visible `{ opacity: 1, y: 0 }` with `transition: { duration: 0.35, ease: [0.22, 1, 0.36, 1] }` (within the 0.3-0.4s budget — CONTEXT smooth & flowing).
    - `fadeIn`: opacity-only variant for simpler cases.
    - `stagger`: visible `{ transition: { staggerChildren: 0.06, delayChildren: 0.05 } }` — 0.06s × 6 items = 0.36s total, under the hard <400ms entrance budget (CONTEXT).
    - `slideFromRight`: panel-enter variant complementing the right-slide Sheet.
    - `useSafeVariants(variants)`: when `useReducedMotion()` is true, returns a deep-ish copy with every `transition.duration` set to 0 (and stagger/delay neutralized) but opacity/visibility preserved so content still appears; when false, returns the variants unchanged.
    - Total entrance budget invariant: for every preset, the longest path (duration + any delay) is < 0.4s.
  </behavior>
  <action>
Create `components/ui/motion.ts`. Re-export `{ motion, AnimatePresence } from "framer-motion"`. Reuse the shared `EASE = [0.22, 1, 0.36, 1]` (matches `--motion-ease` and `animated-list.tsx`) — define it locally or import it; do NOT edit `animated-list.tsx`.

Export the module-level preset constants `fadeUp`, `fadeIn`, `stagger`, `slideFromRight` with the values in the behavior block (all within the <400ms budget — CONTEXT "stays under the hard <400ms total entrance budget"). These fire once per load (mount/drill), never on filter change — that is a usage rule for consuming phases, documented in a header comment.

Export `useSafeVariants<T extends Record<string, unknown>>(variants: T): T` — a hook calling `useReducedMotion()` from framer-motion; when reduced, map each variant state to set `transition: { duration: 0 }` (and zero `staggerChildren`/`delayChildren`) while keeping opacity/transform end-states so content is visible instantly; when not reduced, return `variants` as-is. This is the single reduced-motion enforcement point (ARCHITECTURE §5c).

Re: `"use client"` — per RESEARCH Pitfall 2, the `motion`/`AnimatePresence`/preset exports are safe to import anywhere; `useSafeVariants` is a hook so its callers must be client components. Add `"use client"` at the top of the file (consistent with ARCHITECTURE §2e which shows the directive) so the hook is unambiguously client-bound; this does not prevent the preset constants from being imported by client components.

Write the test first at `components/ui/__tests__/motion.test.tsx` (`// @vitest-environment jsdom`). For the preset budget assertions you can import the constants directly (pure objects) and assert each preset's `transition.duration <= 0.4` and the stagger math (`staggerChildren * 6 < 0.4`). For `useSafeVariants`, mock framer-motion's `useReducedMotion` (`vi.mock("framer-motion", ...)` returning a controllable boolean) and render a tiny probe component (or use `@testing-library/react` `renderHook` if available; otherwise a wrapper component) to assert: when reduced → all durations are 0 and opacity end-state is preserved; when not reduced → variants unchanged. Avoid jest-dom matchers (MEMORY).
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npx vitest run components/ui/__tests__/motion.test.tsx && npx tsc --noEmit && echo MOTION_OK</automated>
  </verify>
  <done>`components/ui/motion.ts` exports `motion`, `AnimatePresence`, `fadeUp`, `fadeIn`, `stagger`, `slideFromRight`, `useSafeVariants`; every preset's total entrance time is < 0.4s; `useSafeVariants` zeroes durations under reduced-motion while keeping content visible; `animated-list.tsx` is unchanged; test passes and `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure animation facade; no input, no network, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | unbounded stagger on large lists | mitigate | Presets cap stagger at 0.06s and consuming phases cap lists at 20 items (STACK §3); reduced-motion path zeroes all motion, preventing animation-driven jank on low-end/projector hardware. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan; consumes the framer-motion bump from plan 02 (already trusted). |
</threat_model>

<verification>
- `npx vitest run components/ui/__tests__/motion.test.tsx` passes.
- `npx tsc --noEmit` exits 0.
- Manual (deferred to Phase 7): in DevTools, enable "Emulate prefers-reduced-motion: reduce" and confirm a component using `useSafeVariants` renders content instantly with no transition (FND-04 / VIS-06 success criterion 4, verified via DevTools emulation).
</verification>

<success_criteria>
- A motion facade exposes reveal/stagger presets and zeroes durations under `prefers-reduced-motion` (FND-04 / VIS-06, success criterion 4).
- All presets stay under the hard <400ms entrance budget and match the "smooth & flowing" personality (CONTEXT).
- `animated-list.tsx` untouched; the import-contract is established for new code (scope guardrail).
</success_criteria>

<output>
Create `.planning/phases/01-shared-design-foundation/01-05-SUMMARY.md` when done.
</output>
