---
phase: 31-click-hover-choreography
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx
  - app/(dashboard)/users/access-analysis/NeighborMatchesPanel.test.tsx
  - app/(dashboard)/users/access-analysis/RightPanelStack.tsx
  - app/(dashboard)/users/access-analysis/RightPanelStack.groupBy.test.tsx
  - app/(dashboard)/users/UserProfilePanel.tsx
  - app/(dashboard)/users/UserProfilePanel.test.tsx
  - app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
  - app/(dashboard)/users/access-analysis/__tests__/embeddingMapSeam.test.tsx
autonomous: true
requirements: [LIFE-04]

must_haves:
  truths:
    - "The floating absolute NeighborMatchesPanel card is removed; selected identity, closest matches/twins/why chips, and the existing ACC profile body render as one continuous right rail"
    - "The unified rail has one identity header and one Close action; it contains no duplicate identity header and no Open profile action"
    - "Closest matches render before the ACC profile body and expose honest loading, empty, and inline error states without fake data or a spinner"
    - "Match and twin clicks re-isolate in place, causing the existing focus session to move without replacing its pre-focus camera snapshot"
    - "User-detail remains above lasso and base controls; clearing user focus reveals the prior lasso/base layer"
    - "The rail transition is 180ms ease-out and becomes instant/opacity-only under reduced motion; match rows do not stagger"
  artifacts:
    - "app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx"
    - "app/(dashboard)/users/access-analysis/RightPanelStack.tsx"
    - "app/(dashboard)/users/UserProfilePanel.tsx"
  key_links:
    - from: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      to: "app/(dashboard)/users/access-analysis/RightPanelStack.tsx"
      via: "neighbor content passed into the user-detail rail"
      pattern: "neighborPanel"
    - from: "app/(dashboard)/users/access-analysis/RightPanelStack.tsx"
      to: "app/(dashboard)/users/UserProfilePanel.tsx"
      via: "single rail composition"
      pattern: "railPrelude"
    - from: "app/(dashboard)/users/UserProfilePanel.tsx"
      to: "app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx"
      via: "matches-first prelude before ACC body"
      pattern: "railPrelude"
---

<objective>
Deliver LIFE-04 by moving the complete Phase-30 similarity evidence into the
existing user-detail rail ahead of the retained ACC profile body, with one header,
one close path, truthful async states, and no new fetch or profile redesign.
</objective>

<context>
@.planning/phases/31-click-hover-choreography/31-CONTEXT.md
@.planning/phases/31-click-hover-choreography/31-01-PLAN.md
@app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx
@app/(dashboard)/users/access-analysis/RightPanelStack.tsx
@app/(dashboard)/users/UserProfilePanel.tsx
@app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
@DESIGN.md
</context>

## Tasks

1. **Convert the Phase-30 panel into rail content**
   - Remove its absolute card/header/Open-profile chrome; retain the twin chip/list,
     match score, why chips, coverage suffixes, and re-isolation callbacks.
   - Render a compact "Closest matches" rail section with static skeleton rows while
     the existing query loads, an honest empty line when no matches/twins exist, and
     an inline unavailable message on query error.
   - Keep it presentational and parent-fed; no query or data-contract change.

2. **Compose one matches-first profile rail**
   - Add one narrow `railPrelude` seam to the existing rail variant of
     `UserProfilePanel`, placing it inside the same scroll region before the existing
     ACC profile body while retaining the current single identity header + Close.
   - Thread the neighbor section through `RightPanelStack` only for the user-detail
     layer. Keep user-detail > lasso > sliders precedence and stable persisted width.
   - Change the rail motion to 180ms ease-out and honor reduced motion without row
     staggering or ornamental motion.

3. **Remove the floating mount and prove integration**
   - Delete the absolute `NeighborMatchesPanel` render from the graph slot.
   - Build the same component once as the right-panel neighbor content, using the
     existing Phase-30 payload, coverage map, nodeId index, and `setIsolated`.
   - Update focused tests to prove content order, single identity header, absence of
     Open profile, loading/empty/error states, re-isolation, and unchanged base-layer
     behavior. Keep the embedding-map seam green.

## Verification

- `npx vitest run "app/(dashboard)/users/access-analysis/NeighborMatchesPanel.test.tsx" "app/(dashboard)/users/UserProfilePanel.test.tsx" "app/(dashboard)/users/access-analysis/RightPanelStack.groupBy.test.tsx" "app/(dashboard)/users/access-analysis/__tests__/embeddingMapSeam.test.tsx"`
- `npx tsc --noEmit`
- `node .agents/skills/impeccable/scripts/detect.mjs --json "app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx" "app/(dashboard)/users/access-analysis/RightPanelStack.tsx" "app/(dashboard)/users/UserProfilePanel.tsx"`

## Phase completion gates

- Re-run all Phase-31 focused tests plus the existing Phase-30
  `whySimilar.test.ts` and PERF-02 `GraphCanvas.test.ts`.
- `npx tsc --noEmit`
- `node scripts/repo-map/check.cjs`
- `node scripts/uat/run-engineering-gates.cjs --static-only`, then the relevant
  authenticated production-browser interaction gate on isolated `:3100` when the
  production build is available.
- `impeccable detect` over the changed TSX surfaces.
- With all gates green and `autoDeploy:true`, follow the repository deploy sequence
  and probe `/users/spatial-graph`.

## Success criteria

The selected user is explained in one continuous matches-first rail, all LIFE-04
truths hold, Phase 31 passes its full gates, and no floating match card remains.

