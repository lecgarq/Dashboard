---
phase: 18-accfolderpermissionsummary-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_acc_folder_permission_summary/migration.sql
  - scripts/backfill-folder-perm-summary.cjs
  - scripts/verify-folder-perm-summary.cjs
  - .planning/phases/18-accfolderpermissionsummary-foundation/18-RECONCILIATION.md
autonomous: true
requirements:
  - PROJ-01

must_haves:
  truths:
    - "An AccFolderPermissionSummary table is live in the local PostgreSQL DB with the grouped columns (projectId, roleId, folderCount, totalBytes, permTypes, refreshedAt)"
    - "The Prisma client exposes db.accFolderPermissionSummary and npx tsc --noEmit passes"
    - "A backfill script populates the projection from AccFolderPermission entirely server-side (INSERT...SELECT...GROUP BY) — no ~6M-row scan into Node heap"
    - "TEST-01 (OOM aggregate guard in lib/server/acc-hot-cache.test.ts) passes byte-identical throughout — acc-hot-cache.ts is not edited"
    - "A reconciliation script proves the projection equals the live includePermissionSummary GROUP BY aggregate (row counts + 0 mismatches + spot-checked keys); the verdict is recorded in 18-RECONCILIATION.md"
    - "No consumer of includePermissionSummary or includePermissionContexts is changed — /access-analysis and /template-mty behave identically"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "AccFolderPermissionSummary model mirroring the live aggregate columns"
      contains: "model AccFolderPermissionSummary"
    - path: "prisma/migrations/*_add_acc_folder_permission_summary/migration.sql"
      provides: "CREATE TABLE for AccFolderPermissionSummary + unique/index constraints"
      contains: "AccFolderPermissionSummary"
    - path: "scripts/backfill-folder-perm-summary.cjs"
      provides: "Server-side INSERT...SELECT...GROUP BY backfill with the folderCrawlStatus IN ('ok','partial') filter"
      min_lines: 30
    - path: "scripts/verify-folder-perm-summary.cjs"
      provides: "Reconciliation diff of projection vs live GROUP BY aggregate (PASS/FAIL harness)"
      min_lines: 40
    - path: ".planning/phases/18-accfolderpermissionsummary-foundation/18-RECONCILIATION.md"
      provides: "Recorded reconciliation verdict (row counts + mismatch count + sample keys)"
  key_links:
    - from: "scripts/backfill-folder-perm-summary.cjs"
      to: "AccFolderPermission / AccFolder / AccProject"
      via: "INSERT INTO AccFolderPermissionSummary SELECT ... GROUP BY projectId, roleId WHERE folderCrawlStatus IN ('ok','partial') — mirrors acc-hot-cache.ts:304-317"
      pattern: "folderCrawlStatus.*ok.*partial"
    - from: "scripts/verify-folder-perm-summary.cjs"
      to: "AccFolderPermissionSummary"
      via: "re-runs the live GROUP BY and full-outer-joins it against the projection on (projectId, roleId), counting mismatches"
      pattern: "GROUP BY"
    - from: "prisma/schema.prisma"
      to: "lib/server/acc-hot-cache.ts:304-317"
      via: "model columns are an exact mirror of the live includePermissionSummary aggregate result shape"
      pattern: "model AccFolderPermissionSummary"
---

<objective>
Add the `AccFolderPermissionSummary` projection foundation (PROJ-01, seed REF-03): a
Prisma model + migration that mirrors the live `includePermissionSummary` GROUP BY
aggregate row-for-row, a server-side backfill that populates it from `AccFolderPermission`
without re-opening the OOM window, and a reconciliation script that proves projection ==
live aggregate BEFORE any consumer is switched.

Purpose: REF-03 is the milestone's risk carrier. This phase lands the projection and PROVES
parity so that Phase 19 (PROJ-02/PROJ-03) can switch consumers and retire the
`includePermissionContexts` raw scan with confidence. No consumer is switched here.

Output: `AccFolderPermissionSummary` model in `prisma/schema.prisma`, its migration applied
to the local DB, `scripts/backfill-folder-perm-summary.cjs`,
`scripts/verify-folder-perm-summary.cjs`, and a recorded reconciliation verdict in
`18-RECONCILIATION.md`.

Workshop-impact note: ZERO. This phase touches no `app/` route, no `components/`, no tRPC
procedure, no chart. The four workshop pages (`/users`, `/access-analysis`, `/template-mty`,
`/forma-proposal`) and `/users/spatial-graph` are untouched. Zinc theme, ECharts, and the
no-new-WebGL rule are satisfied by not touching UI surfaces.

Data-truthfulness note: the projection is a derived aggregate of existing Prisma data. It
inherits the live query's coverage boundary exactly — the `folderCrawlStatus IN ('ok','partial')`
filter is part of the contract, so under-covered projects (`inaccessible` crawl status) are
excluded from the projection precisely as the live aggregate excludes them. No new metric,
no new data source (v2.2 out-of-scope guardrail respected).
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The live aggregate this projection must mirror row-for-row (else-branch of getCachedAccDcBulkUsers)
@lib/server/acc-hot-cache.ts

# TEST-01 — the OOM aggregate guard that must stay green byte-identical (acc-hot-cache.ts is NOT edited)
@lib/server/acc-hot-cache.test.ts

# Source table shape (AccFolderPermission model at line 529; AccFolder rollup columns at 516-523)
@prisma/schema.prisma

# Repo script conventions: PASS/FAIL reconciliation harness + Prisma-adapter connect (DIRECT_URL||DATABASE_URL)
@scripts/verify-accds-merge.cjs

# Repo script conventions: local no-SSL pg connect + census style (DB-02 removed the ssl fossil)
@scripts/count-acc-data.cjs

# Raw-migration + `prisma migrate resolve` precedent (pgvector-safe path used in Phase 09 DB-01)
@prisma/migrations/20260623000000_add_acc_folder_permission_role_id_index/migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AccFolderPermissionSummary model + apply migration (pgvector-safe)</name>
  <files>prisma/schema.prisma, prisma/migrations/*_add_acc_folder_permission_summary/migration.sql</files>
  <action>
Add a new `model AccFolderPermissionSummary` to `prisma/schema.prisma`, placed adjacent to
the `AccFolderPermission` model (line 529) it projects. Its columns MUST be an exact mirror
of the live `includePermissionSummary` GROUP BY aggregate result at `acc-hot-cache.ts:304-317`
(per PROJ-01): `id String @id @default(cuid())` (mirror AccFolderPermission's own id
convention), `projectId String`, `roleId String`, `folderCount Int`, `totalBytes BigInt`,
`permTypes String[]`, and a refresh timestamp `refreshedAt DateTime @default(now())`. Natural
key: `@@unique([projectId, roleId])` (this is the GROUP BY key). Add `@@index([projectId])`
and `@@index([roleId])` to match the query access patterns.

Apply the migration to the local PostgreSQL DB using the pgvector-safe path (STATE lines
115-117: `prisma migrate dev` has historically choked on this DB's pgvector `Unsupported`
column via the shadow DB):

PRIMARY attempt — run `npx prisma migrate dev --name add_acc_folder_permission_summary`. If it
succeeds cleanly (no shadow-DB / pgvector error), the migration dir + client generation are
done; skip to verification.

EXPECTED FALLBACK (mirror the Phase 09 DB-01 precedent) — when `migrate dev` fails on the
pgvector shadow-DB step: (1) hand-write a new migration under
`prisma/migrations/<YYYYMMDDHHMMSS>_add_acc_folder_permission_summary/migration.sql` following
the referenced Phase 09 file's format; the DDL must be the standard Prisma-generated shape for
this model — a `CREATE TABLE "AccFolderPermissionSummary"` with columns `id` text NOT NULL
(primary key), `projectId` text NOT NULL, `roleId` text NOT NULL, `folderCount` integer NOT
NULL, `totalBytes` bigint NOT NULL, `permTypes` text[], `refreshedAt` timestamp(3) NOT NULL
DEFAULT CURRENT_TIMESTAMP; a `CONSTRAINT "AccFolderPermissionSummary_pkey" PRIMARY KEY ("id")`;
a unique index `"AccFolderPermissionSummary_projectId_roleId_key"` on `("projectId","roleId")`;
and plain indexes `"AccFolderPermissionSummary_projectId_idx"` and
`"AccFolderPermissionSummary_roleId_idx"`. (2) Apply the SQL to the local DB with the repo's
pg connect pattern (a node one-liner using `pg` Client + `DATABASE_URL`, no SSL — as in
`count-acc-data.cjs` post-DB-02). (3) Register it as applied with
`npx prisma migrate resolve --applied <YYYYMMDDHHMMSS>_add_acc_folder_permission_summary`.
(4) Run `npx prisma generate` so `db.accFolderPermissionSummary` exists for tsc.

Do NOT edit `lib/server/acc-hot-cache.ts`, `lib/server/folderPermissionTerrainView.ts`,
`lib/server/templateFolderTerrain.ts`, or any terrain loader — no consumer is switched this
phase.
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&1 | grep -qi "add_acc_folder_permission_summary" && npx tsc --noEmit</automated>
  </verify>
  <done>`prisma/schema.prisma` contains `model AccFolderPermissionSummary` with the six grouped columns + `refreshedAt` + `@@unique([projectId, roleId])`; the migration is present and applied (via `migrate dev` OR raw SQL + `migrate resolve`); `npx prisma migrate status` lists it as applied; `npx tsc --noEmit` exits 0 (client regenerated). `acc-hot-cache.ts` and terrain loaders are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Server-side backfill script (INSERT...SELECT...GROUP BY, OOM-safe)</name>
  <files>scripts/backfill-folder-perm-summary.cjs</files>
  <action>
Create `scripts/backfill-folder-perm-summary.cjs` following the `verify-accds-merge.cjs`
connection convention (PrismaClient + `@prisma/adapter-pg` `PrismaPg`, reading
`DIRECT_URL || DATABASE_URL`, clean `$disconnect` in a `finally`; optional `dotenv` require
like that file). Do NOT copy the SSL fossil from the pre-DB-02 `count-acc-data.cjs`.

The backfill MUST aggregate entirely in SQL — it must NEVER `findMany`/`$queryRaw` the ~6M
`AccFolderPermission` rows into Node (that re-opens the exact OOM window TEST-01 guards).
Inside a single transaction: first clear the projection with `TRUNCATE "AccFolderPermissionSummary"`
(exact-mirror semantics — no orphan rows; Phase 19 owns incremental refresh), then run one
`INSERT INTO "AccFolderPermissionSummary" ("id","projectId","roleId","folderCount","totalBytes","permTypes","refreshedAt")
SELECT ... GROUP BY f."projectId", fp."roleId"` whose SELECT body is byte-identical to the live
aggregate at `acc-hot-cache.ts:304-317` — `COUNT(DISTINCT fp."folderId")::int` for folderCount,
`COALESCE(SUM(COALESCE(f."totalSizeBytes",0)),0)::bigint` for totalBytes,
`array_agg(DISTINCT fp."permType")` for permTypes — joining `AccFolderPermission fp` → `AccFolder f`
→ `AccProject p` and carrying the SAME `WHERE p."folderCrawlStatus" IN ('ok','partial')` filter
(this coverage boundary is part of the PROJ-01 contract). Generate the row `id` server-side with
`gen_random_uuid()::text` and set `refreshedAt` to `now()`. VERIFY: `gen_random_uuid()` is in
core Postgres 13+ (this DB is local PG 18); if it is somehow unavailable, fall back to
`md5(random()::text || clock_timestamp()::text)` or `CREATE EXTENSION IF NOT EXISTS pgcrypto`.

Use the generated Prisma client's raw executor (`db.$executeRawUnsafe` for the TRUNCATE + INSERT)
and `db.$queryRawUnsafe` for a post-insert sanity report: print the inserted row count and the
group-row upper bound (`SELECT COUNT(DISTINCT f."projectId")` × `COUNT(DISTINCT fp."roleId")`
over the filtered join) so the operator can see rows ≤ n_projects × n_roles (the same bound
TEST-01 asserts). The script must be idempotent (truncate+insert yields the same result every
run) and exit non-zero on any thrown error.
  </action>
  <verify>
    <automated>node scripts/backfill-folder-perm-summary.cjs && npx vitest run lib/server/acc-hot-cache.test.ts</automated>
  </verify>
  <done>`node scripts/backfill-folder-perm-summary.cjs` runs to completion without OOM or timeout, prints an inserted row count > 0 that is ≤ n_projects × n_roles, and is re-runnable idempotently; TEST-01 (`lib/server/acc-hot-cache.test.ts`) passes green (proves the guarded live path was not disturbed). The script contains no `findMany` of AccFolderPermission and carries the `folderCrawlStatus IN ('ok','partial')` filter.</done>
</task>

<task type="auto">
  <name>Task 3: Reconciliation script + recorded verdict</name>
  <files>scripts/verify-folder-perm-summary.cjs, .planning/phases/18-accfolderpermissionsummary-foundation/18-RECONCILIATION.md</files>
  <action>
Create `scripts/verify-folder-perm-summary.cjs` using the `verify-accds-merge.cjs` PASS/FAIL
harness style (a `const ok = (cond, msg) => {...}` that logs `PASS`/`FAIL`, collects failures,
and `process.exit(1)` if any fail; PrismaClient + `PrismaPg` adapter; `$disconnect` in `finally`).
It proves the projection equals the live `includePermissionSummary` aggregate:

1. Row-count assertion: `COUNT(*)` of the live GROUP BY aggregate (a CTE that re-runs the exact
   `acc-hot-cache.ts:304-317` query, `folderCrawlStatus IN ('ok','partial')` included) equals
   `COUNT(*)` of `AccFolderPermissionSummary`.
2. Full-diff assertion (primary parity proof): a single SQL that `FULL OUTER JOIN`s the live
   aggregate CTE against the projection ON `(projectId, roleId)` and counts rows where the key
   is missing on either side OR `folderCount` differs OR `totalBytes` differs OR `permTypes`
   differs (compare arrays after a stable ordering, e.g. sorted). The mismatch count MUST be 0.
   Do all of this in SQL so no permission rows cross into Node.
3. Spot-check assertion: pull ~20 sample `(projectId, roleId)` keys (e.g. `ORDER BY random() LIMIT 20`)
   and print live-vs-projection `folderCount`/`totalBytes`/`permTypes` side by side; assert each
   sampled trio matches. Any interpolated key value must be escaped/parameterized (single-quote
   escaping as in `verify-accds-merge.cjs`), never string-concatenated from untrusted input.

Print a clearly delimited reconciliation summary block (mode, live row count, projection row
count, mismatch count, sample keys). Then record the verdict: write
`.planning/phases/18-accfolderpermissionsummary-foundation/18-RECONCILIATION.md` capturing the
run date, the live-vs-projection row counts, the mismatch count (must be 0), the ~20 spot-checked
keys, and a PASS/FAIL line. (The executor may write this note from the captured script output, or
have the script emit it via an `--out` arg — either satisfies "recorded in a script log or
planning note".)

Do NOT modify `acc-hot-cache.ts` or any consumer — this task only reads.
  </action>
  <verify>
    <automated>node scripts/verify-folder-perm-summary.cjs</automated>
  </verify>
  <done>`node scripts/verify-folder-perm-summary.cjs` exits 0 with all assertions PASS: projection row count == live aggregate row count, full-outer-join mismatch count == 0, and all ~20 spot-checked `(projectId, roleId)` keys match on folderCount/totalBytes/permTypes. The verdict (row counts + 0 mismatches + sample keys + PASS) is recorded in `18-RECONCILIATION.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → local PostgreSQL DB | Scripts read the connection string from local env (`DIRECT_URL`/`DATABASE_URL`) and mutate/read a single local table. No network input, no untrusted request data crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | backfill/reconciliation SQL | mitigate | Scripts take no untrusted args; the only interpolated values are internal `(projectId, roleId)` GUIDs sampled from the DB itself, single-quote-escaped as in `verify-accds-merge.cjs` — no user-supplied SQL text. |
| T-18-02 | Denial of Service | backfill memory footprint | mitigate | Aggregation stays server-side (`INSERT...SELECT...GROUP BY`); zero of the ~6M permission rows cross into Node heap. TEST-01 guards the sibling live aggregate path and must stay green. |
| T-18-03 | Information Disclosure | script stdout / reconciliation note | accept | Local-only run; output is group counts + internal project/role GUIDs already present in the DB. Never print the connection string or any `.env` value. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase — scripts use already-present `pg`, `@prisma/client`, `@prisma/adapter-pg`. No supply-chain surface added. |
</threat_model>

<verification>
Phase-level checks (all repo-valid, no `next build`/rebuild this phase):

1. `npx prisma migrate status` — the `add_acc_folder_permission_summary` migration is applied.
2. `npx tsc --noEmit` — exits 0 (proves `db.accFolderPermissionSummary` type exists and nothing else broke; success criterion 5).
3. `node scripts/backfill-folder-perm-summary.cjs` — populates without OOM/timeout; inserted count ≤ n_projects × n_roles (success criterion 2).
4. `npx vitest run lib/server/acc-hot-cache.test.ts` — TEST-01 (OOM aggregate guard) passes green byte-identical (success criterion 2, scope fence).
5. `node scripts/verify-folder-perm-summary.cjs` — projection row count == live aggregate, 0 mismatches, spot-checked keys match; verdict recorded in `18-RECONCILIATION.md` (success criterion 3).
6. Scope fence — `git diff --cached --name-only` shows ONLY `prisma/schema.prisma`, `prisma/migrations/**`, the two new `scripts/*.cjs`, and `.planning/**`. `lib/server/acc-hot-cache.ts` and terrain loaders MUST NOT appear (success criterion 4). Commit by explicit path only (never `-A`/`.`) per STATE guardrail — the branch carries unrelated WIP + `.planning/` migration deletions.

Dashboard self-check:
- Context: STATE.md, ROADMAP.md, REQUIREMENTS.md, PROJECT.md, CONCERNS.md, prisma/schema.prisma, lib/server/acc-hot-cache.ts(+test), lib/server/folderPermQuery.ts, scripts/count-acc-data.cjs, scripts/verify-accds-merge.cjs, and the Phase 09 raw-migration precedent all read; all current.
- Evidence: model columns grounded in `acc-hot-cache.ts:304-317` (verbatim live aggregate); source table shape at `prisma/schema.prisma:529`; census ~6.04M `AccFolderPermission` rows (STATE); TEST-01 at `lib/server/acc-hot-cache.test.ts:258`; migration-resolve pattern at `prisma/migrations/20260623000000_.../migration.sql`; test command `vitest run` and prisma 7.8.0 from `package.json`.
- Constraints: behavior-preserving, zero UI/theme/WebGL change, Prisma DB as the only data source, coverage boundary (`folderCrawlStatus IN ('ok','partial')`) inherited and labeled, `/users/spatial-graph` untouched.
- Gates: `npx tsc --noEmit` before any rebuild; no rebuild this phase (backend/DB + scripts only); repo-map check not required (no import-boundary or app data-flow change).
- VERIFY: `gen_random_uuid()` availability on the local PG 18 instance (core 13+; md5/pgcrypto fallback specified). `prisma migrate dev` may choke on pgvector — the raw-migration + `migrate resolve` fallback is the expected branch.
</verification>

<success_criteria>
Maps to the 5 Phase 18 roadmap success criteria:

1. `AccFolderPermissionSummary` model + migration exist and apply cleanly to the local DB (via `migrate dev` OR raw migration + `migrate resolve`). — Task 1
2. The backfill populates the projection from `AccFolderPermission` without OOM/timeout; TEST-01 passes throughout. — Task 2
3. The reconciliation script confirms projection row counts + spot-checked keys match the live `includePermissionSummary` GROUP BY aggregate; the verdict is recorded in `18-RECONCILIATION.md`. — Task 3
4. No consumer of `includePermissionSummary`/`includePermissionContexts` is changed — zero behavior change to `/access-analysis` or `/template-mty`. — scope fence (verification step 6)
5. `npx tsc --noEmit` exits clean after the model + script additions. — verification step 2
</success_criteria>

<output>
Create `.planning/phases/18-accfolderpermissionsummary-foundation/18-01-SUMMARY.md` when done,
plus the `18-RECONCILIATION.md` verdict note from Task 3.
</output>
