---
phase: 33
plan: "33-02"
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
  - .planning/phases/33-perf-closeout-verification/33-BASELINE.md
autonomous: true
requirements: [PERF-06]
must_haves:
  truths:
    - "A fresh in-phase PRE-SPLIT time-to-graph median (isolated :3100 prod build, median-of-5, measure-spatial-graph-baseline.cjs) is recorded BEFORE any split change."
    - "Heavy non-graph UI (framer-motion-carrying panel components) is code-split out of the AccessAnalysisShell initial chunk; graph mounts first, panels stream in behind quiet fade-in placeholders with no layout shift."
    - "A POST-SPLIT median from the identical method is recorded; pass = post-split measurably better than pre-split; delta vs the 28.1 median 4,360 ms recorded honestly in both directions."
    - "PERF-05 no-refetch network evidence is recorded on the isolated :3100 build (prefetched queries hydrate; no client refetch of prefetched data)."
    - "PERF-02 frozen-handle invariant and the baseline spec's __baselineReadyAt graph-rendered signal are unchanged."
  artifacts:
    - ".planning/phases/33-perf-closeout-verification/33-BASELINE.md"
  key_links:
    - pattern: "dynamic\\("
      in: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
---

# Plan 33-02 — PERF-06: pre-split baseline, graph-first code split, post-split re-measure

## Objective

Reduce the shell-chunk parse gap: measure a fresh pre-split time-to-graph
median (with 33-01 aboard, so the pair isolates the split's effect), split
heavy non-graph UI out of the `AccessAnalysisShell` initial chunk with the
owner's "graph first, panels follow" load feel, then re-measure. Pass/fail =
the in-phase pair (locked decision 1); 4,360 ms delta recorded, not gating.

## Context (@ paths)

- @.planning/phases/33-perf-closeout-verification/33-CONTEXT.md (decisions 1–2, defaults)
- @app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx (~60 static imports; split target)
- @app/(dashboard)/users/access-analysis/AccessAnalysisShellClient.tsx (existing dynamic() idiom)
- @app/(dashboard)/users/access-analysis/RightPanelStack.tsx (framer-motion carrier)
- @app/(dashboard)/users/access-analysis/CatalogCollapse.tsx (framer-motion carrier)
- @scripts/measure-spatial-graph-baseline.cjs (method + build recipe in header)
- @tests/e2e/spatial-graph-baseline.spec.ts (graph-rendered signal — must not move)
- @DESIGN.md (visual surface touched — staged reveal must read intentional)

## Facts pinned from source

- `@duckdb/duckdb-wasm` is ALREADY dynamic (`duckdbClient.ts:26`); not a lever.
- `@cosmos.gl/graph` is the graph itself (`GraphCanvas2D.tsx:27` via
  `GraphCanvas`) — it is the priority chunk, NOT a split candidate.
- framer-motion enters the shell chunk via `RightPanelStack.tsx:18` and
  `CatalogCollapse.tsx` — prime candidates.
- The baseline spec waits on the graph bridge signal (`__baselineReadyAt`);
  deferring panels must not delay or move that signal.

## Tasks

1. **Pre-split baseline (no code changes yet).**
   Confirm `:3000` untouched. Build isolated:
   `$env:NEXT_PUBLIC_ACC_GRAPH_TEST="1"; $env:NEXT_DIST_DIR=".next-e2e"; npx next build --webpack`
   (run `npx tsc --noEmit` first), then `$env:NEXT_DIST_DIR=".next-e2e"; npx next start -p 3100`.
   Run `node scripts/measure-spatial-graph-baseline.cjs`. Record full JSON in
   `33-BASELINE.md` under "Pre-split (fresh, Phases 29–32 + PERF-05 aboard)".
2. **PERF-05 network evidence (same build).**
   Authenticated load of `/users/spatial-graph` and `/users` with network
   capture (Playwright request listener or devtools): assert the prefetched
   procedures (`accDcGraph.bulkUsers`, `accDcGraph.instanceEmbedding` on
   spatial-graph; `accDcGraph.bulkUsers` lean + `accMembers.enrichedUsers` +
   directory queries on /users) are NOT fetched client-side after mount.
   Record request lists in `33-BASELINE.md`.
3. **Implement graph-first split in `AccessAnalysisShell.tsx`.**
   Convert non-critical panel UI to `next/dynamic` (`ssr:false` — the shell is
   already client-only) so the initial chunk carries the graph path only:
   - `RightPanelStack` (framer-motion), `NeighborMatchesPanel`, `Legend`,
     `MapClusterLabels`, `Toolbar` (which imports `CatalogCollapse`) — verify
     each candidate's real import weight before converting; convert the ones
     that measurably shrink the chunk (webpack chunk sizes from the build
     output are the evidence).
   - Placeholders: quiet, dimension-matched empty shells (no spinner text),
     CSS fade-in only, no layout shift (reserve the same box). Reduced-motion:
     fade duration honored by existing conventions (CSS transition — safe).
   - The graph canvas mount order and the `__ACC_GRAPH_TEST__` bridge init
     must stay in the initial chunk untouched.
4. **Post-split measure.**
   Rebuild isolated (same recipe), restart `:3100`, re-run
   `node scripts/measure-spatial-graph-baseline.cjs`. Record JSON under
   "Post-split". Compute: post vs pre delta (pass = measurably better) and
   both medians vs 4,360 ms (recorded honestly, non-gating). Also record the
   shell chunk size before/after from build output.

## Verification gates

- `npx tsc --noEmit` clean after task 3.
- `npx vitest run app/(dashboard)/users/access-analysis/GraphCanvas.test.ts` —
  PERF-02 frozen-handle invariant green.
- Focused shell tests: `npx vitest run app/(dashboard)/users/access-analysis`
  (pre-existing failures reported separately, e.g. 3 usePredicateEngine).
- Baseline spec runs green N=5 in both measurement passes (it is the method).
- `impeccable detect` on the staged-load surface (visual change: placeholders
  + fade-in) — zero new findings.
- Pass/fail per locked decision 1: post-split median < pre-split median.

## Success criteria

Truths above; `33-BASELINE.md` holds the pair + network evidence + chunk
sizes; no Phase 29–32 behavior change; graph-rendered signal timing method
identical across the pair.
