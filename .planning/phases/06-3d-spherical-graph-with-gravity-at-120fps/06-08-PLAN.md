---
phase: 06-3d-spherical-graph-with-gravity-at-120fps
plan: 08
type: execute
wave: 5
depends_on: ["06-07"]
files_modified:
  - app/(dashboard)/users/UsersDirectoryClient.tsx
  - app/(dashboard)/users/AccUsersGraph.tsx
  - app/(dashboard)/users/cosmosUtils.ts
  - app/(dashboard)/users/cosmosUtils.test.ts
  - app/(dashboard)/users/graphRenderers.ts
  - app/(dashboard)/users/threeGraphRenderer.ts
  - app/(dashboard)/users/accGraph3d.ts
  - app/(dashboard)/users/accGraphOrganicLayout.ts
  - app/(dashboard)/users/accGraphOrganicLayout.worker.ts
  - app/(dashboard)/users/accGraphTopology.test.ts
  - patches/@cosmos.gl+graph+3.0.0-beta.9.patch
  - package.json
autonomous: false
requirements: [GRAPH3D-12, GRAPH3D-03]
must_haves:
  truths:
    - "UsersDirectoryClient mounts Sphere3DGraph in the 'graph' tab instead of AccUsersGraph"
    - "The graph tab uses display:none toggle (not conditional unmount) to preserve WebGL context across tab switches"
    - "cosmos.gl + d3-force + patch-package patches are fully removed; no references remain in package.json, lockfile, or src"
    - "npm run build + npm ci both succeed from a clean checkout"
    - "Operator-confirmed cross-browser smoke (Chrome + Safari + Firefox) before phase close"
  artifacts:
    - path: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      provides: "Updated tab mount: <Sphere3DGraph users={...} onSelectUser={...} /> behind display:none"
    - path: "package.json"
      provides: "@cosmos.gl/graph and d3-force removed; postinstall updated if patches/ is now empty"
  key_links:
    - from: "UsersDirectoryClient.tsx graph tab"
      to: "Sphere3DGraph.tsx"
      via: "import + JSX mount"
      pattern: "Sphere3DGraph"
---

<objective>
Cut over. Replace AccUsersGraph mount with Sphere3DGraph in UsersDirectoryClient, delete cosmos.gl dependency cascade, drop d3-force, remove the patch file, simplify the tab from conditional-render to display:none toggle. Final UAT checkpoint validates cross-browser parity.

Purpose: This is the irreversible step. By landing it last, every prior wave has been verifiable against the still-working 2D graph.
Output: Updated host file + 9 deletions + package.json cleanup + cross-browser UAT.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-RESEARCH.md
@app/(dashboard)/users/UsersDirectoryClient.tsx
@app/(dashboard)/users/Sphere3DGraph.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount swap in UsersDirectoryClient + display:none tab toggle</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
Locate the `activeTab === "graph"` conditional mount (around line 1168–1178 per RESEARCH). Replace:

```tsx
{activeTab === "graph" && (
  <AccUsersGraph users={mergedAccUsers} onSelectUser={...} />
)}
```

With a display:none toggle wrapper:

```tsx
<div style={{ display: activeTab === "graph" ? "block" : "none", height: "100%" }} className="...same-classes...">
  <Sphere3DGraph users={mergedAccUsers} onSelectUser={(email) => {
    setSelectedPersonEmail(email);
    setActiveTab("general");
  }} />
</div>
```

Import Sphere3DGraph via `next/dynamic` with `{ ssr: false }` per RESEARCH Open Question 7 to keep the chunk lazy: `const Sphere3DGraph = dynamic(() => import("./Sphere3DGraph"), { ssr: false });`.

Remove the `void import("@cosmos.gl/graph")` pre-warm line (referenced in RESEARCH at line 1166).

Verify other usages of AccUsersGraph across the repo via grep — if any other surface imports it, decide per-call-site whether to migrate or stub.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>Build green; graph tab shows the 3D sphere when activated; switching tabs preserves the canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Delete cosmos.gl + d3-force cascade</name>
  <files>
app/(dashboard)/users/AccUsersGraph.tsx,
app/(dashboard)/users/cosmosUtils.ts,
app/(dashboard)/users/cosmosUtils.test.ts,
app/(dashboard)/users/graphRenderers.ts,
app/(dashboard)/users/threeGraphRenderer.ts,
app/(dashboard)/users/accGraph3d.ts,
app/(dashboard)/users/accGraphOrganicLayout.ts,
app/(dashboard)/users/accGraphOrganicLayout.worker.ts,
app/(dashboard)/users/accGraphTopology.test.ts,
patches/@cosmos.gl+graph+3.0.0-beta.9.patch,
package.json
  </files>
  <action>
Follow RESEARCH Pitfall 7 cascade ORDER strictly:
1. Delete `patches/@cosmos.gl+graph+3.0.0-beta.9.patch` first.
2. Check `ls patches/` — if empty, remove the `&& patch-package` segment from the `postinstall` script in package.json (preserve `prisma generate`).
3. Remove `AccUsersGraph.tsx` + all listed sibling files. Before deleting `accGraphTopology.test.ts` and `accGraphOrganicLayout.ts`, verify `buildEdgeBuffers` in `sphere3d/topologyAdapter.ts` does NOT import from them. If it does (Plan 06-03 imported `buildAccTopologyGraph`), MOVE that function into `sphere3d/topologyAdapter.ts` (or into a sibling pure helper) before deleting the source file. Likewise move any kept tests from `accGraphTopology.test.ts` into a sphere3d/__tests__/ location.
4. Run `grep -rn "@cosmos.gl/graph\|cosmosUtils\|graphRenderers\|threeGraphRenderer\|accGraph3d\|accGraphOrganicLayout\|AccUsersGraph\|d3-force" app lib server scripts components` — assert no remaining references.
5. Remove `@cosmos.gl/graph`, `d3-force`, `@types/d3-force` from `package.json` dependencies/devDependencies.
6. `rm -rf node_modules package-lock.json && npm ci` — full clean install to confirm lockfile + deps are coherent.
7. `npm run build` — green.
8. `npx vitest run` — all suites green (except deleted ones).
  </action>
  <verify>
    <automated>npm ci &amp;&amp; npm run build &amp;&amp; npx vitest run &amp;&amp; grep -rn "@cosmos.gl\|cosmosUtils\|graphRenderers" app lib server scripts components 2>&amp;1 | grep -v "Binary file\|node_modules\|.next\|.git" || echo "no matches (good)"</automated>
  </verify>
  <done>Clean install green, build green, all tests green, zero cosmos.gl references in source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-browser UAT</name>
  <files>app/(dashboard)/users/Sphere3DGraph.tsx</files>
  <action>
Human-verify the cosmos.gl→3D sphere swap by running the manual UAT script across Chrome, Safari, and Firefox. Claude does no further automation in this task — all build/test verification was completed in Tasks 1–2.
  </action>
  <verify>Operator confirms via "approved" resume signal after walking the how-to-verify script.</verify>
  <done>All three browsers (Chrome, Safari, Firefox) pass the full UAT script; frame-time p95 ≤ ~12ms reported; no regressions vs prior 2D graph behavior.</done>
  <what-built>
The 2D Cosmos.gl graph has been replaced by the 3D volumetric sphere in the Users dashboard. Cosmos.gl + d3-force + patches removed. Build and unit tests green.
  </what-built>
  <how-to-verify>
On a deployed preview (or local dev):

1. Open the Users dashboard at `/users` in Chrome (desktop):
   - Click the "graph" tab. Expect: 3D ball of nodes, NOT a flat 2D disk.
   - Click and drag — sphere rotates.
   - Scroll the mouse wheel — camera dollies closer. Continue scrolling — you should be able to fly INTO the ball and see nodes from the inside.
   - Wait 4 seconds without input — gentle auto-rotation begins.
   - Click any node — side panel shows that user's details; camera smoothly recenters.
   - Toggle a filter — nodes outside the filter fade out smoothly; remaining nodes settle.
   - Dolly very close to a node — its label appears as floating 3D text.
   - Switch to a different tab and back — the graph reappears WITHOUT a long re-init delay (display:none preserves it).

2. Repeat the full checklist in Safari (macOS).
3. Repeat the full checklist in Firefox.

4. Open browser DevTools → Performance tab → record 5s while interacting. Confirm frame-time p95 ≤ ~12ms on a modern laptop with the production hub (~25k nodes). Report observed value.

5. Confirm there is NO 2D/3D toggle, NO "switch to legacy graph" option, NO patch-package message in the install log.
  </how-to-verify>
  <resume-signal>Type "approved" once all three browsers pass, OR list specific browser/scenario failures.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "cosmos\.gl\|d3-force" app lib server scripts components` → no matches
- `npm ci` → clean install, no patch-package step (or patch-package present but no-op)
- `npm run build` → green
- Chrome/Safari/Firefox UAT all green (manual checkpoint)
</verification>

<success_criteria>
- Sphere3DGraph is the only graph mounted at /users graph tab
- cosmos.gl + d3-force + patch fully removed
- Phase 6 lands across Chrome/Safari/Firefox
</success_criteria>

<output>
After completion, create `06-08-SUMMARY.md` recording: cross-browser pass rates, observed frame-time p95 per browser, any browser-specific tweaks (Safari gl_PointSize, etc.), and a final cosmos.gl removal grep proof.
</output>
