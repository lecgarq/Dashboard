---
phase: 06-3d-spherical-graph-with-gravity-at-120fps
plan: 07
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - app/(dashboard)/users/sphere3d/replayHarness.ts
  - app/(dashboard)/users/Sphere3DGraph.tsx
  - tests/e2e/sphere3d.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [GRAPH3D-11, GRAPH3D-07, GRAPH3D-08]
must_haves:
  truths:
    - "?replay=1 + warm-up frames + window.__sphere3dReady flag enables stable Playwright screenshots"
    - "Playwright run is green: render, select, filter-reflow, dolly-zoom scenarios pass"
    - "Frame-time p95 reported in test output (drei Stats hook or custom measurement)"
    - "Sphere3DGraph composes all sphere3d/* primitives into a working component"
  artifacts:
    - path: "app/(dashboard)/users/sphere3d/replayHarness.ts"
      provides: "Warmup runner + window flag setter + DPR override under ?replay=1"
    - path: "app/(dashboard)/users/Sphere3DGraph.tsx"
      provides: "Final composition — owns data fetch (trpc.users.getPrecomputedGraph), filter state, URL persistence, integrates PhysicsCompute + NodesPoints + EdgesLines + PickPoints + AutoRotateGroup + CameraTween + BillboardLabels"
    - path: "tests/e2e/sphere3d.spec.ts"
      provides: "Playwright E2E coverage: load, click-select, filter, dolly"
  key_links:
    - from: "Sphere3DGraph.tsx"
      to: "trpc.users.getPrecomputedGraph + accGraphFilters + filterAdapter + topologyAdapter"
      via: "data fetch + filter state → adapter outputs"
      pattern: "(getPrecomputedGraph|useFilteredVisibility|buildEdgeBuffers)"
    - from: "tests/e2e/sphere3d.spec.ts"
      to: "?replay=1 URL"
      via: "page.goto with replay flag + waitForFunction __sphere3dReady"
      pattern: "__sphere3dReady"
---

<objective>
Compose all sphere3d primitives into the final Sphere3DGraph component (data fetch + filter state + render pipeline). Add the replay harness that makes Playwright screenshots deterministic. Write the E2E suite.

Purpose: This is "actually wire up the product." Wave 5 only deletes cosmos.gl after this passes.
Output: 1 harness + 1 final component (replaces the Wave 1 scaffold) + 1 E2E spec.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-3d-spherical-graph-with-gravity-at-120fps/06-RESEARCH.md
@app/(dashboard)/users/AccUsersGraph.tsx
@app/(dashboard)/users/sphere3d/PhysicsCompute.tsx
@app/(dashboard)/users/sphere3d/NodesPoints.tsx
@app/(dashboard)/users/sphere3d/EdgesLines.tsx
@app/(dashboard)/users/sphere3d/PickPoints.tsx
@app/(dashboard)/users/sphere3d/AutoRotateGroup.tsx
@app/(dashboard)/users/sphere3d/BillboardLabels.tsx
@app/(dashboard)/users/sphere3d/CameraTween.tsx
@app/(dashboard)/users/sphere3d/SphereCanvas.tsx
@app/(dashboard)/users/sphere3d/filterAdapter.ts
@app/(dashboard)/users/sphere3d/topologyAdapter.ts
@app/(dashboard)/users/sphere3d/deterministicMode.ts

<interfaces>
trpc.users.getPrecomputedGraph returns `{ hit, nodes, edges, nodeIds, dataHash, stats, positions }` — Sphere3DGraph fetches this (same as AccUsersGraph did at line 492 of the file being replaced).

Filter UI: existing 2D filter panel in AccUsersGraph (lines ~700-1100) must be lifted. Two options:
(a) Lift to a new sibling component `Sphere3DFilterPanel.tsx` and render it next to the canvas.
(b) Keep filter panel inside Sphere3DGraph but as a clearly-delineated subcomponent.
Recommendation: option (a) — separation of concerns. URL persistence helpers (readFiltersFromUrl/writeFiltersToUrl) lift along with the panel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: replayHarness.ts</name>
  <files>app/(dashboard)/users/sphere3d/replayHarness.ts</files>
  <action>
Export `useReplayHarness({ physics }: { physics: PhysicsApi | null })`:
- If `!isReplayMode()` → no-op.
- Inside `useFrame`: if a warmup counter `< REPLAY_WARMUP_FRAMES` (240), increment; once == 240, call `signalReady()` exactly once.
- Force `gl.setPixelRatio(1)` on mount under replay mode (override the Canvas dpr).
- Disable Stats HUD if mounted.
- Return current warmup progress for optional UI use.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hook compiles; under ?replay=1 with a mounted physics, window.__sphere3dReady becomes true after ~4s.</done>
</task>

<task type="auto">
  <name>Task 2: Final Sphere3DGraph.tsx composition</name>
  <files>app/(dashboard)/users/Sphere3DGraph.tsx</files>
  <action>
Replace the Wave 1 scaffold with the full composition:
- Fetch via `trpc.users.getPrecomputedGraph` (mirror exact options from AccUsersGraph.tsx:492).
- Build derived data: `nodes` array, edge buffers via `buildEdgeBuffers(nodes)`, cluster ids via `assignClusterIds(nodes)`.
- Filter state: `useState<AccGraphFilterState>(readFiltersFromUrl())`, write back via `writeFiltersToUrl` (lift these helpers from AccUsersGraph to a new `sphere3d/filterUrl.ts` if cleaner).
- `useFilteredVisibility(nodes, filters)` → `{ visibility, version }`.
- Selected node index state.
- Layout: filter panel (left) + canvas (right) inside the existing tab container, identical CSS classes to AccUsersGraph wrapper so the tab layout doesn't shift.
- Inside `<SphereCanvas>`:
  - `<AutoRotateGroup>`
    - `<PhysicsCompute nodes={nodes} onReady={setPhysicsApi} />`
    - `{physicsApi && (<>
      <NodesPoints ... />
      <EdgesLines ... />
      <BillboardLabels ... />
      <PickPoints ... />
      <CameraTween ... />
      <ReplayHarness physics={physicsApi} />
    </>)}`
- WebGL2 absent → render an empty-state message instead of Canvas (capability check happens inside PhysicsCompute; expose via the `onReady(null)` callback signature).
- Click handler on the Canvas div: extracts cssX/cssY → calls picker → translates index → fires `onSelectUser(nodes[idx].email)`.

NOT YET wired into UsersDirectoryClient — that's Plan 06-08. This file just exists, importable, working in isolation.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sphere3DGraph renders a complete ball from real (or fixture) data, supports click-to-select, filters fade nodes, dolly-zooms into the ball.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E spec + config sanity</name>
  <files>tests/e2e/sphere3d.spec.ts, playwright.config.ts</files>
  <action>
Verify `playwright.config.ts` exists (the project already has `playwright` in deps). If not present, scaffold one targeting `localhost:3000` with Chromium-only (Safari/Firefox parity is covered by CONTEXT but adds CI infra cost; add them as `projects:` entries but mark `.skip` initially with a TODO referencing GRAPH3D-03 cross-browser requirement).

Create `tests/e2e/sphere3d.spec.ts`:
- Test 1 — Render: `page.goto('/users?replay=1')`, click "graph" tab (or whatever the existing tab selector is — verify via DOM inspection), `await page.waitForFunction(() => (window as any).__sphere3dReady === true, { timeout: 30000 })`. Assert `data-testid="sphere3d-root"` is visible. Take screenshot.
- Test 2 — Click-select: simulate click at canvas center (approximate; if no node there, the test asserts that nothing crashes). Then use `page.evaluate` to call a debug hook (expose `window.__sphere3dPickAt(x, y)` from PickPoints layer for testability under replay) → assert it returns a valid node index OR -1.
- Test 3 — Filter-reflow: programmatically toggle a filter (find the filter control DOM), wait 1s, screenshot — visually diff against Test 1 expected difference (subset of nodes visible).
- Test 4 — Dolly: dispatch `wheel` event on canvas with `deltaY: -500`, wait 500ms, screenshot — expect camera distance closer.

All tests use the deterministic replay path. Document any frame-time measurements taken inline.

Add `"e2e": "playwright test"` to package.json scripts if not present.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/sphere3d.spec.ts --project=chromium</automated>
  </verify>
  <done>4 Playwright tests pass on Chromium; Safari/Firefox specs scaffolded but skipped with reference to GRAPH3D-03 cross-browser obligation (deferred to a UAT checkpoint in Wave 5).</done>
</task>

</tasks>

<verification>
- Vitest unit suites still green.
- Playwright Chromium green; ≥1 screenshot recorded.
- `npx tsc --noEmit` clean.
- Sphere3DGraph mounted in dev, manual rotation + select + filter all work.
</verification>

<success_criteria>
- Replay harness sets ready flag deterministically
- Sphere3DGraph is the complete, importable replacement for AccUsersGraph (same props)
- Playwright suite green on Chromium
- Cross-browser test scaffolding present even if skipped
</success_criteria>

<output>
After completion, create `06-07-SUMMARY.md` recording: Playwright test count, Chromium pass rate, frame-time p50/p95 observed during render test, cross-browser status (deferred or executed).
</output>
