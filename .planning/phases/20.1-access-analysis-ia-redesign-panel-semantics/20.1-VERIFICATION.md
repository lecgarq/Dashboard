---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
verified: 2026-07-04T15:39:45Z
status: passed
human_verified: 2026-07-04 — owner approved on :3100 webpack production build (isolated .next-uat-20-1 dist; :3000 untouched)
score: 6/7 must-haves verified
behavior_unverified: 1 # UAT-5 scroll-jump fix: code present + wired, e2e regression spec exists but could not be executed this pass (broken webpack dev-server, pre-existing/documented infra issue)
overrides_applied: 0
behavior_unverified_items:
  - truth: "Clicking a role slice/legend entry no longer scrolls/jumps the page to the top (UAT-5)"
    test: "tests/e2e/access-analysis-scroll.spec.ts — click a real role legend row on the Roles tab and assert the legend's getBoundingClientRect().y is unchanged (or within tolerance) before/after"
    expected: "Legend viewport position does not shift after the click (the ratcheted legendMinHeight + drill-list-after-legend reorder in RolesPieChart.tsx prevent the self-inflicted collapse)"
    why_human: "This is a real-browser layout/reflow invariant that jsdom cannot reproduce (IntersectionObserver stubbed, no layout engine) — it requires Playwright. playwright.config.ts's webServer hardcodes `next dev --webpack`, which 500s on every request on this machine/Next 16.2.6 combination (documented in this phase's own deferred-items.md and reproduced identically by the 20.1-07 executor's own re-run attempt). The verifier does not start ad-hoc dev servers per the spot-check constraints, so the spec's PASS could not be independently reproduced this pass — only its presence and content were confirmed by static review."
human_verification:
  - test: "Re-run tests/e2e/access-analysis-scroll.spec.ts against a working dev server (e.g. `npm run dev -- --turbopack` on a free port, overriding playwright.config.ts's webServer.command for this one run) and confirm it passes."
    expected: "Spec passes; role-legend viewport position is stable across a slice click."
    why_human: "Requires a live browser + a running dev server; the repo's own e2e webServer command is documented-broken (webpack), and starting ad-hoc services is outside verifier scope. The 20.1-07 executor also could not get this spec to run this session (hit the same infra bug) — so the fix has only been proven correct by code review (legendMinHeight ratchet + drill-list reorder), never by an actually-passing regression run."
  - test: "Owner visually re-checks the 4 Task-3 gap-closure fixes live on :3100/:access-analysis: (1) Activity-recency panel subtitles on Roles-tab chart + Users-tab detail table state the underlying question; (2) PermissionLevelChart 'Other' bucket expands in place; (3) FolderActivityByCompanyChart 'Other' bucket expands in place; (4) the global project picker never shows a raw GUID (accessInstanceView/moduleActivityView/activityTimelineView all resolve names)."
    expected: "Owner confirms all 4 items now read/behave as intended and there is no remaining ambiguity."
    why_human: "The owner's only live UAT pass on this phase (`20.1-07-SUMMARY.md`) ended in 'approved but' + these 4 itemized gaps, which were fixed AFTER that checkpoint in the same continuation session. There is no recorded owner re-check of the fixes themselves — only the executor's own code-level verification. Per this task's explicit instruction, this must not be represented as full visual sign-off."
  - test: "Owner explicitly confirms the Compare tab's default-view behavior: with the global picker's default (all projects selected), the tab paints in compare-mode over the top-6-staffed intersection rather than the account-wide all-folders overview."
    expected: "Owner either accepts this as the intended empty/default state, or requests the account-wide overview as the true zero-selection default."
    why_human: "This is a UX/product-taste judgment CONTEXT.md flagged as a locked decision ('0 selected → overview mode') that only technically applies when the picker is EXPLICITLY cleared, not at its default fully-selected state. The 20.1-07-SUMMARY.md records this as 'implicitly accepted' because the owner did not raise it when it was surfaced at the Task 3 checkpoint — but the owner never explicitly affirmed it either."
---

# Phase 20.1: Access-Analysis IA Redesign & Panel Semantics Verification Report

**Phase Goal:** `/access-analysis` is reorganized into 6 themed storytelling tabs (Overview ·
Roles · Users · Companies · Projects · Compare) behind ONE global project picker/FilterBanner,
with the owner's Phase-20 UAT semantics delivered: activity-recency-by-role replaces sign-in
recency, permission-volume-by-level replaces the byte footprint, the terrain folds into the
Compare tab driven by the main search bar, a new folder-activity-by-company graph lands in the
Companies tab, and the role-click scroll-jump is reproduced-then-fixed.

**Verified:** 2026-07-04T15:39:45Z
**Status:** passed (human_needed items resolved by owner approval 2026-07-04 — see Human Verification Resolution)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (roadmap requirement) | Status | Evidence |
|---|------|--------|----------|
| 1 | UAT-7: `/access-analysis` reorganized into 6 themed tabs (Overview·Roles·Users·Companies·Projects·Compare), default Overview | ✓ VERIFIED | `AccessAnalysisCharts.tsx:399-500` — Radix `<Tabs value={tab} onValueChange={setTab}>` with exactly the 6 named `TabsTrigger`/`TabsContent` pairs; `useState("overview")` default. Picker/StatStrip/FilterBanner render above the `<Tabs>` root (lines 345-392), shared across all tabs (single `selected`/`sliceFilters` state, no per-tab remount). |
| 2 | UAT-2: activity recency by role replaces sign-in recency | ✓ VERIFIED | `lib/server/activityRecencyView.ts` (server `$queryRaw` GROUP BY on `AccActivityAccds`), `ActivityRecencyChart.tsx` mounted in `RolesTabPanel.tsx:164` + per-user detail table in `UsersTabPanel.tsx:134`. `DormantSignInChart.tsx`, `signInRecencyView.ts`, `signInRecencyCounts.ts` all deleted; zero remaining non-comment references (grep-verified). |
| 3 | UAT-3: permission volume by level replaces byte footprint | ✓ VERIFIED | `lib/server/permissionLevelView.ts` (own bounded `$queryRaw GROUP BY (projectId, roleId, permType)`, verbatim `permType`, NOT routed through `folderPermQuery.ts`), `PermissionLevelChart.tsx` mounted in `RolesTabPanel.tsx`. `PermissionFootprintChart.tsx`/`permissionFootprintView.ts` deleted; `formatBytes` retained in `permissionFootprintCounts.ts` per the locked decision. |
| 4 | UAT-4: terrain folds into a Compare tab driven by the global picker, own picker removed | ✓ VERIFIED | `CompareTabPanel.tsx` mounts `FolderPermissionTerrain` with `externalSelectedIds={[...selected]} hidePickers`. `FolderPermissionTerrain.tsx` has `deriveTerrainSelection()` (0/1/2+ → overview/single/compare, capped top-6-staffed) + a re-sync `useEffect` keyed on `externalSelectedIds`. Terrain internals (`terrainViewModel.ts`, `TerrainStage.tsx`, `TerrainControls.tsx`, `useFolderPermissionTerrainCamera.ts`, `lib/server/folderPermissionTerrainView.ts`) show **zero commits since 2026-07-03** — untouched, as the plan required. |
| 5 | UAT-6: new folder-activity-by-company graph | ✓ VERIFIED | `lib/server/folderActivityByCompanyView.ts` (server GROUP BY, `folderId IS NOT NULL` filter — never the 190k cross-product), `FolderActivityByCompanyChart.tsx` mounted in `CompaniesTabPanel.tsx`, explicit `UNKNOWN_COMPANY` bucket reused from `companyCounts.ts`. |
| 6 | UAT-5: role-click scroll-jump reproduced then fixed | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `RolesPieChart.tsx` has the `legendMinHeight` ratchet (`Math.max(prev, el.scrollHeight)`, never shrinks) + the drill-down list now renders AFTER the legend (was before) — both match the documented root cause (self-inflicted cross-filter collapse). `tests/e2e/access-analysis-scroll.spec.ts` exists and is a substantive, well-targeted repro/regression spec — but it could not be executed in this verification pass (see Behavioral Spot-Checks) because `playwright.config.ts`'s `webServer.command` hardcodes `next dev --webpack`, which 500s on every request on this machine (documented pre-existing infra bug, `deferred-items.md`), and the phase's own executor hit the identical block on re-run. Code is present and wired; the state-transition invariant it targets has not been behaviorally proven this pass. |
| 7 | Owner has re-checked all six UAT items against the redesigned page (20.1-07's own blocking success criterion) | ⚠️ Partial — see Human Verification | The owner's ONLY live checkpoint (`20.1-07-SUMMARY.md`) returned verbatim: *"approved but i dont like the activity recency detail is not clear... in the permission volume by level i want to expand the others... [same for] folder activity by company. tHERES Still... the guid of the projects rather trhan their actual name."* All 4 items were fixed in the SAME continuation session, but **no subsequent owner re-check of those 4 fixes is recorded.** Per this task's explicit instruction, this is reported honestly as pending, not as full sign-off. |

**Score:** 6/7 truths verified (1 present + wired, behavior/owner-sign-off unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `lib/server/activityRecencyView.ts` | Server-side loader, GROUP BY MAX(createdAt) | ✓ VERIFIED | `$queryRaw` GROUP BY `"projectId","userEmail"`; wired into `mainCharts.tsx` via `loadActivityRecencyAction` |
| `app/(dashboard)/access-analysis/activityRecencyCounts.ts` | Bucket + role-stacked transforms | ✓ VERIFIED | Present, imported by `ActivityRecencyChart.tsx` |
| `app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx` | Role-stacked recency chart | ✓ VERIFIED | Mounted in `RolesTabPanel.tsx`; zinc-resolved colors via `useTheme()` |
| `lib/server/permissionLevelView.ts` | Own bounded GROUP BY over `AccFolderPermission` | ✓ VERIFIED | `$queryRaw GROUP BY f."projectId", fp."roleId", fp."permType"`; not routed through `folderPermQuery.ts` |
| `app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx` | Stacked-bar chart, top-10+Other, expandable | ✓ VERIFIED | `expanded` state re-invokes `summarizePermissionLevel(rows, expanded ? rows.length : DEFAULT_TOP_N)` |
| `lib/server/folderActivityByCompanyView.ts` | Bounded headline + drill loaders | ✓ VERIFIED | `$queryRaw` GROUP BY, `folderId" IS NOT NULL` filter |
| `app/(dashboard)/access-analysis/components/FolderActivityByCompanyChart.tsx` | Company bar chart, expandable Other | ✓ VERIFIED | Same expand-in-place pattern as PermissionLevelChart |
| `app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx` | `externalSelectedIds`/`hidePickers` props | ✓ VERIFIED | `deriveTerrainSelection()` pure helper + re-sync effect present |
| `app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx` | Thin shell: state + picker/banner + Tabs root | ✓ VERIFIED | 532 lines; all 6 `TabsContent` panels wired to sibling components |
| `app/(dashboard)/access-analysis/components/{Overview,Roles,Users,Companies,Projects,Compare}TabPanel.tsx` | 6 presentational tab panels | ✓ VERIFIED | All 6 exist, each imported once in `AccessAnalysisCharts.tsx` |
| `app/(dashboard)/access-analysis/mainCharts.tsx` | 9-entry eager `Promise.all` | ✓ VERIFIED | Exactly 9 entries counted (`view, moduleRows, activityActorRows, coordinationData, coverage, terrainProjects, timeline, dcCoverage, ingestFreshness`); the 3 new loaders passed as function props, fetched lazily |
| `tests/e2e/access-analysis-scroll.spec.ts` | Playwright regression pin | ✓ VERIFIED (existence/content) / ⚠️ not executed | Substantive spec with documented measurement methodology; could not run this pass (see Behavioral Spot-Checks) |
| `DormantSignInChart.tsx`, `PermissionFootprintChart.tsx`, `signInRecencyCounts.ts`, `lib/server/signInRecencyView.ts`, `lib/server/permissionFootprintView.ts` | Deleted, no dangling imports | ✓ VERIFIED | All 5 confirmed absent from disk; grep across `app/`, `lib/`, `components/`, `server/` finds ONLY comment references to the old names, zero import statements. `formatBytes` retained per locked decision. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `AccessAnalysisCharts.tsx` | `RolesTabPanel.tsx` / `UsersTabPanel.tsx` / `CompaniesTabPanel.tsx` | Lazy-fetch effect keyed on `tab`, ref flags gate one-shot fetch | ✓ WIRED | `useEffect` at lines 261-283 fires `loadActivityRecency`/`loadPermissionLevel`/`loadFolderScopedActivity` at most once per page load, keyed on first Roles/Users/Companies activation |
| `CompareTabPanel.tsx` | `FolderPermissionTerrain.tsx` | `externalSelectedIds`/`hidePickers` props | ✓ WIRED | Confirmed prop pass-through; terrain re-syncs mode/selection via `useEffect` on `externalKey` change |
| `mainCharts.tsx` | `AccessAnalysisCharts.tsx` | `loadActivityRecency`/`loadPermissionLevel`/`loadFolderScopedActivity`/`loadCompanyFolderBreakdown` function props | ✓ WIRED | All 4 server actions imported and passed; none ride the eager `Promise.all` |
| `lib/server/accessInstanceView.ts` / `moduleActivityView.ts` / `activityTimelineView.ts` | `buildProjectNameMap`/`resolveProjectName` (`folderActivityView.ts`) | Shared import, "Unknown project" fallback | ✓ WIRED | All 3 confirmed importing and calling both functions; matches the pre-existing correct pattern in `coordinationByProjectView.ts`/`permissionLevelView.ts` |
| `RolesPieChart.tsx` legend | drill-down list | Render order (legend before drill list) + `legendMinHeight` ratchet | ✓ WIRED (code) / ⚠️ not behaviorally proven | See Observable Truths #6 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `ActivityRecencyChart.tsx` | `filteredActivityRecencyRows` | `loadActivityRecencyAction` → `lib/server/activityRecencyView.ts` `$queryRaw` on `AccActivityAccds` | Yes — real GROUP BY, not static | ✓ FLOWING |
| `PermissionLevelChart.tsx` | `filteredPermissionLevelRows` | `loadPermissionLevelAction` → `permissionLevelView.ts` `$queryRaw` on `AccFolderPermission` | Yes | ✓ FLOWING |
| `FolderActivityByCompanyChart.tsx` | `filteredFolderScopedActivityRows` | `loadFolderScopedActivityAction` → `folderActivityByCompanyView.ts` `$queryRaw` on `AccActivityAccds` (`folderId IS NOT NULL`) | Yes | ✓ FLOWING |
| `CompareTabPanel.tsx` terrain | `terrainProjects`/`loadTerrain`/`loadOverview` | `mainCharts.tsx` → `lib/server/folderPermissionTerrainView.ts` (untouched this phase) | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `npx tsc --noEmit` clean | `npx tsc --noEmit` | No output, exit 0 | ✓ PASS |
| Full Vitest suite matches SUMMARY's "2392 passed/1 skipped/0 failed" claim | `npm test` (excludes `tests/e2e/**`) | `Test Files 316 passed \| 1 skipped (317)` / `Tests 2392 passed \| 1 skipped (2393)` | ✓ PASS — independently reproduces the exact SUMMARY figure |
| Shell test suite (tab IA) green | `npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx"` | 38/38 passed | ✓ PASS |
| New loader test files green | `npx vitest run lib/server/__tests__/{activityRecencyView,permissionLevelView,folderActivityByCompanyView}.test.ts` | 18/18 passed | ✓ PASS |
| Playwright scroll-jump regression spec passes | `npx playwright test tests/e2e/access-analysis-scroll.spec.ts` | NOT RUN — `playwright.config.ts`'s `webServer.command` (`next dev --webpack`) is documented-broken on this machine (`deferred-items.md`); starting an ad-hoc dev server is outside verifier scope (spot-check constraint: "do not start servers or services") | ? SKIP — routed to Human Verification |
| package.json unchanged (no new npm dependency) since Phase 20 close | `git diff 4c77a106..HEAD -- package.json` | Empty diff | ✓ PASS |
| No commit under 20.1 touches `/users/spatial-graph` or `/template-mty` | `git show --name-only` over all 20 code commits in the phase | Zero matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| UAT-2 | 20.1-01, 20.1-06 | Activity recency by role replaces sign-in recency | ✓ SATISFIED | See Truths #2 |
| UAT-3 | 20.1-02, 20.1-06 | Permission volume by level replaces byte footprint | ✓ SATISFIED | See Truths #3 |
| UAT-4 | 20.1-04, 20.1-05 | Terrain in Compare tab, driven by global picker | ✓ SATISFIED | See Truths #4 |
| UAT-5 | 20.1-07 | Role-click scroll-jump fixed | ⚠️ CODE SATISFIED / BEHAVIOR UNVERIFIED | See Truths #6 |
| UAT-6 | 20.1-03, 20.1-06 | New folder-activity-by-company graph | ✓ SATISFIED | See Truths #5 |
| UAT-7 | 20.1-05, 20.1-06 | Tabbed IA overall | ✓ SATISFIED | See Truths #1 |

No ORPHANED requirements — this is an inserted urgent decimal phase; UAT-2..UAT-7 are the complete requirement set per `ROADMAP.md`'s Phase 20.1 entry, and all 6 are claimed across the 7 plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `app/(dashboard)/access-analysis/components/CompareTabPanel.tsx` | 13 | Comment references deleted `TerrainReveal` | ℹ️ Info | Stale doc comment only, not a dangling import (tsc clean); harmless |
| `app/(dashboard)/access-analysis/mainCharts.tsx`, `page.tsx`, `page.test.tsx` | various | Comments reference deleted `TerrainReveal` | ℹ️ Info | Same — cosmetic doc staleness, no functional impact |
| `app/(dashboard)/access-analysis/components/UsersTabPanel.tsx` | 142-144 | `placeholder="Search name, company, or role…"` | ℹ️ Info | Legitimate HTML input placeholder attribute, not a debt marker |
| `app/(dashboard)/users/access-analysis/HybridAnalyticsPostureSection.tsx` ↔ `HybridAnalyticsView.tsx`; `HybridAnalyticsRankingsSection.tsx` ↔ `HybridAnalyticsView.tsx` | — | 2 circular-import pairs flagged by `node scripts/repo-map/check.cjs` (reported as "4 above baseline") | ℹ️ Info, OUT OF SCOPE | Pre-existing from Phase 17 (`da2f230b`, 2026-07-01) on the **unrelated, unmounted `/users/access-analysis` route** (redirects to `/users/spatial-graph`, no production mount per STATE.md's Phase 17 open item). Zero 20.1 commits touch these files (confirmed via `git log`); the repo-map baseline predates Phase 17 (2026-06-17) so it was never captured there. Not a 20.1 regression — flagged here for visibility only. |

No `TBD`/`FIXME`/`XXX` debt markers found in any file changed by this phase.

### Human Verification Required

#### 1. Playwright scroll-jump regression spec — never actually executed

**Test:** Run `npx playwright test tests/e2e/access-analysis-scroll.spec.ts` against a working dev server (Turbopack, not the config-default webpack) and confirm it passes.
**Expected:** The spec passes — role-legend `getBoundingClientRect().y` stable across a slice click.
**Why human:** Real-browser layout invariant, jsdom cannot reproduce it. The repo's `playwright.config.ts` hardcodes a dev-server command (`next dev --webpack`) that is documented-broken on this machine; the phase's own executor hit the identical block on re-run and never got a passing execution recorded this session. The code fix (legend `minHeight` ratchet + drill-list reorder) is sound by static review, but "the bug is fixed" has only been proven by code reading, never by a green regression run.

#### 2. Owner has not re-checked the 4 Task-3 gap-closure fixes

**Test:** Owner reviews `/access-analysis` live (Roles/Users/Companies tabs + the global project picker) against the 4 items from their own verbatim "approved but" feedback: activity-recency panel clarity, expand-in-place on Permission-volume-by-level's "Other", expand-in-place on Folder-activity-by-company's "Other", and no raw project GUIDs in the picker.
**Expected:** Owner confirms all 4 read/behave as intended.
**Why human:** These fixes were made AFTER the owner's only recorded live checkpoint on this phase; no subsequent owner re-check is documented anywhere in `20.1-07-SUMMARY.md` or `STATE.md`. Only the executor's own code review verifies correctness.

#### 3. Compare-tab default-view assumption

**Test:** Owner explicitly confirms whether the Compare tab, at its default (all-projects-selected) state, should paint compare-mode over the top-6-staffed intersection (current behavior) or the account-wide all-folders overview.
**Expected:** Explicit owner confirmation either way.
**Why human:** `20.1-CONTEXT.md` locked "0 selected → overview mode," which only fires on an EXPLICITLY cleared picker, not the default fully-selected state. `20.1-07-SUMMARY.md` records this as "implicitly accepted" (the owner didn't object when it was surfaced), which is a reasonable inference but not an explicit sign-off.

### Gaps Summary

No FAILED truths, no MISSING/STUB artifacts, no NOT_WIRED key links, and no blocker anti-patterns
were found. Every one of the six owner UAT items (UAT-2/3/4/5/6/7) has a substantive, wired,
data-flowing implementation in the codebase, matching (and in several places exceeding, e.g. the
GUID-leak diagnosis spanning 3 loaders) the SUMMARY narrative. `npx tsc --noEmit` is clean, the
full Vitest suite independently reproduces the exact "2392 passed/1 skipped/0 failed" figure
claimed in `STATE.md`/`20.1-07-SUMMARY.md`, `package.json` has zero diff since Phase 20 closed
(no new dependency), and no commit in the phase touches `/users/spatial-graph` or `/template-mty`.

The phase is held at `human_needed` — not `passed` — for two honest reasons: (1) the UAT-5
scroll-jump fix is well-reasoned code but its own regression proof (the Playwright spec written
for exactly this purpose) has never actually passed in this session or the executor's own
session, both blocked by the same pre-existing, already-documented broken `webpack` dev-server
command; and (2) the owner's only live UAT pass on this phase predates the 4 gap-closure fixes
that followed it — there is no recorded re-check of those specific fixes, only the phase's own
code-level verification. Both are exactly the kind of claim this task explicitly asked not to be
represented as full sign-off.

## Dashboard Self-Check

- **Context:** `.planning/STATE.md`, `.planning/PROJECT.md` (via STATE cross-references),
  `.planning/ROADMAP.md` Phase 20.1 section, `20.1-CONTEXT.md`, all 7 `20.1-0N-PLAN.md` frontmatter,
  `20.1-07-SUMMARY.md`, `deferred-items.md` all read. `.planning/codebase/*.md` and
  `.tools/repo-map/architecture-summary.md` were not separately re-read this pass (STATE.md's
  detailed per-plan decision log already supplied equivalent architecture context for this
  narrowly-scoped verification); repo-map was, however, actually re-run (`npm run repo-map:check`)
  for the boundary/circular-import check.
- **Scope matched:** `/access-analysis` only. Confirmed via `git show --name-only` over all 20
  code commits in the phase — every changed path is under `app/(dashboard)/access-analysis/`,
  `lib/server/`, or `tests/e2e/`. Zero touches to `/users/spatial-graph` or `/template-mty`.
- **Exact artifacts:** every path, loader, component, and test cited above was opened and read
  directly (Read/Grep/Bash), not inferred from SUMMARY prose.
- **Repo roots:** no generic `src/...` claims; all paths use the verified `app/(dashboard)/`,
  `lib/server/` roots.
- **Data truth:** all 3 new loaders confirmed server-side `$queryRaw`/GROUP BY, never
  `findMany` + JS reduce; "Never active"/"Unknown company"/"Unknown project" fallbacks all
  present and grep-verified.
- **UI constraints:** zinc-scale hex colors resolved via `useTheme()`/`resolvedTheme` in all 3
  new charts; no new WebGL/React-Three-Fiber imports found under `access-analysis/components/`;
  no card-inside-card drift observed in the reviewed panel files.
- **Boundary constraints:** new loaders live in `lib/server/`, not inside components; terrain's
  internal geometry/camera/scene modules confirmed untouched (zero commits since 2026-07-03).
- **Gates:** `npx tsc --noEmit` (clean), full `npm test` (2392/1/0, matches SUMMARY), targeted
  Vitest files for the shell + 3 new loaders (all green), `npm run repo-map:check` (clean except
  a pre-existing, out-of-scope circular-import pair from Phase 17 on an unrelated dead route).
  Playwright e2e was **not** run (see Human Verification #1) — this is disclosed, not silently
  skipped.
- **VERIFY:** none beyond what's captured in Human Verification above.

---

_Verified: 2026-07-04T15:39:45Z_
_Verifier: Claude (gsd-verifier)_

## Human Verification Resolution (2026-07-04)

Owner re-checked live on `http://localhost:3100/access-analysis`, served from a
**webpack production build** (`NEXT_DIST_DIR=.next-uat-20-1 npx next build --webpack`
+ `next start`) after the Turbopack dev server proved deterministically unusable
(4/4 fresh-cache boots corrupted generated CSS — same 496 parse errors each run;
see deferred-items.md item 2, now known to be content-deterministic, not ~50%
intermittent). Production pipeline (`next build --webpack`) is unaffected — this
build also serves as the successful pre-deploy preflight for the `:3000` rebuild.

Owner response: **"approved"** — covering:
1. Activity-recency panel goal/clarity copy (gap item 1)
2. PermissionLevelChart expandable "Other" (gap item 2)
3. FolderActivityByCompanyChart expandable "Other" (gap item 3)
4. Global picker GUID resolution (gap item 4)
5. UAT-5 scroll-jump behavior verified visually on the production build (closes
   the `behavior_unverified` item — the e2e spec still cannot run via the repo's
   broken `next dev --webpack` webServer command; deferred-items.md)
6. Compare-tab default-view assumption (compare-mode over top-6-staffed with
   all-selected picker) — implicitly accepted, owner may still override later.
