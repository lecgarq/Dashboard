---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/activityRecencyView.ts
  - lib/server/__tests__/activityRecencyView.test.ts
  - app/(dashboard)/access-analysis/activityRecencyActions.ts
  - app/(dashboard)/access-analysis/activityRecencyCounts.ts
  - app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts
  - app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx
  - app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx
autonomous: true
requirements: [UAT-2]

must_haves:
  truths:
    - "A loader exists that computes each DC membership's latest AccActivityAccds event date, with roles attached"
    - "Bucketing produces the 5 locked bands (<30d/30-90d/90-365d/>365d/Never active) grouped by role, losslessly (band sums == row count)"
    - "The chart renders a role-stacked recency chart with a live coverage caption and an honest 'Never active' explanation"
  artifacts:
    - path: "lib/server/activityRecencyView.ts"
      provides: "loadActivityRecency loader + pure assembleActivityRecency, AccActivityAccds MAX(createdAt) per (projectId,userEmail) joined onto AccDcProjectUser population with resolved roles"
    - path: "app/(dashboard)/access-analysis/activityRecencyCounts.ts"
      provides: "bucketActivityRecency + summarizeActivityRecencyByRole pure transforms"
    - path: "app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx"
      provides: "Role-stacked recency ECharts panel with band drill"
    - path: "app/(dashboard)/access-analysis/activityRecencyActions.ts"
      provides: "Auth-gated 'use server' action for lazy per-tab fetch"
  key_links:
    - from: "lib/server/activityRecencyView.ts"
      to: "AccActivityAccds"
      via: "server-side $queryRaw GROUP BY (projectId,userEmail) MAX(createdAt) — never findMany+reduce"
      pattern: "queryRaw[\\s\\S]*MAX\\(\"createdAt\"\\)"
    - from: "app/(dashboard)/access-analysis/activityRecencyCounts.ts"
      to: "app/(dashboard)/access-analysis/roleCounts.ts"
      via: "UNKNOWN_ROLE / MULTIPLE_ROLES attribution constants (0/1/2+ roles)"
      pattern: "UNKNOWN_ROLE|MULTIPLE_ROLES"
    - from: "app/(dashboard)/access-analysis/activityRecencyActions.ts"
      to: "lib/server/activityRecencyView.ts"
      via: "auth-gated server action wrapper"
      pattern: "loadActivityRecency"
---

<objective>
Build the ENG-01 semantic pivot (owner UAT item 2, verbatim: prefers "activity recency by
role" instead of sign-in recency): a new loader/transform/chart vertical slice computing
recency bands from each user's latest `AccActivityAccds` event, grouped by role. Built
UNMOUNTED — plan 20.1-06 wires it into the Roles/Users tabs and removes the old
sign-in-recency panel. Nothing in this plan touches `mainCharts.tsx` or
`AccessAnalysisCharts.tsx`.

Purpose: the owner rejected sign-in recency as the engagement signal; activity recency is
the truthful one. Workshop impact lands in 20.1-06.
Output: `loadActivityRecency` loader + action + `summarizeActivityRecencyByRole` transform
+ `ActivityRecencyChart` component, all unit-tested.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-CONTEXT.md
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture Patterns §2)
@lib/server/signInRecencyView.ts        (the panel being semantically replaced — copy its assemble/cache/test shape)
@lib/server/activityByActorView.ts      (the (projectId,userEmail) GROUP BY raw-SQL shape to mirror)
@lib/server/accessInstanceView.ts       (mergeRoleNames(dc, live) — reuse, do not re-derive)
@app/(dashboard)/access-analysis/signInRecencyCounts.ts (band boundaries + lossless summarize shape to mirror)
@app/(dashboard)/access-analysis/roleCounts.ts (UNKNOWN_ROLE, MULTIPLE_ROLES constants)
@app/(dashboard)/access-analysis/components/DormantSignInChart.tsx (chart/drill/caption template)
@app/(dashboard)/access-analysis/folderTerrainActions.ts ("use server" action template)

<interfaces>
Live-verified (20.1-RESEARCH.md §2): `AccDcProjectUser` = 22,835 rows; distinct
`(projectId,userEmail)` MAX(createdAt) over AccActivityAccds = 12,269 rows; only ~33.2% of
DC memberships have any accds activity — "Never active" will be the LARGEST bucket, which
is real and expected. accds covers 956/1,153 projects (must be read live, never hardcoded).

From lib/server/accessInstanceView.ts:
```typescript
export function mergeRoleNames(
  dc: Array<{ id: string; name: string }>,
  live: Array<{ id: string; name: string }>,
): Map<string, string>; // live (AccRole) is base, DC (AccDcRole) overrides
```

From prisma/schema.prisma (verified): `model AccDcProjectUserRole { projectId, userId,
roleId, ingestRunId, ingestedAt; @@id([projectId, userId, roleId]) }`

Recommended raw SQL (research-verified, 12,269 rows):
```sql
SELECT "projectId", "userEmail", MAX("createdAt") AS "lastActivityAt"
FROM "AccActivityAccds"
WHERE "userEmail" IS NOT NULL AND "projectId" IS NOT NULL AND "projectId" <> ''
GROUP BY "projectId", "userEmail"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: activityRecencyView loader + server action</name>
  <files>lib/server/activityRecencyView.ts, lib/server/__tests__/activityRecencyView.test.ts, app/(dashboard)/access-analysis/activityRecencyActions.ts</files>
  <action>
Create `lib/server/activityRecencyView.ts` ("server-only", `db` from `@/server/db`),
mirroring `signInRecencyView.ts`'s structure (pure assemble + 5-min TTL cache + loader):

- Export `interface ActivityRecencyRow { projectId: string; email: string; name: string;
  company: string; roles: string[]; lastActivityAt: string | null }`. Roles are RESOLVED
  NAMES (0..n) — the 0/1/2+ attribution happens client-side in the transform (lib must not
  import app modules; Phase 10 boundary rule).
- Export pure `assembleActivityRecency(projectUsers, users, companies, userRoles,
  roleNames, lastActivityByKey)` where `lastActivityByKey` is a
  `Map<"projectId::loweredEmail", string /* ISO */>`. Population = every AccDcProjectUser
  row (same 22,835-row population as signInRecencyView — same DC-coverage class). Fallbacks
  mirror `assembleSignInRecency`: missing AccDcUser → name "Unknown user", company
  "Unknown company"; membership email null/missing from activity map → lastActivityAt null
  (→ "Never active" downstream, never dropped).
- `loadActivityRecency(force = false)`: Promise.all of (a) the raw SQL above via
  `db.$queryRaw` (typed `{ projectId: string; userEmail: string; lastActivityAt: Date }[]`
  — CONTEXT locks the source to AccActivityAccds ONLY; do NOT reuse
  loadActivityByActor's accds+DC-backfill union), (b) `db.accDcProjectUser.findMany`
  (projectId, userId), (c) `db.accDcUser.findMany` (id, name, email, companyId),
  (d) `db.accDcCompany.findMany` (id, name), (e) `db.accDcProjectUserRole.findMany`
  (projectId, userId, roleId), (f) `db.accRole.findMany` + `db.accDcRole.findMany`
  (id, name) resolved through `mergeRoleNames(dcRows, liveRows)` imported from
  `./accessInstanceView`. Join activity by `${projectId}::${email.toLowerCase()}`.
  Serialize dates with `.toISOString()`. 5-min TTL cache, `force` bypass.

Create `app/(dashboard)/access-analysis/activityRecencyActions.ts` ("use server"),
mirroring `folderTerrainActions.ts` exactly: `loadActivityRecencyAction():
Promise<ActivityRecencyRow[] | null>` — `auth()` from `@/server/auth`, return null when no
session, else `loadActivityRecency()`.

Create `lib/server/__tests__/activityRecencyView.test.ts` testing the PURE assemble only
(no DB): (1) bounded output — rows.length === projectUsers.length (never activity-row
scale); (2) unknown-user fallbacks; (3) lastActivityAt null when membership has no
activity-map entry; (4) role names resolve via the merged map, missing roleId → dropped
from roles[] (empty array is legitimate); (5) email join is case-insensitive.
  </action>
  <verify>
    <automated>npx vitest run lib/server/__tests__/activityRecencyView.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Loader + action exist; pure assemble tests green; tsc clean; no lib→app import.</done>
</task>

<task type="auto">
  <name>Task 2: activityRecencyCounts pure transform</name>
  <files>app/(dashboard)/access-analysis/activityRecencyCounts.ts, app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts</files>
  <action>
Create `app/(dashboard)/access-analysis/activityRecencyCounts.ts` (pure, no React/DOM/IO),
mirroring `signInRecencyCounts.ts`:

- `export const ACTIVITY_RECENCY_BANDS = ["<30d", "30–90d", "90–365d", ">365d", "Never active"] as const;`
  (same en-dash style; last band label is "Never active", NOT "Never signed in").
- `bucketActivityRecency(lastActivityAt, nowMs)` — copy `bucketSignInRecency`'s exact
  date-fns defensive parsing and day boundaries (<30 / <=90 / <=365 / else), null/invalid →
  "Never active". CONTEXT locks the boundaries: <30d=[0,29], 30-90d=[30,90],
  90-365d=[91,365], >365d=[366+].
- Role attribution helper `attributeRole(roles: string[]): string` — 0 roles →
  `UNKNOWN_ROLE`, 1 → that name, 2+ → `MULTIPLE_ROLES` (import both constants from
  `./roleCounts`; exactly the `summarizeActivityByRole` rule in roleActivityCounts.ts).
- `export interface ActivityRecencySummary { bands: { band; count }[]; roleNames: string[];
  countsByRole: Map<string /* attributed role */, number[] /* per band, locked order */>;
  usersByBand: Map<Band, { name; company; role; lastActivityAt }[]>; total: number }`
- `summarizeActivityRecencyByRole(rows: ActivityRecencyRow[], nowMs: number)`:
  buckets each row once; `bands` always contains all 5 (zeros kept); `roleNames` = top-8
  attributed roles by total membership count desc + a trailing "Other roles" aggregate when
  more exist (so the stack legend stays readable); countsByRole covers every listed
  roleName incl. "Other roles"; usersByBand drill lists sorted oldest-activity-first per
  dated band ("Never active" keeps input order). LOSSLESS INVARIANT: sum of all band
  counts === rows.length AND sum over countsByRole === rows.length.

Test file: band boundary edges at 29/30/90/91/365/366 days (deterministic nowMs), null and
garbage-string → "Never active", lossless invariants, 0/1/2+ role attribution, all-5-bands
always present, top-8+"Other roles" collapse math.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts"</automated>
  </verify>
  <done>Transform green incl. lossless invariant and boundary-edge tests.</done>
</task>

<task type="auto">
  <name>Task 3: ActivityRecencyChart component</name>
  <files>app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx, app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx</files>
  <action>
Create `"use client"` `ActivityRecencyChart.tsx` using `DormantSignInChart.tsx` as the
literal template (same EChart import from `@/components/ui/EChart`, same CSS-variable
theme-color resolution approach, same band click-to-drill people list):

Props: `{ rows: ActivityRecencyRow[]; coverage?: { covered: number; total: number };
dataFloor?: string | null }`. Internally `useMemo(() => summarizeActivityRecencyByRole(rows,
Date.now()), [rows])`.

- Chart: ECharts stacked bars — x axis = the 5 bands (locked order), one series per
  `roleNames` entry (stack: "recency"), colors via `buildRoleColorMap` from `../roleColors`
  so role colors match the existing Role-distribution donut; "Other roles" and
  UNKNOWN_ROLE/MULTIPLE_ROLES get muted fallback colors. Value labels + tooltip show
  per-role and band-total counts.
- Drill: clicking a band (bar click via `onEvents`, `params.name` = band) toggles an inline
  people list for that band (name · company · role · last-activity date or "Never active"),
  following DormantSignInChart's drill pattern. Motion ≤200ms.
- Honest captions (both mandatory, muted `text-muted-foreground` text): (1) coverage —
  when `coverage` prop present render "Activity data covers {covered} of {total} ACC
  projects — memberships come from the DC snapshot" using the LIVE numbers (never
  hardcoded); (2) semantics — ' "Never active" = no recorded activity in the ACCDS-crawled
  window' plus, when `dataFloor` is set, "data available from {dataFloor}".
- Empty state: `rows.length === 0` renders a "No memberships in this view" line, not a
  broken chart.

Test file (mirror DormantSignInChart.test.tsx's mocking approach for EChart): renders all
5 band labels; "Never active" bucket present with correct count from fixture rows incl. a
null-lastActivityAt row; coverage caption renders the live numbers passed via props;
band-click opens drill list with expected people; empty state renders.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Chart component green + tsc clean. Component is NOT mounted anywhere yet (verify: `grep -r "ActivityRecencyChart" app | grep -v __tests__ | grep -v "components/ActivityRecencyChart"` returns nothing).</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/server/__tests__/activityRecencyView.test.ts "app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx"` green.
- `npx tsc --noEmit` clean.
- No file in `lib/` imports from `app/` (Phase 10 boundary); no edits to `mainCharts.tsx`, `AccessAnalysisCharts.tsx`, `signInRecencyView.ts`, or anything under `/users/spatial-graph`.
- All aggregates are server-side SQL GROUP BY — no `findMany` over AccActivityAccds.
</verification>

<success_criteria>
ENG-01 pivot slice exists end-to-end (loader → action → transform → chart), unit-tested
with lossless band invariants and honest live-coverage captions, ready for 20.1-06 to mount.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-01-SUMMARY.md`
</output>
