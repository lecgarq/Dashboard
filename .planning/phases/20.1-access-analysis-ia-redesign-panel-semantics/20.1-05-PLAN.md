---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "05"
type: execute
wave: 2
depends_on: ["20.1-04"]
files_modified:
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/SectionHeaders.tsx
  - app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx
  - app/(dashboard)/access-analysis/components/RolesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/UsersTabPanel.tsx
  - app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
  - app/(dashboard)/access-analysis/components/CompareTabPanel.tsx
  - app/(dashboard)/access-analysis/components/TerrainReveal.tsx
  - app/(dashboard)/access-analysis/__tests__/TerrainReveal.test.tsx
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: true
requirements: [UAT-7, UAT-4]

must_haves:
  truths:
    - "/access-analysis renders 6 themed tabs (Overview · Roles · Users · Companies · Projects · Compare), defaulting to Overview"
    - "StatStrip, coverage header, global ProjectPicker, and FilterBanner/idle-tip stay pinned ABOVE the tab strip; switching tabs never resets selection or sliceFilters"
    - "The Compare tab hosts the terrain driven by the global picker selection with its own pickers hidden; deselecting everything shows the all-folders overview"
    - "Every one of the current 11 panels is reachable inside its assigned tab (nothing silently dropped)"
  artifacts:
    - path: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      provides: "Thin shell: shared state + picker/banner + <Tabs> root; panel JSX moved out"
    - path: "app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx"
      provides: "Overview tab: Activity over time · Activity by module · Ingest freshness"
    - path: "app/(dashboard)/access-analysis/components/CompareTabPanel.tsx"
      provides: "Compare tab: FolderPermissionTerrain with externalSelectedIds + hidePickers"
    - path: "app/(dashboard)/access-analysis/components/SectionHeaders.tsx"
      provides: "Shared SectionHeader / SectionHeaderWithPeople extracted from the old monolith"
  key_links:
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "components/ui/tabs.tsx"
      via: "Radix Tabs shell (client state, NOT URL params — Pitfall 5)"
      pattern: "TabsList|TabsTrigger|TabsContent"
    - from: "app/(dashboard)/access-analysis/components/CompareTabPanel.tsx"
      to: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      via: "externalSelectedIds={[...selected]} hidePickers"
      pattern: "externalSelectedIds"
---

<objective>
Reorganize `/access-analysis` into the 6 locked themed tabs (owner UAT item 7: current
layout "is all over the place" — tabs are for storytelling), splitting the 722-line
`AccessAnalysisCharts.tsx` into a thin shell (shared state + global picker/FilterBanner
pinned above the tab strip) plus six flat sibling tab-panel components, and fold the
terrain into the Compare tab driven by the global picker (owner UAT item 4). All existing
panels relocate; panel SEMANTICS are unchanged in this plan (the ENG-01/PERM-01 swaps and
new company graph land in 20.1-06). `mainCharts.tsx` is NOT touched — the existing 11-entry
eager fan-out stays as-is until 20.1-06 trims it.

Interim placements this plan uses (locked tab map + Claude-discretion assignments):
- Overview → Activity over time · Activity by module · Ingest freshness
- Roles → Role distribution · Activity by role · Permission footprint (interim, replaced
  in 20.1-06) · Folder Activity by Role (FolderActivityReveal — role-scoped, discretion)
- Users → Dormant users (interim sign-in chart, replaced in 20.1-06)
- Companies → Users by company · Activity by company
- Projects → Issue data coverage · Model Coordination (Ph21-22 issue charts land here later)
- Compare → folder permission terrain (global-picker-driven, own pickers hidden)

RECORDED ASSUMPTION (research Open Question 1, surface to owner at the 20.1-07 checkpoint):
the global picker defaults to ALL projects selected, so Compare's first paint derives
compare-mode over the top-6-staffed intersection; "no project selected → all-folders
overview" applies when the user actively clears the selection.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-CONTEXT.md (locked tab structure + filter scope)
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture §1, §4; Pitfalls 5, 6)
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx (the 722-line file being split)
@components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent — Radix, inactive content UNMOUNTS by default)
@app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx (428 lines / 33 tests to migrate)

<interfaces>
Shell state that MUST stay in AccessAnalysisCharts (all currently at lines 125-279):
`options`, `counts`, `mtySet`, `coverageMap`, `groups`, `selected` (Set<string>, defaults
to ALL), `profileEmail`, `peopleSheet`, `sliceFilters` + `toggleSliceFilter`,
`sliceFilteredProjectIds`, all `useMemo` summaries (timelineSummary, roleSummary,
moduleSummary, activityByRoleSummary, companySummary, activityByCompanySummary,
coordSummary, filteredPermissionFootprintRows, filteredSignInRecencyRows,
filteredIssueCoverageProjects, dormantRoles, dormantCompanies, covCovered/covTotal, kpis).
Tab panels are PRESENTATIONAL — they receive computed summaries/handlers as props; no
Prisma, no new state buses.

Rendered ABOVE the Tabs root (unchanged order): StatStrip → coverage-header <p> →
ProjectPicker → idle-tip/FilterBanner ternary. Global overlays (AuthorProfileDrawer,
DrillSheet + PeopleDrillList) stay in the shell BELOW the Tabs root.

Radix note: `TabsContent` unmounts inactive panels (no forceMount) — desired; per-tab data
caching concerns arrive with the lazy loaders in 20.1-06, not here (all data is already
eager props).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SectionHeaders + create the 6 tab panel components</name>
  <files>app/(dashboard)/access-analysis/components/SectionHeaders.tsx, app/(dashboard)/access-analysis/components/OverviewTabPanel.tsx, app/(dashboard)/access-analysis/components/RolesTabPanel.tsx, app/(dashboard)/access-analysis/components/UsersTabPanel.tsx, app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx, app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx, app/(dashboard)/access-analysis/components/CompareTabPanel.tsx</files>
  <action>
1. Create `SectionHeaders.tsx`: move `SectionHeader` and `SectionHeaderWithPeople`
   (currently private functions at the bottom of AccessAnalysisCharts.tsx, lines 633-722)
   VERBATIM into this new file and export both. No visual changes.
2. Create the six `"use client"` tab panel components (flat siblings, matching the existing
   no-subfolder convention). Each moves the corresponding `<Reveal><PremiumSurface>` panel
   JSX out of AccessAnalysisCharts.tsx UNCHANGED (same titles, subtitles, badges, testids,
   glow props, conditional-render guards, TRUTH-03 tooltip, grid classes), importing
   SectionHeader/SectionHeaderWithPeople from `./SectionHeaders`:
   - `OverviewTabPanel`: Activity over time (full width) + Activity by module + Ingest
     freshness strip. Keep the module panel's `lg:col-span-2` grid treatment by giving the
     panel its own grid wrapper.
   - `RolesTabPanel`: Role distribution + Activity by role (2-up `lg:grid-cols-2` grid) +
     Permission footprint (interim) + FolderActivityReveal (full width).
   - `UsersTabPanel`: Dormant users (interim DormantSignInChart panel).
   - `CompaniesTabPanel`: Users by company + Activity by company (2-up grid).
   - `ProjectsTabPanel`: Issue data coverage + Model Coordination (full width, coverage
     donut FIRST — trust precedes metric). Include a muted placeholder-free comment noting
     Ph21-22 issue charts mount here.
   - `CompareTabPanel`: `<FolderPermissionTerrain projects={terrainProjects}
     initial={null} loadTerrain={loadTerrain} loadOverview={loadOverview}
     externalSelectedIds={[...selected]} hidePickers />` (the 20.1-04 props) inside a
     PremiumSurface with a SectionHeader ("Folder permission terrain", subtitle noting it
     follows the main project selection; explicitly distinguish the page tab from the
     terrain's internal Compare view mode in the subtitle or a muted caption). No
     TerrainReveal expand gate — tab activation IS the lazy gate (Radix mounts content on
     first activation only).
   Prop types for each panel: exactly the summaries/handlers/rows it renders (typed, no
   `any`), e.g. RolesTabPanel gets roleSummary, activityByRoleSummary, dormantRoles,
   sliceFilters, toggleSliceFilter, setProfileEmail, setPeopleSheet, covCovered/covTotal,
   filteredPermissionFootprintRows, folder-activity props, membershipRows, selected.
   Keep each file well under ~400 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>7 new files exist, tsc clean (panels compile against real prop types even before the shell renders them).</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the shell around a Tabs root; retire TerrainReveal</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/components/TerrainReveal.tsx, app/(dashboard)/access-analysis/__tests__/TerrainReveal.test.tsx</files>
  <action>
1. Rewrite AccessAnalysisCharts.tsx's return JSX: keep everything above the panels
   (StatStrip, coverage header, ProjectPicker, idle-tip/FilterBanner ternary) EXACTLY as-is,
   then replace the flat panel wall with:
   ```tsx
   <Tabs defaultValue="overview" className="gap-6">
     <TabsList variant="line" className="w-full justify-start overflow-x-auto">
       <TabsTrigger value="overview">Overview</TabsTrigger>
       ... Roles · Users · Companies · Projects · Compare
     </TabsList>
     <TabsContent value="overview"><OverviewTabPanel ... /></TabsContent>
     ... one TabsContent per tab
   </Tabs>
   ```
   Import from `@/components/ui/tabs`. Plain uncontrolled Radix state (defaultValue) —
   NO useRouter/useSearchParams/URL params (research Pitfall 5: router-driven tabs would
   reintroduce the scroll-jump bug class). Keep the props signature of
   AccessAnalysisCharts UNCHANGED (mainCharts.tsx is not touched this plan). Keep
   AuthorProfileDrawer + DrillSheet at the end of the shell. Zinc theme: the `line`
   TabsList variant on transparent background, no card-inside-card (tab panels contain the
   PremiumSurfaces directly).
2. Delete `TerrainReveal.tsx` and `__tests__/TerrainReveal.test.tsx` — the Compare tab
   replaces the expand-gate (verify first: `grep -rn "TerrainReveal" app components lib`
   must show no remaining importer; template-mty uses FolderPermissionTerrain directly).
3. Shell target: AccessAnalysisCharts.tsx shrinks to roughly 300-380 lines (state +
   header block + Tabs wiring). No memo/summary logic changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/repo-map/check.cjs</automated>
  </verify>
  <done>Shell renders 6 tabs; TerrainReveal fully removed with no dangling imports; tsc + repo-map boundary check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate AccessAnalysisCharts.test.tsx to tab-aware queries</name>
  <files>app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
Migrate the 33 existing test cases (research Pitfall 6 — this is a real task, not a side
effect): content now living in a non-default tab is absent from the DOM until its trigger
is clicked. For each failing assertion, prepend
`await userEvent.click(screen.getByRole("tab", { name: /roles/i }))` (or the relevant tab)
before querying. Add a small local helper `openTab(name)` to avoid repetition. Keep every
existing behavioral assertion intact — these tests characterize picker filtering,
sliceFilters, drills, coverage headers, people sheets; only the navigation preamble
changes.

ADD new tab-IA cases:
1. Default tab is Overview: Activity-over-time section visible without clicks; Role
   distribution NOT in the DOM.
2. All 6 tab triggers render in locked order (getAllByRole("tab") names).
3. Selection survives tab switches: uncheck a project in the picker, switch Overview→Roles
   →back, picker state (and a selection-derived KPI) unchanged.
4. sliceFilters survive tab switches: activate a role slice filter on Roles tab
   (FilterBanner appears), switch to Companies tab — FilterBanner still shown (it lives
   above the tabs).
5. Compare tab mounts FolderPermissionTerrain without its own pickers (assert
   ProjectSelect/ProjectMultiSelect testids absent inside the tab) — the terrain's data
   fetching is already covered by 20.1-04's tests; mock loadTerrain/loadOverview as the
   existing suite does.
6. Panel-inventory guard: iterate all 6 tabs and assert each locked panel title is present
   in its assigned tab (the "nothing silently dropped" check for all 11 current panels).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>All migrated + new cases green; suite still asserts the same behaviors as before plus tab IA.</done>
</task>

</tasks>

<verification>
- `npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"` green.
- `npx tsc --noEmit` clean; `node scripts/repo-map/check.cjs` reports no new boundary violations.
- `npm test` module scope for access-analysis: `npx vitest run "app/(dashboard)/access-analysis"` green (no other panel test broken by the move).
- `git diff --name-only`: only this plan's files; `mainCharts.tsx` untouched; nothing under `/users/spatial-graph` or `/template-mty`.
- No card-inside-card: tab panels place PremiumSurfaces directly (no wrapper surface around a tab's content).
</verification>

<success_criteria>
`/access-analysis` is a 6-tab, Overview-led, story-shaped page with one global
picker/FilterBanner pinned above the tab strip driving every tab including the terrain;
all pre-existing panels relocated intact; the full existing test suite migrated and green.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-05-SUMMARY.md`
</output>
