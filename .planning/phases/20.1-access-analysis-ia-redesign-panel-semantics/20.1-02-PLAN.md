---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/permissionLevelView.ts
  - lib/server/__tests__/permissionLevelView.test.ts
  - app/(dashboard)/access-analysis/permissionLevelActions.ts
  - app/(dashboard)/access-analysis/permissionLevelCounts.ts
  - app/(dashboard)/access-analysis/__tests__/permissionLevelCounts.test.ts
  - app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx
  - app/(dashboard)/access-analysis/__tests__/PermissionLevelChart.test.tsx
autonomous: true
requirements: [UAT-3]

must_haves:
  truths:
    - "A bounded server aggregate exists counting folder permissions per (project, role, permType) — verbatim stored permType strings, never PermTier remapping"
    - "Top-N roles render as horizontal stacked bars segmented by permission level, answering 'which role has the most admin permissions' at a glance"
    - "Per-role project drill preserved (PermissionFootprintChart's toggleDrill pattern)"
  artifacts:
    - path: "lib/server/permissionLevelView.ts"
      provides: "loadPermissionLevel loader: GROUP BY (projectId, roleId, permType) over AccFolderPermission (live-verified 60,110 rows), 5-min TTL"
    - path: "app/(dashboard)/access-analysis/permissionLevelCounts.ts"
      provides: "summarizePermissionLevel top-10+Other stacked transform with PERMISSION_LEVEL_ORDER"
    - path: "app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx"
      provides: "Stacked-bar EChart panel with role drill"
    - path: "app/(dashboard)/access-analysis/permissionLevelActions.ts"
      provides: "Auth-gated 'use server' action for lazy per-tab fetch"
  key_links:
    - from: "lib/server/permissionLevelView.ts"
      to: "AccFolderPermission"
      via: "own $queryRaw GROUP BY — NOT routed through folderPermQuery.ts (that module forbids single-use additions), NOT findMany"
      pattern: "GROUP BY[\\s\\S]*permType"
    - from: "app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx"
      to: "app/(dashboard)/access-analysis/permissionLevelCounts.ts"
      via: "summarizePermissionLevel"
      pattern: "summarizePermissionLevel"
---

<objective>
Build the PERM-01 reframe (owner UAT item 3, verbatim: doesn't care about folder byte
sizes — wants permission VOLUMES by level, "which role has the most admin permissions out
of all"): a new loader/transform/chart slice counting folder permissions per
(role, permission level). Built UNMOUNTED — plan 20.1-06 mounts it in the Roles tab and
removes the byte-sized footprint chart. No edits to `mainCharts.tsx`,
`AccessAnalysisCharts.tsx`, `folderPermQuery.ts`, or `permissionFootprintView.ts` here.

Purpose: the headline permission chart must answer the owner's question in one glance.
Output: `loadPermissionLevel` + action + `summarizePermissionLevel` + `PermissionLevelChart`.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture Patterns §3 + Pitfall 1)
@lib/server/permissionFootprintView.ts   (assemble/cache/name-resolution template — being replaced, NOT edited)
@app/(dashboard)/access-analysis/permissionFootprintCounts.ts (top-N+Other math template)
@app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx (chart + toggleDrill template)
@lib/server/folderActivityView.ts        (buildProjectNameMap / resolveProjectName)
@components/ui/EChart.tsx                (onEvents payload types name + seriesName — stacked click support)

<interfaces>
CRITICAL (research Pitfall 1, live-verified): the stored `AccFolderPermission.permType`
vocabulary has exactly 6 values and does NOT match `lib/acc/permissionMapping.ts`'s
`PermTier`. Use the stored strings VERBATIM as segments; never import/remap through
permissionMapping.ts or call mapActions():

| Stored permType | Live rows |
|---|---|
| View Only | 5,917,818 |
| View+Download | 60,802 |
| View+Download+Upload+Edit | 48,761 |
| View+Download+Upload | 11,059 |
| Full Controller | 2,145 |
| Upload Only | 25 |

Recommended SQL (research-verified, returns 60,110 rows — bounded, index-friendly via the
Phase-09 `@@index([roleId])`):
```sql
SELECT f."projectId" AS "projectId",
       fp."roleId"   AS "roleId",
       fp."permType" AS "permType",
       COUNT(*)::int AS "folderCount"
FROM "AccFolderPermission" fp
JOIN "AccFolder" f ON f.id = fp."folderId"
GROUP BY f."projectId", fp."roleId", fp."permType"
```

From lib/server/folderActivityView.ts:
```typescript
export function buildProjectNameMap(projects, dcProjects): Map<string, string>;
export function resolveProjectName(nameById: Map<string, string>, projectId: string): string; // never a raw GUID
```

From components/ui/EChart.tsx (already supports stacked clicks — no wrapper change):
```typescript
onEvents?: Record<string, (params: { name?: string; data?: unknown; seriesName?: string }) => void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: permissionLevelView loader + server action</name>
  <files>lib/server/permissionLevelView.ts, lib/server/__tests__/permissionLevelView.test.ts, app/(dashboard)/access-analysis/permissionLevelActions.ts</files>
  <action>
Create `lib/server/permissionLevelView.ts` ("server-only"), structured like
`permissionFootprintView.ts`:

- `export interface PermissionLevelRow { projectId: string; projectName: string;
  roleId: string; roleName: string; permType: string; folderCount: number }`.
  `folderCount` comes from `COUNT(*)::int` — plain number, no BigInt crosses any boundary
  (note this in a comment; Pitfall 2 class).
- Pure `assemblePermissionLevel(aggRows, projects, dcProjects, roles)` — resolve
  projectName via `buildProjectNameMap`/`resolveProjectName` (AccProject over AccDcProject,
  "Unknown project" fallback, never a raw GUID) and roleName via a roleId map with
  "Unknown role" fallback. permType passes through VERBATIM.
- `loadPermissionLevel(force = false)`: Promise.all of the `db.$queryRaw` GROUP BY above +
  `db.accProject.findMany({ select: { id, name } })` + `db.accDcProject.findMany` +
  `db.accRole.findMany`; assemble; 5-min TTL cache (research Open Question 3 resolution:
  standard sibling TTL, NOT the ingestFreshness no-cache exception).
- Do NOT import or modify `lib/server/folderPermQuery.ts` — its header forbids single-use
  additions; this account-wide aggregate is a different consumer (same precedent as the
  Ph18 backfill script). Do NOT touch `acc-hot-cache.ts`. TEST-01/02/03 characterization
  tests must remain untouched and green.

Create `app/(dashboard)/access-analysis/permissionLevelActions.ts` ("use server"):
`loadPermissionLevelAction(): Promise<PermissionLevelRow[] | null>` — auth-gated,
mirroring `folderTerrainActions.ts`.

Create `lib/server/__tests__/permissionLevelView.test.ts` testing pure assemble:
(1) bounded output — output.length === aggRows.length (one row per (project,role,permType)
group, never raw-permission scale); (2) verbatim permType passthrough including an
unexpected value like "Some Future Tier" (kept, not remapped/dropped); (3) "Unknown role" /
"Unknown project" fallbacks; (4) AccProject name wins over AccDcProject.
  </action>
  <verify>
    <automated>npx vitest run lib/server/__tests__/permissionLevelView.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Loader + action exist, assemble tests green, tsc clean, `git diff --name-only` shows no change to folderPermQuery.ts / acc-hot-cache.ts / permissionFootprintView.ts.</done>
</task>

<task type="auto">
  <name>Task 2: permissionLevelCounts transform</name>
  <files>app/(dashboard)/access-analysis/permissionLevelCounts.ts, app/(dashboard)/access-analysis/__tests__/permissionLevelCounts.test.ts</files>
  <action>
Create pure `app/(dashboard)/access-analysis/permissionLevelCounts.ts` mirroring
`permissionFootprintCounts.ts`'s top-N+Other structure:

- `export const PERMISSION_LEVEL_ORDER = ["Full Controller", "View+Download+Upload+Edit",
  "View+Download+Upload", "View+Download", "Upload Only", "View Only"] as const;` —
  strongest→weakest stack/legend order (the 6 live values verbatim). Unknown permTypes
  encountered in data are appended after the known ones, alphabetically — honest
  passthrough, never dropped.
- `export interface PermissionLevelBar { roleId: string; roleName: string; total: number;
  byLevel: Record<string, number>; projectCount: number }`
- `export interface PermissionLevelSummary { bars: PermissionLevelBar[]; levels: string[];
  projectsByRole: Map<string, { projectId; projectName; folderCount }[]> }`
- `summarizePermissionLevel(rows: ReadonlyArray<PermissionLevelRow>, topN = 10)`:
  group by roleId; total = sum of folderCount across levels; sort roles by total desc
  (tiebreak roleName localeCompare); keep top-N, collapse rest into "Other (N roles)"
  (aggregated byLevel, no drill entry — same convention as summarizePermissionFootprint);
  `levels` = PERMISSION_LEVEL_ORDER members present in the data + appended unknowns;
  `projectsByRole` keyed by roleName for kept bars, per-project folderCount sorted desc.

Tests: bounded output (bars.length ≤ topN+1); top-N+Other math incl. Other aggregation;
level ordering (known order + unknown appended); zero-fill (role with no rows at a level →
byLevel[level] absent or 0, chart-safe); verbatim labels (assert "Full Controller"
appears, and "Full administrative controls" — the PermTier label — does NOT).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/permissionLevelCounts.test.ts"</automated>
  </verify>
  <done>Transform green with top-N/ordering/verbatim-label tests.</done>
</task>

<task type="auto">
  <name>Task 3: PermissionLevelChart component</name>
  <files>app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx, app/(dashboard)/access-analysis/__tests__/PermissionLevelChart.test.tsx</files>
  <action>
Create `"use client"` `PermissionLevelChart.tsx` using `PermissionFootprintChart.tsx` as
the direct template (same `@/components/ui/EChart` import, same CSS-variable theme color
resolution, same local `toggleDrill` state — NOT the sliceFilters bus):

Props: `{ rows: PermissionLevelRow[] }` (caller passes already-picker-filtered rows).
Internally `useMemo(() => summarizePermissionLevel(rows), [rows])`.

- Horizontal STACKED bars: y axis = role names (top-10 + Other), one ECharts `bar` series
  per entry of `summary.levels` with `stack: "levels"`, values = byLevel counts. X axis =
  folder-permission count (integer, `.toLocaleString()` labels — NO formatBytes; bytes are
  gone from this headline chart per the locked decision). Series colors: sequential ramp
  from the zinc-theme chart palette resolved from CSS variables (follow
  PermissionFootprintChart's existing color-resolution code; strongest level = most intense
  color). Legend shows the verbatim level names.
- Tooltip: role name, per-level count, role total.
- Drill: bar click (`onEvents.click`, `params.name` = roleName) toggles the per-role
  project list (projectName · folderCount), identical UX to PermissionFootprintChart's
  drill list. "Other (…)" bars don't drill.
- Empty state: no rows → "No permission data for this view".
- Include a muted caption: "Counts folder-permission grants by level, from the live
  folder-permission table (View Only dominates account-wide)." — keep it honest about the
  heavy View Only skew so the workshop read isn't confusing.

Test file (mirror PermissionFootprintChart.test.tsx's EChart mocking): renders one series
per level present; legend/level labels are the verbatim stored strings; role click opens
project drill; Other bar doesn't drill; empty state renders.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/PermissionLevelChart.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Chart green + tsc clean; component NOT mounted anywhere yet.</done>
</task>

</tasks>

<verification>
- All three new test files green; `npx tsc --noEmit` clean.
- `git diff --name-only` contains ONLY this plan's files — specifically no
  `folderPermQuery.ts`, `acc-hot-cache.ts`, `permissionFootprintView.ts`,
  `permissionMapping.ts`, or `/users/spatial-graph` paths.
- `npx vitest run lib/server/__tests__/folderPermissionTerrainView.test.ts lib/server/__tests__/templateFolderTerrain.sharedQuery.test.ts` still green (TEST-02/03 untouched).
</verification>

<success_criteria>
PERM-01 reframe slice exists end-to-end with verbatim-permType semantics, bounded
aggregates, and drill parity — ready for 20.1-06 to swap in for the footprint chart.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-02-SUMMARY.md`
</output>
