---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "01"
subsystem: ui
tags: [prisma, echarts, tRPC-free-server-action, access-analysis, activity-recency]

# Dependency graph
requires:
  - phase: 20-foundation-wins-engagement-panels
    provides: signInRecencyView.ts / signInRecencyCounts.ts / DormantSignInChart.tsx shape to mirror; accessInstanceView.ts mergeRoleNames; roleCounts.ts UNKNOWN_ROLE/MULTIPLE_ROLES; roleColors.ts buildRoleColorMap
provides:
  - "loadActivityRecency loader (lib/server/activityRecencyView.ts) + assembleActivityRecency pure fn"
  - "loadActivityRecencyAction auth-gated server action"
  - "bucketActivityRecency / attributeRole / summarizeActivityRecencyByRole pure transform"
  - "ActivityRecencyChart role-stacked ECharts panel, unmounted"
affects: [20.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AccActivityAccds-only MAX(createdAt) GROUP BY (projectId,userEmail) raw SQL recency source (never the accds+DC-backfill union activityByActorView.ts uses)"
    - "Role-stacked ECharts bars with top-8 + Other-roles legend collapse, mirrored from the donut-collapse convention in roleCounts.ts"

key-files:
  created:
    - lib/server/activityRecencyView.ts
    - lib/server/__tests__/activityRecencyView.test.ts
    - app/(dashboard)/access-analysis/activityRecencyActions.ts
    - app/(dashboard)/access-analysis/activityRecencyCounts.ts
    - app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts
    - app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx
    - app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx
  modified: []

key-decisions:
  - "Source is AccActivityAccds only (not the accds+DC-backfill union) — 20.1-CONTEXT.md locks this; the panel's job is a recency date, not a volume total, so re-deriving the backfill-union shape is unnecessary."
  - "Population = every AccDcProjectUser row (same 22,835-row DC-covered population as signInRecencyView.ts), so 'Never active' honestly dominates (~33.2% accds-covered) rather than only showing memberships with activity."

patterns-established:
  - "Role-stacked recency panel pattern: pure assemble (bounded to membership count) -> pure bucket+summarize (lossless band x role invariant) -> chart (drill + honest captions), reusable for future recency-style panels."

requirements-completed: [UAT-2]

# Metrics
duration: ~20min
completed: 2026-07-03
---

# Phase 20.1 Plan 01: ENG-01 Activity-Recency Pivot Summary

**Role-stacked activity-recency chart sourced from `AccActivityAccds` MAX(createdAt) per (project, user), built unmounted to replace the sign-in-recency panel in 20.1-06.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-03T16:07:00-06:00 (approx, first file write)
- **Completed:** 2026-07-03T16:13:07-06:00 (last task commit)
- **Tasks:** 3 completed
- **Files modified:** 7 (all new)

## Accomplishments
- `loadActivityRecency` loader: server-side raw-SQL `GROUP BY (projectId, userEmail) MAX(createdAt)` over `AccActivityAccds`, joined onto every `AccDcProjectUser` membership with roles resolved via `mergeRoleNames` (reused from `accessInstanceView.ts`, not re-derived)
- `summarizeActivityRecencyByRole`: lossless 5-band x role aggregation with top-8-roles + "Other roles" legend collapse and oldest-first drill ordering
- `ActivityRecencyChart`: role-stacked ECharts bars, band click-to-drill, honest live-coverage + "Never active" semantics captions, empty state
- 22 new unit/component tests, all green

## Task Commits

Each task was committed atomically:

1. **Task 1: activityRecencyView loader + server action** - `0e05cca5` (feat) — see "Deviations" for the concurrent-commit note; content verified byte-identical to working tree, `git diff HEAD` empty
2. **Task 2: activityRecencyCounts pure transform** - `7fb8edcd` (feat)
3. **Task 3: ActivityRecencyChart component** - `5d776a37` (feat)

_No plan-metadata commit prefix collision — all 3 tasks are `feat(20.1-01):`._

## Files Created/Modified
- `lib/server/activityRecencyView.ts` - `assembleActivityRecency` pure fn + `loadActivityRecency` loader (5-min TTL cache), raw SQL GROUP BY over `AccActivityAccds`
- `lib/server/__tests__/activityRecencyView.test.ts` - 5 tests: bounded output, unknown-user fallback, null lastActivityAt, role resolution incl. dropped unresolved roleId, case-insensitive email join
- `app/(dashboard)/access-analysis/activityRecencyActions.ts` - `loadActivityRecencyAction` auth-gated server action (mirrors `folderTerrainActions.ts`)
- `app/(dashboard)/access-analysis/activityRecencyCounts.ts` - `ACTIVITY_RECENCY_BANDS`, `bucketActivityRecency`, `attributeRole`, `summarizeActivityRecencyByRole`
- `app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts` - 9 tests: boundary edges, null/garbage handling, lossless invariants, drill ordering, top-8+Other-roles collapse math
- `app/(dashboard)/access-analysis/components/ActivityRecencyChart.tsx` - role-stacked chart, drill, captions, empty state
- `app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx` - 8 tests: band labels, drill contents/toggle, coverage/semantics captions

## Verification Evidence
- **Commands run:** `npx vitest run lib/server/__tests__/activityRecencyView.test.ts "app/(dashboard)/access-analysis/__tests__/activityRecencyCounts.test.ts" "app/(dashboard)/access-analysis/__tests__/ActivityRecencyChart.test.tsx"` -> 3 files, 22 tests, all passed
- **Type/build gate:** `npx tsc --noEmit` -> clean (run after every task)
- **Targeted tests/source checks:**
  - `grep -r "ActivityRecencyChart" app | grep -v __tests__ | grep -v "components/ActivityRecencyChart"` -> empty (component confirmed unmounted)
  - `grep -n "from \"@/app` in `lib/server/activityRecencyView.ts` -> no match (lib does not import app, Phase 10 boundary honored)
  - `grep -n "accActivityAccds.findMany"` in `lib/server/activityRecencyView.ts` -> no match (server-side SQL GROUP BY only, no findMany-over-scale)
  - `git diff HEAD~3 HEAD --name-only | grep -i "mainCharts\|AccessAnalysisCharts\|signInRecencyView\|spatial-graph"` -> empty (none of the guarded files touched)
- **Repo-map check:** not run — this plan adds new leaf files with no changed import edges into existing consumers (nothing mounts these files yet); 20.1-06 (the mounting plan) is the correct place for a repo-map check.

## Dashboard Evidence
- **Workshop surface:** `/access-analysis` (not yet visible — component is unmounted per plan scope; 20.1-06 mounts it and removes `DormantSignInChart`)
- **Workshop impact:** None yet (deliberately unmounted). Once mounted in 20.1-06, replaces the owner-rejected sign-in-recency signal with the truthful activity-recency-by-role signal (owner verbatim UAT item 2).
- **UI guardrails:** zinc theme via `useTheme`/CSS-variable-driven `cTitle`/`cSub`, ECharts colors resolved through `buildRoleColorMap` (matches Role-distribution donut), empty state for zero rows, no new WebGL (2D ECharts bars only)
- **Scope guardrails:** `/users/spatial-graph` untouched (confirmed via git diff of the 3 task commits); `mainCharts.tsx` and `AccessAnalysisCharts.tsx` untouched (plan-locked scope, confirmed via grep)

## Data Truthfulness
- **Data sources:** `AccActivityAccds` (raw SQL `MAX(createdAt)` GROUP BY `projectId, userEmail`), `AccDcProjectUser` (population), `AccDcUser`/`AccDcCompany` (name/company joins with "Unknown user"/"Unknown company" fallbacks), `AccDcProjectUserRole` + `AccRole`/`AccDcRole` merged via `mergeRoleNames` (roles)
- **Coverage limits:** "Never active" is the explicit, never-dropped null bucket and is expected to be the LARGEST bucket per 20.1-RESEARCH.md §2 (only ~33.2% of DC memberships have any accds-recorded activity) — the chart's `coverage` prop renders the live accds project-coverage count (never hardcoded) and the semantics caption states plainly that "Never active" means no recorded activity in the ACCDS-crawled window
- **No fake data:** No fixtures, no hardcoded coverage numbers, no invented routes/procedures; the chart's `coverage`/`dataFloor` props are optional and the caller (20.1-06) must pass live loader output

## Decisions Made
- Sourced recency from `AccActivityAccds` only, not the accds+DC-backfill union `activityByActorView.ts` uses — locked by 20.1-CONTEXT.md and the plan's `<interfaces>` section; this keeps the recency date single-sourced and avoids re-deriving a volume-oriented union shape for a date-only need.
- Population mirrors `signInRecencyView.ts`'s 22,835-row `AccDcProjectUser` population exactly (same DC-coverage class), so the semantic swap from sign-in to activity recency is an apples-to-apples replacement, not a scope change.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, environmental] Concurrent parallel-executor commit captured Task 1's files**
- **Found during:** Task 2 (about to commit Task 1's files individually)
- **Issue:** This wave-1 execution runs multiple plan executors (20.1-01, 20.1-02, 20.1-03, ...) in the SAME working directory/branch (`feat/access-analysis-redesign`), not isolated git worktrees. Between this agent's `git add` of Task 1's 3 files and its intended `git commit`, the concurrent 20.1-02 executor ran its own `git commit -m "..."` without a path-restricted commit, which captured the full index at that moment — including this plan's already-staged Task 1 files. The result: commit `0e05cca5` ("feat(20.1-02): add permissionLevelView loader + server action") legitimately contains exactly the correct Task 1 content (`lib/server/activityRecencyView.ts`, its test, and `activityRecencyActions.ts`) alongside 20.1-02's own files.
- **Fix:** Verified via `git cat-file -p HEAD:<path>` and `git diff HEAD -- <path>` (empty diff) that the committed content is byte-identical to what this plan intended for Task 1 — no code was lost, duplicated, or incorrect. Did not attempt to rewrite/amend/revert `0e05cca5` (a peer agent's commit on a shared branch — rewriting it risks destroying their concurrent work, prohibited by the destructive-git-operations guardrail). Treated `0e05cca5` as satisfying Task 1's "committed" requirement and proceeded. For Tasks 2 and 3, added an explicit `git diff --cached --name-only` check immediately after every `git add` and before every commit to confirm only this plan's own files were staged (both came back clean — exactly 2 files each).
- **Files affected:** `lib/server/activityRecencyView.ts`, `lib/server/__tests__/activityRecencyView.test.ts`, `app/(dashboard)/access-analysis/activityRecencyActions.ts` (all correct, no re-edit needed)
- **Verification:** `npx vitest run lib/server/__tests__/activityRecencyView.test.ts` (5/5 passed), `npx tsc --noEmit` (clean), `git diff HEAD -- <3 paths>` (empty)
- **Committed in:** `0e05cca5` (shared commit with 20.1-02, verified correct)

---

**Total deviations:** 1 auto-handled (1 environmental/process — concurrent shared-working-directory commit, no code defect)
**Impact on plan:** Zero impact on shipped content — all Task 1 code is correct and present in git history. Impact is purely on commit-message attribution for Task 1 (the commit message describes 20.1-02's work, not 20.1-01's, even though it contains both). Flagging this for the orchestrator: **wave-1 parallel executors in this session are sharing one working directory instead of isolated worktrees**, which risks index races on every concurrent commit, not just this one.

## Issues Encountered
None beyond the deviation above.

## User Setup Required
None - no external service configuration required.

## Dashboard Self-Check
- [x] Exact repo paths used; no invented `src/...` paths (all paths verified against `prisma/schema.prisma`, existing `lib/server/*View.ts` siblings, and `app/(dashboard)/access-analysis/*`)
- [x] Relevant Dashboard skill/project instructions followed (zinc theme, resolved ECharts colors, no new WebGL, `/users/spatial-graph` untouched)
- [x] Data coverage is truthful and labeled ("Never active" caption + live coverage prop, never hardcoded)
- [x] Zinc/no-new-WebGL/`/users/spatial-graph` guardrails checked (grep-verified)
- [x] Claims backed by command output, source evidence (test runs, tsc runs, grep checks) — no `VERIFY:` items remain

## Next Phase Readiness
- `loadActivityRecencyAction`, `summarizeActivityRecencyByRole`, and `ActivityRecencyChart` are ready for 20.1-06 to mount into the Roles/Users tab and remove `DormantSignInChart`/`signInRecencyView.ts`'s mount point.
- 20.1-06 will need to supply a live `coverage` prop (accds project-coverage count) — no loader for that exists in this plan; confirm whether `dcCoverageView.ts` or a new accds-specific coverage loader is the right source before wiring.
- **Process flag for the orchestrator:** confirm whether wave-1 plans (01-05) are intended to run in isolated worktrees; if not, every concurrent commit in this wave carries the same index-race risk observed here.

---
*Phase: 20.1-access-analysis-ia-redesign-panel-semantics*
*Completed: 2026-07-03*

## Self-Check: PASSED

All 7 created files found on disk; all 3 task commit hashes (`0e05cca5`, `7fb8edcd`, `5d776a37`) found in git history.
