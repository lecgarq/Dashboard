---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/folderActivityByCompanyView.ts
  - lib/server/__tests__/folderActivityByCompanyView.test.ts
  - app/(dashboard)/access-analysis/folderActivityByCompanyActions.ts
  - app/(dashboard)/access-analysis/folderActivityByCompanyCounts.ts
  - app/(dashboard)/access-analysis/__tests__/folderActivityByCompanyCounts.test.ts
  - app/(dashboard)/access-analysis/components/FolderActivityByCompanyChart.tsx
  - app/(dashboard)/access-analysis/__tests__/FolderActivityByCompanyChart.test.tsx
autonomous: true
requirements: [UAT-6]

must_haves:
  truths:
    - "A bounded headline aggregate exists: folder-scoped activity per (project, user) from AccActivityAccds (~10.6k rows), company-attributed via the existing membership join"
    - "Top-10 companies render as bars with an explicit 'Unknown company' bucket (~20% of memberships have no mapping — labeled, never dropped)"
    - "Clicking a company lazily fetches that company's top-folder breakdown, bounded by construction (that company's emails only)"
  artifacts:
    - path: "lib/server/folderActivityByCompanyView.ts"
      provides: "loadFolderScopedActivity headline aggregate + loadCompanyFolderBreakdown drill query"
    - path: "app/(dashboard)/access-analysis/folderActivityByCompanyCounts.ts"
      provides: "summarizeFolderActivityByCompany top-10+Other transform reusing summarizeActivityByCompany + UNKNOWN_COMPANY"
    - path: "app/(dashboard)/access-analysis/components/FolderActivityByCompanyChart.tsx"
      provides: "Company bar chart with lazy per-company folder drill"
    - path: "app/(dashboard)/access-analysis/folderActivityByCompanyActions.ts"
      provides: "Auth-gated 'use server' actions (headline + drill)"
  key_links:
    - from: "lib/server/folderActivityByCompanyView.ts"
      to: "AccActivityAccds"
      via: "server-side GROUP BY filtered folderId IS NOT NULL — never the 190k (project,user,folder) cross-product account-wide"
      pattern: "folderId\" IS NOT NULL"
    - from: "app/(dashboard)/access-analysis/folderActivityByCompanyCounts.ts"
      to: "app/(dashboard)/access-analysis/companyCounts.ts"
      via: "UNKNOWN_COMPANY constant reuse"
      pattern: "UNKNOWN_COMPANY"
    - from: "app/(dashboard)/access-analysis/components/FolderActivityByCompanyChart.tsx"
      to: "app/(dashboard)/access-analysis/folderActivityByCompanyActions.ts"
      via: "lazy drill action on company click"
      pattern: "loadCompanyFolderBreakdownAction"
---

<objective>
Build the new "Folder activity by company" graph (owner UAT item 6) as a two-pass design
(research §5, using CONTEXT.md's own pre-authorized fallback): a small headline aggregate
(top-10 companies by folder-scoped activity, eager within the Companies tab) plus a lazy
per-company folder drill on click — never materializing the 190,049-row company×folder
cross-product. Built UNMOUNTED — plan 20.1-06 mounts it in the Companies tab.

Purpose: shows which companies touch which folders, honestly labeled (Unknown company
bucket, accds coverage).
Output: loader + drill query + actions + transform + chart, unit-tested.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture Patterns §5 + Pitfall 3)
@lib/server/activityByActorView.ts       (the (projectId,userEmail) GROUP BY + AccDcUser name-resolution template)
@app/(dashboard)/access-analysis/companyActivityCounts.ts (the (email::projectId)→company Map-join to reuse)
@app/(dashboard)/access-analysis/companyCounts.ts (UNKNOWN_COMPANY constant)
@lib/server/folderActivityView.ts        (per-project lazy-drill query shape precedent)
@app/(dashboard)/access-analysis/folderTerrainActions.ts ("use server" auth-gate template)

<interfaces>
Live-verified bounds (research §5): headline `(projectId, userEmail)` GROUP BY with
`folderId IS NOT NULL` = 10,566 rows (safe); full company×folder cross-product = 190,049
rows (FORBIDDEN account-wide); company-mapping coverage = 80.3% of DC memberships →
"Unknown company" is a real ~20% bucket that must render.

From app/(dashboard)/access-analysis/companyActivityCounts.ts:
```typescript
export interface ActivityActorInput { projectId: string; userEmail: string; userName: string; count: number }
export interface MembershipCompanyInput { projectId: string; email: string; company?: string | null }
export function summarizeActivityByCompany(activity, memberships): CompanyActivitySummary;
// CompanyActivitySummary: { slices: RoleSlice[]; total: number; distinctCompanies: number;
//   usersByCompany: Map<string, CompanyActivityUser[]> } — includes UNKNOWN_COMPANY slice
```

Headline SQL shape (mirror activityByActorView.ts, accds-only, folder-scoped):
```sql
SELECT "projectId", "userEmail", COUNT(*)::int AS count
FROM "AccActivityAccds"
WHERE "folderId" IS NOT NULL AND "userEmail" IS NOT NULL
  AND "projectId" IS NOT NULL AND "projectId" <> ''
GROUP BY "projectId", "userEmail"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: folderActivityByCompanyView loader + drill query + actions</name>
  <files>lib/server/folderActivityByCompanyView.ts, lib/server/__tests__/folderActivityByCompanyView.test.ts, app/(dashboard)/access-analysis/folderActivityByCompanyActions.ts</files>
  <action>
Create `lib/server/folderActivityByCompanyView.ts` ("server-only"):

- `export interface FolderActivityActorRow { projectId: string; userEmail: string;
  userName: string; count: number }` (same shape as ActivityActorRow — so
  `filterRowsBySelection` works on it via projectId).
- `loadFolderScopedActivity(force = false)`: the headline SQL above via `db.$queryRaw`,
  plus `db.accDcUser.findMany({ select: { email, name } })` for userName resolution
  (fallback = email) — copy `loadActivityByActor`'s name-map code. 5-min TTL cache.
  Pure `assembleFolderScopedActivity(pairs, users)` exported for tests.
- `export interface CompanyFolderSlice { folderName: string; count: number }`
- `loadCompanyFolderBreakdown(emails: string[], projectIds: string[]):
  Promise<CompanyFolderSlice[]>` — the LAZY drill: parameterized query (use
  `Prisma.sql`/`Prisma.join` from `@prisma/client` — never string interpolation) over
  AccActivityAccds: `WHERE folderId IS NOT NULL AND lower("userEmail") IN (...emails
  lowered) AND "projectId" IN (...projectIds)`, `GROUP BY "folderName"` (COALESCE null
  folderName to '(unnamed folder)'), ORDER BY count DESC. Return top 15 + one aggregated
  "Other (N folders)" row when more exist. Defensive caps: slice emails to 1,000 and
  projectIds to 1,500 before querying; return [] when either input list is empty. No cache
  (per-click, scoped, cheap).

Create `app/(dashboard)/access-analysis/folderActivityByCompanyActions.ts` ("use server"),
auth-gated like folderTerrainActions.ts, exporting:
- `loadFolderScopedActivityAction(): Promise<FolderActivityActorRow[] | null>`
- `loadCompanyFolderBreakdownAction(emails: string[], projectIds: string[]):
  Promise<CompanyFolderSlice[] | null>`

Create `lib/server/__tests__/folderActivityByCompanyView.test.ts` (pure parts):
(1) assemble bounded — output.length === pairs.length (10.6k-class, never raw 4.1M-row
scale — assert on fixture proportions); (2) userName falls back to email; (3) the top-15 +
"Other" collapse helper (extract it as a pure exported function, e.g.
`collapseFolderSlices(rows, topN=15)`) aggregates correctly and returns ≤ 16 rows.
  </action>
  <verify>
    <automated>npx vitest run lib/server/__tests__/folderActivityByCompanyView.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Loader/drill/actions exist; pure tests green; tsc clean; drill query is parameterized (no template-string SQL with user input).</done>
</task>

<task type="auto">
  <name>Task 2: folderActivityByCompanyCounts transform</name>
  <files>app/(dashboard)/access-analysis/folderActivityByCompanyCounts.ts, app/(dashboard)/access-analysis/__tests__/folderActivityByCompanyCounts.test.ts</files>
  <action>
Create pure `app/(dashboard)/access-analysis/folderActivityByCompanyCounts.ts`:

- `export interface FolderCompanyBar { company: string; count: number;
  memberEmails: string[] }` — memberEmails feeds the drill action (the clicked company's
  contributing users, from usersByCompany).
- `summarizeFolderActivityByCompany(activity: ReadonlyArray<ActivityActorInput>,
  memberships: ReadonlyArray<MembershipCompanyInput>, topN = 10):
  { bars: FolderCompanyBar[]; total: number; usersByCompany: Map<string,
  CompanyActivityUser[]> }` — internally CALL `summarizeActivityByCompany` (do not
  re-implement the email::projectId company join), then: take top-N slices by count desc;
  collapse the remainder into "Other (N companies)" (no drill — empty memberEmails);
  UNKNOWN_COMPANY is ranked like any other slice (if it's in the top-N it shows; it must
  NEVER be filtered out). memberEmails per bar = that company's usersByCompany emails.

Tests: reuses summarizeActivityByCompany semantics (activity with no membership →
UNKNOWN_COMPANY bucket present and countable); top-10+Other collapse math; bars.length ≤
topN+1; memberEmails match the company's drill users; UNKNOWN_COMPANY never dropped even
when it would rank in "Other" (assert it's inside the Other aggregate count, i.e. total is
lossless: sum(bars.count) === total).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/folderActivityByCompanyCounts.test.ts"</automated>
  </verify>
  <done>Transform green with lossless-total + Unknown-company tests.</done>
</task>

<task type="auto">
  <name>Task 3: FolderActivityByCompanyChart component</name>
  <files>app/(dashboard)/access-analysis/components/FolderActivityByCompanyChart.tsx, app/(dashboard)/access-analysis/__tests__/FolderActivityByCompanyChart.test.tsx</files>
  <action>
Create `"use client"` `FolderActivityByCompanyChart.tsx` (template: PermissionFootprintChart
for the horizontal-bar + drill shell, DormantSignInChart for caption placement):

Props: `{ rows: FolderActivityActorRow[]; memberships: MembershipCompanyInput[];
selectedProjectIds: string[]; loadFolderBreakdown: (emails: string[], projectIds: string[])
=> Promise<CompanyFolderSlice[] | null>; coverage?: { covered: number; total: number };
onUserClick?: (email: string) => void }`.

- `useMemo(() => summarizeFolderActivityByCompany(rows, memberships), [rows, memberships])`
  (caller passes picker-filtered rows/memberships).
- Chart: horizontal bars, one per top-10 company + Other, value = folder-scoped activity
  count, theme-resolved colors via CSS variables (copy sibling color resolution).
- Drill: clicking a company bar toggles an inline drill area that (a) shows a small loading
  skeleton, (b) calls `loadFolderBreakdown(bar.memberEmails, selectedProjectIds)` ONCE per
  company (cache results in a `useState<Record<string, CompanyFolderSlice[]>>`), (c) lists
  top folders (folderName · count) plus the company's top contributing users (from
  usersByCompany, clickable via onUserClick). "Other" and empty-memberEmails bars don't
  drill. Follow the existing drill visual conventions (drill list above/below chart like
  PermissionFootprintChart).
- Captions (muted): live coverage line from the `coverage` prop ("Folder-scoped activity —
  accds covers {covered} of {total} projects"; never hardcoded) + "Users with no company
  mapping appear as Unknown company."
- Empty state: "No folder-scoped activity for this view".

Test file (mock the EChart + pass a vi.fn() loadFolderBreakdown): renders top companies
incl. an Unknown-company fixture; company click calls the drill action exactly once per
company (second click = cached, no re-call) with that company's emails; drill list renders
returned folders; empty state renders.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderActivityByCompanyChart.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Chart green + tsc clean; component NOT mounted anywhere yet.</done>
</task>

</tasks>

<verification>
- All three new test files green; `npx tsc --noEmit` clean.
- No account-wide (project,user,folder) cross-product query exists anywhere in the new
  loader (grep: the only GROUP BY containing "folderName" must be inside the
  email-parameterized drill function).
- `git diff --name-only` shows only this plan's 7 files.
</verification>

<success_criteria>
Folder-activity-by-company slice exists end-to-end with the two-pass bounded design,
Unknown-company honesty, and lazy per-company drill — ready for 20.1-06 to mount in the
Companies tab.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-03-SUMMARY.md`
</output>
