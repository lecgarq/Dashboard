---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "06"
type: execute
wave: 3
depends_on: ["20.1-01", "20.1-02", "20.1-03", "20.1-05"]
files_modified:
  - app/(dashboard)/access-analysis/mainCharts.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/RolesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/UsersTabPanel.tsx
  - app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx
  - app/(dashboard)/access-analysis/components/DormantSignInChart.tsx
  - app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx
  - app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx
  - app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx
  - app/(dashboard)/access-analysis/permissionFootprintCounts.ts
  - app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts
  - app/(dashboard)/access-analysis/signInRecencyCounts.ts
  - app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts
  - lib/server/signInRecencyView.ts
  - lib/server/permissionFootprintView.ts
  - app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx
autonomous: true
requirements: [UAT-2, UAT-3, UAT-6, UAT-7]

must_haves:
  truths:
    - "Roles tab shows Permission volume by level (PERM-01 reframe) and Activity recency by role (ENG-01 pivot); the byte-footprint and sign-in-recency panels are GONE (removed, not toggled)"
    - "Users tab shows the per-user activity-recency detail list (sortable DataTable) — the user-level cut of ENG-01"
    - "Companies tab shows Folder activity by company with working lazy per-company folder drill"
    - "The three new datasets load lazily on first tab activation and are cached in shell state (no refetch on tab switches); mainCharts' eager fan-out DROPS 11→9"
  artifacts:
    - path: "app/(dashboard)/access-analysis/mainCharts.tsx"
      provides: "9-entry eager Promise.all (loadPermissionFootprint + loadSignInRecency removed); no new eager entries"
    - path: "app/(dashboard)/access-analysis/components/RolesTabPanel.tsx"
      provides: "PermissionLevelChart + ActivityRecencyChart mounted with lazy-fetched data"
    - path: "app/(dashboard)/access-analysis/components/UsersTabPanel.tsx"
      provides: "Per-user recency DataTable from activityRecency rows"
    - path: "app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx"
      provides: "FolderActivityByCompanyChart mounted with lazy-fetched data"
  key_links:
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "app/(dashboard)/access-analysis/{activityRecencyActions,permissionLevelActions,folderActivityByCompanyActions}.ts"
      via: "lazy fetch-on-first-tab-activation, results cached in shell useState (TerrainReveal/FolderActivityReveal pattern)"
      pattern: "loadActivityRecencyAction|loadPermissionLevelAction|loadFolderScopedActivityAction"
    - from: "app/(dashboard)/access-analysis/mainCharts.tsx"
      to: "lib/server/permissionFootprintView.ts"
      via: "REMOVED import (must no longer exist)"
      pattern: "!loadPermissionFootprint"
---

<objective>
Complete the panel-semantics integration: mount the three plan-01/02/03 slices into their
locked tabs with lazy per-tab data loading, and remove the two superseded panels (sign-in
recency, byte footprint) plus their now-dead loaders — resolving STATE.md's flagged
`mainCharts.tsx` fan-out review by CUTTING the eager fan-out from 11 to 9 while the 3 new
loaders ride lazy tab activation (research Pitfall 4: naive flat adds would have hit 14).
`formatBytes` is retained per the locked decision.

Purpose: the owner's ENG-01/PERM-01 semantic pivots and the new company graph go live in
the tabbed IA; page first-paint gets lighter, not heavier.
Output: fully wired 6-tab page with all six UAT items' UI in place (scroll-jump fix and
owner re-check follow in 20.1-07).
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-CONTEXT.md
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture §1 data-loading tie-in, Pitfall 4)
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-01-SUMMARY.md
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-02-SUMMARY.md
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-03-SUMMARY.md
@app/(dashboard)/access-analysis/mainCharts.tsx
@components/ui/DataTable.tsx (virtualized/sortable table for the Users tab detail list)

<interfaces>
From wave-1 plans (verify exact exports against their SUMMARYs):
- `loadActivityRecencyAction(): Promise<ActivityRecencyRow[] | null>`;
  `ActivityRecencyRow { projectId; email; name; company; roles: string[]; lastActivityAt: string | null }`
- `loadPermissionLevelAction(): Promise<PermissionLevelRow[] | null>`;
  `PermissionLevelRow { projectId; projectName; roleId; roleName; permType; folderCount }`
- `loadFolderScopedActivityAction(): Promise<FolderActivityActorRow[] | null>` +
  `loadCompanyFolderBreakdownAction(emails, projectIds)`
- Components: `ActivityRecencyChart { rows; coverage?; dataFloor? }`,
  `PermissionLevelChart { rows }`, `FolderActivityByCompanyChart { rows; memberships;
  selectedProjectIds; loadFolderBreakdown; coverage?; onUserClick? }`

All three row types carry `projectId` → filter with the existing `filterRowsBySelection`
(picker-only, mirroring the Phase-20 locked convention; no sliceFilters extension).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lazy per-tab data wiring in the shell + mainCharts fan-out trim</name>
  <files>app/(dashboard)/access-analysis/mainCharts.tsx, app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx</files>
  <action>
1. `mainCharts.tsx`: remove `loadPermissionFootprint` and `loadSignInRecency` imports,
   Promise.all entries, and the `permissionFootprintRows`/`signInRecencyRows` props —
   fan-out 11→9. Import the three new actions
   (`loadActivityRecencyAction`, `loadPermissionLevelAction`,
   `loadFolderScopedActivityAction`, `loadCompanyFolderBreakdownAction`) and pass them as
   FUNCTION PROPS (like the existing `loadTerrain`/`loadClashes`) — NOT called eagerly.
   Update the header comment documenting the fan-out decision (9 eager + 3 lazy tab
   loaders; cite STATE.md's review flag as resolved).
2. `AccessAnalysisCharts.tsx` shell: drop the removed props/memos
   (`permissionFootprintRows`, `signInRecencyRows`, `filteredPermissionFootprintRows`,
   `filteredSignInRecencyRows`); add the four action props to the signature. Add lazy
   cached state — `const [activityRecencyRows, setActivityRecencyRows] =
   useState<ActivityRecencyRow[] | null>(null)` (same for permissionLevelRows,
   folderScopedActivityRows). Fetch strategy (mirrors TerrainReveal/FolderActivityReveal's
   fetch-on-first-need): make Tabs controlled (`value={tab} onValueChange={setTab}`,
   default "overview") and in an effect, when `tab` first becomes "roles"/"users" fetch
   activity-recency + permission-level if null; when "companies", fetch folder-scoped
   activity if null. Null-result (no session) → keep null and let panels show their empty
   states. Cache means switching back never refetches. Filter in the shell with
   `filterRowsBySelection(rows ?? [], selected)` memos and pass down.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "load" app/"(dashboard)"/access-analysis/mainCharts.tsx</automated>
  </verify>
  <done>mainCharts eager Promise.all has exactly 9 entries; shell compiles with lazy cached tab data; no eager call of the 3 new loaders anywhere in the RSC.</done>
</task>

<task type="auto">
  <name>Task 2: Mount new panels; remove superseded panels and dead code</name>
  <files>app/(dashboard)/access-analysis/components/RolesTabPanel.tsx, app/(dashboard)/access-analysis/components/UsersTabPanel.tsx, app/(dashboard)/access-analysis/components/CompaniesTabPanel.tsx, app/(dashboard)/access-analysis/components/DormantSignInChart.tsx, app/(dashboard)/access-analysis/__tests__/DormantSignInChart.test.tsx, app/(dashboard)/access-analysis/components/PermissionFootprintChart.tsx, app/(dashboard)/access-analysis/__tests__/PermissionFootprintChart.test.tsx, app/(dashboard)/access-analysis/permissionFootprintCounts.ts, app/(dashboard)/access-analysis/__tests__/permissionFootprintCounts.test.ts, app/(dashboard)/access-analysis/signInRecencyCounts.ts, app/(dashboard)/access-analysis/__tests__/signInRecencyCounts.test.ts, lib/server/signInRecencyView.ts, lib/server/permissionFootprintView.ts</files>
  <action>
1. `RolesTabPanel`: replace the interim Permission-footprint panel with
   `<PermissionLevelChart rows={filteredPermissionLevelRows} />` under a SectionHeader
   titled "Permission volume by level" (subtitle answers the owner's question: "Which role
   holds the most access at each permission level? Counted per folder grant."); add the
   `<ActivityRecencyChart rows={filteredActivityRecencyRows} coverage={{covered, total}}
   dataFloor={dataFloor} />` panel titled "Activity recency by role". Loading state while
   rows are null (tab fetch in flight): reuse the existing skeleton pattern
   (DonutSkeletons.tsx) — never a blank pane, never a fake chart.
2. `UsersTabPanel`: replace the interim DormantSignInChart with the ENG-01 user-level cut:
   a `DataTable` (from `@/components/ui/DataTable` — already used on /template-mty and
   /users) listing per-membership recency detail: Name · Company · Role(s) · Last activity
   (date or "Never active") · Band. Source rows = filteredActivityRecencyRows with
   band/attribution computed via activityRecencyCounts helpers. Sortable by last activity
   (default: most dormant first), searchable if DataTable supports it. Same coverage +
   "Never active" captions as the Roles-tab chart. Loading skeleton while null.
3. `CompaniesTabPanel`: add `<FolderActivityByCompanyChart rows={filteredFolderScopedRows}
   memberships={membershipRows} selectedProjectIds={[...selected]}
   loadFolderBreakdown={loadCompanyFolderBreakdownAction} coverage={{covered, total}}
   onUserClick={...setProfileEmail} />` titled "Folder activity by company", placed after
   the two existing company donuts. Loading skeleton while null.
4. Deletions (owner locked: replaced, not kept as toggles) — delete files:
   `DormantSignInChart.tsx` + its test, `PermissionFootprintChart.tsx` + its test,
   `lib/server/signInRecencyView.ts`, `signInRecencyCounts.ts` + its test. Trim
   `permissionFootprintCounts.ts` to ONLY `formatBytes` (kept per locked decision; move
   the `PermissionFootprintRow` type dependency out by deleting the summarize function +
   its interfaces) and trim its test to the formatBytes cases; delete
   `lib/server/permissionFootprintView.ts` (its only consumers were the deleted chart +
   summarize). Before each deletion run `grep -rn "<symbol>" app lib components server` to
   prove no remaining importer; if an unexpected importer exists, STOP and record a
   deviation rather than force-deleting. Note: `ActivityRecencyRow`'s type import for the
   shell/panels comes from `lib/server/activityRecencyView.ts` (type-only import from
   client files is the established convention, e.g. current imports of
   SignInRecencyRow).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/access-analysis" lib/server/__tests__</automated>
  </verify>
  <done>New panels mounted; superseded files gone with zero dangling imports; formatBytes survives with tests; module-scoped suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Test updates + full gates</name>
  <files>app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx</files>
  <action>
1. Update `AccessAnalysisCharts.test.tsx` for the final panel inventory: swap the
   panel-inventory guard's expectations (Roles tab now expects "Permission volume by
   level" + "Activity recency by role"; Users tab expects the recency detail table;
   Companies tab expects "Folder activity by company"; the old "Dormant users" /
   "Permission footprint by role" titles must be asserted ABSENT). Mock the four action
   props with vi.fn() resolving fixture rows; assert lazy behavior: actions NOT called on
   initial (Overview) render; clicking the Roles tab calls loadActivityRecencyAction +
   loadPermissionLevelAction exactly once; returning to Overview and back to Roles does
   NOT call them again (cache).
2. Gates: `npx vitest run "app/(dashboard)/access-analysis" lib/server/__tests__` green;
   `npx tsc --noEmit` clean; `node scripts/repo-map/check.cjs` no new violations; then
   full `npm test` — green modulo the 12 known pre-existing
   `UsersDirectoryClient.integration.test.tsx` failures (test-isolation issue, do NOT
   attempt to fix, per phase scope boundary). Confirm TEST-01/02/03 characterization tests
   pass unchanged.
3. Scope proof: `git diff --name-only <phase-start>..HEAD | grep -E "spatial-graph|template-mty"`
   returns nothing.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" && npx tsc --noEmit && node scripts/repo-map/check.cjs</automated>
  </verify>
  <done>Final-inventory + lazy-loading tests green; all gates pass; scope proof recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
- `npm test` green (2329-passing baseline class; only the 12 known unrelated failures allowed).
- `npx tsc --noEmit` clean; `node scripts/repo-map/check.cjs` clean.
- Grep proofs: no `loadPermissionFootprint|loadSignInRecency|DormantSignInChart|PermissionFootprintChart` anywhere outside git history; `formatBytes` still exported + tested.
- Eager fan-out = 9; the 3 new loaders execute only via tab-activation actions.
- Data truthfulness: every new panel carries live coverage captions; "Never active" and "Unknown company" buckets render; permType labels verbatim.
</verification>

<success_criteria>
All six UAT items' product surface is live behind the 6-tab IA with honest labels, lazy
tab data, a lighter first paint (9 eager loaders), and zero dead code from the superseded
panels. Only the scroll-jump fix + owner re-check (20.1-07) remain.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-06-SUMMARY.md`
</output>
