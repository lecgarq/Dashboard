---
phase: 20.1-access-analysis-ia-redesign-panel-semantics
plan: "07"
type: execute
wave: 4
depends_on: ["20.1-06"]
files_modified:
  - tests/e2e/access-analysis-scroll.spec.ts
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - app/(dashboard)/access-analysis/components/RolesPieChart.tsx
autonomous: false
requirements: [UAT-5]

must_haves:
  truths:
    - "Clicking a role slice/legend entry no longer scrolls/jumps the page to the top"
    - "The root cause was reproduced and measured in a real browser BEFORE any fix was applied (research: jsdom cannot surface this bug — IntersectionObserver is stubbed)"
    - "A Playwright regression spec pins the no-jump behavior"
    - "Owner has re-checked all six UAT items against the redesigned page"
  artifacts:
    - path: "tests/e2e/access-analysis-scroll.spec.ts"
      provides: "Playwright repro/regression: scrollY delta ≈ 0 across a role slice click"
  key_links:
    - from: "tests/e2e/access-analysis-scroll.spec.ts"
      to: "/access-analysis"
      via: "role slice click with window.scrollY measured before/after"
      pattern: "scrollY"
---

<objective>
Diagnose and fix the role-click scroll-jump bug (owner UAT item 5: "clicking a role
scrolls/jumps the filter to the very top — confusing"), then run the phase's owner UAT
re-check of all six items. Research (§6) ruled out router/scrollIntoView/autoFocus
mechanisms by static analysis — the plan's FIRST task is a live-browser reproduction, per
CONTEXT.md's "diagnose and fix" framing; do not guess a fix before reproducing. Note the
20.1-05 redesign may have altered or masked the symptom (the FilterBanner swap still
happens above the tab strip; RolesPieChart's legend collapse still happens in-panel) — the
repro must run against the CURRENT tabbed page.

Purpose: closes the last UAT item and gates the phase on owner approval.
Output: measured repro, targeted fix, e2e regression spec, owner sign-off.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-RESEARCH.md (Architecture §6 + Open Question 2)
@app/(dashboard)/access-analysis/components/RolesPieChart.tsx (toggleDrill → onSliceClick path)
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx (idle-tip ↔ FilterBanner ternary above the tab strip)
@tests/e2e/folder-activity-by-role.spec.ts (existing e2e conventions: :3100 Turbopack dev server + minted auth state via uat-helpers.ts)

<interfaces>
Prime suspects from research §6 (confirm, don't assume):
1. The idle-tip `<p>` ↔ `<FilterBanner>` JSX ternary swap near the top of the page (DOM
   mount/unmount above the viewport anchor → browser scroll anchoring shifts).
2. RolesPieChart's own legend list shrinking on slice click (panel height collapse in the
   very panel the user clicked).
3. An `echarts-for-react` resize side effect on option change.
E2e conventions: dev server on :3100 with Turbopack (webpack dev is broken repo-wide);
Playwright auth via the existing storage-state helpers in tests/e2e/uat-helpers.ts; e2e is
EXCLUDED from `npm test` — run with `npx playwright test <spec>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce and measure in a real browser</name>
  <files>tests/e2e/access-analysis-scroll.spec.ts</files>
  <action>
Write `tests/e2e/access-analysis-scroll.spec.ts` following the existing spec conventions
(auth state from uat-helpers, baseURL :3100): navigate to `/access-analysis`, wait for the
tab strip, scroll down until the Role-distribution panel (Roles tab — click the tab first)
is in view and `window.scrollY > 0` is recorded; click a role slice/legend entry (the same
affordance that fires `onSliceClick` — locate via the chart legend/`data-testid`s the
RolesPieChart tests use); measure `window.scrollY` immediately after and again after a
300ms settle. Assert |delta| ≤ 24px. Also capture a `console.log` of the deltas so the run
output documents the measurement.

Start the dev server (`npm run dev -- --turbopack` on :3100, background) if not already
running; kill it when done. NEVER touch the :3000 Task Scheduler production process.

Run the spec BEFORE any code change and record the result in the SUMMARY:
- If it FAILS (jump reproduced): capture which DOM change coincides (toggle the two
  suspect mechanisms via quick DevTools-style experiments — e.g. temporarily commenting
  the ternary in a scratch run — or by instrumenting the spec to read
  `document.elementFromPoint`/bounding rects before/after) and identify the mechanism.
- If it PASSES (redesign already fixed/masked it): record that honestly, keep the spec as
  a regression pin, skip Task 2's code change, and note for the checkpoint that the owner
  must confirm the symptom is gone on the real page.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/access-analysis-scroll.spec.ts --reporter=line</automated>
  </verify>
  <done>Spec exists and ran against a live browser; repro result (jump measured or absent) + suspected mechanism recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the minimal fix (only if reproduced)</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/components/RolesPieChart.tsx</files>
  <action>
Based on Task 1's measured mechanism, apply the SMALLEST fix that makes the spec pass —
research's cheap candidates, in order of preference:
(a) keep both the idle-tip and FilterBanner permanently mounted and toggle visibility with
CSS (`invisible`/`h-0`/`aria-hidden` treatment preserving layout height) instead of the
JSX ternary — removes the above-viewport DOM mount/unmount;
(b) give RolesPieChart's legend/drill list a stable `min-height` so slice-click filtering
doesn't collapse the clicked panel's height;
(c) if an ECharts resize side effect is the cause, pin the chart container height.
Do NOT introduce scroll restoration hacks (`window.scrollTo` after click), router changes,
or new dependencies. Keep motion ≤200ms and the zinc theme untouched. Re-run the e2e spec
(must pass) AND the unit suites touching the edited files
(`AccessAnalysisCharts.test.tsx`, `RolesPieChart.test.tsx`) — update unit tests only if
the CSS-visibility swap changes queries (e.g. idle tip now always in DOM → assert
visibility instead of presence).
If Task 1 found no reproducible jump, make no code change and record the deviation.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/access-analysis-scroll.spec.ts --reporter=line && npx vitest run "app/(dashboard)/access-analysis/__tests__/AccessAnalysisCharts.test.tsx" "app/(dashboard)/access-analysis/__tests__/RolesPieChart.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Spec green; unit suites green; fix is minimal and mechanism-targeted (or honestly skipped with evidence).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner UAT re-check of all six items</name>
  <files>none — verification checkpoint (dev server on :3100; production :3000 untouched)</files>
  <action>
Before pausing: ensure all gates are green (`npm test` modulo the 12 known failures,
`npx tsc --noEmit`, `node scripts/repo-map/check.cjs`, Playwright scroll spec) and the dev
server is running on :3100. Then present the checkpoint below to the owner and BLOCK until
the resume signal.
  </action>
  <verify>Owner responds "approved" or provides itemized feedback (recorded verbatim in the SUMMARY).</verify>
  <done>Owner verdict captured; on approval the phase is ready for /gsd:verify-work; on feedback, items are routed per resume instructions.</done>
  <what-built>
The full Phase 20.1 delivery: (1) /access-analysis reorganized into 6 themed tabs
(Overview · Roles · Users · Companies · Projects · Compare) with the global picker +
FilterBanner pinned above the tab strip; (2) Activity recency by role replacing sign-in
recency (Roles tab chart + Users tab detail table); (3) Permission volume by level
replacing the byte footprint (Roles tab); (4) terrain folded into the Compare tab driven
by the main search bar, own pickers removed; (5) role-click scroll-jump measured and
fixed (or shown already resolved by the redesign); (6) new Folder activity by company
graph with per-company folder drill (Companies tab).

Full gates already run: npm test green (modulo the 12 known unrelated failures),
npx tsc --noEmit clean, repo-map check clean, TEST-01/02/03 untouched, no
/users/spatial-graph or /template-mty changes, no new WebGL, no new npm dependency.
  </what-built>
  <how-to-verify>
1. Dev server: `npm run dev -- --turbopack` (uses :3100; production :3000 untouched — a
   :3000 rebuild/deploy is deliberately NOT part of this checkpoint; say the word if you
   want the deploy sequence afterwards).
2. Open http://localhost:3100/access-analysis — lands on the Overview tab: Activity over
   time, Activity by module, Ingest freshness. KPI strip/picker/filter tip sit above the
   tabs.
3. Roles tab — check UAT items 2+3: "Permission volume by level" stacked bars (verbatim
   levels like "Full Controller"; click a role → project drill) and "Activity recency by
   role" (5 bands incl. "Never active" with its coverage caption; click a band → people).
4. Users tab — per-user recency table, most-dormant first.
5. Companies tab — UAT item 6: "Folder activity by company" (Unknown company bucket
   visible; click a company → its top folders load).
6. Projects tab — Issue data coverage above Model Coordination (unchanged panels).
7. Compare tab — UAT item 4: terrain with NO separate project picker; change the main
   picker selection → terrain follows (1 project = single view; several = compare planes;
   clear all = account overview). NOTE our recorded assumption: with the default
   everything-selected picker, Compare first paints in compare mode over the top-6-staffed
   projects — confirm this reading of "empty state" or tell us to invert it.
8. UAT item 5: scroll down on the Roles tab, click a role slice — page must NOT jump to
   the top.
9. UAT item 7 overall: does the tab story ("all over the place" → chapters) work for the
   workshop?
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or list any item numbers that still miss the mark (verbatim feedback preferred).</resume-signal>
</task>

</tasks>

<verification>
- Playwright spec green in a real browser; measurement evidence recorded in SUMMARY.
- `npm test` + `npx tsc --noEmit` + `node scripts/repo-map/check.cjs` all green before the checkpoint.
- `git diff --name-only` across the phase: only access-analysis + lib/server + tests/e2e + .planning files; nothing under /users/spatial-graph.
- Owner verdict captured verbatim in the SUMMARY (approved or itemized follow-ups).
</verification>

<success_criteria>
Scroll-jump is measured-then-fixed with a regression pin, and the owner has re-checked all
six verbatim UAT items on the redesigned page. Phase 20.1 is ready for
/gsd:verify-work / phase close.
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-access-analysis-ia-redesign-panel-semantics/20.1-07-SUMMARY.md`
</output>
