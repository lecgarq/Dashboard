---
phase: 05-ui-enrichment-waves
plan: 5.3-dash-wave
type: execute
wave: 3
depends_on: [5.1-graph-wave, 5.2-list-wave]
files_modified:
  - app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/FolderRoleMatrixWidget.tsx
  - app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  - app/(dashboard)/users/dashboard/findingsContext.tsx
  - app/(dashboard)/users/dashboard/selectionContext.tsx
  - lib/acc/dashboardAnalytics.ts
  - lib/acc/dashboardAnalytics.test.ts
  - app/(dashboard)/users/kpiStripCounts.ts
  - app/(dashboard)/users/kpiStripCounts.test.ts
  - app/(dashboard)/users/adminConstellationTier.ts
  - app/(dashboard)/users/adminConstellationTier.test.ts
  - app/(dashboard)/users/heatmapToggle.ts
  - app/(dashboard)/users/heatmapToggle.test.ts
autonomous: false
requirements: [DASH-14, DASH-15, DASH-16, DASH-17, DASH-18]

must_haves:
  truths:
    - "KpiStrip displays three new tiles at the end of the strip: pending invites, project admins, folders crawled — each clickable through to a side-panel detail"
    - "Recommendations widget surfaces 'stale invite' (medium severity) and 'orphan role' (high severity) findings interleaved with existing findings by severity rank"
    - "AdminConstellation renders 3 visually distinct tiers using the same shape language as Wave 5.1 (star/diamond/circle-with-ring), with hover detail and click-through to side panel"
    - "RolesModulesHeatmap has a Hub view / Project view header toggle (default Hub) that swaps the aggregation function"
    - "If Phase 4 GO on GRAPH-04: FolderRoleMatrix matrix-cell click opens the same `kind:'folder'` side-panel body used by the graph (single source of truth). If NO-GO: matrix is the canonical folder surface and the side-panel body is fully fleshed out here."
    - "Every modified widget passes DASH-18 contract: hover detail panel, click-through to side panel, cross-widget selection spotlight"
    - "60 FPS preserved with all widgets rendering; no widget regresses the label polish curve elsewhere"
    - "No manual sync UI: DashboardClient Refresh button confirmed already removed (pre-phase); no new Sync All / Refresh buttons added"
  artifacts:
    - path: "lib/acc/dashboardAnalytics.ts"
      provides: "detectStaleInvites + detectOrphanRoles + DashboardFindings extended with staleInvites/orphanRoles arrays"
      exports: ["detectStaleInvites", "detectOrphanRoles"]
    - path: "lib/acc/dashboardAnalytics.test.ts"
      provides: "Vitest coverage for stale-invite + orphan-role detection + severity interleave"
    - path: "app/(dashboard)/users/kpiStripCounts.ts"
      provides: "Pure aggregators: countPendingInvites, countProjectAdmins, countFoldersCrawled"
    - path: "app/(dashboard)/users/adminConstellationTier.ts"
      provides: "Pure tier assignment + ring color for AdminConstellation orbit nodes"
      exports: ["tierFor", "tierRingColor"]
    - path: "app/(dashboard)/users/heatmapToggle.ts"
      provides: "aggregateHubRoles + aggregateProjectRoles functions for ECharts series data"
    - path: "app/(dashboard)/users/dashboard/selectionContext.tsx"
      provides: "SelectedFinding extended with kind:'staleInvite' (Wave 5.1 added folder/adminTier)"
  key_links:
    - from: "FolderRoleMatrixWidget cell click"
      to: "DashboardSidePanel kind:'folder' body"
      via: "setSelected({ kind: 'folder', folderUrn })"
      pattern: "kind:\\s*['\"]folder['\"]"
    - from: "Stale invite finding click"
      to: "DashboardSidePanel kind:'staleInvite' body"
      via: "setSelected({ kind: 'staleInvite', email })"
      pattern: "kind:\\s*['\"]staleInvite['\"]"
    - from: "Orphan role finding click"
      to: "DashboardSidePanel kind:'folder' body (orphan flag visible)"
      via: "setSelected({ kind: 'folder', folderUrn })"
      pattern: "orphanRoles"
    - from: "AdminConstellation tier click"
      to: "DashboardSidePanel kind:'adminTier' body"
      via: "setSelected({ kind: 'adminTier', tier })"
      pattern: "kind:\\s*['\"]adminTier['\"]"
---

<objective>
Enrich the dashboard surface: KpiStrip gains 3 tiles, Recommendations gains stale-invite + orphan-role findings, AdminConstellation reskins to 3 tiers matching the graph language, RolesModulesHeatmap gains a Hub/Project toggle, and FolderRoleMatrix (Phase 4 widget) is wired to the shared `kind:'folder'` side-panel body. Every widget passes the DASH-18 interactivity contract.

Purpose: DASH-14..18 per ROADMAP Phase 5 + 05-CONTEXT locked decisions. Hub admin = star+gold+1.5×, project admin = diamond, executive = circle-with-ring (same as Wave 5.1).
Output: A dashboard where the new ACC data is observable through every primary widget and every interaction routes back to the shared selection context.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-enrichment-waves/05-CONTEXT.md
@.planning/phases/05-ui-enrichment-waves/05-RESEARCH.md
@.planning/phases/05-ui-enrichment-waves/5.1-graph-wave-SUMMARY.md
@.planning/phases/05-ui-enrichment-waves/5.2-list-wave-SUMMARY.md
@app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
@app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
@app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
@app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
@app/(dashboard)/users/dashboard/findingsContext.tsx
@app/(dashboard)/users/dashboard/selectionContext.tsx
@app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
@lib/acc/dashboardAnalytics.ts
@app/(dashboard)/users/adminTierShape.ts
@lib/acc/acc-types.ts

<interfaces>
From Wave 5.1 (already shipped):
- `adminTierShape.ts` exports `adminTierFor(user) -> { tier, shape, sizeMultiplier, haloColor }`
- SelectedFinding includes `{ kind: 'folder'; folderUrn: string }` and `{ kind: 'adminTier'; tier }`
- DashboardSidePanel has folder body + adminTier body branches (folder body may be a stub if Phase 4 NO-GO — Task 4 fleshes it out here in that case).

From Wave 5.2 (already shipped):
- `BulkAccUser` includes `aggregatedStatus`, `perProjectAdmin`, `perProjectStatuses`.
- `useVisibleRowEmails` + `getLastFileActivityBatch` available.

Phase 5.3 selection-union addition:
- `{ kind: 'staleInvite'; email: string }` for the Recommendations finding click.

DashboardFindings (current shape from RESEARCH Example 4) gains:
- `staleInvites: StaleInviteFinding[]` (severity: MEDIUM)
- `orphanRoles: OrphanRoleFinding[]` (severity: HIGH)

Phase 4 dependency: `5.1-graph-wave-PHASE4-GATE.md` records GO/NO-GO. Task 4 in this plan branches on that gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — analytics extensions + pure aggregators + selection-union addition</name>
  <files>
    lib/acc/dashboardAnalytics.ts
    lib/acc/dashboardAnalytics.test.ts
    app/(dashboard)/users/kpiStripCounts.ts
    app/(dashboard)/users/kpiStripCounts.test.ts
    app/(dashboard)/users/adminConstellationTier.ts
    app/(dashboard)/users/adminConstellationTier.test.ts
    app/(dashboard)/users/heatmapToggle.ts
    app/(dashboard)/users/heatmapToggle.test.ts
    app/(dashboard)/users/dashboard/selectionContext.tsx
    app/(dashboard)/users/dashboard/findingsContext.tsx
  </files>
  <action>
1) Extend `lib/acc/dashboardAnalytics.ts`:
- Add interfaces `StaleInviteFinding { email; daysPending; severity: "MEDIUM" }` and `OrphanRoleFinding { folderUrn; folderPath; roleName; severity: "HIGH" }`.
- Extend `DashboardFindings` interface with `staleInvites: StaleInviteFinding[]` and `orphanRoles: OrphanRoleFinding[]`.
- Implement `detectStaleInvites(members, now)` per RESEARCH Example 4 (status=pending + addedOn older than 30d).
- Implement `detectOrphanRoles(permissions, projectRoles)` per RESEARCH Example 4 (folder-assigned roles with zero member assignments).
- Extend `computeAllFindings` (existing producer) to populate the two new arrays. Wire into `findingsContext.tsx` so consumers see them.

2) Extend `lib/acc/dashboardAnalytics.test.ts`:
- detectStaleInvites: 29-day pending → NOT flagged; 31-day pending → flagged; active+old → NOT flagged; pending+null addedOn → NOT flagged.
- detectOrphanRoles: role with zero assigned members on folder → flagged; role with members → NOT flagged; multi-folder role with zero on one and some on another → flagged for the zero folder only.
- Severity ranking: assemble a mock findings set and verify a sort-by-severity helper interleaves HIGH orphan-role above MEDIUM stale-invite above existing LOW (or however current findings rank). If a sort helper doesn't already exist in `RecommendationsWidget.tsx`, add `sortFindingsBySeverity(findings: DashboardFindings)` returning a flat ranked array of discriminated-union items and test the order.

3) Create `app/(dashboard)/users/kpiStripCounts.ts`:
- `countPendingInvites(users: BulkAccUser[]): number` — count users with aggregatedStatus === "pending"
- `countProjectAdmins(users: BulkAccUser[]): number` — count users with perProjectAdmin === true
- `countFoldersCrawled(folders: { urn: string }[]): number` — folders.length
Create `kpiStripCounts.test.ts` with cases for each (mix of inputs incl. empty arrays).

4) Create `app/(dashboard)/users/adminConstellationTier.ts`:
- Reuse `adminTierFor` from `adminTierShape.ts` (Wave 5.1).
- `function tierFor(user) -> "hub" | "project" | "executive" | null` — null for non-admins (AdminConstellation only renders admins).
- `function tierRingColor(tier): string` — pick from existing brand palette. Discretion: e.g. gold for hub (matches halo), silver for project, bronze for executive — OR Tailwind tokens. Document choice in SUMMARY.
- Create `adminConstellationTier.test.ts`: tier assignment for each combination of input flags + null for non-admin + ring color non-empty for each non-null tier + distinct colors per tier.

5) Create `app/(dashboard)/users/heatmapToggle.ts`:
- `type HeatmapView = "hub" | "project"`
- `function aggregateHubRoles(input): EchartsSeriesData` — aggregates hub master role counts per module (current behaviour preserved). Default view.
- `function aggregateProjectRoles(input): EchartsSeriesData` — aggregates per-project industry role assignment counts per module.
- Same return shape, swappable at render time.
- Create `heatmapToggle.test.ts`: each aggregator produces non-empty data for a populated input + handles empty input gracefully + sums to expected totals.
NOTE: the EchartsSeriesData shape is whatever the current `RolesModulesHeatmapWidget.tsx` consumes. Read that file and match the shape; do NOT invent a new one.

6) Extend `selectionContext.tsx`:
- Add `| { kind: "staleInvite"; email: string }` to SelectedFinding.
- Update `isSelectionValid()` so staleInvite validates against the current users list (or simply always-valid; pick whichever is consistent with existing siblings).

Per-task verification: `npm test -- --run dashboardAnalytics kpiStripCounts adminConstellationTier heatmapToggle` — all four suites green.
  </action>
  <verify>
    <automated>npm test -- --run dashboardAnalytics kpiStripCounts adminConstellationTier heatmapToggle</automated>
  </verify>
  <done>Pure aggregators + tests green, DashboardFindings extended, computeAllFindings populates new arrays, SelectedFinding union extended with staleInvite, findingsContext exposes new arrays to consumers.</done>
</task>

<task type="auto">
  <name>Task 2: DASH-14 KpiStrip 3 new tiles + DASH-15 Recommendations 2 new findings — full interactivity contract</name>
  <files>
    app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
    app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
    app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  </files>
  <action>
1) `KpiStripWidget.tsx` (DASH-14):
- Append three tiles to the existing strip in this order: "Pending Invites", "Project Admins", "Folders Crawled". Read counts via `countPendingInvites`, `countProjectAdmins`, `countFoldersCrawled` (Task 1).
- Each tile follows the existing tile component pattern in this file — do not introduce a new layout. If the current grid is `lg:grid-cols-6` and now overflows, change to `lg:grid-cols-9` OR wrap to two rows (discretion). Document the choice.
- Hover any new tile → existing Tooltip pattern (if widgets use one) shows brief explanation (e.g. "Users with status=pending in any project").
- Click "Pending Invites" tile → `setSelected({ kind: 'staleInvite', email: '*' })` — no, that's wrong. Click "Pending Invites" → open a side-panel body listing pending-status users (reuse `kind: 'day'` or a new generic kind if needed; recommend adding a `kind: 'kpiPendingInvites'` only if no existing kind fits. Otherwise filter the user list via a context setter for KPI-driven filters. **Discretion**: pick whichever is consistent with how existing KpiStrip tiles already behave. Document.
- Click "Project Admins" tile → `setSelected({ kind: 'adminTier', tier: 'project' })` (reuses Wave 5.1 body — cross-widget consistency).
- Click "Folders Crawled" tile → IF Phase 4 GO: route to FolderRoleMatrixWidget (scroll-to + flash highlight) OR open a folders-overview side panel. IF NO-GO: open a side panel listing folders crawled. Document choice.
- Color is supplementary; the number + label is load-bearing.

2) `RecommendationsWidget.tsx` (DASH-15):
- Consume `staleInvites` and `orphanRoles` from `findingsContext` (Task 1).
- Use `sortFindingsBySeverity` (Task 1) to interleave new findings with existing ones in the bubble cluster. HIGH (orphan role) sits with other HIGH findings; MEDIUM (stale invite) sits with other MEDIUM.
- Visual: each new finding gets a distinct bubble icon/shape:
  - stale invite → envelope-with-clock glyph
  - orphan role → folder-with-warning glyph
- Hover bubble → existing hover-detail panel (this widget already has one — do not regress). Show email + daysPending (stale invite) OR folderPath + roleName (orphan role).
- Click stale-invite bubble → `setSelected({ kind: 'staleInvite', email })`.
- Click orphan-role bubble → `setSelected({ kind: 'folder', folderUrn })` (REUSES the Wave 5.1 folder body — single source of truth).
- Color never sole signal: each severity carries text + glyph + size; HIGH is largest bubble, MEDIUM medium.

3) `DashboardSidePanel.tsx`:
- Add the `kind: "staleInvite"` body: card with user name, email, days pending, "Resend invitation" placeholder (or just informational; no write action — REQUIREMENTS Out-of-Scope locks WRITE-*). Cross-widget spotlight: when this body is open, highlight the matching user in the user list / graph via existing spotlight setters (find pattern in selectionContext consumers).
- The folder body (added in Wave 5.1) gains an `orphanRole: { roleName }` section that highlights when the folder has any orphan role. This makes the DASH-15 orphan-role click land on a fully-formed body (not a stub) even if Phase 4 is NO-GO and the body was stubbed in Wave 5.1.

DASH-18 verification per affordance:
- KpiStrip tile: hover (tooltip) ✓ click (side panel or filter) ✓ cross-widget spotlight (via setSelected) ✓
- Recommendations bubble: hover (panel) ✓ click (setSelected) ✓ cross-widget (selectionContext consumer) ✓

Constraints:
- No Sync All / Refresh / manual-sync UI introduced anywhere on the dashboard.
- Do not regress label polish curve in graph (unrelated, but no global CSS changes that could leak).

Per-task verification: `npm test -- --run dashboardAnalytics kpiStripCounts` — both green.
  </action>
  <verify>
    <automated>npm test -- --run dashboardAnalytics kpiStripCounts</automated>
  </verify>
  <done>3 new KPI tiles append + click through to side panel, 2 new finding types interleave by severity in Recommendations with hover+click+spotlight contract honoured, staleInvite side-panel body fleshed out, folder body now shows orphan-role section.</done>
</task>

<task type="auto">
  <name>Task 3: DASH-16 AdminConstellation 3-tier reskin + DASH-17 RolesModulesHeatmap Hub/Project toggle</name>
  <files>
    app/(dashboard)/users/dashboard/widgets/AdminAccessWidget.tsx
    app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
  </files>
  <action>
1) `AdminAccessWidget.tsx` (DASH-16):
- Currently single-tier (`isAccountAdmin === true`). Extend filter to include `perProjectAdmin === true` and `executive === true` users.
- For each rendered orbit node compute tier via `tierFor(user)` (Task 1). Render visually:
  - hub tier: star glyph + gold halo + 1.5× node size (mirrors Wave 5.1 graph language)
  - project tier: diamond glyph + 1.0× size
  - executive tier: circle-with-ring glyph + 1.0× size
- Tier ring color from `tierRingColor(tier)` (Task 1).
- Hover any node → existing hover-detail panel shows tier label ("Hub admin" / "Project admin" / "Executive") + accessLevel breakdown + name + email. Color paired with shape and text — never sole signal.
- Click a node → `setSelected({ kind: 'admin', email })` (existing kind, current behaviour preserved).
- Click a tier-grouping (if widget has tier legend/clusters) → `setSelected({ kind: 'adminTier', tier })` (reuses Wave 5.1 body).
- Optional: small in-widget toggle "Show tier rings" defaulting ON. Discretion.
- Performance: orbit animation with framer-motion — preserve current spring settings; do NOT modify motion values. 60 FPS hard floor.

2) `RolesModulesHeatmapWidget.tsx` (DASH-17):
- Add a single header toggle "Hub view / Project view" — segmented control, default Hub.
- Toggle state stored in URL search param (consistency with Wave 5.1 graph toggles).
- On switch, swap aggregation between `aggregateHubRoles` and `aggregateProjectRoles` (Task 1) and re-feed ECharts data. ECharts handles transition animation automatically; if not, use the existing `setOption(..., { notMerge: true })` pattern from the file.
- Hover a cell → ECharts tooltip already shows count; extend to show project list (Hub view: project list contributing to that role-module cell; Project view: hub roles contributing). Document if ECharts version allows custom HTML tooltip — if not, leave count-only tooltip and note in SUMMARY.
- Click a cell → `setSelected({ kind: 'role', role: <name>, severity: undefined })` (existing kind; cross-widget spotlight to graph + user list).
- DASH-18 contract: hover (ECharts tooltip) ✓ click (setSelected) ✓ cross-widget (existing role-selection consumers fire) ✓.

Constraints:
- Do NOT change current default behaviour beyond adding the toggle (Hub view = current behaviour).
- No Refresh / Sync All UI.
- Color paired with text in tooltips (cells already carry numeric value text overlay).

Per-task verification: `npm test -- --run adminConstellationTier heatmapToggle` — both green.
  </action>
  <verify>
    <automated>npm test -- --run adminConstellationTier heatmapToggle</automated>
  </verify>
  <done>AdminConstellation renders 3 tiers with star/diamond/circle-with-ring + tier rings, hover panel + click both routes to side panel. Heatmap has Hub/Project segmented toggle (default Hub), aggregations swap correctly, click cell sets role selection.</done>
</task>

<task type="auto">
  <name>Task 4: FolderRoleMatrix wiring to kind:'folder' side-panel body (Phase 4 dependency) + DASH-18 final gate</name>
  <files>
    app/(dashboard)/users/dashboard/widgets/FolderRoleMatrixWidget.tsx
    app/(dashboard)/users/dashboard/DashboardSidePanel.tsx
  </files>
  <action>
Read `.planning/phases/05-ui-enrichment-waves/5.1-graph-wave-PHASE4-GATE.md` to determine Phase 4 status.

CASE A — Phase 4 shipped the FolderRoleMatrixWidget (most likely; Phase 4 owns FLDR-04):
- Modify the widget to call `setSelected({ kind: 'folder', folderUrn })` on cell click. Reuses the single Wave 5.1 folder side-panel body (one source of truth — no divergent code paths per RESEARCH Pitfall 3).
- Hover cell → tooltip with folder path + role + member count + orphan flag (if applicable).
- Cross-widget spotlight: when a folder selection is active, dim non-matching widgets via existing selectionContext consumer pattern; if other widgets do not yet observe `kind:'folder'`, add consumers in KpiStripWidget / RecommendationsWidget / RolesModulesHeatmapWidget that highlight matching items (folder count, orphan finding, role rows respectively).
- Ensure `DashboardSidePanel.tsx` folder body shows: folder path (breadcrumb), role table with member counts, orphan flags. Already fleshed out in Task 2 — extend if needed for matrix-driven entry.

CASE B — Phase 4 did NOT ship the matrix widget yet:
- This plan does NOT create the matrix from scratch (it belongs to Phase 4 / FLDR-04). HALT this task and surface to user via the human checkpoint (Task 5) — Wave 5.3 can ship DASH-14..17 but DASH-18 final gate cannot close without the matrix landing.
- Document in SUMMARY that DASH-18 closure is pending FLDR-04 (Phase 4) completion.

Also in this task, perform the DASH-18 cross-widget audit:
- For each modified widget in Wave 5.3 (KpiStrip, Recommendations, AdminConstellation, RolesModulesHeatmap, FolderRoleMatrix when present): verify the file has at least one `setSelected({ kind: ... })` call. Output a checklist into SUMMARY.
- Verify `DashboardSidePanel.tsx` has render branches for every Phase 5 selection kind: `folder`, `adminTier`, `staleInvite`. Output coverage matrix.

Constraints:
- Single source of truth for folder body — no duplicated code paths between graph entry and matrix entry.
- 60 FPS preserved.

Per-task verification: grep `setSelected.*folder` in FolderRoleMatrixWidget.tsx (CASE A) — finds at least one call. Grep `DashboardSidePanel.tsx` for `kind === "folder"`, `kind === "adminTier"`, `kind === "staleInvite"` — all three present.
  </action>
  <verify>
    <automated>npm test -- --run dashboardAnalytics</automated>
  </verify>
  <done>CASE A: matrix-cell click routes to shared folder body, cross-widget folder consumers in place. CASE B: surfaced via checkpoint, SUMMARY documents pending FLDR-04. DASH-18 coverage matrix written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT — DASH-18 interactivity contract gate for Wave 5.3 (FINAL Phase 5 gate)</name>
  <what-built>
DASH-14 (KpiStrip 3 new tiles), DASH-15 (Recommendations stale-invite + orphan-role findings), DASH-16 (AdminConstellation 3-tier reskin), DASH-17 (RolesModulesHeatmap Hub/Project toggle), DASH-18 (cross-widget interactivity contract gate for ALL Phase 5 widgets).
  </what-built>
  <how-to-verify>
Run dev server, navigate to /users dashboard tab.

1. KpiStrip: confirm 3 new tiles appended at end — "Pending Invites" / "Project Admins" / "Folders Crawled" — with correct counts (cross-check against user list filter applied to same criteria). Hover each → tooltip explanation. Click each → side panel opens or list filters (verify chosen approach is consistent). Confirm strip wraps gracefully on narrow viewport (resize browser).

2. Recommendations widget: scroll the bubble cluster — confirm "orphan role" bubbles (HIGH severity, folder-with-warning glyph) appear among other HIGH findings and "stale invite" bubbles (MEDIUM, envelope-with-clock) appear among MEDIUM. Hover one of each → detail panel shows correct content (email/days for stale; folderPath/roleName for orphan). Click stale invite → side panel opens with staleInvite body; click orphan role → side panel opens with folder body showing orphan flag.

3. AdminConstellation: confirm 3 distinct tiers visible by SHAPE (star / diamond / circle-with-ring) not just color. Verify hub admins have gold halo and ~1.5× size (visually obvious). Hover any node → panel with tier label + breakdown. Click → side panel with admin user detail.

4. RolesModulesHeatmap: confirm header has "Hub view / Project view" toggle, default Hub. Switch to Project view → heatmap data changes (different cell intensities). Hover cell → tooltip shows count (and project list if implemented). Click cell → role spotlight triggers across graph + user list.

5. FolderRoleMatrix (if Phase 4 shipped it):
   - Click any cell → side panel opens with the SAME folder body used by the graph's folder click (Wave 5.1). Confirm same content, same UI — single source of truth.
   - If matrix shows orphan-role indicators, click one → side panel shows folder body with orphan-role section highlighted.

6. Cross-widget spotlight chain test:
   - Click an orphan-role bubble in Recommendations → confirm matching folder cell in FolderRoleMatrix flashes/highlights (if Phase 4 shipped matrix), folders-crawled KPI tile flashes/highlights.
   - Click a hub admin in AdminConstellation → confirm matching user in user list scrolls into view + highlights; graph node (if visible) gets halo.

7. Confirm NO Refresh button anywhere on the dashboard. NO Sync All button. NO new manual-sync UI. (Pre-Phase-5 cleanup already removed the v1 Refresh; confirm it stayed removed.)

8. Performance: dashboard tab interaction should feel snappy (<100ms perceived click-to-side-panel). Open browser dev tools performance panel — record 10 seconds of widget interaction. Confirm no long tasks >100ms.

9. 60 FPS hard floor: switch to the graph tab with admin tiers ON + folder cluster ON (if Phase 4 GO), then come back to dashboard — no widget regresses.

10. Coverage matrix sanity (from SUMMARY): each modified widget has a documented hover + click + spotlight wiring. No widget is "color only".

Type "approved" or describe specific failures (e.g. "AdminConstellation tiers indistinguishable in grayscale" / "Heatmap toggle reset on tab switch" / "orphan-role click opened wrong body").
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --run dashboardAnalytics kpiStripCounts adminConstellationTier heatmapToggle` — all four green.
2. `npm run typecheck` — no new errors in files_modified.
3. Manual UAT (Task 5) — all 10 checks pass.
4. Grep `DashboardSidePanel.tsx` confirms 3 new Phase 5 kinds (folder, adminTier, staleInvite) all have render branches.
5. Grep all widget files for `setSelected` calls — coverage matrix in SUMMARY shows each modified widget has at least one.
6. Grep for "Refresh" / "Sync All" in dashboard files — confirm none added.
</verification>

<success_criteria>
- All must_haves truths observable.
- DASH-14..18 fully shipped (DASH-18 closes only with FolderRoleMatrix wired — Phase 4 dependency surfaced explicitly in Task 4 if missing).
- Hub/Project/Executive shape language consistent between Wave 5.1 graph overlay and Wave 5.3 AdminConstellation.
- Folder body is a single source of truth between graph click and matrix click.
- 60 FPS preserved; label polish curve untouched.
- No manual sync UI anywhere on the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-enrichment-waves/5.3-dash-wave-SUMMARY.md` documenting: KPI-strip wrap strategy chosen, KPI-tile click-action chosen per tile, AdminConstellation ring-color palette chosen, Heatmap tooltip extension (count-only vs custom HTML), FolderRoleMatrix CASE A/B outcome, DASH-18 coverage matrix (widget × {hover, click, spotlight}).
</output>
