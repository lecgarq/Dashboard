# GRAPH-04 Phase 4 Gate Document

**Created by:** Wave 5.1 Task 4 dependency check  
**Date:** 2026-05-11

## Phase 4 Status: IN-FLIGHT (not complete)

**Assessment:** Phase 4 has 2 of 7 plans complete (04-01, 04-03). Plans 04-04 through 04-07 have no SUMMARY files, indicating they have not yet been executed.

| Item | Status |
|---|---|
| 04-01 (permission mapping module) | Complete (SUMMARY present) |
| 04-02 (folder crawl logic) | Not started (no SUMMARY) |
| 04-03 (schema migration + accFolders router scaffold) | Complete (SUMMARY present) |
| 04-04 (folder crawl ingest) | Not started (no SUMMARY) |
| 04-05 (folder matrix) | Not started (no SUMMARY) |
| 04-06 (folder widget) | Not started (no SUMMARY) |
| 04-07 (folder role permissions) | Not started (no SUMMARY) |

## Performance Pre-flight Result: NOT YET DECIDED

No performance testing for folder nodes in the graph has been conducted. The Phase 4 perf pre-flight gate is part of Plan 04-04 (or 04-05) scope.

## accFolder Router Availability: SCAFFOLD ONLY

- **File:** `server/routers/acc-folders.ts`
- **Procedures:** Only `ping` placeholder (no `getDetail`, `getMatrix`, or `getProjectFolderTree`)
- **Verdict:** The router exists but does not expose folder detail data needed for GRAPH-04

## Decision: CASE 3 — Phase 4 not complete at execution time

Per plan GRAPH-04 specification, this is **CASE 3**. The executor does NOT make this call autonomously.

### Options presented to the user (Task 5 human checkpoint):

**(a)** Wait for Phase 4 to land before completing GRAPH-04 — the folder cluster helpers (`folderCluster.ts`) are ready; this plan just needs to be re-run after Phase 4 completes.

**(b)** Ship Wave 5.1 without GRAPH-04 now and revisit in a future wave — the `kind:"folder"` side-panel stub from Task 1 remains wired (Wave 5.3 will own the body content). This option delivers GRAPH-01, GRAPH-02, GRAPH-03 immediately.

**(c)** Assume Phase 4 GO and implement folder cluster now — this would require building against the schema's `AccFolder`/`AccFolderPermission` tables directly (they exist in schema) without the full Phase 4 server-side infrastructure.

## What is already in place regardless of decision

- `folderCluster.ts` helpers: `folderInitialPosition()` + `permissionTierEdgeColor()` — ready for CASE 1
- `kind:"folder"` SelectedFinding union member — already in `selectionContext.tsx`
- `FolderBody` stub in `DashboardSidePanel.tsx` — keeps runtime contract closed
- `AccFolder` and `AccFolderPermission` tables exist in Prisma schema

## Contingency if CASE 2 (NO-GO confirmed in future):

"GRAPH-04 deferred per Phase 4 NO-GO. Folder surface delivered exclusively via DASH wave matrix + folder-detail side-panel body (Wave 5.3)."
The `kind:"folder"` selection branch in `DashboardSidePanel.tsx` remains wired — Wave 5.3 owns the body content.
