---
phase: 12-integration-health-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/accessInstanceView.ts
  - lib/server/accessInstanceView.test.ts
  - lib/server/acc-admin.ts
autonomous: true
requirements: [OBS-02, OBS-03]
must_haves:
  truths:
    - "When both AccDcRole and AccRole resolve to zero role names, loadInstanceView emits exactly one [ACC-ROLES] console.warn per cache refresh (per D OBS-02 effective-empty trigger)"
    - "In normal operation (AccRole carries role names) no role-resolution warning is emitted — the by-design AccDcRole-empty state alone does NOT warn"
    - "lib/server/acc-admin.ts no longer carries the stale 02.5 defensive-logging diagnostics or the module flag, and companyRole/lastSignIn resolution is unchanged (per D OBS-03 code-grounded removal)"
  artifacts:
    - path: lib/server/accessInstanceView.ts
      provides: "shouldWarnEmptyRoleResolution predicate + [ACC-ROLES] warn wired at the loadInstanceView refresh boundary"
      contains: "ACC-ROLES"
    - path: lib/server/accessInstanceView.test.ts
      provides: "unit coverage for the effective-empty predicate"
      contains: "shouldWarnEmptyRoleResolution"
    - path: lib/server/acc-admin.ts
      provides: "diagnostics removed; companyRole/lastSignIn resolution intact"
      contains: "companyRole"
  key_links:
    - from: "lib/server/accessInstanceView.ts"
      to: "console.warn"
      via: "loadInstanceView checks shouldWarnEmptyRoleResolution after mergeRoleNames and warns once per refresh"
      pattern: "ACC-ROLES"
---

<!-- planner-discipline-allow: loggedRawShape -->
<!-- planner-discipline-allow: 02.5 -->

<objective>
Close OBS-02 and OBS-03 — two small, file-independent server-side debt fixes.

OBS-02: make the silent "role names cannot resolve" failure observable. Per the
grounded correction in 12-CONTEXT.md, warn ONLY when role-name resolution is
effectively empty (BOTH `AccDcRole` AND the `AccRole` fallback yield zero usable
role names) — NOT on the by-design `AccDcRole`-empty state alone (which is always
empty and would warn on every refresh forever). The real boundary is
`loadInstanceView()` in `lib/server/accessInstanceView.ts` (where `mergeRoleNames`
runs), not the roadmap's literal `lib/server/acc-hot-cache.ts` — that file does
not reference `AccDcRole`. This is a deliberate, documented divergence from the
literal roadmap wording.

OBS-03: remove the two stale `TODO[02.5]` defensive-logging diagnostics from
`lib/server/acc-admin.ts` (the roadmap's `lib/acc/acc-admin.ts` path is wrong —
verified location is `lib/server/acc-admin.ts`). The field names are already
confirmed code-grounded: `acc-admin.ts` itself records that HQ v1 returns
`job_title` (verified 2026-05-18), and `companyRole`/`lastSignIn` are consumed
downstream (`server/routers/users/acc-graph.ts` threads them into the persisted
`AccMemberCache.data`; `server/routers/users/acc-profile.ts` reads them). No live
Autodesk sync is required.

Purpose: convert a silent role-sync failure into an observable server log, and
delete obsolete diagnostics that have served their purpose.

Output: a tested effective-empty warning at the real role-resolution boundary,
and a clean `acc-admin.ts` with no stale diagnostics.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-integration-health-observability/12-CONTEXT.md

# Touched source (read before editing):
@lib/server/accessInstanceView.ts
@lib/server/accessInstanceView.test.ts
@lib/server/acc-admin.ts
</context>

<interfaces>
Verified facts for executors (do NOT re-derive):

OBS-02 — `lib/server/accessInstanceView.ts`:
- `mergeRoleNames(dc, live)` (line 28) is a PURE function: builds a Map from `live`
  then overlays `dc` (DC wins). Keep it pure — it is unit-tested in
  accessInstanceView.test.ts and must not log.
- `loadInstanceView(force)` (line 89) is the cache-refresh boundary: a 5-min TTL
  cache (lines 86-90) returns early on a cache hit, so logic placed here fires once
  per ACTUAL refresh, not per request. Line 91-101 loads `dcRoleNames`
  (`db.accDcRole`) and `liveRoleNames` (`db.accRole`); line 102 computes
  `mergeRoleNames(dcRoleNames, liveRoleNames)`. AccDcRole is empty by design (DC
  never delivers admin_roles.csv); AccRole carries 100% of assigned role ids.
- The effective-empty condition = the merged map is empty (size 0), i.e. BOTH
  sources returned zero usable names.

OBS-03 — `lib/server/acc-admin.ts`:
- Diagnostic 1: line 51 single-line comment + line 52 module flag declaration
  (`let loggedRawShape = false;`).
- Diagnostic 2: lines 207-209 multi-line comment + lines 210-219 guarded block
  (the `[02.5-D-DIAG]` console.info calls + the `loggedRawShape = true` gate) inside
  `fetchAllAccUsers`. The `for (const u of users)` mapping loop at line 220 must
  stay intact.
- Field-name proof (already present, line 233-234): "HQ v1 returns `job_title`
  (verified 2026-05-18 via diagnostic dump)". `companyRole` resolution at lines 177
  and 235; `lastSignIn` resolution at lines 178 and 236 — keep all four unchanged.
- Downstream consumers (proof the fields resolve correctly): `server/routers/users/acc-graph.ts:148-151`
  threads `companyRole`/`lastSignIn` into `cached.data` (AccMemberCache, `data Json`,
  prisma schema line 387); `server/routers/users/acc-profile.ts:181` reads `lastSignIn`.

Tests use Vitest; targeted run: `npx vitest run lib/server/accessInstanceView.test.ts`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OBS-02 — effective-empty role-resolution warning at the loadInstanceView boundary</name>
  <files>lib/server/accessInstanceView.ts, lib/server/accessInstanceView.test.ts</files>
  <behavior>
    - shouldWarnEmptyRoleResolution([], []) === true (both sources empty — the real silent failure).
    - shouldWarnEmptyRoleResolution([], [{ id: "r1", name: "X" }]) === false (AccRole fallback resolves).
    - shouldWarnEmptyRoleResolution([{ id: "r1", name: "X" }], []) === false (DC resolves).
    - The predicate is pure (mergeRoleNames stays pure; the predicate does not log).
  </behavior>
  <action>
    Add an exported pure predicate `shouldWarnEmptyRoleResolution(dc, live)` to
    lib/server/accessInstanceView.ts that returns `mergeRoleNames(dc, live).size === 0` — directly
    mirroring the locked D OBS-02 trigger ("BOTH AccDcRole AND the AccRole fallback yield zero usable
    role names"). Keep `mergeRoleNames` itself pure and log-free.

    In `loadInstanceView` (line 89), after the `mergeRoleNames(dcRoleNames, liveRoleNames)` call
    (line 102), if `shouldWarnEmptyRoleResolution(dcRoleNames, liveRoleNames)` is true, emit a SINGLE
    `console.warn` with the stable greppable prefix `[ACC-ROLES]` stating that role-name resolution is
    empty after refresh — both the AccDcRole snapshot and the AccRole live fallback returned zero
    usable role names, so role labels will be blank, and the AccRole sync should be checked. Because
    this sits behind the 5-min TTL cache, it fires once per actual refresh (cache-miss), not per
    request. Always on (not dev-gated, not rate-limited beyond once-per-refresh). Do not change the
    returned view.

    Add a header note (and a one-line SUMMARY entry) recording the deliberate divergence from the
    literal roadmap wording: the warning lives at the verified real role-resolution boundary
    (loadInstanceView), not acc-hot-cache.ts (which does not reference AccDcRole), and fires on
    effective-empty rather than on the by-design AccDcRole-empty state.

    Tests (accessInstanceView.test.ts): add a `describe("shouldWarnEmptyRoleResolution")` with the
    three cases in the behavior block.
  </action>
  <verify>
    <automated>npx vitest run lib/server/accessInstanceView.test.ts</automated>
  </verify>
  <done>shouldWarnEmptyRoleResolution exported + tested (3 cases pass); [ACC-ROLES] console.warn wired in loadInstanceView after mergeRoleNames; mergeRoleNames stays pure; silent in normal operation; npx tsc --noEmit clean.</done>
</task>

<task type="auto">
  <name>Task 2: OBS-03 — delete the stale defensive-logging diagnostics from lib/server/acc-admin.ts</name>
  <files>lib/server/acc-admin.ts</files>
  <action>
    Field names are already confirmed code-grounded (see interfaces: the file records job_title as the
    real HQ v1 key, and companyRole/lastSignIn are consumed downstream and persisted into
    AccMemberCache.data) — the "confirm before delete" gate is satisfied without a live Autodesk sync.

    Remove the obsolete defensive-logging diagnostics: (1) the single-line stale marker comment at
    line 51 and the module flag declaration at line 52; (2) the multi-line marker comment at lines
    207-209 and the entire guarded diagnostic block at lines 210-219 (the redacted-shape console.info
    calls and their gate assignment) inside `fetchAllAccUsers`. Leave the `for (const u of users)`
    mapping loop (line 220 onward) fully intact, and keep the companyRole (`u.job_title || ...`) and
    lastSignIn resolution lines (235-236) and the fetchAccUserByEmail resolution (lines 177-178)
    unchanged. Make no other behavioral change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && [ "$(grep -cE 'loggedRawShape|02\.5' lib/server/acc-admin.ts)" = "0" ] && [ "$(grep -c companyRole lib/server/acc-admin.ts)" -ge "1" ] && [ "$(grep -c lastSignIn lib/server/acc-admin.ts)" -ge "1" ]</automated>
  </verify>
  <done>No stale-diagnostic markers (the 02.5 family), no module flag, and no redacted-shape console.info block remain in acc-admin.ts; companyRole + lastSignIn resolution intact; npx tsc --noEmit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server role-resolution → server log | The `[ACC-ROLES]` warn carries only a static, secret-free message (no role data, no PII). OBS-03 only deletes diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | `[ACC-ROLES]` console.warn | accept | Message is a fixed string with no role names, emails, or counts of records — nothing sensitive is logged. |
| T-12-05 | Tampering | removal of acc-admin.ts diagnostics | mitigate | Pure deletion of dead diagnostics; `npx tsc --noEmit` + grep gates prove no live behavior (companyRole/lastSignIn resolution) is touched. No package installs in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` is clean (run before any rebuild — typechecks the whole tree incl. tests).
- `npx vitest run lib/server/accessInstanceView.test.ts` passes (effective-empty predicate: both-empty
  warns, either-source-resolves does not).
- OBS-02 stays silent in normal operation: with AccRole carrying names, no `[ACC-ROLES]` line appears;
  the warn only fires when both sources are empty (asserted via the predicate test).
- OBS-03 grep gates: `grep -cE 'loggedRawShape|02\.5' lib/server/acc-admin.ts` returns 0;
  `grep -c companyRole lib/server/acc-admin.ts` and `grep -c lastSignIn lib/server/acc-admin.ts`
  each return >= 1 (resolution intact). No downstream regression — acc-graph.ts / acc-profile.ts
  consumers unchanged.
- Guardrails: no workshop-page change; `/users/spatial-graph` untouched; no schema change; no new
  WebGL/ECharts; no secrets logged.
</verification>

<success_criteria>
- OBS-02 satisfied: a silent AccRole-sync failure (no resolvable role names from either source) becomes
  an observable `[ACC-ROLES]` server warning at the real role-resolution refresh boundary, while the
  by-design AccDcRole-empty state stays silent.
- OBS-03 satisfied: `lib/server/acc-admin.ts` no longer contains the two stale defensive-logging
  diagnostics or the module flag; `companyRole`/`lastSignIn` resolution is unchanged and `tsc` is clean.
</success_criteria>

<output>
Create `.planning/phases/12-integration-health-observability/12-02-SUMMARY.md` when done.
</output>
