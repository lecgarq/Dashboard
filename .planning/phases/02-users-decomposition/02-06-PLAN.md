---
phase: 02-users-decomposition
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - app/(dashboard)/users/ActivityAuditPanel.tsx
  - app/(dashboard)/users/DirectoryFilterBar.tsx
  - app/(dashboard)/users/UsersDirectoryClient.tsx
autonomous: false
requirements: [USR-01, PERF-03]

must_haves:
  truths:
    - "ActivityAuditPanel and ActiveFilterPill live outside the shell"
    - "UsersDirectoryClient.tsx is reduced to a ~200-line orchestrator shell (store + data hook + derived memos + layout + modal/sheet)"
    - "All golden-path behavior (search/filter/sort/view/group/modal/scroll) is observably identical end-to-end"
    - "A human projector click-through confirms zero visible change vs the pre-refactor baseline"
    - "npx tsc --noEmit exits 0 and the full test count holds"
  artifacts:
    - path: "app/(dashboard)/users/ActivityAuditPanel.tsx"
      provides: "Activity audit tab content (extracted)"
      min_lines: 100
    - path: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      provides: "~200-line orchestrator shell: store + data hook + derived memos + layout + modal/sheet"
      max_lines: 320
  key_links:
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/useUsersDirectoryStore.ts"
      via: "shell reads UI state from the store"
      pattern: "useUsersDirectoryStore"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/useUsersDirectoryData.ts"
      via: "shell reads all data from the hook"
      pattern: "useUsersDirectoryData"
---

<objective>
Finish the decomposition: extract the remaining large/standalone pieces
(ActivityAuditPanel, the search+filter bar, ActiveFilterPill), reduce
UsersDirectoryClient to a ~200-line orchestrator shell, and gate the phase on a live
projector click-through confirming zero visible change.

Purpose: USR-01 success criterion 2 (the ~200-line shell) and the CONTEXT sign-off
lock (not done on green tests alone — a before/after projector click-through is the
real acceptance test). PERF-03 is re-confirmed end-to-end.
Output: ActivityAuditPanel.tsx + DirectoryFilterBar.tsx; a ~200-line shell; a
human-verified zero-visible-change sign-off.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-users-decomposition/02-RESEARCH.md
@.planning/phases/02-users-decomposition/02-CONTEXT.md

# The monolith — ActivityAuditPanel ~1105-1245, ActiveFilterPill ~1033-1054,
# search+filter bar JSX ~2017-2330
@app/(dashboard)/users/UsersDirectoryClient.tsx
# The golden-path safety net — must stay green
@app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ActivityAuditPanel and ActiveFilterPill</name>
  <files>app/(dashboard)/users/ActivityAuditPanel.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/ActivityAuditPanel.tsx` (`"use client"`) and MOVE
    `ActivityAuditPanel` (lines ~1105-1245) verbatim, including its internal useState
    and any local query/loading state. Export it (named). Move `ActiveFilterPill`
    (lines ~1033-1054) into ActivityAuditPanel.tsx ONLY if it is exclusively used there;
    otherwise place ActiveFilterPill in DirectoryFilterBar.tsx (Task 2) since the active
    filter pills render in the filter bar region. In UsersDirectoryClient.tsx delete the
    moved code and add the import(s). Keep all behavior and rendered data identical
    (behavior parity, bug-for-bug per CONTEXT). If ActivityAuditPanel is not currently
    rendered by the shell's main return path, still extract it (USR-01 shell-reduction)
    and preserve whatever import/usage exists.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>ActivityAuditPanel.tsx owns the audit panel; ActiveFilterPill is extracted; shell imports them; tsc 0; golden-path test green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the search + filter bar and reduce the shell to ~200 lines</name>
  <files>app/(dashboard)/users/DirectoryFilterBar.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/DirectoryFilterBar.tsx` (`"use client"`) and move the
    search input + filter-dropdown bar + active-filter-pill row JSX (lines ~2017-2330)
    into a `DirectoryFilterBar` component. It receives the option lists
    (departments/jobTitles/costCenters/accProjects/accRoles/accModules) and the current
    filter values + setters as props, OR reads them directly from
    `useUsersDirectoryStore` (prefer store reads to keep the prop surface small).
    Include `ActiveFilterPill` here if not already placed in Task 1. Keep the search
    input wired to the shell's `handleSearchChange` (debounce ref stays in the shell —
    pass `search` + `onSearchChange` as props). Do not change any markup, placeholder
    text, icons, or class names. After extraction, the shell's main `return` should be:
    TooltipProvider > header (title/stats + group-by + view toggle) > DataCoverageStrip >
    DirectoryFilterBar > error/banner/skeleton > results (visibleGroups/renderPeople +
    show-more + empty state) > PersonDetailModal > activity Sheet. Trim the shell to a
    ~200-line orchestrator: store reads, data-hook read, the activity-sort block, the
    filtering/grouping memos, `renderPeople`, handlers, and the layout JSX above —
    nothing else. Confirm the line count with `wc -l`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx" && echo "shell lines:" && wc -l "app/(dashboard)/users/UsersDirectoryClient.tsx"</automated>
  </verify>
  <done>DirectoryFilterBar.tsx owns the search+filter+pill bar; the shell is a ~200-line orchestrator (target ≤ ~250, hard ceiling 320 per frontmatter); golden-path test green; tsc 0.</done>
</task>

<task type="auto">
  <name>Task 3: Final full-suite + tsc + scope-guard + repo-map gates</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Run the complete gate set: `npx tsc --noEmit` (must be 0, incl. test files — the
    next build typechecks the whole tree per MEMORY/STATE constraint), full `npm test`
    (count holds or grows vs the 02-01 baseline, no existing test lost), the hard scope
    guard, and `npm run repo-map:check`. Record final shell line count, final test
    count, and confirm no files under users/access-analysis or users/spatial-graph were
    touched across the whole phase.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -20 && git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph" && echo "SCOPE VIOLATION" || echo "scope clean"</automated>
  </verify>
  <done>tsc 0; full suite green (count held/grew); scope guard clean; repo-map:check passes; final shell line count recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Projector click-through — confirm zero visible change</name>
  <action>
    BLOCKING human verification — Claude does not proceed past this gate until the
    owner approves. Rebuild and run the dashboard, then walk the /users before/after
    click-through below on the live page (projector context) to confirm zero visible
    change vs the pre-refactor baseline. This is the CONTEXT sign-off lock: green
    tests alone are not acceptance.
  </action>
  <what-built>
    The /users monolith is fully decomposed: a ~200-line UsersDirectoryClient
    orchestrator shell reading a Zustand store (filter/search/sort/view/selection) and
    a single data hook (all tRPC queries, PERF-03 shared input), with extracted
    sub-components (PersonDetailModal, PersonRow, PersonRowList, DirectoryPills,
    DataCoverageStrip, CollapsibleGroup, ActivityAuditPanel, DirectoryFilterBar). All
    automated gates are green (golden-path test, store unit test, PERF-03 unit test,
    tsc 0, full suite, scope guard, repo-map ratchet).
  </what-built>
  <how-to-verify>
    Rebuild and run the dashboard (per the project deploy mechanism — `npm run build`
    then restart, NOT under a running :3000), then on the projector / live page do a
    before/after click-through on /users to confirm ZERO visible change vs today:
    1. Type a name in search — the list narrows exactly as before (same debounce feel).
    2. Apply a Department filter, then add a Job Title filter — results narrow and
       combine; the active-filter pills show both; dismissing a pill restores rows.
    3. Toggle grid <-> list view and pick a Group-by option — filters/search persist;
       list view shows the virtualized rows with the File Activity columns.
    4. Click the "Last File Activity" header to cycle sort off -> desc -> asc -> off.
    5. Scroll down the list, click a person to open the centered profile Dialog, then
       close it — confirm filters, search text, AND scroll position are exactly where
       you left them (the strictest check; most visible in a live demo).
    6. Click a row's file-activity cell to open the right-side activity Sheet.
    7. Confirm every number/list (user counts, departments, coverage strip, activity)
       reads identically to the pre-refactor page — no value changed.
    8. (Optional) Open DevTools Network and reload — confirm each tRPC endpoint is
       requested at most once (no post-mount bulkUsers refetch).
  </how-to-verify>
  <resume-signal>Type "approved" to confirm zero visible change, or describe any difference observed.</resume-signal>
</task>

</tasks>

<verification>
- ActivityAuditPanel.tsx + DirectoryFilterBar.tsx exist and are imported by the shell.
- UsersDirectoryClient.tsx is a ~200-line orchestrator (hard ceiling 320).
- `npx tsc --noEmit` exits 0; full suite count holds/grows; scope guard clean; repo-map:check passes.
- Golden-path test green end-to-end.
- Human projector click-through confirms zero visible change + unchanged data + single-fetch.
</verification>

<success_criteria>
The decomposition is complete and proven: a ~200-line shell over a Zustand store +
single data hook + extracted sub-components, with USR-01 behavior unchanged, PERF-03
single-fetch confirmed, all automated gates green, and a human-verified zero-visible-
change sign-off on the real page.
</success_criteria>

<output>
Create `.planning/phases/02-users-decomposition/02-06-SUMMARY.md` when done. Record the
final shell line count, final test count, and the human approval outcome.
</output>
