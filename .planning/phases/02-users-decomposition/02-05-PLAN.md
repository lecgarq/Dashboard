---
phase: 02-users-decomposition
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - app/(dashboard)/users/useUsersDirectoryData.ts
  - app/(dashboard)/users/useUsersDirectoryData.test.ts
  - app/(dashboard)/users/UsersDirectoryClient.tsx
  - lib/server/acc-route-hydration.ts
autonomous: true
requirements: [USR-01, PERF-03]

must_haves:
  truths:
    - "All main-body tRPC queries (bulkUsers, bulkAccSummary, enrichedUsers, getOrgDirectory, getDirectory, getCoverage x2, listInvitations) live in useUsersDirectoryData"
    - "useUsersDirectoryData exports BULK_USERS_LEAN_INPUT = { leanProjects: true } as const"
    - "acc-route-hydration.ts imports BULK_USERS_LEAN_INPUT and uses it for the bulkUsers prefetch (one shared referentially-stable input)"
    - "The data hook returns the same derived shapes the shell consumes (people, accSummary, accSummaryMap, mergedAccUsers, coverage, stats, isLoading, etc.)"
    - "listInvitations keeps enabled:false; bulkAccSummary keeps its dcEmpty gate; query inputs are unchanged"
    - "A unit assertion proves bulkUsers is called with { leanProjects: true }"
  artifacts:
    - path: "app/(dashboard)/users/useUsersDirectoryData.ts"
      provides: "Single data hook over all /users directory tRPC queries + derivations + BULK_USERS_LEAN_INPUT"
      min_lines: 120
      contains: "BULK_USERS_LEAN_INPUT"
    - path: "app/(dashboard)/users/useUsersDirectoryData.test.ts"
      provides: "PERF-03 assertion that bulkUsers is queried with { leanProjects: true }"
      min_lines: 30
  key_links:
    - from: "lib/server/acc-route-hydration.ts"
      to: "app/(dashboard)/users/useUsersDirectoryData.ts"
      via: "imports BULK_USERS_LEAN_INPUT and passes it to bulkUsers.prefetch"
      pattern: "BULK_USERS_LEAN_INPUT"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/useUsersDirectoryData.ts"
      via: "shell calls useUsersDirectoryData() for all directory data"
      pattern: "useUsersDirectoryData"
---

<objective>
Consolidate the eight main-body tRPC queries and their derivations into a single
data hook, and make the PERF-03 fix structural: export one referentially-stable
`BULK_USERS_LEAN_INPUT` constant that both the client query and the SSR prefetch
import, so the hydration-key cache-miss double-fetch can never drift back in.

Purpose: USR-01 (single data hook is the named target and Phase 4's DataTable will
read from it) + PERF-03 (shared query-key input kills the double-fetch by
construction). RESEARCH Patterns 2 + Pitfall 1.
Output: useUsersDirectoryData.ts + PERF-03 unit test; shell consumes the hook;
prefetch imports the shared input.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-users-decomposition/02-RESEARCH.md

# The monolith — tRPC calls ~1442-1497, derivations ~1474-1543, coverage ~1616-1660,
# derived option lists ~1588-1614, stats ~1813-1819
@app/(dashboard)/users/UsersDirectoryClient.tsx
# The SSR prefetch helper — bulkUsers.prefetch must use the shared input (line ~31)
@lib/server/acc-route-hydration.ts
# Merge/select helpers the hook reuses (leave unchanged)
@app/(dashboard)/users/useMergedAccUsers.ts
# The golden-path safety net — must stay green
@app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUsersDirectoryData with the shared BULK_USERS_LEAN_INPUT constant</name>
  <files>app/(dashboard)/users/useUsersDirectoryData.ts</files>
  <action>
    Create `app/(dashboard)/users/useUsersDirectoryData.ts` (`"use client"`). At module
    scope export `export const BULK_USERS_LEAN_INPUT = { leanProjects: true } as const`.
    MOVE into the hook (verbatim inputs + options) the eight main-body queries:
    `accDcGraph.bulkUsers.useQuery(BULK_USERS_LEAN_INPUT, { staleTime:
    ACC_SNAPSHOT_STALE_TIME_MS, retry:false })` (use the new constant in place of the
    inline `{ leanProjects: true }` literal — do NOT change the value),
    `users.bulkAccSummary.useQuery(undefined, { ..., enabled: !dcLoading &&
    dcUsersRaw.length === 0 })` (preserve the dcEmpty gate exactly),
    `accMembers.enrichedUsers.useQuery(undefined, ...)`,
    `accActivity.listInvitations.useQuery({ windowDays:90, limit:100 }, { ...,
    enabled:false })` (KEEP enabled:false — RESEARCH Pitfall 5),
    `accActivity.getCoverage.useQuery`, `accFolders.getCoverage.useQuery`,
    `users.getOrgDirectory.useQuery`, `users.getDirectory.useQuery`. Also MOVE the
    derivations the shell needs: `accSource`/`accSummary` (selectAccSummarySource +
    mergeAccSummaryWithEnrichment), `people`, `isLoading`, `accSummaryMap`,
    `mergedAccUsers`, `noProjectsCount`, `usingFallbackDirectory`, `directoryBanner`,
    `error`, `coverage`, and the option lists `departments`/`jobTitles`/`costCenters`/
    `accProjects`/`accRoles`/`accModules`. Return a single object with all of these.
    Import the merge helpers from `./useMergedAccUsers`, `ACC_SNAPSHOT_STALE_TIME_MS`
    from `./useMergedAccUsers` (re-exported there) or `@/lib/acc/cachePolicy`,
    `OrgPerson`/`LocalDirectoryUser` + `uniqueSorted` from `./directoryUtils`, and
    `BulkAccUser` from `@/lib/acc/acc-types`. Do NOT move into this hook: the
    activity-sort infinite query + its auto-fetch effect + `orderedActivityEmails`
    (those depend on store `activitySort` and the sort page ref — keep them in the
    shell), the hover-prefetch `utils`/timers, or the derived filtering memos
    (`filtered`/`displayRows`/`groups`) which depend on store filter state.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>useUsersDirectoryData.ts owns the 8 queries + their derivations and exports BULK_USERS_LEAN_INPUT; inputs/gates (enabled:false on invitations, dcEmpty on bulkAccSummary) unchanged; tsc 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the shell to the data hook and the prefetch to the shared input</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx, lib/server/acc-route-hydration.ts</files>
  <action>
    In UsersDirectoryClient.tsx, replace the eight inline tRPC calls and their moved
    derivations (lines ~1442-1660 region, minus the activity-sort block which stays)
    with a single `const { people, accSummary, accSummaryMap, mergedAccUsers,
    coverage, departments, jobTitles, costCenters, accProjects, accRoles, accModules,
    noProjectsCount, usingFallbackDirectory, directoryBanner, error, isLoading } =
    useUsersDirectoryData()`. Keep the activity-sort infinite query, its auto-fetch
    effect, and `orderedActivityEmails` in the shell (they depend on store
    activitySort). Keep the filtering memos (`filtered`/`displayRows`/`groups`/
    `visibleFiltered`/`visibleGroups`) and `stats` in the shell (they depend on store
    filter state + the hook's `people`/`accSummaryMap`). In
    `lib/server/acc-route-hydration.ts`, import `BULK_USERS_LEAN_INPUT` from
    `@/app/(dashboard)/users/useUsersDirectoryData` and pass it to
    `helpers.accDcGraph.bulkUsers.prefetch(BULK_USERS_LEAN_INPUT, snapshotOptions)`
    (line ~31) so the prefetch and client share one referentially-stable input. Keep
    the existing explanatory comment about the cache key. Do NOT alter the other
    prefetches or the access-analysis prefetch function.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>Shell calls useUsersDirectoryData() for all directory data; activity-sort + filtering memos stay in the shell; acc-route-hydration.ts prefetches bulkUsers with the imported BULK_USERS_LEAN_INPUT; golden-path test green; tsc 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PERF-03 unit assertion + parity/scope gates + repo-map ratchet</name>
  <files>app/(dashboard)/users/useUsersDirectoryData.test.ts</files>
  <behavior>
    - When useUsersDirectoryData runs (with trpc mocked), `accDcGraph.bulkUsers.useQuery`
      is called exactly once with the first argument deep-equal to { leanProjects: true }.
    - `BULK_USERS_LEAN_INPUT` is referentially identical to the value passed to that
      useQuery call (assert the same object reference is used, not a fresh inline literal),
      proving prefetch and client cannot drift.
  </behavior>
  <action>
    Create `app/(dashboard)/users/useUsersDirectoryData.test.ts` (`// @vitest-environment
    jsdom`). Mock `@/lib/core/trpc` with `vi.fn()` spies for each query; render the hook
    via `@testing-library/react` `renderHook` (or a tiny harness component). Assert the
    bulkUsers spy received `{ leanProjects: true }` and that the argument is the exported
    `BULK_USERS_LEAN_INPUT` reference (import it and compare with `toBe`). Then run the
    full suite (count holds/grows vs 02-01 baseline, no existing test lost), confirm the
    scope guard, and run `npm run repo-map:check` to ratchet against any re-introduced
    fetch surface.
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/useUsersDirectoryData.test.ts" && npx tsc --noEmit && npm test 2>&1 | tail -20 && git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph" && echo "SCOPE VIOLATION" || echo "scope clean"</automated>
  </verify>
  <done>PERF-03 unit test proves bulkUsers is called once with the shared { leanProjects: true } reference; tsc 0; full suite green; scope guard clean; repo-map:check ratchet recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
- useUsersDirectoryData.ts owns all 8 main-body queries + derivations; exports BULK_USERS_LEAN_INPUT.
- acc-route-hydration.ts imports and uses BULK_USERS_LEAN_INPUT for the bulkUsers prefetch.
- PERF-03 unit test: bulkUsers called once with the shared { leanProjects: true } reference.
- `npx tsc --noEmit` exits 0.
- Golden-path test green; full suite count holds/grows.
- Scope guard clean; `npm run repo-map:check` ratchet run.
</verification>

<success_criteria>
All directory tRPC fetching is consolidated in one hook; the PERF-03 double-fetch is
eliminated by construction via a single shared, referentially-stable bulkUsers input
shared between prefetch and client; each endpoint is fetched at most once per load;
golden-path + PERF-03 unit tests + tsc gate green.
</success_criteria>

<output>
Create `.planning/phases/02-users-decomposition/02-05-SUMMARY.md` when done. Record the
shell line count, the full-suite count, and the repo-map:check result.
</output>
