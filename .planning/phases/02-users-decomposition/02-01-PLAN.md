---
phase: 02-users-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
autonomous: true
requirements: [USR-01, PERF-03]

must_haves:
  truths:
    - "zustand is installed as a dependency and resolves at version 5.x"
    - "A golden-path integration test exists and PASSES against the current monolith (before any extraction)"
    - "The test asserts: search narrows the list, multiple filters combine, viewMode/groupBy switch without resetting filters, clicking a person opens the modal, closing preserves filters+search+scroll"
    - "The test asserts bulkUsers is queried with { leanProjects: true } exactly once (PERF-03 baseline)"
  artifacts:
    - path: "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"
      provides: "Golden-path regression safety net for the whole decomposition"
      min_lines: 120
    - path: "package.json"
      provides: "zustand dependency entry"
      contains: "zustand"
  key_links:
    - from: "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"
      to: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      via: "renders the real UsersDirectoryClient with mocked trpc fixtures"
      pattern: "UsersDirectoryClient"
    - from: "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"
      to: "@/lib/core/trpc"
      via: "vi.mock of the trpc client returning fixture data"
      pattern: "vi.mock.*trpc"
---

<objective>
Install zustand and write the golden-path integration test that pins ALL current
`/users` directory behavior, proving it GREEN against the un-refactored 2,474-line
monolith. This test is the regression safety net for every subsequent extraction —
it must stay green before and after each step.

Purpose: USR-01 mandates the test is written and passing BEFORE the first extraction.
This is the non-negotiable Wave 0 of the decomposition (RESEARCH Validation
Architecture + CONTEXT "Protected flows" lock). zustand must be present so later
waves can import it.
Output: zustand in package.json, golden-path test file proven green on the monolith.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-users-decomposition/02-RESEARCH.md
@.planning/phases/02-users-decomposition/02-CONTEXT.md
@.planning/codebase/TESTING.md

# The component under test and its current state inventory (DO NOT modify it in this plan)
@app/(dashboard)/users/UsersDirectoryClient.tsx
# Existing component-test mock pattern to mirror (route test)
@app/(dashboard)/users/page.test.tsx
# Existing trpc client shape (createTRPCReact)
@lib/core/trpc.ts
# The visibility hook PersonRowList depends on (its IntersectionObserver must be stubbed in the test)
@app/(dashboard)/users/useVisibleRowEmails.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install zustand and confirm the registry version</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm view zustand version` first to confirm the published registry version
    (expected 5.0.x from pmndrs) — this satisfies the RESEARCH Package Legitimacy
    Gate [ASSUMED] verification before install. Then run `npm install zustand` to add
    it to `dependencies` (not devDependencies — the store is shipped app code). Do NOT
    pin to an old major; accept the current 5.x. After install, confirm resolution with
    `node -e "console.log(require('zustand/package.json').version)"`. Do not write any
    store code in this plan — zustand is installed now so Wave 4 can import it cleanly.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.zustand) throw new Error('zustand missing from dependencies'); console.log('zustand', p.dependencies.zustand)"</automated>
  </verify>
  <done>zustand appears in package.json dependencies and `require('zustand/package.json').version` resolves to a 5.x string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the golden-path integration test and prove it GREEN on the monolith</name>
  <files>app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx</files>
  <behavior>
    The test renders the REAL `UsersDirectoryClient` (imported from
    `../UsersDirectoryClient`) with `// @vitest-environment jsdom`, mocking only
    `@/lib/core/trpc` and the dynamic-imported panels. Cases (each its own `it`):
    - Search narrows the list: typing a name into the search input reduces visible
      rows to matching people (respect the existing 150ms debounce — advance fake
      timers or wait).
    - Multiple filters combine: applying a department filter AND a job-title filter
      shows only people matching BOTH.
    - viewMode toggle preserves filters: switching grid to list (and back) does NOT
      clear an active filter or the search text.
    - groupBy switch preserves filters: choosing a Group-by option keeps the active
      filter applied.
    - Click opens modal: clicking a person row/card opens the profile modal showing
      that person's name.
    - Close preserves state (the strictest case): after opening and closing the
      modal, the search text, the active filter, AND `window.scrollY` are all
      unchanged from just before the open. Set a non-zero `window.scrollY` before
      open and assert it is identical after close.
    - PERF-03 baseline: assert the mocked `accDcGraph.bulkUsers.useQuery` was called
      with `{ leanProjects: true }` and that no fixture query is invoked twice with a
      conflicting input.
  </behavior>
  <action>
    Create `app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx`.
    Mirror the mock approach in `page.test.tsx` and the jsdom component-test patterns
    in `.planning/codebase/TESTING.md`. Mock `@/lib/core/trpc` so every hook used by
    the component returns deterministic fixtures: `accDcGraph.bulkUsers.useQuery`
    returns a small `BulkAccUser[]` (give two users distinct department + jobTitle so
    the combine-filters case is provable); `users.getOrgDirectory.useQuery` returns
    `{ status: "ok", people: [...] }` with matching emails; `users.getDirectory`,
    `users.bulkAccSummary`, `accMembers.enrichedUsers`, `accActivity.getCoverage`,
    `accFolders.getCoverage`, `accActivity.listInvitations` return empty/undefined
    data with `isLoading:false`; `accActivity.getLastFileActivityBatch.useQuery` and
    `accActivity.getFileActivityForUser.useQuery` return `{ data: undefined }`;
    `accActivity.usersOrderedByLastFileActivity.useInfiniteQuery` returns
    `{ data: undefined, isLoading:false, isFetching:false, hasNextPage:false }`; and
    `trpc.useUtils` returns an object whose
    `accActivity.getFileActivityForUser.prefetch` is a `vi.fn()`. Stub
    `IntersectionObserver` and `ResizeObserver` as no-op classes via `vi.stubGlobal`
    (PersonRowList's virtualizer + useVisibleRowEmails need them). Mock the
    `next/dynamic` panels (`UserProfilePanel`, `UserActivityBody`) to render a simple
    div with the email so the modal-open assertion can find the name. Use a shared
    `vi.fn()` reference for `bulkUsers.useQuery` so the PERF-03 case can assert its
    call arguments. Capture `window.scrollY` via `Object.defineProperty(window,
    "scrollY", { value: 800, configurable: true })` before opening the modal and
    assert it is still 800 after close (closing must NOT call scroll-to-top). Run the
    test and confirm every case PASSES against the current unmodified component.
    Do NOT modify `UsersDirectoryClient.tsx` — if a case cannot pass without changing
    the component, the fixture/mock is wrong, not the component (behavior parity is
    bug-for-bug per CONTEXT).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>All golden-path cases pass against the un-refactored monolith; the scroll-preservation case asserts window.scrollY is unchanged after modal close; the PERF-03 case asserts bulkUsers was called with { leanProjects: true }.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm full-suite + tsc gates and scope guard</name>
  <files>app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx</files>
  <action>
    Run the full type gate and the full unit suite to establish the green baseline the
    rest of the phase must hold. Record the exact total test count in the SUMMARY — every
    later extraction plan must hold or exceed it. Confirm the hard scope guard: the only
    files touched are package.json, package-lock.json, and the new test file — nothing
    under users/access-analysis or users/spatial-graph.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -20 && git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph" && echo "SCOPE VIOLATION" || echo "scope clean"</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; full suite green; git diff shows no files under users/access-analysis or users/spatial-graph; baseline test count recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
- zustand resolves at 5.x via require.
- Golden-path test green on the monolith (all cases, incl. scroll preservation + PERF-03 baseline).
- `npx tsc --noEmit` exits 0.
- Full unit suite green; baseline total count recorded.
- Scope guard: `git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph"` is empty.
</verification>

<success_criteria>
The regression safety net is in place and proven: zustand installed, golden-path
test passing against the current monolith, baseline test count captured. No
extraction has occurred yet — this is the gate that every later wave must keep green.
</success_criteria>

<output>
Create `.planning/phases/02-users-decomposition/02-01-SUMMARY.md` when done. Record the
exact baseline total test count and the zustand version installed.
</output>
