---
phase: 02-users-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(dashboard)/users/directoryUtils.ts
  - app/(dashboard)/users/DirectoryPills.tsx
  - app/(dashboard)/users/DataCoverageStrip.tsx
  - app/(dashboard)/users/PersonDetailModal.tsx
  - app/(dashboard)/users/CollapsibleGroup.tsx
  - app/(dashboard)/users/UsersDirectoryClient.tsx
autonomous: true
requirements: [USR-01]

must_haves:
  truths:
    - "Pure helper functions and stateless display sub-components live in their own files"
    - "UsersDirectoryClient imports the extracted helpers/components instead of defining them inline"
    - "The golden-path test stays green after the extraction"
    - "Search, filters, grid/list, group-by, modal open/close, and scroll preservation are observably identical"
  artifacts:
    - path: "app/(dashboard)/users/directoryUtils.ts"
      provides: "normalize, uniqueSorted, parseSearchTokens, matchesPerson + OrgPerson/LocalDirectoryUser/GroupByField/ViewMode types"
      min_lines: 90
    - path: "app/(dashboard)/users/PersonDetailModal.tsx"
      provides: "Centered Dialog profile modal (PersonDetailModal + PersonAvatar + CopyButton + InfoRow)"
      min_lines: 100
    - path: "app/(dashboard)/users/DirectoryPills.tsx"
      provides: "StatusPill, AdminPill, AccBadge + STATUS_PILL_LABEL/STATUS_PILL_CLASS"
      min_lines: 60
    - path: "app/(dashboard)/users/DataCoverageStrip.tsx"
      provides: "DataCoverageStrip + CoveragePill"
      min_lines: 40
    - path: "app/(dashboard)/users/CollapsibleGroup.tsx"
      provides: "Group-by collapsible header"
      min_lines: 25
  key_links:
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/directoryUtils.ts"
      via: "import of parseSearchTokens/matchesPerson/uniqueSorted + OrgPerson type"
      pattern: "from \"./directoryUtils\""
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/PersonDetailModal.tsx"
      via: "import of PersonDetailModal used in the shell render"
      pattern: "from \"./PersonDetailModal\""
---

<objective>
Extract the pure helpers and the stateless display sub-components out of the
monolith into their own files, leaving behavior byte-identical. These are the
lowest-risk seams (no tRPC state, no virtualizer): helper utilities, the profile
Dialog, the status/admin pills, the coverage strip, and the group-by header.

Purpose: USR-01 decomposition — shrink the monolith by moving self-contained units
first so later, riskier extractions (PersonRowList, store, data hook) land on a
smaller, clearer shell. Behavior parity is bug-for-bug (CONTEXT lock).
Output: 5 new files; UsersDirectoryClient imports them; golden-path test still green.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-users-decomposition/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

# The monolith — exact line ranges to extract are listed per task below
@app/(dashboard)/users/UsersDirectoryClient.tsx
# The golden-path safety net — must stay green
@app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure helpers and shared types into directoryUtils.ts</name>
  <files>app/(dashboard)/users/directoryUtils.ts, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/directoryUtils.ts` and MOVE (verbatim, no logic
    change) these definitions out of UsersDirectoryClient.tsx: the `OrgPerson`
    interface (line ~109), the `LocalDirectoryUser` interface (line ~120), the
    `GroupByField` and `ViewMode` type aliases (lines ~129-130), and the helper
    functions `normalize`, `uniqueSorted`, `parseSearchTokens`, `matchesPerson`
    (lines ~138-229). Export each as a named export per CONVENTIONS (no default
    export). In UsersDirectoryClient.tsx, replace the moved definitions with
    `import type { OrgPerson, LocalDirectoryUser, GroupByField, ViewMode } from
    "./directoryUtils"` and `import { normalize, uniqueSorted, parseSearchTokens,
    matchesPerson } from "./directoryUtils"`. Do NOT alter any function body — this
    is a move, not a rewrite. Keep `OrgPerson` exported because PersonDetailModal,
    PersonRow, and the store (later waves) all need it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>directoryUtils.ts exports the 4 helpers + 4 types; UsersDirectoryClient imports them; tsc 0; golden-path test green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the profile Dialog into PersonDetailModal.tsx</name>
  <files>app/(dashboard)/users/PersonDetailModal.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/PersonDetailModal.tsx` (`"use client"`) and MOVE
    verbatim: `PersonAvatar` (line ~421), `CopyButton` (line ~463), `InfoRow`
    (line ~481), and `PersonDetailModal` (lines ~513-620). Keep PersonDetailModal a
    centered shadcn `Dialog` exactly as-is — do NOT migrate it to the Phase 1
    DrillSheet/Sheet (that is explicitly deferred to Phase 4 per RESEARCH Open Q1).
    Move the `UserProfilePanel` dynamic import (lines ~89-96) into this file since the
    modal is its only consumer. Import `OrgPerson` from `./directoryUtils` and
    `BulkAccUser` from `@/lib/acc/acc-types`. Export `PersonDetailModal` (named) and,
    if PersonAvatar is referenced elsewhere in the shell render, export it too;
    otherwise keep it module-private. In UsersDirectoryClient.tsx, delete the moved
    code and add `import { PersonDetailModal } from "./PersonDetailModal"`. The shell's
    existing `<PersonDetailModal ... />` JSX (line ~2440) stays unchanged including its
    `accUser` resolution via `mergedAccUsers.find(...)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>PersonDetailModal.tsx owns the Dialog + avatar/copy/inforow + UserProfilePanel dynamic import; modal still opens/closes; modal remains a centered Dialog (not migrated to Sheet); tsc 0; golden-path test green.</done>
</task>

<task type="auto">
  <name>Task 3: Extract pills, coverage strip, and collapsible group</name>
  <files>app/(dashboard)/users/DirectoryPills.tsx, app/(dashboard)/users/DataCoverageStrip.tsx, app/(dashboard)/users/CollapsibleGroup.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create three files (`"use client"` where they use hooks/state). (1) `DirectoryPills.tsx`:
    MOVE `STATUS_PILL_LABEL` + `STATUS_PILL_CLASS` (lines ~631-641), `StatusPill`
    (line ~648), `AdminPill` (line ~691), and `AccBadge` (line ~721) verbatim. Export
    `STATUS_PILL_LABEL` (named) because the shell render references it for the active
    filter pill (lines ~2199, ~2315). (2) `DataCoverageStrip.tsx`: MOVE `CoveragePill`
    (line ~1056) and `DataCoverageStrip` (line ~1086) verbatim; export `DataCoverageStrip`.
    (3) `CollapsibleGroup.tsx`: MOVE `CollapsibleGroup` (lines ~999-1031) verbatim; export it.
    In each new file import only what that unit needs (lucide icons, cn, AggregatedStatus,
    BulkAccUser, Badge/Tooltip primitives). In UsersDirectoryClient.tsx delete the moved
    definitions and add the three imports plus `import { STATUS_PILL_LABEL } from
    "./DirectoryPills"`. Do not change any markup or class names — pill text is
    load-bearing (CONTEXT lock); color is decoration only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx" && git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph" && echo "SCOPE VIOLATION" || echo "scope clean"</automated>
  </verify>
  <done>DirectoryPills/DataCoverageStrip/CollapsibleGroup each own their components; STATUS_PILL_LABEL is imported by the shell; tsc 0; golden-path test green; scope guard clean.</done>
</task>

</tasks>

<verification>
- Each extracted file compiles and is imported by the shell.
- `npx tsc --noEmit` exits 0 after every task.
- Golden-path test green after every task (run per-task as the gate).
- Full suite count holds: run `npm test` at end and confirm against the 02-01 baseline.
- Scope guard: no files under users/access-analysis or users/spatial-graph touched.
</verification>

<success_criteria>
The five lowest-risk units (helpers, profile Dialog, pills, coverage strip, group
header) live in their own files; UsersDirectoryClient is meaningfully smaller and
imports them; the golden-path test and tsc gate remain green; observable behavior
and rendered data are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/02-users-decomposition/02-02-SUMMARY.md` when done. Record the
new UsersDirectoryClient.tsx line count and confirm the test count held vs the 02-01 baseline.
</output>
