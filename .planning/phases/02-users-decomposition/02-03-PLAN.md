---
phase: 02-users-decomposition
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/(dashboard)/users/PersonRow.tsx
  - app/(dashboard)/users/PersonRowList.tsx
  - app/(dashboard)/users/UsersDirectoryClient.tsx
autonomous: true
requirements: [USR-01]

must_haves:
  truths:
    - "PersonRow (list-view row + FileActivityCell + LastFileActivityCell) lives in PersonRow.tsx"
    - "PersonRowList (window virtualizer + visibility hook + batch query) lives in PersonRowList.tsx"
    - "The useWindowVirtualizer + [, setMounted] scrollMargin-init hack is copied verbatim, not refactored"
    - "Window-virtualization behavior and scroll position are observably identical"
    - "The golden-path test stays green"
  artifacts:
    - path: "app/(dashboard)/users/PersonRow.tsx"
      provides: "List-view row with hover-prefetch cell + last-activity cell + status/admin pills"
      min_lines: 120
      contains: "getFileActivityForUser"
    - path: "app/(dashboard)/users/PersonRowList.tsx"
      provides: "Window-virtualized row list with viewport-driven batch activity query"
      min_lines: 90
      contains: "useWindowVirtualizer"
  key_links:
    - from: "app/(dashboard)/users/PersonRowList.tsx"
      to: "app/(dashboard)/users/useVisibleRowEmails.ts"
      via: "import useVisibleRowEmails for viewport visibility tracking"
      pattern: "useVisibleRowEmails"
    - from: "app/(dashboard)/users/PersonRowList.tsx"
      to: "app/(dashboard)/users/PersonRow.tsx"
      via: "renders PersonRow per virtual item"
      pattern: "PersonRow"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/PersonRowList.tsx"
      via: "shell renderPeople uses PersonRowList in list view"
      pattern: "from \"./PersonRowList\""
---

<objective>
Extract the list-view row and the window-virtualized list — the single riskiest
behavioral seam in the decomposition — into PersonRow.tsx and PersonRowList.tsx.
The virtualizer's scrollMargin-init hack and the viewport-driven batch activity
query move verbatim so scroll position and lazy activity loading stay identical.

Purpose: USR-01 decomposition of the largest stateful render sub-tree. The
virtualizer reads window.scrollY; remounting or refactoring it would reset scroll
and break the strictest CONTEXT lock (scroll preservation on modal close). This
plan isolates that risk in one focused step guarded by the golden-path test.
Output: PersonRow.tsx + PersonRowList.tsx; shell renders PersonRowList in list view.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-users-decomposition/02-RESEARCH.md

# The monolith — PersonRow ~803-901, PersonRowList ~904-997, file-activity cells ~295-420
@app/(dashboard)/users/UsersDirectoryClient.tsx
# The visibility hook PersonRowList consumes (leave unchanged)
@app/(dashboard)/users/useVisibleRowEmails.ts
# The golden-path safety net — must stay green
@app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PersonRow + the two file-activity cells into PersonRow.tsx</name>
  <files>app/(dashboard)/users/PersonRow.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/PersonRow.tsx` (`"use client"`) and MOVE verbatim:
    the file-activity column helpers `FileActivityKey` type + `FILE_ACTIVITY_COLUMNS`
    (lines ~235-242), `FileActivityCell` (lines ~295-372, owns
    `trpc.accActivity.getFileActivityForUser.useQuery` gated by `enabled: active` —
    preserve that exactly), `LastFileActivityCell` (lines ~374-420), and `PersonRow`
    (lines ~803-901). Import `OrgPerson` from `./directoryUtils`, `BulkAccUser` from
    `@/lib/acc/acc-types`, `AggregatedStatus`/`reduceMemberStatus` from
    `@/lib/acc/accStatusReduction`, and the pills from `./DirectoryPills`. Export
    `PersonRow` (named). Keep all hover-prefetch / `enabled` flags and class names
    byte-identical. In UsersDirectoryClient.tsx delete the moved definitions; the
    import of PersonRow can be transitive (PersonRowList imports it in Task 2) — do
    not add a direct PersonRow import to the shell unless something there still
    references it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PersonRow.tsx owns PersonRow + both activity cells + the column-key helpers; getFileActivityForUser stays enabled-gated by hover; tsc 0.</done>
</task>

<task type="auto">
  <name>Task 2: Extract PersonRowList.tsx with the virtualizer hack verbatim</name>
  <files>app/(dashboard)/users/PersonRowList.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
    Create `app/(dashboard)/users/PersonRowList.tsx` (`"use client"`) and MOVE the
    entire `PersonRowList` function (lines ~904-997) verbatim, INCLUDING the
    `const [, setMounted] = useState(false); useEffect(() => setMounted(true), [])`
    re-render hack (lines ~927-928) that lets `scrollMargin: parentRef.current?.offsetTop`
    pick up the DOM offset after mount. Do NOT refactor that hack away — it is the
    scroll-correctness invariant (RESEARCH Pitfall 3). Keep the `useWindowVirtualizer`
    config (estimateSize 72, overscan 8, scrollMargin from parentRef offsetTop), the
    `useVisibleRowEmails` import, the `getLastFileActivityBatch.useQuery` with
    `emails: visibleEmails.slice(0,200)` and `placeholderData: (prev) => prev`, and the
    composed ref callback that calls both `virtualizer.measureElement(el)` and
    `registerRefForEmail(el)` — all verbatim. Import `PersonRow` from `./PersonRow`,
    `OrgPerson` from `./directoryUtils`, and the same trpc/virtual/types deps. Export
    `PersonRowList` (named). In UsersDirectoryClient.tsx delete the moved function and
    add `import { PersonRowList } from "./PersonRowList"`. The shell's `renderPeople`
    list-view branch (line ~1907) keeps its existing `<PersonRowList ... />` props
    unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"</automated>
  </verify>
  <done>PersonRowList.tsx owns the virtualizer + visibility hook + batch query with the setMounted hack copied verbatim; shell imports it; tsc 0; golden-path test green (incl. list-view + scroll-preservation cases).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm parity gates and scope guard</name>
  <files>app/(dashboard)/users/PersonRowList.tsx</files>
  <action>
    Run the full unit suite and confirm the count holds vs the 02-01 baseline. Then
    manually reason through the scroll-preservation invariant: the golden-path
    scroll-preservation case must still pass AND `PersonRowList` must not be remounted
    on filter/selection changes (its key/position in the render tree is unchanged).
    Confirm the hard scope guard.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -20 && git diff --name-only HEAD | grep -E "users/access-analysis|users/spatial-graph" && echo "SCOPE VIOLATION" || echo "scope clean"</automated>
  </verify>
  <done>tsc 0; full suite count holds vs baseline; scope guard clean; scroll-preservation case green.</done>
</task>

</tasks>

<verification>
- PersonRow.tsx and PersonRowList.tsx compile and are wired (PersonRowList → PersonRow → shell).
- The setMounted scrollMargin hack is present verbatim in PersonRowList.tsx.
- `npx tsc --noEmit` exits 0.
- Golden-path test green, especially the list-view + scroll-preservation cases.
- Full suite count holds vs 02-01 baseline.
- Scope guard clean.
</verification>

<success_criteria>
The window-virtualized list and its row render in their own files with the scroll
init hack preserved verbatim; list-view rendering, lazy activity loading, and scroll
position are observably identical; golden-path test and tsc gate stay green.
</success_criteria>

<output>
Create `.planning/phases/02-users-decomposition/02-03-SUMMARY.md` when done. Note the
shell line count and confirm the scroll-preservation case still passes.
</output>
