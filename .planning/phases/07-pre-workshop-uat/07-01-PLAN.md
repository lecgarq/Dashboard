---
phase: 07-pre-workshop-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/uat-workshop.spec.ts
  - tests/e2e/uat-helpers.ts
  - scripts/uat/run-engineering-gates.cjs
  - .planning/phases/07-pre-workshop-uat/UAT-REPORT.md
autonomous: true
requirements:
  - VIS-05
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - THM-01
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
user_setup: []

must_haves:
  truths:
    - "All four pages (/users, /access-analysis, /template-mty, /forma-proposal) load against the production build on :3100 with no page error or crash"
    - "npx tsc --noEmit exits 0 including test files, captured in the report"
    - "npm run repo-map:check exits 0 (no new fetch/effect/Prisma-in-UI regression), captured in the report"
    - "git diff --name-only origin/deploy...HEAD shows zero files under users/access-analysis/ (spatial-graph boundary intact)"
    - "No conditional GraphCanvas mount appears in any of the four UAT page directories"
    - "Each of the three RSC pages (/access-analysis, /template-mty, /forma-proposal) issues 0 POST /api/trpc requests; /users issues <=2 batch POSTs with no duplicate procedure key across batches"
    - "Canvas count is 0 on /access-analysis and /template-mty, <=1 on /users and /forma-proposal"
    - "prefers-reduced-motion:reduce leaves layout visible with no horizontal overflow at 1280px on all four pages"
    - "WCAG AA color-contrast (axe-core color-contrast rule) shows zero serious/critical violations on DOM-accessible data labels in BOTH light and dark themes on all four pages"
    - "Every drill in Success Criterion #2 opens (donut legend -> people drill, View N people -> people-sheet, terrain expand, coordination row expand, table row -> drill panel, role-similarity node -> RoleOverviewSheet)"
    - "An all-green/red engineering-gate report exists at .planning/phases/07-pre-workshop-uat/UAT-REPORT.md with per-page results and attached screenshots"
  artifacts:
    - path: "tests/e2e/uat-workshop.spec.ts"
      provides: "Playwright UAT spec driving all 4 pages with every scriptable gate + drill smoke + per-page-per-theme screenshots"
      min_lines: 200
    - path: "tests/e2e/uat-helpers.ts"
      provides: "Shared helpers: tRPC batch-body parser, axe-core CDN injector, theme toggle, reduced-motion + overflow check, uatScreenshot attach"
      min_lines: 40
    - path: "scripts/uat/run-engineering-gates.cjs"
      provides: "Wrapper that runs tsc/repo-map:check/boundary-greps, then the Playwright UAT spec, and writes UAT-REPORT.md"
      min_lines: 60
    - path: ".planning/phases/07-pre-workshop-uat/UAT-REPORT.md"
      provides: "Single pass/fail engineering report with per-page gate table, screenshot links, defect log, and the owner checklist stub"
      contains: "Engineering Gates"
  key_links:
    - from: "tests/e2e/uat-workshop.spec.ts"
      to: "playwright.verify.config.ts"
      via: "run via --config playwright.verify.config.ts against E2E_BASE_URL=http://localhost:3100 (production next start, no webServer block)"
      pattern: "playwright.verify.config"
    - from: "tests/e2e/uat-workshop.spec.ts"
      to: "tests/e2e/uat-helpers.ts"
      via: "imports parseTrpcBatch, injectAxe, toggleTheme, assertNoHorizontalOverflow, uatScreenshot"
      pattern: "from ['\"]\\./uat-helpers"
    - from: "scripts/uat/run-engineering-gates.cjs"
      to: ".planning/phases/07-pre-workshop-uat/UAT-REPORT.md"
      via: "writes the aggregated pass/fail report after running every gate"
      pattern: "UAT-REPORT"
---

<objective>
Build the full automated UAT harness for Phase 7 and emit ONE engineering-gate report covering all four redesigned pages (/users, /access-analysis, /template-mty, /forma-proposal). The harness drives a production build served on :3100, runs every scriptable gate, exercises every drill in Success Criterion #2, captures per-page-per-theme screenshots, and writes a single pass/fail report. Defects found are fixed inline and the failing gate re-run to green, all within Phase 7.

Purpose: The engineering gates (tsc-0, repo-map ratchet, boundary greps, fetch-once, GPU/canvas proxy, reduced-motion, 1280px overflow, WCAG AA contrast both themes, drill smoke) are ABSOLUTE and objective — they protect the live demo from a runaway fetch, GPU leak, illegible label, or a drill that does not open. This plan makes them all scriptable and produces the evidence the owner reads before the live projector pass.
Output: tests/e2e/uat-workshop.spec.ts, tests/e2e/uat-helpers.ts, scripts/uat/run-engineering-gates.cjs, and .planning/phases/07-pre-workshop-uat/UAT-REPORT.md.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pre-workshop-uat/07-CONTEXT.md
@.planning/phases/07-pre-workshop-uat/07-RESEARCH.md

# Harness base + auth (read before writing the spec)
@playwright.verify.config.ts
@playwright/global-setup.ts
@tests/e2e/forma-proposal.spec.ts
@tests/e2e/acc-dc-graph.spec.ts

# tRPC batching + theme + motion mechanics (the gates depend on these)
@lib/core/providers.tsx
@components/ui/motion.ts

# The four pages' client components + drill testids
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/template-mty/components/TemplateAnalysisCharts.tsx
@app/(dashboard)/users/UsersDirectoryClient.tsx
@app/(dashboard)/forma-proposal/components/FormaProposalClient.tsx
@components/ui/DataTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve build/serve logistics and write the UAT helpers module</name>
  <files>tests/e2e/uat-helpers.ts</files>
  <action>
    Create tests/e2e/uat-helpers.ts exporting the shared primitives the UAT spec needs. Pull every selector and mechanic from 07-RESEARCH.md (Q3, Q4, Don't Hand-Roll table) — do not invent selectors.

    Resolve the five RESEARCH open decisions in this file's header comment block so the executor and owner share one source of truth:
    (a) BUILD ISOLATION — the proven-safe path is: stop the :3000 Task Scheduler instance, then build, then serve on :3100; do NOT npm run build while :3000 is live (it 500s the running app ~8 min). Record that NEXT_DIST_DIR=.next-uat isolation is UNVERIFIED and must be validated by the owner runbook in plan 07-02 before relying on it. (The owner runs the build/serve; this spec only consumes :3100.)
    (b) NEXT_PUBLIC_NEW_ACCESS_ANALYSIS is NOT required for the UAT build — verified that the flag appears only in app/(dashboard)/users/page.test.tsx as a test fixture; the new /access-analysis route (app/(dashboard)/access-analysis/page.tsx) is the default, not flag-gated. Do not set it.
    (c) axe-core is pinned to version 4.10.0, injected via CDN with page.addScriptTag({ url: "https://cdnjs.cloudflare.com/ajax/libs/axe-core/4.10.0/axe.min.js" }).
    (d) RoleSimilarityGraph node click requires a settle wait — waitForLoadState("networkidle") then a ~2000ms settle before clicking the first settled SVG circle (per RESEARCH Pitfall 8 settle-and-freeze).
    (e) the inline fix-and-re-run loop is implemented in Task 3 of this plan.

    Export these helpers:
    - parseTrpcBatch(postData: string | null): string[] — parse a tRPC httpBatchLink POST body (JSON object keyed "0","1",...) and return the procedure keys. Used to detect duplicate procedure keys across separate batch POSTs (PERF-03/PERF-04).
    - injectAxeAndRunContrast(page): inject axe-core 4.10.0 from CDN, run axe.run with runOnly color-contrast, return violations filtered to impact serious|critical.
    - toggleTheme(page, theme: "Light"|"Dark"): click getByRole("button",{name:"Toggle theme"}) then the menuitem; for Dark, waitForSelector("html.dark") then waitForTimeout(500) so EChart key={resolvedTheme} remounts (RESEARCH Pitfall 3).
    - assertNoHorizontalOverflow(page): compare document.documentElement.scrollWidth vs clientWidth with a 5px tolerance.
    - uatScreenshot(page, testInfo, name): page.screenshot({fullPage:false}) then testInfo.attach (mirror proofShot in acc-dc-graph.spec.ts).
    Keep helpers pure/stateless; no global state.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "uat-helpers" | grep -qx 0 && echo "helpers typecheck clean"</automated>
  </verify>
  <done>tests/e2e/uat-helpers.ts exists, typechecks with zero errors referencing it, and exports parseTrpcBatch, injectAxeAndRunContrast, toggleTheme, assertNoHorizontalOverflow, uatScreenshot. The five decisions are recorded in its header comment.</done>
</task>

<task type="auto">
  <name>Task 2: Write the UAT spec driving all four pages with every scriptable gate + drill smoke</name>
  <files>tests/e2e/uat-workshop.spec.ts</files>
  <action>
    Create tests/e2e/uat-workshop.spec.ts. It MUST run under playwright.verify.config.ts (production next start on :3100, no webServer block) — never the default dev config. Add test.use({ viewport: { width: 1280, height: 800 } }) at file scope (RESEARCH Pitfall 5 — the default config is 1600x1000). Import helpers from ./uat-helpers.

    STATIC engineering gates (one test block each; shell out via node child_process execSync, fail the test on non-clean output):
    - tsc-0: run npx tsc --noEmit; assert exit 0. (Also run by the wrapper in Task 3; assert here too so the report carries it.)
    - boundary diff: run git diff --name-only origin/deploy...HEAD; assert zero lines contain "users/access-analysis/".
    - GraphCanvas grep: grep -rn "GraphCanvas" across UsersDirectoryClient.tsx, app/(dashboard)/access-analysis/, app/(dashboard)/template-mty/, app/(dashboard)/forma-proposal/; assert no matches (no conditional mount introduced).

    PER-PAGE gates — one describe block per page. For each of the four pages register a page.on("request") collector that pushes {url, postData} for every POST whose url includes /api/trpc BEFORE page.goto. After goto + waitForLoadState("networkidle"):
    - PERF-01 skeleton/load: navigate and assert the page's real anchor is visible within the verify-config timeout. Use REAL selectors (the kpi-strip testid does NOT exist): /users -> getByTestId("users-directory") (skeleton getByTestId("users-table-skeleton")); /access-analysis -> the h1 "Access Analysis" then getByTestId("role-legend"); /template-mty -> its h1 then the members table tbody; /forma-proposal -> getByText("VDC Specialist") path per the existing forma-proposal.spec.
    - PERF-03/04 fetch-once: parse each collected POST body via parseTrpcBatch; build a count map of procedure keys across ALL batches. Assert no procedure key appears in two SEPARATE batch POSTs. For /access-analysis, /template-mty, /forma-proposal assert the collector is empty (0 POST /api/trpc). For /users assert <=2 batch POSTs and that accDcGraph.bulkUsers appears in exactly one batch (the heavy ~15MB query — RESEARCH Q4).
    - PERF-05 canvas/GPU proxy: page.locator("canvas").count() — assert 0 on /access-analysis and /template-mty, <=1 on /users and /forma-proposal. Also capture (performance as any).memory?.usedJSHeapSize via page.evaluate and record it (proxy only; true GPU<400MB is owner-observed per RESEARCH Q3).
    - reduced-motion + 1280px overflow: in a second context (or after page.emulateMedia({reducedMotion:"reduce"}) BEFORE goto), re-navigate, assert the page anchor is visible, then assertNoHorizontalOverflow(page).
    - THM-01 contrast both themes: run injectAxeAndRunContrast(page) in light theme, assert zero serious|critical violations; toggleTheme(page,"Dark"), re-run, assert zero. Capture uatScreenshot in BOTH themes (name e.g. users-light / users-dark).

    DRILL SMOKE (Success Criterion #2) — every drill must open:
    - /users: click a DataTable body row (table tbody tr.first()) -> assert getByTestId("user-detail-panel") (or [role=dialog]) visible (INT-03). Exercise inline row-expand via the [data-expand] affordance and assert it toggles.
    - /access-analysis: click a getByTestId("role-legend") button -> assert getByTestId("module-drilldown") or the inline role-drilldown people list appears (INT-02); click getByTestId("view-people-role") -> assert getByTestId("people-sheet") visible (INT-01); click getByTestId("terrain-expand") -> assert aria-expanded toggles to true and getByTestId("terrain-canvas") mounts (ACC-03); expand a CoordinationByProject row (button[aria-expanded=false]) -> assert aria-expanded becomes true (lazy clash drill); select a project in ProjectPicker -> assert getByTestId("filter-banner") visible and getByTestId("filter-scope") shows "of", then getByTestId("filter-clear") dismisses it (INT-04/INT-05).
    - /template-mty: click a members-table body row -> assert getByTestId("author-profile-drawer") visible; wait ~2000ms for RoleSimilarityGraph settle then click the first settled svg circle -> assert a [role=dialog] (RoleOverviewSheet) with role content appears (TPL-02); click getByTestId("terrain-expand") if present -> assert expand/collapse cycle.
    - /forma-proposal: reuse the existing tests/e2e/forma-proposal.spec.ts happy path — click role (VDC Specialist visible), set getByLabel("permission tier"), reload -> draft persists, Export JSON triggers a download. Confirm canvas count <=1 before and after role selection (RESEARCH Open Q3 — record both counts).

    Every assertion failure must produce a uatScreenshot attachment named for the page+gate so the report links the evidence. Do NOT run any spatial-graph spec in this invocation (RESEARCH Pitfall 2 — lasso flake).
  </action>
  <verify>
    <automated>npx playwright test uat-workshop --config playwright.verify.config.ts --list 2>&1 | grep -v '^#' | grep -c "uat-workshop.spec.ts" | grep -qvx 0 && echo "spec discovered by verify config"</automated>
  </verify>
  <done>tests/e2e/uat-workshop.spec.ts exists, is discovered by playwright.verify.config.ts, uses 1280x800 viewport, and contains: the 3 static gates, per-page PERF-01/03/04/05 + reduced-motion + 1280px overflow + THM-01 both-theme contrast + screenshots, and the full Success Criterion #2 drill smoke for all four pages using the verified testids.</done>
</task>

<task type="auto">
  <name>Task 3: Engineering-gate wrapper + report emitter + inline fix-and-re-run loop</name>
  <files>scripts/uat/run-engineering-gates.cjs, .planning/phases/07-pre-workshop-uat/UAT-REPORT.md</files>
  <action>
    Create scripts/uat/run-engineering-gates.cjs — a Node CommonJS wrapper that runs every scriptable gate in order and writes the single report. It is the orchestrator the owner-facing runbook (plan 07-02) invokes. Steps, each capturing PASS/FAIL + stderr tail:
    1. npx tsc --noEmit (the cheap full-tree gate incl. test files; do NOT npm run build here — that 500s :3000 per RESEARCH Q5/Pitfall 7).
    2. npm run repo-map:check (npm run repo-map then node scripts/repo-map/check.cjs; exit 0 = pass — ratchets re-introduced fetch/effect/direct-prisma-in-ui).
    3. git diff --name-only origin/deploy...HEAD piped to a filter for "users/access-analysis/" — pass iff empty.
    4. the GraphCanvas grep across the four UAT page directories — pass iff no matches.
    5. The Playwright run: E2E_BASE_URL=http://localhost:3100 npx playwright test uat-workshop --config playwright.verify.config.ts. (The wrapper assumes :3100 is already serving the production build — it does NOT build or start the server; that is the owner runbook's job in 07-02, honoring the do-not-build-while-:3000-is-live hazard.)

    The wrapper writes/overwrites .planning/phases/07-pre-workshop-uat/UAT-REPORT.md using the structure in RESEARCH Q6: an "Engineering Gates" table (gate | command | result | notes), a "Per-Page Gate Results" section for each of the four pages, a "Screenshots" section linking the Playwright-attached captures, a "Defects Found" table (#, page, severity BLOCKER|COSMETIC, description, status FIXED|NOTED), and an "Owner Perceptual Checklist" stub that plan 07-02 fills. Print a final ALL-GREEN or FAILED summary line to stdout with the count of failing gates.

    INLINE FIX-AND-RE-RUN LOOP (decision e) — document this loop as a top-of-file comment AND implement it as the wrapper's exit contract: when any gate fails, the wrapper exits non-zero and prints the exact failing gate + its re-run command. The execution protocol is: (1) the executor reads the failing gate, (2) fixes the underlying defect inline within Phase 7 — but if the fix would touch any file under users/access-analysis/ STOP and surface it (the boundary is absolute, never patch the spatial-graph to make a gate pass), (3) re-runs ONLY that gate's command to green, (4) re-runs the full wrapper once more for a clean all-green report. Cosmetic-only findings are logged in the Defects table as severity COSMETIC status NOTED and do NOT block (per the blocker bar in CONTEXT.md); engineering-gate failures are absolute and MUST reach green.

    Run the wrapper once now to generate the initial UAT-REPORT.md. If :3100 is not serving (owner has not built yet), the Playwright step will fail — record that step as "BLOCKED: build/serve runbook (07-02) not yet run" rather than FAIL, and confirm steps 1-4 (which need no server) are captured green.
  </action>
  <verify>
    <automated>node scripts/uat/run-engineering-gates.cjs --static-only 2>&1 | grep -v '^#' | grep -Eqi "tsc|repo-map|boundary|graphcanvas" && test -f .planning/phases/07-pre-workshop-uat/UAT-REPORT.md && echo "wrapper ran + report written"</automated>
  </verify>
  <done>scripts/uat/run-engineering-gates.cjs runs tsc-0, repo-map:check, the boundary diff, the GraphCanvas grep, and the Playwright UAT run; writes .planning/phases/07-pre-workshop-uat/UAT-REPORT.md with the Engineering Gates table, per-page results, screenshots, defect log, and owner-checklist stub; exits non-zero on any engineering-gate failure printing the exact re-run command; the inline fix-and-re-run loop (with the absolute users/access-analysis/ boundary) is documented at the top of the file. The static gates (steps 1-4) are captured green in the initial report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner -> .env | global-setup reads AUTH_SECRET, ADMIN_EMAIL, DATABASE_URL to mint a real session JWT; established pattern, no new surface |
| axe-core CDN script -> page context | external script injected into the authenticated page for contrast checks |
| UAT spec -> live data | runs against the production build with a real ADMIN JWT and real DB rows (read-only navigation + drills) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | git diff boundary gate | mitigate | boundary diff vs origin/deploy is a hard fail; fix-loop forbids touching users/access-analysis/ to pass a gate |
| T-07-02 | Information Disclosure | axe-core CDN injection | accept | injected only into a localhost authenticated page during a manual UAT run; no secrets exposed to the script; version pinned (4.10.0) to avoid supply drift |
| T-07-03 | Denial of Service | building while :3000 live | mitigate | wrapper never runs npm run build; build/serve isolated to :3100 via the 07-02 owner runbook (stop :3000 first) |
| T-07-04 | Tampering | inline fix-and-re-run | mitigate | engineering gates are absolute; cosmetic-only defects are NOTED not patched; spatial-graph boundary is never patched to pass |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages this phase (RESEARCH Package Legitimacy Audit: all tooling already installed); slopcheck N/A |
</threat_model>

<verification>
- npx tsc --noEmit exits 0 (full tree incl. test files).
- npm run repo-map:check exits 0.
- git diff --name-only origin/deploy...HEAD has zero lines under users/access-analysis/.
- GraphCanvas grep across the four UAT page dirs returns no matches.
- npx playwright test uat-workshop --config playwright.verify.config.ts passes every per-page gate + drill smoke against :3100 (production build), once the 07-02 runbook has built+served :3100.
- .planning/phases/07-pre-workshop-uat/UAT-REPORT.md exists and reads ALL-GREEN for the engineering gates (or lists each defect with FIXED/NOTED status).
</verification>

<success_criteria>
The automated harness exists and produces ONE engineering-gate report. All four pages clear: tsc-0, repo-map:check, boundary diff, GraphCanvas grep, fetch-once (0 tRPC on the three RSC pages; no duplicate procedure on /users), canvas count (0 on data pages, <=1 on /users + /forma-proposal), reduced-motion + 1280px no-overflow, WCAG AA contrast in both themes, and every Success Criterion #2 drill opens. Defects found during the run are fixed inline and the failing gate re-run to green; cosmetic nits are logged NOTED. The spatial-graph boundary is never touched.
</success_criteria>

<output>
Create `.planning/phases/07-pre-workshop-uat/07-01-SUMMARY.md` when done.
</output>
