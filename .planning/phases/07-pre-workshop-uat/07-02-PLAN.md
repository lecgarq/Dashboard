---
phase: 07-pre-workshop-uat
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md
  - .planning/phases/07-pre-workshop-uat/UAT-REPORT.md
autonomous: false
requirements:
  - VIS-05
  - PERF-01
  - PERF-05
  - THM-01
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
user_setup:
  - service: local-deploy
    why: "The owner must build the production UAT copy and serve it on :3100 without taking the live :3000 down, then run the live projector pass on his real secondary display at projector-reduced brightness — no automation can simulate the room"
    dashboard_config:
      - task: "Stop the :3000 Task Scheduler instance (proven-safe build isolation), build the UAT copy, start it on :3100, run the gate wrapper, then run the live projector pass, then promote to :3000 only after sign-off"
        location: "Owner's PC — Task Scheduler + PowerShell + secondary display/projector"

must_haves:
  truths:
    - "A reproducible build-and-serve-on-:3100 runbook exists that never runs npm run build while :3000 is live"
    - "The runbook resolves build isolation: it either verifies NEXT_DIST_DIR=.next-uat leaves the live .next/ untouched, or falls back to stopping the :3000 Task Scheduler task before building"
    - "The owner perceptual checklist exists per page covering projector-brightness legibility, no clipped modals, no horizontal overflow at 1280px, premium feel, smooth/directional drill motion, and GPU<400MB observed in DevTools"
    - "A combined sign-off section records DoD = engineering report ALL-GREEN AND owner's explicit 'approved on the projector', with every blocker cleared"
    - "Room-breaking blockers (invisible/illegible label, clipped modal, horizontal overflow, page error/crash, drill that doesn't open) are distinguished from cosmetic nits (ship-with-note)"
  artifacts:
    - path: ".planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md"
      provides: "Owner runbook: build-isolation decision + exact :3100 build/serve commands + gate-wrapper invocation + live projector procedure + promote-to-:3000 step"
      min_lines: 50
      contains: "3100"
    - path: ".planning/phases/07-pre-workshop-uat/UAT-REPORT.md"
      provides: "Owner Perceptual Checklist section filled + Combined Sign-Off section with the projector verdict"
      contains: "Combined Sign-Off"
  key_links:
    - from: ".planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md"
      to: "scripts/uat/run-engineering-gates.cjs"
      via: "runbook invokes the gate wrapper from plan 07-01 after :3100 is serving"
      pattern: "run-engineering-gates"
    - from: ".planning/phases/07-pre-workshop-uat/UAT-REPORT.md"
      to: "owner sign-off"
      via: "Combined Sign-Off section captures engineering ALL-GREEN AND the owner's explicit projector approval"
      pattern: "approved on the projector"
---

<objective>
Produce the owner-run side of the Phase 7 gate: the build-and-serve-on-:3100 runbook, the structured per-page perceptual checklist, and the combined sign-off protocol. The owner runs the live projector pass himself on the real secondary display at projector-reduced brightness — nothing automated can simulate the room, and the owner is the final acceptance authority.

Purpose: The perceptual gates (projector-brightness legibility, "does it pop"/premium feel, clipped modals, horizontal overflow felt on the real display, smooth/directional drill motion, GPU<400MB observed) are the true acceptance condition. The green engineering report from 07-01 is necessary but NOT sufficient — Phase 7 is DONE only when the report is ALL-GREEN AND the owner says "approved on the projector" with every blocker cleared.
Output: .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md and the filled Owner Perceptual Checklist + Combined Sign-Off sections of UAT-REPORT.md.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pre-workshop-uat/07-CONTEXT.md
@.planning/phases/07-pre-workshop-uat/07-RESEARCH.md
@.planning/phases/07-pre-workshop-uat/07-01-SUMMARY.md
@.planning/phases/07-pre-workshop-uat/UAT-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the build-and-serve-on-:3100 owner runbook</name>
  <files>.planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md</files>
  <action>
    Create .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md — a step-by-step runbook the owner (non-technical) can follow. Pull exact commands from 07-RESEARCH.md Q5. Structure it as numbered steps with the literal PowerShell commands and what each produces. Resolve decision (a) build isolation with a verify-then-fallback:

    STEP 0 — Decide isolation (do this once):
    - SAFEST DEFAULT: stop the :3000 Task Scheduler instance first (the dashboard task at logon), build, serve :3100, run UAT, then promote + restart :3000. State that npm run build while :3000 is live freezes the running app ~8 min (RESEARCH Pitfall 7) — never do it.
    - OPTIONAL FASTER PATH (must be verified before trusting): set $env:NEXT_DIST_DIR=".next-uat" and build; verify the live .next/ directory's modified-time did NOT change during the build (so :3000 stays up). If .next/ is touched, abandon this path and use the safe default. Record the verification result inline in the runbook so future runs know which path is safe on this machine.

    STEP 1 — Build the UAT copy: the next build command writing to the isolated dist (NEXT_DIST_DIR=.next-uat). Note: do NOT set NEXT_PUBLIC_NEW_ACCESS_ANALYSIS=1 — the new /access-analysis route is the default (verified: the flag exists only in a /users test fixture). Do NOT set NEXT_PUBLIC_ACC_GRAPH_TEST=1 — that flag only affects the out-of-scope spatial-graph (RESEARCH Pitfall 1).

    STEP 2 — Serve on :3100: NEXT_DIST_DIR=.next-uat, PORT=3100, next start --port 3100. Confirm http://localhost:3100/users loads (production mode, real built output — not dev).

    STEP 3 — Run the engineering gates: node scripts/uat/run-engineering-gates.cjs (from plan 07-01). This regenerates UAT-REPORT.md. Confirm the ALL-GREEN summary line. If any gate is red, hand back to the inline fix-and-re-run loop (07-01) before the live pass.

    STEP 4 — Live projector pass: open http://localhost:3100 on the SECONDARY display at projector-reduced brightness, 1280px width, and walk the Owner Perceptual Checklist (Task 2) in BOTH light and dark (zinc) themes for all four pages.

    STEP 5 — Promote (only after sign-off): once the report is ALL-GREEN and the owner approves on the projector, promote to :3000 — restart the Task Scheduler dashboard task so the live :3000 serves the approved build. Promotion happens ONLY after sign-off.

    Include a one-line "if anything 500s or the build hangs" recovery note (restart the Task Scheduler task to bring :3000 back).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md && grep -q "3100" .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md && grep -qi "run-engineering-gates" .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md && echo "runbook present + wired"</automated>
  </verify>
  <done>UAT-RUNBOOK.md exists with the build-isolation decision (verify NEXT_DIST_DIR=.next-uat or stop :3000 first), the exact :3100 build/serve commands, the gate-wrapper invocation, the live projector procedure, and the promote-only-after-sign-off step. It never instructs building while :3000 is live.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the Owner Perceptual Checklist into UAT-REPORT.md</name>
  <files>.planning/phases/07-pre-workshop-uat/UAT-REPORT.md</files>
  <action>
    Replace the "Owner Perceptual Checklist" stub that plan 07-01 wrote into UAT-REPORT.md with the full structured checklist from 07-RESEARCH.md Q6. (Read the current UAT-REPORT.md first; edit only the checklist + add the Combined Sign-Off section — do not disturb the automated engineering-gate tables 07-01 produced.)

    The checklist is run live on the secondary display at projector-reduced brightness, 1280px, both light and dark (zinc) themes. Each item is marked PASS / BLOCK / COSMETIC. Cover all four pages with EQUAL depth (no hero page):
    - /users: no horizontal overflow at 1280px; data labels legible at projector brightness (both themes); table rows crisp, sticky header visible, density toggle works; row click -> drill panel slides in smoothly (<=200ms, directional); motion fires only on mount, not on filter changes; no clipped modals/panels.
    - /access-analysis: KPIs + donuts load first (streaming, not one big load); donut legend click -> inline people drill; "View N people" -> people-sheet slides in; terrain "Show" -> mounts + interactive; coordination row expand -> lazy clash data loads; filter banner visible when project selected + Clear works; cross-filter updates panels without page navigation; no horizontal overflow at 1280px; labels legible both themes.
    - /template-mty: members table loads, search works, row click -> profile drawer; role-similarity graph settles + node click -> RoleOverviewSheet; terrain expand works; labels legible both themes; no clipped modals.
    - /forma-proposal: role rail loads (VDC Specialist visible); click role -> folder tree updates; set tier -> chip updates; reload -> draft persists; Export JSON downloads; R3F particle accent visible but subtle (behind editor, off the data); no clipped modals at 1280px; labels legible both themes.
    - GPU observation (owner, DevTools Memory tab or chrome://gpu): GPU memory stays < 400MB across all four pages.

    State the blocker bar at the top of the checklist: BLOCK = room-breaking only (invisible/illegible label, clipped modal, horizontal overflow, page error/crash, a drill that doesn't open). COSMETIC = polish nit; ship-with-note, logged in the Defects table as NOTED, does NOT hold the phase.
  </action>
  <verify>
    <automated>grep -q "Owner Perceptual Checklist" .planning/phases/07-pre-workshop-uat/UAT-REPORT.md && grep -qi "projector" .planning/phases/07-pre-workshop-uat/UAT-REPORT.md && grep -qi "forma-proposal" .planning/phases/07-pre-workshop-uat/UAT-REPORT.md && echo "perceptual checklist present, all pages"</automated>
  </verify>
  <done>UAT-REPORT.md's Owner Perceptual Checklist is filled with equal-depth per-page items for all four pages + the GPU observation + the blocker-bar definition; the automated engineering-gate tables from 07-01 are intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Owner live projector pass + combined sign-off</name>
  <action>The owner runs the build/serve-on-:3100 runbook, confirms the engineering report is ALL-GREEN, then runs the live projector pass and records the Combined Sign-Off verdict. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>The automated engineering-gate report (07-01) and the owner runbook + perceptual checklist (this plan). The owner now runs the build/serve-on-:3100, confirms the report is ALL-GREEN, then runs the live projector pass himself on the real secondary display.</what-built>
  <how-to-verify>
    1. Follow UAT-RUNBOOK.md: stop :3000 (or verify NEXT_DIST_DIR=.next-uat isolation), build the UAT copy, serve it on :3100.
    2. Run `node scripts/uat/run-engineering-gates.cjs` and confirm the final line is ALL-GREEN (every engineering gate passed: tsc-0, repo-map:check, boundary diff, GraphCanvas grep, fetch-once, canvas/GPU, reduced-motion, 1280px overflow, WCAG AA contrast both themes, all drills open). If any gate is red, run the inline fix-and-re-run loop until green before continuing.
    3. Open http://localhost:3100 on the SECONDARY display at projector-reduced brightness, 1280px, and walk the Owner Perceptual Checklist for ALL FOUR pages in BOTH light and dark (zinc) themes. Mark each item PASS / BLOCK / COSMETIC.
    4. Clear every BLOCK (room-breaking) item via the inline fix-and-re-run loop; COSMETIC nits may remain (logged NOTED).
    5. Record the verdict in the Combined Sign-Off section. Then promote to :3000 (restart the Task Scheduler task) only after approving.
  </how-to-verify>
  <resume-signal>Type "approved on the projector" (with the report ALL-GREEN and every blocker cleared) to complete Phase 7, or describe the remaining blockers to send them back to the inline fix-and-re-run loop.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner build/serve -> live :3000 | building the UAT copy must not take the live dashboard down |
| Owner approval -> promote to :3000 | promotion swaps the live build; gated behind explicit sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Denial of Service | building while :3000 live | mitigate | runbook STEP 0 forces stop-:3000-first or verified NEXT_DIST_DIR isolation; never build against live :3000 |
| T-07-06 | Repudiation | sign-off provenance | mitigate | Combined Sign-Off section records the explicit "approved on the projector" verdict + ALL-GREEN report state + cleared-blocker list |
| T-07-07 | Elevation of Privilege | premature promote to :3000 | mitigate | promotion is a separate runbook step gated strictly behind sign-off; the blocking-human checkpoint will not resume without it |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- .planning/phases/07-pre-workshop-uat/UAT-RUNBOOK.md exists, references :3100 and the gate wrapper, and never instructs building while :3000 is live.
- UAT-REPORT.md contains a filled Owner Perceptual Checklist for all four pages + GPU observation + blocker-bar definition, with the 07-01 engineering tables intact.
- The blocking-human checkpoint resumes only on "approved on the projector" with the report ALL-GREEN and every blocker cleared.
- A Combined Sign-Off section records the DoD: engineering ALL-GREEN AND owner projector approval.
</verification>

<success_criteria>
The owner can build/serve the production UAT copy on :3100 without taking :3000 down, read the ALL-GREEN engineering report, and run the live projector pass on his real secondary display at projector-reduced brightness. Phase 7 is DONE when the engineering report is ALL-GREEN AND the owner records "approved on the projector" with every room-breaking blocker cleared; cosmetic nits may remain (NOTED). Promotion to :3000 happens only after sign-off.
</success_criteria>

<output>
Create `.planning/phases/07-pre-workshop-uat/07-02-SUMMARY.md` when done.
</output>
