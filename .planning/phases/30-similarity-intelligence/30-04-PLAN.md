---
phase: 30-similarity-intelligence
plan: 04
type: execute
wave: 4
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - lib/acc/embedding/similarityEdgeSet.ts
  - server/routers/acc-dc-graph.ts
autonomous: true
requirements: [SIM-01, SIM-02, SIM-03]

must_haves:
  truths:
    - "A live pipeline run against the DB wrote the v2 payload for all ~22k nodes under a fresh embeddingRunId; run output (twin distribution, dup rate, trust gate) recorded"
    - "Stale-shape fallback exercised LIVE: the panel rendered old bare-array rows gracefully (matches, no why chips, no twin chip, no console error) BEFORE the pipeline rerun, and the full v2 experience after"
    - "Post-collapse inter/intra edge score distributions measured and recorded; interReserveFrac=0.4 and the 18k budget kept or re-tuned WITH those numbers (decision + evidence in the SUMMARY, not vibes)"
    - "Panel eyeballed on the live graph via the isolated :3100 prod harness: distinct matches + twin chip + why labels render, no console errors; similarity web renders from de-twinned edges"
    - "Full gate set green: python pytest, focused vitest suites, npx tsc --noEmit, repo-map check, PERF-02 GraphCanvas.test.ts frozen-handle invariant"
  artifacts:
    - "30-04-SUMMARY.md carrying the measured distributions + re-tune decision"
  key_links:
    - from: "server/routers/acc-dc-graph.ts"
      to: "lib/acc/embedding/similarityEdgeSet.ts"
      via: "final (possibly re-tuned) selection call"
      pattern: "dedupeAndSelectClusterAware"
---

<objective>
SIM-03 evidence half + phase-wide live verification: run the pipeline against
the live DB, measure the post-collapse edge score distributions, make the
evidence-based keep/re-tune decision for `interReserveFrac`/budget, and verify
the whole SIM stack live (including the stale-shape fallback, exercised for
real against pre-rerun rows).
</objective>

<context>
@.planning/phases/30-similarity-intelligence/30-CONTEXT.md
@lib/acc/embedding/similarityEdgeSet.ts
@.claude/skills/lecg-dashboard/references/deploy-sequence.md
</context>

## Tasks — ORDER MATTERS (stale-shape exercise must precede the rerun)

1. **Build the :3100 harness** (never touch :3000 for a code check):
   `npx tsc --noEmit`, then the established isolated prod build + `next start`
   on :3100 (webpack prod build to isolated dist — dev servers are broken,
   standing memory).

2. **Stale-shape fallback, live**: with the DB still holding OLD bare-array
   neighbors rows, open `/users/spatial-graph` on :3100, click a node: panel
   must render the plain match list (no why chips, no twin chip), zero console
   errors. Screenshot/console evidence into the summary.

3. **Live pipeline run**: `npx tsx scripts/build-instance-features.ts` then
   `python scripts/compute_instance_embeddings.py` (env per repo convention).
   Record: fresh embeddingRunId, node count, twin-group distribution line,
   dup-rate line, trust-gate line. Non-fatal wiring in dc-daily-ingest.cjs is
   untouched — no action needed, just don't break it.

4. **Measure post-collapse edge distributions**: one-off scratchpad script
   (scratchpad dir, NOT committed) that loads all neighbors rows + clusters,
   maps to RawNodeNeighbors via normalizeNeighborsPayload, runs dedupeEdges
   logic, and prints: total unique edges, inter vs intra counts, score
   quantiles (min/p25/p50/p75/max) per class, and how many inter edges survive
   the current 0.4 reserve at the 18k budget vs plain top-N.
   - Decision rule: if plain top-N no longer starves inter edges (twin
     saturation was the premise), consider lowering the reserve or keeping it —
     either way justify from the measured quantiles. If the edge total dropped
     far below 18k, consider whether the cap still binds. Record numbers +
     decision; apply any constant change in `similarityEdgeSet.ts` /
     `acc-dc-graph.ts` and update `similarityEdgeSet.test.ts` if semantics
     change (default: no code change — evidence first).

5. **Full v2 live eyeball** (:3100, post-rerun): click nodes across clusters —
   distinct matches + twin chip + expandable twin list + why chips with
   coverage suffixes render; similarity web draws (de-twinned); no console
   errors; morph/slider smoke (drag a Group-by slider — web fades, no crash).

6. **Full gates**:
   - `python -m pytest scripts/test_compute_instance_embeddings.py -q`
   - `npx vitest run lib/acc/embedding "app/(dashboard)/users/access-analysis/whySimilar.test.ts" "app/(dashboard)/users/access-analysis/NeighborMatchesPanel.test.tsx" "app/(dashboard)/users/access-analysis/similarityWeb.test.ts" "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"` (adjust paths to the real PERF-02 test location — verify with glob first)
   - `npx tsc --noEmit`
   - `node scripts/repo-map/check.cjs`
   - Report pre-existing failures (usePredicateEngine ×3, e2e drift) separately.

7. Stop the :3100 harness.

## Verification

Steps 2–6 ARE the verification. Evidence (run ids, distribution numbers, gate
outputs) goes verbatim into `30-04-SUMMARY.md`.

## Success criteria

- All truths hold; the re-tune decision is recorded with measured numbers;
  phase is ready for completion audit + `/lecg-ship` (autoDeploy true).
