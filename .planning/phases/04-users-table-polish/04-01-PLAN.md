---
phase: 04-users-table-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/directoryTableRow.ts
  - app/(dashboard)/users/directoryTableRow.test.ts
  - app/(dashboard)/users/DirectoryTableColumns.tsx
  - app/(dashboard)/users/DirectoryTableColumns.test.tsx
  - app/(dashboard)/users/PeekPanel.tsx
  - app/(dashboard)/users/PeekPanel.test.tsx
autonomous: true
requirements: [USR-02, INT-03, PERF-04]
must_haves:
  truths:
    - "buildDirectoryRows derives lastActivity as max(project.lastActivity), NOT lastSignIn"
    - "A user with all-null project.lastActivity yields lastActivity: null (renders '— No data', muted)"
    - "Office column derives from project names via officeCodeFor() without the mty-allowlist"
    - "Multiple roles render as primary role + '+N' badge"
    - "Dormant flag is true only when lastActivity is non-null AND older than 90 days"
    - "The five columns are: name (pinned, sortable), role, office (sortable), lastActive (sortable), projects (sortable)"
    - "PeekPanel renders only cheap in-memory fields (avatar, title, office, last active, project/role/module counts) — no tRPC query"
  artifacts:
    - path: "app/(dashboard)/users/directoryTableRow.ts"
      provides: "DirectoryRow type + buildDirectoryRows(people, accSummaryMap)"
      contains: "buildDirectoryRows"
      min_lines: 60
    - path: "app/(dashboard)/users/DirectoryTableColumns.tsx"
      provides: "USERS_COLUMNS: ColumnDef<DirectoryRow>[] (5 columns) + cell renderers"
      contains: "USERS_COLUMNS"
    - path: "app/(dashboard)/users/PeekPanel.tsx"
      provides: "renderExpanded slot content — inline peek with 'See full profile' affordance"
      contains: "PeekPanel"
    - path: "app/(dashboard)/users/directoryTableRow.test.ts"
      provides: "Unit coverage for the derivation rules"
    - path: "app/(dashboard)/users/DirectoryTableColumns.test.tsx"
      provides: "Cell render coverage incl. '— No data' for null lastActivity"
  key_links:
    - from: "app/(dashboard)/users/directoryTableRow.ts"
      to: "app/(dashboard)/access-analysis/projectGroups.ts"
      via: "imports officeCodeFor + officeLabel for office derivation"
      pattern: "officeCodeFor|officeLabel"
    - from: "app/(dashboard)/users/DirectoryTableColumns.tsx"
      to: "app/(dashboard)/users/directoryTableRow.ts"
      via: "ColumnDef<DirectoryRow> typed against the row type"
      pattern: "DirectoryRow"
    - from: "app/(dashboard)/users/DirectoryTableColumns.tsx"
      to: "app/(dashboard)/users/ProfileAvatar.tsx"
      via: "NameCell reuses ProfileAvatar (photo + initials fallback)"
      pattern: "ProfileAvatar"
---

<objective>
Build the pure data layer for the premium /users DataTable: the flat `DirectoryRow` type, the `buildDirectoryRows()` derivation function, the five typed TanStack `ColumnDef<DirectoryRow>` definitions with cell renderers, and the inline `PeekPanel` (renderExpanded content). This is the foundation that Plan 03 wires into the shell.

Purpose: Isolate all derivation logic (last-activity, office, dormant, role badge) and column/cell rendering into pure, unit-testable modules so the shell wiring in Plan 03 stays thin and the derivation rules (which are the riskiest correctness surface — see RESEARCH Pitfall 1) are pinned by tests BEFORE they are consumed.

Output: `directoryTableRow.ts` (+ test), `DirectoryTableColumns.tsx` (+ test), `PeekPanel.tsx` (+ test). All pure / presentational — zero tRPC, zero new fetch. Implements USR-02 (5-column premium table), INT-03 (peek content), PERF-04 (peek reads only cheap in-memory data).
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-users-table-polish/04-CONTEXT.md
@.planning/phases/04-users-table-polish/04-RESEARCH.md

# Interface contracts the executor must wire against (read these — do not re-derive):
@app/(dashboard)/users/useMergedAccUsers.ts        # OrgPerson interface (resourceName, displayName, email, photoUrl, department, jobTitle, phoneNumber, costCenter)
@lib/acc/acc-types.ts                              # BulkAccUser (allRoles, allModules, projects, photoUrl) + BulkAccProject (lastActivity at line 29 — the P5-C field)
@app/(dashboard)/access-analysis/projectGroups.ts  # officeCodeFor(option, mtyIds?) + officeLabel(code) — office derivation source of truth
@app/(dashboard)/users/ProfileAvatar.tsx           # ProfileAvatar (photo + getInitials fallback) — reuse in NameCell
@components/ui/DataTable.tsx                        # DataTableProps<T>: columns is ColumnDef<T>[]; renderExpanded receives Row<T>; defaultSort, pinnedColumn props
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — DirectoryRow type + buildDirectoryRows() derivation</name>
  <files>app/(dashboard)/users/directoryTableRow.ts, app/(dashboard)/users/directoryTableRow.test.ts</files>
  <behavior>
    Write directoryTableRow.test.ts FIRST (RED), then implement directoryTableRow.ts to GREEN. Test cases:
    - lastActivity = the maximum (latest ISO) of project.lastActivity across all of the user's projects. A user whose projects have lastActivity ["2026-01-01...","2026-06-01...",null] yields "2026-06-01...".
    - lastActivity = null when EVERY project.lastActivity is null/absent (this is the "— No data" case). Assert it does NOT fall back to lastSignIn.
    - lastActivity ignores BulkAccUser.lastSignIn entirely (RESEARCH Pitfall 1): a user with recent lastSignIn but all-null project.lastActivity yields lastActivity: null.
    - primaryRole = allRoles[0] (or null when allRoles empty); extraRoleCount = max(0, allRoles.length - 1).
    - officeCode/officeLabel derived via officeCodeFor({id, name}) (NO mtyIds arg — name-token path per RESEARCH Office section; mty-allowlist.json does not exist) taking the MODE (most common code) across the user's projects; null when the user has no projects.
    - projectCount = the user's projects.length (ALL memberships per CONTEXT — not active-only, not admin-only).
    - isDormant = lastActivity !== null AND (now - lastActivity) > 90 days. A user with lastActivity === null is NOT dormant (status unknown — no dot). Use a 90-day threshold constant.
    - Falls back gracefully when accSummaryMap has no entry for a person.email.toLowerCase() (accUser null; counts 0; office null; lastActivity null).
  </behavior>
  <action>Create `DirectoryRow` interface exactly per RESEARCH Pattern 1 (fields: email, resourceName, displayName, photoUrl, primaryRole, extraRoleCount, officeCode, officeLabel, lastActivity, projectCount, isDormant, jobTitle, department, accUser). Implement `buildDirectoryRows(people: OrgPerson[], accSummaryMap: Map<string, BulkAccUser>): DirectoryRow[]`. For each OrgPerson, look up accUser via `accSummaryMap.get(person.email.toLowerCase())`. Derive lastActivity by mapping `accUser.projects` to their lastActivity, filtering nulls, sorting, taking the latest (`.at(-1) ?? null`). Derive office by computing officeCodeFor for each project name and selecting the most frequent code (ties → first by project order); map to officeLabel. Export a `DORMANT_THRESHOLD_DAYS = 90` constant (Claude's-discretion per CONTEXT) and compute isDormant from it. Key the row by resourceName for stable TanStack row ids. Pure module — NO React, NO tRPC imports.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/directoryTableRow"</automated>
  </verify>
  <done>directoryTableRow.test.ts is green; buildDirectoryRows derives lastActivity from project.lastActivity (never lastSignIn), office via officeCodeFor name-token path, "+N" count, and the 90-day dormant flag; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN — five column definitions + cell renderers</name>
  <files>app/(dashboard)/users/DirectoryTableColumns.tsx, app/(dashboard)/users/DirectoryTableColumns.test.tsx</files>
  <behavior>
    Write DirectoryTableColumns.test.tsx FIRST (RED), then implement to GREEN. Test cases (render each cell in isolation with a fabricated DirectoryRow):
    - USERS_COLUMNS has exactly 5 entries with ids: name, role, office, lastActive, projects.
    - name column: enableSorting true; cell renders displayName + ProfileAvatar (assert initials fallback text when photoUrl null). office, lastActive, projects: enableSorting true. role: enableSorting false (multi-role badge is not meaningfully sortable).
    - LastActiveCell with a null iso renders muted "— No data" (assert the literal text "No data" appears).
    - LastActiveCell with a real ISO renders relative text via formatDistanceToNowStrict (assert it contains "ago"); the exact timestamp is exposed on hover (title attribute carries the ISO/exact date).
    - RoleCell with extraRoleCount 0 renders only the primary role; with extraRoleCount 2 renders primary role + a "+2" badge.
    - NameCell renders a status dot element when isDormant true and omits it when isDormant false; renders NO dot when lastActivity is null (status unknown).
  </behavior>
  <action>Implement `USERS_COLUMNS: ColumnDef<DirectoryRow>[]` using `createColumnHelper<DirectoryRow>()` per RESEARCH Pattern 1. Column sizes/headers per the pattern (Name 240 pinned, Role 180, Office 120, Last active 140, Projects 90). Build small cell components in this file: NameCell (ProfileAvatar from ProfileAvatar.tsx + displayName + subtle status dot when isDormant; dot uses CSS token color, not hardcoded hex — e.g. amber for dormant via a token-driven class), RoleCell (primary + "+N" Badge), LastActiveCell (date-fns `formatDistanceToNowStrict` relative text with `title` = exact date; muted "— No data" span when iso is null). Office and Projects use plain accessor text (officeLabel, projectCount). Import `formatDistanceToNowStrict` from "date-fns" (already transitive — consistent with PersonRow.tsx). Do NOT add any tRPC query inside any cell (RESEARCH Pitfall 2). Use the typed `ColumnDef<DirectoryRow, string>[]` value-type param where createColumnHelper requires it (STATE decision: explicit value-type param needed with TanStack v8 strict generics).</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/DirectoryTableColumns"</automated>
  </verify>
  <done>DirectoryTableColumns.test.tsx green; 5 columns with correct ids/sortability; "— No data" renders for null lastActivity; "+N" badge for multi-role; status dot only when isDormant; `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PeekPanel inline expand content (cheap data only)</name>
  <files>app/(dashboard)/users/PeekPanel.tsx, app/(dashboard)/users/PeekPanel.test.tsx</files>
  <behavior>
    - PeekPanel({ row, onOpenProfile }) renders a compact snapshot: avatar, displayName, jobTitle, officeLabel, last-active relative text, and three key counts (projects / roles / modules) sourced ONLY from the DirectoryRow + its accUser (in-memory).
    - Renders a "See full profile →" control that calls onOpenProfile when clicked (INT-03 peek→panel affordance per CONTEXT).
    - Renders NO tRPC query and triggers no fetch (PERF-04) — assert by rendering without any trpc mock and confirming it does not throw.
    - Module/role counts come from row.accUser.allModules.length / row.accUser.allRoles.length (0 when accUser null).
  </behavior>
  <action>Create `PeekPanel({ row, onOpenProfile }: { row: DirectoryRow; onOpenProfile: () => void })`. Lay out a calm, low-noise mini snapshot using CSS tokens / existing utility classes (PremiumSurface variant="inset" is acceptable for the peek container). Show avatar (ProfileAvatar), name, jobTitle, office, last-active, and a small row of counts: Projects {projectCount} · Roles {allRoles.length} · Modules {allModules.length}. Add a right-aligned "See full profile →" button wired to onOpenProfile. Strictly read-only from props — no useQuery, no useUtils. This is the renderExpanded slot Plan 03 will pass to DataTable.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/PeekPanel"</automated>
  </verify>
  <done>PeekPanel renders snapshot + counts from in-memory row data, fires onOpenProfile on the "See full profile" control, performs zero fetches; test green; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run "app/(dashboard)/users/directoryTableRow" "app/(dashboard)/users/DirectoryTableColumns" "app/(dashboard)/users/PeekPanel"` all green.
- `npx tsc --noEmit` exits 0 (whole-tree typecheck — HARD repo rule; test files are in scope).
- Scope boundary: `git diff --name-only` touches ZERO files under `app/(dashboard)/users/access-analysis/` and no spatial-graph files.
- `grep -n "lastSignIn" app/(dashboard)/users/directoryTableRow.ts` returns nothing (last-active must NOT read lastSignIn — RESEARCH Pitfall 1).
- No new tRPC import in any of the three new source files: `grep -rn "trpc" app/(dashboard)/users/directoryTableRow.ts app/(dashboard)/users/DirectoryTableColumns.tsx app/(dashboard)/users/PeekPanel.tsx` returns nothing.
</verification>

<success_criteria>
- DirectoryRow + buildDirectoryRows correctly derive last-activity (max project.lastActivity), office (name-token mode), "+N" roles, and 90-day dormant flag.
- Five typed columns render with the locked semantics; "— No data" shows for null activity.
- PeekPanel supplies the INT-03 inline peek from cheap in-memory data with a "See full profile" affordance.
- Full /users test subset green; tsc 0; scope boundary respected.
</success_criteria>

<output>
Create `.planning/phases/04-users-table-polish/04-01-SUMMARY.md` when done. Stage commits by EXPLICIT path only (shared-WIP branch — never `git add -A/.`); verify `git diff --cached --name-only` before each commit.
</output>
