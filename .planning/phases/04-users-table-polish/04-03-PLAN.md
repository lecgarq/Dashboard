---
phase: 04-users-table-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - app/(dashboard)/users/UsersDirectoryClient.tsx
  - app/(dashboard)/users/loading.tsx
  - app/(dashboard)/users/UsersTableSkeleton.tsx
  - app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx
autonomous: true
requirements: [USR-02, INT-03, PERF-01, PERF-04, VIS-03]
must_haves:
  truths:
    - "The directory renders via DataTable<DirectoryRow> — pinned Name column, sortable, density toggle, A→Z default sort"
    - "Row-click opens the shared profile in DrillSheet (right-edge slide-in); the table stays visible"
    - "Row-expand (chevron) shows PeekPanel inline; 'See full profile' in the peek also opens the DrillSheet"
    - "PersonDetailModal (centered Dialog) is removed from the shell in the same commit DrillSheet is added (no two-panel overlap)"
    - "A table-shaped shimmer skeleton renders while loading (loading.tsx route boundary + inline isLoading)"
    - "Whole-page content fades in once on mount (motion facade fadeIn/useSafeVariants); instant on sort/filter/density/panel-open"
    - "An inline error state ('Couldn't load the directory' + Retry) renders when the data load fails"
    - "Filtered-empty shows 'No one matches those filters' + Clear filters wired to DataTable.onClearFilters → store.clearAllFilters"
    - "bulkUsers is fetched once with BULK_USERS_LEAN_INPUT — no second bulkUsers query is added (PERF-03/PERF-04 hold)"
    - "The grid/list viewMode toggle UI is removed; the viewMode store field is left inert (no store/test churn)"
    - "The existing activityEmail Sheet (activity-sort side panel) remains functional"
  artifacts:
    - path: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      provides: "DataTable-driven shell with DrillSheet, skeleton, entrance fade, error+retry, filtered-empty"
      contains: "DataTable"
    - path: "app/(dashboard)/users/UsersTableSkeleton.tsx"
      provides: "Table-shaped shimmer skeleton (5-column header + 8 rows)"
      contains: "UsersTableSkeleton"
    - path: "app/(dashboard)/users/loading.tsx"
      provides: "Route-level skeleton swapped from card-grid to table-shaped"
    - path: "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx"
      provides: "New cases: row-click→DrillSheet, row-expand→PeekPanel, PERF-04 single-fetch assertion"
  key_links:
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "components/ui/DataTable.tsx"
      via: "DataTable<DirectoryRow> with data=rows, columns=USERS_COLUMNS, onRowClick, renderExpanded, pinnedColumn='name', defaultSort name asc"
      pattern: "DataTable"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/directoryTableRow.ts"
      via: "buildDirectoryRows(displayRows, accSummaryMap) → DataTable data"
      pattern: "buildDirectoryRows"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "components/ui/DrillSheet.tsx"
      via: "DrillSheet open={!!selectedEmail} wrapping UserProfilePanel with person + user props"
      pattern: "DrillSheet"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/useUsersDirectoryData.ts"
      via: "imports BULK_USERS_LEAN_INPUT path indirectly via the data hook (no new bulkUsers call)"
      pattern: "useUsersDirectoryData"
---

<objective>
Wire the premium DataTable into `UsersDirectoryClient`: feed it `DirectoryRow[]` (from Plan 01) + the five columns, route row-click into a `DrillSheet` carrying the extended `UserProfilePanel` (from Plan 02), put `PeekPanel` in the renderExpanded slot, replace the card/grid + list rendering paths, swap the loading skeleton to a table-shaped shimmer, add the whole-page entrance fade, and add the inline error+retry and filtered-empty states. This is the convergence plan where USR-02, INT-03, PERF-01, and VIS-03 all land.

Purpose: Plans 01 and 02 built the pure pieces; this plan assembles them into the live shell while preserving the Phase-2 PERF-03/PERF-04 single-fetch seam and the activity-sort side panel. The pre-sorted-data seam (RESEARCH Open Q3) keeps DataTable receiving already-filtered/sorted rows from `useDirectoryRows`, so DataTable's own column sort stays independent and the activity-sort path still works.

Output: a DataTable-driven /users directory with slide-in detail, inline peek, table skeleton, calm entrance fade, error/empty states, and updated integration tests. Resolves Open Q2 (retire viewMode toggle UI, leave store field inert) and Open Q3 (pre-sort seam).
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-users-table-polish/04-CONTEXT.md
@.planning/phases/04-users-table-polish/04-RESEARCH.md

# Prior-plan artifacts this plan consumes:
@app/(dashboard)/users/directoryTableRow.ts        # (Plan 01) DirectoryRow + buildDirectoryRows
@app/(dashboard)/users/DirectoryTableColumns.tsx   # (Plan 01) USERS_COLUMNS
@app/(dashboard)/users/PeekPanel.tsx               # (Plan 01) renderExpanded content
# (Plan 02) UserProfilePanel now accepts optional person?: OrgPerson

# Interface contracts (read — do not re-derive):
@components/ui/DataTable.tsx                        # DataTableProps<T>: data, columns, onRowClick(Row<T>), renderExpanded(Row<T>), pinnedColumn, defaultSort (SortingState), hasActiveFilter, onClearFilters, emptyMessage, filteredEmptyMessage. row.original is the DirectoryRow.
@components/ui/DrillSheet.tsx                        # DrillSheetProps {open, onClose, title?, description?, children}
@components/ui/motion.ts                             # motion, fadeIn, useSafeVariants — import motion ONLY from here, never framer-motion
@app/(dashboard)/users/UsersDirectoryClient.tsx     # CURRENT shell topology: store reads, useUsersDirectoryData, useDirectoryRows, renderPeople (grid/list), PersonDetailModal, activityEmail Sheet, DirectorySkeleton const
@app/(dashboard)/users/useDirectoryRows.ts          # returns {filtered, visibleFiltered, visibleGroups, ...} pre-filtered/sorted rows (OrgPerson[])
@app/(dashboard)/users/useUsersDirectoryStore.ts    # selectedEmail/setSelectedEmail, activityEmail, clearAllFilters, viewMode (leave inert)
@app/(dashboard)/users/loading.tsx                  # current card-grid-ish route skeleton to swap
@app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx  # mock harness (bulkUsersQuerySpy via vi.hoisted; next/dynamic mocked to a div; data-testid selectors)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Table-shaped skeleton + wire DataTable into the shell (replace grid/list render)</name>
  <files>app/(dashboard)/users/UsersTableSkeleton.tsx, app/(dashboard)/users/UsersDirectoryClient.tsx, app/(dashboard)/users/loading.tsx</files>
  <behavior>
    - UsersTableSkeleton renders a 5-column header row + 8 shimmer data rows matching the column widths (per RESEARCH Pattern 5), using the shadcn Skeleton primitive.
    - UsersDirectoryClient builds rows via buildDirectoryRows from the pre-filtered/sorted people list (useDirectoryRows output) + accSummaryMap, and renders DataTable<DirectoryRow> with columns=USERS_COLUMNS, pinnedColumn="name", defaultSort=[{id:"name", desc:false}], data=rows.
    - The grid-card and list rendering paths (renderPeople, PersonCard, the viewMode toggle buttons) are removed from the shell render; the viewMode store field is left in the store (inert) to avoid test churn (Open Q2).
    - While isLoading, the inline body shows UsersTableSkeleton (not the old card-grid DirectorySkeleton).
    - Filtered-empty: DataTable receives hasActiveFilter (from useDirectoryRows hasActiveFilters || search) and onClearFilters wired to store.clearAllFilters; filteredEmptyMessage "No one matches those filters"; emptyMessage for the true-empty case.
    - loading.tsx (route boundary) renders the same table-shaped skeleton shape so the ~200ms Suspense fallback matches the real layout (PERF-01).
  </behavior>
  <action>Create `UsersTableSkeleton` per RESEARCH Pattern 5 (Skeleton header bar with 5 widths + 8 rows each with a round avatar skeleton + 5 cell skeletons). Import it into both loading.tsx (replace the current card-ish skeleton) and UsersDirectoryClient.tsx. In the shell: import buildDirectoryRows + USERS_COLUMNS + PeekPanel + DataTable. Compute `rows = useMemo(() => buildDirectoryRows(visibleFiltered ?? filtered, accSummaryMap), [...])` — pass the ALREADY-filtered/sorted list from useDirectoryRows so the pre-sort seam (Open Q3) holds; DataTable's own column sort operates on top. Replace the `renderPeople`/grid/list/CollapsibleGroup body with `<DataTable data={rows} columns={USERS_COLUMNS} pinnedColumn="name" defaultSort={[{id:"name", desc:false}]} renderExpanded={(r) => <PeekPanel row={r.original} onOpenProfile={() => setSelectedEmail(r.original.email)} />} onRowClick={(r) => setSelectedEmail(r.original.email)} hasActiveFilter={hasActiveFilters || !!search} onClearFilters={clearAllFilters} filteredEmptyMessage="No one matches those filters" emptyMessage="No people found in your organization directory." />`. Remove the grid/list viewMode toggle buttons + LayoutGrid/List imports; leave the `groupBy` Select if desired or remove if it no longer applies to a flat table (prefer removing grouping UI since DataTable is a single flat sorted list — but keep groupBy store field inert). Keep the DataCoverageStrip, DirectoryFilterBar, and the activityEmail Sheet untouched. Do NOT add any new tRPC query (PERF-04). Do NOT alter useUsersDirectoryData or the BULK_USERS_LEAN_INPUT constant.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration"</automated>
  </verify>
  <done>DataTable renders the directory rows with pinned Name + A→Z sort; table-shaped skeleton shows while loading; viewMode toggle UI removed (store field inert); filtered-empty wired to clearAllFilters; existing integration cases stay green (fixtures updated as needed); `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap PersonDetailModal → DrillSheet + entrance fade + error/retry state</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <behavior>
    - PersonDetailModal is REMOVED from the shell JSX in the same change that adds DrillSheet (no two-panel overlap — RESEARCH Pitfall 5).
    - DrillSheet open={!!selectedEmail} onClose={() => setSelectedEmail(null)} renders UserProfilePanel with the resolved person (people.find by email) AND user (mergedAccUsers.find by email) AND variant="dialog" — reusing the Plan-02 person chrome inside the slide-in panel.
    - The top-level content wrapper uses motion.div with fadeIn + useSafeVariants (motion facade), firing once on mount. Replace the existing `animate-fade-up` CSS class wrapper. Motion does NOT re-fire on sort/filter/density/panel-open (initial/animate on mount only — VIS-05/VIS-03).
    - VIS-03 reconciliation: the unified whole-page fade is the visible behavior; a barely-perceptible row under-stagger is acceptable but the calm unified fade is the locked preference (CONTEXT). Do not add a pronounced cascade.
    - Error state: when the data hook reports a load error, render an inline "Couldn't load the directory" message + a Retry button (calls a refetch/utils invalidation or window-reload fallback), with page chrome intact (CONTEXT error-state lock). This replaces/augments the existing amber "Could not load organization directory" banner styling to match the locked copy + Retry affordance.
  </behavior>
  <action>Import { motion, fadeIn, useSafeVariants } from "@/components/ui/motion" and { DrillSheet } from "@/components/ui/DrillSheet". Call `const safeFade = useSafeVariants(fadeIn)` ONCE at component scope (Rules of Hooks). Wrap the outer container: replace `<div className="... animate-fade-up">` with `<motion.div initial={safeFade.hidden} animate={safeFade.visible} className="mx-auto max-w-[1600px] p-6 space-y-4">`. Remove the `<PersonDetailModal .../>` block and its import; add `<DrillSheet open={!!selectedEmail} onClose={() => setSelectedEmail(null)}>{selectedEmail && <UserProfilePanel person={people.find(p => p.email.toLowerCase() === selectedEmail.toLowerCase()) ?? undefined} user={mergedAccUsers.find(u => u.email.toLowerCase() === selectedEmail.toLowerCase()) ?? null} email={selectedEmail} variant="dialog" />}</DrillSheet>`. Keep UserProfilePanel as a dynamic ssr:false import (matches the existing PersonDetailModal pattern + integration-test next/dynamic mock). For the error state, render an inline block with the exact heading copy "Couldn't load the directory" and a Retry button wired to re-run the directory queries (use trpc utils invalidate for the relevant query keys, or a `window.location.reload()` fallback if a clean refetch handle is not exposed by the hook). Keep the activityEmail Sheet and DataCoverageStrip exactly as-is. Use only CSS tokens for any new styling.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration"</automated>
  </verify>
  <done>Row-click opens DrillSheet with UserProfilePanel (person + user); PersonDetailModal removed (no Dialog + Sheet double-open); entrance is a single motion-facade fade on mount; inline "Couldn't load the directory" + Retry renders on load error; tsc 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test cases — row-click→DrillSheet, expand→PeekPanel, PERF-04 single-fetch</name>
  <files>app/(dashboard)/users/__tests__/UsersDirectoryClient.integration.test.tsx</files>
  <behavior>
    - New case: clicking a row's data cell (data-cell) calls setSelectedEmail and renders the DrillSheet content (the next/dynamic-mocked panel div carrying the email) — assert via the dynamic-panel data-email or the store selectedEmail.
    - New case: clicking the row expand chevron (data-expand) reveals the PeekPanel content (avatar/name/counts) inline.
    - New case (PERF-04/PERF-03): bulkUsersQuerySpy is called with BULK_USERS_LEAN_INPUT ({leanProjects:true}) and the directory render does not trigger a second bulkUsers call — assert bulkUsersQuerySpy call count and the argument shape.
    - Existing golden-path cases (search/filter/clear) stay green; fixtures updated to whatever the DataTable wiring needs (e.g. the mocked rows must produce visible DataTable rows). The mock fixtures already include projects[].lastActivity? — add lastActivity to at least one mocked project so a relative time and a "— No data" case both appear.
  </behavior>
  <action>Extend the existing integration test. Reuse the vi.hoisted bulkUsersQuerySpy + the next/dynamic mock (which renders a div with data-email — this stands in for UserProfilePanel inside DrillSheet). Add the three cases above. For the PERF-04 assertion, import BULK_USERS_LEAN_INPUT (or assert the call arg equals { leanProjects: true }) and assert `bulkUsersQuerySpy` was called the expected number of times for one render (no extra call). Add `lastActivity` values into the mocked projects so LastActiveCell renders both a relative time and a "— No data" row. Use the DataTable test selectors already in the primitive: `data-cell`, `data-expand`, `data-testid="density-toggle"`, `data-testid="clear-filters-btn"`. Keep the IntersectionObserver/ResizeObserver/scrollTo stubs. Per the HARD repo rule, any prop-shape drift in the mocks is fixed here in the same commit as the wiring.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration"</automated>
  </verify>
  <done>Row-click→DrillSheet, expand→PeekPanel, and the PERF-04 single-fetch assertion all pass; the full golden-path suite stays green; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run "app/(dashboard)/users"` green (integration + unit subset).
- `npx vitest run` — full suite holds at or above the Phase-2 baseline of 2015 pass (STATE decision).
- `npx tsc --noEmit` exits 0 (whole-tree — HARD repo rule; mocks/fixtures updated in the same commit).
- `npm run repo-map:check` passes — confirms NO new unconditional fetch/effect was introduced (PERF-04 ratchet, RESEARCH PERF-04 seam).
- Scope boundary: `git diff --name-only` touches ZERO files under `app/(dashboard)/users/access-analysis/` and no spatial-graph files.
- No two-panel overlap: `grep -n "PersonDetailModal" app/(dashboard)/users/UsersDirectoryClient.tsx` returns nothing (Dialog removed).
- Motion facade only: `grep -n "from \"framer-motion\"" app/(dashboard)/users/UsersDirectoryClient.tsx` returns nothing (must import motion from @/components/ui/motion).
</verification>

<success_criteria>
- /users renders as a premium DataTable with pinned Name, A→Z sort, density toggle, row-click → DrillSheet, row-expand → PeekPanel (USR-02, INT-03).
- Table-shaped skeleton within ~200ms (PERF-01); calm whole-page entrance fade once per load (VIS-03).
- No new fetch; bulkUsers called once with the lean input (PERF-04 verified by repo-map:check + integration assertion).
- Inline error+retry and filtered-empty states present; activity Sheet still works; viewMode toggle retired (Open Q2); pre-sort seam holds (Open Q3).
- Full suite green at baseline; tsc 0; scope boundary respected.
</success_criteria>

<output>
Create `.planning/phases/04-users-table-polish/04-03-SUMMARY.md` when done. Stage commits by EXPLICIT path only (shared-WIP branch — never `git add -A/.`); verify `git diff --cached --name-only` before each commit.
</output>
