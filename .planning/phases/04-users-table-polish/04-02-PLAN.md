---
phase: 04-users-table-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/users/UserProfilePanel.tsx
  - app/(dashboard)/users/UserProfilePanel.test.tsx
autonomous: true
requirements: [INT-03, USR-02]
must_haves:
  truths:
    - "UserProfilePanel accepts an optional person?: OrgPerson prop"
    - "When person is supplied, the panel renders the person header chrome (avatar banner, name, job title, contact/info rows) ABOVE the existing ACC profile body"
    - "When person is omitted, the panel behaves exactly as before (rail + dialog variants unchanged) — existing tests still pass"
    - "The person header chrome is gated behind the variant=dialog usage and does not appear in variant=rail"
    - "No new tRPC fetch is introduced — info rows read only from the in-memory OrgPerson"
  artifacts:
    - path: "app/(dashboard)/users/UserProfilePanel.tsx"
      provides: "UserProfilePanel with optional person prop + header chrome (single source of truth for the drill panel body)"
      contains: "person?: OrgPerson"
    - path: "app/(dashboard)/users/UserProfilePanel.test.tsx"
      provides: "Coverage that person chrome renders when supplied and is absent otherwise; existing cases still green"
  key_links:
    - from: "app/(dashboard)/users/UserProfilePanel.tsx"
      to: "app/(dashboard)/users/directoryUtils.ts"
      via: "imports OrgPerson type for the new person prop"
      pattern: "OrgPerson"
---

<objective>
Resolve RESEARCH Open Question 1 by extending the shared `UserProfilePanel` with an optional `person?: OrgPerson` prop that renders the person header chrome (avatar banner, name, job title, contact/info rows) inline above the existing ACC profile body. This makes `UserProfilePanel` the single source of truth for the drill-panel content so Plan 03 can drop it straight into `DrillSheet` with no `/users`-specific wrapper — completing the CONTEXT-locked centered-modal → slide-in-Sheet direction.

Purpose: The Phase-2 decision left `PersonDetailModal` as a centered Dialog whose outer chrome (gradient banner avatar, info rows, quick actions) is NOT in `UserProfilePanel`. CONTEXT locks the slide-in-from-right panel and reuse of the shared `UserProfilePanel`. Rather than duplicating chrome into a `/users` wrapper, fold it into `UserProfilePanel` behind an optional prop (minimal diff, single component handles all chrome — RESEARCH Open Q1 recommendation). This plan does the additive component change; Plan 03 does the shell swap.

Output: `UserProfilePanel.tsx` gains `person?: OrgPerson` + header chrome; its test gains coverage. Backward-compatible — existing rail/dialog callers and tests are unaffected.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-users-table-polish/04-CONTEXT.md
@.planning/phases/04-users-table-polish/04-RESEARCH.md

# Interface contracts (read — do not re-derive):
@app/(dashboard)/users/UserProfilePanel.tsx        # CURRENT panel: UserProfilePanelProps {user, email, onClose?, variant?}; body var; dialog returns <div data-testid="user-detail-panel">{body}</div>
@app/(dashboard)/users/PersonDetailModal.tsx       # Source of the chrome to fold in: header banner + PersonAvatar + Badge tags + InfoRow rows (Mail/Building2/Briefcase/Phone/DollarSign). PersonAvatar/CopyButton/InfoRow live here and are exported.
@app/(dashboard)/users/directoryUtils.ts           # OrgPerson type (re-exported from useMergedAccUsers): resourceName, displayName, email, photoUrl, department, jobTitle, phoneNumber, costCenter
@app/(dashboard)/users/UserProfilePanel.test.tsx   # Existing fixtures (stub/found BulkAccUser); current cases pass {user,email,variant,onClose} — must STAY green
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional person prop + header chrome to UserProfilePanel</name>
  <files>app/(dashboard)/users/UserProfilePanel.tsx</files>
  <behavior>
    - New prop: `person?: OrgPerson` added to UserProfilePanelProps (optional — existing callers compile unchanged).
    - When `person` is supplied AND variant === "dialog": render a person header block ABOVE the ACC body — gradient banner with PersonAvatar (size lg), centered name + jobTitle, department/costCenter Badge tags, and the InfoRow contact rows (email mailto + copy, department, jobTitle, costCenter, phone tel + copy). This is the chrome currently in PersonDetailModal.
    - When `person` is omitted: byte-for-byte the current behavior — `variant="dialog"` returns `<div data-testid="user-detail-panel">{body}</div>` with no header; `variant="rail"` unchanged.
    - The person chrome reads ONLY from the in-memory OrgPerson — no tRPC fetch added.
  </behavior>
  <action>Import `OrgPerson` from "./directoryUtils" and the `PersonAvatar`, `InfoRow`-style helpers. Reuse the existing exported `PersonAvatar` from PersonDetailModal.tsx (import it) rather than re-implementing; if `InfoRow`/`CopyButton` are not exported, export them from PersonDetailModal.tsx (additive export — does not change PersonDetailModal's own rendering) OR inline equivalent minimal info rows in UserProfilePanel. Prefer importing to avoid duplication. Add `person?: OrgPerson` to `UserProfilePanelProps`. In the `variant === "dialog"` branch, when `person` is truthy, render the header chrome (banner + avatar + name/title + Badge tags + InfoRow rows) followed by `{body}` inside the existing `<div data-testid="user-detail-panel">`. Keep the Badge tags and info rows lifted verbatim from PersonDetailModal so the visual matches the prior modal. Do NOT touch the `variant === "rail"` return path. NOTE: UserProfilePanel.tsx has pre-existing uncommitted WIP on this shared branch — preserve all unrelated changes; only add the prop + dialog-chrome block.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/UserProfilePanel"</automated>
  </verify>
  <done>UserProfilePanel compiles with the optional person prop; supplying person in dialog variant renders the header chrome; omitting it preserves the prior output; rail variant untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test coverage for the person chrome (and regression of existing cases)</name>
  <files>app/(dashboard)/users/UserProfilePanel.test.tsx</files>
  <behavior>
    - New case: rendering `<UserProfilePanel user={found} email person={...} variant="dialog" />` shows the person displayName, jobTitle, and email contact row (the chrome).
    - New case: rendering without `person` (variant="dialog") shows NO person header — only the ACC body (assert the displayName-as-heading chrome is absent).
    - All existing cases (not-synced state, null user, avatar initials, cost center, all-time activity label, admin stat toggle, zero-count disable) stay green — the prop is optional and additive.
  </behavior>
  <action>Add a small OrgPerson fixture (resourceName, displayName "Ada Lovelace", email, photoUrl null, department, jobTitle, phoneNumber, costCenter). Add the two new cases above. The existing trpc mock already covers getAccProfile + getAccUserActivity + getAccUserFolderAccess; if the person chrome introduces no new tRPC call, no mock change is needed. Keep all existing fixtures and cases intact. This satisfies the HARD repo rule that prop-shape changes update test fixtures in the SAME commit.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/UserProfilePanel"</automated>
  </verify>
  <done>New person-chrome cases pass; all prior UserProfilePanel cases remain green; `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run "app/(dashboard)/users/UserProfilePanel"` green (new + existing cases).
- `npx tsc --noEmit` exits 0 (whole-tree — HARD repo rule).
- Scope boundary: `git diff --name-only` touches ZERO files under `app/(dashboard)/users/access-analysis/`.
- Surgical staging: this plan touches `UserProfilePanel.tsx` which has pre-existing WIP. Verify `git diff --cached --name-only` before committing and stage ONLY UserProfilePanel.tsx + its test (+ PersonDetailModal.tsx only if a helper export was added) by explicit path.
- `person` prop is optional: `grep -n "person?" app/(dashboard)/users/UserProfilePanel.tsx` confirms the optional marker.
</verification>

<success_criteria>
- UserProfilePanel is the single source of truth for the drill-panel body, optionally rendering person chrome.
- Backward-compatible: rail + chrome-less dialog usages unchanged; existing tests green.
- Open Question 1 resolved via the optional-prop approach (no separate wrapper).
- tsc 0; scope boundary respected; surgical staging honored.
</success_criteria>

<output>
Create `.planning/phases/04-users-table-polish/04-02-SUMMARY.md` when done. Stage commits by EXPLICIT path only (shared-WIP branch — never `git add -A/.`); verify `git diff --cached --name-only` before each commit.
</output>
