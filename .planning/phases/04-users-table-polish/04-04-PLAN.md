---
phase: 04-users-table-polish
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - package.json
  - package-lock.json
  - app/(dashboard)/users/UsersTableHeader.tsx
  - app/(dashboard)/users/UsersTableHeader.test.tsx
  - app/(dashboard)/users/HeaderParticleAccent.tsx
  - app/(dashboard)/users/UsersDirectoryClient.tsx
autonomous: false
requirements: [VIS-04, USR-02]
user_setup: []
must_haves:
  truths:
    - "The page header shows KPI glass tiles (e.g. Total users · Active 30d · Admins) whose numbers count up smoothly (~1s ease-out) once per load"
    - "Count-up fires once on mount only — never on sort/filter/density/panel-open re-render"
    - "A subtle R3F particle accent renders ONLY behind the header KPI strip (absolute, inset:0, pointer-events:none) — never over the DataTable"
    - "The R3F Canvas is dynamic ssr:false + frameloop:demand; the data table introduces NO WebGL"
    - "If @react-three/fiber install is blocked, a CSS-only pulsing radial-gradient fallback renders instead (zero GPU)"
    - "KPI values are derived from in-memory people/accSummaryMap — no new tRPC query"
  artifacts:
    - path: "app/(dashboard)/users/UsersTableHeader.tsx"
      provides: "Glass KPI strip + AnimatedNumber count-up + header particle mount"
      contains: "UsersTableHeader"
    - path: "app/(dashboard)/users/HeaderParticleAccent.tsx"
      provides: "R3F Canvas particle accent (dynamic-import target)"
      contains: "Canvas"
    - path: "app/(dashboard)/users/UsersTableHeader.test.tsx"
      provides: "KPI derivation + AnimatedNumber-reaches-target coverage"
  key_links:
    - from: "app/(dashboard)/users/UsersTableHeader.tsx"
      to: "app/(dashboard)/users/HeaderParticleAccent.tsx"
      via: "dynamic(() => import('./HeaderParticleAccent'), { ssr: false })"
      pattern: "dynamic"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "app/(dashboard)/users/UsersTableHeader.tsx"
      via: "header rendered above the DataTable; KPI counts passed from in-memory derivations"
      pattern: "UsersTableHeader"
---

<objective>
Add the premium page header: glass KPI tiles with a smooth count-up (~1s ease-out, once per load) and a subtle R3F particle accent confined to the header strip behind the KPIs. This is the only net-new dependency in the phase (`@react-three/fiber@9`) and the only WebGL on the page — strictly off the data table.

Purpose: Delivers VIS-04 (count-up KPIs) and the header half of the premium feel, with the WebGL accent placed exactly where PERF-05/VIS-06 allow (header only, ssr:false, frameloop:demand, pointer-events:none). A CSS-only fallback guarantees the page degrades gracefully if the R3F install is blocked (RESEARCH R3F Availability table).

Output: `UsersTableHeader.tsx` (+ test), `HeaderParticleAccent.tsx`, the `@react-three/fiber@9` install, and the header wired into `UsersDirectoryClient` above the DataTable.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-users-table-polish/04-CONTEXT.md
@.planning/phases/04-users-table-polish/04-RESEARCH.md

# Interface contracts (read — do not re-derive):
@components/ui/motion.ts                             # motion + facade; framer-motion animate()/useMotionValue for count-up is acceptable (RESEARCH Pattern 3)
@components/ui/PremiumSurface.tsx                    # glass variant for KPI tiles
@app/(dashboard)/users/UsersDirectoryClient.tsx     # (after Plan 03) the DataTable-driven shell; header mounts above the table, inside the motion.div fade wrapper
# RESEARCH Pattern 3 (count-up), Pattern 4 (R3F accent), Pitfall 4 (WebGL must stay header-scoped), Pitfall 6 (@react-three/fiber@9 for React 19)
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New dependency `@react-three/fiber@9` is fetched at install time |
| browser GPU → header Canvas | WebGL context created client-side (ssr:false); pure visual, no data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install @react-three/fiber@9 | mitigate | RESEARCH tagged this package [ASSUMED]; blocking-human checkpoint verifies provenance on npmjs.com BEFORE install (Task 1) |
| T-04-01 | Denial of Service | header WebGL context | mitigate | frameloop="demand" + ~200 points + GPU < 400MB; confined to header (Pitfall 4); CSS fallback if blocked |
| T-04-02 | Information Disclosure | KPI tiles | accept | KPI counts derive from already-loaded in-memory data; no new endpoint, no PII beyond what /users already shows |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify @react-three/fiber legitimacy before install</name>
  <action>BLOCKING human checkpoint — do not run npm install until approved. This verifies the legitimacy of the net-new dependency BEFORE install (T-04-SC).</action>
  <what-built>Nothing yet — this gate verifies the legitimacy of the net-new dependency BEFORE install.</what-built>
  <how-to-verify>
    `@react-three/fiber` was tagged [ASSUMED] in RESEARCH (Package Legitimacy Audit not re-run via seam). Confirm provenance before installing:
    1. Open https://www.npmjs.com/package/@react-three/fiber
    2. Confirm: published by the pmndrs org, 1M+ weekly downloads, source repo github.com/pmndrs/react-three-fiber, latest 9.x targets React 19.
    3. Confirm React 19 compatibility (the repo is on React 19 — RESEARCH Pitfall 6 requires v9, NOT v8).
    Approve to install `@react-three/fiber@9`, or instruct to use the CSS-only fallback (skip the install and Task 3's R3F path).
  </how-to-verify>
  <verify>Human confirms package provenance on npmjs.com and selects install or css-fallback path.</verify>
  <done>Human has approved the install (or chosen the CSS-only fallback); no npm install ran before this gate cleared.</done>
  <resume-signal>Type "approved" to install @react-three/fiber@9, or "css-fallback" to skip R3F and ship the CSS accent only.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: KPI glass strip + count-up AnimatedNumber (once per load)</name>
  <files>app/(dashboard)/users/UsersTableHeader.tsx, app/(dashboard)/users/UsersTableHeader.test.tsx</files>
  <behavior>
    Write the test alongside implementation. Cases:
    - UsersTableHeader({ totalUsers, active30d, admins }) renders three KPI tiles with those labels/values.
    - AnimatedNumber starts at 0 and reaches the target integer after the animation (test by advancing fake timers / flushing the rAF or framer animate, then asserting the final rendered number equals the target).
    - Count-up runs once on mount: re-rendering the header with the SAME value does not restart the animation from 0 (assert the displayed value stays at target on a parent re-render).
    - KPI tiles use PremiumSurface glass styling (assert the glass surface wrapper is present).
  </behavior>
  <action>Create `UsersTableHeader` with a title block ("Users") on the left and a row of glass KPI tiles (PremiumSurface variant="glass"). Implement `AnimatedNumber({ value })` per RESEARCH Pattern 3 — either framer-motion `animate()` + `useMotionValue`/`useTransform` OR a `useEffect` + `requestAnimationFrame` eased interpolation (either satisfies VIS-04; the rAF approach is easier to test deterministically). Ease ~1s ease-out, run ONCE on mount (depend only on the initial value; guard against restarting on incidental re-renders). KPI selection is Claude's discretion (CONTEXT) — default to Total users · Active 30d · Admins. KPIs are display-only (CONTEXT: clickable KPIs → display-only by default) — no onClick. The header has `position: relative` so the particle Canvas can absolutely-position within it (Pitfall 4). All colors via CSS tokens. NO tRPC query — values are passed in as props derived in the shell.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/UsersTableHeader"</automated>
  </verify>
  <done>Three glass KPI tiles render; AnimatedNumber reaches the target once on mount and does not restart on re-render; display-only (no click handlers); test green; tsc 0.</done>
</task>

<task type="auto">
  <name>Task 2: R3F particle accent (install + Canvas) with CSS-only fallback</name>
  <files>package.json, package-lock.json, app/(dashboard)/users/HeaderParticleAccent.tsx, app/(dashboard)/users/UsersTableHeader.tsx</files>
  <action>If the checkpoint approved the install: run `npm install @react-three/fiber@9` (NOT v8 — React 19 incompatible per Pitfall 6; `three@0.184.0` is already installed as a peer). Create `HeaderParticleAccent.tsx` (default export) per RESEARCH Pattern 4: a `<Canvas frameloop="demand" style={{position:"absolute", inset:0, pointerEvents:"none"}} camera={{position:[0,0,2]}}>` rendering ~150–200 drifting points (`bufferGeometry` + `pointsMaterial`, brand-tinted via a token-aligned color, low opacity). For ambient drift under frameloop="demand", drive a slow periodic `invalidate()` (or a single low-cost `frameloop="always"` at ~200 points — GPU cost trivial; keep GPU < 400MB). In UsersTableHeader, mount it via `const HeaderParticleAccent = dynamic(() => import("./HeaderParticleAccent"), { ssr: false })` inside the relative header container, behind the KPI tiles (z-index below the tiles). If the checkpoint chose "css-fallback" (or the install fails): DO NOT create the R3F Canvas; instead render a CSS-only pulsing radial-gradient accent (a `@keyframes`-driven absolutely-positioned div, pointer-events:none) in the same header slot — achieves ambient depth at zero GPU. Either way, the accent is confined to the header strip and never shares a WebGL ancestor with the DataTable (Pitfall 4). Stage package.json + package-lock.json by explicit path only.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Header accent renders behind the KPIs only (R3F Canvas ssr:false+frameloop:demand, OR CSS fallback); no WebGL on the data table; tsc 0. If installed, package.json shows @react-three/fiber@9.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the header in the shell above the DataTable</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>Derive the KPI values from in-memory data (no new query): totalUsers = people.length; active30d = count of users whose derived lastActivity is within 30 days (reuse the row-level lastActivity from buildDirectoryRows or accSummaryMap projects' lastActivity — same derivation, no fetch); admins = count where accUser.adminCount > 0 OR projectAdmin (use the existing enrichment fields already in accSummaryMap). Render `<UsersTableHeader totalUsers={...} active30d={...} admins={...} />` near the top of the shell, ABOVE the DataTable and DataCoverageStrip/filter bar, INSIDE the existing motion.div fade wrapper from Plan 03 so the header participates in the single calm entrance fade. Do NOT introduce WebGL anywhere except inside HeaderParticleAccent. Do NOT add a tRPC query. Replace the old inline `<h1>Users</h1>` title block with the header (the header owns the title now) to avoid a duplicate heading. Keep all other shell behavior (DrillSheet, activity Sheet, filter bar, DataTable) intact.</action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/users/__tests__/UsersDirectoryClient.integration"</automated>
  </verify>
  <done>The header renders above the DataTable inside the fade wrapper with count-up KPIs derived from in-memory data; no new fetch; integration suite green; tsc 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run "app/(dashboard)/users"` green (header unit + integration).
- `npx vitest run` — full suite holds at/above the Phase-2 baseline of 2015 pass.
- `npx tsc --noEmit` exits 0 (whole-tree — HARD repo rule).
- `npm run repo-map:check` passes — confirms the header added no new fetch/effect on the data path (PERF-04 ratchet).
- WebGL confined to header: `grep -rn "Canvas\|@react-three" app/(dashboard)/users/UsersDirectoryClient.tsx app/(dashboard)/users/DirectoryTableColumns.tsx app/(dashboard)/users/PeekPanel.tsx` returns nothing — the only Canvas usage is in HeaderParticleAccent.tsx (PERF-05/Pitfall 4).
- Scope boundary: `git diff --name-only` touches ZERO files under `app/(dashboard)/users/access-analysis/`.
- GPU budget (manual at UAT): Chrome DevTools confirms header particle field keeps GPU memory < 400MB (carried to Phase 7 projector UAT).
</verification>

<success_criteria>
- KPI numbers count up smoothly once on first load (VIS-04); KPIs are display-only and derive from in-memory data (no new fetch).
- A subtle particle accent renders only behind the header KPIs (ssr:false, frameloop:demand) — never over the DataTable (PERF-05/VIS-06); CSS fallback available if R3F install blocked.
- Dependency legitimacy verified via blocking checkpoint before install.
- Full suite green at baseline; tsc 0; repo-map:check clean; scope boundary respected.
</success_criteria>

<output>
Create `.planning/phases/04-users-table-polish/04-04-SUMMARY.md` when done. Stage commits by EXPLICIT path only (shared-WIP branch — never `git add -A/.`); verify `git diff --cached --name-only` before each commit.
</output>
