---
phase: 37
plan: "37-04"
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03"]
autonomous: false
checkpoint: human-verify
requirements: [SCALE-01]
files_modified:
  - tests/e2e/scale-spike.spec.ts
  - scripts/measure-scale-spike.cjs
  - .planning/phases/37-scale-feasibility-spike/37-BASELINE.md
must_haves:
  truths:
    - "All render scenarios + payload timings are measured on the isolated :3100 PRODUCTION build (median + min over ≥10 s samples), never a dev server."
    - "37-BASELINE.md records: machine GPU/renderer string, per-scenario fps, context-loss events, GPU-memory accounting (measured vs estimated labeled), payload bytes + wire/decode/upload ms, embedding numbers from 37-03, and the environment record (BUILD_ID, commit, cosmos.gl version)."
    - "The ladder rung is chosen by the OWNER at an explicit checkpoint (numbers table + live demo on this machine); the decision + evidence are recorded in the phase artifacts."
  artifacts:
    - ".planning/phases/37-scale-feasibility-spike/37-BASELINE.md"
    - ".planning/phases/37-scale-feasibility-spike/37-04-SUMMARY.md"
  key_links:
    - pattern: "__SCALE_SPIKE__"
      in: "tests/e2e/scale-spike.spec.ts"
    - pattern: "playwright.verify.config.ts"
      in: "scripts/measure-scale-spike.cjs"
---

# Plan 37-04 — Measurement run, 37-BASELINE.md, ladder-rung owner checkpoint

## Objective

Turn the three tracks into recorded evidence and an owner decision. Ends the phase
at the checkpoint: rung locked, every later phase builds to it.

## Context

- @.planning/phases/37-scale-feasibility-spike/37-CONTEXT.md (decision 5: sign-off format)
- @scripts/measure-spatial-graph-baseline.cjs (orchestrator pattern to clone: preflight :3100, environment record, spec run, merged JSON)
- @tests/e2e/spatial-graph-baseline.spec.ts (addInitScript early-poll pattern; JSON output to test-results/)
- @playwright.verify.config.ts (storageState auth, :3100 baseURL, out-of-band server)
- 37-01/02/03 SUMMARYs (bridge API actually shipped, embedding numbers)

## Tasks

1. **`tests/e2e/scale-spike.spec.ts`** — navigates `/users/scale-spike?n=4862301`, waits for `__SCALE_SPIKE__.isReady()`, runs scenarios sequentially (rest → panzoom → cpuAmbient → payload), each ≥10 s sample; second pass with `?gpu=1` for gpuDrift. Asserts only honesty invariants (node count set == requested, samples nonempty — NOT fps thresholds; this spec records, the owner judges). Writes `test-results/scale-spike.json`.
2. **`scripts/measure-scale-spike.cjs`** — clone of the baseline orchestrator: :3100 preflight (printing the PowerShell build instructions with BOTH flags: `NEXT_PUBLIC_ACC_GRAPH_TEST="1"` for auth-bridge parity, `NEXT_PUBLIC_ACC_SCALE_SPIKE="1"`, `NEXT_DIST_DIR=".next-e2e"`), environment record, spec run, merged JSON print.
3. **Isolated build + run (PowerShell, standing rules):** `npx tsc --noEmit` → isolated `.next-e2e` build with the two flags → `next start -p 3100` → `node scripts/measure-scale-spike.cjs`. `:3000` untouched throughout. If the full 4.86M set hard-fails (context loss / allocation failure), re-run at descending `?n=` to find the ceiling and record it — a measured ceiling is the deliverable, not a failure of the plan.
4. **`37-BASELINE.md`** — assemble all numbers + environment + the 37-03 embedding JSON + explicit L0–L3 readiness table (what the evidence supports per rung). VERIFY items from CONTEXT resolved here (cosmos 4.86M ceiling, GPU model/VRAM recorded).
5. **CHECKPOINT (human-verify)** — owner reviews the numbers table + live spike-route demo on this machine; picks the rung. Record decision verbatim in 37-BASELINE.md ("Ladder decision" section) + SUMMARY. Do not advance the phase without it.

## Verification gates

- Spec green on :3100; `test-results/scale-spike.json` exists with all scenarios.
- `npx tsc --noEmit` → 0 (spec + script included in the tsc program).
- 37-BASELINE.md complete per truth #2; owner decision recorded.

## Success criteria

Phase success criteria 1–5 (ROADMAP) all evidenced; SCALE-01 closable; rung locked.
