---
phase: 08-dc-per-module-ingest-permission-csvs
plan: 02
type: tdd
wave: 1
depends_on: [08-01]
files_modified:
  - lib/acc/dcKnownBots.ts
  - lib/acc/dcKnownBots.test.ts
  - lib/acc/dcAnomalyChecks.ts
  - lib/acc/dcAnomalyChecks.test.ts
autonomous: true
requirements:
  - DC8-02
  - DC8-09
must_haves:
  truths:
    - "isBotActor({name:'Autodesk Cloud Worker'}) returns true; isBotActor({name:'Luis Cortes'}) returns false"
    - "assertNoAnomalies throws when user-count drops by more than 10% vs previous run"
    - "assertNoAnomalies returns silently when no previous baseline exists (first-ever run)"
  artifacts:
    - path: "lib/acc/dcKnownBots.ts"
      provides: "KNOWN_BOT_NAMES set + KNOWN_BOT_AUTODESK_IDS set + isBotActor predicate"
      exports: ["KNOWN_BOT_NAMES", "KNOWN_BOT_AUTODESK_IDS", "isBotActor"]
    - path: "lib/acc/dcAnomalyChecks.ts"
      provides: "AnomalyError class + assertNoAnomalies(tx, previous, thresholds) + DEFAULT_THRESHOLDS"
      exports: ["AnomalyError", "assertNoAnomalies", "DEFAULT_THRESHOLDS", "AnomalyThresholds"]
  key_links:
    - from: "lib/acc/dcActivityCsvIngest.ts (plan 08-04)"
      to: "lib/acc/dcKnownBots.ts"
      via: "named import { isBotActor }"
      pattern: "import.*isBotActor.*dcKnownBots"
    - from: "lib/acc/dcAdminCsvIngest.ts (plan 08-05)"
      to: "lib/acc/dcAnomalyChecks.ts"
      via: "named import { assertNoAnomalies }"
      pattern: "import.*assertNoAnomalies.*dcAnomalyChecks"
---

<objective>
TDD-build two pure modules that gate the ingest pipeline: (1) a bot-filter that drops system-actor rows at the parse boundary, and (2) anomaly checks that block transaction commit when a snapshot looks broken. Both are pure (no Prisma, no fetch, no fs) so they're cheap to test.

Purpose: DC8-02 prevents bot noise from polluting Activity widgets; DC8-09 prevents a bad APS day from wiping the dashboard.
Output: 2 pure libraries + 2 GREEN test files.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dc-per-module-ingest-permission-csvs/08-CONTEXT.md
@.planning/phases/08-dc-per-module-ingest-permission-csvs/08-RESEARCH.md
@lib/acc/dcKnownBots.test.ts
@lib/acc/dcAnomalyChecks.test.ts

<interfaces>
RESEARCH.md Example 2 defines KNOWN_BOT_NAMES bootstrap list:
- 'Autodesk Cloud Worker', 'Construction Cloud Sync', 'BIM 360 System', 'ACC System', 'Autodesk Insight', 'Autodesk Service Account'

RESEARCH.md Pattern 4 defines AnomalyThresholds shape:
```typescript
export interface AnomalyThresholds {
  maxUserDropPct: number;        // default 10
  maxProjectDropPct: number;     // default 5
  requireAllAdminCsvs: boolean;  // default true
  requireNonZeroInsert: boolean; // default true
}

export interface PreviousRunMetrics {
  userCount: number;
  projectCount: number;
  rowsByAdminCsv: Record<string, number>;
}
```

Prisma transaction client type for tx parameter:
```typescript
import type { Prisma } from '@prisma/client';
type PrismaTx = Prisma.TransactionClient;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED-GREEN dcKnownBots — write tests + minimal implementation</name>
  <files>lib/acc/dcKnownBots.test.ts, lib/acc/dcKnownBots.ts</files>
  <action>
    REPLACE the placeholder in `lib/acc/dcKnownBots.test.ts` (from plan 08-01) with real tests. Required cases:

    1. `isBotActor({ name: 'Autodesk Cloud Worker' })` returns true
    2. `isBotActor({ name: 'Construction Cloud Sync' })` returns true
    3. `isBotActor({ name: 'BIM 360 System' })` returns true
    4. `isBotActor({ name: 'ACC System' })` returns true
    5. `isBotActor({ name: 'Luis Cortes' })` returns false (real human)
    6. `isBotActor({ name: null })` returns false (no name to match)
    7. `isBotActor({ autodeskId: 'BOT123', name: null })` returns true when `'BOT123'` is in `KNOWN_BOT_AUTODESK_IDS`
    8. `isBotActor({ autodeskId: 'USER456', name: 'Luis Cortes' })` returns false
    9. `KNOWN_BOT_NAMES.size` >= 6 (sanity check the constant)

    Run vitest — confirm RED (no module exists yet). Then create `lib/acc/dcKnownBots.ts` using RESEARCH.md Example 2 verbatim (KNOWN_BOT_NAMES set, empty KNOWN_BOT_AUTODESK_IDS set, isBotActor predicate). Re-run vitest — confirm GREEN.

    Commit RED first (`test(08-02): add failing tests for isBotActor`), then GREEN (`feat(08-02): implement dcKnownBots`).

    Pure module — no Prisma imports, no fetch, no fs.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/dcKnownBots.test.ts</automated>
  </verify>
  <done>9 tests GREEN; module exports KNOWN_BOT_NAMES, KNOWN_BOT_AUTODESK_IDS, isBotActor; no `@prisma/client` import; no `node:fs` import.</done>
</task>

<task type="auto">
  <name>Task 2: RED-GREEN dcAnomalyChecks — write tests + minimal implementation</name>
  <files>lib/acc/dcAnomalyChecks.test.ts, lib/acc/dcAnomalyChecks.ts</files>
  <action>
    REPLACE the placeholder in `lib/acc/dcAnomalyChecks.test.ts` with real tests. Use a mocked Prisma tx (plain object with `.accDcUser.count()` returning a stubbed number) — do NOT spin up a real DB. Required cases:

    1. No previous baseline -> `assertNoAnomalies(mockTx, null, defaults)` resolves (returns void)
    2. User count drops 5% (within threshold) -> resolves
    3. User count drops 15% (above 10% threshold) -> throws `AnomalyError` with message containing "User count dropped"
    4. Project count drops 8% (above 5% threshold) -> throws `AnomalyError` with message containing "Project count"
    5. `previous.rowsByAdminCsv.admin_users = 100` but new ingest got `admin_users = 0` rows with `requireNonZeroInsert: true` -> throws
    6. Same scenario with `requireNonZeroInsert: false` -> resolves
    7. Threshold object override works (passing `{ maxUserDropPct: 50 }` lets 30% drops through)

    Build mock tx like:
    ```typescript
    const mockTx = { accDcUser: { count: vi.fn().mockResolvedValue(85) }, accDcProject: { count: vi.fn().mockResolvedValue(92) } } as unknown as Prisma.TransactionClient;
    ```

    Run vitest — RED. Implement `lib/acc/dcAnomalyChecks.ts`:
    - Export `class AnomalyError extends Error` with name `'AnomalyError'`
    - Export `interface AnomalyThresholds` matching RESEARCH.md Pattern 4
    - Export `interface PreviousRunMetrics { userCount: number; projectCount: number; rowsByAdminCsv: Record<string, number> }`
    - Export `DEFAULT_THRESHOLDS: AnomalyThresholds = { maxUserDropPct: 10, maxProjectDropPct: 5, requireAllAdminCsvs: true, requireNonZeroInsert: true }`
    - Export `async function assertNoAnomalies(tx, previous, thresholds = DEFAULT_THRESHOLDS)` — returns early if previous is null; queries `tx.accDcUser.count()` + `tx.accDcProject.count()`; computes drop pct; throws on violation.

    Re-run vitest — GREEN. Commit RED then GREEN.

    Pure module aside from the Prisma type-only import.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/dcAnomalyChecks.test.ts</automated>
  </verify>
  <done>7 tests GREEN; AnomalyError thrown with descriptive messages; threshold override works; first-run path (previous=null) is silent.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/acc/dcKnownBots.test.ts lib/acc/dcAnomalyChecks.test.ts` shows 16 tests passing.
- Neither module imports `@prisma/client` at runtime (type-only imports OK in dcAnomalyChecks).
- Each module under 150 lines (pure logic, no orchestration).
</verification>

<success_criteria>
Plan 08-04 (dcActivityCsvIngest) can `import { isBotActor }` and drop bot rows at parse boundary. Plan 08-05 (dcAdminCsvIngest) can `import { assertNoAnomalies }` inside its transaction to roll back on bad snapshots.
</success_criteria>

<output>
Create `.planning/phases/08-dc-per-module-ingest-permission-csvs/08-02-SUMMARY.md` listing: bot names shipped, anomaly thresholds chosen, test counts.
</output>
