---
phase: 10-layering-boundary-fixes
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - lib/acc/coordinationCounts.ts
  - lib/acc/timelineCounts.ts
  - lib/acc/moduleCountsTypes.ts
  - app/(dashboard)/access-analysis/coordinationCounts.ts
  - app/(dashboard)/access-analysis/timelineCounts.ts
  - app/(dashboard)/access-analysis/moduleCounts.ts
  - lib/server/coordinationByProjectView.ts
  - lib/server/activityTimelineView.ts
  - lib/server/moduleActivityView.ts
  - .planning/codebase/CONCERNS.md
autonomous: false
requirements: [BND-03, BND-04]
must_haves:
  truths:
    - "node scripts/repo-map/check.cjs passes; the lib->app edge list contains only spatial-graph-coupled and Phase-14-monolith-blocked edges, each documented as deferred with a reason."
    - "Every app->server edge is confirmed to originate from a Server Component, Server Action, API route handler, or the dashboard layout — zero client-component (\"use client\") violations — and this is documented."
    - "/access-analysis and /template-mty render identically after the boundary cleanup (clash drill-down, module donut, timeline, coordination panel, template terrain all unchanged)."
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "BND-03/BND-04 resolution note: enumerated lib->app edges with in-scope-fixed vs deferred classification, and the app->server audit verdict"
      contains: "BND-03"
    - path: "lib/acc/coordinationCounts.ts"
      provides: "Pure coordination aggregation moved out of app/ (clears the coordinationByProjectView lib->app edge)"
      contains: "summarizeCoordination"
  key_links:
    - from: "lib/server/coordinationByProjectView.ts"
      to: "lib/acc/coordinationCounts.ts"
      via: "type import repointed from app/ to lib/"
      pattern: "from \"@/lib/acc/coordinationCounts\""
    - from: "app/(dashboard)/access-analysis/coordinationCounts.ts"
      to: "lib/acc/coordinationCounts.ts"
      via: "re-export barrel preserves the UI import path"
      pattern: "from \"@/lib/acc/coordinationCounts\""
---

<objective>
BND-03 + BND-04: Resolve the architecture-boundary warnings conservatively, then
document the deferred remainder. This is an audit-and-document slice plus a
small, zero-risk set of clean module moves; it runs in wave 2 so the repo-map
enumeration reflects the edges already removed by 10-01 (coordinationActions →
@/server/db) and 10-02 (4 diag scripts → app).

VERIFIED this session (fresh dependency-cruiser.json enumeration):

lib->app edges = 20. Classification:
- SPATIAL-GRAPH-COUPLED (DEFER): lib/acc/dcUserAssembly.ts → users/access-analysis/internalDomains.ts;
  lib/acc/activityAggregate.ts → users/access-analysis/accTaxonomy.ts;
  lib/server/acc-route-hydration.ts → users/useUsersDirectoryData.ts. Plus the new
  lib/acc/activityClassification.ts → users/access-analysis/accTaxonomy + accNormalize
  edge introduced by 10-02. internalDomains.ts is explicitly out-of-scope per
  REQUIREMENTS. All under the /users/ (spatial-graph) surface → documented-deferred.
- PHASE-14-MONOLITH-BLOCKED (DEFER, REF-01): every edge into
  app/(dashboard)/access-analysis/folderTerrain.ts (1,093-line monolith) —
  folderPermissionTerrainView.ts, templateFolderTerrain.ts, templateRoleTree.ts,
  templateView.ts. Also folderInheritance.ts (imports ./folderTerrain),
  projectGroups.ts (imports ./projectFilter), and template-mty/permissionAccess.ts
  (imports ./folderTerrain TIER_LEGEND). Moving any of these cleanly requires the
  folderTerrain split, which is REF-01/Phase 14 (characterization tests ship there
  first). Documented-deferred — moving them now would violate the zero-behavior-
  change contract and pull Phase 14 forward.
- CLEANLY IN-SCOPE (FIX HERE): the self-contained, type-only-imported aggregation
  modules — coordinationCounts.ts, timelineCounts.ts, moduleCounts.ts (the type
  consumed by the lib/server view; moduleCounts also has the BND-02 classifier
  dependency, handled below). These have no app-only import chain and are imported
  `type`-only by their lib/server/*View.ts, so moving the type to lib/ and
  re-exporting from app/ is zero-risk.

app->server edges = 29. Audit verdict (VERIFIED): ALL 29 originate from Server
Components, Server Actions ("use server"), app/api/**/route.ts handlers, or
app/(dashboard)/layout.tsx — there are ZERO "use client" component imports of
server/. The one coordinationActions.ts → server/db.ts edge is removed by 10-01;
its → server/auth.ts edge remains and is legitimate. BND-04's deliverable is the
documented verdict, not a code fix.

Purpose: establish a clean, documented boundary baseline for Phases 13/14 while
honoring the locked Conservative decision (remove only edges fixable outside
spatial-graph AND outside the Phase-14 monolith; document the rest).

Output: a small set of clean type-module moves with re-export barrels, repointed
lib/server view imports, and a BND-03/BND-04 resolution note in CONCERNS.md
enumerating fixed vs deferred edges. Final full gate sequence + rebuild
checkpoint.

Workshop impact: invisible, behavior identical. Data truthfulness: no data
change — only import paths and type homes move.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-layering-boundary-fixes/10-CONTEXT.md
@.planning/codebase/CONCERNS.md

# Source under change — read before editing
@app/(dashboard)/access-analysis/coordinationCounts.ts
@lib/server/coordinationByProjectView.ts

<interfaces>
<!-- Verified this session (dependency-cruiser.json + source reads). -->

In-scope clean-move modules and their SINGLE lib/server type importer (the
lib->app edge each move clears):
- app/(dashboard)/access-analysis/coordinationCounts.ts (70 lines, NO imports):
  exports CoordinationRow, CoordinationProject, CoordinationSummary,
  isClosedStatus(), summarizeCoordination(). Importer:
  lib/server/coordinationByProjectView.ts:3
    import type { CoordinationRow } from "@/app/(dashboard)/access-analysis/coordinationCounts";
- app/(dashboard)/access-analysis/timelineCounts.ts (107 lines, NO app imports):
  exports ActivityTimelineRow (+ transforms). Importer:
  lib/server/activityTimelineView.ts:3
    import type { ActivityTimelineRow } from "@/app/(dashboard)/access-analysis/timelineCounts";
- app/(dashboard)/access-analysis/moduleCounts.ts (96 lines): imports
  classifyActivity/donutModules + re-exports UNMAPPED_MODULE from
  ./moduleOverrides (which after 10-02 is the lib/acc/activityClassification
  barrel). Importer of its TYPE:
  lib/server/moduleActivityView.ts:3
    import type { ModuleActivityRow } from "@/app/(dashboard)/access-analysis/moduleCounts";

app->server audit command (BND-04 verdict, re-run to confirm zero client
violations):
  rg -n "from \"@/server" app/ -g "*.ts" -g "*.tsx"
  then for each hit confirm the file is a route handler (app/api/**/route.ts),
  has "use server" (Server Action), is a Server Component (no "use client"
  directive, default RSC), or is app/(dashboard)/layout.tsx.

DEFER list to document verbatim in CONCERNS (do NOT edit these files):
- lib->app spatial-graph: dcUserAssembly→internalDomains, activityAggregate→accTaxonomy,
  acc-route-hydration→useUsersDirectoryData, activityClassification→accTaxonomy/accNormalize.
- lib->app monolith (REF-01/Phase 14, folderTerrain.ts): folderPermissionTerrainView,
  templateFolderTerrain, templateRoleTree, templateView (all →folderTerrain),
  folderInheritance(→folderTerrain), projectGroups(→projectFilter),
  permissionAccess(→folderTerrain TIER_LEGEND).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the three clean type/aggregation modules to lib/acc with re-export barrels</name>
  <files>lib/acc/coordinationCounts.ts, lib/acc/timelineCounts.ts, lib/acc/moduleCountsTypes.ts, app/(dashboard)/access-analysis/coordinationCounts.ts, app/(dashboard)/access-analysis/timelineCounts.ts, app/(dashboard)/access-analysis/moduleCounts.ts, lib/server/coordinationByProjectView.ts, lib/server/activityTimelineView.ts, lib/server/moduleActivityView.ts</files>
  <action>
    Goal: clear the three cleanly-fixable lib->app edges
    (coordinationByProjectView, activityTimelineView, moduleActivityView) by
    moving the PURE content they import out of app/ into lib/acc, then making the
    app/ files re-export barrels so every UI import path is unchanged.

    (a) coordinationCounts: move the entire verbatim body of
    `app/(dashboard)/access-analysis/coordinationCounts.ts` (it has NO imports) to
    `lib/acc/coordinationCounts.ts`. Replace the app/ file with
    `export * from "@/lib/acc/coordinationCounts";`. Repoint
    `lib/server/coordinationByProjectView.ts:3` to
    `import type { CoordinationRow } from "@/lib/acc/coordinationCounts";`.

    (b) timelineCounts: move the entire verbatim body of
    `app/(dashboard)/access-analysis/timelineCounts.ts` (no app imports) to
    `lib/acc/timelineCounts.ts`. Replace the app/ file with
    `export * from "@/lib/acc/timelineCounts";`. Repoint
    `lib/server/activityTimelineView.ts:3` to
    `import type { ActivityTimelineRow } from "@/lib/acc/timelineCounts";`.

    (c) moduleCounts type only: `moduleCounts.ts` imports the classifier (now the
    lib/acc/activityClassification barrel via ./moduleOverrides), so do NOT move
    the whole file. Instead extract ONLY the pure row TYPE the lib/server view
    needs: create `lib/acc/moduleCountsTypes.ts` containing the
    `ModuleActivityRow` interface (move the interface declaration verbatim from
    moduleCounts.ts), and in `moduleCounts.ts` re-export it
    (`export type { ModuleActivityRow } from "@/lib/acc/moduleCountsTypes";`) so
    its existing exports are unchanged. Repoint
    `lib/server/moduleActivityView.ts:3` to
    `import type { ModuleActivityRow } from "@/lib/acc/moduleCountsTypes";`.

    Verbatim only — do not change any interface field, function body, or logic.
    Every app/ component/test importer of these three modules keeps working
    through the re-export barrels (VERIFIED importers include AccessAnalysisCharts,
    CoordinationByProject, ActivityTimelineChart, ModulesPieChart and their tests).
    Run `npx tsc --noEmit` to confirm no type drift, then `npm run repo-map` and
    confirm these three lib->app edges are gone from dependency-cruiser.json.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run repo-map >/dev/null 2>&1; node -e "const j=require('./.tools/repo-map/dependency-cruiser.json');const norm=s=>String(s||'').replaceAll('\\\\','/');const gone=['coordinationCounts','timelineCounts','moduleCounts'];let bad=0;for(const m of j.modules||[]){const s=norm(m.source);if(!s.startsWith('lib/server/'))continue;for(const d of m.dependencies||[]){const t=norm(d.resolved||d.module);if(t.startsWith('app/')&&gone.some(g=>t.endsWith('/'+g+'.ts')))bad++}}console.log('remaining lib/server->app edges on the 3 moved modules:',bad);process.exit(bad===0?0:1)"</automated>
  </verify>
  <done>The three view files import their row types from `lib/acc/*`; the app/ modules are re-export barrels with unchanged public surface; `npx tsc --noEmit` passes; dependency-cruiser shows 0 lib/server→app edges into coordinationCounts/timelineCounts/moduleCounts.</done>
</task>

<task type="auto">
  <name>Task 2: Document the BND-03/BND-04 verdict in CONCERNS.md and run the full gate sequence</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Append a "## BND-03 / BND-04 Resolution (Phase 10)" note to
    `.planning/codebase/CONCERNS.md` (under or adjacent to sections 7.1/7.2) with:

    BND-03 (lib->app): the fresh `node scripts/repo-map/check.cjs` enumeration and
    the three classes — (1) FIXED IN PHASE 10: coordinationCounts, timelineCounts,
    moduleCounts row type moved to lib/acc (edges removed). (2) DEFERRED —
    SPATIAL-GRAPH-COUPLED: dcUserAssembly→internalDomains,
    activityAggregate→accTaxonomy, acc-route-hydration→useUsersDirectoryData,
    activityClassification→accTaxonomy/accNormalize — all rooted in the /users/
    spatial-graph surface, out of v2.1 scope (REQUIREMENTS carve-out). (3)
    DEFERRED — PHASE-14 MONOLITH (REF-01): all edges into folderTerrain.ts plus
    folderInheritance/projectGroups/permissionAccess — cannot be cleanly moved
    until the 1,093-line folderTerrain monolith is split; Phase 14 ships its
    characterization tests first. State the conservative rationale: removing the
    deferred edges now would either touch spatial-graph files or pull the Phase 14
    split forward, both violating the locked scope and the zero-behavior-change
    contract.

    BND-04 (app->server): record the audit verdict — all app->server edges (29 at
    baseline, 28 after 10-01 drops coordinationActions→server/db) originate from
    Server Components, Server Actions ("use server"), app/api/**/route.ts
    handlers, or app/(dashboard)/layout.tsx. ZERO "use client" component imports
    of server/ exist, so there is no violation to fix; the edges are the expected,
    legitimate RSC/Action/route boundary. Include the exact audit command
    (`rg -n "from \"@/server" app/ -g "*.ts" -g "*.tsx"`) and the classification
    method for reproducibility.

    Keep the note factual and dated; do not alter the existing section text.

    Then run the FULL Phase 10 gate sequence (this is the last plan):
    1. `npx tsc --noEmit` (must pass before any rebuild — next build typechecks the
       whole tree incl. tests).
    2. `npm run repo-map` then `node scripts/repo-map/check.cjs` — gate passes;
       no-scripts-to-app down to the 2 documented spatial-graph edges; lib->app
       list contains only the documented spatial-graph + monolith edges.
    3. `npx ast-grep scan --config sgconfig.yml --json=pretty` filtered for
       direct-prisma-in-ui = 0 matches.
    4. `npm test` (vitest run) green, including the two new pin tests from 10-01
       (projectClashView) and 10-02 (activityClassification).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && node scripts/repo-map/check.cjs && npx ast-grep scan --config sgconfig.yml --json=pretty | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);const n=a.filter(x=>x.ruleId==='direct-prisma-in-ui').length;console.log('direct-prisma-in-ui:',n);process.exit(n===0?0:1)})"</automated>
  </verify>
  <done>CONCERNS.md carries the dated BND-03/BND-04 resolution note (fixed vs deferred edges + app->server verdict); `npx tsc --noEmit`, `npm test`, `node scripts/repo-map/check.cjs`, and the `direct-prisma-in-ui`=0 ast-grep gate all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rebuild and human spot-check /access-analysis + /template-mty render identically</name>
  <action>
    Rebuild on :3000 and confirm zero visible behavior change across the two
    affected workshop pages. This is the final Phase 10 gate after all automated
    checks pass. Follow the how-to-verify steps; resume only on owner approval.
  </action>
  <what-built>
    Phase 10 boundary cleanup complete: the clash drill-down query runs through
    the accCoordination tRPC procedure (no direct Prisma in the Server Action),
    activity classification lives in lib/acc, the four diag scripts and three
    lib/server view type imports point at lib/, and the deferred lib->app /
    app->server edges are documented in CONCERNS.md. All automated gates
    (tsc, npm test, repo-map check, ast-grep direct-prisma-in-ui=0) pass.
  </what-built>
  <how-to-verify>
    1. Rebuild and restart per the deploy sequence (Task Scheduler stop → `npx tsc
       --noEmit` → `npm run build` → restart) on `:3000`. Do NOT run `npm run build`
       while the running :3000 instance is live (it 500s the server) — stop the
       scheduled task first.
    2. Open `http://localhost:3000/access-analysis`.
    3. Module-activity donut: confirm the same module slices, counts, labels, and
       drill-down as before (BND-02 moved the classifier verbatim — output must be
       identical).
    4. Model-Coordination panel: expand a project to trigger the clash drill-down.
       Confirm the same clash issues load (same count, titles, authors, statuses,
       ordering) and clicking an author still opens the shared UserProfilePanel
       (BND-01 moved the query verbatim through tRPC — behavior must be identical).
    5. Activity timeline + coordination-by-project panels: confirm unchanged.
    6. Open `http://localhost:3000/template-mty` and confirm the terrain/role views
       render identically (template view type imports were repointed; logic
       unchanged).
    7. Confirm no console errors and no theme/layout drift (zinc theme untouched,
       no new WebGL).
  </how-to-verify>
  <resume-signal>Type "approved" if /access-analysis and /template-mty render identically, or describe any visible difference.</resume-signal>
</task>

</tasks>

<verification>
Full Phase 10 gate sequence (this plan owns the complete run):
- `npx tsc --noEmit` — passes (required before any rebuild).
- `node scripts/repo-map/check.cjs` — gate passes; `no-scripts-to-app` warnings
  only the 2 documented spatial-graph `build-instance-features` edges; `lib->app`
  edge list contains only documented spatial-graph-coupled + Phase-14-monolith
  edges.
- `npx ast-grep scan --config sgconfig.yml` `direct-prisma-in-ui` — 0 matches.
- `npm test` — green incl. the 10-01 (projectClashView) and 10-02
  (activityClassification) pin tests.
- Rebuild + Task Scheduler restart on `:3000`; human spot-check that
  `/access-analysis` (clash drill-down + module donut + timeline + coordination)
  and `/template-mty` render identically.
- Guardrails: zinc theme untouched, no new WebGL, `/users/spatial-graph` files NOT
  modified, spatial-graph + monolith edges documented-deferred in CONCERNS.md.
</verification>

<success_criteria>
ROADMAP Phase 10 success criteria 3 and 4 are met: all `lib->app` edges NOT rooted
in spatial-graph AND not blocked by the Phase-14 folderTerrain monolith are
removed; `node scripts/repo-map/check.cjs` output lists only spatial-graph-coupled
and monolith-deferred edges, each documented with a reason in CONCERNS.md. All
`app->server` edges are audited and confirmed to come from Server Components /
Server Actions / route handlers / layout (zero client-component violations); the
acceptable edges and the verdict are documented in CONCERNS.md. `/access-analysis`
and `/template-mty` render identically.
</success_criteria>

<output>
Create `.planning/phases/10-layering-boundary-fixes/10-03-SUMMARY.md` when done.
</output>
