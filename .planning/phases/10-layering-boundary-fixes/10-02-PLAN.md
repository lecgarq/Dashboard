---
phase: 10-layering-boundary-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/acc/activityClassification.ts
  - lib/acc/activityClassification.test.ts
  - app/(dashboard)/access-analysis/moduleOverrides.ts
  - scripts/diag-activity-coordination.cjs
  - scripts/diag-activity-types.cjs
  - scripts/diag-activity-service-xtab.cjs
  - scripts/diag-activity-module-audit.cjs
autonomous: true
requirements: [BND-02]
must_haves:
  truths:
    - "classifyActivity, donutModules, CATEGORY_LABELS produce byte-identical output before and after the extraction (module-activity donut on /access-analysis renders identically)."
    - "The four scripts/diag-activity-*.cjs import classification logic from lib/acc/activityClassification.ts, not from app/."
    - "node scripts/repo-map/check.cjs shows 0 scripts->app dependency-cruiser warnings on the moduleOverrides path (the 4 moduleOverrides edges clear)."
  artifacts:
    - path: "lib/acc/activityClassification.ts"
      provides: "Pure activity classification (classifyActivity, donutModules, CATEGORY_LABELS, CATEGORY_ORDER, UNMAPPED_MODULE) moved verbatim from moduleOverrides.ts"
      contains: "classifyActivity"
    - path: "lib/acc/activityClassification.test.ts"
      provides: "Pin test asserting classifier output identity for representative rawActions + donutModules + CATEGORY_LABELS"
      contains: "classifyActivity"
    - path: "app/(dashboard)/access-analysis/moduleOverrides.ts"
      provides: "Verbatim re-export of the pure classifier so every existing UI import path is unchanged"
      contains: "export"
  key_links:
    - from: "app/(dashboard)/access-analysis/moduleOverrides.ts"
      to: "lib/acc/activityClassification.ts"
      via: "re-export (export { classifyActivity, donutModules, CATEGORY_LABELS, ... })"
      pattern: "from \"@/lib/acc/activityClassification\""
    - from: "scripts/diag-activity-coordination.cjs"
      to: "lib/acc/activityClassification.ts"
      via: "require(\"../lib/acc/activityClassification.ts\")"
      pattern: "lib/acc/activityClassification"
---

<objective>
BND-02: Move the pure activity-classification logic out of the app route into
`lib/acc/`, clearing the four `scripts->app` dependency-cruiser warnings on the
`moduleOverrides` path while keeping the `/access-analysis` module-activity donut
byte-identical.

VERIFIED this session — `app/(dashboard)/access-analysis/moduleOverrides.ts`
exports exactly: `classifyActivity`, `donutModules`, `CATEGORY_LABELS`,
`CATEGORY_ORDER`, `UNMAPPED_MODULE`. (There is NO `n()` export — the
REQUIREMENTS/CONTEXT mention of `n` is stale; do not invent one.) The four
diagnostic scripts import: `diag-activity-coordination.cjs` →
`{ classifyActivity, donutModules }`; `diag-activity-types.cjs` →
`{ classifyActivity, CATEGORY_LABELS }` and `{ donutModules }`;
`diag-activity-service-xtab.cjs` → `{ classifyActivity, donutModules }`;
`diag-activity-module-audit.cjs` → `{ classifyActivity, donutModules }`.

Move the classifier verbatim to `lib/acc/activityClassification.ts`, re-export
ALL public symbols from `moduleOverrides.ts` so every UI import path
(`ModulesPieChart.tsx`, `moduleCounts.ts`, and their tests — VERIFIED importers)
keeps working unchanged, and repoint the four scripts to `require` from `lib/`.

Purpose: clean the scripts->app boundary so `moduleOverrides.ts` can be
refactored without breaking diagnostic scripts. ZERO behavior change.

KNOWN CONSTRAINT (do not "fix" here): the classifier imports its taxonomy from
`../users/access-analysis/accTaxonomy` and `../users/access-analysis/accNormalize`
(VERIFIED). Those modules live under the `/users/` (spatial-graph) surface and
are OUT OF SCOPE per REQUIREMENTS. The new `lib/acc/activityClassification.ts`
will therefore import them via `@/app/(dashboard)/users/access-analysis/...`,
creating a `lib->app` taxonomy edge. That edge is DOCUMENTED as spatial-graph-
deferred in Plan 10-03 (BND-03), NOT eliminated here. This matches the locked
Conservative decision.

Output: new `lib/acc/activityClassification.ts` + `.test.ts`, re-export-only
`moduleOverrides.ts`, four repointed `scripts/diag-activity-*.cjs`.

Workshop impact: invisible, behavior identical. Data truthfulness: no data
change — same `rawAction`-based classification, same donut output.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-layering-boundary-fixes/10-CONTEXT.md

# Source under change — read before editing
@app/(dashboard)/access-analysis/moduleOverrides.ts

<interfaces>
<!-- Verified this session. -->

moduleOverrides.ts PUBLIC exports (the full set to move + re-export):
- export const CATEGORY_LABELS: Readonly<Record<string,string>>
- export const CATEGORY_ORDER: readonly string[]
- export const UNMAPPED_MODULE = "unmapped"
- export function donutModules(): Array<{ id: string; label: string }>
- export function classifyActivity(rawAction: string):
    { moduleId: string; label: string; category: string }
Internal (move with them, keep non-exported): ADMIN_ACTIONS_ID/LABEL,
MODEL_COORDINATION_ID, DATA_MANAGEMENT_ID, BUILD_ID, CAT alias,
COORDINATION_ISSUE_LABELS, CATEGORY_OVERRIDES, EXTRA_ACTIONS, ADMIN_ACTION_IDS.

moduleOverrides.ts taxonomy imports (KEEP — spatial-graph-coupled, deferred):
- import { getActionsByModule, getModules, getAction, resolveActionId }
    from "../users/access-analysis/accTaxonomy";
- import { normalizeActionId } from "../users/access-analysis/accNormalize";

VERIFIED importers of moduleOverrides (must keep working via re-export):
- app/(dashboard)/access-analysis/components/ModulesPieChart.tsx
- app/(dashboard)/access-analysis/moduleCounts.ts (imports classifyActivity,
  donutModules; also re-exports UNMAPPED_MODULE)
- app/(dashboard)/access-analysis/__tests__/moduleCounts.test.ts
- the four scripts/diag-activity-*.cjs (repointed in this plan)

Script import lines to repoint (exact, VERIFIED):
- scripts/diag-activity-coordination.cjs:12
    const { classifyActivity, donutModules } = require("../app/(dashboard)/access-analysis/moduleOverrides.ts");
- scripts/diag-activity-types.cjs:13
    const { classifyActivity, CATEGORY_LABELS } = require("../app/(dashboard)/access-analysis/moduleOverrides.ts");
  scripts/diag-activity-types.cjs:14
    const { donutModules } = require("../app/(dashboard)/access-analysis/moduleOverrides.ts");
- scripts/diag-activity-service-xtab.cjs:12
    const { classifyActivity, donutModules } = require("../app/(dashboard)/access-analysis/moduleOverrides.ts");
- scripts/diag-activity-module-audit.cjs:14
    const { classifyActivity, donutModules } = require("../app/(dashboard)/access-analysis/moduleOverrides.ts");
Scripts run via `require("tsx/cjs")` so a `.ts` require target is fine.

dependency-cruiser rule (VERIFIED): no-scripts-to-app currently flags exactly 6
edges; 4 are the diag-activity scripts -> moduleOverrides.ts (cleared here), 2
are scripts/build-instance-features.ts -> graphNodesFromUsers.ts /
instanceFeatureTokens.ts (spatial-graph-coupled, OUT OF SCOPE — left as
documented-deferred, the warning budget for those 2 stays at baseline).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move classifier verbatim to lib/acc/activityClassification.ts + pin test</name>
  <files>lib/acc/activityClassification.ts, lib/acc/activityClassification.test.ts</files>
  <behavior>
    - classifyActivity maps a coordination issue rawAction (e.g. "issue-attach")
      to moduleId "build" with the Build label and workflowChange category.
    - classifyActivity maps an EXTRA_ACTIONS rawAction (e.g. "create-project") to
      moduleId "adminActions".
    - classifyActivity maps an unknown rawAction to moduleId UNMAPPED_MODULE with
      category CATEGORY_LABELS.unknown.
    - donutModules() returns the excel modules minus modelCoordination, with
      "adminActions" inserted right after "preconstruction".
    - classifyActivity never returns moduleId "modelCoordination" (redirected to
      dataManagement).
  </behavior>
  <action>
    Create `lib/acc/activityClassification.ts` and move the ENTIRE current body
    of `app/(dashboard)/access-analysis/moduleOverrides.ts` into it verbatim —
    the file header comment, the taxonomy imports, all internal consts, and the
    exported `CATEGORY_LABELS`, `CATEGORY_ORDER`, `UNMAPPED_MODULE`,
    `donutModules`, `classifyActivity`. Update ONLY the two relative taxonomy
    import paths so they resolve from `lib/acc/` to the unchanged spatial-graph
    modules: use the `@/` alias —
    `import { getActionsByModule, getModules, getAction, resolveActionId } from "@/app/(dashboard)/users/access-analysis/accTaxonomy";`
    and `import { normalizeActionId } from "@/app/(dashboard)/users/access-analysis/accNormalize";`
    (the `../users/...` relative path is invalid from `lib/acc/`). Do not change
    any classification logic, mapping table, label, or ordering — this is a
    verbatim move. Add a short top-of-file note that the `accTaxonomy`/
    `accNormalize` imports are a known spatial-graph-coupled `lib->app` edge,
    deferred per BND-03 (documented in 10-03).

    Add `lib/acc/activityClassification.test.ts` modeled on existing
    `lib/acc/*.test.ts` files. Pin the classifier output for a representative set
    of rawActions covering each branch (coordination issue -> build; an
    EXTRA_ACTIONS id -> adminActions; a preconstruction/admin action ->
    adminActions; a normal catalog action -> its module; an unknown action ->
    UNMAPPED_MODULE). Assert `donutModules()` contains "adminActions" immediately
    after "preconstruction" and never contains "modelCoordination". Assert
    `CATEGORY_LABELS` and `CATEGORY_ORDER` equal their known values. Keep it a
    LIGHT pin test (per locked test-depth decision) that fixes the CURRENT output
    so the move cannot silently drift the donut.
  </action>
  <verify>
    <automated>npx vitest run lib/acc/activityClassification.test.ts</automated>
  </verify>
  <done>lib/acc/activityClassification.ts holds the verbatim classifier with corrected `@/`-alias taxonomy imports; the pin test passes and locks classifyActivity/donutModules/CATEGORY_LABELS/CATEGORY_ORDER output.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export from moduleOverrides.ts and repoint the four diag scripts</name>
  <files>app/(dashboard)/access-analysis/moduleOverrides.ts, scripts/diag-activity-coordination.cjs, scripts/diag-activity-types.cjs, scripts/diag-activity-service-xtab.cjs, scripts/diag-activity-module-audit.cjs</files>
  <action>
    Replace the body of `app/(dashboard)/access-analysis/moduleOverrides.ts` with
    a single verbatim re-export of the moved public API so every existing UI
    import path is byte-identical:
    `export { classifyActivity, donutModules, CATEGORY_LABELS, CATEGORY_ORDER, UNMAPPED_MODULE } from "@/lib/acc/activityClassification";`
    Keep a one-line file comment noting the implementation moved to
    `lib/acc/activityClassification.ts` (BND-02) and that this barrel preserves the
    UI import path. Confirm `moduleCounts.ts` (which both imports and re-exports
    `UNMAPPED_MODULE` from here) still resolves — re-exporting `UNMAPPED_MODULE`
    above covers it. Do NOT leave the old taxonomy imports or logic behind in
    `moduleOverrides.ts`; it becomes a pure barrel.

    Repoint the four scripts to require from `lib/` instead of `app/` (the
    require target path changes only; the destructured names stay identical):
    - `scripts/diag-activity-coordination.cjs` line 12:
      `const { classifyActivity, donutModules } = require("../lib/acc/activityClassification.ts");`
    - `scripts/diag-activity-types.cjs` line 13:
      `const { classifyActivity, CATEGORY_LABELS } = require("../lib/acc/activityClassification.ts");`
      and line 14:
      `const { donutModules } = require("../lib/acc/activityClassification.ts");`
    - `scripts/diag-activity-service-xtab.cjs` line 12:
      `const { classifyActivity, donutModules } = require("../lib/acc/activityClassification.ts");`
    - `scripts/diag-activity-module-audit.cjs` line 14:
      `const { classifyActivity, donutModules } = require("../lib/acc/activityClassification.ts");`
    Do not touch the surrounding `require("tsx/cjs")` / dotenv / Prisma lines in
    these scripts.

    Then regenerate repo-map and confirm the 4 moduleOverrides scripts->app
    warnings are gone. Run `npm run repo-map` (regenerates dependency-cruiser.json
    + ast-grep-report.json) then `node scripts/repo-map/check.cjs`. The
    no-scripts-to-app warning count must drop from 6 to 2 (only the two
    spatial-graph build-instance-features edges remain — those are documented-
    deferred, not in scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run repo-map >/dev/null 2>&1; node -e "const j=require('./.tools/repo-map/dependency-cruiser.json');const norm=s=>String(s||'').replaceAll('\\\\','/');let n=0;for(const m of j.modules||[]){for(const d of m.dependencies||[]){for(const r of d.rules||[]){if(r.name==='no-scripts-to-app'&&norm(d.resolved||d.module).includes('moduleOverrides'))n++}}}console.log('moduleOverrides scripts->app warnings:',n);process.exit(n===0?0:1)"</automated>
  </verify>
  <done>`npx tsc --noEmit` passes; `moduleOverrides.ts` is a pure re-export barrel; the four diag scripts require from `lib/acc/activityClassification.ts`; repo-map shows 0 `scripts->app` warnings on the moduleOverrides path (total no-scripts-to-app drops 6→2, the 2 remaining being the documented spatial-graph build-instance-features edges).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — passes (re-export barrel + moved module typecheck; no UI import path changed).
- `npx vitest run lib/acc/activityClassification.test.ts` — green (classifier identity pin test).
- `npm run repo-map` then `node scripts/repo-map/check.cjs` — `no-scripts-to-app` warnings drop from 6 to 2 (0 on the moduleOverrides path; the 2 remaining are the spatial-graph `build-instance-features.ts` edges, documented-deferred in 10-03).
- `rg -n "moduleOverrides" scripts/diag-activity-*.cjs` — no output (scripts no longer reference the app module).
- `rg -n "from \"@/lib/acc/activityClassification\"" app/(dashboard)/access-analysis/moduleOverrides.ts` — present (re-export wired).
- Full `npm test` and the rebuild/donut spot-check are owned by Plan 10-03's final gate sequence.
</verification>

<success_criteria>
ROADMAP Phase 10 success criterion 2 is met: all four `scripts/diag-activity-*.cjs`
import classification logic from `lib/acc/activityClassification.ts`;
`node scripts/repo-map/check.cjs` shows 0 `scripts->app` warnings for the
`moduleOverrides` path. The `/access-analysis` module-activity donut output is
unchanged (the classifier moved verbatim; the UI import path via
`moduleOverrides.ts` is preserved by re-export).
</success_criteria>

<output>
Create `.planning/phases/10-layering-boundary-fixes/10-02-SUMMARY.md` when done.
</output>
