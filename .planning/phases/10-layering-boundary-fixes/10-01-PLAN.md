---
phase: 10-layering-boundary-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/projectClashView.ts
  - lib/server/projectClashView.test.ts
  - server/routers/acc-coordination.ts
  - server/routers/root.ts
  - app/(dashboard)/access-analysis/coordinationActions.ts
autonomous: true
requirements: [BND-01]
must_haves:
  truths:
    - "The /access-analysis Model-Coordination panel clash drill-down returns the same ClashIssue[] on project expand (no visible change)."
    - "ast-grep rule direct-prisma-in-ui returns 0 matches across app/(dashboard) and components/."
    - "coordinationActions.ts no longer imports db from @/server/db; the clash query runs through a tRPC procedure backed by a lib/server helper."
  artifacts:
    - path: "lib/server/projectClashView.ts"
      provides: "Server-only helper owning the per-project clash drill query (accIssue.findMany + author resolution), moved verbatim from coordinationActions.ts"
      contains: "loadProjectClashes"
    - path: "server/routers/acc-coordination.ts"
      provides: "tRPC accCoordination router exposing getProjectClashes(projectId) -> ClashIssue[]"
      contains: "getProjectClashes"
    - path: "lib/server/projectClashView.test.ts"
      provides: "Pin test asserting the clash-query result shape mapped through toClashIssue"
      contains: "ClashIssue"
  key_links:
    - from: "app/(dashboard)/access-analysis/coordinationActions.ts"
      to: "lib/server/projectClashView.ts"
      via: "thin Server Action calls the moved helper server-side (call site mainCharts.tsx unchanged)"
      pattern: "from \"@/lib/server/projectClashView\""
    - from: "server/routers/acc-coordination.ts"
      to: "lib/server/projectClashView.ts"
      via: "procedure delegates to the helper"
      pattern: "projectClashView"
    - from: "server/routers/root.ts"
      to: "server/routers/acc-coordination.ts"
      via: "appRouter registers accCoordination key"
      pattern: "accCoordination"
---

<objective>
BND-01: Remove the direct-Prisma boundary violation in the `/access-analysis`
clash drill-down. Today `app/(dashboard)/access-analysis/coordinationActions.ts`
is a `"use server"` action `loadProjectClashes(projectId)` that imports
`db` from `@/server/db` directly and runs `accIssue.findMany` (+ `accProjectMember`
/ `accDcUser` author resolution, `take: 500`) — the single `direct-prisma-in-ui`
ast-grep match (baseline 1).

Move the query verbatim into a new `lib/server/projectClashView.ts` helper
(server-only, next to the existing `lib/server/coordinationByProjectView.ts`),
expose it via a new `server/routers/acc-coordination.ts` tRPC procedure
`getProjectClashes`, register that router in `server/routers/root.ts`, and keep
`coordinationActions.ts` as a THIN Server Action that calls the helper
server-side. The call site (`mainCharts.tsx` passes `loadProjectClashes` as the
`loadClashes` prop, lazy-load-on-expand) and its signature
`(projectId: string) => Promise<ClashIssue[]>` stay identical.

Purpose: clean the layering boundary (Prisma access belongs in
`server/routers/` or `lib/server/`, not a route Server Action) with ZERO
behavior change for the workshop demo. Per D (BND-01, locked): preferred over
growing `acc-members.ts`; `clash.ts` is NOT a viable home — VERIFIED this
session it is the clash-WIKI/TASK domain (`clashWiki`/`clashTask` via
`createModuleRouter`), unrelated to `accIssue` coordination drill-down.

Output: new `lib/server/projectClashView.ts` + `.test.ts`, new
`server/routers/acc-coordination.ts`, edited `root.ts`, thinned
`coordinationActions.ts`. `direct-prisma-in-ui` → 0 matches.

Workshop impact: invisible, behavior identical. Data truthfulness: no data
change — same Prisma models (`accIssue`, `accProjectMember`, `accDcUser`), same
`select`/`where`/`orderBy`/`take: 500`.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-layering-boundary-fixes/10-CONTEXT.md

# Source under change — read before editing
@app/(dashboard)/access-analysis/coordinationActions.ts
@app/(dashboard)/access-analysis/coordinationClash.ts
@lib/server/coordinationByProjectView.ts
@server/routers/root.ts

<interfaces>
<!-- Verified this session. Use these directly — no codebase exploration needed. -->

coordinationActions.ts CURRENT body (the code to move verbatim into the helper):
- "use server" directive at top; imports: auth from "@/server/auth",
  db from "@/server/db", { toClashIssue, type ClashIssue } from "./coordinationClash".
- export async function loadProjectClashes(projectId: string): Promise<ClashIssue[]>
  - const session = await auth(); if (!session || !projectId) return [];
  - db.accIssue.findMany({ where: { projectId, isCoordination: true },
    select: { displayId, title, description, status, createdBy, confidence,
    coordinationSource, clashValidated, createdAt, rawJson }, orderBy:
    [{ status: "asc" }, { createdAt: "desc" }], take: 500 })
  - creatorIds = unique non-null rows.createdBy; Promise.all([
    db.accProjectMember.findMany({ where: { autodeskId: { in: creatorIds } },
    select: { autodeskId, name, email } }),
    db.accDcUser.findMany({ where: { autodeskId: { in: creatorIds } },
    select: { autodeskId, name, email } }) ]); members first, dcUsers overwrite
    (DC snapshot wins).
  - return rows.map((r) => toClashIssue(r, (id) => infoById.get(id) ?? null));

coordinationClash.ts (PURE, no change) exports:
- interface ClashIssue { displayId, title, description, status, author,
  authorEmail, confidence, source, validated, createdAt, closedAt,
  commentCount, attachmentCount }
- interface ClashIssueRow { ...subset of AccIssue + rawJson }
- function toClashIssue(row, resolveAuthor?) : ClashIssue
- function summarizeAuthors(clashes) : AuthorTally[]

lib/server/coordinationByProjectView.ts (CONVENTION model for the new helper):
- starts with: import "server-only";
- import { db } from "@/server/db";
- exports a typed async loader; uses Prisma groupBy/findMany on accIssue.

server/routers/root.ts (registration shape — VERIFIED):
- imports each router: import { accMembersRouter } from "./acc-members"; etc.
- appRouter = router({ ... accMembers: accMembersRouter, accGraph: accGraphRouter,
  accDcGraph: accDcGraphRouter, accPersonGraph: accPersonGraphRouter });
- export type AppRouter = typeof appRouter;

tRPC builder: procedures use the project trpc helpers — VERIFY the exact
public/protected procedure + input-validation pattern by reading an existing
ACC router (e.g. server/routers/acc-members.ts) and the trpc builder in
server/trpc.ts before authoring the procedure. Match the existing auth/session
pattern (the action currently gates on auth()).

ast-grep rule scope (VERIFIED): direct-prisma-in-ui only scans
app/(dashboard)/**/*.{ts,tsx} and components/**/*.{ts,tsx}. The new lib/server
helper and server/routers file are OUTSIDE the rule's file glob, so importing
@/server/db there is correct and will NOT trip the rule.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the clash drill query into lib/server/projectClashView.ts + pin test</name>
  <files>lib/server/projectClashView.ts, lib/server/projectClashView.test.ts</files>
  <behavior>
    - Given creatorIds resolvable from accProjectMember and accDcUser, when a DC
      user and a project member share the same autodeskId, the DC snapshot name/email
      wins (DC overwrites member).
    - The helper returns rows mapped through toClashIssue, so the output element
      shape matches the ClashIssue interface (displayId, title, status, author,
      authorEmail, validated, createdAt, closedAt, commentCount, attachmentCount).
    - When session is null OR projectId is empty, the helper returns [].
  </behavior>
  <action>
    Create `lib/server/projectClashView.ts` mirroring the convention of the
    sibling `lib/server/coordinationByProjectView.ts`: start the file with
    `import "server-only";`, then `import { auth } from "@/server/auth";`,
    `import { db } from "@/server/db";`, and
    `import { toClashIssue, type ClashIssue } from "@/app/(dashboard)/access-analysis/coordinationClash";`.
    Export `async function loadProjectClashes(projectId: string): Promise<ClashIssue[]>`
    containing the EXACT body currently in
    `app/(dashboard)/access-analysis/coordinationActions.ts` (the auth() gate, the
    `accIssue.findMany` with the same select/where `{ projectId, isCoordination: true }`/
    orderBy `[{ status: "asc" }, { createdAt: "desc" }]`/`take: 500`, the
    creatorIds Promise.all over accProjectMember + accDcUser with DC-wins
    overwrite, and the final `rows.map((r) => toClashIssue(...))`). Do NOT change
    any query field, ordering, limit, or the auth gate — this is a verbatim move.

    Add `lib/server/projectClashView.test.ts` modeled on
    `lib/server/coordinationByProjectView.test.ts`: mock `@/server/auth` (auth
    returns a truthy session) and `@/server/db` so `accIssue.findMany` returns a
    small fixed row set and the member/dcUser finders return overlapping
    autodeskIds. Assert: (a) the returned array elements have the ClashIssue
    shape (assert the literal key set, e.g. displayId/title/status/author/
    authorEmail/validated/createdAt present), (b) DC snapshot name wins over
    member name on a shared autodeskId, (c) null session yields []. Keep it a
    LIGHT pin test on the moved code only (per locked test-depth decision) — no
    live DB, no heavy fixtures.
  </action>
  <verify>
    <automated>npx vitest run lib/server/projectClashView.test.ts</automated>
  </verify>
  <done>lib/server/projectClashView.ts exports loadProjectClashes with the verbatim query; the pin test passes asserting result shape, DC-wins author resolution, and the empty-session guard.</done>
</task>

<task type="auto">
  <name>Task 2: Add acc-coordination tRPC router, register in root.ts, thin the Server Action</name>
  <files>server/routers/acc-coordination.ts, server/routers/root.ts, app/(dashboard)/access-analysis/coordinationActions.ts</files>
  <action>
    First read `server/trpc.ts` and `server/routers/acc-members.ts` to copy the
    exact procedure builder + session/auth pattern this repo uses (do not invent
    a procedure helper name). Create `server/routers/acc-coordination.ts`
    exporting `accCoordinationRouter` with a single procedure
    `getProjectClashes` that accepts an input `projectId: string` (validate with
    the same zod/input pattern existing ACC routers use) and returns
    `loadProjectClashes(input.projectId)` imported from
    `@/lib/server/projectClashView`. Use the existing protected/session
    procedure so the auth gate is enforced at the boundary (the helper also keeps
    its internal auth() guard — leave it; double-gating is safe and preserves the
    return [] contract).

    Register it in `server/routers/root.ts`: add
    `import { accCoordinationRouter } from "./acc-coordination";` alongside the
    other acc router imports, and add `accCoordination: accCoordinationRouter,`
    to the `appRouter = router({ ... })` object (place it next to the other
    `acc*` keys). Do not reorder or remove existing keys.

    Then thin `app/(dashboard)/access-analysis/coordinationActions.ts` to a
    "use server" re-export wrapper: keep the `"use server"` directive and the
    `export async function loadProjectClashes(projectId: string): Promise<ClashIssue[]>`
    signature (mainCharts.tsx imports this name and passes it as the
    `loadClashes` prop — DO NOT change the export name or signature), but its body
    now just `return loadProjectClashes(projectId)` delegating to the
    `@/lib/server/projectClashView` helper. Re-export the `ClashIssue` type from
    `./coordinationClash` if the page imports it from here (VERIFY current
    importers with `rg -n "from \"./coordinationActions\"" app/`). CRITICAL: after
    this edit the file must NOT contain `import { db } from "@/server/db"` or any
    `@/server/db` / `@prisma/client` import — that import is the entire
    `direct-prisma-in-ui` match. The thin action must not import Prisma at all.

    Per locked decision: keep this as a server-side helper call (the simplest
    convention-matching path), NOT a client `useQuery` — `mainCharts.tsx` lazy-
    load-on-expand timing stays identical. The new tRPC procedure exists to
    satisfy the "runs through a tRPC procedure" wording and to be the canonical
    home for any future client-side caller.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx ast-grep scan --config sgconfig.yml --json=pretty | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);const n=a.filter(x=>x.ruleId==='direct-prisma-in-ui').length;console.log('direct-prisma-in-ui matches:',n);process.exit(n===0?0:1)})"</automated>
  </verify>
  <done>`npx tsc --noEmit` passes; `acc-coordination.ts` exports `getProjectClashes` and is registered as `accCoordination` in root.ts; `coordinationActions.ts` is a thin delegating Server Action with the same export signature and no `@/server/db` import; `direct-prisma-in-ui` returns 0 matches.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — passes (no type drift from the move; the helper, router, and thin action all typecheck).
- `npx ast-grep scan --config sgconfig.yml --json=pretty` filtered for `direct-prisma-in-ui` — 0 matches (was 1, at `coordinationActions.ts` L2).
- `npx vitest run lib/server/projectClashView.test.ts` — green (clash-shape pin test).
- `rg -n "@/server/db" "app/(dashboard)/access-analysis/coordinationActions.ts"` — no output (Prisma import removed).
- `rg -n "accCoordination" server/routers/root.ts` — present (router registered).
- Manual spot-check deferred to Plan 10-03's rebuild checkpoint (clash drill-down renders identically on `/access-analysis`).
</verification>

<success_criteria>
ROADMAP Phase 10 success criterion 1 is met: `ast-grep` rule `direct-prisma-in-ui`
returns 0 matches; `coordinationActions.ts` no longer imports `@/server/db`
directly; the clash query runs through the `accCoordination.getProjectClashes`
tRPC procedure backed by `lib/server/projectClashView.ts`. Drill-down behavior on
`/access-analysis` is byte-for-byte unchanged (same query, same auth gate, same
call site).
</success_criteria>

<output>
Create `.planning/phases/10-layering-boundary-fixes/10-01-SUMMARY.md` when done.
</output>
