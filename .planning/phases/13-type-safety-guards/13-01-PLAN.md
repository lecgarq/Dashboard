---
phase: 13-type-safety-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/server/acc-hot-cache.ts
  - app/(dashboard)/users/accGraphFilters.ts
autonomous: true
requirements: [TYPE-01, TYPE-02]

must_haves:
  truths:
    - "TYPE-01: the lean bulkUsers payload's per-project roles/modules are typed never[] at the construction site, so populating them in the lean branch is a compile error"
    - "TYPE-01: an explicit 'lean payload — always empty; use bulkUser / hover-prefetch for per-project data' comment documents the contract at the lean construction site"
    - "TYPE-01: the shared BulkAccUser interface in lib/acc/acc-types.ts is unchanged (full/non-lean producers still type roles/modules as string[])"
    - "TYPE-02: accGraphFilters.ts contains a one-directional subset assert — every SimilarityDimKey must be a valid SimilarityDim — that compiles today (7 of the 12 engine dims)"
    - "TYPE-02: if a filter key drifts out of the engine union, npx tsc --noEmit fails and the error surfaces the drifted key (via Exclude<SimilarityDimKey, SimilarityDim>)"
    - "TYPE-02: the drift assert uses a type-only import of SimilarityDim, so the pure module pulls no runtime deps and the accGraphFilters Vitest suite still passes"
    - "npx tsc --noEmit passes for the whole tree with both guards in place"
  artifacts:
    - path: "lib/server/acc-hot-cache.ts"
      provides: "TYPE-01 lean-return never[] narrowing + explicit lean-payload comment at the leanProjects branch"
      contains: "never[]"
    - path: "app/(dashboard)/users/accGraphFilters.ts"
      provides: "TYPE-02 compile-time subset drift assert replacing the line-15 TODO; type-only import of SimilarityDim"
      contains: "SimilarityDim"
  key_links:
    - from: "app/(dashboard)/users/accGraphFilters.ts"
      to: "lib/acc/userSimilarity.ts"
      via: "type-only import of SimilarityDim (erased at runtime — no cycle, no runtime dep)"
      pattern: "import type.*SimilarityDim.*userSimilarity"
    - from: "lib/server/acc-hot-cache.ts"
      to: "lib/acc/acc-types.ts"
      via: "lean branch returns a local lean variant of BulkAccUser with roles/modules: never[]; shared BulkAccUser untouched"
      pattern: "never\\[\\]"
---

<objective>
Land the two deferred compile-time type guards for milestone v2.1, so two latent
bug classes become hard `npx tsc --noEmit` errors instead of silent failures:

- TYPE-01 (CONCERNS §4.1): the lean `bulkUsers` payload empties each project's
  `roles`/`modules`; today they are typed `string[]`, so a future contributor can
  populate them in the lean branch (breaking the ~15 MB payload contract) or expect
  them populated with no compiler pushback. Type the lean return's `roles`/`modules`
  as `never[]` with an explicit "always empty" comment.
- TYPE-02 (CONCERNS §4.2): `accGraphFilters.ts:15` carries a `TODO` to add a
  compile-time assert if the filter union (`SimilarityDimKey`, 7 members) ever drifts
  out of the canonical engine union (`SimilarityDim`, 12 members). Replace the TODO
  with a one-directional subset assert.

Purpose: contributor safety. No runtime behavior, no workshop-page, data-authority,
UI, or deploy/rebuild change. Types describe an existing runtime contract.

Output: two narrowly edited files — `lib/server/acc-hot-cache.ts` (TYPE-01) and
`app/(dashboard)/users/accGraphFilters.ts` (TYPE-02) — both green under `tsc`.

## VERIFY — CONTEXT location correction (read before Task 1)

CONTEXT.md / ROADMAP / CONCERNS §4.1 assumed the lean `bulkUsers` return type lives in
`server/routers/acc-members.ts`, but flagged the exact procedure name and shape location
as a `VERIFY` item for the planner. Verified against source:

- `acc-members.ts` has NO `bulkUsers` procedure (its procedures are `enrichedUsers`,
  `getProductsForUser`, `getKpiSummary`, `getGovernanceCompliance`,
  `getPermissionRiskScorecard`).
- The lean payload is CONSTRUCTED in `lib/server/acc-hot-cache.ts` →
  `getCachedAccDcBulkUsers(db, { leanProjects: true })`, lean branch at lines 392-402:
  `roles: [] as string[]`, `modules: [] as string[]` inside the project `.map`. The
  function returns `Promise<BulkAccUser[]>`.
- It is EXPOSED via the `bulkUsers` procedure in `server/routers/acc-dc-graph.ts`
  (lines 21-30), which forwards `input` straight to `getCachedAccDcBulkUsers`.

So TYPE-01's edit goes in `lib/server/acc-hot-cache.ts` (the real lean construction +
return site), NOT `acc-members.ts`. The locked TYPE-01 *semantics* are unchanged
(`never[]`, explicit comment, narrow the lean return only, do NOT touch shared
`BulkAccUser`). Only the assumed file path was wrong, and CONTEXT pre-authorized the
planner to resolve it.

Phase dependency: depends on Phase 10 (boundary fixes) — COMPLETE. This is the only plan
in Phase 13, so plan-level `depends_on` is empty.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-type-safety-guards/13-CONTEXT.md

@lib/server/acc-hot-cache.ts
@app/(dashboard)/users/accGraphFilters.ts
@lib/acc/userSimilarity.ts
@lib/acc/acc-types.ts

<interfaces>
<!-- Verified contracts. Executor should use these directly — no codebase exploration needed. -->

TYPE-01 — lean construction site, lib/server/acc-hot-cache.ts (verified):
  - Function: `getCachedAccDcBulkUsers(db, input: { includePermissionContexts?; includePermissionSummary?; includeActivityMix?; leanProjects?: boolean }): Promise<BulkAccUser[]>`
  - Lean branch (lines ~391-402): `if (!leanProjects) return assembled;` then
    `return assembled.map((u) => ({ ...u, projects: u.projects.map((p) => ({ id, name, status, isAdmin, roles: [] as string[], modules: [] as string[] })) }));`
  - This is the site to narrow. `roles`/`modules` here are the per-project lean arrays.

TYPE-01 — shared type, lib/acc/acc-types.ts (verified — DO NOT MODIFY):
  - `interface BulkAccUser` whose nested project shape declares `roles: string[]; modules: string[]`.
  - Non-lean / full producers (`bulkUser`, `enrichedUsers`) populate these. Changing the
    shared interface would break them — narrow ONLY the lean branch's return.

TYPE-02 — filter union, app/(dashboard)/users/accGraphFilters.ts (verified):
  - `export type SimilarityDimKey` = 7 members: "project-members" | "roles" |
    "folder-permissions" | "activity-logs" | "data-coverage" | "last-sign-in" |
    "recent-additions" (lines 19-26).
  - Line 15 holds: `// TODO: if the two unions ever drift, add a compile-time assert helper.`

TYPE-02 — engine union, lib/acc/userSimilarity.ts (verified):
  - `export type SimilarityDim` = 12 members: the 7 above PLUS "admin-tier" |
    "internal-external" | "company-role" | "module-mix" | "firm-affiliation" (lines 20-32).
  - Intentional SUBSET relationship: 7 ⊆ 12. The 5 extra engine dims are deliberately
    NOT exposed as filters (see CONTEXT Deferred Ideas).

Path alias: `@/*` → repo root (tsconfig.json). `strict: true`, `isolatedModules: true`.
Import for the assert: `import type { SimilarityDim } from "@/lib/acc/userSimilarity";`
(type-only — erased at runtime; userSimilarity.ts does not import accGraphFilters.ts, so
no type cycle; `import type` pulls no runtime deps into the pure filter module).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TYPE-01 — type the lean bulkUsers return roles/modules as never[]</name>
  <files>lib/server/acc-hot-cache.ts</files>
  <action>
    In `getCachedAccDcBulkUsers`, narrow ONLY the lean-branch return (the
    `if (!leanProjects) return assembled;` / `return assembled.map(...)` block, lines
    ~391-402) so each lean project's `roles` and `modules` are typed `never[]` instead of
    `string[]`. Implements TYPE-01 (CONCERNS §4.1).

    Concrete approach (executor discretion on exact alias name/syntax, semantics fixed):
    Introduce an import-light local type alias near the function — e.g.
    `LeanBulkAccUser` derived from the imported `BulkAccUser` via `Omit`/intersection so
    its nested `projects[]` element keeps every other field but redeclares
    `roles: never[]` and `modules: never[]`. Annotate the lean `.map` result as
    `LeanBulkAccUser[]` (or return it as such). Because `never[]` is assignable to
    `string[]`, `LeanBulkAccUser[]` remains assignable to the function's declared
    `Promise<BulkAccUser[]>` return, so the non-lean branch and all current callers keep
    compiling unchanged. Write the per-project literals as `roles: [] as never[]` and
    `modules: [] as never[]`.

    Add an explicit comment on this lean narrowing with exactly this wording:
    lean payload — always empty; use bulkUser / hover-prefetch for per-project data

    Hard constraints (locked decisions):
    - Do NOT modify the shared `BulkAccUser` interface in lib/acc/acc-types.ts — it is
      also produced (populated) by the full/non-lean path; narrow the lean return only.
    - Use `never[]`, NOT `readonly never[]` (over-strict — blocks reassignment and risks
      consumer touch-ups beyond this phase) and NOT comment-only (must be compiler-enforced).
    - `never[]` keeps readers (`.map`, `.length`) compiling and blocks assigning/pushing
      non-empty values into the lean arrays.
    - No new files, packages, or dependencies. Do NOT edit acc-members.ts or
      acc-dc-graph.ts — the procedure forwards this function's output; guarding the
      construction/return site here is the in-scope, consumer-safe realization.
    - Do NOT touch /users/spatial-graph or any UI; this is server-lib type work only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Negative-case proof (then revert): temporarily set one lean-branch literal to a
    non-empty value (e.g. `roles: ["x"]`) or append `.push("x")` to the lean roles array;
    re-run `npx tsc --noEmit` and confirm it errors with "Type 'string' is not assignable
    to type 'never'"; then revert to `[] as never[]`.
  </verify>
  <done>
    The lean branch of `getCachedAccDcBulkUsers` returns per-project `roles`/`modules`
    typed `never[]` with the exact "lean payload — always empty…" comment; shared
    `BulkAccUser` is unchanged; `npx tsc --noEmit` passes; the negative case errors and
    has been reverted.
  </done>
</task>

<task type="auto">
  <name>Task 2: TYPE-02 — add one-directional subset drift assert in accGraphFilters.ts</name>
  <files>app/(dashboard)/users/accGraphFilters.ts</files>
  <action>
    Replace the line-15 `TODO` ("if the two unions ever drift, add a compile-time assert
    helper") with a compile-time assert that every `SimilarityDimKey` is a valid
    `SimilarityDim`. Implements TYPE-02 (CONCERNS §4.2).

    Add a type-only import at the top: `import type { SimilarityDim } from "@/lib/acc/userSimilarity";`
    (type-only is mandatory — it is erased at runtime and cannot reintroduce the documented
    cross-module cycle or pull the similarity engine's runtime deps into this pure module,
    which is imported by both client UI and pure Vitest tests).

    Then add a one-directional SUBSET assert. Recommended shape (executor discretion on
    exact `satisfies`-vs-conditional form as long as semantics are subset and failure
    surfaces the drifted key):
      - a conditional-type alias `_DimKeysAreValid` = `SimilarityDimKey extends SimilarityDim`
        ? `true` : `["DRIFT", Exclude<SimilarityDimKey, SimilarityDim>]`
      - a value-level guard `const _dimKeyGuard: _DimKeysAreValid = true;`
    This compiles today (7 ⊆ 12). If a filter key is renamed/removed so it no longer maps
    to the engine union, `_DimKeysAreValid` resolves to the `["DRIFT", ...]` tuple, the
    `= true` assignment fails to compile, and the error type names the drifted key via
    `Exclude<SimilarityDimKey, SimilarityDim>` (self-explaining, per CONTEXT "Specific Ideas").

    Prefix the guard const with `_` (and/or `void` it) so it does not trip unused-var lint.
    Update or remove the surrounding TODO note so the comment block now describes the live
    assert rather than a deferred one.

    Hard constraints (locked decisions):
    - One-directional subset only. Do NOT use bidirectional equality — the engine has 5
      dims the filter union deliberately omits; equality would fail immediately and pressure
      a 5-filter UI scope expansion (rejected, wrong for a type-safety phase).
    - Do NOT expose the 5 engine-only dims as filters; do NOT change `SimilarityDimKey`
      membership, `DEFAULT_FILTERS`, or `nodeMatchesFilters` behavior.
    - Keep the module import-light: only a `import type` is added. No runtime import, no
      new files/packages.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Then: `npx vitest run accGraphFilters` (the pure suite app/(dashboard)/users/accGraphFilters.test.ts)
    must still pass — proves the type-only import added no runtime dependency.
    Negative-case proof (then revert): temporarily rename one `SimilarityDimKey` member to
    a value absent from `SimilarityDim` (e.g. "roles" → "roles-XYZ"); re-run
    `npx tsc --noEmit` and confirm the `_dimKeyGuard` assignment errors (drift detected);
    then revert.
  </verify>
  <done>
    The line-15 TODO is replaced by a live one-directional subset assert backed by a
    type-only `SimilarityDim` import; `npx tsc --noEmit` passes and `vitest run
    accGraphFilters` is green; the negative case (drifted key) errors and has been reverted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Compile-time-only type edits. No untrusted input crosses any boundary; no network, runtime, schema, or payload change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | lib/server/acc-hot-cache.ts lean return type | accept | Type-only narrowing; `never[]` is assignable to `string[]` so no runtime/serialization behavior changes. Gate: `npx tsc --noEmit` + negative-case proof. |
| T-13-02 | Tampering | app/(dashboard)/users/accGraphFilters.ts assert + type-only import | accept | `import type` is erased at runtime; userSimilarity.ts does not import this module (no cycle). Gate: `vitest run accGraphFilters` confirms no runtime dep pulled in. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (no new files, packages, or dependencies — locked). Nothing to vet. |
</threat_model>

<verification>
Primary and sufficient gate (both criteria): `npx tsc --noEmit` passes with both guards
in place. No rebuild / `:3000` restart — there is no runtime change (do NOT run
`next build` or restart Task Scheduler).

Supporting gates:
- `npx vitest run accGraphFilters` (pure suite) stays green — confirms the TYPE-02
  type-only import pulled no runtime deps into the pure filter module. (Optionally `npm test`
  for the full suite.)
- Negative-case proof for EACH guard (encoded per task): break it, confirm `tsc` errors,
  then revert — proves each guard actually bites.

Scope / Dashboard guardrails:
- `git status --short` after edits shows only `lib/server/acc-hot-cache.ts` and
  `app/(dashboard)/users/accGraphFilters.ts` changed (plus this plan's docs).
- `lib/acc/acc-types.ts` `BulkAccUser` UNCHANGED.
- Zinc theme untouched, no new WebGL, `/users/spatial-graph` not modified.
</verification>

<success_criteria>
- TYPE-01: the lean `bulkUsers` return (`getCachedAccDcBulkUsers` lean branch in
  `lib/server/acc-hot-cache.ts`) types per-project `roles`/`modules` as `never[]` with the
  explicit "lean payload — always empty; use bulkUser / hover-prefetch for per-project data"
  comment; shared `BulkAccUser` unchanged; `npx tsc --noEmit` passes.
- TYPE-02: `app/(dashboard)/users/accGraphFilters.ts` contains a one-directional subset
  compile-time assert (filter union ⊆ engine union) using a type-only `SimilarityDim`
  import; `npx tsc --noEmit` confirms it compiles; the pure Vitest suite still passes.
- Both negative-case proofs error then revert cleanly. No runtime/UI/data change.

## Dashboard self-check
- Context: read STATE.md, ROADMAP.md (Phase 13), REQUIREMENTS.md (TYPE-01/02),
  13-CONTEXT.md, CONCERNS.md §4.1/§4.2, and verified source files acc-hot-cache.ts,
  acc-dc-graph.ts, acc-members.ts, accGraphFilters.ts, userSimilarity.ts, acc-types.ts,
  tsconfig.json. CLAUDE.md + SKILL.md briefing applied.
- Evidence: lean payload construction verified at acc-hot-cache.ts:392-402 (returns
  `BulkAccUser[]`); procedure at acc-dc-graph.ts:21-30; SimilarityDimKey 7-member
  (accGraphFilters.ts:19-26, TODO line 15); SimilarityDim 12-member (userSimilarity.ts:20-32);
  `@/*`→root + strict + isolatedModules (tsconfig).
- Constraints: never[] not readonly; subset not equality; type-only import; shared
  BulkAccUser untouched; no new files/packages; spatial-graph out of scope.
- Gates: `npx tsc --noEmit` (sufficient, no rebuild) + `vitest run accGraphFilters` +
  per-guard negative-case proof.
- VERIFY: TYPE-01 target file corrected from the CONTEXT-assumed `server/routers/acc-members.ts`
  to the verified `lib/server/acc-hot-cache.ts` (lean construction/return site). CONTEXT
  flagged this path as a planner VERIFY item; semantics unchanged. Otherwise: none.
</success_criteria>

<output>
Create `.planning/phases/13-type-safety-guards/13-01-SUMMARY.md` when done.
</output>
