---
phase: 16-folderterrain-monolith-split
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
  - "app/(dashboard)/access-analysis/components/useFolderPermissionTerrainCamera.ts"
  - "app/(dashboard)/access-analysis/components/terrainViewModel.ts"
  - "app/(dashboard)/access-analysis/components/TerrainStage.tsx"
  - "app/(dashboard)/access-analysis/components/TerrainControls.tsx"
autonomous: false
requirements: [SPLIT-02]

must_haves:
  truths:
    - "FolderPermissionTerrain.test.tsx is FULLY green — including the 2 previously-failing pre-existing WIP cases — and byte-identical (zero test-file edits)."
    - "The FolderPermissionTerrain component is still exported from ../components/FolderPermissionTerrain with an unchanged props signature (projects, initial, loadTerrain, loadOverview?, singleProject?)."
    - "TEST-02 golden masters (folderPermissionTerrainView.test.ts) remain green; full npm test passes."
    - "No single resulting component-side file exceeds ~400 lines."
    - "npx tsc --noEmit exits 0; /access-analysis and /template-mty render identically (owner visual parity)."
  artifacts:
    - path: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      provides: "Thin composition shell — state orchestration + top-level JSX; still exports FolderPermissionTerrain"
      contains: "export function FolderPermissionTerrain"
      max_lines: 400
    - path: "app/(dashboard)/access-analysis/components/useFolderPermissionTerrainCamera.ts"
      provides: "Interaction hooks: useCamera (Revit-style orbit/pan/zoom/pivot), useGrowth, prefersReducedMotion, clamp"
      max_lines: 400
    - path: "app/(dashboard)/access-analysis/components/terrainViewModel.ts"
      provides: "Pure view-model transforms + shared types/consts: buildView, activeDims, defaultPivot, fitScale, crossProjectTiers, mixHex, Theme/SceneEntry/StageView/Mode/Metric/Hover/Picked"
      max_lines: 400
    - path: "app/(dashboard)/access-analysis/components/TerrainStage.tsx"
      provides: "Presentational SVG scene: SceneStage, SceneLayer, FadingScene, TerrainDefs, Backdrop, Compass, Tooltip, ToolButton"
      max_lines: 400
    - path: "app/(dashboard)/access-analysis/components/TerrainControls.tsx"
      provides: "Presentational chrome: ModeToggle, ProjectSelect, ProjectMultiSelect, useOfficeGroups, TierLegend, DetailPanel, TierBreakdown"
      max_lines: 400
  key_links:
    - from: "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"
      to: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      via: "import { FolderPermissionTerrain } from ../components/FolderPermissionTerrain — path + export + props unchanged"
      pattern: "import \\{ FolderPermissionTerrain \\}"
    - from: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      to: "app/(dashboard)/access-analysis/components/TerrainStage.tsx"
      via: "shell renders <SceneStage> from TerrainStage and controls from TerrainControls"
      pattern: "from \"\\./TerrainStage\""
    - from: "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx"
      to: "app/(dashboard)/access-analysis/folderTerrain.ts"
      via: "still imports scene/camera builders (buildCameraScene, buildStackedScenes, projectCamera, easeCamera, Camera, ...) via the barrel from Plan 16-01"
      pattern: "from \"\\.\\./folderTerrain\""
---

<objective>
Split the 1,045-line `app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx`
into a data/interaction hook, a pure view-model transform module, and two presentational
view modules — each under ~400 lines — while `FolderPermissionTerrain.tsx` remains a thin
composition shell that still exports `FolderPermissionTerrain`. This is SPLIT-02 of REF-01:
behavior-preserving. `/access-analysis` and `/template-mty` (the same component drives both
via the `singleProject` prop) must render identically.

The filename `components/FolderPermissionTerrain.tsx` MUST stay in place because
`FolderPermissionTerrain.test.tsx` imports `{ FolderPermissionTerrain }` from
`../components/FolderPermissionTerrain`. Renaming/moving it, or changing the props signature,
would break the pinning test and violate the byte-identical-test contract.

The presentational layer (~585 lines) exceeds the ~400 ceiling on its own, so it is split into
TWO view files (TerrainStage.tsx + TerrainControls.tsx). This is a justified deviation from the
roadmap's literal "one thin view" wording: the hard gate is "no file > ~400 lines", which two
view files satisfy where one oversized view would not.

Purpose: isolate the camera interaction, the pure scene composition, and the SVG rendering so
each is independently reviewable — with zero behavior change.
Output: 4 new component-side modules + a thin shell; FolderPermissionTerrain.test.tsx fully green
(incl. the 2 pre-existing WIP fixes) and byte-identical.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The component being split (read in full — the internal seams are marked below):
@app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx

# The pinning test that MUST end fully green + byte-identical (currently has 2 pre-existing WIP failures):
@app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx

# The barrel this component imports from (produced by Plan 16-01 — export surface is unchanged):
@app/(dashboard)/access-analysis/folderTerrain.ts

<interfaces>
<!-- Extraction seams within FolderPermissionTerrain.tsx (verified by direct read). Move VERBATIM. -->

useFolderPermissionTerrainCamera.ts (interaction hooks — "use client"):
  - clamp (module const), DragMode type
  - useCamera(viewport)  → { cam, dragRef, dragMode, setDragMode, handlers, wheelZoom, setPivotCell, framePivot, resetTo, tweenTo }
  - prefersReducedMotion()  (also consumed by FadingScene → export it)
  - useGrowth(key): number
  Imports from ../folderTerrain: Camera, projectCamera, easeCamera, HOME_YAW, HOME_PITCH, MIN_PITCH, MAX_PITCH

terrainViewModel.ts (PURE — no React, no JSX):
  - Types: Mode, Metric, Hover, Picked, Theme, SceneEntry, StageView
  - Consts: PLANE_GAP, SLAB_MAXBAR, VIEW_H, MANY_FOLDERS
  - mixHex(hex, target, t)
  - centerPivot, activeDims, defaultPivot, fitScale, buildView, crossProjectTiers
  Imports from ../folderTerrain: buildCameraScene, buildSharedAxes, buildStackedScenes, Camera,
    FolderTerrainData, TerrainCell, TerrainScene, HOME_PITCH, STEP

TerrainStage.tsx (presentational scene — "use client"):
  - SceneStage, SceneLayer, FadingScene, TerrainDefs, Backdrop, Compass, Tooltip, ToolButton
  Imports: types/consts from ./terrainViewModel; useCamera type + prefersReducedMotion from
    ./useFolderPermissionTerrainCamera; colorForRank, topGradientId, TIER_GRADIENTS, projectCamera,
    TerrainScene from ../folderTerrain

TerrainControls.tsx (presentational chrome — "use client"):
  - ModeToggle, useOfficeGroups, ProjectSelect, ProjectMultiSelect, TierLegend, DetailPanel, TierBreakdown
  Imports: Mode/Metric/TerrainCell types from ./terrainViewModel or ../folderTerrain;
    colorForRank, tierTextColor, TIER_LEGEND, TIER_COLORS, TerrainProjectOption from ../folderTerrain;
    officeLabel from ../projectGroups

FolderPermissionTerrain.tsx (thin shell — "use client", keeps the export):
  - export function FolderPermissionTerrain(props) — SAME props: { projects, initial, loadTerrain, loadOverview?, singleProject? }
  - Owns: theme derivation, mode/hover/picked/cache/selected/overview/loading state, the two
    lazy-load effects, viewport ResizeObserver, useCamera() wiring, dataKey/growth, the view memo
    (calls buildView), reframe effects, and the top-level JSX that composes <ModeToggle>/<ProjectSelect>/
    <SceneStage>/<DetailPanel>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish a green baseline — fix the 2 pre-existing WIP failures</name>
  <files>app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx</files>
  <action>
    BEFORE any refactor, run `npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"`
    and capture the exact 2 failing cases and their assertion messages (STATE.md records these as
    unrelated uncommitted branch WIP; Phase 15 confirmed them unchanged/not-newly-broken).
    Diagnose the ROOT CAUSE against the current component source — do NOT edit the test file, and
    do NOT paper over a failure by weakening an assertion. The likely source is the uncommitted WIP
    already present in FolderPermissionTerrain.tsx (or a helper it calls). Apply the SMALLEST fix that
    makes those 2 cases pass without changing any other rendered behavior. If the root cause is genuinely
    outside this component's scope (e.g. a shared util under a different owner), STOP and record precisely
    why in the SUMMARY rather than expanding scope — but the default expectation is to fix them here, as
    this plan touches that surface directly (STATE.md "resolve early in v2.2").
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"</automated>
  </verify>
  <done>FolderPermissionTerrain.test.tsx is 100% green (all cases pass, 0 failing); the test file itself is unedited (`git diff --name-only -- "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"` prints nothing).</done>
</task>

<task type="auto">
  <name>Task 2: Extract the camera hook + the pure view-model</name>
  <files>app/(dashboard)/access-analysis/components/useFolderPermissionTerrainCamera.ts, app/(dashboard)/access-analysis/components/terrainViewModel.ts, app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx</files>
  <action>
    Create `useFolderPermissionTerrainCamera.ts` ("use client") and move VERBATIM: the `clamp`
    const, the `DragMode` type, the `useCamera` hook, `prefersReducedMotion`, and `useGrowth`.
    Export `useCamera`, `useGrowth`, `prefersReducedMotion`, `clamp`, and `DragMode` (prefersReducedMotion
    is consumed by FadingScene in Task 3). Add its imports from "../folderTerrain" (Camera, projectCamera,
    easeCamera, HOME_YAW, HOME_PITCH, MIN_PITCH, MAX_PITCH).

    Create `terrainViewModel.ts` (PURE — no React import, no JSX) and move VERBATIM: the Mode/Metric/
    Hover/Picked types, the Theme/SceneEntry/StageView interfaces, the PLANE_GAP/SLAB_MAXBAR/VIEW_H
    consts, the MANY_FOLDERS const (currently local inside buildView — hoist it to module scope
    unchanged), `mixHex`, `centerPivot`, `activeDims`, `defaultPivot`, `fitScale`, `buildView`, and
    `crossProjectTiers`. Add its imports from "../folderTerrain" (buildCameraScene, buildSharedAxes,
    buildStackedScenes, Camera, FolderTerrainData, TerrainCell, TerrainScene, HOME_PITCH, STEP).

    Update `FolderPermissionTerrain.tsx` to import `useCamera`, `useGrowth` (and `prefersReducedMotion`
    if still referenced) from "./useFolderPermissionTerrainCamera", and the types/consts/`buildView`/
    `activeDims`/`defaultPivot`/`fitScale`/`crossProjectTiers`/`mixHex` from "./terrainViewModel".
    The presentational components (SceneStage etc.) still live inline in the shell for now — they will
    reference the imported hook types/pure helpers. Change NO behavior, values, or JSX. The tree MUST
    type-check and the test MUST stay green at task end.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx"</automated>
  </verify>
  <done>useFolderPermissionTerrainCamera.ts + terrainViewModel.ts exist with the moved code; the shell imports from both; `npx tsc --noEmit` is 0; FolderPermissionTerrain.test.tsx stays fully green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Extract presentational view (TerrainStage + TerrainControls), finalize thin shell, verify + owner parity + commit</name>
  <files>app/(dashboard)/access-analysis/components/TerrainStage.tsx, app/(dashboard)/access-analysis/components/TerrainControls.tsx, app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx</files>
  <action>
    Extract the presentational components into TerrainStage.tsx + TerrainControls.tsx (verbatim) and
    reduce FolderPermissionTerrain.tsx to the thin shell that still exports FolderPermissionTerrain with
    the identical props. Then run the automated gates listed under what-built (tsc; full npm test with
    FolderPermissionTerrain.test.tsx fully green + TEST-02 green; both pinning tests byte-identical;
    wc -l ≤400; export-present check; explicit-path staging proof), confirm zero visible behavior change
    on /access-analysis and /template-mty, and commit by explicit path only. Resume only on owner approval.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx" "lib/server/__tests__/folderPermissionTerrainView.test.ts"</automated>
  </verify>
  <done>Owner confirms /access-analysis + /template-mty render + behave identically; all five component-side files ≤ ~400 lines; FolderPermissionTerrain.test.tsx fully green & byte-identical; TEST-02 green; component export + props unchanged; commit landed.</done>
  <what-built>
    The presentational view is extracted into TerrainStage.tsx (SceneStage, SceneLayer, FadingScene,
    TerrainDefs, Backdrop, Compass, Tooltip, ToolButton) and TerrainControls.tsx (ModeToggle,
    useOfficeGroups, ProjectSelect, ProjectMultiSelect, TierLegend, DetailPanel, TierBreakdown), each
    moved VERBATIM; `FolderPermissionTerrain.tsx` is reduced to the thin composition shell that still
    `export`s `FolderPermissionTerrain` with the identical props signature and imports SceneStage from
    ./TerrainStage plus the controls from ./TerrainControls.

    Before requesting verification, the executor MUST have completed this extraction AND confirmed
    automatically:
    (1) `npx tsc --noEmit` exits 0;
    (2) `npm test` passes — specifically FolderPermissionTerrain.test.tsx is fully green AND TEST-02
        (folderPermissionTerrainView.test.ts) is green;
    (3) both pinning tests are byte-identical —
        `git diff --name-only -- "app/(dashboard)/access-analysis/__tests__/FolderPermissionTerrain.test.tsx" "lib/server/__tests__/folderPermissionTerrainView.test.ts"` prints nothing;
    (4) no resulting file exceeds ~400 lines —
        `wc -l "app/(dashboard)/access-analysis/components/FolderPermissionTerrain.tsx" "app/(dashboard)/access-analysis/components/useFolderPermissionTerrainCamera.ts" "app/(dashboard)/access-analysis/components/terrainViewModel.ts" "app/(dashboard)/access-analysis/components/TerrainStage.tsx" "app/(dashboard)/access-analysis/components/TerrainControls.tsx"`
        shows each ≤ 400;
    (5) `export function FolderPermissionTerrain` is still present in components/FolderPermissionTerrain.tsx;
    (6) the git index contains ONLY the diagnosed WIP-fix file(s) from Task 1 plus the 5 component-side
        files of this plan — stage by explicit path only (never `git add -A` / `git add .`), then run
        `git diff --cached --name-only` and confirm no unrelated WIP, no .planning deletions, and no
        /users/spatial-graph files appear.
    Then commit: `refactor(16): split FolderPermissionTerrain.tsx into hook/view-model/view modules (SPLIT-02)`
    (fold the Task-1 baseline fix into this commit or a preceding explicit-path commit).
  </what-built>
  <how-to-verify>
    1. This is a source-only refactor. If the running :3000 build predates the change, run
       `npx tsc --noEmit` FIRST, then rebuild + Task-Scheduler restart on :3000 per the deploy
       sequence (never a branch merge).
    2. Open http://localhost:3000/access-analysis → Folder Permission Terrain: exercise Single /
       Compare / Overview; orbit, pan, zoom, wheel-zoom; Orbit/Pan tool buttons; Frame + Reset;
       click a cell (details card, cross-project row in compare); hover (bar lift + tooltip);
       project single-select + multi-select (search, office groups, "All"); tier legend; inherited
       dimming; compass tilt read-out. Everything must behave and look exactly as before.
    3. Open http://localhost:3000/template-mty and confirm its single-project terrain (same component,
       singleProject mode) renders and behaves identically.
    4. Confirm NO visual or interaction difference vs. pre-phase on either page.
  </how-to-verify>
  <resume-signal>Type "approved" if both pages render and behave identically, or describe any difference.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-component relocation. No new data sources, network calls, auth paths, or schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | FolderPermissionTerrain rendered output + props surface | mitigate | tsc + FolderPermissionTerrain.test.tsx (DOM golden assertions) + TEST-02 + owner visual parity prove behavior + props unchanged |
| T-16-02-SC | Tampering | npm installs | accept | No package installs in this plan — pure file moves; nothing added to package.json/lockfile |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (gates the rebuild — `next build` typechecks the whole tree incl. tests).
- `npm test` green; specifically FolderPermissionTerrain.test.tsx fully green (all cases, incl. the 2
  formerly-failing WIP cases) and folderPermissionTerrainView.test.ts (TEST-02) green.
- `git diff --name-only` on both pinning test files prints nothing (byte-identical — no test edits).
- `wc -l` on all five component-side files: each ≤ ~400.
- `export function FolderPermissionTerrain` still present at components/FolderPermissionTerrain.tsx; props unchanged.
- Owner visual + interaction parity on /access-analysis AND /template-mty (same component drives both).
- Repo-map check OPTIONAL and skipped: extraction is co-located under
  `app/(dashboard)/access-analysis/components/`, adds no new lib↔app or app↔server edges, and the public
  component export path is unchanged — import boundaries do not meaningfully change.
</verification>

<success_criteria>
- FolderPermissionTerrain.tsx is a thin shell (≤ ~400 lines) still exporting FolderPermissionTerrain with unchanged props.
- useFolderPermissionTerrainCamera.ts + terrainViewModel.ts + TerrainStage.tsx + TerrainControls.tsx each ≤ ~400 lines.
- FolderPermissionTerrain.test.tsx fully green + byte-identical; TEST-02 green; full npm test passes.
- `npx tsc --noEmit` exits 0.
- /access-analysis and /template-mty render + behave identically (owner-approved).
- Commit staged by explicit path only, verified via `git diff --cached --name-only`.
</success_criteria>

<output>
Create `.planning/phases/16-folderterrain-monolith-split/16-02-SUMMARY.md` when done.
</output>

## Dashboard Self-Check

- **Context:** Loaded STATE.md (incl. the 2-WIP-failure fold-in directive + explicit-path commit hazard),
  ROADMAP.md (Phase 16 success criteria), REQUIREMENTS.md (SPLIT-02), and the real source:
  FolderPermissionTerrain.tsx, FolderPermissionTerrain.test.tsx, folderTerrain.ts. No CONTEXT.md /
  RESEARCH.md exist for this phase (intentional).
- **Evidence:** Extraction seams + the exact component export/props signature verified by direct read of
  the 1,045-line component. Test import surface (`import { FolderPermissionTerrain } from ../components/...`
  and `type { FolderTerrainData, TerrainProjectOption } from ../folderTerrain`) read from the test file.
  Shared-component fact (`singleProject` prop → /template-mty) grounded in the component's own prop comment.
- **Constraints applied:** Behavior-preserving; byte-identical tests (no test edits); original filename +
  props kept; presentational split into 2 files to honor the ~400 ceiling; co-located (no lib/acc migration);
  no new WebGL (SVG terrain only, unchanged); zinc theme untouched; /users/spatial-graph not touched;
  2 pre-existing WIP failures fixed first (Task 1); explicit-path commit; tsc before any rebuild.
- **Gates:** tsc --noEmit; npm test (FolderPermissionTerrain.test.tsx + TEST-02); byte-identical git-diff;
  wc -l ≤400; export-present check; owner visual+interaction parity on /access-analysis + /template-mty.
  Repo-map check skipped (justified). Depends on 16-01 (settled folderTerrain barrel).
- **VERIFY:** the exact identity/root-cause of the 2 pre-existing FolderPermissionTerrain.test.tsx WIP
  failures is resolved at execution time (Task 1 runs the test first, then fixes) — it cannot be pinned at
  plan time without running the suite. All other paths, symbols, and seams are grounded in files read this session.
