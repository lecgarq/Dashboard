---
phase: 28.1-spatial-graph-regression-debug
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
autonomous: false
requirements: [PERF-04]
must_haves:
  truths:
    - "The +42% time-to-graph regression (8822 ms vs the Phase-24 6193.2 ms baseline, frozen NEXT_PUBLIC_ACC_GRAPH_TEST=1 path) is localized to a NAMED stage of the nav→first-render boot with a measured per-stage number BEFORE any fix is proposed — no guessing."
    - "buildCatalogTargets/buildCatalogWeights are NOT the cause (already exonerated by source: buildCatalogWeights is a single O(n) loop per dim; buildCatalogTargets over the widened ~19-dim set is well under ~150 ms for 22,279 nodes). The discovery pass confirms or overturns this with a real number, not the assumption."
    - "The fix removes the localized regression on the frozen path OR, only if root-cause proves the cost is intrinsic to the shipped widened surface, it is documented-and-accepted with the owner — default is to drive median back under the ~6812 ms gate (baseline +10%)."
    - "The fix touches no data source, loader, Prisma table, or npm dependency; cosmos.gl stays 3.3.0; zinc theme + no-new-WebGL preserved; PERF-02 frozen-handle invariant (GraphCanvas.test.ts) stays green."
  artifacts:
    - path: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      provides: "The localized-and-fixed boot stage (exact edit determined by the discovery task)"
      contains: "performance"
  key_links:
    - from: "tests/e2e/spatial-graph-baseline.spec.ts"
      to: "app/(dashboard)/users/access-analysis/graphTestBridge.ts"
      via: "median time-to-graph-rendered re-measured by the identical committed script vs 6193.2 ms"
      pattern: "getRenderedNodeCount"
---

<objective>
Root-cause and fix PERF-04: the frozen-path `/users/spatial-graph` time-to-graph-rendered
regressed +2628.8 ms (+42.4%) vs the Phase-24 baseline (6193.2 ms), measured apples-to-apples
in 28-02 (same cosmos 3.3.0, same script, same 22,279 nodes; only delta = Phases 25/26/27).

The metric spans the WHOLE client boot (nav-start → first cosmos render with >0 nodes), so the
cost could sit in bundle parse/hydration, the post-`users` build effect
(`AccessAnalysisShell.tsx:591-730`), the heavier Phase-26 shell mount, or late post-freeze
state settling. Source analysis has already EXONERATED the widened target-building
(`buildCatalogTargets`/`buildCatalogWeights`) as the +2.6 s driver. This plan is
discovery-first: instrument the boundaries, build+measure ONCE to localize, THEN make the
smallest fix — obeying the Iron Law (no fix before a measured root cause).
</objective>

<context>
@.planning/STATE.md
@.planning/phases/28.1-spatial-graph-regression-debug/28.1-CONTEXT.md
@.planning/phases/28-performance-closeout-verification/28-02-SUMMARY.md
@.planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md
@app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
@app/(dashboard)/users/access-analysis/graphTestBridge.ts
@scripts/measure-spatial-graph-baseline.cjs
@tests/e2e/spatial-graph-baseline.spec.ts
</context>

<tasks>

<task type="discovery">
  <name>Task 1: Instrument boot boundaries + measure once to localize the +2.6 s</name>
  <files>app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx</files>

  Add temporary `performance.mark`/`performance.measure` (test-flag-gated, so zero prod cost)
  at each boundary of the boot critical path, then read them from the baseline spec's
  `page.evaluate` OR the browser trace. Boundaries to fence (minimum set):
    a. `users` (bulkUsers) resolved  →  effect entry
    b. `buildGraphNodesFromUsers` done
    c. `buildStructuralDimensions` + `curatedSliderDimensions` + `groupByDimensions` done
    d. `buildCatalogTargets` + `buildCatalogWeights` done
    e. `createStaticLayer` done → setPhysics/setFeatures/setCatalog
    f. first cosmos render with getRenderedNodeCount()>0 (the metric's end)

  Build ONCE to isolated `:3100` (`:3000`-safe; `NEXT_DIST_DIR=.next-e2e` exempt):
  ```powershell
  $env:NEXT_PUBLIC_ACC_GRAPH_TEST="1"; $env:NEXT_DIST_DIR=".next-e2e"; npx next build --webpack
  $env:NEXT_DIST_DIR=".next-e2e"; npx next start -p 3100
  ```
  Run the measurement once (`node scripts/measure-spatial-graph-baseline.cjs`, or a single
  `page.evaluate` reading the marks). Record the per-stage deltas. The gap between first-paint
  (956 ms) and graph-rendered (8822 ms) is ~7.9 s — attribute it across a–f.

  DELIVERABLE (resolves the VERIFY:): a named dominant stage with a millisecond figure. This
  task proposes NO fix. Verify live `.next/BUILD_ID` unchanged; kill `:3100`.

  <verify>Per-stage timing table recorded in 28.1-02-SUMMARY.md; dominant stage named with ms</verify>
</task>

<task type="auto">
  <name>Task 2: Fix the localized stage (smallest complete diff)</name>
  <files>app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx</files>

  Apply the narrowest fix for the stage Task 1 named. Likely shapes, chosen by evidence — do
  NOT pre-commit to one:
    - If it is a synchronous CPU build blocking first paint → defer/idle-schedule or memoize it
      off the render-blocking path (mirror 28-01's idle guard; keep the frozen path correct).
    - If it is a heavier Phase-26 shell subtree mounting synchronously before the graph → lazy
      or defer that subtree so the graph paints first (the catalog wall is already meant to be
      lazy — verify it did not regress onto the critical path).
    - If it is redundant recompute across the post-freeze re-renders (the same churn that trips
      PERF-03) → stabilize/memoize so the boot does the work once.
    - If Task 1 proves the cost is intrinsic to the widened surface and irreducible → STOP, take
      it to the owner as documented-accept (per the CONTEXT open question), do not force a fix.

  Remove all temporary instrumentation from Task 1 unless a mark is genuinely worth keeping
  (test-flag-gated only). No data/loader/dep/table change.

  <verify>npx tsc --noEmit</verify>
  <verify>npx vitest run "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"</verify>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Re-measure identical PERF-04 gate on :3100</name>
  <files>scripts/measure-spatial-graph-baseline.cjs</files>

  Rebuild `:3100` with the fix, run the IDENTICAL committed
  `scripts/measure-spatial-graph-baseline.cjs` (N=5, frozen path). PASS gate: median
  time-to-graph-rendered ≤ ~6812 ms (baseline 6193.2 + 10%). Record the N=5 table.
  Verify `.next/BUILD_ID` (`kZKWbfeAqYW1R4bYrUx2U`) unchanged; `:3000/login` 200; kill `:3100`.

  If the fix lands the median under the gate → PERF-04 met. If it materially improves but does
  not fully clear the gate, present the number to the owner for accept-or-continue (do not
  weaken the gate or mix paths).

  <verify>median time-to-graph-rendered ≤ ~6812 ms over N=5 on :3100</verify>
</task>

</tasks>

<verification>
- Task 1 names the dominant stage with a measured ms figure BEFORE Task 2 edits anything.
- `npx tsc --noEmit` clean; `GraphCanvas.test.ts` (PERF-02 frozen invariant) green.
- All temporary instrumentation removed (or test-flag-gated with zero prod effect).
- Task 3: N=5 median ≤ ~6812 ms on the identical script, or an honest documented-accept with
  the owner; BUILD_ID unchanged; `:3100` killed.
</verification>

<success_criteria>
- The regression is localized to a named stage with a number (truth 1), target-building
  confirmed/overturned with real data (truth 2).
- The frozen-path median is driven back under the +10% gate, or documented-accepted with the
  owner as intrinsic (truth 3).
- No data/dep/WebGL/theme change; frozen-handle invariant intact (truth 4).
</success_criteria>
