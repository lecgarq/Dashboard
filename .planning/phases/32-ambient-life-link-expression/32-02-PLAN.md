---
phase: 32-ambient-life-link-expression
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - app/(dashboard)/users/access-analysis/similarityWeb.ts
  - app/(dashboard)/users/access-analysis/similarityWeb.test.ts
  - app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx
  - app/(dashboard)/users/access-analysis/SimilarityWebOverlay.test.tsx
  - app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
  - tests/e2e/phase32-ambient.spec.ts
autonomous: true
requirements: [LIFE-05]

must_haves:
  truths:
    - "Existing real-distribution normalized edge strength maps at exact 1/3 and 2/3 boundaries into weak, medium, and strong bands whose opacity and width rise monotonically"
    - "The ambient 18k-edge web remains palette/band batched; it is not converted to one styled stroke per ambient edge and no second canvas or renderer is added"
    - "Selected curves render after ambient curves and hovered incident curves render last; both inherit their edge strength band plus a small priority width/alpha lift"
    - "During Catalog/Group-by morphs the ambient web follows current rendered positions at about 30Hz and eases only to 25% of its normal opacity, then returns over about 180ms; it never disappears"
    - "Reduced motion snaps link opacity to the same rest/morph targets while preserving selected and hovered draw priority"
  artifacts:
    - "app/(dashboard)/users/access-analysis/similarityWeb.ts"
    - "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx"
    - "app/(dashboard)/users/access-analysis/similarityWeb.test.ts"
  key_links:
    - from: "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"
      to: "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx"
      via: "normalized strength bands plus ambient position version"
      pattern: "strength|band|getPositionVersion"
    - from: "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx"
      to: "app/(dashboard)/users/access-analysis/similarityWeb.ts"
      via: "shared band style and morph-opacity helpers"
      pattern: "strengthBand|linkBandStyle|morphOpacityTarget"
---

<objective>
Deliver LIFE-05 by making the existing Canvas2D similarity web express the real
strength distribution through three deliberate bands and remain legibly attached to
moving nodes throughout catalog morphs.
</objective>

<context>
@.planning/phases/32-ambient-life-link-expression/32-CONTEXT.md
@.planning/phases/32-ambient-life-link-expression/32-01-PLAN.md
@app/(dashboard)/users/access-analysis/similarityWeb.ts
@app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx
@app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
@app/(dashboard)/users/access-analysis/ambientMotion.ts
@DESIGN.md
</context>

## Tasks

1. **Band the existing honest strength signal**
   - Add pure weak `[0,1/3)`, medium `[1/3,2/3)`, and strong `[2/3,1]`
     classification plus restrained projector-legible width/alpha styles.
   - Return one compact band buffer beside the existing palette bucket buffer;
     preserve min-max normalization and endpoint-hue blending. Unit-pin exact
     boundaries, monotonic style ordering, equal-score handling, and palette reuse.

2. **Batch-aware overlay and visible morph transition**
   - Batch ambient curves by palette + three band widths. Keep focused curves small
     and transient, inheriting the global band when present and score-derived band
     for authoritative fallback matches.
   - Replace the morph target of zero with a 25% floor and a dt-based ~180ms settle;
     snap under reduced motion. Consume the ambient layer's position version so the
     overlay redraws when nodes move but parks again in Tier 2/static idle.
   - Preserve ambient < selected < hovered ordering and the existing selected-focus
     ambient dimming; add focused helper/component checks for floor, timing, band
     props, and canvas presence.

3. **Complete visual and production verification**
   - Extend the Phase-32 browser gate to drive a real Group-by strength control,
     assert the similarity canvas remains mounted through the morph, ambient pauses,
     and the page returns without console errors; keep reduced-motion focus/link state
     functional.
   - Run the full Phase-32 focused suite, TEST-01/02/03 engineering gates, PERF-02
     frozen-handle test, typecheck, repo-map, and `impeccable detect` over the changed
     TSX. Use the isolated production build for the final full-data gate.

## Verification

- `npx vitest run "app/(dashboard)/users/access-analysis/ambientMotion.test.ts" "app/(dashboard)/users/access-analysis/similarityWeb.test.ts" "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.test.tsx" "app/(dashboard)/users/access-analysis/GraphCanvas.test.ts"`
- `npx tsc --noEmit`
- `node scripts/repo-map/check.cjs`
- `node scripts/uat/run-engineering-gates.cjs --static-only`
- `node .agents/skills/impeccable/scripts/detect.mjs --json "app/(dashboard)/users/access-analysis/GraphCanvas.tsx" "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx" "app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx"`
- Isolated `:3100`: `npx playwright test tests/e2e/phase32-ambient.spec.ts --project=chromium`

## Phase completion gates

- Audit LIFE-03/LIFE-05 and all four ROADMAP success criteria against shipped code
  and recorded full-data outputs; write `32-VERIFICATION.md` with exact FPS/tier/
  node/edge evidence and any truthful full-mode failure.
- Roll durable summary debt to `.planning/codebase/CONCERNS.md`.
- With all required gates green and `autoDeploy:true`, run `$lecg-ship`, probe
  `/users/spatial-graph` and `/users/access-analysis`, then advance to Phase 33.

## Success criteria

All LIFE-05 truths above hold, the edge web remains visible and position-attached
during real morphs, the full Phase-32 gates pass, and impeccable reports no unresolved
findings on the changed visual surface.
