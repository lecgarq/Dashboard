---
phase: 39
plan: "39-03"
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - server/routers/acc-dc-graph.ts (remove graphSnapshot/instanceEmbedding/instanceNeighbors/similarityEdges; keep dataVersion/bulkUsers/bulkUser)
  - lib/server/graphSnapshotCompression.ts + test (delete)
  - lib/acc/embedding/neighborPayload.ts + test (delete)
  - lib/acc/embedding/similarityEdgeSet.ts + test (delete)
  - app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx (delete)
  - app/(dashboard)/users/access-analysis/GraphCanvas.tsx + test (delete — 2D/3D switch)
  - app/(dashboard)/users/access-analysis/GraphCanvas3D.tsx + test (delete)
  - app/(dashboard)/users/access-analysis/GraphInteractions.tsx (delete)
  - app/(dashboard)/users/access-analysis/NeighborMatchesPanel.tsx + test (delete)
  - app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx + test (delete)
  - app/(dashboard)/users/access-analysis/graphTestBridge.ts (delete — superseded by activityTestBridge)
  - 3D lasso module(s) (lasso3d*) (delete)
  - graphNodesFromCompactPayload site (delete — locate exact file at execution)
  - scripts/build-instance-features.ts (delete)
  - scripts/compute_instance_embeddings.py + its pytest (delete)
  - scripts/dc-daily-ingest.cjs (remove instance-embedding block, lines ~139-146)
  - orphaned tests of deleted files (delete alongside)
  - kept-but-unmounted files needing minimal import severing (edit only if tsc breaks)
autonomous: true
requirements: [ACT-03]
must_haves:
  truths:
    - "acc-dc-graph router exports exactly dataVersion, bulkUsers, bulkUser — the surviving /users + charts consumers stay green"
    - "No file imports graphSnapshotCompression, neighborPayload, similarityEdgeSet, GraphCanvas3D, lasso3d, or NeighborMatchesPanel"
    - "Nightly dc-daily-ingest.cjs no longer builds instance features/embeddings; node --check clean"
    - "Kept-for-Ph40 files (CatalogSliderSidebar, DimensionSlider, GroupByControls, RightPanelStack, physics layer, catalog machinery) remain on disk and COMPILE"
    - "AccInstanceEmbedding table untouched (drop = milestone-close item); person-graph path untouched"
    - "TEST-01/02/03 pins green; full npm test has no NEW failures vs pre-phase baseline"
  artifacts:
    - .planning/phases/39-activity-universe-swap/39-03-SUMMARY.md (records expected e2e breakage list for Phase 41)
  key_links:
    - pattern: "bulkUsers"
      in: server/routers/acc-dc-graph.ts
    - pattern: "dataVersion"
      in: server/routers/acc-dc-graph.ts
---

# 39-03 — Instance path retirement sweep (ACT-03)

## Objective

Clean-diff removal of the user×project instance data path: procedures, codec,
kNN/similarity chain, 3D mode, embedding pipeline, nightly block. Kept-for-Ph40
slider machinery stays on disk and compiling.

## Context (@ paths)

- @.planning/phases/39-activity-universe-swap/39-CONTEXT.md (decisions 1, 2)
- Consumer audit (this phase's planning evidence): retiring chains are consumed
  ONLY by AccessAnalysisShell.tsx (itself retiring) + their own tests.
  Survivors verified: /users directory (bulkUsers lean + dataVersion),
  /access-analysis charts AuthorProfileDrawer (bulkUsers full),
  UserProfilePanel (bulkUser), person-graph (zero overlap).
- @server/routers/acc-dc-graph.ts
- @scripts/dc-daily-ingest.cjs lines ~139-146 (instance-embedding try/catch)

## Tasks

1. **Router prune** — remove the four retiring procedures + their imports from
   `acc-dc-graph.ts`. Keep dataVersion/bulkUsers/bulkUser + hot-cache import.
2. **Delete the chains** (file + co-located/matching tests together):
   codec (`graphSnapshotCompression`), compact-payload node builder
   (`graphNodesFromCompactPayload` — rg for the defining file first),
   kNN (`neighborPayload`, `similarityEdgeSet`, `NeighborMatchesPanel`),
   similarity web (`SimilarityWebOverlay`, `similarityWeb.ts` if instance-only),
   3D (`GraphCanvas3D`, lasso3d modules, `GraphCanvas.tsx` switch),
   orchestration (`AccessAnalysisShell.tsx`, `GraphInteractions.tsx`,
   `graphTestBridge.ts`). Before EACH deletion: `rg` the symbol/file for
   remaining importers; a kept-file hit → minimal import-severing edit on the
   kept file (never delete kept-for-Ph40 machinery).
3. **Pipeline retirement** — delete `scripts/build-instance-features.ts`,
   `scripts/compute_instance_embeddings.py` + its pytest; remove the
   dc-daily-ingest.cjs instance block; `node --check scripts/dc-daily-ingest.cjs`.
4. **Test sweep** — delete tests that exist solely to pin deleted files
   (GraphCanvas.test, GraphCanvas3D.test, NeighborMatchesPanel.test,
   SimilarityWebOverlay.test, shell-mount tests like AccessAnalysisPage.test if
   they mount the deleted shell — inspect first; if AccessAnalysisPage.tsx
   survives for chart composition, adapt not delete).
5. **Gate sweep** — `npx tsc --noEmit`; `node scripts/repo-map/check.cjs`;
   full `npm test` — TEST-01/02/03 green, record any pre-existing failures
   separately; record the EXPECTED e2e spec breakage list
   (acc-dc-graph.spec, lasso, cluster-labels, ambient — Phase 41 re-baseline)
   in the SUMMARY without chasing them.

## Verification gates

Full sweep in task 5. No new npm-test failures; kept files compile.

## Success criteria

Truths above; diff is deletions + the minimal severing edits only.
