---
phase: 35
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - app/(dashboard)/users/access-analysis/similarityWeb.ts
  - app/(dashboard)/users/access-analysis/similarityWeb.test.ts
  - app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx
  - app/(dashboard)/users/access-analysis/SimilarityWebOverlay.test.tsx
  - app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx
  - app/(dashboard)/users/access-analysis/GraphCanvas.test.ts
  - app/(dashboard)/users/access-analysis/graphTestBridge.ts
  - tests/e2e/phase32-ambient.spec.ts
autonomous: true
requirements: [REND-01, REND-03]
must_haves:
  truths:
    - "The measured winner renders the full similarity web without the Phase-33 main-thread Canvas2D raster ceiling"
    - "Weak/medium/strong boundaries and monotone width/alpha remain exact; ambient renders below complete selected and hovered focus links"
    - "Morphing retains the 25% opacity floor, reduced motion snaps, and the flag-OFF frozen Cosmos handle never starts or reheats simulation"
    - "Any shipped decimation is deterministic, zoomed-out ambient-only, and restores the full settled close web; selected/hovered links are never decimated"
  artifacts:
    - "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx (winning renderer controller)"
    - "app/(dashboard)/users/access-analysis/similarityWeb.ts (renderer-neutral native buffers/focus composition)"
    - "app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx (minimal installed-Cosmos link controls)"
    - "tests/e2e/phase32-ambient.spec.ts (full-data winner evidence)"
  key_links:
    - pattern: "setLinkWidths|linkOpacity|curvedLinks"
      in: "app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx"
    - pattern: "morphOpacityTarget|resolveFocusEdges|linkBandStyle"
      in: "app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx"
---

# Plan 35-02: Implement and verify the measured renderer winner

## Objective

Replace the main-thread Canvas2D similarity-web raster path with the Plan-01 winner,
keeping the existing data/model boundary and every Phase-32 semantic contract.

## Context

@.planning/phases/35-similarity-web-renderer-rethink/35-CONTEXT.md
@.planning/phases/35-similarity-web-renderer-rethink/35-BASELINE.md
@app/(dashboard)/users/access-analysis/AccessAnalysisShell.tsx
@app/(dashboard)/users/access-analysis/GraphCanvas.tsx
@app/(dashboard)/users/access-analysis/GraphInteractions.tsx
@app/(dashboard)/users/access-analysis/SimilarityWebOverlay.tsx
@app/(dashboard)/users/access-analysis/similarityWeb.ts
@app/(dashboard)/users/access-analysis/GraphCanvas2D.tsx
@app/(dashboard)/users/access-analysis/graphTestBridge.ts

## Tasks

1. Extend `similarityWeb.ts` only where the winning renderer needs renderer-neutral
   typed buffers or focus composition. Reuse current indexed edges, palette, exact band
   constants, `resolveFocusEdges`, `morphOpacityTarget`, and `stepOpacity`; add focused
   unit pins for ordering, full-focus preservation, and deterministic decimation only if
   decimation actually ships.
2. Add the minimum winning-renderer controls to `GraphCanvas2DHandle` and its Cosmos
   instance. For the expected native winner: curved links, per-link widths/colors,
   global opacity, and an atomic update path; all runtime config changes use
   `setConfigPartial`, and no simulation method is called.
3. Convert `SimilarityWebOverlay` in place into the winning imperative controller so
   `AccessAnalysisShell` and the rollback flag keep their existing composition seam.
   Delete the Canvas2D element/raster loop when native wins; do not keep parallel live
   renderers. Ambient, selected, then hovered buffer order is explicit. Synthesized
   selected-match edges remain complete.
4. Extend `GraphCanvas.test.ts`, `SimilarityWebOverlay.test.tsx`, and
   `similarityWeb.test.ts` to catch config/buffer drift, exact visual semantics,
   reduced-motion snap, and zero simulation start/reheat.
5. Extend the existing test bridge/spec minimally to name the active renderer and record
   a final full-data >=10s sample. Run rest/focus/morph browser checks on both route
   aliases, plus the Phase-34 re-baselined graph suite.
6. Run the visual design gate (`impeccable detect`) on the changed spatial-graph surface;
   record any unavailable tool as `VERIFY:` rather than inventing a pass.
7. Run focused Vitest, `npx tsc --noEmit`, `node scripts/repo-map/check.cjs`, and scoped
   diff checks. Commit only the exact implementation/test paths in coherent groups.

## Verification gates

- Focused Vitest: `similarityWeb.test.ts`, `SimilarityWebOverlay.test.tsx`, and the
  GraphCanvas native-link tests all green.
- PERF-02 frozen-handle invariant remains green; no `start`/`unpause` call on flag-OFF.
- `npx tsc --noEmit` clean.
- `node scripts/repo-map/check.cjs` at or below the recorded unrelated baseline.
- Isolated `:3100` full-data browser evidence: renderer identity, exact node/link counts,
  >=10s FPS/tier sample, rest/focus/morph contract, both route aliases, no console errors.
- Phase-34 `acc-dc-graph.spec.ts` green.

## Success criteria

The measured winner is the only live similarity-web renderer, main-thread raster cost is
materially reduced, and REND-03 remains exact and test-pinned.
