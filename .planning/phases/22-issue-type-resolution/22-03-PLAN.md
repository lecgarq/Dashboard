---
phase: 22-issue-type-resolution
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - app/(dashboard)/access-analysis/components/IssueTypeChart.tsx
  - app/(dashboard)/access-analysis/__tests__/IssueTypeChart.test.tsx
  - app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
autonomous: false
requirements: [ISSUE-05]

must_haves:
  truths:
    - "User sees an issues-by-type horizontal-bar chart on the Projects tab, stacked directly below Issues by status, with every bar labeled by a human-readable type name — never a raw GUID anywhere"
    - "Unresolved GUIDs render as a ranked 'Unknown type' bar and null issueTypeId as a ranked 'No type set' bar; 'Other (N types)' expands in place with a collapse-back link"
    - "Clicking a type bar drills to a ranked per-project list with resolved project names ('Unknown project' fallback, never a GUID)"
    - "The caption live-computes 'N of M type GUIDs resolved to names' plus the existing issue-fetch coverage line — zero hardcoded figures; a never-backfilled state shows an explicit run-the-script notice instead of 100% Unknown bars"
    - "Panel totals reconcile with the status donut above (same population, same picker-only filtering via selected)"
  artifacts:
    - path: "app/(dashboard)/access-analysis/components/IssueTypeChart.tsx"
      provides: "Horizontal-bar top-N + Other chart with local drill, expand-in-place, honest captions"
      min_lines: 120
    - path: "app/(dashboard)/access-analysis/__tests__/IssueTypeChart.test.tsx"
      provides: "Component tests incl. the self-grep no-cross-filter-bus convention"
      min_lines: 60
    - path: "app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx"
      provides: "Third issue panel mounted below IssueStatusChart"
      contains: "IssueTypeChart"
  key_links:
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx"
      via: "filteredIssueTypeRows memo threaded as a prop"
      pattern: "filteredIssueTypeRows"
    - from: "app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx"
      to: "app/(dashboard)/access-analysis/components/IssueTypeChart.tsx"
      via: "rows + coverageProjects props"
      pattern: "IssueTypeChart"
    - from: "app/(dashboard)/access-analysis/components/IssueTypeChart.tsx"
      to: "app/(dashboard)/access-analysis/issueTypeCounts.ts"
      via: "summarizeIssueType with expand-in-place re-invocation"
      pattern: "summarizeIssueType"
---

<objective>
ISSUE-05 user-visible: build `IssueTypeChart.tsx` (horizontal bars, top-10 + expandable
Other, local per-project drill, honest coverage captions), mount it as the third issue
panel on the Projects tab, and close the phase with the single owner `:3100`
production-preflight checkpoint.

Purpose: completes the issue story on `/access-analysis` — coverage → volume over time →
status → type — with real ACC type names, never GUIDs.
Output: chart + tests, wiring in ProjectsTabPanel/AccessAnalysisCharts, owner approval.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-issue-type-resolution/22-CONTEXT.md
@app/(dashboard)/access-analysis/components/ProvisionedModulesChart.tsx
@app/(dashboard)/access-analysis/components/PermissionLevelChart.tsx
@app/(dashboard)/access-analysis/components/IssueStatusChart.tsx
@app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
@app/(dashboard)/access-analysis/__tests__/ProvisionedModulesChart.test.tsx
</context>

<interfaces>
From `app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx` (verified wiring —
the fetch already exists, only a memo + prop threading is new):
```ts
// line 298-332: issueFunnelData state + issueFunnelFetchedRef lazy fetch on tab === "projects" — UNCHANGED
// line 360-362 (the memo pattern to clone):
const filteredIssueStatusRows: IssueFunnelStatusRow[] = useMemo(
  () => filterRowsBySelection(issueFunnelData?.statusRows ?? [], selected),
  [issueFunnelData, selected],
);
// line 552-555: props threaded to <ProjectsTabPanel ... />
```

From `app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx`: props include
`loadIssueFunnel?`, `issueFunnelLoading?`, `filteredIssueStatusRows?`; each issue panel is
`{loadIssueFunnel ? <Reveal><PremiumSurface variant="base" ...><SectionHeader .../>{issueFunnelLoading ? <DonutPanelSkeleton/> : <Chart/>}</PremiumSurface></Reveal> : null}`.
The "Issues by status" panel ends at line 120; Model Coordination follows. The placeholder
comment at line 141 is stale relative to the locked decision — the new panel goes BELOW
IssueStatusChart and ABOVE Model Coordination (CONTEXT.md: "Full-width third panel stacked
below IssueStatusChart"); delete the line-141 placeholder comment when mounting.

From plan 22-02 (new, available at this wave):
```ts
// lib/server/issueFunnelView.ts
export interface IssueFunnelTypeRow { projectId: string; projectName: string; issueTypeId: string | null; typeName: string | null; count: number; }
// app/(dashboard)/access-analysis/issueTypeCounts.ts
export function summarizeIssueType(rows: IssueFunnelTypeRow[], topN?: number): IssueTypeSummary;
```

From `app/(dashboard)/access-analysis/__tests__/ProvisionedModulesChart.test.tsx:93-100`:
the self-grep test reads the component SOURCE TEXT and fails if the cross-filter-bus token
strings appear ANYWHERE in the file — including doc comments (21.1-02 and 21.1-03 both hit
this trap; do not write those tokens even to explain their absence).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build IssueTypeChart.tsx with tests</name>
  <files>app/(dashboard)/access-analysis/components/IssueTypeChart.tsx, app/(dashboard)/access-analysis/__tests__/IssueTypeChart.test.tsx</files>
  <action>
Create `IssueTypeChart.tsx` ("use client") cloning `ProvisionedModulesChart.tsx`'s
horizontal-bar EChart shape (yAxis category / xAxis value, `useState<string|null>` drill
toggle, capped drill list with "+N more" overflow, data-testid on the drilldown block) plus
`PermissionLevelChart.tsx`'s expand/collapse-Other pattern (local `expanded` state re-invokes
`summarizeIssueType(rows, expanded ? rows.length : DEFAULT_TOP_N)`, collapse-back link that
rank-checks whether an in-flight drill survives the collapse — 20.1-07 convention).

Props: `{ rows: IssueFunnelTypeRow[]; coverageProjects: IssueCoverageProjectRow[] }`.

Behavior:
- Bars = `summary.buckets` (top-10 + Other). Accent: a single teal ramp (e.g. `#2dd4bf`
  base) — distinct from the amber timeline and the status donut's semantic map; "Unknown
  type" and "No type set" get muted zinc tones; Other gets the muted rollup color
  (executor's discretion on exact hexes, resolved theme colors only).
- Click a named/unknown/none bar → per-project drill from `summary.projectsByBucket`
  (ranked, resolved names). The Other bar's click is a guaranteed no-op (21.1-03 convention).
- Caption combines: "N of M type GUIDs resolved to names" from
  `summary.resolvedTypeGuids`/`summary.totalTypeGuids` (live-computed, never hardcoded) +
  the existing `deriveIssueCoverageCaption(coverageProjects)` line (import from
  `../issueFunnelCounts`).
- Never-backfilled state: if `rows.length > 0` AND `summary.totalTypeGuids > 0` AND
  `summary.resolvedTypeGuids === 0`, render an explicit notice — "Type names not yet
  backfilled — run scripts/acc-issue-types-backfill.cjs" — instead of the bars.
- Genuinely-empty state: `rows.length === 0` → the coverage-aware "No issues for this view"
  empty-state convention from `IssueStatusChart.tsx`.

Test file clones `ProvisionedModulesChart.test.tsx`'s structure: renders bars from a fixture;
drill opens on bar click with ranked project rows; Other click is a no-op; expand-Other
re-renders all buckets with a collapse link; "Unknown type" and "No type set" both render as
bars; never-backfilled notice appears for an all-unresolved fixture; the self-grep case
asserting the three cross-filter-bus token strings do NOT appear in the component source
(build the expected strings by concatenation inside the test so the test file itself stays
grep-clean, matching the existing convention).
  </action>
  <verify>
    <automated>npx vitest run "app/(dashboard)/access-analysis/__tests__/IssueTypeChart.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Chart component green on 7+ cases including the self-grep pin, unmounted, zinc-theme-compliant, no new WebGL, no new deps.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the chart into the Projects tab</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, app/(dashboard)/access-analysis/components/ProjectsTabPanel.tsx</files>
  <action>
`AccessAnalysisCharts.tsx` (2 additions, nothing else):
1. New memo directly below `filteredIssueStatusRows` (line ~362), cloning its shape:
   `const filteredIssueTypeRows: IssueFunnelTypeRow[] = useMemo(() => filterRowsBySelection(issueFunnelData?.typeRows ?? [], selected), [issueFunnelData, selected]);`
   Picker-only filtering via `selected` — locked decision, never `sliceFilteredProjectIds`.
2. Thread `filteredIssueTypeRows={filteredIssueTypeRows}` to `<ProjectsTabPanel />` (line ~555).
Do NOT touch the lazy-fetch effect, `mainCharts.tsx`, or any eager `Promise.all`.

`ProjectsTabPanel.tsx`:
1. Add optional prop `filteredIssueTypeRows?: IssueFunnelTypeRow[]` (import the type from
   `@/lib/server/issueFunnelView`).
2. Mount the new panel directly BELOW the "Issues by status" panel and ABOVE Model
   Coordination, cloning the sibling panel block exactly (`loadIssueFunnel` presence gate,
   `Reveal` > `PremiumSurface variant="base"` > `SectionHeader` + `DonutPanelSkeleton`
   while `issueFunnelLoading`):
   - Title: "Issues by type". Subtitle states the question (20.1-07 owner preference), e.g.
     "What kinds of issues do we actually have? Every fetched issue by its resolved ACC
     type name — honest buckets for unresolved and untyped issues."
   - `<IssueTypeChart rows={filteredIssueTypeRows ?? []} coverageProjects={filteredIssueCoverageProjects} />`
3. Delete the stale line-141 placeholder comment; update the file-top doc comment's tab map
   to include "Issues by type".
4. Update the doc comment only as needed — no change to the shipped timeline/status/
   coordination blocks (locked: do not touch 21-04's layout other than appending the sibling).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Full suite green (baseline 2477 passed + all new Phase 22 cases, zero regressions); chart renders on the Projects tab behind the existing lazy fetch; `git diff --name-only` shows only this plan's 4 files across both tasks; `mainCharts.tsx` untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner production-preflight checkpoint on :3100</name>
  <files>none — verification-only task (isolated `.next-uat-22` build dist is throwaway)</files>
  <action>
Run the automated preflight (21-04/21.1-04 shape, `:3000` untouched), then present the
owner verification steps below and wait for the resume signal:
1. `npx tsc --noEmit` → 0 errors.
2. Webpack production build to an isolated dist (`.next-uat-22`), start on `:3100`
   (same commands the 21.1-04 checkpoint used; NEVER touch the `:3000` Task Scheduler service).
3. Sanity-load `/access-analysis`, open the Projects tab, confirm the new panel renders
   before handing off to the owner.
  </action>
  <verify>
    <automated>npx tsc --noEmit (preflight gate; the checkpoint itself is human — owner verdict recorded in SUMMARY)</automated>
  </verify>
  <done>Owner types "approved" (or all raised issues are closed as gap-closure items); `:3000` untouched throughout.</done>
  <what-built>The complete Phase 22 stack: AccIssueType lookup table backfilled from APS (22-01), loadIssueFunnel type cut + summarizeIssueType transform (22-02), and the Issues-by-type chart wired into the Projects tab (this plan).</what-built>
  <how-to-verify>
Owner verification steps:
1. Open `http://localhost:3100/access-analysis` → Projects tab.
2. Confirm the panel order: Issue data coverage → Issues over time → Issues by status →
   **Issues by type** → Model Coordination.
3. Issues by type shows horizontal bars with real ACC type names (e.g. Quality,
   Coordination) — no GUID anywhere on the panel, tooltips, or drill.
4. "Unknown type" and/or "No type set" bars appear if the data has them (22-01's SUMMARY
   records whether a live unresolved GUID exists); they read as honest buckets, not errors.
5. Click a type bar → ranked per-project list with real project names; click "Other (N
   types)" → nothing happens; use the expand link → all types render in place with a
   collapse-back link.
6. Caption shows "N of M type GUIDs resolved to names" + the issue-fetch coverage line;
   numbers look live, not canned.
7. Panel total reconciles with the Issues-by-status donut above (same population).
8. Change the global project picker → the type chart follows the selection like its siblings.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (any change requests become gap-closure items, 21.1-04 pattern)</resume-signal>
</task>

</tasks>

<verification>
- `npm test` full suite green; `npx tsc --noEmit` clean — both BEFORE the preflight build.
- Live-page verification (not just tsc): names render, no raw GUID, Unknown/No-type buckets
  present, Other expands in place, totals reconcile with the status donut (roadmap SC 3/4).
- If 22-01 recorded a live unresolved GUID, the "Unknown type" bar is verified against it on
  the live page (roadmap SC 4); otherwise the unit-test fallback path stands as evidence.
- Zinc theme, resolved ECharts colors, no new WebGL, no new npm deps, `/users/spatial-graph`
  untouched, explicit-path commits with `git diff --cached --name-only` proof.
- `mainCharts.tsx` has zero diff for this phase (fan-out guardrail).
</verification>

<success_criteria>
ISSUE-05 complete and owner-approved: issues-by-type renders as a top-N + Other breakdown
with every issueTypeId resolved to a human-readable name via the ISSUE-04 lookup table,
honest fallbacks for unresolved/null ids, and live-computed coverage captions. Phase 22's
roadmap success criteria 1-5 all TRUE. `:3000` untouched until the owner asks for a deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/22-issue-type-resolution/22-03-SUMMARY.md`
including the owner's verbatim checkpoint verdict and any gap-closure items.
</output>
