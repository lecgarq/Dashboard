---
phase: 01-foundation-schema-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]

must_haves:
  truths:
    - "Prisma schema contains 8 new ACC v2.0 models plus SyncMeta"
    - "Composite indexes on AccActivity exist from day one (no retroactive index addition)"
    - "Initial migration applied successfully on local dev database"
    - "Migration is additive-only — zero column drops or renames"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "AccProject, AccProjectMember, AccRole, AccProjectRole, AccFolder, AccFolderPermission, AccActivity, AccDataConnectorJob, SyncMeta models"
      contains: "model AccActivity"
    - path: "prisma/migrations/"
      provides: "Generated migration SQL for additive ACC v2.0 schema"
      contains: "CREATE TABLE \"AccActivity\""
  key_links:
    - from: "AccActivity"
      to: "Postgres index"
      via: "@@index([autodeskId, createdAt(sort: Desc)])"
      pattern: "createdAt\\(sort: Desc\\)"
    - from: "AccProjectMember"
      to: "AccProject"
      via: "relation onDelete: Cascade"
      pattern: "@relation\\(fields: \\[projectId\\]"
---

<objective>
Add the v2.0 relational data layer to Prisma: 8 ACC domain models plus `SyncMeta`. Generate and apply the additive migration locally so Phase 2 extraction and downstream sync orchestration have real tables to write to.

Purpose: Every downstream phase reads/writes these tables. Composite indexes on `AccActivity` must exist from day one (SCHEMA-02) so Phase 3 ingest is never blocked on retroactive index creation.
Output: Updated `prisma/schema.prisma` + generated migration SQL.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-schema-sync/01-CONTEXT.md
@.planning/phases/01-foundation-schema-sync/01-RESEARCH.md
@prisma/schema.prisma

<interfaces>
<!-- Existing Prisma setup — extracted from prisma/schema.prisma + server/db.ts -->
<!-- The repo uses @prisma/adapter-pg (driver adapters mode). Schema ends with AccGraphLayoutCache. -->
<!-- New section goes UNDER existing ACC sections — strictly additive. -->

Prisma version: ^7.7.0 (driver-adapters / @prisma/adapter-pg)
Composite-index sort syntax (Prisma 5+/7.x): @@index([field, otherField(sort: Desc)])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 8 ACC v2.0 models + SyncMeta to prisma/schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <action>
Append a new `// ============ MODULE: ACC V2.0 RELATIONAL LAYER ============` section AFTER the existing `AccGraphLayoutCache` model. Add these 9 models verbatim from `01-RESEARCH.md` Pattern 3:

  - `AccProject` — id (no `b.` prefix), accountId, name, jobNumber, type, status default "active", createdAt, updatedAt. Indexes: accountId, status. Relations: members[], roles[], folders[].
  - `AccProjectMember` — cuid id, projectId FK Cascade, autodeskId, email, name, status, companyName, phone, addedOn, lastSignIn, projectAdmin, executive, products Json, syncedAt, roles[]. `@@unique([projectId, autodeskId])`. Indexes: email, projectId, autodeskId.
  - `AccRole` — id (APS role ID), accountId, name, memberCount default 0, syncedAt, projectRoles[]. Index: accountId.
  - `AccProjectRole` — cuid id, projectId FK Cascade, roleId FK Cascade, memberId FK SetNull, docsAccessLevel, projectAdminAccessLevel. `@@unique([projectId, roleId, memberId])`. Indexes: projectId, roleId.
  - `AccFolder` — id (folder URN, `b.` prefix preserved), projectId FK Cascade, parentId, name, fullPath, syncedAt, permissions[]. Indexes: projectId, parentId.
  - `AccFolderPermission` — cuid id, folderId FK Cascade, roleId, actions String[], permType, syncedAt. `@@unique([folderId, roleId])`. Index: folderId.
  - `AccActivity` — cuid id, autodeskId, projectId nullable, action, service, tool, details, createdAt. **Indexes (SCHEMA-02 critical):** `@@index([autodeskId, createdAt(sort: Desc)])`, `@@index([projectId, createdAt(sort: Desc)])`, `@@index([action])`. Sort: Desc is mandatory per SCHEMA-02.
  - `AccDataConnectorJob` — cuid id, requestId @unique, status default "pending", serviceGroups String[], dateRange, startedAt default now(), completedAt, downloadUrl, errorMessage. Indexes: status, startedAt.
  - `SyncMeta` — id String @id (values "quick" | "deep" — no enum), lastRunAt nullable, lastStatus nullable, lastError nullable, updatedAt @updatedAt.

**Strict additive rule (per CONTEXT.md):** Do NOT modify or remove any existing model, field, relation, or index. Do NOT add new fields to v1.0 models like `BulkAccUser`, `AccGraphLayoutCache`, etc. Phase 2 introduces dual-write; this phase is schema-only DDL.

**No seed data, no Prisma enum types** — `status` columns stay as plain `String` for migration simplicity (CONTEXT.md: "Pure DDL, no seed data").
  </action>
  <verify>
    <automated>npx prisma format && npx prisma validate</automated>
  </verify>
  <done>Schema validates; 9 new models present; no existing model touched; `grep -c "model AccProject\b\|model AccProjectMember\|model AccRole\b\|model AccProjectRole\|model AccFolder\b\|model AccFolderPermission\|model AccActivity\|model AccDataConnectorJob\|model SyncMeta" prisma/schema.prisma` returns 9.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply additive migration on local dev</name>
  <files>prisma/migrations/</files>
  <action>
Run `npx prisma migrate dev --name acc_v2_foundation --create-only` to generate the migration SQL **without** applying it yet. Open the generated `migration.sql` and verify by inspection:

  1. Only `CREATE TABLE` / `CREATE INDEX` / `ALTER TABLE ... ADD CONSTRAINT` statements present. **Zero `DROP`**, **zero `ALTER ... DROP COLUMN`**, **zero `ALTER ... RENAME`**.
  2. `AccActivity` indexes use `DESC` for `createdAt` in the two composite indexes — confirm SQL contains `"createdAt" DESC` in both `(autodeskId, createdAt)` and `(projectId, createdAt)` index definitions. If Prisma 7 emits ASC despite the `sort: Desc` hint, fall back to manually editing the migration.sql to set DESC (the schema annotation drives intent; the SQL is the source of truth for the DB).
  3. All 9 tables present.

After inspection, apply with `npx prisma migrate deploy` against the local dev DB (uses `DATABASE_URL`). Then run `npx prisma generate` to refresh the client. Document the migration name in the summary so Phase 2 callers can reference it.

**Pitfall guard (RESEARCH Pitfall 3):** If `@@index([field, createdAt(sort: Desc)])` does not emit DESC in the generated SQL on this Prisma version, edit `migration.sql` directly to add `DESC` to the offending index columns before running `migrate deploy`. The schema annotation stays as documentation of intent.
  </action>
  <verify>
    <automated>npx prisma migrate status</automated>
  </verify>
  <done>`prisma migrate status` reports "Database schema is up to date"; migration.sql contains zero DROP statements; `AccActivity` composite indexes specify DESC on createdAt; prisma client regenerated.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- `npx prisma migrate status` reports clean state
- `grep -E "DROP|ALTER.*DROP COLUMN|RENAME" prisma/migrations/*_acc_v2_foundation/migration.sql` returns no matches (additive-only assertion)
- `grep -c "CREATE TABLE" prisma/migrations/*_acc_v2_foundation/migration.sql` returns 9
- `grep -E "createdAt.*DESC" prisma/migrations/*_acc_v2_foundation/migration.sql` returns at least 2 matches (the two composite indexes on AccActivity)
</verification>

<success_criteria>
- 9 new models in prisma/schema.prisma; zero changes to existing models
- Migration generated, inspected, applied locally; additive-only verified
- Prisma client regenerated and importable from `@prisma/client` (downstream plans can reference AccDataConnectorJob, SyncMeta types)
- SCHEMA-01 satisfied (8 domain models + SyncMeta); SCHEMA-02 satisfied (DESC composite indexes from day one)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-sync/01-01-SUMMARY.md` documenting:
- Migration filename + applied timestamp
- Whether `sort: Desc` emitted correctly or required manual SQL edit
- Any deviations from RESEARCH Pattern 3 schema definitions
</output>
