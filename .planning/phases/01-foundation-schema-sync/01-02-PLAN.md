---
phase: 01-foundation-schema-sync
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/server/acc-helpers.ts
  - lib/server/__tests__/acc-helpers.test.ts
  - server/routers/users.ts
autonomous: true
requirements: [SCHEMA-03]

must_haves:
  truths:
    - "getAccountId(db) returns the hub ID with the leading 'b.' prefix stripped"
    - "getAccountId(db) throws (non-TRPC error) when the hub ID is missing"
    - "getProjectIdForDM(rawId) returns the raw ID unchanged (preserves 'b.' prefix)"
    - "Existing users router uses the extracted helper — no inline 'b.' stripping remains in server/"
    - "Helper unit tests cover prefix-strip, no-prefix passthrough, missing-hub throw, and DM-passthrough"
  artifacts:
    - path: "lib/server/acc-helpers.ts"
      provides: "getAccountId(db), getProjectIdForDM(rawId)"
      exports: ["getAccountId", "getProjectIdForDM"]
      min_lines: 25
    - path: "lib/server/__tests__/acc-helpers.test.ts"
      provides: "Vitest suite covering both helpers"
      contains: "describe('getAccountId'"
    - path: "server/routers/users.ts"
      provides: "users router now imports getAccountId from shared helper (no inline definition)"
      contains: "from \"@/lib/server/acc-helpers\""
  key_links:
    - from: "server/routers/users.ts"
      to: "lib/server/acc-helpers.ts"
      via: "import { getAccountId }"
      pattern: "import.*getAccountId.*acc-helpers"
    - from: "getAccountId"
      to: "DB.project.findFirst"
      via: "select apsHubId, strip /^b\\./"
      pattern: "replace\\(/\\^b\\\\\\.\\/"
---

<objective>
Extract `getAccountId` from `server/routers/users.ts` into a shared helper module, add a sibling `getProjectIdForDM` helper, and back both with Vitest unit tests. This is SCHEMA-03 — the helpers must work from non-tRPC callers (release script, cron script) which Plan 03 will consume.

Purpose: Phase 2 ACC extraction code (and the Plan 03 release/cron scripts) cannot import from `server/routers/users.ts` cleanly. Centralizing the helpers and dropping the `TRPCError` dependency makes them safe to call from anywhere on the server.
Output: New helper module, test file, refactored users router.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-schema-sync/01-CONTEXT.md
@.planning/phases/01-foundation-schema-sync/01-RESEARCH.md
@server/routers/users.ts
@lib/server/acc-admin.ts
@vitest.config.ts
@vitest.setup.ts

<interfaces>
<!-- Current getAccountId — embedded at server/routers/users.ts lines ~211-221 -->
<!-- Pattern: async function getAccountId(db: any): Promise<string> -->
<!--   reads db.project.findFirst({ select: { apsHubId: true } }) -->
<!--   throws TRPCError if null/empty -->
<!--   returns hub.apsHubId.replace(/^b\./, "") -->
<!-- After extraction: throws plain Error (or IntegrationError if codebase uses one); tRPC caller wraps via existing toAccRouterError. -->
<!-- Audit result from RESEARCH: only ONE inline `b.`-strip exists across server/ — inside getAccountId itself. No other callers strip. -->
</interfaces>
</context>

<feature>
  <name>Shared ACC ID helpers</name>
  <files>lib/server/acc-helpers.ts, lib/server/__tests__/acc-helpers.test.ts</files>
  <behavior>
**getAccountId(db: { project: { findFirst: ... } }): Promise<string>**
  - Input: `db.project.findFirst({ select: { apsHubId: true } })` returns `{ apsHubId: "b.abc-123" }` → output `"abc-123"`.
  - Input: returns `{ apsHubId: "abc-123" }` (no prefix) → output `"abc-123"`.
  - Input: returns `null` or `{ apsHubId: "" }` → throws `Error` with message containing `"hub"` and `"not configured"` (do NOT throw TRPCError; tRPC caller wraps).

**getProjectIdForDM(rawId: string): string**
  - Input: `"b.proj-xyz"` → output `"b.proj-xyz"` (unchanged).
  - Input: `"proj-xyz"` → output `"proj-xyz"` (unchanged, even if missing prefix — caller's job to supply correct ID).
  - Input: `""` → throws `Error("getProjectIdForDM requires a non-empty project id")`.

Cases:
  - getAccountId, prefixed → strips
  - getAccountId, unprefixed → returns as-is
  - getAccountId, null record → throws
  - getAccountId, empty apsHubId → throws
  - getProjectIdForDM, prefixed → identity
  - getProjectIdForDM, unprefixed → identity
  - getProjectIdForDM, empty → throws
  </behavior>
  <implementation>
1. **RED:** Create `lib/server/__tests__/acc-helpers.test.ts` first. Use Vitest (`describe`/`it`/`expect`); mock the `db` argument with a Vitest `vi.fn()` returning the test scenario. Run `npx vitest run lib/server/__tests__/acc-helpers.test.ts` — expect ALL tests to fail (module does not exist yet).

2. **GREEN:** Create `lib/server/acc-helpers.ts`:

```typescript
// lib/server/acc-helpers.ts
// Pure helpers — no "server-only" import (release/cron scripts call these too).

type ProjectFindFirst = {
  project: { findFirst: (args: { select: { apsHubId: true } }) => Promise<{ apsHubId: string } | null> };
};

export async function getAccountId(db: ProjectFindFirst): Promise<string> {
  const hub = await db.project.findFirst({ select: { apsHubId: true } });
  if (!hub || !hub.apsHubId) {
    throw new Error("ACC hub is not configured (project.apsHubId missing)");
  }
  return hub.apsHubId.replace(/^b\./, "");
}

export function getProjectIdForDM(rawId: string): string {
  if (!rawId) {
    throw new Error("getProjectIdForDM requires a non-empty project id");
  }
  return rawId;
}
```

   Run `npx vitest run lib/server/__tests__/acc-helpers.test.ts` — ALL tests pass.

3. **REFACTOR:** Update `server/routers/users.ts`:
   - Delete the inline `getAccountId` function (lines ~211-221).
   - Add `import { getAccountId } from "@/lib/server/acc-helpers";` near the top of the file.
   - Find every existing call site of the local `getAccountId(ctx.db)` (or `getAccountId(db)`) — leave the call signatures unchanged.
   - The local users router catches the new plain `Error` thrown by the helper. The existing `toAccRouterError` (or equivalent) already maps generic errors to `TRPCError`. If `getAccountId` is called in a path that does NOT route through that wrapper, wrap the call in `try/catch` and convert manually to `TRPCError({ code: "PRECONDITION_FAILED", message: err.message })`.
   - Run `npx tsc --noEmit` to confirm no type regressions.
   - Run the relevant users-router tests if any exist; rerun the helper test file.

4. **Audit (SCHEMA-03 requirement):** Run `grep -rn "replace(/\^b\\." server/ lib/server/` — should return ONLY the line inside `lib/server/acc-helpers.ts`. If other call sites are found, log them in the SUMMARY as Phase 2 follow-up but do NOT modify them in this plan (out of scope).
  </implementation>
</feature>

<verification>
- `npx vitest run lib/server/__tests__/acc-helpers.test.ts` — all 7 cases pass
- `npx tsc --noEmit` — no new type errors
- `grep -n "function getAccountId" server/routers/users.ts` returns nothing (local function deleted)
- `grep -n "from \"@/lib/server/acc-helpers\"" server/routers/users.ts` returns at least one line (import added)
- `grep -rn "replace(/\\^b\\\\\\.\\/" server/ lib/server/ | grep -v acc-helpers.ts` returns nothing (audit clean)
</verification>

<success_criteria>
- SCHEMA-03 fulfilled: two helpers exist, unit-tested, audit clean
- Helpers callable from non-tRPC contexts (no TRPCError throw, no `"server-only"` import) — Plan 03 release script can require/import them
- Users router unchanged in behavior; tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-sync/01-02-SUMMARY.md` documenting:
- All call sites of `getAccountId` that were updated (file:line list)
- Audit result of `grep -rn "replace(/\\^b\\.\\/" server/ lib/server/` AFTER refactor
- Any places where `toAccRouterError`-style wrapping was added because the call wasn't already wrapped
</output>
