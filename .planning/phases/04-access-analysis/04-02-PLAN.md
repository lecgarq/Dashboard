---
phase: 04-access-analysis
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/acc/acc-types.ts
  - lib/server/acc-admin.ts
  - server/routers/users.ts
autonomous: true
requirements:
  - DASH-06
must_haves:
  truths:
    - "BulkAccUser objects carry an addedOn (member creation / ACC join date) field — string ISO or null"
    - "When ACC Admin API exposes a creation/join date, it is extracted; when it does not, a documented fallback is used"
    - "Recently-Added widget can filter users by addedOn within 7d/30d/90d windows"
  artifacts:
    - path: "lib/acc/acc-types.ts"
      provides: "BulkAccUser.addedOn field declaration"
      contains: "addedOn"
    - path: "lib/server/acc-admin.ts"
      provides: "addedOn extraction (or fallback first-seen timestamp) plumbed into user record"
      contains: "addedOn"
    - path: "server/routers/users.ts"
      provides: "addedOn in bulkAccSummary output"
      contains: "addedOn"
  key_links:
    - from: "lib/server/acc-admin.ts"
      to: "ACC Admin API user record"
      via: "addedOn / created_at / created field on raw user payload"
      pattern: "addedOn|created_at|created"
    - from: "server/routers/users.ts"
      to: "BulkAccUser.addedOn"
      via: "bulkAccSummary transform reads cached field"
      pattern: "addedOn"
---

<objective>
Plumb member-creation / ACC join date end-to-end so the Recently-Added widget (DASH-06) has a real source field. Research confirmed `BulkAccUser.syncedAt` is sync time (NOT join date) — without this Wave 0 fix, the widget would show "everyone is recent."

Purpose: Resolve Open Question 1 from RESEARCH.md by spiking the raw ACC Admin API user payload, identifying the join-date field if present, and wiring it through. If ACC doesn't expose it, fall back to a documented "first time we saw this email in the cache" proxy.

Output: `BulkAccUser.addedOn: string | null` (ISO date) populated by ACC Admin API or fallback proxy, available in bulkAccSummary.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-access-analysis/04-CONTEXT.md
@.planning/phases/04-access-analysis/04-RESEARCH.md

@lib/acc/acc-types.ts
@lib/server/acc-admin.ts
@server/routers/users.ts

<interfaces>
<!-- BulkAccUser already has syncedAt (sync time). Add addedOn (join date) — different semantics. -->

From lib/acc/acc-types.ts:
```typescript
export interface BulkAccUser {
  email: string;
  fullName: string | null;
  companyRole: string | null;
  lastSignIn: string | null;
  syncedAt: string;       // sync timestamp — DO NOT REUSE for "added on"
  // ADD: addedOn: string | null;  // ISO date member was added to ACC, OR fallback first-seen-by-cache, OR null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — log raw ACC Admin API user payload to find a creation/join-date field</name>
  <files>lib/server/acc-admin.ts</files>
  <action>
    In the same user-record processing loop in lib/server/acc-admin.ts where Task 1 of 04-01 spiked accountAdmin, add a temporary console.log of candidate join-date fields from the FIRST user's raw payload: `addedOn`, `added_on`, `createdAt`, `created_at`, `created`, `joinedAt`. Log both the keys and string values.

    Trigger one ACC sync. Capture the log output.

    Decide one of two paths:
      A) Field exists and is a usable ISO timestamp string → "ACC API source"
      B) No usable field → "Fallback: first-seen in cache"

    Document the decision inline in a comment at the call site, including the exact field name (path A) or the explicit fallback rationale (path B):
      // ACC join-date source: <fieldName> | FALLBACK first-seen in accMemberCache (rationale: ACC API does not expose addedOn — verified <date>).

    Remove the temporary console.log before committing.

    Avoid: silently picking a field without verifying its value is actually a date for multiple users; treating epoch=0 as a real date.
  </action>
  <verify>
    <automated>git diff lib/server/acc-admin.ts | grep -E "ACC join-date source|addedOn"</automated>
  </verify>
  <done>Comment in lib/server/acc-admin.ts documents either the verified ACC field name OR the fallback decision; no temporary console.log remains in committed code.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BulkAccUser + plumb addedOn (with fallback) through acc-admin.ts and bulkAccSummary</name>
  <files>lib/acc/acc-types.ts,lib/server/acc-admin.ts,server/routers/users.ts</files>
  <action>
    1) lib/acc/acc-types.ts — Add `addedOn: string | null;` (NOT optional — must be deterministic null vs missing) near syncedAt.

    2) lib/server/acc-admin.ts — Two paths based on Task 1 outcome:
       - Path A (ACC field exists): read it from the raw payload, normalize to ISO via `new Date(value).toISOString()` if needed; null on parse failure.
       - Path B (fallback): when persisting per-user records to accMemberCache, check whether the row already exists in the DB for that email. If it does, preserve its existing data.addedOn; if it does NOT, set addedOn = current sync timestamp. This makes "first time we saw this email" the proxy. Document the caveat in a JSDoc comment on the field write.

    3) server/routers/users.ts — In bulkAccSummary transform, read addedOn off the cache row (default null for legacy rows) and emit on BulkAccUser.

    Use `null` (not undefined) so widget code can branch deterministically. Legacy cache rows produce `addedOn: null` (Recently-Added widget will simply not surface them — acceptable until next sync).

    Avoid: using `Date.now()` written EVERY sync as the fallback (would defeat the point — must be first-seen). Mutating syncedAt semantics.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "addedOn" lib/acc/acc-types.ts lib/server/acc-admin.ts server/routers/users.ts</automated>
  </verify>
  <done>BulkAccUser.addedOn is `string | null`; ACC sync writes it via the chosen path; bulkAccSummary returns it; tsc passes; grep finds the field in all three files.</done>
</task>

<task type="auto">
  <name>Task 3: Resync + verify addedOn distribution</name>
  <files>lib/server/acc-admin.ts</files>
  <action>
    Trigger a fresh ACC sync. Then query accMemberCache (Prisma Studio or raw SQL) and aggregate:
      - count rows where data->>'addedOn' IS NULL
      - count rows in last 7 days
      - count rows in last 30 days
      - count rows in last 90 days
      - count rows older than 90 days

    Sanity-check: the buckets should NOT all be "last 7d" (that would mean addedOn is being overwritten with sync time). For Path A, distribution should reflect actual ACC join history. For Path B, all NEW emails will be in last 7d on first sync (first-seen=now); on subsequent syncs, only newcomers will land in the recent buckets.

    Document the observed distribution + chosen path in the plan SUMMARY along with the caveat that downstream widget (DASH-06) MUST handle null gracefully.

    Avoid: declaring success when distribution is uniformly "last 7d" (that's the bug we're trying to avoid).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Distribution across 7d/30d/90d/>90d/null is documented; not all buckets collapse to "last 7d"; SUMMARY notes the chosen path and caveats for DASH-06 widget.</done>
</task>

</tasks>

<verification>
- addedOn field exists in BulkAccUser as `string | null`
- Fresh ACC sync populates it via verified field OR documented fallback
- Distribution makes sense (not 100% in last-7d unless this is genuinely a new account)
</verification>

<success_criteria>
- DASH-06 unblocked: Recently-Added widget can filter `users.filter(u => u.addedOn && differenceInDays(now, parseISO(u.addedOn)) <= window)`
- Fallback (if needed) is documented inline + in SUMMARY so widget can warn user about caveat
- No Prisma migration introduced (JSON column)
</success_criteria>

<output>
After completion, create `.planning/phases/04-access-analysis/04-02-SUMMARY.md` documenting:
- Path A vs Path B decision + ACC field name (if A)
- addedOn distribution observed post-sync
- Caveats DASH-06 widget consumers must handle
</output>
