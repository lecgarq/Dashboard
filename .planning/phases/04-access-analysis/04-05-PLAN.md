---
phase: 04-access-analysis
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - app/(dashboard)/users/dashboard/page.tsx
  - app/(dashboard)/users/dashboard/DashboardClient.tsx
  - app/(dashboard)/users/dashboard/widgetRegistry.ts
  - app/(dashboard)/users/dashboard/useWidgetOrder.ts
  - app/(dashboard)/users/dashboard/useWidgetOrder.test.ts
  - app/(dashboard)/users/dashboard/SortableWidget.tsx
  - lib/acc/csvExport.ts
  - lib/acc/csvExport.test.ts
  - package.json
autonomous: true
requirements:
  - DASH-11
  - DASH-12
  - DASH-13
must_haves:
  truths:
    - "Visiting /users/dashboard renders a 2-column widget grid with placeholder widgets at 1280px"
    - "User can drag widgets to reorder; the new order persists across page reloads via localStorage"
    - "downloadCsv produces RFC4180-escaped CSV with BOM that Excel opens cleanly"
    - "Required dashboard chart libraries are installed and importable (Nivo / ECharts / @xyflow/react)"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/page.tsx"
      provides: "Server component shell that authenticates + renders DashboardClient"
      contains: "DashboardClient"
    - path: "app/(dashboard)/users/dashboard/DashboardClient.tsx"
      provides: "Client root with DndContext + SortableContext + 2-col grid"
      contains: "DndContext"
      min_lines: 50
    - path: "app/(dashboard)/users/dashboard/widgetRegistry.ts"
      provides: "WIDGETS registry + DEFAULT_ORDER constant"
      exports: ["WIDGETS", "DEFAULT_ORDER"]
    - path: "app/(dashboard)/users/dashboard/useWidgetOrder.ts"
      provides: "localStorage-backed widget order hook"
      exports: ["useWidgetOrder"]
    - path: "app/(dashboard)/users/dashboard/SortableWidget.tsx"
      provides: "Reusable sortable card wrapper with drag handle"
      contains: "useSortable"
    - path: "lib/acc/csvExport.ts"
      provides: "downloadCsv helper using xlsx for RFC4180 escaping + BOM"
      exports: ["downloadCsv"]
    - path: "lib/acc/csvExport.test.ts"
      provides: "Vitest coverage of escaping (commas, quotes, newlines, unicode) + BOM"
      contains: "describe"
  key_links:
    - from: "app/(dashboard)/users/dashboard/DashboardClient.tsx"
      to: "@dnd-kit/sortable"
      via: "import { SortableContext, rectSortingStrategy, arrayMove }"
      pattern: "@dnd-kit/sortable"
    - from: "app/(dashboard)/users/dashboard/DashboardClient.tsx"
      to: "useWidgetOrder"
      via: "const [order, setOrder] = useWidgetOrder()"
      pattern: "useWidgetOrder"
    - from: "app/(dashboard)/users/dashboard/useWidgetOrder.ts"
      to: "window.localStorage"
      via: "useEffect-guarded read/write under key 'acc-dashboard-widget-order'"
      pattern: "localStorage"
    - from: "lib/acc/csvExport.ts"
      to: "xlsx"
      via: "import * as XLSX from 'xlsx'; XLSX.utils.json_to_sheet + sheet_to_csv"
      pattern: "json_to_sheet"
---

<objective>
Stand up the dashboard route shell, the drag-reorderable widget grid scaffolding, the localStorage persistence hook, and the shared CSV export helper — every piece needed BEFORE any chart widget is built. Also install the locked chart dependencies.

Purpose: This plan sets the contract that subsequent widget plans (04-06, 04-07) implement against. It explicitly creates the registry, order hook, sortable wrapper, and CSV helper as interfaces, so widget executors don't reinvent them. The route is wired with placeholder widgets so navigation + drag-reorder + persistence can be verified BEFORE any chart implementation lands.

Output: Working `/users/dashboard` page with 9 placeholder cards in a draggable 2-col grid, persistence verified, plus a tested CSV helper and installed chart dependencies.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-access-analysis/04-CONTEXT.md
@.planning/phases/04-access-analysis/04-RESEARCH.md
@.planning/phases/04-access-analysis/04-01-SUMMARY.md
@.planning/phases/04-access-analysis/04-02-SUMMARY.md
@.planning/phases/04-access-analysis/04-03-SUMMARY.md
@.planning/phases/04-access-analysis/04-04-SUMMARY.md

# Existing patterns to mirror (READ for pattern; do NOT modify)
@app/(dashboard)/users/AccAnalysisPanel.tsx
@app/(dashboard)/users/page.tsx
@components/ui/card.tsx
@components/ui/skeleton.tsx

<interfaces>
<!-- Targets that downstream plans (04-06, 04-07, 04-08) will implement against. -->

```typescript
// app/(dashboard)/users/dashboard/widgetRegistry.ts
export const WIDGETS = {
  coverage:        { component: ..., title: "ACC Coverage",       span: "col-span-1" },
  tiers:           { component: ..., title: "Active Users",       span: "col-span-1" },
  kpi:             { component: ..., title: "Overview",           span: "col-span-2" },
  recommendations: { component: ..., title: "Recommendations",    span: "col-span-2" },
  heatmap:         { component: ..., title: "Roles × Modules",    span: "col-span-2" },
  outliers:        { component: ..., title: "Unusual Access",     span: "col-span-1" },
  flow:            { component: ..., title: "Role Relationships", span: "col-span-2" },
  recent:          { component: ..., title: "Recently Added",     span: "col-span-1" },
  admins:          { component: ..., title: "Account Admins",     span: "col-span-1" },
} as const;

export const DEFAULT_ORDER = [
  "coverage", "tiers", "kpi", "recommendations", "heatmap",
  "outliers", "flow", "recent", "admins",
] as const satisfies readonly (keyof typeof WIDGETS)[];

// app/(dashboard)/users/dashboard/useWidgetOrder.ts
export function useWidgetOrder(): [string[], (next: string[]) => void];

// lib/acc/csvExport.ts
export function downloadCsv(filename: string, rows: Record<string, string | number | null | undefined>[]): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chart deps + create CSV helper with Vitest coverage</name>
  <files>package.json,lib/acc/csvExport.ts,lib/acc/csvExport.test.ts</files>
  <action>
    1) Install chart libraries (research-locked versions):
       ```
       npm install @nivo/core@0.99.0 @nivo/pie@0.99.0 @nivo/bar@0.99.0 echarts echarts-for-react@3.0.6 @xyflow/react@12.10.2
       ```
       Verify package.json shows all six as runtime deps. `xlsx@0.20.3` is already installed (do not reinstall).

    2) Create `lib/acc/csvExport.ts` per RESEARCH.md "CSV export helper" code example:
       - Use `XLSX.utils.json_to_sheet(rows)` then `XLSX.utils.sheet_to_csv(ws, { FS: ",", RS: "\n" })`.
       - Prepend BOM `"﻿"` so Excel auto-detects UTF-8.
       - `Blob` + `URL.createObjectURL` + temporary `<a>` click + `URL.revokeObjectURL`.
       - Guard `typeof window === "undefined"` early-return (defensive — should never be called server-side).

    3) Create `lib/acc/csvExport.test.ts` (Vitest):
       - Mock `URL.createObjectURL` / `revokeObjectURL` / a click handler. Capture the Blob payload.
       - Assert: payload starts with BOM `﻿`.
       - Assert: a row with value `'O'Brien, "VP"'` is properly escaped per RFC4180 (XLSX handles this — assert the resulting CSV does NOT contain a raw unescaped quote inside a quoted field).
       - Assert: newlines inside cell values are preserved within quoted fields.
       - Assert: unicode `"日本語"` round-trips intact in the BOM-prefixed UTF-8.
       - Assert: empty rows array → just the BOM + empty CSV (no crash).

    Avoid: hand-rolling CSV escape (Pitfall: research called this out — use xlsx); skipping the BOM (Excel will mangle UTF-8 without it).
  </action>
  <verify>
    <automated>npm run test -- csvExport && grep -E "@nivo/pie|echarts-for-react|@xyflow/react" package.json</automated>
  </verify>
  <done>All six chart deps in package.json; csvExport.test.ts passes (>=4 cases); BOM + RFC4180 escaping confirmed by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build widget registry + useWidgetOrder hook + SortableWidget wrapper (with hook test)</name>
  <files>app/(dashboard)/users/dashboard/widgetRegistry.ts,app/(dashboard)/users/dashboard/useWidgetOrder.ts,app/(dashboard)/users/dashboard/useWidgetOrder.test.ts,app/(dashboard)/users/dashboard/SortableWidget.tsx</files>
  <action>
    1) `widgetRegistry.ts`:
       - Export `WIDGETS` as a `const` map of 9 entries with placeholder components (each component returns `<div className="text-sm text-muted-foreground">{title} placeholder</div>`). `span` follows the locked layout: kpi, recommendations, heatmap, flow are `col-span-2`; the others are `col-span-1`.
       - Export `DEFAULT_ORDER` as a readonly tuple matching the locked sequence: `coverage, tiers, kpi, recommendations, heatmap, outliers, flow, recent, admins`.
       - Export type `WidgetId = keyof typeof WIDGETS`.

    2) `useWidgetOrder.ts`:
       - Hook returning `[order: string[], setOrder: (next: string[]) => void]`.
       - Initial state via `useState(() => DEFAULT_ORDER as string[])` — DO NOT read localStorage here (Pitfall 9: SSR).
       - `useEffect` on mount: `localStorage.getItem("acc-dashboard-widget-order")` → if present and parseable JSON array of strings AND every item is a known WIDGET id AND set covers DEFAULT_ORDER → setOrder(stored). If invalid OR missing IDs (e.g., new widget added since last save), fall back to DEFAULT_ORDER.
       - `setOrder` writes JSON.stringify to localStorage AND updates state.
       - Re-validate on every read so adding a NEW widget id to WIDGETS doesn't make stored arrays orphan that id.

    3) `useWidgetOrder.test.ts` (Vitest, jsdom env — confirm `vitest.config.*` has `environment: "jsdom"`; if not, add a per-file directive):
       - Cases: empty localStorage → DEFAULT_ORDER returned after effect.
       - Stored valid order → returned after effect.
       - Stored order missing a known id → falls back to DEFAULT_ORDER.
       - setOrder writes to localStorage.
       - Stored garbage (non-JSON) → falls back to DEFAULT_ORDER without throwing.

    4) `SortableWidget.tsx`:
       - Client component (`"use client"`).
       - Props: `{ id: string; children: React.ReactNode; span: string; title: string }`.
       - Use `useSortable({ id })` from `@dnd-kit/sortable`.
       - Render a shadcn `Card` wrapping children. Drag listeners attach ONLY to a header `<button>` containing a `GripVertical` icon (Pitfall 8 — don't put listeners on the whole card or chart hovers will break).
       - Apply `style={{ transform: CSS.Transform.toString(transform), transition }}` from `@dnd-kit/utilities`.
       - Apply the `span` className on the outermost element so the parent grid honors col-span.

    Avoid: reading localStorage during render (Pitfall 9); spreading dnd-kit listeners across the whole card (Pitfall 8); inventing widget ids — IDs MUST come from WIDGETS keys so widget plans 04-06/04-07 fill in the same keys.
  </action>
  <verify>
    <automated>npm run test -- useWidgetOrder && npx tsc --noEmit</automated>
  </verify>
  <done>useWidgetOrder.test.ts passes (5 cases); WIDGETS exports 9 entries with correct spans; SortableWidget compiles with drag-handle listener pattern; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create /users/dashboard route + DashboardClient with DndContext-wrapped 2-col grid</name>
  <files>app/(dashboard)/users/dashboard/page.tsx,app/(dashboard)/users/dashboard/DashboardClient.tsx</files>
  <action>
    1) `page.tsx` — server component:
       - Mirror the auth pattern from `app/(dashboard)/users/page.tsx` (existing route): pull session, redirect if unauthenticated.
       - Render `<DashboardClient />`.
       - Page metadata: `export const metadata = { title: "ACC Access Analysis Dashboard" }`.
       - DO NOT import any chart library here (Pitfall 1: SSR + Nivo).

    2) `DashboardClient.tsx` — client component (`"use client"`):
       - Wraps a `<DndContext collisionDetection={closestCenter} onDragEnd={...}>` from `@dnd-kit/core`.
       - Inside, `<SortableContext items={order} strategy={rectSortingStrategy}>` from `@dnd-kit/sortable`.
       - Maps `order` over a CSS grid: `<div className="grid grid-cols-1 md:grid-cols-2 gap-4 p-4">`.
       - For each id, renders `<SortableWidget id={id} span={WIDGETS[id].span} title={WIDGETS[id].title}>{Component()}</SortableWidget>`.
       - Layout density per CONTEXT.md "2-col at 1280px, generous whitespace, half-width charts; matrix + KPI strip full-width" — `gap-4` minimum, `p-4` page padding.
       - onDragEnd: `if (e.over && e.active.id !== e.over.id) setOrder(arrayMove(order, order.indexOf(...), order.indexOf(...)))`.
       - Header strip with page title + a "Refresh" button (no-op for now; widget plans wire it later).

    3) Manual smoke (no checkpoint here — defer to Plan 04-08): `npm run dev` → navigate to `/users/dashboard` → see 9 placeholder cards in 2-col layout → drag one → reload → confirm new order persists.

    Avoid: rendering anything chart-related here (placeholders only); blocking SSR with client-only imports at module scope (use the `DashboardClient` boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>npm run build succeeds (no SSR window-undefined errors — Pitfall 1); /users/dashboard renders 9 placeholder cards; drag-reorder + reload persistence works in manual smoke; tsc passes.</done>
</task>

</tasks>

<verification>
- All six chart deps in package.json
- csvExport.test.ts passes
- useWidgetOrder.test.ts passes
- npm run build passes (no SSR crashes)
- /users/dashboard renders 9 placeholder cards in 2-col grid; drag-reorder persists across reload
</verification>

<success_criteria>
- DASH-11 (drag-reorder + persistence) demonstrably working with placeholders
- DASH-13 (single-page 2-col grid) layout established
- DASH-12 building block (downloadCsv) ready for widget plans to import
- Locked chart libraries installed at correct versions
- Widget registry contract published — Plans 04-06/04-07 fill components into WIDGETS by id
</success_criteria>

<output>
After completion, create `.planning/phases/04-access-analysis/04-05-SUMMARY.md` documenting:
- Installed dep versions (any deviations from research)
- Confirmed widget registry shape + 9 IDs (so 04-06/04-07 know what to fill)
- Note any vitest config tweaks (jsdom env)
- Manual smoke result for drag-reorder persistence
</output>
