---
phase: 11-data-truthfulness-labels
plan: 04
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - lib/server/dcCoverageView.ts
  - lib/server/__tests__/dcCoverageView.test.ts
  - app/(dashboard)/access-analysis/mainCharts.tsx
  - app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [TRUTH-01, TRUTH-03]
must_haves:
  truths:
    - "A restrained muted header line on /access-analysis leads with the free-crawl activity coverage (~956 of 1,153 projects), computed live — never the rejected '428 DC' framing"
    - "DC-metadata-derived metrics carry their own coverage (~550 of 1,153), computed live, so coverage reads as metric-specific rather than one blanket number"
    - "The Activity-by-module donut title carries a small ⓘ info-icon whose Radix tooltip states classification is rawAction-based and Autodesk's service attribution (~40.7% disagreement) is not yet reconciled — candid on hover, clean at rest"
  artifacts:
    - path: "lib/server/dcCoverageView.ts"
      provides: "Live count of AccDcProject coverage (~550) over the AccProject total (~1,153) for the DC-metadata coverage label"
      exports: ["loadDcCoverage"]
      contains: "accDcProject"
    - path: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      provides: "Header coverage line (activity ~956/1,153 via existing activityCoverageCounts + DC ~550/1,153) and the module-donut ⓘ Radix tooltip"
      contains: "rawAction"
  key_links:
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "coverageCounts.ts (activityCoverageCounts) + lib/server/dcCoverageView.ts"
      via: "header line reads live activity coverage + DC coverage props"
      pattern: "activityCoverageCounts|dcCoverage"
    - from: "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx"
      to: "components/ui/tooltip.tsx (Radix)"
      via: "ⓘ TooltipTrigger/TooltipContent next to the 'Activity by module' SectionHeader title"
      pattern: "TooltipContent"
---

<objective>
Make `/access-analysis` honestly label its coverage (TRUTH-01, CONCERNS §5.2) and caveat the module donut (TRUTH-03, CONCERNS §2.5).

TRUTH-01 — owner-corrected framing: a single restrained muted header line that LEADS with the free ACCDS-crawl **activity coverage (~956 of 1,153 projects)**, plus DC-metadata-derived metrics labeled with their own **(~550 of 1,153)** coverage. Coverage is metric-specific, not one blanket number. The stale "428 of 1,152 Data Connector" example from REQUIREMENTS/ROADMAP is EXPLICITLY REJECTED as the headline (428 = old DC-API-extractable count, superseded by the free crawl). All integers are live-DB counts, NOT hard-coded. No new bordered provenance card — reuse the existing muted badge family.

Verified reuse: the page already computes activity coverage live. `AccessAnalysisCharts` calls `activityCoverageCounts(coverage)` → `{ covered: covCovered, total: covTotal }`, where `coverage` is `loadProjectCoverage()` (covered = projects with unified ACCDS+DC-backfill activity ≈ 956; total ≈ AccProject universe ≈ 1,153). That IS the free-crawl activity headline source — reuse `covCovered`/`covTotal`, do not re-query. The existing per-chart `ActivityCoverageBadge` already renders "Activity: N / M projects" on activity-derived panels — keep it. What is MISSING: (a) the header coverage LINE, and (b) the DC-metadata (~550) coverage figure (not currently computed) for DC-derived metrics.

TRUTH-03: a small ⓘ next to the "Activity by module" `SectionHeader` title with a Radix tooltip — candid copy: classification is `rawAction`-based and Autodesk's own `service` attribution is not yet reconciled (~40.7% disagreement). The ~40.7% figure also lives in INTEGRATIONS.md (line ~179 already records it; expand the wording to match the tooltip). NOT an always-on caption — "honest at a glance, full candor on hover." Note: `TooltipProvider` is NOT mounted in this tree today (it lives in `UsersDirectoryClient.tsx`), so the ⓘ must wrap its trigger in a local `TooltipProvider`.

Purpose: the presenter can point to exactly which projects each metric covers and disclose the donut's classification caveat — credible "we're transparent" talking points without making the dashboard read as uncertain.
Output: a header coverage line, a DC-coverage figure (live), the module ⓘ tooltip, and an aligned INTEGRATIONS.md note.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-data-truthfulness-labels/11-CONTEXT.md

# Existing live activity-coverage plumbing — REUSE covCovered/covTotal (≈956/1153); do not re-query
@app/(dashboard)/access-analysis/coverageCounts.ts
@lib/server/projectCoverageView.ts

# The muted badge family + SectionHeader to reuse; the module-donut SectionHeader is the ⓘ host
@app/(dashboard)/access-analysis/components/ActivityCoverageBadge.tsx
@app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx

# Radix tooltip primitive (note: no global TooltipProvider in this tree — wrap locally)
@components/ui/tooltip.tsx

# The RSC that loads page data — add the DC-coverage load + pass it down
@app/(dashboard)/access-analysis/mainCharts.tsx

# TRUTH-03 doc target: line ~179 already records the 40.7% service note — align wording to the tooltip
@.planning/codebase/INTEGRATIONS.md

# DC project model count source
@lib/server/dcCoverageView.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a live DC-metadata coverage count (loadDcCoverage) with a pure unit test</name>
  <files>lib/server/dcCoverageView.ts, lib/server/__tests__/dcCoverageView.test.ts</files>
  <behavior>
    - The pure counter returns { covered, total } where covered = number of AccDcProject rows and total = number of AccProject rows.
    - covered ≤ total (DC is a subset of the live project universe).
    - Given representative inputs (e.g. covered=550, total=1153) it returns those values verbatim — no rounding, no hard-coded constant.
  </behavior>
  <action>
    Create `lib/server/dcCoverageView.ts` mirroring the structure of `lib/server/projectCoverageView.ts` (server-only, 5-min cache, an exported pure helper for the count assembly). It loads two live counts via Prisma: `db.accDcProject.count()` (DC-metadata coverage ≈ 550) and `db.accProject.count()` (total ≈ 1,153), and returns `{ covered, total }` (a `DcCoverage` interface). Split the trivial assembly into an exported pure function so it can be unit-tested without a DB (precedent: `buildCoverage`). Write `lib/server/__tests__/dcCoverageView.test.ts` FIRST (RED) for the three behaviors above, then implement to GREEN.
    Do NOT hard-code 550 or 1,153 in production code — they come from `count()`. Keep DB access in this `lib/server` module (no Prisma in components).
  </action>
  <verify>
    <automated>npx vitest run lib/server/__tests__/dcCoverageView.test.ts</automated>
  </verify>
  <done>`loadDcCoverage()` returns live `{ covered, total }` from `accDcProject.count()`/`accProject.count()`; the pure assembly is exported and unit-pinned; no hard-coded counts.</done>
</task>

<task type="auto">
  <name>Task 2: Load DC coverage in MainCharts and render the header coverage line + per-chart DC label</name>
  <files>app/(dashboard)/access-analysis/mainCharts.tsx, app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx</files>
  <action>
    TRUTH-01 — header coverage line (live, metric-specific, no new card):
    1. `mainCharts.tsx`: add `loadDcCoverage()` to the `Promise.all` and pass its result as a `dcCoverage` prop to `<AccessAnalysisCharts ... />`. (This file is also touched by plan 11-03 for the timeline floor props — this plan is wave 2 / depends_on 11-03, so build on the 11-03 version; add the DC-coverage load alongside the timeline changes already present.)
    2. `AccessAnalysisCharts.tsx`: accept `dcCoverage?: { covered: number; total: number }`. Render ONE restrained muted header line at the top of the surface (Claude's discretion on exact placement: directly under the `StatStrip` or just above the `ProjectPicker`), leading with the free-crawl ACTIVITY coverage from the existing `covCovered`/`covTotal` (already computed via `activityCoverageCounts(coverage)`), e.g. "Activity data covers {covCovered} of {covTotal} ACC projects · Data Connector metadata covers {dcCoverage.covered} of {dcCoverage.total}". Style: `text-muted-foreground` + `text-xs`, semantic CSS vars, zinc theme — NO bordered card, NO `surface-card`. Give it `data-testid="coverage-header"`.
       - The activity figure (~956/1,153) MUST lead; the DC figure (~550) is the secondary, DC-specific clause. Do NOT render "428" anywhere (the rejected framing).
       - All numbers come from props/live counts (`covCovered`, `covTotal`, `dcCoverage`), never literals in JSX.
    3. Keep every existing per-chart `ActivityCoverageBadge` exactly as-is (the activity-derived panels already carry their badge). For the DC-derived label requirement, surface the DC coverage figure in the header line (above) — do NOT add a second badge family. If a panel is specifically DC-metadata-derived and warrants its own inline note, reuse the existing `ActivityCoverageBadge` styling/markup rather than inventing a new component (Claude's discretion, but minimal).
    Guardrails: do NOT touch the timeline floor wiring added by 11-03 (only add the new `dcCoverage` prop + header line); preserve the existing `SectionHeader`/`SectionHeaderWithPeople` structure; no card-inside-card; ≤200ms motion (no new animation on the static line).
  </action>
  <verify>
    <automated>npx tsc --noEmit && rg -n "428" "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx" | grep -v '^#'</automated>
  </verify>
  <done>A live, muted, metric-specific header coverage line renders (activity ~956/1,153 leading + DC ~550/1,153 secondary), sourced from props/live counts; no "428" literal in the component; per-chart `ActivityCoverageBadge`s unchanged; `npx tsc --noEmit` passes. (The rg gate expects ZERO "428" matches.)</done>
</task>

<task type="auto">
  <name>Task 3: Add the module-donut ⓘ Radix tooltip caveat + align the INTEGRATIONS.md service note</name>
  <files>app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx, .planning/codebase/INTEGRATIONS.md</files>
  <action>
    TRUTH-03 — module-donut caveat (info-icon + tooltip, candid, not always-on):
    1. In `AccessAnalysisCharts.tsx`, add a small ⓘ info-icon next to the "Activity by module" `SectionHeader` title (the SectionHeader at the module donut, currently `title="Activity by module"`). Wrap the ⓘ in Radix `Tooltip`/`TooltipTrigger`/`TooltipContent` from `@/components/ui/tooltip`, and — because no global `TooltipProvider` exists in this tree — wrap the trigger in a local `TooltipProvider` (import it from the same module). The ⓘ glyph should be a small muted icon (`text-muted-foreground`, ~`h-3.5 w-3.5` or a unicode ⓘ) with an accessible label (e.g. `aria-label="About module classification"`); keep it inline with the title without breaking the existing flex header.
       - Tooltip copy (candid, ≤ ~2 short sentences): classification is derived from each activity's `rawAction`, and Autodesk's own `service` product-attribution is not yet reconciled — the two disagree on ~40.7% of rows. Give the content `data-testid="module-caveat"`.
       - This MUST be hover/focus-only (Radix default) — NOT an always-on caption. "Honest at a glance, full candor on hover."
       - The `SectionHeader` helper takes a `badge` slot — it is acceptable to pass the ⓘ+tooltip through that slot or to add a small adjacent element; pick the option that keeps the title row clean (Claude's discretion). Do NOT alter the other `SectionHeader` call sites.
       - Motion: rely on the tooltip's existing fade/zoom animation (already ≤200ms-class); add none.
    2. `.planning/codebase/INTEGRATIONS.md`: the existing line ~179 already states "AccActivity.service field carries Autodesk product attribution (40.7% of rows) — not used by classifyActivity (uses rawAction only)". Expand it (one or two lines) so it explicitly frames the ~40.7% as the unreconciled `service`-vs-`rawAction` disagreement that the module-donut ⓘ tooltip surfaces, matching the tooltip wording. Keep the edit scoped to that note; do NOT touch the AccDcRole fallback note (plan 11-01 owns it) to avoid an intra-phase conflict.
    Optionally add a small assertion to an existing access-analysis test that the `module-caveat` content text contains "rawAction" / "40.7%" — only if it fits the existing jsdom test harness cleanly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rg -n "rawAction" "app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx" | grep -v '^#'</automated>
  </verify>
  <done>The "Activity by module" title carries a hover/focus-only ⓘ Radix tooltip (wrapped in a local TooltipProvider) stating rawAction-based classification + the ~40.7% unreconciled `service` gap; INTEGRATIONS.md line ~179 is aligned to that wording; `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → RSC (`loadDcCoverage`) | Two aggregate counts read from Postgres; no untrusted input crosses |
| RSC → client component | `dcCoverage` serialized as `{covered,total}` to AccessAnalysisCharts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information disclosure | coverage header line + caveat tooltip | accept | Disclosing coverage ratios and a classification caveat is the explicit, owner-approved goal of the phase; no sensitive data |
| T-11-07 | Spoofing/Tampering | hard-coded vs live counts | mitigate | Counts come from `count()` queries + props; the "428" negative-grep gate and "no literal numbers in JSX" rule prevent a stale/spoofed figure from shipping |

No package installs in this plan (Radix tooltip + Prisma already in deps) → no supply-chain (T-{phase}-SC) checkpoint required.
</threat_model>

<verification>
- `npx vitest run lib/server/__tests__/dcCoverageView.test.ts` passes (live `{covered,total}`; covered ≤ total; no hard-coded constants).
- `npx tsc --noEmit` passes (new `dcCoverage` prop threaded mainCharts → AccessAnalysisCharts; ⓘ tooltip typed).
- `rg "428" app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx` returns ZERO matches (rejected framing absent).
- `rg "rawAction" app/(dashboard)/access-analysis/components/AccessAnalysisCharts.tsx` shows the caveat tooltip copy.
- No hard-coded coverage integers in JSX (header line reads `covCovered`/`covTotal`/`dcCoverage` props only).
- Existing per-chart `ActivityCoverageBadge`s and the 11-03 timeline floor wiring are intact (no regression in the shared file).
- Visual gate (end-of-phase, deferred): on `/access-analysis`, a muted header coverage line leads with ~956/1,153 activity + ~550 DC; the module donut title shows a ⓘ whose tooltip appears on hover/focus only with the rawAction/40.7% copy; zinc theme + resolved colors; no new WebGL; `/users/spatial-graph` untouched. (Deploy = rebuild on :3000; do NOT `npm run build` under a running :3000.)
</verification>

<success_criteria>
`/access-analysis` shows a live, metric-specific coverage header line leading with free-crawl activity coverage (~956/1,153) plus DC-metadata coverage (~550/1,153) — never the rejected 428 framing — and the module-activity donut carries a hover/focus-only ⓘ tooltip disclosing the rawAction/`service` (~40.7%) caveat, with INTEGRATIONS.md aligned (ROADMAP Phase 11 success criteria 1 + 3 / REQUIREMENTS TRUTH-01 + TRUTH-03).
</success_criteria>

<dashboard_self_check>
- Context: 11-CONTEXT.md (TRUTH-01 owner correction → free-crawl 956/1,153 leads, 428 rejected; TRUTH-03 ⓘ tooltip not always-on), STATE.md census (956/1,153/550), coverageCounts.ts + projectCoverageView.ts (existing live activity coverage), ActivityCoverageBadge.tsx (reuse), AccessAnalysisCharts.tsx (SectionHeader host), tooltip.tsx (no global provider), INTEGRATIONS.md line ~179 (40.7% note).
- Evidence: `covCovered`/`covTotal` already derived from `loadProjectCoverage` (≈956/1,153); `TooltipProvider` only in UsersDirectoryClient → must wrap locally; INTEGRATIONS.md already records 40.7% (expand, don't invent); dependency on 11-03 because both edit AccessAnalysisCharts.tsx + mainCharts.tsx.
- Constraints: zinc theme + semantic CSS vars; muted text (`text-muted-foreground`, `text-xs`/`text-[10px]`); NO new bordered card; reuse existing badge family; hover/focus-only tooltip; ≤200ms motion (tooltip default only); no new WebGL; spatial-graph untouched; live counts only (no hard-coded 428/550/956/1153 in JSX).
- Gates: dcCoverageView Vitest + `npx tsc --noEmit` + two rg gates (no "428", caveat copy present); visual + light/dark deferred to end-of-phase per `human_verify_mode: end-of-phase`.
- VERIFY: none open — counts are live; the 428-rejection and "no hard-coded number" rules are enforced by the negative grep.
</dashboard_self_check>

<output>
Create `.planning/phases/11-data-truthfulness-labels/11-04-SUMMARY.md` when done.
</output>
