---
phase: 11-data-truthfulness-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [TRUTH-04]
must_haves:
  truths:
    - "INTEGRATIONS.md fully documents the AccDcRole-empty → AccRole role-name fallback and its DC-conflict behavior, not just the one-line known-gap note that exists today"
  artifacts:
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Authoritative record of the AccDcRole→AccRole fallback + DC-conflict behavior, reusing the existing 'downstream views must label this' precedent phrasing"
      contains: "AccDcRole"
  key_links:
    - from: ".planning/codebase/INTEGRATIONS.md"
      to: "lib/acc (mergeRoleNames) + AccDcRole/AccRole Prisma models"
      via: "documentation cross-reference of the empty-DC-role fallback path"
      pattern: "mergeRoleNames"
---

<objective>
Document the `AccDcRole`-empty → `AccRole` role-name fallback (and what happens when DC and the live ACC API disagree on a role name) in `.planning/codebase/INTEGRATIONS.md`, closing CONCERNS §2.4 (TRUTH-04).

This is a pure documentation add. No app code, no UI, no DB query. INTEGRATIONS.md today carries only a single terse "known gap" line (line ~64: "AccDcRole permanently empty (DC never sends admin_roles.csv); role names sourced from live AccRole via mergeRoleNames"). TRUTH-04 asks for a fuller account of the fallback path and the conflict-resolution behavior, written in the same candid, "downstream views must label this" voice the ACCDS section already uses (line ~104).

Purpose: a presenter or future maintainer can read INTEGRATIONS.md and understand why role names on `/access-analysis` come from `AccRole` rather than `AccDcRole`, and what is shown if the two sources disagree — without reading source.
Output: an expanded role-fallback subsection in INTEGRATIONS.md under/near the existing ACC Data Connector section.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-data-truthfulness-labels/11-CONTEXT.md

# Target doc + the existing precedent phrasing it should reuse
@.planning/codebase/INTEGRATIONS.md

# Source of truth for the fallback behavior being documented — read to ground
# the wording against actual code; do NOT modify these files in this plan.
@lib/server/projectCoverageView.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the role-fallback behavior against source before writing</name>
  <files>(read-only verification — no file writes in this task)</files>
  <action>
    Ground the documentation against actual code so the prose is evidence-backed, not invented.
    1. Locate the role-name fallback: search the repo for `mergeRoleNames` (rg) and read its definition and the call site that fills role names from `AccRole` when `AccDcRole` is empty. Confirm which module owns it (the existing INTEGRATIONS line claims it lives in `lib/`). Record the exact file path.
    2. Confirm in `prisma/schema.prisma` that both `AccDcRole` and `AccRole` models exist and note the fields used for matching (e.g. role id / name).
    3. Confirm the DC-conflict behavior: determine, from the merge code, which source WINS when DC supplies a role and the live `AccRole` also supplies one (precedence), and what is shown if neither resolves.
    4. If any of these cannot be verified from source, capture the gap as a `VERIFY:` note to embed in the doc rather than asserting it as fact.
    Do NOT write to any file in this task; this is the grounding pass whose findings Task 2 records.
  </action>
  <verify>
    <automated>rg -n "mergeRoleNames" --type ts | head -20</automated>
  </verify>
  <done>The exact module owning `mergeRoleNames`, the `AccDcRole`/`AccRole` schema fields used, and the DC-vs-AccRole precedence behavior are confirmed from source (or explicitly flagged `VERIFY:` where unconfirmable).</done>
</task>

<task type="auto">
  <name>Task 2: Expand the AccDcRole→AccRole fallback documentation in INTEGRATIONS.md</name>
  <files>.planning/codebase/INTEGRATIONS.md</files>
  <action>
    Per TRUTH-04 (CONCERNS §2.4): expand the role-fallback documentation in `.planning/codebase/INTEGRATIONS.md`. Place it under/near the existing ACC Data Connector section, adjacent to the current one-line known-gap note (line ~64). Wording is Claude's discretion per CONTEXT, but it MUST:
    - State that `AccDcRole` is permanently empty because the Data Connector never delivers `admin_roles.csv`, so role names cannot come from DC.
    - Document the fallback: role names are sourced from the live `AccRole` table via the merge helper confirmed in Task 1 (cite the exact module path found in Task 1, not a guess).
    - Document the DC-conflict behavior from Task 1: which source wins when both supply a role, and what surfaces when neither resolves a name.
    - Reuse the candid precedent voice already in the file — mirror the ACCDS "downstream views must label this" phrasing style (line ~104) so the doc reads consistently.
    - Embed any unresolved item from Task 1 as a `VERIFY:` line rather than asserting it.
    Keep the edit scoped: do NOT rewrite unrelated sections, do NOT touch the stale "428/1,152" DC line (TRUTH-01 handles coverage in the UI, not in this doc), and do NOT renumber or reflow the rest of the file. Update the trailing audit-provenance line only if you add a new heading.
    Note: the existing line ~179 ("AccActivity.service ... 40.7% ... not used by classifyActivity") is the TRUTH-03 target and is edited by plan 11-04 — leave it untouched here to avoid a merge conflict within wave assignment.
  </action>
  <verify>
    <automated>rg -n "AccDcRole" ".planning/codebase/INTEGRATIONS.md" | grep -v '^#' | head</automated>
  </verify>
  <done>INTEGRATIONS.md contains an expanded subsection documenting the `AccDcRole`-empty → `AccRole` fallback (with the verified module path) and the DC-conflict precedence, written in the existing precedent voice; no unrelated sections were modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change; no code path, input surface, or data flow is added or altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | INTEGRATIONS.md content | accept | Doc records architecture facts already present in the repo; contains no secrets, tokens, or env values (Local Secret Hygiene honored — env vars referenced by NAME only if at all) |

No package installs in this plan → no supply-chain (T-{phase}-SC) checkpoint required.
</threat_model>

<verification>
- `rg -n "AccDcRole" .planning/codebase/INTEGRATIONS.md` shows the expanded fallback documentation.
- The cited module path for the merge helper matches what Task 1 found in source (no invented path).
- No literal secrets/tokens/DATABASE_URL values added (Local Secret Hygiene).
- Diff is confined to INTEGRATIONS.md; `git status --short` shows only that file changed.
- No app code, no `/access-analysis` files, and `/users/spatial-graph` untouched.
- No build/tsc needed — this plan changes only a `.planning/` markdown doc.
</verification>

<success_criteria>
`.planning/codebase/INTEGRATIONS.md` documents the `AccDcRole`-empty → `AccRole` fallback and the DC-conflict behavior (ROADMAP Phase 11 success criterion 4 / REQUIREMENTS TRUTH-04), grounded in the verified `mergeRoleNames` module path, with any unconfirmable detail marked `VERIFY:`.
</success_criteria>

<dashboard_self_check>
- Context: read 11-CONTEXT.md, ROADMAP/REQUIREMENTS (TRUTH-04 → §2.4), STATE.md, INTEGRATIONS.md (existing line ~64 known-gap + line ~104 ACCDS precedent + line ~179 TRUTH-03 target).
- Evidence: `mergeRoleNames` + `AccDcRole`/`AccRole` to be re-confirmed from source in Task 1 before the doc asserts a path; existing INTEGRATIONS line ~64 already names the fallback.
- Constraints: documentation-only; Local Secret Hygiene; scoped diff (no unrelated section rewrites); no overlap with the TRUTH-03 INTEGRATIONS edit (plan 11-04 owns line ~179).
- Gates: rg checks only — no tsc/rebuild needed for a `.planning/` doc.
- VERIFY: exact owning module of `mergeRoleNames` and the DC-vs-AccRole precedence are resolved in Task 1; anything still open is embedded as `VERIFY:` in the doc.
</dashboard_self_check>

<output>
Create `.planning/phases/11-data-truthfulness-labels/11-01-SUMMARY.md` when done.
</output>
