---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 04
type: execute
wave: 3
depends_on: ["04.1-02", "04.1-03"]
files_modified:
  - app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-04
  - DASH-07
  - DASH-08
  - DASH-09
  - DASH-13

must_haves:
  truths:
    - "Existing five graphical widgets read severity colors from _shared/dashboardTokens.ts (no hardcoded #EF4444 / #F59E0B / #3B82F6 anywhere in those files)"
    - "Hover treatment unified: hovered element opacity HOVER_OPACITY_FOCUS, siblings dim to HOVER_OPACITY_DIM (where applicable per widget)"
    - "Selection-aware highlight: when useSelection().selected matches an element these widgets render, that element animates to the spotlight state"
    - "Reduced-motion users: any animations these widgets ship pass through useTransition wrapper"
    - "User human-verify confirms: dashboard feels like one product end-to-end (cosmos-aligned palette + motion vocabulary), no jarring style mismatches"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx"
      provides: "Polish pass — token consumption + spotlight"
    - path: "app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx"
      provides: "Polish pass — token consumption + spotlight"
    - path: "app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx"
      provides: "Polish pass — token consumption (severity badge colors)"
    - path: "app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx"
      provides: "Polish pass — selection-aware highlighting via useSelection roleSeverityIndex (already wired Phase 4 — verify still works post-token-swap)"
    - path: "app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx"
      provides: "Polish pass — token consumption on custom node types"
  key_links:
    - from: "All five existing graphical widgets"
      to: "_shared/dashboardTokens.ts"
      via: "Severity color imports replace hardcoded hex"
      pattern: "from [\"']\\./_shared/dashboardTokens[\"']"
---

<objective>
Polish-only pass on the five widgets that survived the reskin (Coverage donut, Active-user tiers, KPI strip, Roles×Modules heatmap, Role-relationship flow). Pull severity colors from `_shared/dashboardTokens.ts`, unify hover treatment to the new `HoverSpotlight` pattern where applicable, wire selection-aware spotlighting where elements correspond to a `useSelection().selected` value, and respect reduced-motion via `useTransition`. NO functional changes — capabilities, signatures, CSV exports, and registry contract are untouched.

Purpose: CONTEXT.md "existing graphical widgets eligible for in-scope polish — Planner / researcher decides per widget whether a polish pass is needed." Ship a single coherent visual language across the dashboard so the cosmos-feel claim survives end-to-end review. Includes the human-verify checkpoint that approves Phase 4.1.

Output: Edits in place to all five widget files, plus a single human-verify checkpoint covering the entire dashboard at 1280px in both themes.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-CONTEXT.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-RESEARCH.md
@app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx
@app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx
@app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx
@app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx
@app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token + spotlight polish on the five existing graphical widgets</name>
  <files>
    app/(dashboard)/users/dashboard/widgets/CoverageDonutWidget.tsx,
    app/(dashboard)/users/dashboard/widgets/ActiveUserTiersWidget.tsx,
    app/(dashboard)/users/dashboard/widgets/KpiStripWidget.tsx,
    app/(dashboard)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx,
    app/(dashboard)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx
  </files>
  <action>
For each of the five widgets, perform the consistency pass below. Make only the changes that produce a visible diff — if a widget already does the right thing, leave it alone and document the no-op in the SUMMARY.

**For all five — common edits:**
1. Search the file for any hardcoded severity hex (`#EF4444`, `#F59E0B`, `#3B82F6`, plus close cousins). Replace with imports from `./_shared/dashboardTokens` via `useSeverityColor()` (theme-aware).
2. Wrap any framer-motion `transition` prop already in the file with `useTransition(...)` so reduced-motion users get duration: 0.
3. If the widget renders elements that correspond to a `useSelection().selected` (e.g., heatmap cells for a role, flow nodes for a role, KPI strip "X HIGH-severity roles" pills), wire a spotlight: when `selected?.kind === "role"|"junk"|"duplicate"` and the role matches, that element jumps to the focus opacity / scale and gets a `<FocusRing>`; siblings dim to HOVER_OPACITY_DIM.

**Per-widget specifics:**

- **CoverageDonutWidget.tsx:** Likely a SVG/Nivo donut. If Nivo controls colors, override its `colors` prop with an array sourced from tokens (NEUTRAL for "in both", a HIGH-leaning hue for missing-from-ACC, a LOW-leaning hue for missing-from-Workspace). Hover-arc lift via Nivo's built-in `isInteractive` is fine — no need to rewrite as raw SVG.

- **ActiveUserTiersWidget.tsx:** Stacked bar across 7d/30d/90d/>90d/never. Map the five buckets to a perceptual sequence sourced from tokens (e.g., HIGHLIGHT → NEUTRAL gradient). Hover dim siblings via spotlight. No selection wiring needed (no kind matches a tier).

- **KpiStripWidget.tsx:** Severity-count pills (e.g., "3 HIGH"). Replace pill background colors with `useSeverityColor()` map. If the pill is clickable to filter recommendations, leave behavior alone — only swap colors.

- **RolesModulesHeatmapWidget.tsx:** ECharts heatmap with inline severity dots in axis labels (Phase 4 P08). The dot colors are currently sourced from `findingsContext` `roleSeverityIndex`. Verify those colors come from tokens; if they're hardcoded inside the rich-text formatter, swap them to `useSeverityColor()` resolved at render time and passed into the formatter. Add: when `selected?.kind === "role"`, dim heatmap rows whose y-axis label is not the selected role (use `series.itemStyle.opacity` via formatter trick OR set `series.data` opacity per cell). Skip this enhancement if it requires re-architecting the ECharts series — log decision and move on.

- **RoleRelationshipFlowWidget.tsx:** @xyflow/react custom nodeTypes (Phase 4 P08). Replace any hardcoded severity colors in custom node renderers with `useSeverityColor()`. When `selected?.kind === "role"`, animate non-matching nodes to opacity 0.4 via xyflow's `style.opacity` or a className the node renderer reads from a context.

**Discipline:** A polish pass is *not* a redesign. If a change requires rewriting more than ~30 lines per widget or touching props of registry / data-fetch code, stop and document as "deferred to v1.x" in the SUMMARY. CONTEXT explicitly says "no functional changes, no scope expansion."

After edits, run `npx tsc --noEmit` and confirm zero errors in any of the five files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(CoverageDonut|ActiveUserTiers|KpiStrip|RolesModulesHeatmap|RoleRelationshipFlow)Widget\.tsx" | head -10 ; echo "---hex-leak-check---" ; grep -nE "#EF4444|#F59E0B|#3B82F6|#FF6B6B|#FFB454|#5DADE2" app/\(dashboard\)/users/dashboard/widgets/CoverageDonutWidget.tsx app/\(dashboard\)/users/dashboard/widgets/ActiveUserTiersWidget.tsx app/\(dashboard\)/users/dashboard/widgets/KpiStripWidget.tsx app/\(dashboard\)/users/dashboard/widgets/RolesModulesHeatmapWidget.tsx app/\(dashboard\)/users/dashboard/widgets/RoleRelationshipFlowWidget.tsx 2>/dev/null | head -10</automated>
  </verify>
  <done>All five files compile; no hardcoded severity hex remains in any of them (or, where unavoidable inside a third-party config, the value is sourced from the shared token module via JS); spotlight wiring lands where applicable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 4.1 human-verify (single checkpoint covering all four reskins + polish)</name>
  <files>app/(dashboard)/users/dashboard/**</files>
  <action>Pause execution. Present the what-built + how-to-verify steps to the user. Wait for "approved" or a list of gaps before resuming. On gap report, recommend `/gsd:plan-phase 04.1 --gaps`.</action>
  <verify>User responds "approved" — no automated check (UI verification by definition).</verify>
  <done>User types "approved".</done>
  <what-built>
- Four widgets rewritten as interactive graphics: Recommendations (severity bubble cluster), Outliers (treemap), RecentlyAdded (90-day calendar heatmap), AdminAccess (orbit constellation).
- Five existing widgets received a polish pass for token consistency and selection spotlighting.
- Side panel handles two new selection kinds (admin, day) and reconciles selection on data refresh.
- All widgets are reduced-motion-aware and keyboard-navigable.
  </what-built>
  <how-to-verify>
1. **Boot dev server:** `npm run dev` (or use the running one) and visit `/users/dashboard` (or whichever route hosts the dashboard — Phase 4 P08 sidebar entry).
2. **Light + dark theme parity** — toggle `next-themes` and visually scan all nine widgets. The four reskinned widgets should feel cohesive with the cosmos.gl graph tab in BOTH themes. Severity colors (HIGH/MEDIUM/LOW) should be the same hues across Recommendations bubble, KPI pills, RolesModulesHeatmap dots, and RoleFlow nodes.
3. **Bubble cluster (Recommendations):**
   - HIGH/MEDIUM/LOW visually distinct via color, member-count-distinct via size; junk left, duplicate right.
   - Hover one bubble → it glows, others dim to ~40% opacity.
   - Click → side panel opens with the correct JunkBody / DuplicateBody.
   - Press Tab to focus the widget, then arrow keys move focus, Enter opens side panel — drag does NOT activate.
4. **Treemap (Outliers):**
   - Cells sized by member count, color intensity by rarity.
   - Click → side panel opens with the correct OutlierBody.
   - Long module names don't bleed out of small cells.
5. **Calendar (RecentlyAdded):**
   - 90-day grid renders; today is on the right.
   - 7d / 30d / 90d toggle dims non-band cells.
   - Click a populated day → side panel opens with that day's members.
   - Tooltip shows count only, NO names.
6. **Constellation (AdminAccess):**
   - Orbit ring of admin nodes (or correctly-handled 0/1/2-admin edge case).
   - Hover a node → initials reveal (no full name, no email).
   - Click → side panel opens with that admin.
7. **Cross-widget selection linking (CONTEXT spec):** Click a HIGH-severity bubble. Confirm the role's row in the Roles×Modules heatmap and the role's node in the Flow widget both spotlight (others dim).
8. **Refresh resilience:** Click any finding → side panel open. Click Refresh on the dashboard. Confirm panel either (a) stays open with refreshed counts if the finding still exists, or (b) closes gracefully if it's gone.
9. **Reduced-motion:** OS-level Settings → Reduce Motion ON. Reload `/users/dashboard`. Confirm entrance animations are absent on the four reskinned widgets but hover/selection still respond instantly.
10. **CSV exports:** click the CSV button on each of the four reskinned widgets and the heatmap. Each download should produce a non-empty CSV with the previously-locked column order (recommendations: `Type,Severity,Roles,Members,Modules,SuggestedAction`).
11. **Drag-reorder still works:** drag a widget. Order persists across reload (Phase 4 contract).
12. **No member-name leak in canvas (the headline goal):** scan each of the four reskinned widget bodies — confirm zero email or full-name strings rendered in the SVG. Constellation initials count as initials, not names.
  </how-to-verify>
  <resume-signal>Type "approved" if everything passes, or list specific gaps (which step failed and what you saw). On gaps, planner runs `/gsd:plan-phase 04.1 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Hex-leak grep returns no matches inside the five polished widget files.
- Human-verify checkpoint approved by user.
</verification>

<success_criteria>
- Polish pass: severity colors flow through tokens; spotlight applied where matchable.
- Phase 4.1 ships only when the human-verify checkpoint returns "approved".
- No functional regressions to Phase 4 capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-04-SUMMARY.md`
</output>
