---
phase: 04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
  - app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-09
  - DASH-10
  - DASH-11

must_haves:
  truths:
    - "RecommendationsWidget renders SVG bubble cluster — zero <table>, <ul>, or member-name DOM in the widget body"
    - "Each bubble's color encodes severity (HIGH/MEDIUM/LOW) using SEVERITY_COLOR; size encodes affected-member count via d3-hierarchy.pack()"
    - "Junk findings cluster in left region; duplicate findings cluster in right region (two grouped sub-trees)"
    - "Bubble click dispatches setSelected({ kind: 'junk'|'duplicate', finding }) — opens existing DashboardSidePanel"
    - "OutlierCombosWidget renders SVG treemap — zero <table>, zero member names; cells sized by member count, color intensity by rarity"
    - "Treemap cell click dispatches setSelected({ kind: 'outlier', finding })"
    - "Both widgets keep CSV download button in their header (data exported = same shape as Phase 4)"
    - "Both widgets show shape-matched skeleton (BubbleSkeleton / TreemapSkeleton) on loading state"
    - "Hover spotlights: hovered element animates to opacity 1.0; siblings animate to 0.4"
    - "Reduced-motion users: entrance + diff animations skipped; hover/selection transitions kept (subtle)"
    - "Keyboard nav: tab into widget, arrow keys move focus across shapes, Enter dispatches setSelected; SortableWidget drag does NOT fire"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx"
      provides: "Severity bubble cluster (SVG, d3-hierarchy.pack + framer-motion)"
      contains: "pack()"
      min_lines: 100
    - path: "app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx"
      provides: "Module-set treemap (SVG, d3-hierarchy.treemap + framer-motion)"
      contains: "treemap()"
      min_lines: 80
  key_links:
    - from: "RecommendationsWidget.tsx"
      to: "selectionContext setSelected"
      via: "circle onClick"
      pattern: "setSelected\\(\\{\\s*kind:\\s*[\"']junk[\"']|setSelected\\(\\{\\s*kind:\\s*[\"']duplicate[\"']"
    - from: "OutlierCombosWidget.tsx"
      to: "selectionContext setSelected"
      via: "rect/g onClick"
      pattern: "setSelected\\(\\{\\s*kind:\\s*[\"']outlier[\"']"
    - from: "Both widgets"
      to: "_shared/dashboardTokens.ts"
      via: "import { SEVERITY_COLOR_*, BUBBLE_SPRING / CELL_SPRING, HOVER_OPACITY_* }"
      pattern: "from [\"']\\./_shared/dashboardTokens[\"']"
---

<objective>
Rewrite RecommendationsWidget (table → severity bubble cluster) and OutlierCombosWidget (table → squarified treemap) using SVG + d3-hierarchy + framer-motion. No member names anywhere in the canvas; click opens the side panel via the existing `selectionContext`. Keep widget IDs (`recommendations`, `outliers`) and `widgetRegistry.ts` spans unchanged so localStorage drag-order survives.

Purpose: First half of the four name-list-widget reskin (CONTEXT.md locked decision). These two share `setSelected({kind:"junk"|"duplicate"|"outlier"})` which already works post Wave 1, so they're paired in one plan to keep file ownership clean for parallel execution with 04.1-03.

Output: Two widget files fully rewritten in place (same exports — `RecommendationsWidget`, `OutlierCombosWidget`); CSV button preserved; skeletons swapped to shape-matched primitives.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-CONTEXT.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-RESEARCH.md
@.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-01-PLAN.md
@app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx
@app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx
@app/(dashboard)/users/dashboard/widgetRegistry.ts
@app/(dashboard)/users/dashboard/findingsContext.tsx
@app/(dashboard)/users/dashboard/selectionContext.tsx

<interfaces>
<!-- From lib/acc/dashboardAnalytics.ts -->
```ts
export type Severity = "HIGH" | "MEDIUM" | "LOW";
export interface JunkRoleFinding { role: string; severity: Severity; affectedMembers: string[]; affectedProjects: string[]; signals: { zeroMembers: boolean; zeroModules: boolean; allInactive90d: boolean }; }
export interface DuplicateRoleFinding { roleA: string; roleB: string; nameOverlap: number; affectedMembers: string[]; affectedProjects: string[]; }
export interface OutlierFinding { moduleSet: string[]; affectedMembers: string[]; memberCount: number; totalMembers: number; pct: number; }
```

<!-- From _shared/dashboardTokens.ts (Wave 1) -->
```ts
useSeverityColor(): { HIGH: string; MEDIUM: string; LOW: string }
BUBBLE_SPRING / CELL_SPRING: framer-motion Transition
HOVER_OPACITY_DIM (0.4) / HOVER_OPACITY_FOCUS (1.0)
```

<!-- WidgetCommonProps (widgetRegistry.ts) -->
```ts
interface WidgetCommonProps { users: BulkAccUser[]; workspaceEmails: string[]; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite RecommendationsWidget as severity bubble cluster</name>
  <files>app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx</files>
  <action>
Replace the entire file contents. Keep the named export `RecommendationsWidget` and the same `WidgetCommonProps` signature (the widget can ignore props it doesn't need). Implementation:

1. **Imports:** `useMemo`, `useRef`, `useState` from React; `hierarchy, pack` from `d3-hierarchy`; `motion, AnimatePresence` from `framer-motion`; `useFindings` from `../findingsContext`; `useSelection` from `../selectionContext`; tokens from `./_shared/dashboardTokens`; `useTransition` from `./_shared/useTransition`; `BubbleSkeleton` from `./_shared/WidgetSkeleton`; `useHoverSpotlight` from `./_shared/HoverSpotlight`; `FocusRing` from `./_shared/FocusRing`; `Button` from `@/components/ui/button`; `Download` from `lucide-react`; `downloadCsv` from `@/lib/acc/csvExport`.

2. **Layout:** width 600, height 360 (fits col-span-2 card). `viewBox`-based SVG with `preserveAspectRatio="xMidYMid meet"` and `className="w-full h-auto"`. Build a two-level hierarchy: root → [junkRoot, duplicateRoot] → leaves. Use `pack().size([W, H]).padding(8)`. Force junk children into left half / duplicate into right half by either (a) using a horizontal force-displacement on the root packing using two sub-packs sized to [W/2, H], OR (b) pre-positioning two `pack()` calls — one at x∈[0, W/2-pad], another at x∈[W/2+pad, W]. Pick (b) for determinism.

3. **Color:** `useSeverityColor()` for HIGH/MEDIUM/LOW. Junk severity comes from `finding.severity` directly. For Duplicate findings, derive severity = MEDIUM (per Phase 4 P03 decision: "duplicate-flagged roles contribute MEDIUM"). Bubble fill = severity color; subtle stroke for legibility (`stroke="rgba(0,0,0,0.15)"`, `strokeWidth={1}`).

4. **Sizing:** value = `Math.max(1, finding.affectedMembers.length)` so zero-member junk bubbles still render.

5. **Animation:** wrap in `<AnimatePresence>`; each `motion.circle` gets `initial={{ opacity: 0, scale: 0.4 }}`, `animate={{ opacity: getOpacity(id), scale: 1 }}`, `exit={{ opacity: 0, scale: 0.4 }}`, `transition={useTransition(BUBBLE_SPRING)}`. Animate `cx`, `cy`, `r` directly via `animate={{ cx, cy, r }}` — DO NOT use the `layout` prop on SVG (Pitfall 3 — perf).

6. **layoutId namespacing (Pitfall 1):** Each circle gets `layoutId={`recommendations:${kind}:${id}`}` where `id` is `finding.role` for junk, `${roleA}|${roleB}` for duplicate.

7. **Hover spotlight:** wire `useHoverSpotlight()`; `bind(id)` returns mouse handlers attached to a wrapping `<g role="button" tabIndex={0}>` per bubble (Pitfall 8 — `tabIndex` on `<g>`, not `<circle>`). `getOpacity(id)` drives the circle's animate.opacity.

8. **Keyboard nav:** Track a `focusIndex` state. Root SVG `onKeyDown` (with `event.stopPropagation()` so the SortableWidget drag listeners don't fire on Enter — Pitfall 2): ArrowRight/Down → focusIndex++, ArrowLeft/Up → focusIndex--, Enter → call `setSelected` for the focused leaf. Render `<FocusRing>` around the focused bubble.

9. **Click handler:** `setSelected({ kind: leaf.data._kind, finding: leaf.data })` where `_kind` is "junk" or "duplicate".

10. **Aria:** SVG `role="img" aria-roledescription="severity bubble cluster"`. Each `<g>` gets `aria-label={`${severity} severity, ${title}, ${count} members`}`. Add a visually hidden summary `<title>` element on the SVG: "{N} junk findings, {M} duplicate findings".

11. **Loading state:** when `useFindings()` returns the sentinel empty arrays AND `users.length === 0`, render `<BubbleSkeleton />` instead of the SVG (Pitfall 9 — never let AnimatePresence see an empty exit).

12. **Empty state:** if findings returns 0 junk + 0 duplicate, render a centered text node "No recommendations — all roles healthy".

13. **CSV button:** Card-internal header row at the top with title-less `<Button variant="outline" size="sm">` containing `<Download />` + "CSV". Export uses Phase 4 column order: `Type, Severity, Roles, Members, Modules, SuggestedAction`. Reuse the column derivation from the previous file's `buildCsvRows` logic — preserve it byte-for-byte if it exists, else port it from the Phase 4 file (`Modules` column emits empty for junk findings whose `signals.zeroModules` is true, otherwise union of affected-member modules via per-user lookup; `SuggestedAction` is the same string used in the side panel JunkBody helper).

14. **Selected highlight:** if `useSelection().selected` matches a bubble (`kind === leaf.data._kind && id matches`), force `opacity: 1` and add a `scale: 1.04` and `<FocusRing>` around it.

Do not import any d3-force or React-flow modules. Do not pull from ECharts or Nivo. SVG only.

After writing, run `npx tsc --noEmit` and confirm no errors in this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "RecommendationsWidget" | head -10 ; echo "---" ; grep -nE "table|<ul|<li|<TableRow|<TableCell" "app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx" | head -5 ; echo "---names-check---" ; grep -nE "u\.email|user\.email|finding\.affectedMembers\.map" "app/(dashboard)/users/dashboard/widgets/RecommendationsWidget.tsx" | head -5</automated>
  </verify>
  <done>File compiles; contains `pack()` + `motion.circle`; contains zero `<Table*>` / `<ul>` / `<li>` JSX; setSelected called for both junk and duplicate kinds; CSV button present with Phase 4 column order.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite OutlierCombosWidget as squarified treemap</name>
  <files>app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx</files>
  <action>
Replace the entire file. Keep the named export `OutlierCombosWidget` and the same WidgetCommonProps signature.

1. **Imports:** `useMemo`, `useState` from React; `hierarchy, treemap, treemapSquarify` from `d3-hierarchy`; `scaleSequential` from `d3-scale`; `interpolateOranges` from `d3-scale-chromatic`; `motion, AnimatePresence` from `framer-motion`; tokens / hooks from `./_shared/*`; `TreemapSkeleton` from WidgetSkeleton; CSV helpers + Button + Download icon as in Task 1.

2. **Layout:** W=360, H=320 (fits col-span-1 with header). `viewBox` SVG full-width-of-card. `treemap().size([W, H]).tile(treemapSquarify).padding(2)` over a single-level hierarchy of `outliers` from `useFindings()`, value = `o.affectedMembers.length || 1`.

3. **Color:** `scaleSequential(interpolateOranges).domain([0.05, 0])` so lower share (rarer) = darker. (Domain reversed so the <5% threshold from DASH-03 maps to deep orange.) Stable colorblind-readable.

4. **Cells:** `motion.g` per leaf with `initial={{ opacity: 0 }}`, `animate={{ opacity: getOpacity(id), x: node.x0, y: node.y0 }}`, `exit={{ opacity: 0 }}`, `transition={useTransition(CELL_SPRING)}`. Inner `motion.rect width={x1-x0} height={y1-y0} rx={2} fill={color(node.data.pct)}`. layoutId = `outliers:${moduleSet.join(",") || "empty"}`.

5. **Labels (Pitfall 7):** Render `<text>` with first module name only when `w > 60 && h > 24`; otherwise omit. Add a `<title>` child for native browser tooltip (`{moduleSet.join(", ")} — {memberCount} members ({(pct*100).toFixed(1)}%)`).

6. **Hover/focus/keyboard:** Same pattern as Task 1 — `useHoverSpotlight`, `<FocusRing>`, ArrowKeys + Enter, `event.stopPropagation()` on the SVG keydown.

7. **Click:** `setSelected({ kind: "outlier", finding: leaf.data })`.

8. **Loading:** `<TreemapSkeleton width={W} height={H} />` while findings are sentinel + users empty.

9. **Empty state:** If `outliers.length === 0`, render centered text "No outlier combinations (all module sets held by ≥5%)".

10. **CSV button:** header row, downloads outliers in same shape as Phase 4 (one row per outlier; columns include ModuleSet, Members, MemberCount, Total, Pct). If the previous file already has the row builder, port it verbatim.

11. **Aria:** SVG `role="img" aria-roledescription="module-set treemap"`. Each `<g>` gets `aria-label={`Module set ${modules.join(", ") || "(none)"}, ${memberCount} members, ${(pct*100).toFixed(1)}%`}`.

After writing, typecheck.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "OutlierCombosWidget" | head -10 ; echo "---" ; grep -nE "table|<ul|<li|<TableRow|<TableCell" "app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx" | head -5 ; echo "---" ; grep -n "treemap\|setSelected" "app/(dashboard)/users/dashboard/widgets/OutlierCombosWidget.tsx" | head -10</automated>
  </verify>
  <done>File compiles; contains `treemap()` and `motion.rect`; zero list/table JSX; setSelected fires with kind:"outlier"; CSV button present.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across both files.
- Manual eye-grep: neither widget body renders email or member name strings.
- Each widget keeps its widget-registry id and span (verify via `grep -n "RecommendationsWidget\|OutlierCombosWidget" app/(dashboard)/users/dashboard/widgetRegistry.ts` returns the existing entries).
</verification>

<success_criteria>
- RecommendationsWidget = bubble cluster, two grouped regions (junk left, duplicate right), severity-colored, member-count-sized.
- OutlierCombosWidget = squarified treemap, area = members, color = rarity.
- Both: keyboard nav works, hover spotlight applied, click opens existing side panel, CSV download still functional.
- Phase 4 contracts (widget IDs, spans, props) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-access-analysis-tab-replace-lists-with-astonishing-interactive-graphics-only-no-name-list-widgets/04.1-02-SUMMARY.md`
</output>
