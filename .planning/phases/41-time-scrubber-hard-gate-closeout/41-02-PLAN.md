---
phase: 41
plan: "41-02"
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - lib/server/activityUniverseTestFixture.ts (new + test)
  - app/api/activity-universe/payload/route.ts
  - tests/e2e/acc-dc-graph.spec.ts
  - tests/e2e/acc-3d-lasso.spec.ts
  - tests/e2e/spatial-graph-baseline.spec.ts
  - tests/e2e/activity-universe-hard-gate.spec.ts (new)
  - scripts/measure-spatial-graph-baseline.cjs
autonomous: true
requirements: [E2E-03]
must_haves:
  truths:
    - "A deterministic three-month activity fixture is served only when NEXT_PUBLIC_ACC_GRAPH_TEST=1 and ACC_ACTIVITY_TEST_FIXTURE=1; the same build serves the full machine artifact when the server-only fixture flag is absent"
    - "acc-dc-graph.spec.ts is replaced by a compact activity-native contract covering both aliases, finite/populated state, dimensions+morph, exact-month/All/playback truth, and reduced-motion static stepping"
    - "acc-3d-lasso.spec.ts is replaced by a 2D activity lasso gate proving a non-empty strict subset and visible selected count"
    - "The median-of-5 baseline reads __ACTIVITY_UNIVERSE_TEST__ and records full navigation-to-ready separately from the existing payload fetch+decode budget"
    - "The headed hard-gate spec rejects SwiftShader and records a >=10 second rAF sample with renderer, ambient, tier, and fps evidence"
  artifacts:
    - lib/server/activityUniverseTestFixture.ts
    - tests/e2e/acc-dc-graph.spec.ts
    - tests/e2e/acc-3d-lasso.spec.ts
    - tests/e2e/activity-universe-hard-gate.spec.ts
  key_links:
    - pattern: "ACC_ACTIVITY_TEST_FIXTURE"
      in: app/api/activity-universe/payload/route.ts
    - pattern: "__ACTIVITY_UNIVERSE_TEST__"
      in: tests/e2e/acc-dc-graph.spec.ts
    - pattern: "SwiftShader|d3d11|SCENARIO_MS"
      in: tests/e2e/activity-universe-hard-gate.spec.ts
---

# 41-02 — Activity fixture, E2E re-baseline, and measurement harness

## Objective

Replace retired instance-universe browser assumptions with a small deterministic
activity gate while preserving a separate full-scale workshop measurement path.

## Context (@ paths)

- @.planning/phases/41-time-scrubber-hard-gate-closeout/41-CONTEXT.md
- @lib/acc/columnarPayload.ts
- @lib/server/activityUniversePayload.ts
- @app/api/activity-universe/payload/route.ts
- @app/(dashboard)/users/access-analysis/activity/activityTestBridge.ts
- @tests/e2e/acc-dc-graph.spec.ts
- @tests/e2e/acc-3d-lasso.spec.ts
- @tests/e2e/activity-payload.spec.ts
- @tests/e2e/spatial-graph-baseline.spec.ts
- @tests/e2e/scale-spike.spec.ts
- @scripts/measure-spatial-graph-baseline.cjs

## Tasks

1. Add a deterministic organic-position activity fixture with three months,
   multiple modules/dimensions, honest meta, and enough points for strict-subset
   lasso. Unit-test that its binary decodes with aligned required columns and
   month counts. Route it only behind both test flags; never alter the real route
   when `ACC_ACTIVITY_TEST_FIXTURE` is absent.
2. Delete and replace the ~1,100-line instance `acc-dc-graph.spec.ts` with the
   locked activity bridge/selector contract. Delete and replace the 3D lasso spec
   with the activity overlay contract. Keep person-graph tests untouched.
3. Port `spatial-graph-baseline.spec.ts` to the activity bridge and record five
   navigation-to-ready runs. Add the headed D3D11 hard-gate recorder using the
   Phase-37 renderer guard and >=10 second sample; adapt the existing measurement
   orchestrator to run/read the payload, navigation, and hard-gate artifacts
   instead of creating a second orchestrator.

## Verification gates

- `npx vitest run lib/server/activityUniverseTestFixture.test.ts`
- `npx tsc --noEmit`
- Isolated fixture server (`NEXT_PUBLIC_ACC_GRAPH_TEST=1` build;
  `ACC_ACTIVITY_TEST_FIXTURE=1` at `next start`):
  `npx playwright test acc-dc-graph acc-3d-lasso --config playwright.verify.config.ts`

## Success criteria

E2E-03 is green on `:3100`; no test references `__ACC_GRAPH_TEST__`, retired
3D/similarity/user-footprint/search/preset/right-rail behavior, or the 22,279
instance count.

