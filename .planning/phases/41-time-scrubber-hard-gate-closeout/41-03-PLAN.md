---
phase: 41
plan: "41-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations-raw/2026-06-08-acc-instance-embedding.sql (delete)
  - prisma/migrations-raw/2026-07-21-drop-acc-instance-embedding.sql (new)
  - scripts/create-instance-embedding-table.ts (delete)
  - lib/server/acc-route-hydration.test.ts
  - app/(dashboard)/users/access-analysis/interactionTypes.ts
  - app/(dashboard)/users/access-analysis/SelectionContext.tsx (delete if zero-importer audit remains clean)
  - docs/erd.md (remove only the retired model block; preserve pre-existing AccActivityEmbedding WIP)
autonomous: true
requirements: [REND-04]
must_haves:
  truths:
    - "No runtime, test, script, or current generated-doc consumer references AccInstanceEmbedding before its table is dropped"
    - "The retired Prisma model/create SQL/create script are gone; an idempotent raw DROP migration is applied to the live local DB and retained as reproducible lineage"
    - "AccActivityEmbedding remains intact and its id comment matches the real accds:<id> | AccActivity.id stream"
    - "Only clean zero-runtime-importer instance sidebar files are deleted; catalogTargets.ts, dimensionCatalog.types.ts, catalogTargets.test.ts and every dirty file are preserved"
  artifacts:
    - prisma/schema.prisma
    - prisma/migrations-raw/2026-07-21-drop-acc-instance-embedding.sql
  key_links:
    - pattern: "DROP TABLE IF EXISTS \"AccInstanceEmbedding\""
      in: prisma/migrations-raw/2026-07-21-drop-acc-instance-embedding.sql
    - pattern: "accds:"
      in: prisma/schema.prisma
---

# 41-03 — Proven instance-era cleanup

## Objective

Remove the retired instance embedding boundary and only importer-proven clean
orphans, without staging the repository's unrelated WIP.

## Context (@ paths)

- @.planning/phases/41-time-scrubber-hard-gate-closeout/41-CONTEXT.md
- @.planning/codebase/CONCERNS.md (§3.11)
- @prisma/schema.prisma
- @prisma/migrations-raw/2026-06-08-acc-instance-embedding.sql
- @scripts/create-instance-embedding-table.ts
- @lib/server/acc-route-hydration.test.ts
- @app/(dashboard)/users/access-analysis/interactionTypes.ts
- @docs/erd.md

## Tasks

1. Re-run `rg` plus repo-map importer evidence for `AccInstanceEmbedding` and
   every §3.11 candidate. Record the result in the summary. Preserve any dirty or
   imported candidate; delete only a clean zero-runtime-importer file and its
   uniquely-owned test when that remains proved.
2. Remove the Prisma model and obsolete create lineage, add an idempotent raw
   drop SQL file, remove stale current test/type/generated-doc references, and
   correct only the `AccActivityEmbedding.id` comment. In dirty `docs/erd.md`,
   preserve the pre-existing activity-model addition and stage only this plan's
   deletion hunk.
3. Run Prisma format/generate, schema/repo-map gates, then apply the raw drop via
   `npx prisma db execute --file prisma/migrations-raw/2026-07-21-drop-acc-instance-embedding.sql`
   only after all consumer checks are clean. Verify the command succeeds without
   printing connection values.

## Verification gates

- `npx prisma format`
- `npx prisma generate`
- `npx vitest run lib/server/acc-route-hydration.test.ts`
- `npx tsc --noEmit`
- `node scripts/repo-map/check.cjs`
- `rg -n "AccInstanceEmbedding|acc-instance-embedding|create-instance-embedding-table" app lib server scripts prisma tests docs/erd.md` returns no live/create reference (drop migration excluded explicitly)

## Success criteria

The obsolete table is absent from schema/live DB, activity storage is unchanged,
and the cleanup commit contains no catalog or unrelated dirty-tree work.

