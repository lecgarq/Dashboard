---
phase: 41
plan: "41-04"
type: execute
wave: 3
depends_on: ["41-01", "41-02", "41-03"]
files_modified:
  - .planning/phases/41-time-scrubber-hard-gate-closeout/41-VERIFICATION.md (new)
  - .planning/codebase/CONCERNS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [REND-04]
must_haves:
  truths:
    - "On the final full-artifact :3100 build, median-of-5 payload fetch+decode stays <=2500 ms and full navigation-to-ready is recorded separately"
    - "A headed Chromium D3D11 run over >=10 seconds sustains >=50 fps with ambient active and Tier 0 before/after; SwiftShader or any demotion blocks closeout"
    - "Focused tests, tsc, full npm test (including TEST-01/02/03), activity E2E, and repo-map all pass on the final code"
    - "autoDeploy runs only after gates are green; the authenticated populated activity graph probe and build id are recorded before Phase 41 advances"
  artifacts:
    - .planning/phases/41-time-scrubber-hard-gate-closeout/41-VERIFICATION.md
  key_links:
    - pattern: "TIME-01|REND-04|E2E-03"
      in: .planning/phases/41-time-scrubber-hard-gate-closeout/41-VERIFICATION.md
    - pattern: "D3D11|Tier 0|fps|10"
      in: .planning/phases/41-time-scrubber-hard-gate-closeout/41-VERIFICATION.md
---

# 41-04 — Binding hard gate, deploy, and phase closeout

## Objective

Measure last on the workshop machine, block on any hard-gate miss, deploy the
green build per policy, and leave complete phase evidence for milestone close.

## Context (@ paths)

- @.planning/phases/41-time-scrubber-hard-gate-closeout/41-CONTEXT.md
- @.planning/phases/37-scale-feasibility-spike/37-BASELINE.md
- @.planning/phases/38-activity-data-pipeline/38-VERIFICATION.md
- @.planning/phases/39-activity-universe-swap/39-VERIFICATION.md
- @.planning/phases/40-dimensions-sliders-at-scale/40-VERIFICATION.md
- @tests/e2e/activity-payload.spec.ts
- @tests/e2e/spatial-graph-baseline.spec.ts
- @tests/e2e/activity-universe-hard-gate.spec.ts
- @scripts/measure-spatial-graph-baseline.cjs
- @.agents/skills/lecg-ship/SKILL.md

## Tasks

1. Run focused Vitest, `npx tsc --noEmit`, full `npm test`, and repo-map. Resolve
   only Phase-41 regressions; report pre-existing failures separately.
2. Build the isolated `:3100` production harness with
   `NEXT_PUBLIC_ACC_GRAPH_TEST=1`. Run fixture E2E with the runtime fixture flag,
   then restart the same build without it and run the median-of-5 payload +
   navigation measurements and headed D3D11 hard gate last. `<50 fps`, Tier
   demotion, inactive ambient, SwiftShader, or payload >2500 ms blocks the phase.
3. Audit requirements and summaries; write `41-VERIFICATION.md` with exact
   outputs and roll durable debt into CONCERNS. With all gates green and
   `autoDeploy:true`, invoke `$lecg-ship` end-to-end and append its authenticated
   populated-route probe/build id.
4. Check Phase 41 in ROADMAP and update STATE to route to
   `$lecg-close-milestone`; do not close the milestone inside this plan.

## Verification gates

- Focused plan gates from 41-01 through 41-03
- `npx tsc --noEmit`
- `npm test`
- `node scripts/repo-map/check.cjs`
- Fixture `acc-dc-graph` + `acc-3d-lasso` on isolated `:3100`
- `node scripts/measure-spatial-graph-baseline.cjs` on the full artifact (headed D3D11)
- `$lecg-ship` deploy sequence and authenticated populated graph probe

## Success criteria

TIME-01, E2E-03, and REND-04 all have concrete green evidence; deployed `:3000`
serves the populated activity universe; STATE routes to milestone close.

