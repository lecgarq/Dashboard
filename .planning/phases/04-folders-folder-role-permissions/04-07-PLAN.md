---
phase: 04-folders-folder-role-permissions
plan: 07
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - scripts/perf-preflight.cjs
  - .planning/phases/04-folders-folder-role-permissions/PERF-GATE.md
autonomous: false
requirements: [FLDR-04]
must_haves:
  truths:
    - "Perf pre-flight measures FPS + GPU memory on real Hermosillo node count at 1×, 2×, 5× multipliers"
    - "GO threshold encoded: 60 FPS sustained AND GPU memory < 512MB at the projected node count"
    - "PERF-GATE.md artifact written with methodology, raw numbers, GO/NO-GO decision, contingency rationale"
    - "Decision is binary and unambiguous — Phase 5 GRAPH-04 reads PERF-GATE.md directly"
    - "If NO-GO: contingency contract logged (folders stay dashboard-only; graph-node integration deferred)"
  artifacts:
    - path: "scripts/perf-preflight.cjs"
      provides: "Standalone script that adds N synthetic folder nodes to the Cosmos.gl graph and samples FPS + memory"
    - path: ".planning/phases/04-folders-folder-role-permissions/PERF-GATE.md"
      provides: "Methodology, raw numbers per multiplier, GO/NO-GO decision, contingency notes — feeds Phase 5"
  key_links:
    - from: ".planning/phases/04-folders-folder-role-permissions/PERF-GATE.md"
      to: "Phase 5 GRAPH-04 plan"
      via: "Phase 5 planner reads PERF-GATE.md; GRAPH-04 conditional on GO"
      pattern: "PERF-GATE"
---

<objective>
Produce the GO/NO-GO perf artifact that gates Phase 5 GRAPH-04. Measure real Cosmos.gl FPS + GPU memory at the current Hermosillo node count plus 2× and 5× synthetic growth multipliers. The decision determines whether folder nodes ship in the graph or stay dashboard-only.

Purpose: Measure-then-decide. Per CONTEXT.md: strict 60 FPS + <512MB at projected node count, otherwise NO-GO. Luis prefers honest NO-GO over sluggish graph integration.

Output: PERF-GATE.md with the decision Phase 5 planner reads directly.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-folders-folder-role-permissions/04-CONTEXT.md
@.planning/phases/04-folders-folder-role-permissions/04-RESEARCH.md
@.planning/phases/04-folders-folder-role-permissions/CRAWL-ESTIMATE.md

<interfaces>
Memory context (from MEMORY.md cosmos alpha inversion): `getSimulationAlpha()` returns `1 - progress` in cosmos.gl v3 — INVERTED from d3 alpha. Use this fact when interpreting any alpha-based progress reading.

Existing graph entry point: confirm via `grep -rn "cosmos" app/(dashboard)/users/` — the renderer is mounted in the spatial graph view; the perf-preflight script should programmatically drive that surface (or stand up a headless variant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement perf-preflight script — load real graph + inject synthetic folder nodes at 1×, 2×, 5×</name>
  <files>scripts/perf-preflight.cjs</files>
  <action>
Create a perf measurement script. Approach options (pick the simplest that actually measures):

**Option A (preferred — DOM-driven):** Launch a Playwright Chromium instance, navigate to the spatial graph view at the dev server URL, wait for the cosmos canvas to render. Then evaluate in-page:
- Read current node count via `window.__cosmosGraph?.nodes?.length` (or whatever debug surface is already exposed in the renderer — `grep -rn "__cosmos" lib/ app/` to find).
- Use `requestAnimationFrame` sampling over 5 seconds to compute mean FPS (count frames / 5).
- Read `performance.memory.usedJSHeapSize` AND `WebGLRenderingContext.getExtension("WEBGL_debug_renderer_info")` GPU info; for GPU memory specifically use `navigator.gpu` if available OR Chrome's `performance.measureUserAgentSpecificMemory()` proxy (heap as approximation if true GPU mem is not introspectable).
- Inject 2× more synthetic folder nodes (random positions, simple style) via whatever node-add API the graph exposes. Re-sample FPS for 5 seconds.
- Inject up to 5× total. Re-sample.

If Playwright is not installed in this repo (check `package.json`): use `puppeteer` if available, OR fall back to **Option B**.

**Option B (fallback — manual measurement):** If headless instrumentation is hard, the script generates a measurement HTML page at `tmp/perf-preflight.html` that bundles the existing Cosmos graph component + a control panel for "inject 2×/5× synthetic nodes" + an FPS counter overlay. Print URL to console. The user opens it in a real browser, follows on-screen instructions, and pastes the numbers back into the script's stdin prompt. The script writes PERF-GATE.md from the input.

Choose Option A if Playwright/Puppeteer is reachable; Option B otherwise. Document choice in PERF-GATE.md.

Script outputs:
- `console.log` lines like `[perf] 1x nodes=N fps=F gpuMB=M`.
- At end, write `.planning/phases/04-folders-folder-role-permissions/PERF-GATE.md` with the template in Task 2.

DO NOT modify the actual graph renderer code in this task — measurement only.
  </action>
  <verify>
    <automated>node --check scripts/perf-preflight.cjs</automated>
  </verify>
  <done>Script parses. Has a clear "approach used" (Option A vs B) console message at startup. Either drives a headless browser to measure OR generates an instrumented measurement page and prompts the user.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script and write PERF-GATE.md with GO/NO-GO decision</name>
  <files>.planning/phases/04-folders-folder-role-permissions/PERF-GATE.md</files>
  <action>
Run: `node scripts/perf-preflight.cjs`. Capture the three measurement triplets (FPS, GPU/heap MB, node count) for 1×, 2×, 5×.

Write `.planning/phases/04-folders-folder-role-permissions/PERF-GATE.md` with this exact structure:

```markdown
# Phase 4 Perf Pre-Flight: GO/NO-GO Decision

**Measured:** [ISO date]
**Hub:** Hermosillo
**Methodology:** [Option A: Playwright headless | Option B: in-browser instrumented page]
**Cosmos.gl version:** [from package.json]

## Measurements

| Multiplier | Node count | Mean FPS (5s) | Memory proxy (MB) |
|------------|------------|---------------|--------------------|
| 1×         | [N]        | [F]           | [M]                |
| 2×         | [2N]       | [F]           | [M]                |
| 5×         | [5N]       | [F]           | [M]                |

**Memory proxy note:** [If true GPU memory is unreadable, state the proxy used — JS heap, performance.measureUserAgentSpecificMemory, etc. — and the conservativeness of the conversion factor.]

## GO Threshold (strict, per CONTEXT.md)
- 60 FPS sustained AND
- GPU memory < 512MB
- Evaluated at the **projected node count** (5× as the worst-case growth headroom)

## Decision

**[GO | NO-GO]**

### Rationale
[3-5 sentences: which multiplier failed/passed, the gap to the threshold, whether the trend across 1×/2×/5× suggests a hardware ceiling vs algorithmic ceiling.]

### Contingency
[If GO: folder nodes will be added in Phase 5 GRAPH-04 as the 5th node kind, hidden by default with a filter toggle.]
[If NO-GO: folder nodes do NOT ship in graph. Phase 5 GRAPH-04 plan logs the contingency, folders stay dashboard-only via the FolderPermissionsWidget already shipped in Plan 06, and reconsideration is gated on either (a) Cosmos.gl v4 release with claimed perf gains, or (b) a switch to LOD-based culling. Document which is more likely.]

## Raw Console Output
```
[paste console.log lines from the script run]
```

## Phase 5 GRAPH-04 Plan Instruction
Phase 5 planner: read this file before planning GRAPH-04. The decision is binary — implement the conditional branch in GRAPH-04 plan accordingly. Do not re-measure unless the underlying Cosmos.gl version changes.
```

Be conservative: if FPS at 5× is 58-60 (within measurement noise), recommend NO-GO + retry at Cosmos.gl v4. If GPU memory is unreadable and heap proxy is well below 256MB, document the proxy uncertainty but lean GO; if heap is 400MB+, lean NO-GO out of caution.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-folders-folder-role-permissions/PERF-GATE.md && grep -E "^\*\*\[GO|^\*\*GO|^\*\*NO-GO" .planning/phases/04-folders-folder-role-permissions/PERF-GATE.md</automated>
  </verify>
  <done>PERF-GATE.md exists with the table populated with real numbers (not placeholders), an unambiguous **GO** or **NO-GO** verdict, and a contingency paragraph.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Luis approves the perf decision before Phase 5 planning consumes it</name>
  <what-built>
- `scripts/perf-preflight.cjs` measurement script.
- `.planning/phases/04-folders-folder-role-permissions/PERF-GATE.md` with raw numbers + GO/NO-GO verdict + contingency notes.
  </what-built>
  <how-to-verify>
1. Open PERF-GATE.md.
2. Confirm 1×/2×/5× rows have real numbers (no `[N]` placeholders).
3. Confirm the decision matches the data — if any of the three multipliers reports FPS < 60 OR memory ≥ 512MB at 5×, the decision should be NO-GO.
4. Confirm the contingency paragraph names a specific next-step trigger (Cosmos.gl v4, LOD culling, etc.) if NO-GO.
5. If decision and methodology look correct: type `approved`.
6. If methodology is suspect (e.g., heap used as GPU proxy without acknowledging the proxy limitation), or you want a re-measurement under different conditions: describe the concern.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the decision into Phase 5 planning. Otherwise describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/04-folders-folder-role-permissions/PERF-GATE.md` exists with populated measurements and a GO or NO-GO decision.
- Phase 5 GRAPH-04 plan (when drafted) can reference this file directly.
</verification>

<success_criteria>
- Perf pre-flight artifact produced and Luis-approved.
- Phase 5 GRAPH-04 gate decision is unambiguous and traceable.
- If NO-GO: contingency contract logged so the user/future-Claude understands why folder nodes don't appear in the graph.
</success_criteria>

<output>
After completion, create `.planning/phases/04-folders-folder-role-permissions/04-07-SUMMARY.md` recording the GO/NO-GO outcome in one sentence + a link to PERF-GATE.md.
</output>
