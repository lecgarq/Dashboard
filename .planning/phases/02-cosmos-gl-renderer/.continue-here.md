---
phase: 02-cosmos-gl-renderer
plan: 02
task: 4
total_tasks: 4
status: blocked
last_updated: 2026-04-28T21:44:51.904Z
---

<current_state>
Plan 02-02 tasks 1–3 are committed and complete. Stuck at Task 4 (human-verify checkpoint) because the GPU renderer shows a white/blank canvas instead of rendering the graph. Two bugs were fixed this session but the white screen persists.

We are mid-debugging the white screen. Context ran out before finding the root cause.
</current_state>

<completed_work>

**Phase 02-01 (Wave 1) — DONE, committed:**
- Installed `@cosmos.gl/graph@3.0.0-beta.8`
- Created `app/(dashboard)/users/cosmosUtils.ts` with 6 buffer helpers
- Implemented `CosmosGraphRenderer` in `graphRenderers.ts` replacing WebGpuGraphRenderer stub
- Key deviation: `selectNode()` uses actual v3 API `selectPointByIndex`/`unselectPoints`

**Phase 02-02 (Wave 2) tasks 1–3 — DONE, committed:**
- Task 1: State, refs, effects, toggle wiring in AccUsersGraph.tsx
- Task 2: JSX — cosmos div, spinner, hover label, toolbar toggle button
- Task 3: Physics panel + node legend

**Bugs fixed this session (committed):**
- `5f94d56`: `setIsCosmosLoading(false)` added to effect cleanup (was never called when Fast Refresh fired during init, leaving spinner stuck forever); also added cosmos pre-warm import on mount to prevent webpack compilation cascade when user first clicks toggle
- `2a8b2be`: Scaled organic layout positions from [0,1] to Cosmos space `(x - 0.5) × 2000`; added `fitView(600)` on first data load
- `3x` script syntax error fixed in `scripts/run_dev_stack.py` (literal backtick-n sequences on lines 103, 106, 109)
</completed_work>

<remaining_work>

**Still broken: white canvas on GPU toggle (Task 4 checkpoint)**

Two bugs remain unresolved — debugging was cut off at 88% context:

1. **Is `draw()` actually being called?** The RAF loop only calls draw when `needsRenderRef.current = true`. Need to confirm `markGraphDirty()` fires AFTER `activeRendererRef.current = renderer` is set, and that `disposed` is false at that point.

2. **Is `frame.nodes` populated?** `nodesRef.current` is `SimNode[]`, set at line 787 in AccUsersGraph.tsx. Need to confirm it has data when the Cosmos renderer first calls draw — but more importantly, `SimNode` may not have `kind` or `color` fields that `GraphRenderNode` requires. `buildNodeColorBuffer(frame.nodes)` reads `nodes[i].color` — if that field is missing, all colors default to gray-400 fallback.

3. **Possible root cause (most likely):** `SimNode` type might not have a `color` field that maps to the hex strings `buildNodeColorBuffer` expects. Check what fields `SimNode` actually has vs what `GraphRenderNode` interface requires. The Canvas 2D renderer uses `nodesRef.current` too — look at how Canvas 2D draws nodes to understand what fields are available.

4. **Alternative: Cosmos needs `render()` call after `setPointPositions()`** — after data is set mid-session, Cosmos's internal RAF may need an explicit `this.graph.render()` call to wake up. Add `this.graph.render()` at the end of `draw()` if `isFirstLoad`.
</remaining_work>

<decisions_made>

- Using `@cosmos.gl/graph@3.0.0-beta.8` (beta, not stable v2.6.1 — this was the plan's choice)
- Cosmos coordinate space: positions scaled by `(x - 0.5) × 2000`, giving range [-1000, 1000] within Cosmos default spaceSize 4096
- `fitView(600)` called on first data load in `draw()` — 600ms animation
- Pre-warm import: `void import("@cosmos.gl/graph")` on component mount when webgl2Available
- Persistent localStorage keys: `acc-graph-renderer` and `acc-graph-physics`
- Physics defaults: `{ repulsion: 1.0, linkSpring: 1.0, gravity: 0.25 }`
</decisions_made>

<blockers>

**Main blocker: white canvas on GPU toggle**

Suspected causes in priority order:
1. `SimNode` missing `color` / `kind` fields → `buildNodeColorBuffer` gets undefined, all nodes invisible
2. `draw()` never called because `disposed = true` when Cosmos init resolves (Fast Refresh loop)
3. `this.graph.render()` needed after data push (Cosmos render loop stopped after empty init)
4. `frame.nodes.length === 0` at time of first draw call

**Pre-existing issue (not our bug):** Fast Refresh fires every ~150ms on /users even before GPU toggle — visible in browser console as continuous `[Fast Refresh] rebuilding` cycle. This interferes with Cosmos init.
</blockers>

<context>
The overall flow works (toggle button shows, spinner shows, spinner hides, cosmosContainer div becomes visible) — the rendering pipeline is wired correctly. The issue is specifically that no nodes/graph is rendered inside the Cosmos canvas. Cosmos initializes without error (no fallback to Canvas 2D occurs). The white is either the Cosmos background (#F8F7F4 which looks cream/white) with no nodes drawn, or the div's white parent showing through a transparent Cosmos canvas.

Best next step is to add a temporary `console.log` in `CosmosGraphRenderer.draw()` to see: (1) what `nodeCount` is, (2) what the first position value is, (3) whether `isFirstLoad` triggers. Open DevTools Console on /users, click GPU toggle, read the output.

Second best: look at how `CanvasGraphRenderer.draw()` uses `frame.nodes` — specifically what fields it reads. Those same fields are what `buildNodeColorBuffer` needs. If Canvas 2D uses `node.color` directly, check how that field gets onto `SimNode`.
</context>

<next_action>
Start with: Open `app/(dashboard)/users/graphRenderers.ts`, find `CosmosGraphRenderer.draw()`, add `console.log("[Cosmos draw]", nodeCount, frame.positions[0], frame.positions[1])` as the first line. Test in browser, check DevTools console after clicking GPU toggle. This immediately tells us if draw() is called and what data it receives.

Then: Run `/gsd:resume-work` to reload this context.
</next_action>
