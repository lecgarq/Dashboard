---
phase: 08-activity-re-extraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md
autonomous: false
requirements: [DATA-04]
user_setup:
  - service: APS (Autodesk Platform Services) app credentials
    why: "folder-crawl-cron.cjs authenticates 2-legged (client_credentials). No session, no DC quota, no refresh-token rotation — but APS_CLIENT_ID / APS_CLIENT_SECRET must be present in .env."
    env_vars:
      - name: APS_CLIENT_ID
        source: ".env (already used by the existing weekly folder-crawl production path)"
      - name: APS_CLIENT_SECRET
        source: ".env (already used by the existing weekly folder-crawl production path)"

must_haves:
  truths:
    - "AccFolder.totalSizeBytes is populated for crawled folders so the Phase 13 storage treemap (FOLD-04) has real data"
    - "The folder crawl ran on APS 2-legged app credentials — no ACC session, no DC quota, no APS refresh-token rotation"
    - "The folder coverage gate is recorded as a labeled fact (count of AccFolder rows with non-null totalSizeBytes)"
    - "Any AccFolderPermission verification query uses GROUP BY + LIMIT (v2.0 OOM: 5M rows, 77s)"
  artifacts:
    - path: ".planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md"
      provides: "Folder coverage gate record: AccProject active count, totalSizeBytes non-null count, AccFolderPermission row count (aggregated query only)"
      contains: "FOLDER GATE"
  key_links:
    - from: "scripts/folder-crawl-cron.cjs"
      to: "AccFolder"
      via: "extractAndPersistFolders → totalSizeBytes upsert"
      pattern: "extractAndPersistFolders"
    - from: "scripts/folder-crawl-cron.cjs"
      to: "AccProject"
      via: "active-status project source"
      pattern: "folderCrawlStatus"
---

<objective>
Populate folder storage data (`AccFolder.totalSizeBytes`, plus `AccFolderPermission`) by running the existing 2-legged APS folder crawl, so the Phase 13 storage treemap (FOLD-04) renders real data instead of an empty/labeled state. This job is fully independent of the activity crawl — different auth, different source table, no shared files — so it runs in parallel (Wave 1).

Purpose: FOLD-04 (treemap) is conditional on `AccFolder.totalSizeBytes IS NOT NULL` count > 0 after Phase 8 (STATE pending todo, CONTEXT locked "yes"). This is the standing weekly production path (`scripts/folder-crawl-cron.cjs` → `lib/acc/folderCrawl.ts`) on APS app credentials — none of the session/quota/rotation friction the owner is avoiding (CONTEXT/RESEARCH Q5).

Output: Populated folder size data and `08-FOLDER-VERIFICATION.md` recording the folder coverage gate as a labeled fact.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-activity-re-extraction/08-CONTEXT.md
@.planning/phases/08-activity-re-extraction/08-RESEARCH.md

# Verified source references (read for exact behavior, do not modify):
@scripts/folder-crawl-cron.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm APS credentials and the AccProject source count</name>
  <files>.planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md</files>
  <action>Confirm the folder crawl can run and record the source scope (RESEARCH Q5, Pitfall 6). Verify APS_CLIENT_ID and APS_CLIENT_SECRET are present in .env (check presence only — never print secret values; note existence per the evidence rules). Then run a read-only node one-shot (DIRECT_URL/DATABASE_URL from .env, same Prisma pattern the diag scripts use) for: SELECT COUNT(*) FROM "AccProject" WHERE status = 'active'. Record both findings in 08-FOLDER-VERIFICATION.md under `## Pre-flight`. Note whether the active AccProject count (the folder-crawl source) approximates the activity-crawl target so the treemap scope is understood — if AccProject is ≈428 while the activity spike expanded to full membership, label this scope difference as a known fact (the folder crawl follows AccProject, not the activity set). Do NOT block on a scope mismatch — record it as a labeled fact.</action>
  <verify>
    <automated>test -f .planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md &amp;&amp; grep -v '^#' .planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md | grep -qi 'pre-flight' &amp;&amp; echo OK</automated>
  </verify>
  <done>08-FOLDER-VERIFICATION.md `## Pre-flight` records APS credential presence (existence only) and the active AccProject count, with any activity-vs-folder scope difference labeled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the 2-legged APS folder crawl</name>
  <action>Claude supplies the folder-crawl-cron.cjs command and the verified scope knobs (FOLDER_CRAWL_PROJECT_IDS / FOLDER_CRAWL_LIMIT / FOLDER_CRAWL_STATUSES); the owner runs the long (~48min–4h) 2-legged APS job and reports completion or any auth error.</action>
  <what-built>Claude prepares the folder-crawl command from the verified env knobs. The owner runs it because it is a long-running background job (~48min best / ~4h worst per the script header) against live APS — no session and no quota, but long enough to warrant operator oversight.</what-built>
  <how-to-verify>
    1. Run the full folder crawl (defaults: active AccProject rows with folderCrawlStatus in never,partial,failed):
       `node scripts/folder-crawl-cron.cjs`
       (To isolate or batch, the verified knobs are: `FOLDER_CRAWL_PROJECT_IDS=id1,id2`, `FOLDER_CRAWL_LIMIT=N`, `FOLDER_CRAWL_STATUSES=never,partial,failed`. Use `FOLDER_CRAWL_LIMIT=1` to smoke-test one project first if desired.)
    2. Watch for the `[folder-crawl-cron ...]` per-project log lines and a clean completion (no fatal token/auth error). A 2-legged auth failure means APS_CLIENT_ID/SECRET are wrong — fix in .env and re-run.
    3. The crawl is additive/idempotent (upserts) — re-running to finish remaining projects is safe.
  </how-to-verify>
  <resume-signal>Type "folder crawl done" once the cron printed completion with no fatal auth error, or paste the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Prove the folder coverage gate (FOLD-04 enabler) with OOM-safe queries</name>
  <files>.planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md</files>
  <action>Record the folder coverage gate as a labeled fact (DATA-04 readiness for Phase 13). Run read-only node one-shots (DIRECT_URL/DATABASE_URL from .env): (a) SELECT COUNT(*) FILTER (WHERE "totalSizeBytes" IS NOT NULL) FROM "AccFolder" — this is the FOLD-04 gate; expect > 0. (b) An OOM-SAFE AccFolderPermission sanity count: SELECT COUNT(*) FROM "AccFolderPermission" (a scalar aggregate is safe). Never run an unbounded row-returning AccFolderPermission query (no SELECT * / findMany without GROUP BY + LIMIT — v2.0 OOM, 5M rows, 77s). If a per-folder breakdown is wanted, use SELECT "folderId", COUNT(*) FROM "AccFolderPermission" GROUP BY "folderId" LIMIT 1000. Write 08-FOLDER-VERIFICATION.md `## FOLDER GATE` with the non-null totalSizeBytes count (PASS if > 0), the AccFolderPermission scalar count, and a labeled note on coverage scope (how many of the active AccProject set produced folder-size data). This gate is presence-based and report-only; it does not block on a freshness threshold.</action>
  <verify>
    <automated>test -f .planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md &amp;&amp; grep -v '^#' .planning/phases/08-activity-re-extraction/08-FOLDER-VERIFICATION.md | grep -qi 'folder gate' &amp;&amp; echo OK</automated>
  </verify>
  <done>08-FOLDER-VERIFICATION.md `## FOLDER GATE` records the non-null totalSizeBytes count (> 0) and the AccFolderPermission scalar count, using only GROUP BY + LIMIT or scalar-aggregate queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| folder-crawl-cron → APS auth (2-legged) | client_credentials exchange using APS_CLIENT_ID/SECRET over TLS |
| Crawler → AccFolder / AccFolderPermission (Postgres) | Batched upserts into local DB |
| Verification → AccFolderPermission (Postgres) | Read of a 5M-row table — OOM hazard on unbounded reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Denial of Service | AccFolderPermission unbounded read OOM | mitigate | Task 3 uses only scalar-aggregate or GROUP BY + LIMIT queries (v2.0 incident: 5M rows / 77s, STATE blocker). No SELECT * / unbounded findMany. |
| T-08-08 | Information Disclosure | APS_CLIENT_ID / APS_CLIENT_SECRET in .env | mitigate | Check presence only; never print values; .env is not staged (`git diff --cached --name-only` before any commit). |
| T-08-09 | Repudiation / data truth | folder scope differs from activity scope | mitigate | Task 1 + Task 3 label the AccProject-vs-activity scope difference rather than implying full-hub coverage (CONTEXT coverage-honesty). |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH: no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- `SELECT COUNT(*) FILTER (WHERE "totalSizeBytes" IS NOT NULL) FROM "AccFolder"` returns > 0.
- AccFolderPermission is queried only via scalar aggregate or GROUP BY + LIMIT.
- 08-FOLDER-VERIFICATION.md records `## Pre-flight` and `## FOLDER GATE` with labeled scope facts.
- `npx tsc --noEmit` = 0 (standing gate; no .ts changed — expected 0).
</verification>

<success_criteria>
- AccFolder.totalSizeBytes is populated (> 0 non-null) so FOLD-04 has real data.
- The crawl ran on APS 2-legged credentials with no session, quota, or rotation.
- The folder coverage gate is recorded as a labeled fact; all AccFolderPermission queries are OOM-safe.
</success_criteria>

<output>
Create `.planning/phases/08-activity-re-extraction/08-03-SUMMARY.md` when done. State the non-null totalSizeBytes count and the folder-vs-activity scope note — Phase 13 (FOLD-04 treemap) gates on this.
</output>
