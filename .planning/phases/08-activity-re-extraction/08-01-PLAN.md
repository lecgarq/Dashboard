---
phase: 08-activity-re-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-activity-re-extraction/08-SPIKE.md
autonomous: false
requirements: [DATA-03, DATA-01]
user_setup:
  - service: ACC web session
    why: "Bootstraps scratch/acc-session.json (password-equivalent ACC login + MFA) — the only auth the ACCDS crawler uses. Claude cannot mint Luis's session."
    dashboard_config:
      - task: "Run node scripts/accds-login.cjs, complete the ACC login + MFA in the opened browser; verify scratch/acc-session.json is written"
        location: "Local terminal in C:/LECG/Dashboard"

must_haves:
  truths:
    - "Owner's ACC web session is bootstrapped to scratch/acc-session.json and proven live by a single-project crawl"
    - "The owner's full ACC hub project membership is enumerated and counted (expected ~1,152)"
    - "It is empirically known whether accds/v0 returns activity rows for a project where Luis is a member but NOT a project admin"
    - "The crawl project source for Plan 08-02 is decided: full membership (spike PASS) or the ≈428 admin set (spike admin-gated)"
  artifacts:
    - path: ".planning/phases/08-activity-re-extraction/08-SPIKE.md"
      provides: "Spike outcome record: membership count, chosen member-only project id, inserted row count, PASS/admin-gated verdict, and the decided crawl source for 08-02"
      contains: "SPIKE VERDICT"
  key_links:
    - from: "scripts/accds-login.cjs"
      to: "scratch/acc-session.json"
      via: "Playwright storageState write"
      pattern: "acc-session"
    - from: "scripts/accds-activity-ingest.cjs"
      to: "AccActivityAccds"
      via: "ACCDS_PROJECT single-project crawl → createMany"
      pattern: "ACCDS_PROJECT"
---

<objective>
Settle the two facts that determine the entire shape of Phase 8 before any long crawl runs: (1) is the owner's ACC web session live, and (2) does the free `accds/v0` endpoint return activity for projects where Luis is a member but NOT an admin. The answer to (2) branches the crawl source in Plan 08-02 between the full hub membership (~1,152, the "extract ALL" win, no permission change) and the inherited ≈428 admin set.

Purpose: De-risk the crawl. The 428 ceiling is inherited from the old DC extraction, not a property of the session endpoint (CONTEXT locked, RESEARCH Q1). The spike is the cheap, reversible way to find out whether the 428-vs-1,152 admin problem is moot for *reading* activity — without touching any production permission.

Output: A bootstrapped `scratch/acc-session.json`, an enumerated membership count, and `08-SPIKE.md` recording the verdict + the decided crawl source for 08-02.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-activity-re-extraction/08-CONTEXT.md
@.planning/phases/08-activity-re-extraction/08-RESEARCH.md

# Verified source references (read for exact behavior, do not modify):
@scripts/accds-activity-ingest.cjs
@scripts/get-aps-hub-id.mjs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Owner bootstraps the ACC web session and verifies live</name>
  <action>Owner-only step (Claude cannot mint Luis's session). Claude confirms Playwright is present and supplies the exact login + smoke-crawl commands; the owner runs scripts/accds-login.cjs and the single-project smoke crawl, then reports the result.</action>
  <what-built>Nothing is built — this is the one step Claude literally cannot do: minting Luis's authenticated ACC session. The crawler's only auth is the logged-in ACC web session.</what-built>
  <how-to-verify>
    1. Confirm Playwright is present (already VERIFIED in research: playwright@1.59.1): `npm ls playwright`
    2. Run `node scripts/accds-login.cjs` — a browser opens; log in to ACC with MFA.
    3. Confirm the session file exists: `ls -la scratch/acc-session.json` (it is gitignored, password-equivalent — never commit it).
    4. Prove the session is live with a single-project smoke crawl against a KNOWN admin project (any id from `SELECT DISTINCT "projectId" FROM "AccActivity" LIMIT 1`):
       `ACCDS_PROJECT=<known-admin-id> ACCDS_MONTHS_BACK=24 node scripts/accds-activity-ingest.cjs`
       Expected stdout line: `✓ <id>  fetched=N inserted=M` with no `SessionExpiredError`.
  </how-to-verify>
  <resume-signal>Type "session live" once scratch/acc-session.json exists and the smoke crawl printed a `✓ ... fetched=` line, or paste the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Enumerate full ACC hub membership and pick one member-only project</name>
  <files>.planning/phases/08-activity-re-extraction/08-SPIKE.md</files>
  <action>Enumerate the owner's full ACC project membership using the verified APS Data Management 3-leg pattern from scripts/get-aps-hub-id.mjs (token from `prisma.account` where provider=autodesk; APS host developer.api.autodesk.com). Run an inline one-shot node command (operations only — do NOT add a product script): read the access_token from the autodesk account row, GET the hub id, then GET hubs/:hubId/projects paginated until exhausted, collecting every returned project id. VERIFY: the exact projects-list path and its pagination params against official APS docs (https://aps.autodesk.com/en/docs/data/v2/reference/http/hubs-hub_id-projects-GET/) before relying on a page-count — mark the pagination scheme VERIFY: in 08-SPIKE.md if it cannot be confirmed in one pass. Record the total membership count (expected ~1,152). Then compute the member-only candidate set = enumerated ids MINUS the existing admin set (`SELECT DISTINCT "projectId" FROM "AccActivity"`), and choose ONE candidate that is likely to have recent activity (pick from the enumerated list; if metadata is available prefer a non-archived project). If the APS enumeration cannot be completed (token/scope failure), fall back to recording that the spike could not enumerate membership and that 08-02 must use the ≈428 admin set — do NOT invent a project id. Write all findings (membership count or enumeration-failure note, the chosen member-only candidate id, and the admin-set count) into 08-SPIKE.md under a `## Membership Enumeration` heading.</action>
  <verify>
    <automated>test -f .planning/phases/08-activity-re-extraction/08-SPIKE.md &amp;&amp; grep -v '^#' .planning/phases/08-activity-re-extraction/08-SPIKE.md | grep -qi 'membership' &amp;&amp; echo OK</automated>
  </verify>
  <done>08-SPIKE.md exists with the membership count (or a clearly stated enumeration-failure fallback), the admin-set count, and exactly one chosen member-only candidate project id (or a note that none could be derived).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the member-only spike and record the verdict</name>
  <action>Claude prepares the exact ACCDS_PROJECT spike command using the member-only candidate id from Task 2; the owner runs it (depends on the live session) and reports fetched/inserted; Claude then writes the SPIKE VERDICT and decided crawl source into 08-SPIKE.md.</action>
  <what-built>Claude prepared the exact spike command using the member-only candidate id from Task 2. The owner runs it (it depends on the live session) and reports the inserted count, which is the empirical pass/fail signal.</what-built>
  <how-to-verify>
    1. Using the chosen member-only candidate id from 08-SPIKE.md, run:
       `ACCDS_PROJECT=<member-only-id> ACCDS_MONTHS_BACK=24 node scripts/accds-activity-ingest.cjs`
       (24 months widens the chance of hitting real rows — RESEARCH Pitfall 5: a 200 with empty results is NOT a pass.)
    2. Read the per-project stdout line: `✓ <id>  fetched=N inserted=M`.
    3. Interpret:
       - `inserted=M` with M > 0  → SPIKE PASS: accds/v0 is member-accessible. 08-02 crawl source = full membership.
       - `fetched=0 inserted=0` (HTTP 200, empty) → inconclusive: try a DIFFERENT member-only id known to have recent activity before concluding.
       - HTTP 403 / access error in stdout → admin-gated: 08-02 crawl source = the ≈428 admin set; the ~724 member-only projects become deferred Account-Admin work (CONTEXT Deferred — do NOT self-elevate).
    4. Claude records the verdict in 08-SPIKE.md under `## SPIKE VERDICT` with: the project id used, fetched/inserted counts, PASS / admin-gated / inconclusive, and the decided crawl source string for 08-02 (`full-membership` or `admin-428`).
  </how-to-verify>
  <resume-signal>Paste the `✓ ... fetched= inserted=` line (or the 403/error). Claude writes the verdict and decided crawl source into 08-SPIKE.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner browser → scratch/acc-session.json | Password-equivalent ACC session credential lands on local disk |
| Local script → accds/v0 (Autodesk) | Bearer derived from session cookie crosses to Autodesk over TLS |
| DB account row → APS DM API | 3-leg autodesk access_token used to enumerate hub membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | scratch/acc-session.json | mitigate | File is gitignored + password-equivalent (RESEARCH Security Domain). Never read/print its contents in summaries; run `git diff --cached --name-only` before any commit to confirm it is not staged. |
| T-08-02 | Spoofing | accds/v0 access via session cookie | accept | The session is the owner's own authenticated ACC identity; the spike only reads activity Luis already has UI access to. No elevation performed. |
| T-08-03 | Elevation of Privilege | member-only project access | mitigate | The spike intentionally does NOT call scripts/acc-grant-project-admin.cjs; admin grant is deferred and gated behind Account-Admin sign-off (CONTEXT locked). Spike only reads. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH: no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- `ls -la scratch/acc-session.json` shows the session file exists (Task 1).
- 08-SPIKE.md contains `## Membership Enumeration` and `## SPIKE VERDICT` with a decided crawl source (`full-membership` or `admin-428`).
- `git diff --cached --name-only` does NOT list scratch/acc-session.json before any commit.
- `npx tsc --noEmit` = 0 (standing gate; no .ts changed — expected 0).
</verification>

<success_criteria>
- The ACC session is bootstrapped and proven live by a single-project smoke crawl.
- Full membership is enumerated (or an enumeration-failure fallback is recorded), and the admin-set count is recorded.
- The member-only spike has been run and 08-SPIKE.md records PASS / admin-gated / inconclusive with the decided crawl source for Plan 08-02.
- No production permission was changed; no admin grant was performed.
</success_criteria>

<output>
Create `.planning/phases/08-activity-re-extraction/08-01-SUMMARY.md` when done. The summary MUST state the spike verdict and the decided crawl source (`full-membership` or `admin-428`) — Plan 08-02 reads this to choose its project source.
</output>
