---
phase: 08-activity-re-extraction
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-activity-re-extraction/08-VERIFICATION.md
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "AccActivityAccds holds rows for the full crawled project set decided by the 08-01 spike (full membership if PASS, else the ≈428 admin set)"
    - "The crawl completed with NO Data Connector quota — session-cookie auth only; a SessionExpiredError mid-run is recovered by re-login + ACCDS_RESUME=1"
    - "Partially-crawled projects (mid-expiry gaps) are detected and re-crawled per-project via ACCDS_PROJECT so no project has a silent history gap"
    - "The latest AccActivityAccds.createdAt is recorded as a labeled fact (report, not a recency gate) and the real history span (MIN createdAt) is labeled"
    - "ACCDS vs AccActivity reconciliation passes with no silent gaps"
  artifacts:
    - path: ".planning/phases/08-activity-re-extraction/08-VERIFICATION.md"
      provides: "Coverage + presence gate record: distinct project count vs target, MAX/MIN createdAt, per-day histogram gap review, verify-accds-merge result"
      contains: "COVERAGE GATE"
  key_links:
    - from: "scripts/accds-activity-ingest.cjs"
      to: "AccActivityAccds"
      via: "full-set crawl → createMany skipDuplicates"
      pattern: "createMany"
    - from: "scripts/diag-accds-recency.cjs"
      to: "AccActivityAccds"
      via: "raw SQL count / date range / distinct projects / per-day histogram"
      pattern: "distinct .*projectId"
    - from: "scripts/verify-accds-merge.cjs"
      to: "AccActivity"
      via: "partitioned-merge reconciliation CTE"
      pattern: "AccActivityAccds"
---

<objective>
Run the full ACCDS activity crawl across the project set decided by the 08-01 spike, recover cleanly from session expiry, repair any mid-expiry per-project gaps, then prove coverage + presence and reconcile against the DC baseline. This is the hard data gate that unblocks Phases 12–14.

Purpose: Bring `AccActivityAccds` current for the whole reachable hub with no DC quota, no 403-bisect, and no APS refresh-token rotation (CONTEXT/RESEARCH locked). The gate is coverage + presence, NOT recency — record the latest date as a labeled fact and do not block on a freshness threshold (CONTEXT: "report, don't block").

Output: A current `AccActivityAccds` table over the decided project set and `08-VERIFICATION.md` recording the coverage gate, the labeled history span, and the reconciliation result.
</objective>

<execution_context>
@C:/LECG/Dashboard/.claude/gsd-core/workflows/execute-plan.md
@C:/LECG/Dashboard/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-activity-re-extraction/08-CONTEXT.md
@.planning/phases/08-activity-re-extraction/08-RESEARCH.md

# Reads the spike outcome from 08-01 — the decided crawl source lives here:
@.planning/phases/08-activity-re-extraction/08-01-SUMMARY.md

# Verified source references (read for exact behavior, do not modify):
@scripts/accds-activity-ingest.cjs
@scripts/diag-accds-recency.cjs
@scripts/verify-accds-merge.cjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the full ACCDS activity crawl over the decided project set</name>
  <action>Claude reads the decided crawl source from 08-01-SUMMARY.md, selects the correct invocation (default source for admin-428, or per-project ACCDS_PROJECT loop for full-membership), and supplies the login + crawl + resume commands; the owner runs the long crawl (live session, multi-hour) and reports completion (DATA-01, DATA-02, DATA-03).</action>
  <what-built>Claude confirms the crawl source from 08-01 (full-membership vs admin-428) and prepares the exact command. The owner runs it because it depends on the live, password-equivalent session and can run for hours (DATA-01, DATA-02, DATA-03).</what-built>
  <how-to-verify>
    1. Read the decided crawl source from 08-01-SUMMARY.md (`full-membership` or `admin-428`).
       - `admin-428`: the crawler's built-in source (`distinct AccActivity.projectId`) is already correct — run as-is, no override.
       - `full-membership`: the crawler iterates `distinct AccActivity.projectId` by default. To crawl beyond the 428 with NO source-code change, drive it project-by-project from the enumerated membership list using `ACCDS_PROJECT=<id>` in a shell loop (each `ACCDS_PROJECT` run bypasses RESUME and crawls exactly one project — VERIFIED: ingest line `const ONLY = process.env.ACCDS_PROJECT`). Record this choice in 08-VERIFICATION.md. Do NOT edit the ingest script to change its default source unless the owner explicitly approves a source-code change.
    2. Fresh-login immediately before the long run (RESEARCH Pitfall 2 — expiry aborts the whole run): `node scripts/accds-login.cjs`.
    3. Start the crawl with a wide history window (Claude's discretion per CONTEXT — use ACCDS_MONTHS_BACK=84 ≈ 6.9 yrs; bump to 120 if the oldest project predates that):
       `ACCDS_MONTHS_BACK=84 ACCDS_CONCURRENCY=6 ACCDS_PAGE_CONCURRENCY=8 node scripts/accds-activity-ingest.cjs`
    4. Optional live monitor: `python scripts/accds_progress_ui.py` → http://localhost:4322.
    5. On `SessionExpiredError` (run halts): `node scripts/accds-login.cjs` then resume the remainder:
       `ACCDS_MONTHS_BACK=84 ACCDS_RESUME=1 node scripts/accds-activity-ingest.cjs`
       (RESUME skips any project that already has ANY accds rows — VERIFIED ingest lines 78–85.)
    6. Repeat login→resume until the crawl reports `[accds] done.` with the remaining-project count at 0.
  </how-to-verify>
  <resume-signal>Type "crawl done" once the crawler printed `[accds] done.` and a resume run shows 0 remaining projects, or paste the blocking error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Detect and repair mid-expiry per-project gaps</name>
  <action>Claude runs diag-accds-recency.cjs, reads the per-day histogram, and identifies projects with the project-level resume-skip gap hazard (RESEARCH Pitfall 1); the owner re-crawls each suspect project individually via ACCDS_PROJECT (bypasses resume skip); Claude records the suspect list and repair outcome in 08-VERIFICATION.md.</action>
  <what-built>Claude reviews the per-day histogram for the project-level resume-skip hazard (RESEARCH Pitfall 1): a project whose session expired after a few of its date windows is marked "done" and skipped on resume, leaving a silent history gap. Claude identifies suspect projects; the owner re-crawls them individually.</what-built>
  <how-to-verify>
    1. Run `node scripts/diag-accds-recency.cjs`. Read the per-day histogram (last 21d) and the `createdAt range`.
    2. Identify suspect projects: any that show rows only for recent windows but nothing earlier, or whose distinct-project count is below the decided target. (For per-project depth, optionally `node scripts/diag-accds-vs-dc.cjs <projectId> <YYYY-MM>` against a DC-covered month.)
    3. For each suspect project, re-crawl it individually (bypasses resume skip — `ACCDS_PROJECT` makes RESUME a no-op):
       `ACCDS_PROJECT=<id> ACCDS_MONTHS_BACK=84 node scripts/accds-activity-ingest.cjs`
    4. Re-run `node scripts/diag-accds-recency.cjs` and confirm the gap closed.
    5. Claude records the suspect list and the repair outcome in 08-VERIFICATION.md.
  </how-to-verify>
  <resume-signal>Type "gaps clean" once the histogram shows no unexplained empty spans and per-project re-crawls are done, or list the projects still showing gaps.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Prove the coverage + presence gate and reconcile vs DC; record labeled span</name>
  <files>.planning/phases/08-activity-re-extraction/08-VERIFICATION.md</files>
  <action>Run the coverage gate and reconciliation, then record results as labeled facts (DATA-04). (a) Run the gate SQL via a read-only node one-shot (DIRECT_URL/DATABASE_URL from .env, the same pattern diag-accds-recency.cjs uses): SELECT COUNT(DISTINCT "projectId"), MAX("createdAt"), MIN("createdAt") FROM "AccActivityAccds". Confirm the distinct-project count equals the decided target from 08-01 (full membership count, or the ≈428 admin-set count). (b) Run `node scripts/verify-accds-merge.cjs` (default partitioned mode) and confirm it prints all assertions passed. (c) Write 08-VERIFICATION.md with a `## COVERAGE GATE` section: decided target vs actual distinct-project count (PASS only if they match), the MAX createdAt recorded as a labeled "latest activity as of <date>" FACT (this is report-only — it is NOT a freshness gate, per CONTEXT "report, don't block"), the MIN createdAt labeled as the real history span start (VERIFY: whether accds/v0 floored filter[created_at] — if MIN is much later than the requested 84-month window, label that as the platform history floor rather than implying full history), and the verify-accds-merge result. Do NOT fail the gate on recency; fail it only on a project-count shortfall or a reconciliation failure.</action>
  <verify>
    <automated>test -f .planning/phases/08-activity-re-extraction/08-VERIFICATION.md &amp;&amp; grep -v '^#' .planning/phases/08-activity-re-extraction/08-VERIFICATION.md | grep -qi 'coverage gate' &amp;&amp; node scripts/verify-accds-merge.cjs</automated>
  </verify>
  <done>08-VERIFICATION.md records the distinct-project count matching the decided target, the labeled latest + earliest activity dates, and `verify-accds-merge.cjs` printing all assertions passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local crawler → accds/v0 (Autodesk) | Long-running bearer-authenticated read crossing to Autodesk over TLS |
| Crawler → AccActivityAccds (Postgres) | Batched createMany writes into the local DB |
| Diagnostics → Postgres | Read-only SQL over potentially large tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | accds/v0 rate limits during a long crawl | mitigate | fetchActivityWindow already honours retry-after with backoff (4 attempts, VERIFIED). If 429s persist, lower ACCDS_CONCURRENCY / ACCDS_PAGE_CONCURRENCY (Claude's discretion per CONTEXT). |
| T-08-05 | Tampering / data integrity | partial-crawl silent gaps on resume | mitigate | Task 2 detects per-project gaps via the diag histogram and repairs with per-project ACCDS_PROJECT re-crawl (RESEARCH Pitfall 1). |
| T-08-06 | Repudiation / data truth | implying full history when accds/v0 floors created_at | mitigate | Task 3 labels MIN createdAt as the real span start and marks any platform floor VERIFY: rather than implying full lifetime (CONTEXT coverage-honesty). |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH: no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- `SELECT COUNT(DISTINCT "projectId"), MAX("createdAt"), MIN("createdAt") FROM "AccActivityAccds"` returns a distinct-project count equal to the decided target.
- `node scripts/diag-accds-recency.cjs` per-day histogram shows no unexplained empty spans.
- `node scripts/verify-accds-merge.cjs` prints all assertions passed (partitioned mode).
- 08-VERIFICATION.md `## COVERAGE GATE` records target-vs-actual, labeled latest/earliest dates, and the reconciliation result.
- `npx tsc --noEmit` = 0 (standing gate; no .ts changed — expected 0).
</verification>

<success_criteria>
- AccActivityAccds holds rows for the full decided project set; distinct-project count matches the target.
- The crawl completed with no DC quota; expiry was recovered via re-login + ACCDS_RESUME=1; per-project gaps were repaired.
- Latest and earliest activity dates are recorded as labeled facts; no recency threshold blocked the gate.
- ACCDS vs DC reconciliation passes with no silent gaps.
</success_criteria>

<output>
Create `.planning/phases/08-activity-re-extraction/08-02-SUMMARY.md` when done. State the final distinct-project count, the labeled latest/earliest activity dates, and the reconciliation result — Phases 12 and 14 gate on this.
</output>
