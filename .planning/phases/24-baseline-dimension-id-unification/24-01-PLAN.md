---
phase: 24-baseline-dimension-id-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure-spatial-graph-baseline.cjs
  - tests/e2e/spatial-graph-baseline.spec.ts
  - .planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "A committed, re-runnable script measures /users/spatial-graph first-paint AND time-to-graph-rendered against an isolated :3100 prod build"
    - "The baseline record states median-of-5 timings plus its own conditions: cosmos.gl 3.3.0, node count, BUILD_ID, commit hash, date, and the test-bridge caveat"
    - "The measurement was taken BEFORE any v2.4 runtime file changed (this plan runs before 24-02)"
    - "The live :3000 instance and its .next dist were never touched"
  artifacts:
    - path: "scripts/measure-spatial-graph-baseline.cjs"
      provides: "Re-runnable orchestrator Phase 28 re-executes identically (env record + playwright invocation + output merge)"
    - path: "tests/e2e/spatial-graph-baseline.spec.ts"
      provides: "Playwright spec doing N=5 measured loads via playwright.verify.config.ts"
    - path: ".planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md"
      provides: "First-class baseline record (the honest number the milestone is judged against)"
      contains: "time-to-graph-rendered"
  key_links:
    - from: "scripts/measure-spatial-graph-baseline.cjs"
      to: "tests/e2e/spatial-graph-baseline.spec.ts"
      via: "npx playwright test spatial-graph-baseline --config playwright.verify.config.ts"
      pattern: "playwright\\.verify\\.config"
    - from: "tests/e2e/spatial-graph-baseline.spec.ts"
      to: "window.__ACC_GRAPH_TEST__"
      via: "graph-rendered detection (isReady + getRenderedNodeCount)"
      pattern: "__ACC_GRAPH_TEST__"
---

<objective>
Capture the honest first-paint / time-to-graph-rendered baseline for `/users/spatial-graph`
BEFORE any other v2.4 change touches the surface. This is the number Phase 28's PERF-04
no-regression check compares against — the stale pre-milestone ~0.46s figure is explicitly
NOT reused.

Purpose: Roadmap Phase 24 success criterion 1. This plan carries the baseline half of
PERF-04 (its verification half stays in Phase 28); the phase's own requirement IDs DIM-03 and
DIM-06 are covered by plan 24-02.
Output: a committed measurement script (re-run identically in Phase 28), a Playwright spec,
and a committed baseline record `24-BASELINE.md`.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-baseline-dimension-id-unification/24-CONTEXT.md
@playwright.verify.config.ts
@playwright/global-setup.ts
@app/(dashboard)/users/access-analysis/graphTestBridge.ts
@tests/e2e/acc-dc-graph.spec.ts

**Verified environment facts (do not re-derive):**
- NO working dev server for measurement. The proven isolated-prod mechanism (used by 21.1-04
  and the 2026-07-14 dependency-update e2e) is:
  `$env:NEXT_PUBLIC_ACC_GRAPH_TEST="1"; $env:NEXT_DIST_DIR=".next-e2e"; npx next build --webpack`
  then `$env:NEXT_DIST_DIR=".next-e2e"; npx next start -p 3100`.
  `next.config.ts:30` honors `NEXT_DIST_DIR`. Live `:3000` (Task Scheduler) is untouched —
  guard by asserting `.next/BUILD_ID` mtime is unchanged after the isolated build.
- Auth: `playwright.verify.config.ts` has NO webServer (server started out-of-band on :3100)
  and runs `playwright/global-setup.ts`, which mints a real NextAuth admin cookie into
  `playwright/.auth/storageState.json` (needs `AUTH_SECRET`, `ADMIN_EMAIL`, `DATABASE_URL`
  from `.env` — global-setup calls `loadEnv()` itself). Do NOT invent new auth.
- Graph-rendered signal: `window.__ACC_GRAPH_TEST__` (gated by build-time
  `NEXT_PUBLIC_ACC_GRAPH_TEST === "1"`, `graphTestBridge.ts:31`) exposes `isReady()`
  (physics + features present, `:357-359`) and `getRenderedNodeCount()` (`:366-368`).
- HONESTY CAVEAT to record verbatim in the baseline: with `NEXT_PUBLIC_ACC_GRAPH_TEST="1"`,
  `GraphCanvas.tsx:137-139` forces the GPU 2D sim OFF (frozen/static-layer path), so the
  measured path is not byte-identical to live `:3000` behavior. Phase 28 re-runs the
  identical script, so the regression comparison remains apples-to-apples.
- Expected node count ~22,279 (post cosmos.gl 3.3.0 dep refresh, commit 0bfe0962); cosmos.gl
  version to record: `@cosmos.gl/graph` from package.json (3.3.0 + patch).
- `npm test` (vitest) excludes `tests/e2e/**`, so the new spec does not affect the unit gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the baseline measurement spec + orchestrator script</name>
  <files>tests/e2e/spatial-graph-baseline.spec.ts, scripts/measure-spatial-graph-baseline.cjs</files>
  <action>
Create `tests/e2e/spatial-graph-baseline.spec.ts` — one test, generous timeout (reuse the
360s timeout of playwright.verify.config.ts), that performs N=5 measured loads of
`/users/spatial-graph`:

- Each run uses a FRESH browser context (`browser.newContext({ storageState: "./playwright/.auth/storageState.json" })`)
  so client caches don't compound; run 1 after server start is the cold run — label it.
- Per run: `page.goto("/users/spatial-graph", { waitUntil: "commit" })`, then inside the page
  capture:
  (a) **first-paint / FCP**: `performance.getEntriesByType("paint")` → `first-contentful-paint`
      startTime (fall back to `first-paint` if FCP missing).
  (b) **time-to-graph-rendered**: a `page.evaluate` requestAnimationFrame loop that resolves
      with `performance.now()` at the first frame where
      `window.__ACC_GRAPH_TEST__?.isReady() && window.__ACC_GRAPH_TEST__.getRenderedNodeCount() > 0`
      (frame-accurate; do not use waitForFunction's default 100ms polling for the timestamp).
      Both timestamps are relative to the run's navigation timeOrigin, so they are directly
      comparable.
- Also capture once: `getRenderedNodeCount()` and `getFeatureCount()` (expected ~22,279).
- Compute median of the 5 for both metrics; write a JSON artifact
  `test-results/spatial-graph-baseline.json` with: per-run values (cold flagged), medians,
  node/feature counts, timestamp. `console.log` the JSON too.
- Skip-guard: if `__ACC_GRAPH_TEST__` is undefined after load, fail with a clear message
  ("server not built with NEXT_PUBLIC_ACC_GRAPH_TEST=1").

Create `scripts/measure-spatial-graph-baseline.cjs` — the re-runnable orchestrator Phase 28
executes identically:

1. Preflight: HTTP GET `http://localhost:3100/login` must respond (plain `fetch`, Node >=22);
   if not, exit non-zero printing the exact build+serve commands (from context above). The
   script does NOT build or start the server itself (keeps it side-effect free; the build is
   an explicit, owner-visible step).
2. Record environment: `@cosmos.gl/graph` version from `package.json`, presence of
   `patches/@cosmos.gl+graph+3.3.0.patch`, `.next-e2e/BUILD_ID` contents,
   `git rev-parse HEAD`, ISO date.
3. Run `npx playwright test spatial-graph-baseline --config playwright.verify.config.ts`
   (spawnSync, inherit stdio) — this triggers global-setup auth minting automatically.
4. Read `test-results/spatial-graph-baseline.json`, merge with the environment record, and
   print a single final JSON block (the content 24-BASELINE.md is written from).

Keep both files boring: no new dependencies, `@playwright/test` only in the spec, Node
stdlib only in the .cjs.
  </action>
  <verify>
    <automated>node -e "require('./scripts/measure-spatial-graph-baseline.cjs')" 2>&1 | head -5 — must fail gracefully with the "server not running" preflight message (server not yet started), not a stack trace; and `npx tsc --noEmit` passes with the new spec.</automated>
  </verify>
  <done>Both files exist and are committed; the orchestrator preflight-fails cleanly when :3100 is down; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Build isolated :3100 prod dist and run the measurement</name>
  <files>none — runs the build and script; no source edits</files>
  <action>
Confirm the working tree's runtime code is at the baseline commit state (no v2.4 runtime
changes yet — `git status --short` should show only pre-existing WIP outside
`app/(dashboard)/users/access-analysis/`; plan 24-02 has NOT run).

1. Record `.next/BUILD_ID` mtime (live :3000 dist) before starting.
2. Build isolated: PowerShell
   `$env:NEXT_PUBLIC_ACC_GRAPH_TEST="1"; $env:NEXT_DIST_DIR=".next-e2e"; npx next build --webpack`
   (NEVER plain `npm run build` — that would hit the live `.next`). This is the proven
   21.1-04 mechanism; :3000 keeps serving throughout.
3. Assert `.next/BUILD_ID` mtime unchanged — abort and investigate if it moved.
4. Start the isolated server in the background:
   `$env:NEXT_DIST_DIR=".next-e2e"; npx next start -p 3100`
   Wait until `http://localhost:3100/login` responds.
5. Run `node scripts/measure-spatial-graph-baseline.cjs`. Capture the final JSON block.
6. Sanity: rendered node count in the output should be ~22,279 (matches the post-0bfe0962
   graph); if it differs wildly, stop and report rather than recording a bogus baseline.
7. Kill the :3100 server process when done.
  </action>
  <verify>
    <automated>node scripts/measure-spatial-graph-baseline.cjs — exits 0 and prints the merged JSON with 5 runs, two medians, BUILD_ID, commit hash, cosmos version.</automated>
  </verify>
  <done>Measurement JSON in hand with median first-paint AND median time-to-graph-rendered from 5 runs against the :3100 prod build; :3000 verified untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the baseline record as a first-class artifact</name>
  <files>.planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md</files>
  <action>
Write `24-BASELINE.md` from the script output. Required content (this is the record Phase 28
judges against — treat it as a contract, not a scratch note):

- **The judged metric:** median time-to-graph-rendered (Phase 28's PERF-04 no-regression
  check judges THIS number). First-paint median recorded as supporting context.
- Per-run table (5 runs, cold run flagged), medians.
- **Conditions of measurement:** cosmos.gl `3.3.0` (+ patch file name), node count observed
  (expected ~22,279 — record actual), `.next-e2e/BUILD_ID`, commit hash, date,
  `next build --webpack` prod build served by `next start -p 3100`,
  `NEXT_PUBLIC_ACC_GRAPH_TEST=1`.
- **Honesty caveats, verbatim:** (1) the test bridge forces the GPU 2D sim OFF
  (`GraphCanvas.tsx:137-139`), so this measures the frozen/static-layer path, not live
  `:3000` behavior — valid for regression comparison only via the identical script;
  (2) the stale pre-milestone ~0.46s figure is superseded and must not be cited.
- **Re-run instructions for Phase 28:** the exact build/serve/run commands from Task 2.

Commit the record together with the script + spec (docs commit per workflow).
  </action>
  <verify>
    <automated>grep -q "time-to-graph-rendered" ".planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md" && grep -q "BUILD_ID" ".planning/phases/24-baseline-dimension-id-unification/24-BASELINE.md"</automated>
  </verify>
  <done>24-BASELINE.md committed with both metrics, 5-run data, full environment record, caveats, and Phase 28 re-run instructions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` green (new spec typechecks).
- `npm test` green (vitest excludes tests/e2e — no unit-test surface changed).
- `.next/BUILD_ID` (live :3000 dist) mtime unchanged by the isolated build.
- No runtime source file under `app/`, `components/`, `lib/`, `server/` modified by this plan.
- Baseline record exists with both metrics + environment + caveats.
</verification>

<success_criteria>
Roadmap Phase 24 SC#1 satisfied: a first-paint (and graph-rendered) baseline for
`/users/spatial-graph` is measured on the isolated :3100 prod build and committed BEFORE any
v2.4 runtime change, with a re-runnable script Phase 28 executes identically.
</success_criteria>

<output>
After completion, create `.planning/phases/24-baseline-dimension-id-unification/24-01-SUMMARY.md`
</output>
