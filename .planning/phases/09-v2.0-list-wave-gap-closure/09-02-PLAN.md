---
phase: 09-v2.0-list-wave-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/(dashboard)/users/UsersDirectoryClient.tsx
autonomous: true
requirements: [LIST-01, LIST-02]
must_haves:
  truths:
    - "User sees a Status column on every directory row with one of three pills: Active / Pending / Deleted"
    - "User sees an inline Admin pill on rows where the user is project admin on at least one project"
    - "Toolbar exposes a multi-select Status facet that filters the visible list to the selected statuses"
    - "Toolbar exposes a Project Admin binary facet that filters to admin-only rows"
    - "Clicking a Status pill on a row replaces the Status facet selection with that single status (re-click clears)"
    - "Clicking the Admin pill toggles the Project Admin facet on/off"
    - "Filter pill text is load-bearing — color is decoration only (accessibility)"
  artifacts:
    - path: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      provides: "Status column + Admin pill + Status/Admin facets + pill click handlers"
      contains: "statusFilter"
  key_links:
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "lib/acc/accStatusReduction.ts"
      via: "reduceMemberStatus fallback when aggregatedStatus undefined"
      pattern: "reduceMemberStatus"
    - from: "app/(dashboard)/users/UsersDirectoryClient.tsx"
      to: "BulkAccUser.aggregatedStatus / BulkAccUser.projectAdmin"
      via: "useMergedAccUsers consumes enrichedUsers"
      pattern: "aggregatedStatus|projectAdmin"
---

<objective>
Surface `status` and `projectAdmin` on the user directory: column + inline pill + multi-select facet (status) + binary facet (admin) + pill-click handlers that drive facet reduction (CONTEXT lock: facet-reduction IS the spotlight).

Purpose: Close LIST-01 + LIST-02. Both data fields already live on `BulkAccUser` via `accMembers.enrichedUsers` (shipped Wave 5.1) — this plan is pure UI wiring inside the canonical directory surface.

Output: Status pills, Admin pills, Status multi-select facet, Project Admin binary facet, click-through handlers, accessible pill text.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-CONTEXT.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-RESEARCH.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-01-SUMMARY.md

# Surface this plan modifies
@app/(dashboard)/users/UsersDirectoryClient.tsx
@app/(dashboard)/users/useMergedAccUsers.ts
@lib/acc/acc-types.ts

<interfaces>
<!-- Already shipped — consume verbatim. -->

From `lib/acc/acc-types.ts`:
```typescript
export interface BulkAccUser {
  // ...
  aggregatedStatus?: "active" | "pending" | "deleted";  // canonical from enrichedUsers
  projectAdmin?: boolean;                                // canonical from enrichedUsers
  // (perProjectStatuses lives on enrichedUsers row, not BulkAccUser — fetch separately for tooltip if needed)
}
```

From Plan 09-01 (shipped):
```typescript
import { reduceMemberStatus, type AggregatedStatus } from "@/lib/acc/accStatusReduction";
```

From existing filter state in `UsersDirectoryClient.tsx` (lines 1019-1025, per RESEARCH):
```typescript
// existing facets are single-select strings — keep that shape for new ones we add
const [filterAccProject, setFilterAccProject] = useState<string | null>(null);
// etc.
```

Existing components reused:
- shadcn `Badge` (variant prop) — CONTEXT lock, no new pill component.
- shadcn `Tooltip` — for hover detail per DASH-18.
- `ActiveFilterPill` rendering at line ~792 — extend with status + admin entries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status column + Admin inline pill on every row</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
Read `BulkAccUser.aggregatedStatus` and `BulkAccUser.projectAdmin` (already merged into rows via `useMergedAccUsers`).

**Status column:**
- Add a new column header `Status` to the directory header row (whatever existing pattern the file uses — locate by grepping for an existing column header label like `Last Sign-In` or `Company`).
- In the row cell, render a shadcn `Badge` whose **text** is the canonical label (CONTEXT carry-forward: pill text load-bearing, color supplementary):
  - `aggregatedStatus === "active"` → `<Badge variant="success">Active</Badge>` (Tailwind `bg-emerald-100 text-emerald-900` if `variant="success"` doesn't exist in `components/ui/badge.tsx` — check first, use the closest existing variant).
  - `"pending"` → `<Badge variant="warning">Pending</Badge>` (amber).
  - `"deleted"` → `<Badge variant="secondary">Deleted</Badge>` (gray).
  - `undefined` (enrichedUsers in flight) → fallback to `reduceMemberStatus(row.perProjectStatuses ?? [])` — if `perProjectStatuses` not present on BulkAccUser, just render a skeleton `<Badge variant="outline" className="opacity-50">…</Badge>` until enrichment arrives.
- Wrap each pill in a `<Tooltip>` whose content displays the per-project status breakdown if available (`row.perProjectStatuses?.map(p => p.projectName + ": " + p.status).join(", ")` else "Aggregated across all projects").
- Make the pill clickable (Task 3 wires the handler).

**Admin pill (inline):**
- Render in the existing user identity cell (next to the user name / email — locate where the file already renders identity), only when `row.projectAdmin === true`. Use `<Badge variant="default" className="ml-2">Admin</Badge>`.
- Wrap in `<Tooltip content="Project Admin on at least one project">`.
- Click handler hooked in Task 3.

Use existing icons from `lucide-react` if you need a leading glyph (`ShieldCheck` for Admin is conventional — already imported in the file? grep first; if not, add to the lucide-react import at the top, do NOT add a new package).

Accessibility: Pill `<Badge>` already renders as a `<div>` / `<span>`; wrap in `<button type="button">` when clickable so keyboard + screen readers work. Tooltip provides expanded text per DASH-18.

Touch ONLY the column + row render JSX in this task — Task 2 owns the facet toolbar; Task 3 owns the click handlers.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run app/(dashboard)/users/cosmosUtils.test.ts</automated>
  </verify>
  <done>Status column visible on every row; Admin pill renders when projectAdmin=true; pill text matches canonical label exactly (Active/Pending/Deleted/Admin); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Status multi-select facet + Project Admin binary facet in toolbar</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
Extend the filter toolbar (existing facets cluster — `filterDept`, `filterJobTitle`, `filterAccProject`, etc. around line 1019).

**Status facet — multi-select** (resolves RESEARCH Open Question 4 → CONTEXT-aligned recommendation):
- Add state: `const [statusFilter, setStatusFilter] = useState<AggregatedStatus[]>([])`. Empty array = "all statuses pass" (no filtering).
- Render a new toolbar dropdown labelled `Status` that opens a checkbox list of `["active","pending","deleted"]` (use shadcn `Popover` + `Checkbox` if the existing facets use that pattern — match the existing facets' visual idiom verbatim; if existing facets use a `DropdownMenu` with `DropdownMenuCheckboxItem`, mirror that instead).
- Multi-select semantics: clicking a checkbox toggles that status in the array. "Clear" button resets to `[]`.
- The filter predicate added to the main `useMemo` row-filter pipeline (locate the existing predicate composition, around the same area as the other facet filters):
  ```typescript
  if (statusFilter.length > 0) {
    const rowStatus = row.aggregatedStatus ?? reduceMemberStatus(row.perProjectStatuses?.map(p => p.status) ?? []);
    if (!statusFilter.includes(rowStatus)) return false;
  }
  ```
- Update `ActiveFilterPill` rendering (around line ~792) to include one chip per selected status (e.g., "Status: Active") with an `x` to remove that specific status from the array.
- Update `hasActiveFilters` + `activeFilterCount` (existing helpers in the file — grep for them) to count `statusFilter.length > 0` as an active filter, and `statusFilter.length` as the count contribution.

**Project Admin facet — binary** (CONTEXT discretion: "toggle Project Admin binary facet on/off"):
- Add state: `const [projectAdminFilter, setProjectAdminFilter] = useState<boolean>(false)`. Default off.
- Render as a simple `<Toggle>` / `<Switch>` (whichever exists in `components/ui/`; if neither, use a checkbox-styled `<Button variant="outline">` with active-state highlight, matching how `compactionMode`-style binary toggles are done elsewhere in the file).
- Filter predicate:
  ```typescript
  if (projectAdminFilter && row.projectAdmin !== true) return false;
  ```
- Add chip to `ActiveFilterPill` when on: "Project Admin only".
- Include in `hasActiveFilters` + `activeFilterCount`.

Import `AggregatedStatus` from `@/lib/acc/accStatusReduction` and `reduceMemberStatus` for the fallback.

DO NOT introduce a new event channel — facet reduction IS the spotlight (CONTEXT lock + RESEARCH Pattern 3). The visible row count change is the cross-widget signal.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Status dropdown + Project Admin toggle appear in toolbar; selecting them filters the list; chips appear in ActiveFilterPill area; clearing chips restores the row; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Pill click handlers (status replace-selection, admin toggle) + scroll-to-top</name>
  <files>app/(dashboard)/users/UsersDirectoryClient.tsx</files>
  <action>
Wire the pills shipped in Task 1 to the facet state shipped in Task 2.

**Status pill click handler** (CONTEXT lock: "replace selection — re-clicking same pill clears all status filters"):
```typescript
const handleStatusPillClick = useCallback((status: AggregatedStatus) => {
  setStatusFilter((prev) =>
    prev.length === 1 && prev[0] === status ? [] : [status]
  );
  // Scroll to top per CONTEXT discretion recommendation
  rowVirtualizerRef.current?.scrollToIndex(0, { align: "start" });
}, []);
```
- Attach to the `<button type="button" onClick={() => handleStatusPillClick(rowStatus)}>` wrapping the status Badge.
- Locate the existing `rowVirtualizer` from `useVirtualizer` (top of file imports `@tanstack/react-virtual` per RESEARCH). The exact API is `virtualizerInstance.scrollToIndex(0)` — if the file already holds the virtualizer in a variable like `rowVirtualizer`, call that. If a ref isn't yet available, capture the return of `useVirtualizer` and call `.scrollToIndex(0)` directly (no ref needed — it's already an instance).
- The click is keyboard-accessible via the `<button>` wrapper (Enter / Space) — Tooltip remains hover-only.

**Admin pill click handler** (CONTEXT discretion: "toggle Project Admin binary facet on/off"):
```typescript
const handleAdminPillClick = useCallback(() => {
  setProjectAdminFilter((prev) => !prev);
  rowVirtualizerRef.current?.scrollToIndex(0, { align: "start" });
}, []);
```
- Attach to the `<button type="button" onClick={handleAdminPillClick}>` wrapping the Admin Badge.

**No new event channel.** The visible list-reduction is the spotlight signal (CONTEXT lock). Do not dispatch into `SelectionContext` for these clicks — pills are facet shortcuts only.

**Prevent click propagation:** Each pill `<button>` must `stopPropagation()` in onClick so clicking the pill does NOT also trigger the row's existing "open side panel" handler. Pattern:
```typescript
onClick={(e) => { e.stopPropagation(); handleStatusPillClick(rowStatus); }}
```

**Update `activeFilterCount` / `hasActiveFilters`** if not already done in Task 2 — verify both facets are counted.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run app/(dashboard)/users/cosmosUtils.test.ts</automated>
  </verify>
  <done>Clicking a Status pill filters the list to that status only; clicking the same pill again clears it; clicking Admin pill toggles the admin-only filter; both scroll the virtualized list back to top; clicking a pill does NOT open the side panel; tsc clean.</done>
</task>

</tasks>

<verification>
- `grep -n "statusFilter\|projectAdminFilter" app/(dashboard)/users/UsersDirectoryClient.tsx` → multiple hits (state, predicate, chips, handlers).
- `grep -n "reduceMemberStatus\|AggregatedStatus" app/(dashboard)/users/UsersDirectoryClient.tsx` → ≥ 2 hits (import + fallback).
- `grep -n "handleStatusPillClick\|handleAdminPillClick" app/(dashboard)/users/UsersDirectoryClient.tsx` → 2 distinct handlers.
- `grep -n "stopPropagation" app/(dashboard)/users/UsersDirectoryClient.tsx` → at least 2 hits (one per pill).
- `grep -n "Active\|Pending\|Deleted" app/(dashboard)/users/UsersDirectoryClient.tsx` → status pill labels present as literals.
- `grep -n "lastFileActivity" lib/acc/acc-types.ts server/routers/users.ts` → still 0 hits (Pitfall 1 — no eager loading).
- `pnpm tsc --noEmit` passes.
</verification>

<success_criteria>
- LIST-01 closes: Status column + multi-select facet visible and functional.
- LIST-02 closes: Admin pill + binary facet visible and functional.
- Pill clicks drive facet state (no spotlight side-channel).
- Pill text load-bearing on all states.
- No regression to existing facets, virtualization, or side-panel open behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-v2.0-list-wave-gap-closure/09-02-SUMMARY.md` documenting: pill variant choices, the existing Badge component reused, the chosen toolbar idiom (Popover+Checkbox vs DropdownMenuCheckboxItem), the scroll-to-top API call site, and any DASH-18 tooltip text shipped.
</output>
