---
phase: 09-v2.0-list-wave-gap-closure
plan: 05
type: execute
wave: 4
depends_on: ["09-02", "09-03", "09-04"]
files_modified:
  - .planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04]
must_haves:
  truths:
    - "Luis personally observes Status pills + Admin pills + multi-select Status facet + binary Project Admin facet behaving per CONTEXT decisions"
    - "Luis personally observes Last File Activity column resolving lazily + server-sort header with spinner + empty-append behavior"
    - "Luis personally observes Module Access section in the side panel rendering top-level + deviations + warning icons + click-through filtering"
    - "Luis personally observes no raw JSON, no manual sync UI regression, no broken side-panel-open behavior, and no console errors"
    - "UAT.md DECISION line flips to APPROVED or to GAPS-DOCUMENTED with explicit issues"
  artifacts:
    - path: ".planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md"
      provides: "Phase 09 manual UAT verdict + issue list"
      contains: "DECISION:"
  key_links:
    - from: "09-UAT.md"
      to: "LIST-01..04 in REQUIREMENTS.md"
      via: "checklist mapping each truth → requirement ID"
      pattern: "LIST-0[1-4]"
---

<objective>
Block phase 09 acceptance behind a Luis-driven manual UAT pass on his localhost dashboard. All four LIST requirements are user-perceptible — they MUST be verified by the user before the phase closes.

Purpose: Honor the user-driven UAT pattern established in Phases 4, 7, and 8 (RETROSPECTIVE consistently lands DECISION lines from manual passes, not automated checks). Provide a scaffold so the user knows exactly what to test.

Output: `09-UAT.md` with a structured checklist + DECISION line.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-v2.0-list-wave-gap-closure/09-CONTEXT.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-01-SUMMARY.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-02-SUMMARY.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-03-SUMMARY.md
@.planning/phases/09-v2.0-list-wave-gap-closure/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 09-UAT.md with PENDING DECISION + checklist</name>
  <files>.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md</files>
  <action>
Create `09-UAT.md` following the Phase 8 UAT.md template (see `.planning/phases/08-dc-per-module-ingest-permission-csvs/08-UAT.md` for the canonical shape). Include:

**Frontmatter:**
```yaml
---
phase: 09-v2.0-list-wave-gap-closure
status: PENDING-MANUAL-UAT
created: <today's date>
---
```

**Sections:**

1. **DECISION** — literal token line: `DECISION: PHASE-9-ACCEPT=PENDING-MANUAL-UAT` (Luis flips to `APPROVED`, `GAPS-DOCUMENTED`, or `REVERT` after testing).

2. **Pre-flight (auto-verified by execute-plan)** — listing of what shipped per Plan SUMMARY files:
   - 09-01: tRPC procedures (`getLastFileActivityBatch`, `usersOrderedByLastFileActivity`, `getProductsForUser`) + helpers + EXPLAIN ANALYZE
   - 09-02: Status column + Admin pill + facets in directory
   - 09-03: Module Access section in side panel
   - 09-04: Lazy file-activity column + server sort + IntersectionObserver hook

3. **Manual UAT Checklist** — 4 sections, one per LIST requirement. Each line includes the **observable truth**, the **steps**, and a `[ ]` Pass / `[ ]` Fail / `[ ]` Notes column. Sample:

   ### LIST-01: Status column + facet
   - [ ] Pass / [ ] Fail — Open `/users`. Each row shows a colored pill labelled exactly `Active`, `Pending`, or `Deleted`.
   - [ ] Pass / [ ] Fail — Hovering a status pill shows a tooltip listing per-project statuses (or "Aggregated across all projects").
   - [ ] Pass / [ ] Fail — Click a `Pending` pill on any row → list reduces to only pending users; chip appears in active filter row.
   - [ ] Pass / [ ] Fail — Click the same `Pending` pill again → list returns to all rows; chip disappears.
   - [ ] Pass / [ ] Fail — Open the Status facet in the toolbar → checkbox list shows all three; selecting two filters to the union.

   ### LIST-02: Admin pill + facet
   - [ ] Pass / [ ] Fail — Some rows show an `Admin` pill inline with the user name. Hover shows "Project Admin on at least one project".
   - [ ] Pass / [ ] Fail — Click `Admin` pill → list filters to admin-only; toggle exists in toolbar; re-click clears.
   - [ ] Pass / [ ] Fail — Clicking the pill does NOT open the side panel.

   ### LIST-03: Last File Activity column + sort
   - [ ] Pass / [ ] Fail — Column shows skeleton placeholders for off-screen rows; scrolling resolves cells to relative timestamps (e.g., "3 days ago") or em-dash "—".
   - [ ] Pass / [ ] Fail — Em-dash hover shows tooltip "No activity in 90d".
   - [ ] Pass / [ ] Fail — Click column header → sort arrow appears; spinner shows briefly; rows reorder most-recent-first.
   - [ ] Pass / [ ] Fail — Click header again → sort flips to ascending.
   - [ ] Pass / [ ] Fail — Click header a third time → sort clears, original row order restored.
   - [ ] Pass / [ ] Fail — Users with no file activity sort to the bottom (alphabetically among themselves) in BOTH directions.
   - [ ] Pass / [ ] Fail — Network tab shows ONE `getLastFileActivityBatch` request per visible-rows change (not N parallel). Sort fetches `usersOrderedByLastFileActivity` pages.

   ### LIST-04: Module Access section
   - [ ] Pass / [ ] Fail — Click any row to open the side panel. A `Module Access` section is visible and EXPANDED by default.
   - [ ] Pass / [ ] Fail — Section shows e.g. "Docs: Member across all projects" for modules with consistent tier, and "except Project X: Administrator" for deviations.
   - [ ] Pass / [ ] Fail — Modules with unknown keys show a warning icon (⚠) with tooltip "Unknown module from APS".
   - [ ] Pass / [ ] Fail — Loading state shows skeleton rows, NEVER raw JSON.
   - [ ] Pass / [ ] Fail — Clicking a module row narrows the directory list to users with that module (and ideally same tier — see 09-03 SUMMARY for any trade-offs).

   ### Cross-cutting
   - [ ] Pass / [ ] Fail — No console errors on `/users` page load or during interactions.
   - [ ] Pass / [ ] Fail — Existing facets, side panel sections, virtualized scroll all still work.
   - [ ] Pass / [ ] Fail — No "Refresh" / "Sync All" / stale banner UI introduced (feedback memory).
   - [ ] Pass / [ ] Fail — Pill text matches canonical labels (Active / Pending / Deleted / Admin) — color is supplementary.

4. **Issue Log** — empty table for Luis to fill in:

   | # | Severity | Requirement | Observation | Suggested Fix |
   |---|---|---|---|---|

5. **Sign-off Box** — date + Luis's name when DECISION flips.

Add a closing note that gap-closure follow-up should re-run `/gsd:plan-phase 09 --gaps` if any LIST requirement fails.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md','utf8'); if(!t.includes('DECISION: PHASE-9-ACCEPT=PENDING-MANUAL-UAT')) process.exit(1); if(!t.includes('LIST-01')||!t.includes('LIST-02')||!t.includes('LIST-03')||!t.includes('LIST-04')) process.exit(2); console.log('OK');"</automated>
  </verify>
  <done>09-UAT.md exists; DECISION token present; all four LIST requirement sections + cross-cutting + issue log + sign-off scaffolded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Luis manual UAT pass + DECISION line</name>
  <files>.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md</files>
  <action>
Luis walks through the 09-UAT.md checklist on his PC. Marks Pass / Fail / Notes for each line. Fills the Issue Log table for any failures. Flips the DECISION token at the top of 09-UAT.md from `PENDING-MANUAL-UAT` to `APPROVED`, `GAPS-DOCUMENTED`, or `REVERT`. Commits the updated file.

Claude has already automated everything that can be automated (4 execute plans shipping LIST-01..04 code + the UAT.md scaffold). This checkpoint exists because the truths in must_haves are subjective ("Luis personally observes...") and the canonical project pattern (Phases 4, 7, 8) is that Luis owns the acceptance verdict.
  </action>
  <what-built>
Phase 09 ships LIST-01..04 across four execute plans:
- 09-01: tRPC procedures + pure helpers + EXPLAIN ANALYZE
- 09-02: Status column + Admin pill + facets (LIST-01, LIST-02)
- 09-03: Module Access section in side panel (LIST-04)
- 09-04: Lazy file-activity column + server-side sort (LIST-03)

UAT.md scaffold is in place. The phase is code-complete pending Luis's manual UAT pass.
  </what-built>
  <how-to-verify>
1. Pull deploy branch on Luis's PC and restart the dashboard via Task Scheduler (or `pnpm dev` if testing locally first).
2. Open `http://localhost:3000/users` in Chrome.
3. Walk through `.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md` checklist top to bottom — every line corresponds to one observable behavior.
4. Mark Pass/Fail/Notes for each line. Fill the Issue Log table for any failures.
5. Flip the DECISION token at the top of 09-UAT.md from `PENDING-MANUAL-UAT` to one of:
   - `APPROVED` — all sections pass, phase closes.
   - `GAPS-DOCUMENTED` — some failures, list them in Issue Log; trigger `/gsd:plan-phase 09 --gaps` later.
   - `REVERT` — major breakage, revert the phase entirely.
6. Commit the updated 09-UAT.md.

Expected positive outcome: every row shows a Status pill, admins show an Admin pill, the Last File Activity column shows skeletons that resolve as you scroll, clicking the column header sorts the list, and the side panel shows a Module Access section with summary + deviations. No raw JSON, no console errors, no manual sync UI.
  </how-to-verify>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md','utf8'); const m=t.match(/DECISION:\\s*PHASE-9-ACCEPT=(APPROVED|GAPS-DOCUMENTED|REVERT)/); if(!m){console.error('DECISION still PENDING');process.exit(1)} console.log('DECISION='+m[1])"</automated>
  </verify>
  <done>DECISION line in 09-UAT.md reads APPROVED, GAPS-DOCUMENTED, or REVERT (no longer PENDING-MANUAL-UAT); file committed.</done>
  <resume-signal>Type "approved" if the DECISION line reads APPROVED, "gaps" if GAPS-DOCUMENTED (include list of failed checklist lines), or "revert" if REVERT.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/09-v2.0-list-wave-gap-closure/09-UAT.md` exists.
- DECISION token line present.
- All four LIST-0X requirement sections present.
- Issue Log scaffold present.
</verification>

<success_criteria>
- UAT.md scaffolded and committed.
- Luis runs through the checklist on his PC.
- DECISION line flipped to APPROVED / GAPS-DOCUMENTED / REVERT.
- If APPROVED → Phase 09 closes, LIST-01..04 marked complete in REQUIREMENTS.md by the post-phase update step.
- If GAPS-DOCUMENTED → trigger gap-closure planning with `/gsd:plan-phase 09 --gaps` consuming 09-UAT.md.
</success_criteria>

<output>
After Luis flips the DECISION line, create `.planning/phases/09-v2.0-list-wave-gap-closure/09-05-SUMMARY.md` capturing the DECISION verdict, any documented gaps, and a "next steps" pointer (either "Phase 09 complete — proceed to Phase 10" if APPROVED, or "Run /gsd:plan-phase 09 --gaps next" if gaps remain).
</output>
