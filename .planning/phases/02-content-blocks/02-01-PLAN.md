---
phase: 02-content-blocks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/clash/wiki-editor/table-node/extensions/table-node-extension.ts
  - components/clash/wiki-editor/table-node/ui/table-handle.tsx
  - components/clash/wiki-editor/table-node/styles/table-node.scss
  - components/clash/wiki-editor/slash-menu/index.tsx
  - components/clash/wiki-editor/drag-handle/index.tsx
autonomous: true
requirements:
  - REQ-02

must_haves:
  truths:
    - "All new npm packages install without conflicts or audit errors"
    - "Tiptap CLI components (table-node, drag-context-menu, slash-dropdown-menu) are copied into the project"
    - "sass devDependency is present and Next.js compiles SCSS without error"
    - "SheetJS 0.20.3 is installed from CDN tarball (not npm registry 0.18.5)"
  artifacts:
    - path: "package.json"
      provides: "All Phase 2 dependencies declared"
      contains: "@tiptap/extension-table"
    - path: "components/clash/wiki-editor/table-node/extensions/table-node-extension.ts"
      provides: "TableKit CLI-installed extension"
    - path: "components/clash/wiki-editor/slash-menu/index.tsx"
      provides: "SlashDropdownMenu CLI-installed component"
    - path: "components/clash/wiki-editor/drag-handle/index.tsx"
      provides: "DragHandle wrapper component scaffold"
  key_links:
    - from: "package.json"
      to: "node_modules/@tiptap/extension-table"
      via: "npm install"
      pattern: "@tiptap/extension-table"
    - from: "package.json"
      to: "node_modules/xlsx"
      via: "CDN tarball install"
      pattern: "cdn.sheetjs.com"
---

<objective>
Install all Phase 2 dependencies and scaffold the CLI-installed Tiptap UI components (table-node, slash-dropdown-menu, drag-context-menu) into the project. This is the foundation wave — all subsequent plans depend on these packages being present.

Purpose: Phase 2 blocks cannot be built without the packages in place. Running Tiptap CLI separately before implementation avoids mid-task npm failures that corrupt context.
Output: Updated package.json, CLI-installed component files, sass devDependency, SheetJS CDN tarball.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-blocks/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 npm dependencies</name>
  <files>package.json</files>
  <action>
Run the following installs from the project root (C:/LECG/Dashboard):

```bash
# Tiptap table + drag handle extensions
npm install @tiptap/extension-table @tiptap/extension-drag-handle @tiptap/extension-drag-handle-react @tiptap/extension-node-range

# PDF viewer
npm install react-pdf

# SCSS for Tiptap UI Components
npm install --save-dev sass

# SheetJS from CDN tarball — DO NOT use npm install xlsx (npm registry version 0.18.5 has high-severity CVE)
npm install --save https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
```

After install, run `npm audit` to verify no new high/critical vulnerabilities introduced (xlsx from CDN tarball should show clean). Do NOT run `npm audit fix` — just verify.

Also verify the installed xlsx version is 0.20.3 (not 0.18.5) by checking package.json — the entry should contain "cdn.sheetjs.com/xlsx-0.20.3".
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && node -e "require('@tiptap/extension-table'); require('react-pdf'); require('xlsx'); console.log('deps OK')"</automated>
  </verify>
  <done>
package.json updated with @tiptap/extension-table, @tiptap/extension-drag-handle, @tiptap/extension-drag-handle-react, @tiptap/extension-node-range, react-pdf, sass (devDep), xlsx from CDN tarball. All packages resolve without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Tiptap UI Components via CLI</name>
  <files>
    components/clash/wiki-editor/table-node/extensions/table-node-extension.ts
    components/clash/wiki-editor/table-node/ui/table-handle.tsx
    components/clash/wiki-editor/table-node/styles/table-node.scss
    components/clash/wiki-editor/slash-menu/index.tsx
    components/clash/wiki-editor/drag-handle/index.tsx
    app/globals.css
  </files>
  <action>
Run the Tiptap CLI from the project root to install the three required UI components:

```bash
cd C:/LECG/Dashboard

# Table node (installs TableKit, TableHandle, TableTriggerButton, TableSelectionOverlay)
npx @tiptap/cli@latest add table-node

# Drag context menu (right-click block operations)
npx @tiptap/cli@latest add drag-context-menu

# Slash dropdown menu (/ command menu)
npx @tiptap/cli@latest add slash-dropdown-menu
```

The CLI copies component files into the project (shadcn-style) and injects SCSS imports into `app/globals.css`.

**After CLI runs:**
1. Verify `app/globals.css` now contains SCSS `@import` statements for the Tiptap components (the CLI injects these automatically).
2. If CLI placed components under `components/tiptap-node/` or `components/tiptap-ui/` instead of `components/clash/wiki-editor/`, leave them where the CLI put them — do NOT move them. Document the actual paths in the SUMMARY.md.
3. Check `_variables.scss` injected by CLI for any CSS variable name conflicts with existing Tailwind/shadcn variables (look for `--background`, `--foreground`, `--primary` conflicts). If conflicts exist, add a comment noting them — resolution happens in 02-05.
4. Run `npm run build 2>&1 | head -30` to confirm SCSS compiles without error. If build error mentions SCSS variable conflicts, prefix the conflicting Tiptap variables with `--tiptap-` in the installed SCSS files.
  </action>
  <verify>
    <automated>cd C:/LECG/Dashboard && npm run build 2>&1 | grep -E "error|Error|failed|Failed" | head -20 || echo "build clean"</automated>
  </verify>
  <done>
Tiptap CLI components installed. SCSS compiles without error. app/globals.css contains Tiptap SCSS imports. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('@tiptap/extension-table')"` exits 0
2. `node -e "require('react-pdf')"` exits 0
3. `node -e "require('xlsx')"` exits 0 AND version is 0.20.3
4. Tiptap CLI component files exist in project (table-node, slash-dropdown-menu, drag-context-menu directories)
5. `npm run build` exits 0 with no SCSS errors
</verification>

<success_criteria>
All Phase 2 npm packages installed. Tiptap CLI components copied into project. SCSS compiles. Build passes. Ready for Wave 2 feature implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-blocks/02-01-SUMMARY.md` documenting:
- Exact CLI component installation paths (where CLI placed the files)
- Any SCSS variable conflicts found and how resolved
- Any install errors and how resolved
</output>
