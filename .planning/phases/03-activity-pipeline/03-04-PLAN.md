---
phase: 03-activity-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
  - components/layout/SyncFreshnessPill.tsx
  - server/routers/acc-sync.ts
autonomous: true
requirements:
  - ACTV-04
must_haves:
  truths:
    - "RecentlyAddedWidget surfaces a row list (10 rows visible, See all link) below the existing 90-day heatmap"
    - "Each invitation row shows stacked avatars: invitee in front, inviter slightly behind with ~6px overlap"
    - "Unresolved inviter renders 'Invited by Unknown' with a small warning icon"
    - "Clicking an inviter avatar/name applies a 'Filtered by Jane Doe ×' pill at the top of the widget; while the pill is active, the time-window filter is relaxed (all invitations by Jane regardless of date)"
    - "Re-invited users show primary inviter + '(+N others)' suffix; hover reveals full list newest-first"
    - "SyncFreshnessPill rolls up activity sync state — pill turns amber on Deep-Sync-ingest failure or partial-success; hover reveals reason"
  artifacts:
    - path: "app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx"
      provides: "Row-list section with stacked avatars, inviter filter pill, segmented time-window control"
      contains: "listInvitations"
    - path: "components/layout/SyncFreshnessPill.tsx"
      provides: "Amber/Partial state surfaced when Deep-Sync-ingest fails or partial"
      contains: "Partial"
    - path: "server/routers/acc-sync.ts"
      provides: "getSyncFreshness extended to include deep-sync-ingest status + partial-success label"
      contains: "ingest"
  key_links:
    - from: "RecentlyAddedWidget row list"
      to: "trpc.accActivity.listInvitations"
      via: "useQuery with {windowDays, inviterFilter}"
      pattern: "listInvitations"
    - from: "Inviter pill"
      to: "widget state"
      via: "click inviter avatar → setInviterFilter(autodeskId) → query refetch with relaxed window"
      pattern: "inviterFilter"
    - from: "SyncFreshnessPill"
      to: "accSync.getSyncFreshness"
      via: "polled query reading SyncMeta + AccDataConnectorJob ingest state"
      pattern: "getSyncFreshness"
---

<objective>
Complete ACTV-04 (WHO-added-WHOM attribution) by extending RecentlyAddedWidget with a row-list section, stacked avatars, and inviter-filter pill. Also extend SyncFreshnessPill to surface activity-sync failure / partial-success — the only Phase 3 sync visibility (CONTEXT.md: "No new pill. Extend existing.").

Purpose: ACTV-04 contract requires "stacked avatars on each invitation row" — but the current widget is a 90-day calendar heatmap (RESEARCH Open Q1). Resolution: KEEP the heatmap as the at-a-glance view (it's the original product value) and ADD a row-list section below it with the 10 most recent invitations, "See all" → DashboardSidePanel.

Output: Widget shows heatmap (top) + segmented time-window control + filter pill (when active) + 10-row invitation list with stacked avatars (bottom); SyncFreshnessPill amber state surfaces Deep-Sync-ingest failures.
</objective>

<execution_context>
@C:/Users/luis.cortes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luis.cortes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-activity-pipeline/03-CONTEXT.md
@.planning/phases/03-activity-pipeline/03-RESEARCH.md
@.planning/phases/03-activity-pipeline/03-02-SUMMARY.md
@app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx
@components/layout/SyncFreshnessPill.tsx
@server/routers/acc-sync.ts

<interfaces>
Plan 02 contract (consumed here):
```typescript
trpc.accActivity.listInvitations.useQuery({
  windowDays: 7 | 30 | 90,    // default 30
  inviterFilter?: string,      // autodeskId of inviter — when present, time window is server-side relaxed
}) → Array<{
  inviteeEmail: string,
  inviteeName: string,
  inviteeAvatarUrl: string | null,
  inviterAutodeskId: string | null,    // null when unresolved
  inviterName: string | null,
  inviterAvatarUrl: string | null,
  otherInviters?: Array<{name: string, autodeskId: string, createdAt: Date}>,
  createdAt: Date,
  projectId: string,
  projectName: string,
}>
```

CONTEXT.md locks for this widget:
- Time window default: 30d (segmented control [7d|30d|90d])
- 10 rows visible; "See all" → DashboardSidePanel (a new kind, or reuse `kind:"userActivity"` filtered by `rawAction IN invitations`)
- Newest first
- Filter pill style: `Filtered by Jane Doe ×` — match existing FILT cascade chip styling (CONTEXT specific guidance)
- Inviter scope: avatars/text appear on invitation rows ONLY (other rows in the widget remain unchanged — but currently the widget is a heatmap; the row list IS the new "invitation rows" layer)
- Multi-inviter: primary + "(+N others)" suffix; hover popover shows full list newest-first
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend acc-sync router with ingest status + Partial label</name>
  <files>server/routers/acc-sync.ts</files>
  <action>Extend the existing `getSyncFreshness` procedure (or add a sibling — match existing router structure) to return Deep-Sync-ingest state alongside Quick-Sync state.

Behavior:
1. Read the most recent AccDataConnectorJob row (`orderBy: { createdAt: 'desc' }, take: 1`).
2. Compute an `ingestState` field with values: `"green" | "amber" | "running"`:
   - `running` → row exists with status `pending` or `running`.
   - `amber` → most recent COMPLETED job has `status: "failed"` OR (job succeeded but `rowsByFile` JSON shows zero rows in one of the two CSVs — "partial-success"). Determine partial-success heuristic: if `rowsByFile.admin === 0` XOR `rowsByFile.project === 0`, it's partial. If both > 0, full success.
   - `green` → most recent COMPLETED job has `status: "success"` with both CSVs > 0 rows.
3. Add a `partial: boolean` flag distinct from full-failure (CONTEXT lock).
4. Add `lastIngestAt: Date | null` and `lastIngestError: string | null` for hover detail.
5. Preserve all existing Quick-Sync fields — DO NOT break SYNC-03.
6. Keep `getActiveDeepSyncJob` procedure unchanged (it already exists for Phase 1's pill polling cadence).

Type the response with explicit zod output if the router uses output schemas (match the existing pattern).</action>
  <verify>
    <automated>npx tsc --noEmit server/routers/acc-sync.ts</automated>
  </verify>
  <done>getSyncFreshness returns {ingestState, partial, lastIngestAt, lastIngestError} in addition to existing fields; typechecks clean; existing SyncFreshnessPill callers still receive their fields.</done>
</task>

<task type="auto">
  <name>Task 2: SyncFreshnessPill amber/Partial state for activity ingest</name>
  <files>components/layout/SyncFreshnessPill.tsx</files>
  <action>Extend the pill's state computation:

1. Pull `ingestState`, `partial`, `lastIngestAt`, `lastIngestError` from the extended `getSyncFreshness` query (Task 1).
2. Color/label decision logic (preserve existing Quick-Sync semantics — add ingest as an additional gate):
   - If Quick-Sync is amber → keep current Quick-Sync amber behavior (existing logic wins for failure surfacing).
   - Else if `ingestState === 'amber'` → pill turns amber; label includes "Partial" if `partial === true`, else "Sync failed" (CONTEXT lock: "Partial" wording is the chosen amber-partial label).
   - Else if `ingestState === 'running'` → small spinner/badge alongside green (per existing pattern; do not turn amber).
   - Else green.
3. Hover tooltip: when amber and ingest is the cause, show `Activity sync: {Partial | Failed} — {lastIngestError ?? 'see logs'} (last attempt {formatDistanceToNow(lastIngestAt)} ago)`. Use the existing Tooltip primitive.
4. NO TOASTS, NO MODALS (CONTEXT lock). Visual surface is the pill only.
5. NO MANUAL TRIGGER UI anywhere (CONTEXT lock: "Manual trigger: No manual UI anywhere").
6. Preserve existing 15s-while-running / 5min-otherwise refetch cadence.

Do not invent new color tokens — re-use existing amber/green (CONTEXT specific: "preserve current amber/green semantics — do not invent new colors").</action>
  <verify>
    <automated>npx tsc --noEmit components/layout/SyncFreshnessPill.tsx</automated>
  </verify>
  <done>Pill turns amber when most recent ingest failed; hover reveals reason; "Partial" label appears for partial-success runs; no manual sync button anywhere in the pill.</done>
</task>

<task type="auto">
  <name>Task 3: RecentlyAddedWidget — row list, stacked avatars, inviter filter pill</name>
  <files>app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx</files>
  <action>Extend RecentlyAddedWidget with a row-list section below the existing 90-day heatmap. Plan-time resolution of RESEARCH Open Q1: KEEP heatmap as at-a-glance, ADD row list below — heatmap and row list both serve different scan modes (calendar density vs. recent-first).

UI structure (top-to-bottom inside widget):
1. **Header** (preserve current title + any chip): add segmented control `[7d | 30d | 90d]` (default 30d per CONTEXT lock). Selecting updates BOTH the heatmap window AND the row list's tRPC query.
2. **Filter pill** (conditional): when `inviterFilter` state is set, render a chip `Filtered by {inviterName} ×` styled per existing FILT cascade chip (grep for existing `<FilterChip>` or `<Badge variant="filter">`; reuse the same component). The `×` clears the filter.
3. **Heatmap** (preserve existing): respects the segmented control window. If `inviterFilter` is active, the heatmap MAY visually highlight only Jane's invitation cells, OR remain unchanged — CONTEXT.md doesn't require it; choose "remain unchanged" to minimize complexity and document the choice in SUMMARY.
4. **Row list section** (NEW):
   - Header: `Recent invitations` + small `See all →` link (links to DashboardSidePanel with a new kind `invitationsList` or reuse `userActivity` filtered to memberEvent — implement via clicking opening side panel with a `kind: "invitations"` payload; if extending the side panel is too much for this plan, keep "See all" as a no-op stub link with a `data-testid` and document follow-up).
   - Data source: `trpc.accActivity.listInvitations.useQuery({ windowDays: <selected>, inviterFilter })`. When `inviterFilter` is set, the server relaxes the window (Plan 02 contract); the widget doesn't need to change `windowDays` itself.
   - Render up to 10 rows, newest first.
   - **Row layout**: Stacked avatars on the left (invitee in front, ~28px; inviter behind, ~22px, with ~6px overlap — CONTEXT discretion on exact pixel values, follow existing avatar sizing tokens). Body text: `{inviterName} invited {inviteeName}` (line 1); `{projectName} · {formatDistanceToNow(createdAt)} ago` (line 2). If `otherInviters` has length > 0, append `(+{otherInviters.length} others)` after the inviter name; on hover, a popover shows the full inviter list newest-first with names + dates.
   - **Unresolved inviter** (inviterAutodeskId === null): render generic icon placeholder + text `Invited by Unknown` with a small warning icon (use existing `<AlertTriangle>` or equivalent from lucide-react — verify availability in current package.json; researcher noted Trello → Kanban replacement, suggesting curated icon set).
   - **Inviter click** (filter mode): clicking the inviter avatar OR the inviter name sets `inviterFilter` state to the inviter's autodeskId. This triggers the filter pill to render at the top and the listInvitations query to refetch with relaxed time window.
5. **Empty states**: if no invitations in window AND no inviterFilter → render existing widget empty state (preserve current behavior). If inviterFilter is set but returns zero rows → `No invitations by {inviterName}` + clear-filter button.

Performance: listInvitations returns at most 100 rows (Plan 02 contract); the widget slices to 10. The full list lives in the side panel via "See all".

DO NOT add cross-widget spotlight on row click (DEFERRED per CONTEXT: "Phase 5 may add"). Rows ARE clickable for inviter-filter behavior, but invitee-row clicks do NOT open side panels (the "See all" link is the panel entry point).</action>
  <verify>
    <automated>npx tsc --noEmit app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx && npm run lint -- app/(dashboard)/users/dashboard/widgets/RecentlyAddedWidget.tsx</automated>
  </verify>
  <done>Widget shows heatmap + segmented control + row list of 10 invitations with stacked avatars; clicking an inviter applies a filter pill that relaxes the time window; multi-inviter cases show '+N others' with hover detail; unresolved inviter shows 'Invited by Unknown' with warning icon.</done>
</task>

</tasks>

<verification>
- TypeScript typechecks across all modified files.
- Manual UAT (operator): navigate to dashboard; widget shows heatmap + new row list below; segmented control toggles between 7/30/90d and refetches; click an inviter avatar — filter pill appears, time window relaxes (older invitations from Jane appear); click `×` on pill — filter clears, default 30d list returns; unresolved inviter row shows warning icon; SyncFreshnessPill turns amber when most recent AccDataConnectorJob row has status=failed.
- ACTV-04 deliverable: WHO-added-WHOM is visible and inviter is filterable.
</verification>

<success_criteria>
- ACTV-04 fully implemented: stacked avatars, unresolved-state UI, inviter-click filter mode with pill + relaxed window, multi-inviter "+N others" with hover detail, scoped to invitation rows.
- SyncFreshnessPill amber/Partial wording surfaces Deep-Sync-ingest failure WITHOUT introducing a second pill (CONTEXT lock).
- No manual sync UI introduced anywhere.
- All existing pill semantics (Quick-Sync amber/green, 15s-while-running cadence) preserved.
- "See all" link in row list either opens the side panel or has a documented follow-up stub.
</success_criteria>

<output>
After completion, create `.planning/phases/03-activity-pipeline/03-04-SUMMARY.md` documenting:
- The exact avatar sizes / overlap chosen.
- Whether heatmap is visually filtered when inviterFilter is active (recommendation: no).
- The "See all" link target (real side panel handler vs. stub follow-up).
- Confirmation of ACTV-04 acceptance: invitation row visible, inviter resolvable to AccProjectMember, unresolved inviter logged in UnresolvedAttribution.
</output>
