---
milestone: 2.5
audited: 2026-05-06T00:00:00Z
status: passed
scores:
  requirements: 4/4
  phases: 1/1
  integration: ok
  flows: ok
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 02.5-acc-data-filter-refinement
    items:
      - "VERIFICATION.md is stale (2026-04-30) — predates plans 05/06 closure. Should be regenerated via gsd-verifier or treated as superseded by 02.5-05-SUMMARY.md / 02.5-06-SUMMARY.md."
      - "SUMMARY.md frontmatter `requirements_completed` field is empty for plans 01, 02, 04, 05, 06; only plan 03 lists [FILT-01, DATA-01]. Frontmatter coverage does not reflect actual REQ closure — body content does. Future GSD audits would benefit from populated frontmatter."
      - "TD-006 (Cosmos slider feel refinement) — REND-02 deferred from Phase 2; not in scope of 2.5 but still open."
      - "TD-007 (Canvas2D vestigial branch) — deferred from Phase 2; not in scope of 2.5 but still open."
      - "lib/server/acc-admin.ts ~line 47 — one-time `[02.5]` diagnostic log for companyRole/lastSignIn field-name confirmation. Remove after first ACC sync confirms field names."
notes_on_audit_method:
  - "VERIFICATION.md predates the work that closed its listed gaps. Audit relies on the SUMMARY.md narrative for plans 05 (FILT-01 take-1 fix validated via runtime trace) and 06 (UX polish — dropdown, ON/OFF text, count badge, empty-state) plus live MCP verification performed in this session."
  - "Cross-phase integration checker NOT spawned. Justification: Phase 2.5 integration surface is a single page (`/users` ACC Users Graph v1 tab) backed by two files (AccUsersGraph.tsx, graphRenderers.ts). End-to-end was exercised live via Chrome DevTools MCP in this same session: URL `?moff=insight,docs,build` → React filter state → nodeMatchesFilters → setVisibleIndices → GPU point-size buffer → visible count `14,891 of 23,559`. Each link in the chain produced direct evidence."
---

# v2.5 Milestone Audit — ACC Data + Filter Refinement

**Phase scope:** `02.5-acc-data-filter-refinement` (single-phase audit, decimal phase inserted into milestone v1.0)
**Audited:** 2026-05-06
**Status:** ✅ **passed**

---

## 1. Phase Scope

| # | Plan | Status | Requirements |
|---|------|--------|--------------|
| 1 | 02.5-01 | ✅ Complete | DATA-01 (data extraction) |
| 2 | 02.5-02 | ✅ Complete | FILT-01, FILT-02, FILT-03 (filter logic + 28 Vitest tests) |
| 3 | 02.5-03 | ✅ Complete | FILT-01, DATA-01 (renderer hide + side panel row) |
| 4 | 02.5-04 | ✅ Complete | FILT-02, FILT-03, DATA-01 (UI overhaul + human-verify gate) |
| 5 | 02.5-05 | ✅ Complete | FILT-01 (gap-closure: renderer hide validated 2026-05-06) |
| 6 | 02.5-06 | ✅ Complete | DATA-01, FILT-03, FILT-02 (UX polish closure) |

All 6 plans complete. Phase status from ROADMAP.md not yet ticked but should be marked `[x]` on milestone close.

---

## 2. Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Description | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | **Final Status** |
|--------|-------------|-----------------|---------------------|-----------------|------------------|
| DATA-01 | companyRole extracted, in side panel, usable as filter | partial→ closed (06) | listed in 03 only | `[x]` | **satisfied** |
| FILT-01 | Excluded user nodes fully hidden, edges hidden | failed → closed (05) | listed in 03 only | `[x]` | **satisfied** |
| FILT-02 | From/To date inputs with inclusive range matching | uncertain → closed (06) | not listed | `[x]` | **satisfied** |
| FILT-03 | One toggle per module, all-on default, exclude-list semantics | partial → closed (06) | not listed | `[x]` | **satisfied** |

**Cross-source resolution.** VERIFICATION.md predates plans 05 and 06 (its mtime is 2026-04-30; closure work was 2026-05-06). The "failed" / "partial" statuses there are obsolete. Authoritative evidence:

- **DATA-01:**
  - data plumbing — `lib/server/acc-admin.ts` extracts companyRole + lastSignIn (plan 01); `lib/acc/graphSnapshot.ts` propagates through the type chain; `BulkAccUser.companyRole` field added.
  - side panel — `AccUserSidePanel.tsx` renders Company Role row with "Unspecified" fallback (plan 03).
  - filter — `nodeMatchesFilters` companyRoles include-list (plan 02, Vitest-verified).
  - UX — `CompanyRoleFilter` is a dropdown popover with click-outside + Escape dismiss, verified live via MCP (plan 06).

- **FILT-01:**
  - take-1 fix in `graphRenderers.ts` (`draw()` preserve-on-nodeCount-change) and `AccUsersGraph.tsx` (renderer-ready re-apply) — commit `84e73ec`.
  - take-2 runtime validation — captured via Chrome DevTools MCP 2026-05-06: URL `?moff=insight,docs,build` produced `14,891 of 23,559 instances` and GPU point-size buffer dump `innerInputPointSizes[0..8]= 4,0,6,0,6,4,4,4` (zeros at filtered indices). All v2 instrumentation stripped (commit `c5af051`).

- **FILT-02:**
  - Date inputs render in the filter panel (plan 04).
  - Inclusive range logic in `nodeMatchesFilters` covered by Vitest (plan 02 — 4 of the 28 tests target dateFrom/dateTo).
  - Live MCP session this audit observed the panel rendering with From/To Date controls under "LAST ACTIVITY" (uid 18_166–18_186 in plan 06 verification snapshot).

- **FILT-03:**
  - Exclude-list semantic ("hide only when ALL of user's modules are disabled") in `nodeMatchesFilters` (plan 02, Vitest-verified).
  - Module toggle ON/OFF affordance — explicit ON/OFF text label, strikethrough on disabled label, gray-300 OFF track (plan 06).
  - Runtime trace 2026-05-06 confirmed pipeline: filter `disabledModules: [insight, docs, build]` → `visibleSetSize=14891` → `8,668` zeroed in GPU buffer.

**Orphan detection:** No REQ-IDs from REQUIREMENTS.md traceability are unaccounted for in this phase scope. All 4 phase REQ-IDs are claimed by ≥1 plan and verified by ≥1 evidence source.

---

## 3. ROADMAP Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each ACC user record contains companyRole; appears in side panel; usable as filter | ✅ | Side panel row + dropdown filter, runtime confirmed |
| 2 | When a filter excludes a user, that node is fully hidden; edges hidden | ✅ | 14,891/23,559 visible after URL-seeded filter; GPU buffer dump shows zeros at excluded indices |
| 3 | Date filter has from/to inputs and inclusively matches | ✅ | Inputs render; Vitest-covered logic; URL params `from=`/`to=` round-trip |
| 4 | Module filter renders on/off toggle per module; all-on shows all; toggle-off filters | ✅ | 9 module toggles render; toggle-3 produces 14,891 visible (correctly excluding only "exclusive-to-disabled" users per FILT-03 exclude-list semantic) |

All 4 ROADMAP success criteria pass.

---

## 4. Cross-Phase Integration

Phase 2.5 builds on Phase 2 (Cosmos GPU renderer) which builds on Phase 1 (foundation). Integration verified end-to-end in this session:

```
URL query params  (?moff=insight,docs,build)
  → readFiltersFromUrl  (AccUsersGraph.tsx:225)
  → useState filters seed  (AccUsersGraph.tsx:394)
  → useEffect [filters]  (AccUsersGraph.tsx:1191)
  → rebuildVisibleIndices → visibleIndexSetRef.current
  → cosmosRendererRef.current.setVisibleIndices(visibleSet)
  → CosmosGraphRenderer.setVisibleIndices  (graphRenderers.ts ~826)
  → Float32Array sizes (zero for excluded)
  → Cosmos.gl graph.setPointSizes(sizes)
  → graph.render()
  → GPU point-size buffer reflects visible count
```

Each link produced direct runtime evidence in the 2026-05-06 MCP session. No broken flows. No undeclared dependencies.

**Foundation handoff (Phase 1 → Phase 2 → 2.5):** verified previously by their own VERIFICATION.md files (already passed).

---

## 5. Tech Debt

| Phase | Item | Severity |
|-------|------|----------|
| 02.5 | VERIFICATION.md is stale (predates plans 05/06) — body still reads `gaps_found` | low (cosmetic) |
| 02.5 | SUMMARY frontmatter `requirements_completed` empty for 5 of 6 plans | low (audit traceability) |
| 02.5 | `lib/server/acc-admin.ts:~47` one-time diagnostic log for companyRole/lastSignIn field confirmation | trivial (remove after first sync) |
| 02 | TD-006 (REND-02): Cosmos slider feel refinement deferred — out of 2.5 scope | low (deferred, not blocking) |
| 02 | TD-007: Canvas2D vestigial branch — out of 2.5 scope | low (deferred, not blocking) |

None of the above blocks milestone close. The two phase-level items (stale VERIFICATION + empty frontmatter) are GSD bookkeeping; the diagnostic log is a one-line cleanup.

---

## 6. Audit Method Notes

- **VERIFICATION.md not regenerated.** The 2026-04-30 verification report is treated as superseded by `02.5-05-SUMMARY.md` and `02.5-06-SUMMARY.md`, both of which explicitly enumerate gap closure with concrete evidence. A fresh `gsd-verifier` pass would more rigorously update this artifact; recommended as a low-priority follow-up before the eventual milestone v1.0 close (when Phase 3 + 4 are also done).
- **Integration checker not spawned.** End-to-end exercised live in the same session via Chrome DevTools MCP against the live 23,559-node Hermosillo dataset. The integration surface is a single page backed by two files; running a separate agent would be ceremonial overhead with no incremental signal.

---

_Audited: 2026-05-06 by Claude (gsd-audit-milestone, single-phase scope `2.5`)._
