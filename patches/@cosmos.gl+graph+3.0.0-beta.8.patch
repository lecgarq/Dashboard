diff --git a/node_modules/@cosmos.gl/graph/dist/index.js b/node_modules/@cosmos.gl/graph/dist/index.js
index 6128933..5f74236 100644
--- a/node_modules/@cosmos.gl/graph/dist/index.js
+++ b/node_modules/@cosmos.gl/graph/dist/index.js
@@ -4266,8 +4266,9 @@ void main() {
 
   pointPosition.rg += pointVelocity.rg;
 
-  pointPosition.r = clamp(pointPosition.r, 0.0, spaceSize);
-  pointPosition.g = clamp(pointPosition.g, 0.0, spaceSize);
+  // [LECG patch] clamp removed — see patches/@cosmos.gl+graph+3.0.0-beta.8.patch
+  // pointPosition.r = clamp(pointPosition.r, 0.0, spaceSize);
+  // pointPosition.g = clamp(pointPosition.g, 0.0, spaceSize);
   
   fragColor = pointPosition;
 }`, vt = `#version 300 es
